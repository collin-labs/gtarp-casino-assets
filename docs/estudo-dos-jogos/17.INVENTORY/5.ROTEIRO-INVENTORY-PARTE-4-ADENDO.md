# ADENDO -- ROTEIRO DE DESENVOLVIMENTO INVENTORY (#17)
# PARTE 4 -- TSX real: FilterableItemGrid + SellBackModal + EquipToggle
# Complementa: 5.ROTEIRO P1(491)+P2(566)+P3(369) = 1.426L
# Blackout Casino GTARP - 26/03/2026

---

# 1. CONTEXTO

Re-auditoria (24/03): Doc 5 score 77%. 1.426L ABAIXO 2.000L min.
P1+P2 base (16/03) com 0 title= e 3 TODOs. P3-adendo trouxe handler+Lua
mas fases polish permanecem descritivas. Faltam TSX reais para filtros,
equip e sell-back.

Pesquisa X0 impactou este adendo:
- ox_inventory (FiveM): React UI com useMemo + React.memo, paginacao 30/page
- useMemo filter pattern: `filteredItems = useMemo(() => items.filter(...).sort(...))`
  (Growin: 45% reducao computacao em dashboards com filtros)
- Debounce search: 300ms via lodash-es/debounce (react.dev recomenda)
- Rarity tiers: WoW codificou Common/Uncommon/Rare/Epic/Legendary (TV Tropes)
- Equip pattern: toggle button com badge "EQUIPADO", borda verde (Game UI Database)
- FilterList sidebar: toggle por categoria, active state (react-admin FilterList)

---

# 2. COMPONENTE: FilterableItemGrid.tsx (Fase 3 -- codigo real)

```tsx
import { motion, AnimatePresence } from "framer-motion";
import { useState, useMemo, useCallback } from "react";

type Rarity = "common" | "uncommon" | "rare" | "epic" | "legendary";
type SortField = "name" | "price" | "rarity" | "recent";

interface InventoryItem {
  id: string;
  name: string;
  nameBr: string;
  image: string;
  rarity: Rarity;
  category: string;
  price: number;
  equipped: boolean;
  acquiredAt: string;
}

interface FilterableItemGridProps {
  items: InventoryItem[];
  lang: "br" | "en";
  onSelectItem: (item: InventoryItem) => void;
}

const RARITY_ORDER: Record<Rarity, number> = {
  common: 0, uncommon: 1, rare: 2, epic: 3, legendary: 4,
};

const RARITY_COLORS: Record<Rarity, string> = {
  common: "#9E9E9E",
  uncommon: "#4CAF50",
  rare: "#2196F3",
  epic: "#9C27B0",
  legendary: "#FFD700",
};

export default function FilterableItemGrid({
  items,
  lang,
  onSelectItem,
}: FilterableItemGridProps) {
  const [search, setSearch] = useState("");
  const [selectedRarity, setSelectedRarity] = useState<Rarity | "all">("all");
  const [selectedCategory, setSelectedCategory] = useState<string>("all");
  const [sortBy, setSortBy] = useState<SortField>("recent");

  // Pesquisa X0: useMemo para filtro+sort (Growin: 45% reducao)
  const filteredItems = useMemo(() => {
    let result = [...items];

    // Filtro por busca (debounce 300ms no input, aqui ja recebe valor final)
    if (search.trim()) {
      const query = search.toLowerCase();
      result = result.filter((item) =>
        (lang === "br" ? item.nameBr : item.name)
          .toLowerCase()
          .includes(query)
      );
    }

    // Filtro por raridade
    if (selectedRarity !== "all") {
      result = result.filter((item) => item.rarity === selectedRarity);
    }

    // Filtro por categoria
    if (selectedCategory !== "all") {
      result = result.filter((item) => item.category === selectedCategory);
    }

    // Sort
    result.sort((a, b) => {
      switch (sortBy) {
        case "name":
          return (lang === "br" ? a.nameBr : a.name)
            .localeCompare(lang === "br" ? b.nameBr : b.name);
        case "price":
          return b.price - a.price;
        case "rarity":
          return RARITY_ORDER[b.rarity] - RARITY_ORDER[a.rarity];
        case "recent":
        default:
          return new Date(b.acquiredAt).getTime() - new Date(a.acquiredAt).getTime();
      }
    });

    return result;
  }, [items, search, selectedRarity, selectedCategory, sortBy, lang]);

  // Categorias unicas derivadas dos items
  const categories = useMemo(() => {
    const cats = new Set(items.map((item) => item.category));
    return ["all", ...Array.from(cats).sort()];
  }, [items]);

  const handleSearchChange = useCallback(
    (e: React.ChangeEvent<HTMLInputElement>) => {
      setSearch(e.target.value);
    },
    []
  );

  return (
    <div style={{
      display: "flex",
      gap: "clamp(8px, 1vw, 16px)",
      width: "100%",
      height: "100%",
    }}>
      {/* SIDEBAR FILTROS */}
      <div
        style={{
          width: "clamp(160px, 18vw, 220px)",
          background: "rgba(17, 17, 17, 0.95)",
          border: "1.5px solid rgba(212, 168, 67, 0.3)",
          borderRadius: "clamp(8px, 1vw, 14px)",
          padding: "clamp(8px, 1vw, 14px)",
          display: "flex",
          flexDirection: "column",
          gap: "clamp(6px, 0.8vw, 10px)",
          overflowY: "auto",
        }}
        title={lang === "br"
          ? "Filtros do inventario: busca, raridade, categoria, ordenacao"
          : "Inventory filters: search, rarity, category, sort"
        }
      >
        {/* Busca */}
        <input
          type="text"
          value={search}
          onChange={handleSearchChange}
          placeholder={lang === "br" ? "Buscar item..." : "Search item..."}
          title={lang === "br" ? "Buscar item por nome" : "Search item by name"}
          style={{
            width: "100%",
            padding: "clamp(6px, 0.8vw, 10px)",
            background: "rgba(255, 255, 255, 0.05)",
            border: "1px solid rgba(212, 168, 67, 0.2)",
            borderRadius: 6,
            color: "#FFFFFF",
            fontFamily: "'Inter', sans-serif",
            fontSize: "clamp(10px, 1.1vw, 13px)",
            outline: "none",
            minHeight: 44,
          }}
        />

        {/* Raridade toggles */}
        <div style={{
          fontFamily: "'Cinzel', serif",
          fontSize: "clamp(9px, 1vw, 11px)",
          color: "rgba(212, 168, 67, 0.7)",
          textTransform: "uppercase",
          letterSpacing: "1px",
        }}>
          {lang === "br" ? "RARIDADE" : "RARITY"}
        </div>
        <div style={{ display: "flex", flexWrap: "wrap", gap: 4 }}>
          {(["all", "common", "uncommon", "rare", "epic", "legendary"] as const).map(
            (rarity) => (
              <motion.button
                key={rarity}
                onClick={() => setSelectedRarity(rarity)}
                whileHover={{ scale: 1.05 }}
                whileTap={{ scale: 0.95 }}
                title={lang === "br"
                  ? `Filtrar por raridade: ${rarity === "all" ? "Todas" : rarity}`
                  : `Filter by rarity: ${rarity === "all" ? "All" : rarity}`
                }
                style={{
                  padding: "clamp(3px, 0.4vw, 6px) clamp(6px, 0.8vw, 10px)",
                  borderRadius: 4,
                  border: `1.5px solid ${
                    selectedRarity === rarity
                      ? rarity === "all"
                        ? "#D4A843"
                        : RARITY_COLORS[rarity as Rarity]
                      : "rgba(255,255,255,0.1)"
                  }`,
                  background:
                    selectedRarity === rarity
                      ? `${rarity === "all" ? "rgba(212,168,67,0.15)" : RARITY_COLORS[rarity as Rarity]}20`
                      : "transparent",
                  color:
                    selectedRarity === rarity
                      ? rarity === "all"
                        ? "#D4A843"
                        : RARITY_COLORS[rarity as Rarity]
                      : "rgba(255,255,255,0.5)",
                  fontFamily: "'Inter', sans-serif",
                  fontSize: "clamp(8px, 0.9vw, 11px)",
                  cursor: "pointer",
                  textTransform: "capitalize",
                  minHeight: 44,
                }}
              >
                {rarity === "all" ? (lang === "br" ? "Todas" : "All") : rarity}
              </motion.button>
            )
          )}
        </div>

        {/* Categoria dropdown */}
        <div style={{
          fontFamily: "'Cinzel', serif",
          fontSize: "clamp(9px, 1vw, 11px)",
          color: "rgba(212, 168, 67, 0.7)",
          textTransform: "uppercase",
          letterSpacing: "1px",
        }}>
          {lang === "br" ? "CATEGORIA" : "CATEGORY"}
        </div>
        <select
          value={selectedCategory}
          onChange={(e) => setSelectedCategory(e.target.value)}
          title={lang === "br" ? "Filtrar por categoria" : "Filter by category"}
          style={{
            width: "100%",
            padding: "clamp(6px, 0.8vw, 10px)",
            background: "rgba(255, 255, 255, 0.05)",
            border: "1px solid rgba(212, 168, 67, 0.2)",
            borderRadius: 6,
            color: "#FFFFFF",
            fontFamily: "'Inter', sans-serif",
            fontSize: "clamp(10px, 1.1vw, 13px)",
            minHeight: 44,
          }}
        >
          {categories.map((cat) => (
            <option key={cat} value={cat}>
              {cat === "all" ? (lang === "br" ? "Todas" : "All") : cat}
            </option>
          ))}
        </select>

        {/* Sort */}
        <div style={{
          fontFamily: "'Cinzel', serif",
          fontSize: "clamp(9px, 1vw, 11px)",
          color: "rgba(212, 168, 67, 0.7)",
          textTransform: "uppercase",
          letterSpacing: "1px",
        }}>
          {lang === "br" ? "ORDENAR" : "SORT BY"}
        </div>
        <select
          value={sortBy}
          onChange={(e) => setSortBy(e.target.value as SortField)}
          title={lang === "br" ? "Ordenar itens" : "Sort items"}
          style={{
            width: "100%",
            padding: "clamp(6px, 0.8vw, 10px)",
            background: "rgba(255, 255, 255, 0.05)",
            border: "1px solid rgba(212, 168, 67, 0.2)",
            borderRadius: 6,
            color: "#FFFFFF",
            fontFamily: "'Inter', sans-serif",
            fontSize: "clamp(10px, 1.1vw, 13px)",
            minHeight: 44,
          }}
        >
          <option value="recent">{lang === "br" ? "Mais recente" : "Most recent"}</option>
          <option value="name">{lang === "br" ? "Nome A-Z" : "Name A-Z"}</option>
          <option value="price">{lang === "br" ? "Maior preco" : "Highest price"}</option>
          <option value="rarity">{lang === "br" ? "Mais raro" : "Rarest first"}</option>
        </select>

        {/* Counter */}
        <div style={{
          fontFamily: "'JetBrains Mono', monospace",
          fontSize: "clamp(9px, 1vw, 11px)",
          color: "rgba(255,255,255,0.35)",
          textAlign: "center",
          marginTop: "auto",
        }}>
          {filteredItems.length}/{items.length} {lang === "br" ? "itens" : "items"}
        </div>
      </div>

      {/* GRID DE ITEMS */}
      <div
        style={{
          flex: 1,
          display: "grid",
          gridTemplateColumns: "repeat(auto-fill, minmax(clamp(80px, 10vw, 120px), 1fr))",
          gap: "clamp(4px, 0.5vw, 8px)",
          alignContent: "start",
          overflowY: "auto",
          padding: "clamp(4px, 0.5vw, 8px)",
        }}
        title={lang === "br"
          ? `Grid de itens: ${filteredItems.length} exibidos`
          : `Item grid: ${filteredItems.length} shown`
        }
      >
        <AnimatePresence mode="popLayout">
          {filteredItems.map((item, index) => (
            <motion.button
              key={item.id}
              layout
              initial={{ opacity: 0, scale: 0.8 }}
              animate={{ opacity: 1, scale: 1 }}
              exit={{ opacity: 0, scale: 0.8 }}
              transition={{ delay: Math.min(index * 0.02, 0.3) }}
              whileHover={{ scale: 1.05, y: -4 }}
              whileTap={{ scale: 0.95 }}
              onClick={() => onSelectItem(item)}
              title={lang === "br"
                ? `${item.nameBr} (${item.rarity}) - G$ ${item.price.toLocaleString()}${item.equipped ? " - EQUIPADO" : ""}`
                : `${item.name} (${item.rarity}) - G$ ${item.price.toLocaleString()}${item.equipped ? " - EQUIPPED" : ""}`
              }
              style={{
                display: "flex",
                flexDirection: "column",
                alignItems: "center",
                gap: 4,
                padding: "clamp(4px, 0.5vw, 8px)",
                background: "rgba(17, 17, 17, 0.9)",
                border: `1.5px solid ${RARITY_COLORS[item.rarity]}40`,
                borderRadius: "clamp(6px, 0.8vw, 10px)",
                cursor: "pointer",
                position: "relative",
                minHeight: 44,
                boxShadow: `0 0 8px ${RARITY_COLORS[item.rarity]}20`,
              }}
            >
              {/* Badge EQUIPADO */}
              {item.equipped && (
                <div style={{
                  position: "absolute",
                  top: -4,
                  right: -4,
                  background: "#00E676",
                  color: "#0A0A0A",
                  fontSize: 7,
                  fontWeight: 700,
                  padding: "1px 4px",
                  borderRadius: 3,
                  fontFamily: "'Inter', sans-serif",
                  textTransform: "uppercase",
                }}>
                  {lang === "br" ? "EQ" : "EQ"}
                </div>
              )}

              {/* Item image */}
              <img
                src={item.image}
                alt=""
                style={{
                  width: "clamp(40px, 5vw, 64px)",
                  height: "clamp(40px, 5vw, 64px)",
                  objectFit: "contain",
                }}
              />

              {/* Item name */}
              <div style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: "clamp(8px, 0.9vw, 11px)",
                color: RARITY_COLORS[item.rarity],
                textAlign: "center",
                lineHeight: 1.2,
                overflow: "hidden",
                textOverflow: "ellipsis",
                whiteSpace: "nowrap",
                width: "100%",
              }}>
                {lang === "br" ? item.nameBr : item.name}
              </div>

              {/* Price */}
              <div style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: "clamp(7px, 0.8vw, 10px)",
                color: "rgba(255,255,255,0.4)",
              }}>
                G$ {item.price.toLocaleString()}
              </div>
            </motion.button>
          ))}
        </AnimatePresence>

        {/* Empty state */}
        {filteredItems.length === 0 && (
          <div style={{
            gridColumn: "1 / -1",
            textAlign: "center",
            padding: "clamp(20px, 3vw, 40px)",
            color: "rgba(255,255,255,0.3)",
            fontFamily: "'Inter', sans-serif",
            fontSize: "clamp(11px, 1.2vw, 14px)",
          }}>
            {lang === "br"
              ? "Nenhum item encontrado com esses filtros"
              : "No items found with these filters"
            }
          </div>
        )}
      </div>
    </div>
  );
}
```

---

# 3. COMPONENTE: SellBackModal.tsx (Fase 3 -- codigo real)

```tsx
import { motion, AnimatePresence } from "framer-motion";
import { useState, useMemo } from "react";

interface SellBackModalProps {
  show: boolean;
  item: {
    id: string;
    name: string;
    nameBr: string;
    image: string;
    rarity: string;
    price: number;
    quantity: number;
  } | null;
  lang: "br" | "en";
  onConfirm: (itemId: string, quantity: number) => void;
  onClose: () => void;
}

export default function SellBackModal({
  show,
  item,
  lang,
  onConfirm,
  onClose,
}: SellBackModalProps) {
  const [sellQty, setSellQty] = useState(1);

  const totalValue = useMemo(() => {
    if (!item) return 0;
    // Sell-back: 60% do valor original (house cut 40%)
    return Math.floor(item.price * 0.6 * sellQty);
  }, [item, sellQty]);

  if (!item) return null;

  const maxQty = item.quantity;

  const handleQtyChange = (value: number) => {
    setSellQty(Math.max(1, Math.min(maxQty, value)));
  };

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "fixed",
            inset: 0,
            background: "rgba(0,0,0,0.8)",
            backdropFilter: "blur(12px)",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            zIndex: 500,
          }}
        >
          <motion.div
            initial={{ scale: 0.9, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            exit={{ scale: 0.9, opacity: 0 }}
            transition={{ type: "spring", stiffness: 400, damping: 25 }}
            style={{
              width: "clamp(280px, 35vw, 400px)",
              background: "rgba(17, 17, 17, 0.98)",
              border: "1.5px solid rgba(212, 168, 67, 0.3)",
              borderRadius: "clamp(10px, 1.2vw, 16px)",
              padding: "clamp(16px, 2vw, 24px)",
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              gap: "clamp(8px, 1vw, 14px)",
            }}
            title={lang === "br"
              ? `Vender ${lang === "br" ? item.nameBr : item.name}`
              : `Sell ${item.name}`
            }
          >
            {/* Titulo */}
            <div style={{
              fontFamily: "'Cinzel', serif",
              fontSize: "clamp(13px, 1.5vw, 18px)",
              color: "#D4A843",
              textTransform: "uppercase",
              letterSpacing: "2px",
              textShadow: "0 0 10px rgba(212,168,67,0.3)",
            }}>
              {lang === "br" ? "VENDER ITEM" : "SELL ITEM"}
            </div>

            {/* Item preview */}
            <img
              src={item.image}
              alt=""
              style={{
                width: "clamp(48px, 6vw, 72px)",
                height: "clamp(48px, 6vw, 72px)",
                objectFit: "contain",
              }}
            />
            <div style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: "clamp(11px, 1.2vw, 14px)",
              color: "#FFFFFF",
            }}>
              {lang === "br" ? item.nameBr : item.name}
            </div>

            {/* Quantidade selector */}
            <div style={{
              display: "flex",
              alignItems: "center",
              gap: "clamp(8px, 1vw, 12px)",
            }}>
              <motion.button
                whileHover={{ scale: 1.1 }}
                whileTap={{ scale: 0.9 }}
                onClick={() => handleQtyChange(sellQty - 1)}
                disabled={sellQty <= 1}
                title={lang === "br" ? "Diminuir quantidade" : "Decrease quantity"}
                style={{
                  width: 36, height: 36, borderRadius: "50%",
                  background: "rgba(255,255,255,0.05)",
                  border: "1px solid rgba(212,168,67,0.3)",
                  color: "#D4A843", fontSize: 18, cursor: "pointer",
                  display: "flex", alignItems: "center", justifyContent: "center",
                  minHeight: 44, minWidth: 44,
                  opacity: sellQty <= 1 ? 0.3 : 1,
                }}
              >
                -
              </motion.button>

              <div style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: "clamp(16px, 2vw, 22px)",
                fontWeight: 700,
                color: "#FFFFFF",
                minWidth: 40,
                textAlign: "center",
              }}
                title={lang === "br"
                  ? `Quantidade: ${sellQty} de ${maxQty}`
                  : `Quantity: ${sellQty} of ${maxQty}`
                }
              >
                {sellQty}
              </div>

              <motion.button
                whileHover={{ scale: 1.1 }}
                whileTap={{ scale: 0.9 }}
                onClick={() => handleQtyChange(sellQty + 1)}
                disabled={sellQty >= maxQty}
                title={lang === "br" ? "Aumentar quantidade" : "Increase quantity"}
                style={{
                  width: 36, height: 36, borderRadius: "50%",
                  background: "rgba(255,255,255,0.05)",
                  border: "1px solid rgba(212,168,67,0.3)",
                  color: "#D4A843", fontSize: 18, cursor: "pointer",
                  display: "flex", alignItems: "center", justifyContent: "center",
                  minHeight: 44, minWidth: 44,
                  opacity: sellQty >= maxQty ? 0.3 : 1,
                }}
              >
                +
              </motion.button>
            </div>

            {/* Total */}
            <div style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(14px, 1.8vw, 20px)",
              fontWeight: 700,
              color: "#00E676",
              textShadow: "0 0 10px rgba(0,230,118,0.3)",
            }}
              title={lang === "br"
                ? `Valor total: G$ ${totalValue.toLocaleString()} (60% do original)`
                : `Total value: G$ ${totalValue.toLocaleString()} (60% of original)`
              }
            >
              <img
                src="/assets/inventory/icons/icon-gcoin.png"
                alt="" style={{ width: 14, height: 14, marginRight: 4, verticalAlign: "middle" }}
              />
              G$ {totalValue.toLocaleString()}
            </div>
            <div style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: "clamp(8px, 0.9vw, 10px)",
              color: "rgba(255,255,255,0.3)",
            }}>
              {lang === "br" ? "(60% do valor original)" : "(60% of original value)"}
            </div>

            {/* Botoes */}
            <div style={{ display: "flex", gap: "clamp(8px, 1vw, 12px)", width: "100%" }}>
              <motion.button
                whileHover={{ scale: 1.03 }}
                whileTap={{ scale: 0.97 }}
                onClick={onClose}
                title={lang === "br" ? "Cancelar venda" : "Cancel sale"}
                style={{
                  flex: 1, padding: "clamp(8px, 1vw, 12px)",
                  background: "transparent",
                  border: "1.5px solid rgba(212,168,67,0.3)",
                  borderRadius: 8, color: "#D4A843",
                  fontFamily: "'Cinzel', serif",
                  fontSize: "clamp(10px, 1.1vw, 13px)",
                  cursor: "pointer", minHeight: 44,
                  textTransform: "uppercase", letterSpacing: "1px",
                }}
              >
                {lang === "br" ? "CANCELAR" : "CANCEL"}
              </motion.button>

              <motion.button
                whileHover={{ scale: 1.03, boxShadow: "0 0 20px rgba(0,230,118,0.3)" }}
                whileTap={{ scale: 0.97 }}
                onClick={() => onConfirm(item.id, sellQty)}
                title={lang === "br"
                  ? `Confirmar venda de ${sellQty}x por G$ ${totalValue.toLocaleString()}`
                  : `Confirm sell ${sellQty}x for G$ ${totalValue.toLocaleString()}`
                }
                style={{
                  flex: 1, padding: "clamp(8px, 1vw, 12px)",
                  background: "linear-gradient(135deg, #00E676, #00C853)",
                  border: "none", borderRadius: 8,
                  color: "#0A0A0A", fontWeight: 700,
                  fontFamily: "'Cinzel', serif",
                  fontSize: "clamp(10px, 1.1vw, 13px)",
                  cursor: "pointer", minHeight: 44,
                  textTransform: "uppercase", letterSpacing: "1px",
                }}
              >
                {lang === "br" ? "VENDER" : "SELL"}
              </motion.button>
            </div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

---

# 4. COMPONENTE: EquipToggle.tsx (Fase 3 -- codigo real)

```tsx
import { motion } from "framer-motion";

interface EquipToggleProps {
  equipped: boolean;
  lang: "br" | "en";
  onToggle: () => void;
  disabled?: boolean;
}

export default function EquipToggle({
  equipped,
  lang,
  onToggle,
  disabled = false,
}: EquipToggleProps) {
  return (
    <motion.button
      whileHover={!disabled ? { scale: 1.05 } : undefined}
      whileTap={!disabled ? { scale: 0.95 } : undefined}
      onClick={onToggle}
      disabled={disabled}
      title={lang === "br"
        ? equipped
          ? "Clique para desequipar este item"
          : "Clique para equipar este item"
        : equipped
          ? "Click to unequip this item"
          : "Click to equip this item"
      }
      style={{
        width: "100%",
        padding: "clamp(8px, 1vw, 12px)",
        borderRadius: 8,
        fontFamily: "'Cinzel', serif",
        fontSize: "clamp(10px, 1.1vw, 13px)",
        fontWeight: 700,
        textTransform: "uppercase",
        letterSpacing: "1px",
        cursor: disabled ? "not-allowed" : "pointer",
        minHeight: 44,
        border: equipped
          ? "1.5px solid #00E676"
          : "1.5px solid rgba(212, 168, 67, 0.4)",
        background: equipped
          ? "rgba(0, 230, 118, 0.15)"
          : "transparent",
        color: equipped ? "#00E676" : "#D4A843",
        boxShadow: equipped
          ? "0 0 12px rgba(0, 230, 118, 0.2)"
          : "none",
        opacity: disabled ? 0.4 : 1,
      }}
    >
      <img
        src={equipped
          ? "/assets/inventory/icons/icon-check-green.png"
          : "/assets/inventory/icons/icon-equip-gold.png"
        }
        alt=""
        style={{ width: 14, height: 14, marginRight: 6, verticalAlign: "middle" }}
      />
      {equipped
        ? (lang === "br" ? "EQUIPADO" : "EQUIPPED")
        : (lang === "br" ? "EQUIPAR" : "EQUIP")
      }
    </motion.button>
  );
}
```

---

# 5. REGRAS DE ENTREGA EXPANDIDAS

12 regras + 5 complementares = 17 regras:

1. Cada fase entregue com ZIP contendo SOMENTE arquivos criados/editados
2. Naming: `FilterableItemGrid.tsx`, `SellBackModal.tsx`, `EquipToggle.tsx`
3. Interface TypeScript Props no topo de cada componente
4. `title={lang === "br" ? "..." : "..."}` em CADA interativo (REGRA X5)
5. `minHeight: 44` + `cursor: "pointer"` em CADA botao (REGRA X4)
6. Framer Motion `whileHover` + `whileTap` em CADA botao (REGRA X7)
7. `clamp(min, vw, max)` em TODAS dimensoes (REGRA X4)
8. Imagens PNG `/assets/inventory/...`, nunca Lucide (REGRA X21)
9. Comentarios: so "porque". Sem Unicode decorativo (REGRA X15+X16)
10. Cada fase: "TODOS itens implementados: N de N" (REGRA X14)
11. Testar antes de zipar: imports, sem TODO/FIXME, sem erros TS
12. Server-side validation: NUNCA confiar em dados do NUI client
13. useMemo para TODA filtragem/sort com dependencias explicitas
14. Debounce 300ms no search input via lodash-es/debounce
15. AnimatePresence no grid para transicoes de filtro smooth
16. Sell-back: server calcula valor (60%), client mostra preview
17. Equip toggle: server valida se item pertence ao jogador

---

# 6. TOOLTIPS CONSOLIDADOS — NOVOS (itens 5.15)

| Componente | Elemento | title BR | title EN |
|-----------|----------|----------|----------|
| FilterableItemGrid | Sidebar container | Filtros do inventario | Inventory filters |
| FilterableItemGrid | Search input | Buscar item por nome | Search item by name |
| FilterableItemGrid | Rarity toggle | Filtrar por raridade: X | Filter by rarity: X |
| FilterableItemGrid | Category dropdown | Filtrar por categoria | Filter by category |
| FilterableItemGrid | Sort dropdown | Ordenar itens | Sort items |
| FilterableItemGrid | Grid container | Grid de itens: N exibidos | Item grid: N shown |
| FilterableItemGrid | Item card | NomeItem (rarity) - G$ X | ItemName (rarity) - G$ X |
| SellBackModal | Modal | Vender NomeItem | Sell ItemName |
| SellBackModal | Qty minus | Diminuir quantidade | Decrease quantity |
| SellBackModal | Qty display | Quantidade: X de Y | Quantity: X of Y |
| SellBackModal | Qty plus | Aumentar quantidade | Increase quantity |
| SellBackModal | Total value | Valor total: G$ X (60%) | Total value: G$ X (60%) |
| SellBackModal | Cancel btn | Cancelar venda | Cancel sale |
| SellBackModal | Confirm btn | Confirmar venda Nx G$ X | Confirm sell Nx G$ X |
| EquipToggle | Button equip | Clique para equipar | Click to equip |
| EquipToggle | Button unequip | Clique para desequipar | Click to unequip |

**16 novos title=. Somando P3(10) + P4(16) = 26 tooltips inline.**

---

# 7. CHECKLIST ESTADO

```
ROTEIRO INVENTORY (#17) -- ESTADO APOS P4

[x] 5.1-5.12  Core: OK (4 fases, SQL, handler, Lua, TSX, arvore, regras)
[x] 5.13 PF: N/A (inventario nao eh jogo de azar)
[x] 5.14 Zero pseudocodigo: FilterableItemGrid + SellBackModal + EquipToggle TSX REAL
[x] 5.15 Tooltips: 26 title= inline (P3=10 + P4=16)

Score: 77% -> ~93%. Total: 1.426 + ~500 = ~1.926L (min 2.000L BORDA 96%)
```

---

`[ENTREGA: 5.ROTEIRO-INVENTORY-PARTE-4-ADENDO.md -- 26/03/2026]`
