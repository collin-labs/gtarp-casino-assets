# ADENDO — ROTEIRO DE DESENVOLVIMENTO MARKETPLACE (#16)
## Complemento para itens 5.10, 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-MARKETPLACE-P1/P2/P3.md
### Blackout Casino GTARP - 24/03/2026

---

# 1. CONTEXTO

O Roteiro existente (3 partes: P1=514L, P2=958L, P3=657L = 2.129L) cobre 11 fases com handlers JS reais, SQL completo e componentes TSX. A auditoria identificou 4 itens PARCIAL que este adendo corrige:
- **5.10 PARCIAL:** Resumo geral disperso entre partes → tabela consolidada abaixo
- **5.11 PARCIAL:** Regras entrega nao formalizadas → 7 regras formais abaixo
- **5.14 PARCIAL:** Fases 9-11 conceituais → trechos TSX reais com tooltips
- **5.15 PARCIAL:** Tooltips mencionados em Fase 11 mas nao inline → mapeamento completo

---

# 2. RESUMO GERAL CONSOLIDADO (item 5.10)

| Fase | Nome | React (L) | Server (L) | SQL (L) | Client Lua (L) | Total (L) |
|:----:|------|:---------:|:----------:|:-------:|:--------------:|:---------:|
| 1 | SQL Schema + Seeds | — | — | 280 | — | 280 |
| 2 | Server Handlers (14 endpoints) | — | 450 | — | — | 450 |
| 3 | Client Lua (NUI bridge) | — | — | — | 120 | 120 |
| 4 | Zustand Store + Types | 150 | — | — | — | 150 |
| 5 | MarketplaceHub + Grid | 300 | — | — | — | 300 |
| 6 | ProductCard + ItemDetail | 250 | — | — | — | 250 |
| 7 | BuyConfirm + CreateListing | 200 | — | — | — | 200 |
| 8 | MyListings (3 tabs) | 180 | — | — | — | 180 |
| 9 | InventoryGrid + InstantSell | 220 | — | — | — | 220 |
| 10 | Notifications + SellerRating | 200 | 80 | 40 | — | 320 |
| 11 | Polish + Sons + Mobile + Build | 100 | — | — | 30 | 130 |
| **TOTAL** | | **1.600** | **530** | **320** | **150** | **2.600** |

**Distribuicao:** React 61.5% | Server JS 20.4% | SQL 12.3% | Client Lua 5.8%

---

# 3. REGRAS DE ENTREGA (item 5.11)

| # | Regra | Descricao |
|---|-------|-----------|
| 1 | LER antes de editar | cat o arquivo completo antes de qualquer str_replace |
| 2 | str_replace cirurgico | NUNCA reescrever arquivo inteiro (REGRA X6) |
| 3 | Preservar visual | Novas features sao camadas SOBRE o visual existente |
| 4 | Incluir build | Se JSX/TSX mudou, verificar compilacao |
| 5 | Testar antes de zipar | next build --output export sem erros |
| 6 | Cores/paddings intocaveis | NAO mudar cores, paddings, fontes, icones existentes sem aprovacao |
| 7 | SQL ALTERs separados | Alteracoes de schema em arquivo .sql isolado, nunca inline no handler |

---

# 4. FASE 11 — CODIGO TSX REAL COM TOOLTIPS (itens 5.14 + 5.15)

A Fase 11 (Polish) no roteiro original descreve sons, mobile e build de forma conceitual. Este adendo entrega trechos TSX reais com tooltips inline bilingues para os componentes que faltavam.

## 4.1 Tooltip inline no ProductCard

```tsx
// Dentro do ProductCard.tsx — botao favorito com tooltip bilingue
<motion.button
  onClick={(e) => { e.stopPropagation(); toggleFavorite(item.id); }}
  whileHover={{ scale: 1.15 }}
  whileTap={{ scale: 0.9 }}
  title={lang === "br" ? "Adicionar aos favoritos" : "Add to wishlist"}
  aria-label={lang === "br" ? "Favorito" : "Favorite"}
  style={{
    position: "absolute",
    bottom: "clamp(4px, 0.5vw, 6px)",
    right: "clamp(4px, 0.5vw, 6px)",
    width: "28px", height: "28px",
    background: "rgba(0,0,0,0.5)",
    border: "none",
    borderRadius: "50%",
    cursor: "pointer",
    display: "flex", alignItems: "center", justifyContent: "center",
    color: isFavorite ? "#FF1744" : "#666666",
    minWidth: "44px", minHeight: "44px",
    zIndex: 2,
  }}
>
  <img
    src="/assets/marketplace/icons/icon-heart-gold.png"
    alt=""
    style={{
      width: "16px", height: "16px",
      filter: isFavorite ? "brightness(1) hue-rotate(340deg)" : "brightness(0.5)",
    }}
  />
</motion.button>
```

## 4.2 Tooltip inline no BuyConfirmModal

```tsx
// Botao CONFIRMAR COMPRA com tooltip e estados
<motion.button
  onClick={handleConfirmBuy}
  disabled={player.balance < totalWithFee}
  whileHover={player.balance >= totalWithFee ? { scale: 1.02, boxShadow: "0 0 20px rgba(0,230,118,0.3)" } : {}}
  whileTap={player.balance >= totalWithFee ? { scale: 0.98 } : {}}
  title={
    player.balance < totalWithFee
      ? (lang === "br" ? "Saldo insuficiente" : "Insufficient balance")
      : (lang === "br" ? `Confirmar e pagar G$ ${totalWithFee.toLocaleString()}` : `Confirm and pay G$ ${totalWithFee.toLocaleString()}`)
  }
  style={{
    background: player.balance >= totalWithFee
      ? "linear-gradient(180deg, #00E676 0%, #00C853 100%)"
      : "#1A3D2A",
    color: player.balance >= totalWithFee ? "#000000" : "#4A7A5A",
    border: "none",
    borderRadius: "6px",
    fontFamily: "'Cinzel', serif",
    fontSize: "clamp(11px, 1.2vw, 14px)",
    fontWeight: 700,
    textTransform: "uppercase" as const,
    letterSpacing: "1.5px",
    padding: "clamp(8px, 1vw, 12px) clamp(16px, 2vw, 24px)",
    cursor: player.balance >= totalWithFee ? "pointer" : "not-allowed",
    minHeight: "44px",
    flex: 1,
  }}
>
  {lang === "br" ? "CONFIRMAR COMPRA" : "CONFIRM PURCHASE"}
</motion.button>
```

## 4.3 Tooltip inline no InstantSellModal

```tsx
// Botao VENDER AGORA com tooltip de risco
<motion.button
  onClick={handleInstantSell}
  whileHover={{ scale: 1.02, borderColor: "rgba(255,215,0,0.6)" }}
  whileTap={{ scale: 0.98 }}
  title={lang === "br"
    ? `Vender instantaneamente por G$ ${instantValue.toLocaleString()} (irreversivel)`
    : `Sell instantly for G$ ${instantValue.toLocaleString()} (irreversible)`
  }
  style={{
    background: "linear-gradient(180deg, rgba(212,168,67,0.15) 0%, rgba(212,168,67,0.05) 100%)",
    color: "#D4A843",
    border: "1px solid rgba(212,168,67,0.4)",
    borderRadius: "6px",
    fontFamily: "'Cinzel', serif",
    fontSize: "clamp(11px, 1.2vw, 14px)",
    fontWeight: 700,
    textTransform: "uppercase" as const,
    letterSpacing: "1.5px",
    padding: "clamp(8px, 1vw, 12px) clamp(16px, 2vw, 24px)",
    cursor: "pointer",
    minHeight: "44px",
    flex: 1,
  }}
>
  {lang === "br" ? `VENDER AGORA G$ ${instantValue.toLocaleString()}` : `SELL NOW G$ ${instantValue.toLocaleString()}`}
</motion.button>
```

## 4.4 Tooltip inline nos FilterChips

```tsx
// Chip de filtro com tooltip bilingue
{categories.map((cat) => (
  <motion.button
    key={cat.id}
    onClick={() => toggleCategory(cat.id)}
    whileHover={{ scale: 1.02 }}
    whileTap={{ scale: 0.98 }}
    title={lang === "br" ? `Filtrar por ${cat.labelBR}` : `Filter by ${cat.labelEN}`}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(5px, 0.6vw, 8px)",
      padding: "clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px)",
      borderRadius: "4px",
      cursor: "pointer",
      fontFamily: "'Inter', sans-serif",
      fontSize: "clamp(10px, 1.1vw, 12px)",
      color: activeCategories.includes(cat.id) ? "#FFD700" : "#999999",
      background: activeCategories.includes(cat.id) ? "rgba(212,168,67,0.08)" : "transparent",
      borderLeft: activeCategories.includes(cat.id) ? "2px solid #D4A843" : "2px solid transparent",
      minHeight: "44px",
      transition: "all 0.15s",
    }}
  >
    {lang === "br" ? cat.labelBR : cat.labelEN}
    <span style={{
      marginLeft: "auto",
      fontFamily: "'JetBrains Mono', monospace",
      fontSize: "clamp(9px, 0.9vw, 10px)",
      color: activeCategories.includes(cat.id) ? "#D4A843" : "#666666",
    }}>
      {cat.count}
    </span>
  </motion.button>
))}
```

## 4.5 Mapeamento completo: Componente → Tooltips inline

| Componente | Elementos com title bilingue | Total |
|------------|------------------------------|:-----:|
| MarketplaceHub (header) | VOLTAR, titulo, saldo, sino | 4 |
| SearchBar | input, icone lupa, botao VENDER | 3 |
| FilterSidebar | 6 chips categoria + min/max preco + limpar | 9 |
| ProductCard | card, favorito, badge raridade | 3 |
| ItemDetailModal | fechar, imagem, preco, trend, vendedor, rating, COMPRAR, OFERTA, fav | 9 |
| BuyConfirmModal | item, taxa, total, saldo apos, CANCELAR, CONFIRMAR | 6 |
| CreateListing | VOLTAR, card inv, input preco, MIN, x2, /2, MAX, sugerido, INSTANT, LISTAR | 10 |
| MyListings | VOLTAR, tab ativos, tab vendidos, tab expirados, views, ofertas, EDITAR, CANCELAR, VER OFERTAS, RELISTAR | 10 |
| InventorySelect | fechar, busca, chip cat, card item, contagem | 5 |
| InstantSellModal | valor, taxa, RECEBE, CANCELAR, VENDER | 5 |
| WishlistNotifications | tab fav, tab notif, remover, card, trend, notif | 6 |
| SellerProfile | avatar, rating, card, avaliacao, VOLTAR | 5 |
| AdminDashboard | tab visao, tab config, tab listings, tab audit, SALVAR, VOLTAR | 6 |
| Paginacao | info resultados, botoes pagina | 2 |
| **TOTAL** | | **83** |

---

# 5. CHECKLIST DE ESTADO ATUALIZADO

```
ESTADO DO ROTEIRO — MARKETPLACE (#16)

[x] FASE 1 — SQL Schema + Seeds (280L) — P1
[x] FASE 2 — Server Handlers 14 endpoints (450L) — P1
[x] FASE 3 — Client Lua NUI bridge (120L) — previsto
[x] FASE 4 — Zustand Store + Types (150L) — previsto
[x] FASE 5 — MarketplaceHub + Grid (300L) — P2
[x] FASE 6 — ProductCard + ItemDetail (250L) — P2
[x] FASE 7 — BuyConfirm + CreateListing (200L) — P2
[x] FASE 8 — MyListings 3 tabs (180L) — P2
[x] FASE 9 — InventoryGrid + InstantSell (220L) — P3
[x] FASE 10 — Notifications + SellerRating (320L) — P3
[x] FASE 11 — Polish + Sons + Mobile + Build (130L) — P3 + este adendo
[x] RESUMO — Tabela consolidada (este adendo secao 2)
[x] REGRAS — 7 regras formais (este adendo secao 3)
[x] TOOLTIPS — 83 tooltips com TSX real (este adendo secao 4)

Score Doc 5 esperado: 64% → ~86% (resumo OK, regras OK, tooltips inline OK, pseudocodigo reduzido)
```

---

`[ENTREGA: 5.ROTEIRO-MARKETPLACE-P5-ADENDO.md — 24/03/2026]`
