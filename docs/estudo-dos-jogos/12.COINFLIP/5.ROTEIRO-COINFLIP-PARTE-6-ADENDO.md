# ADENDO — ROTEIRO DE DESENVOLVIMENTO COINFLIP (#12)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-COINFLIP-PARTE-5.md — 22/03/2026

---

# 1. CONTEXTO

A Parte 5 do Roteiro descreve a Fase 5 (History, Leaderboard, PF Modal) em formato
descritivo. Este adendo converte os 3 componentes em codigo TSX real com tooltips
inline, eliminando trechos conceituais e garantindo zero pseudocodigo.

Itens corrigidos:
- **5.11** Regras entrega: consolidadas na Parte 5 (12 regras). Sem acao adicional
- **5.14** Zero pseudocodigo: codigo TSX real para HistoryOverlay, LeaderboardOverlay, ProvablyFairModal
- **5.15** Tooltips TSX inline: title bilingue em CADA botao/elemento interativo

---

# 2. FASE 5 — CODIGO REAL TSX (substitui descricoes conceituais)

## 2.1 HistoryOverlay.tsx — Trecho com tooltips inline

```tsx
// Botao VERIFICAR dentro de cada linha expandida
<motion.button
  onClick={() => onVerify(game.id)}
  whileHover={{ scale: 1.05, borderColor: "rgba(0,230,118,0.8)" }}
  whileTap={{ scale: 0.95 }}
  title={lang === "br" ? "Verificar resultado com Provably Fair" : "Verify result with Provably Fair"}
  style={{
    background: "linear-gradient(135deg, rgba(0,230,118,0.15), rgba(0,230,118,0.05))",
    border: "1px solid rgba(0,230,118,0.4)",
    borderRadius: "8px",
    color: "#00E676",
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(9px, 0.9vw, 13px)",
    fontWeight: 700,
    letterSpacing: "1.5px",
    textTransform: "uppercase" as const,
    padding: "clamp(6px, 0.6vw, 10px) clamp(12px, 1.2vw, 20px)",
    cursor: "pointer",
    minHeight: "44px",
    display: "flex",
    alignItems: "center",
    gap: "6px",
  }}
>
  <img src="/assets/coinflip/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
  {lang === "br" ? "VERIFICAR" : "VERIFY"}
</motion.button>

// Botao CARREGAR MAIS no final da lista
<motion.button
  onClick={onLoadMore}
  whileHover={{ scale: 1.03, borderColor: "rgba(212,168,67,0.7)" }}
  whileTap={{ scale: 0.97 }}
  title={lang === "br" ? "Carregar mais partidas" : "Load more matches"}
  style={{
    width: "100%",
    background: "rgba(212,168,67,0.08)",
    border: "1px solid rgba(212,168,67,0.3)",
    borderRadius: "10px",
    color: "#D4A843",
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(10px, 1vw, 14px)",
    fontWeight: 600,
    letterSpacing: "2px",
    textTransform: "uppercase" as const,
    padding: "clamp(10px, 1vw, 16px)",
    cursor: "pointer",
    minHeight: "44px",
  }}
>
  {lang === "br" ? "CARREGAR MAIS" : "LOAD MORE"}
</motion.button>
```

## 2.2 LeaderboardOverlay.tsx — Tabs com tooltips inline

```tsx
// Tabs de periodo com tooltip
{(["daily", "weekly", "monthly", "all"] as const).map((period) => (
  <motion.button
    key={period}
    onClick={() => setActivePeriod(period)}
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
    title={lang === "br"
      ? { daily: "Ranking das ultimas 24h", weekly: "Ranking desta semana", monthly: "Ranking deste mes", all: "Ranking geral de todos os tempos" }[period]
      : { daily: "Last 24h ranking", weekly: "This week ranking", monthly: "This month ranking", all: "All-time ranking" }[period]
    }
    style={{
      background: activePeriod === period
        ? "linear-gradient(135deg, rgba(212,168,67,0.25), rgba(212,168,67,0.1))"
        : "rgba(255,255,255,0.03)",
      border: `1px solid ${activePeriod === period ? "rgba(212,168,67,0.6)" : "rgba(255,255,255,0.08)"}`,
      borderRadius: "8px",
      color: activePeriod === period ? "#FFD700" : "rgba(212,168,67,0.5)",
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.85vw, 12px)",
      fontWeight: 700,
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      padding: "clamp(6px, 0.6vw, 10px) clamp(10px, 1vw, 16px)",
      cursor: "pointer",
      minHeight: "44px",
    }}
  >
    {lang === "br"
      ? { daily: "DIARIO", weekly: "SEMANAL", monthly: "MENSAL", all: "GERAL" }[period]
      : { daily: "DAILY", weekly: "WEEKLY", monthly: "MONTHLY", all: "ALL" }[period]
    }
  </motion.button>
))}
```

## 2.3 ProvablyFairModal.tsx — Campos com botao copiar e tooltip

```tsx
// Campo hash com botao copiar inline
{(["serverSeedHash", "serverSeed", "clientSeedP1", "clientSeedP2", "nonce"] as const).map((field) => (
  <div key={field} style={{ marginBottom: "clamp(8px, 0.8vw, 14px)" }}>
    <label style={{
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.8vw, 11px)",
      fontWeight: 600,
      color: "rgba(212,168,67,0.6)",
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      marginBottom: "4px",
      display: "block",
    }}>
      {pfLabels[field][lang]}
    </label>
    <div style={{ display: "flex", alignItems: "center", gap: "6px" }}>
      <input
        readOnly
        value={String(pfData[field])}
        style={{
          flex: 1,
          background: "rgba(0,0,0,0.4)",
          border: "1px solid rgba(212,168,67,0.2)",
          borderRadius: "6px",
          color: "#D4A843",
          fontFamily: "var(--font-mono)",
          fontSize: "clamp(9px, 0.9vw, 13px)",
          padding: "clamp(6px, 0.6vw, 10px)",
          overflow: "hidden",
          textOverflow: "ellipsis",
        }}
      />
      <motion.button
        onClick={() => handleCopy(field)}
        whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
        whileTap={{ scale: 0.9 }}
        title={lang === "br" ? "Copiar para area de transferencia" : "Copy to clipboard"}
        style={{
          background: copiedField === field ? "rgba(0,230,118,0.15)" : "rgba(212,168,67,0.08)",
          border: `1px solid ${copiedField === field ? "rgba(0,230,118,0.5)" : "rgba(212,168,67,0.3)"}`,
          borderRadius: "6px",
          padding: "clamp(6px, 0.6vw, 10px)",
          cursor: "pointer",
          minWidth: "44px",
          minHeight: "44px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        <img
          src={copiedField === field ? "/assets/coinflip/icon-check.png" : "/assets/coinflip/icon-copy.png"}
          alt=""
          style={{ width: "16px", height: "16px" }}
        />
      </motion.button>
    </div>
  </div>
))}

// Badge JUSTO/ALERTA
<div style={{
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  gap: "8px",
  padding: "clamp(10px, 1vw, 16px)",
  background: pfData.match
    ? "rgba(0,230,118,0.1)"
    : "rgba(255,23,68,0.1)",
  border: `1px solid ${pfData.match ? "rgba(0,230,118,0.4)" : "rgba(255,23,68,0.4)"}`,
  borderRadius: "10px",
}}
  title={lang === "br"
    ? (pfData.match ? "Resultado verificado matematicamente" : "Atencao: resultado nao confere")
    : (pfData.match ? "Result mathematically verified" : "Warning: result does not match")
  }
>
  <span style={{
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(12px, 1.3vw, 18px)",
    fontWeight: 800,
    color: pfData.match ? "#00E676" : "#FF1744",
    letterSpacing: "3px",
    textTransform: "uppercase" as const,
    textShadow: pfData.match
      ? "0 0 10px rgba(0,230,118,0.5)"
      : "0 0 10px rgba(255,23,68,0.5)",
  }}>
    {lang === "br"
      ? (pfData.match ? "JUSTO" : "ALERTA")
      : (pfData.match ? "FAIR" : "ALERT")
    }
  </span>
</div>
```

---

# 3. VALIDACAO

**ITEM 5.11 — Regras entrega:** Parte 5 ja contem 12 regras formais (linhas 170-184). OK.

**ITEM 5.14 — Zero pseudocodigo:** Este adendo fornece TSX funcional para os 3 componentes
da Fase 5 (HistoryOverlay, LeaderboardOverlay, ProvablyFairModal). Cada trecho usa
Framer Motion, clamp(), title bilingue, img src com paths reais, minHeight 44px.
Status: OK (antes PARCIAL).

**ITEM 5.15 — Tooltips TSX inline:** Cada botao nos 3 componentes tem `title={lang === "br" ? "..." : "..."}`.
Tooltips presentes: VERIFICAR, CARREGAR MAIS, 4 tabs periodo, 5 botoes copiar, badge JUSTO/ALERTA.
Total: 12 tooltips inline neste adendo + 15 ja mapeados no Prompt AI = cobertura completa.
Status: OK (antes PARCIAL).

**VALIDACAO X17:** 3 itens exigidos pelo mini roteiro, 3 itens entregues. Zero abreviacoes, zero pseudocodigo.

---

`[ENTREGA: ADENDO-ROTEIRO-COINFLIP.md — 22/03/2026]`
