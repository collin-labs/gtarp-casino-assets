# ADENDO DESIGN SYSTEM — COINFLIP (#12)
## PARTE 2 DE 4 — Componentes CSS Real (1.9-1.16)
### Blackout Casino GTARP - 20/03/2026

---

## 1.9 Modal Criar Sala — Componentes Internos (DS P2 §9.1 + §8.8)

```css
/* COPIAR EXATAMENTE — input de aposta (DS P2 §9.1 input de aposta) */
.coinflip-modal__bet-input {
  width: 100%;
  background: rgba(10,10,10,0.8);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: clamp(6px, 0.8vw, 10px);
  padding: clamp(12px, 1.5vw, 20px);
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(20px, 2.5vw, 32px);
  font-weight: 700;
  color: #00E676;
  text-align: center;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
  outline: none;
  transition: all 0.2s ease;
}
.coinflip-modal__bet-input:focus {
  border-color: rgba(0,230,118,0.4);
  box-shadow: 0 0 12px rgba(0,230,118,0.15);
}
.coinflip-modal__bet-input--error {
  border-color: #FF1744;
  box-shadow: 0 0 12px rgba(255,23,68,0.15);
  color: #FF1744;
}
.coinflip-modal__bet-input::placeholder {
  color: #666666;
  text-shadow: none;
}

/* COPIAR — container de shortcuts (DS P2 §8.8 botoes atalho) */
.coinflip-modal__shortcuts {
  display: flex;
  gap: clamp(6px, 0.8vw, 10px);
  justify-content: center;
  margin-top: clamp(8px, 1vw, 12px);
}

/* COPIAR — shortcut button individual */
.coinflip-modal__shortcut {
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 1.5vw, 18px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  background: transparent;
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 12px);
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-modal__shortcut:hover {
  background: rgba(212,168,67,0.08);
  border-color: rgba(212,168,67,0.4);
}
.coinflip-modal__shortcut:active {
  transform: scale(0.95);
}

/* COPIAR — container cards de lado */
.coinflip-modal__sides {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(10px, 1.2vw, 16px);
  margin-top: clamp(12px, 1.5vw, 20px);
}

/* COPIAR — side card base (nao selecionado) */
.coinflip-modal__side-card {
  background: #141414;
  border: 2px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(16px, 2vw, 28px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(8px, 1vw, 12px);
  cursor: pointer;
  opacity: 0.5;
  transition: all 0.25s ease;
}
.coinflip-modal__side-card:hover {
  opacity: 0.8;
  border-color: rgba(212,168,67,0.3);
  transform: translateY(-2px);
}

/* COPIAR — side card CARA selecionado */
.coinflip-modal__side-card--selected-cara {
  opacity: 1;
  border-color: #00E676;
  box-shadow: 0 0 16px rgba(0,230,118,0.15);
}
.coinflip-modal__side-card--selected-cara:hover {
  opacity: 1;
  transform: none;
}

/* COPIAR — side card COROA selecionado */
.coinflip-modal__side-card--selected-coroa {
  opacity: 1;
  border-color: #FFD700;
  box-shadow: 0 0 16px rgba(255,215,0,0.15);
}
.coinflip-modal__side-card--selected-coroa:hover {
  opacity: 1;
  transform: none;
}

/* COPIAR — imagem face da moeda no side card */
.coinflip-modal__side-img {
  width: clamp(56px, 7vw, 88px);
  height: clamp(56px, 7vw, 88px);
  border-radius: 50%;
}

/* COPIAR — label do lado */
.coinflip-modal__side-label {
  font-family: 'Cinzel', serif;
  font-size: clamp(12px, 1.4vw, 16px);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.coinflip-modal__side-label--cara {
  color: #00E676;
}
.coinflip-modal__side-label--coroa {
  color: #FFD700;
}

/* COPIAR — info saldo e rake */
.coinflip-modal__info {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  color: #A8A8A8;
  margin-top: clamp(12px, 1.5vw, 16px);
}
.coinflip-modal__info-value {
  color: #00E676;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
}
.coinflip-modal__info-error {
  color: #FF1744;
}

/* COPIAR — botao CONFIRMAR (DS P2 §8.1 primario) */
.coinflip-modal__btn-confirm {
  width: 100%;
  padding: clamp(12px, 1.5vw, 18px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
  margin-top: clamp(16px, 2vw, 24px);
  transition: all 0.25s ease;
}
.coinflip-modal__btn-confirm:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.coinflip-modal__btn-confirm:active {
  transform: translateY(1px);
}
.coinflip-modal__btn-confirm--disabled {
  background: #222222;
  border-color: rgba(255,255,255,0.06);
  color: #666666;
  box-shadow: none;
  cursor: not-allowed;
}
.coinflip-modal__btn-confirm--loading {
  pointer-events: none;
  opacity: 0.7;
}

/* COPIAR — botao CANCELAR secundario (DS P2 §8.3) */
.coinflip-modal__btn-cancel {
  width: 100%;
  padding: clamp(8px, 1vw, 12px);
  background: transparent;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.2vw, 14px);
  font-weight: 500;
  cursor: pointer;
  min-height: 44px;
  margin-top: clamp(6px, 0.8vw, 10px);
  transition: all 0.2s ease;
}
.coinflip-modal__btn-cancel:hover {
  border-color: rgba(212,168,67,0.4);
  color: #D4A843;
}
.coinflip-modal__btn-cancel:active {
  transform: scale(0.98);
}
```

---

## 1.10 Modal Entrar Sala — Componentes Especificos (DS P2 §12.2 confirmacao)

```css
/* COPIAR EXATAMENTE — bloco do criador */
.coinflip-modal__creator {
  display: flex;
  align-items: center;
  gap: clamp(10px, 1.2vw, 16px);
  margin-bottom: clamp(16px, 2vw, 20px);
}
.coinflip-modal__creator-avatar {
  width: clamp(40px, 5vw, 56px);
  height: clamp(40px, 5vw, 56px);
  border-radius: 50%;
  border: 2px solid #D4A843;
  object-fit: cover;
}
.coinflip-modal__creator-name {
  font-family: 'Cinzel', serif;
  font-size: clamp(13px, 1.5vw, 17px);
  font-weight: 600;
  color: #F5F5F5;
}
.coinflip-modal__creator-side {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  color: #A8A8A8;
}

/* COPIAR — bloco "VOCE RECEBE" (destaque) */
.coinflip-modal__your-side {
  border: 1px solid rgba(255,215,0,0.2);
  border-radius: clamp(6px, 0.8vw, 10px);
  padding: clamp(12px, 1.5vw, 18px);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1vw, 12px);
  margin-bottom: clamp(16px, 2vw, 20px);
}
.coinflip-modal__your-side--cara {
  background: rgba(0,230,118,0.06);
  border-color: rgba(0,230,118,0.2);
}
.coinflip-modal__your-side--coroa {
  background: rgba(255,215,0,0.06);
  border-color: rgba(255,215,0,0.2);
}
.coinflip-modal__your-side-label {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 500;
  color: #A8A8A8;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.coinflip-modal__your-side-value--cara {
  font-family: 'Cinzel', serif;
  font-size: clamp(16px, 2vw, 22px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
}
.coinflip-modal__your-side-value--coroa {
  font-family: 'Cinzel', serif;
  font-size: clamp(16px, 2vw, 22px);
  font-weight: 700;
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.3);
}
.coinflip-modal__your-side-icon {
  width: clamp(28px, 3.5vw, 40px);
  height: clamp(28px, 3.5vw, 40px);
  border-radius: 50%;
}

/* COPIAR — linhas de detalhes (pot, rake, payout) */
.coinflip-modal__details {
  display: flex;
  flex-direction: column;
  gap: clamp(4px, 0.5vw, 8px);
  margin-bottom: clamp(12px, 1.5vw, 20px);
}
.coinflip-modal__detail-row {
  display: flex;
  justify-content: space-between;
  padding: clamp(4px, 0.5vw, 6px) 0;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
}
.coinflip-modal__detail-label {
  color: #A8A8A8;
}
.coinflip-modal__detail-value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  color: #F5F5F5;
}
.coinflip-modal__detail-value--green {
  color: #00E676;
  text-shadow: 0 0 6px rgba(0,230,118,0.2);
}
.coinflip-modal__detail-value--red {
  color: #FF1744;
}
```

---

## 1.11 Moeda 3D — Coin Component (DS custom — nao existe no DS base)

```css
/* COPIAR EXATAMENTE — container com perspectiva */
.coinflip-coin {
  width: clamp(100px, 14vw, 180px);
  height: clamp(100px, 14vw, 180px);
  perspective: 1000px;
  position: relative;
}

/* COPIAR — inner que rota */
.coinflip-coin__inner {
  width: 100%;
  height: 100%;
  position: relative;
  transform-style: preserve-3d;
  /* rotateY controlado por Framer Motion animate */
}

/* COPIAR — face CARA (frente) */
.coinflip-coin__face--cara {
  position: absolute;
  inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  border-radius: 50%;
  background-image: url('/assets/coinflip/coin-cara.png');
  background-size: cover;
  background-position: center;
  box-shadow:
    0 0 20px rgba(255,215,0,0.2),
    inset 0 -4px 8px rgba(0,0,0,0.3),
    inset 0 4px 8px rgba(255,215,0,0.15);
}

/* COPIAR — face COROA (verso, rotateY 180deg) */
.coinflip-coin__face--coroa {
  position: absolute;
  inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  border-radius: 50%;
  background-image: url('/assets/coinflip/coin-coroa.png');
  background-size: cover;
  background-position: center;
  box-shadow:
    0 0 20px rgba(255,215,0,0.2),
    inset 0 -4px 8px rgba(0,0,0,0.3),
    inset 0 4px 8px rgba(255,215,0,0.15);
  transform: rotateY(180deg);
}

/* COPIAR — glow pulsante durante spin */
.coinflip-coin__glow {
  position: absolute;
  inset: -10%;
  border-radius: 50%;
  pointer-events: none;
  /* Framer Motion: boxShadow oscila
     '0 0 20px rgba(255,215,0,0.2)' ->
     '0 0 40px rgba(255,215,0,0.5)' ->
     '0 0 20px rgba(255,215,0,0.2)'
     duration 0.5s, repeat Infinity */
}

/* COPIAR — drop shadow dinamica */
.coinflip-coin__shadow {
  position: absolute;
  bottom: -12%;
  left: 15%;
  width: 70%;
  height: 10%;
  border-radius: 50%;
  background: radial-gradient(ellipse, rgba(0,0,0,0.4) 0%, transparent 70%);
  pointer-events: none;
  /* scaleX varia com rotateY via Framer Motion useTransform:
     moeda de frente (0deg): scaleX(1)
     moeda de perfil (90deg): scaleX(0.3)
     moeda de costas (180deg): scaleX(1) */
}

/* COPIAR — estado idle (antes do flip) */
.coinflip-coin--idle .coinflip-coin__face--cara,
.coinflip-coin--idle .coinflip-coin__face--coroa {
  box-shadow:
    0 0 12px rgba(255,215,0,0.15),
    inset 0 -3px 6px rgba(0,0,0,0.2),
    inset 0 3px 6px rgba(255,215,0,0.1);
}

/* COPIAR — estado resultado (moeda parada, bounce concluido) */
.coinflip-coin--landed .coinflip-coin__face--cara,
.coinflip-coin--landed .coinflip-coin__face--coroa {
  box-shadow:
    0 0 30px rgba(255,215,0,0.3),
    0 0 60px rgba(255,215,0,0.1),
    inset 0 -4px 8px rgba(0,0,0,0.3),
    inset 0 4px 8px rgba(255,215,0,0.2);
}
```

---

## 1.12 Tela de Flip — Container e Jogadores (DS P1 §4.2 + custom)

```css
/* COPIAR EXATAMENTE — overlay da tela de flip */
.coinflip-flip {
  position: absolute;
  inset: 0;
  z-index: 70;
  background-color: rgba(5,5,5,0.95);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
  padding: clamp(16px, 2vw, 32px);
}

/* COPIAR — pot total no topo */
.coinflip-flip__pot {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(18px, 2.5vw, 30px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.4);
  text-align: center;
}
.coinflip-flip__rake {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  color: #666666;
}

/* COPIAR — area central (jogadores + moeda) */
.coinflip-flip__arena {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(24px, 5vw, 60px);
  width: 100%;
}

/* COPIAR — card de jogador (esquerdo e direito) */
.coinflip-flip__player {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  min-width: clamp(100px, 15vw, 160px);
}
.coinflip-flip__player-avatar {
  width: clamp(48px, 6vw, 72px);
  height: clamp(48px, 6vw, 72px);
  border-radius: 50%;
  object-fit: cover;
}
.coinflip-flip__player-avatar--cara {
  border: 3px solid #00E676;
}
.coinflip-flip__player-avatar--coroa {
  border: 3px solid #FFD700;
}
.coinflip-flip__player-name {
  font-family: 'Cinzel', serif;
  font-size: clamp(12px, 1.4vw, 16px);
  font-weight: 700;
  color: #F5F5F5;
  text-align: center;
}
.coinflip-flip__player-side {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.coinflip-flip__player-side--cara {
  color: #00E676;
}
.coinflip-flip__player-side--coroa {
  color: #FFD700;
}
.coinflip-flip__player-value {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(12px, 1.4vw, 16px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 6px rgba(0,230,118,0.2);
}

/* COPIAR — barra de suspense (base) */
.coinflip-flip__suspense {
  width: 80%;
  height: clamp(3px, 0.4vw, 5px);
  border-radius: 3px;
  background: #1A1A1A;
  overflow: hidden;
  margin-top: auto;
}
.coinflip-flip__suspense-fill {
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, #D4A843, #FFD700, #00E676);
  background-size: 200% 100%;
  border-radius: 3px;
}

@keyframes coinflip-suspense-bar {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(0%); }
}
.coinflip-flip__suspense-fill--active {
  animation: coinflip-suspense-bar 2.5s ease-in-out forwards;
}
```

---

## 1.13 Countdown — 3, 2, 1 (DS P3A §20.1 + custom)

```css
/* COPIAR EXATAMENTE — numero do countdown */
.coinflip-flip__countdown {
  position: absolute;
  z-index: 10;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(48px, 8vw, 96px);
  color: #D4A843;
  text-shadow:
    0 0 30px rgba(255,215,0,0.5),
    0 0 60px rgba(255,215,0,0.2);
  /* Framer Motion: AnimatePresence mode="wait"
     initial: scale 1.5, opacity 1
     animate: scale 1.0, opacity 1
     exit: scale 0.8, opacity 0
     duration: 0.8s ease */
}
```

---

## 1.14 Overlay Resultado — Vencedor (DS P1 §7 + DS P3A §20.2)

```css
/* COPIAR EXATAMENTE — container resultado */
.coinflip-result {
  position: absolute;
  inset: 0;
  z-index: 75;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(16px, 2vw, 28px);
  padding: clamp(16px, 2vw, 32px);
}

/* COPIAR — flash branco no momento do resultado */
.coinflip-result__flash {
  position: absolute;
  inset: 0;
  z-index: 80;
  background: #FFFFFF;
  pointer-events: none;
  /* Framer Motion: opacity [0, 0.3, 0], duration 0.2s */
}

/* COPIAR — avatar vencedor com glow */
.coinflip-result__avatar--winner {
  width: clamp(56px, 7vw, 80px);
  height: clamp(56px, 7vw, 80px);
  border-radius: 50%;
  object-fit: cover;
  border: 3px solid #FFD700;
  /* Framer Motion: boxShadow oscila
     '0 0 20px rgba(255,215,0,0.3)' ->
     '0 0 35px rgba(255,215,0,0.5)' ->
     '0 0 20px rgba(255,215,0,0.3)'
     duration 1.5s, repeat Infinity */
}

/* COPIAR — badge VENCEDOR */
.coinflip-result__badge--winner {
  padding: clamp(4px, 0.5vw, 6px) clamp(10px, 1.2vw, 16px);
  background: rgba(255,215,0,0.15);
  border: 1px solid rgba(255,215,0,0.3);
  border-radius: 6px;
  font-family: 'Cinzel', serif;
  font-size: clamp(9px, 1vw, 12px);
  font-weight: 700;
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

/* COPIAR — texto payout "+9.800 GC" */
.coinflip-result__payout--win {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(24px, 3.5vw, 40px);
  color: #00E676;
  text-shadow: 0 0 16px rgba(0,230,118,0.5), 0 0 40px rgba(0,230,118,0.2);
  /* Framer Motion: initial scale 0.5, animate scale 1, spring stiffness 200 damping 15 */
}

/* COPIAR — mini coin (voam do centro para o vencedor) */
.coinflip-result__mini-coin {
  position: absolute;
  width: clamp(12px, 1.5vw, 18px);
  height: clamp(12px, 1.5vw, 18px);
  border-radius: 50%;
  background: radial-gradient(circle, #FFD700, #D4A843);
  box-shadow: 0 0 6px rgba(255,215,0,0.5);
  pointer-events: none;
  /* Framer Motion: animate x/y para direcao do vencedor, stagger 0.08s, fade out */
}
```

---

## 1.15 Overlay Resultado — Perdedor (DS custom)

```css
/* COPIAR EXATAMENTE — card do perdedor (tudo esmaecido) */
.coinflip-result__player--loser {
  opacity: 0.4;
  /* Framer Motion: initial opacity 1, animate opacity 0.4, duration 0.5s */
}

/* COPIAR — avatar perdedor */
.coinflip-result__avatar--loser {
  width: clamp(48px, 6vw, 72px);
  height: clamp(48px, 6vw, 72px);
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid rgba(212,168,67,0.15);
  filter: grayscale(30%);
}

/* COPIAR — texto payout negativo */
.coinflip-result__payout--lose {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 2vw, 24px);
  color: #FF1744;
  opacity: 0.6;
}
```

---

## 1.16 Botoes da Tela de Resultado (DS P2 §8.1 + §8.3)

```css
/* COPIAR EXATAMENTE — container dos botoes */
.coinflip-result__actions {
  display: flex;
  gap: clamp(10px, 1.2vw, 16px);
  margin-top: auto;
}

/* COPIAR — VOLTAR AO LOBBY (primario verde) */
.coinflip-result__btn-lobby {
  padding: clamp(10px, 1.2vw, 16px) clamp(20px, 2.5vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2);
  transition: all 0.25s ease;
}
.coinflip-result__btn-lobby:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.coinflip-result__btn-lobby:active {
  transform: translateY(1px);
}

/* COPIAR — VERIFICAR (secundario dourado) */
.coinflip-result__btn-verify {
  padding: clamp(10px, 1.2vw, 16px) clamp(20px, 2.5vw, 36px);
  background: transparent;
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(10px, 1.2vw, 14px);
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-result__btn-verify:hover {
  border-color: rgba(212,168,67,0.5);
  background: rgba(212,168,67,0.05);
}
.coinflip-result__btn-verify:active {
  transform: scale(0.97);
}
```

---

**[FIM DA PARTE 2 — Continua com Componentes 1.17-1.20 (Historico, Leaderboard, PF, Tooltip), Atmosferas, Timelines]**

`[ENTREGA: 4.ADENDO-DS-COINFLIP-PARTE-2.md — 20/03/2026]`
