# CSS-COMPONENTES — JACKPOT (#13)
## Arquivo único com CSS real de TODOS os componentes
## Colar JUNTO com qualquer PROMPT-V0 do Jackpot
### Blackout Casino GTARP · 13/03/2026

---

# COMO USAR

Copiar INTEIRO junto com o PROMPT-V0 ao colar no V0/Bolt.
O V0 vai usar estes CSS exatos em vez de inventar.

---

# 1. FUNDO DO JOGO

```css
.jackpot-bg {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
  padding: clamp(8px, 1.5vw, 20px);
}
```

# 2. DONUT CHART (conic-gradient)

```css
.jackpot-donut {
  position: relative;
  width: clamp(200px, 30vw, 350px); aspect-ratio: 1;
  border-radius: 50%;
  border: 3px solid rgba(212,168,67,0.3);
  box-shadow: 0 0 30px rgba(212,168,67,0.1), 0 0 60px rgba(212,168,67,0.04);
  /* conic-gradient é setado inline via JS */
}
```

# 3. CENTRO DO DONUT

```css
.jackpot-donut-center {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: 50%; height: 50%; border-radius: 50%;
  background: radial-gradient(circle, rgba(15,15,15,0.95) 0%, rgba(5,5,5,0.98) 100%);
  border: 2px solid rgba(212,168,67,0.3);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  z-index: 2;
}
```

# 4. PONTEIRO DOURADO

```css
.jackpot-pointer {
  position: absolute; top: -2px; left: 50%;
  transform: translateX(-50%);
  width: 0; height: 0;
  border-left: clamp(10px, 1.5vw, 14px) solid transparent;
  border-right: clamp(10px, 1.5vw, 14px) solid transparent;
  border-top: clamp(16px, 2.5vw, 22px) solid #D4A843;
  filter: drop-shadow(0 2px 6px rgba(212,168,67,0.5));
  z-index: 5; pointer-events: none;
}
```

# 5. PLAYER CARD

```css
.jackpot-player-card {
  background: linear-gradient(180deg, #1A1A1A 0%, #141414 100%);
  border-radius: 8px;
  padding: clamp(6px, 0.8vw, 10px);
  display: flex; align-items: center; gap: clamp(6px, 1vw, 12px);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
/* Faixa lateral cor do jogador */
.jackpot-player-card { border-left: 4px solid var(--player-color); }
.jackpot-player-card:hover { transform: translateX(2px); border-left-width: 6px; }
```

# 6. PLAYER COLOR DOT

```css
.jackpot-color-dot {
  width: 12px; height: 12px; border-radius: 50%; flex-shrink: 0;
  box-shadow: 0 0 6px var(--player-color);
}
```

# 7. DEPOSIT INPUT

```css
.jackpot-input {
  background: #0A0A0A;
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 6px; color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(16px, 2vw, 24px); font-weight: 700;
  text-align: center; padding: 10px 16px; width: 100%; outline: none;
}
.jackpot-input:focus { border-color: #D4A843; box-shadow: 0 0 8px rgba(212,168,67,0.2); }
.jackpot-input.invalid { border-color: #FF1744; }
```

# 8. BOTÕES RÁPIDOS (MIN/×2/÷2/MAX)

```css
.jackpot-shortcut {
  background: rgba(26,26,26,0.8);
  border: 1px solid rgba(212,168,67,0.12);
  border-radius: 4px; color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-size: 10px; font-weight: 600; padding: 4px 10px; cursor: pointer;
}
.jackpot-shortcut:hover { color: #D4A843; border-color: rgba(212,168,67,0.3); }
```

# 9. BOTÃO DEPOSITAR (primário verde)

```css
.jackpot-btn-deposit {
  background: linear-gradient(180deg, #00C853 0%, #00E676 30%, #004D25 100%);
  border: 1px solid rgba(0,230,118,0.3); border-radius: 8px;
  color: #FFFFFF; font-family: 'Cinzel', serif;
  font-size: clamp(11px, 1.3vw, 16px); font-weight: 700;
  text-transform: uppercase; letter-spacing: 1px;
  padding: 10px 24px; min-height: 44px; cursor: pointer;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.15), 0 2px 4px rgba(0,0,0,0.4), 0 0 12px rgba(0,230,118,0.15);
  position: relative; overflow: hidden;
}
.jackpot-btn-deposit::before {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(180deg, rgba(255,255,255,0.12) 0%, transparent 50%);
  border-radius: inherit; pointer-events: none;
}
.jackpot-btn-deposit:hover { background: linear-gradient(180deg, #00E676 0%, #00693C 100%); transform: translateY(-1px); }
.jackpot-btn-deposit:disabled { opacity: 0.4; cursor: not-allowed; }
```

# 10. BOTÃO VOLTAR

```css
.jackpot-btn-back {
  background: rgba(0,0,0,0.6); border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px; color: #D4A843;
  font-family: 'Cinzel', serif; font-size: clamp(9px, 1.1vw, 14px);
  font-weight: 700; letter-spacing: 2px; padding: 8px 16px; cursor: pointer;
}
.jackpot-btn-back:hover { border-color: rgba(212,168,67,0.8); transform: scale(1.05); }
```

# 11. BOTÃO DOURADO OUTLINE (HISTÓRICO)

```css
.jackpot-btn-gold {
  background: transparent; border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 10px; padding: clamp(8px,1.2vw,14px) clamp(16px,3vw,40px);
  color: #D4A843; font-family: 'Cinzel', serif;
  font-size: clamp(10px,1.2vw,14px); font-weight: 700; letter-spacing: 1.5px;
  text-transform: uppercase; cursor: pointer;
}
.jackpot-btn-gold:hover { border-color: rgba(212,168,67,0.8); transform: scale(1.05); }
```

# 12. TIMER

```css
.jackpot-timer {
  font-family: 'Cinzel', serif; font-weight: 900;
  font-size: clamp(16px, 2vw, 28px); color: #D4A843;
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
}
.jackpot-timer.urgent { color: #FF6B6B; animation: timerUrgent 0.5s ease-in-out infinite; }
@keyframes timerUrgent { 0%,100% { opacity: 1; } 50% { opacity: 0.6; } }
```

# 13. BANNER STATUS

```css
.jackpot-banner {
  font-family: 'Cinzel', serif; font-size: clamp(9px, 1vw, 12px);
  font-weight: 700; text-transform: uppercase; letter-spacing: 1.5px;
  padding: 4px 12px; border-radius: 6px;
}
.jackpot-banner-open { color: #D4A843; background: rgba(212,168,67,0.08); border: 1px solid rgba(212,168,67,0.2); }
.jackpot-banner-closed { color: #FF6B6B; background: rgba(255,107,107,0.08); border: 1px solid rgba(255,107,107,0.2); }
.jackpot-banner-spinning { color: #00E676; background: rgba(0,230,118,0.08); border: 1px solid rgba(0,230,118,0.2); }
```

# 14. RESULTADO OVERLAY

```css
.jackpot-result-overlay {
  position: absolute; inset: 0; z-index: 50;
  background: rgba(0,0,0,0.75); backdrop-filter: blur(6px);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
}
.jackpot-result-card {
  background: linear-gradient(180deg, rgba(20,20,20,0.9) 0%, rgba(10,10,10,0.95) 100%);
  border-radius: 14px; padding: clamp(16px, 2.5vw, 32px);
  text-align: center; min-width: clamp(220px, 35vw, 360px);
}
.jackpot-result-card-win { border: 2px solid rgba(255,215,0,0.4); box-shadow: 0 8px 32px rgba(0,0,0,0.6), 0 0 30px rgba(255,215,0,0.15); }
.jackpot-result-card-lose { border: 1px solid rgba(212,168,67,0.15); }
```

# 15. TEXTOS

```css
.jackpot-title { font-family: 'Cinzel', serif; font-weight: 900; font-size: clamp(14px, 2vw, 24px); color: #D4A843; text-transform: uppercase; letter-spacing: 3px; text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1); }
.jackpot-win-text { font-family: 'Cinzel', serif; font-weight: 900; font-size: clamp(28px, 5vw, 52px); color: #FFD700; text-transform: uppercase; letter-spacing: 6px; text-shadow: 0 0 20px rgba(255,215,0,0.6), 0 0 40px rgba(255,215,0,0.3), 0 2px 8px rgba(0,0,0,1); }
.jackpot-lose-text { font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(16px, 2.5vw, 28px); color: rgba(212,168,67,0.7); text-shadow: 0 0 8px rgba(212,168,67,0.3); }
.jackpot-value { font-family: 'JetBrains Mono', monospace; font-weight: 700; color: #00E676; text-shadow: 0 0 10px rgba(0,230,118,0.5); }
.jackpot-prize { font-family: 'JetBrains Mono', monospace; font-weight: 700; font-size: clamp(24px, 4vw, 44px); color: #00E676; text-shadow: 0 0 16px rgba(0,230,118,0.5), 0 0 32px rgba(0,230,118,0.25); }
.jackpot-label { font-family: 'Cinzel', serif; font-size: clamp(9px, 1vw, 12px); font-weight: 700; color: #D4A843; text-transform: uppercase; letter-spacing: 1.5px; }
```

# 16. TOOLTIP

```css
.jackpot-tooltip {
  position: absolute; bottom: calc(100% + 8px); left: 50%;
  transform: translateX(-50%) scale(0.95);
  background: rgba(17,17,17,0.95); backdrop-filter: blur(8px);
  border: 1px solid rgba(212,168,67,0.15); border-radius: 6px;
  padding: 6px 12px; font-family: 'Inter', sans-serif;
  font-size: 11px; color: #E0E0E0; white-space: nowrap;
  pointer-events: none; opacity: 0;
  transition: all 0.15s cubic-bezier(0.0, 0.0, 0.2, 1); z-index: 100;
}
.jackpot-tooltip.visible { opacity: 1; transform: translateX(-50%) scale(1); }
```

# 17. GLASSMORPHISM (modais)

```css
.jackpot-modal {
  background: rgba(10,10,10,0.85);
  backdrop-filter: blur(12px) saturate(1.2);
  -webkit-backdrop-filter: blur(12px) saturate(1.2);
  border: 1px solid rgba(212,168,67,0.15); border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.5), 0 2px 8px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.05);
  padding: clamp(16px, 2vw, 32px);
}
```

# 18. HASH FIELD (Provably Fair)

```css
.jackpot-hash { font-family: 'JetBrains Mono', monospace; font-size: 10px; color: #666; background: rgba(0,0,0,0.3); padding: 8px 10px; border-radius: 4px; word-break: break-all; }
```

# 19. HISTORY ENTRY

```css
.jackpot-history-entry {
  background: linear-gradient(180deg, #1A1A1A 0%, #141414 100%);
  border: 1px solid rgba(212,168,67,0.15); border-radius: 10px;
  padding: clamp(8px, 1vw, 14px);
  box-shadow: 0 2px 8px rgba(0,0,0,0.3), inset 0 1px 0 rgba(212,168,67,0.05);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.jackpot-history-entry:hover { border-color: rgba(212,168,67,0.35); transform: translateY(-2px); }
```

# 20. ANIMAÇÕES

```css
@keyframes spinnerGlow {
  0%, 100% { box-shadow: 0 0 30px rgba(212,168,67,0.15), 0 0 60px rgba(212,168,67,0.06); }
  50%      { box-shadow: 0 0 50px rgba(212,168,67,0.3), 0 0 100px rgba(212,168,67,0.12); }
}
@keyframes flashGold { 0% { background-color: transparent; } 15% { background-color: rgba(255,215,0,0.08); } 100% { background-color: transparent; } }
@keyframes screenShake { 0%,100% { transform: translate(0,0); } 10% { transform: translate(-3px,-1px); } 20% { transform: translate(3px,2px); } 30% { transform: translate(-2px,-2px); } 40% { transform: translate(2px,1px); } }
@keyframes confettiFall { 0% { transform: translateY(0) rotate(0deg); opacity: 1; } 100% { transform: translateY(300px) rotate(720deg); opacity: 0; } }
```

# 21. IMAGENS

| # | Asset | Path |
|---|-------|------|
| 1 | Fundo casino | /assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png |
| 2 | Logo BR | /assets/logos-br-para-cards/13.LOGO-BR-JACKPOT.png |
| 3 | Logo EN | /assets/logos-in-para-cards/13.LOGO-IN-JACKPOT.png |
| 4 | Card image | /assets/image-para-cards/13.JACKPOT-IMAGE-768_1024.png |
| 5 | Imagem dourada | /assets/image-background-transparent/.../13.IMAGEM-DOURADA-JACKPOT.png |

# 22. PROIBIDO

1. Fundo branco ou cinza claro
2. Fontes padrão navegador
3. Cores fora da paleta (#D4A843, #FFD700, #00E676, #0A0A0A + 10 cores jogadores)
4. SVG para elementos do jogo
5. ease genérico (usar cubic-bezier)
6. Botão sem tooltip bilíngue
7. Ação sem feedback visual
8. Modal sem fechar
9. Texto 1 idioma
10. px fixo para layout
11. Scroll vertical container principal
12. Animar conic-gradient diretamente (usar transform rotate)
13. Math.random() para RNG
14. Desaceleração linear no spinner
15. Resultado calculado client-side

---

`[ENTREGA: 7.CSS-COMPONENTES-JACKPOT-NOVO.md — 22 seções — 13/03/2026]`
