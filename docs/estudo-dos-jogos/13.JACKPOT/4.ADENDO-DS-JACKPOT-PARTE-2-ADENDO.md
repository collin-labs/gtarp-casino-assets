# ADENDO DESIGN SYSTEM — JACKPOT (#13) PvP Pot Multi-Player
## PARTE 1 DE 3 — Componentes CSS Real (1.1-1.8)
## SUBSTITUI: 4.ADENDO-DS-JACKPOT.md (437L, score 29%)
## Referencias diretas ao DS com CSS real — Complemento ao Prompt AI
### Blackout Casino GTARP - 24/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Doc 4 do Checklist Mestre (17 itens)
- **Gold Standard consultado:** Adendo DS Coinflip #12 (2.176L, 4 partes, 20 componentes, score 100%), Adendo DS Dice #7 (1.981L, 3 partes)
- **Benchmark:** 1.500-2.000L (3 partes de ~500L)
- **Itens a cobrir:** 4.1-4.17 (todos os 17 itens)
- **Componentes planejados:** 22 componentes + 5 atmosferas + 4 timelines
- **Pesquisa X0:** 2 buscas: jackpot spinner CSS conic-gradient dark UI (MDN conic-gradient, Dribbble casino UI, CodePen Casino Lottery Wheel), CSGOEmpire jackpot pot PvP interface (GameTyrant, Bitcoinist, EgamersWorld). Descobertas: conic-gradient nativo no CEF desde Chrome 69 (FiveM usa CEF 90+), CSGOEmpire simplifica UI ao maximo — roulette/coinflip sem excesso visual, jackpot PvP real usa donut proporcional ao deposito com pointer fixo no topo
- **O que teria feito errado sem pesquisa:** Usaria Canvas 2D para o donut (desnecessario — conic-gradient faz nativamente), nao teria percebido que sites reais simplificam o layout ao maximo (lobby + spinner + resultado, sem submenus)

---

# COMO USAR ESTE DOCUMENTO

Este documento contem CSS REAL copiavel para CADA componente do Jackpot. Para usar:
1. Localizar o componente desejado pelo numero (§1.1, §1.2, etc)
2. Verificar a referencia DS (Design System) entre parenteses
3. Copiar o bloco CSS "COPIAR EXATAMENTE" para o style={{}} do React
4. Adaptar SOMENTE: textos bilingues, dados mock, logica de estado
5. NAO alterar: cores hex, sombras, bordas, efeitos — sao padrao DS

---

# SECAO 1 — COMPONENTES COM CSS REAL (22 Componentes)

## 1.1 Container Principal do Jogo (DS P1 §4.1 + §4.2)

O container ocupa todo o painel. Background com textura bg-casino.png + gradientes de profundidade + vinheta.

```
COPIAR EXATAMENTE:
```
```css
.jackpot-container {
  position: absolute;
  inset: 0;
  z-index: 60;
  border-radius: inherit;
  overflow: hidden;
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  display: flex;
  flex-direction: column;
}
```

**Estados:**
- **NORMAL:** CSS acima
- **HOVER:** N/A (container nao tem hover)
- **ACTIVE:** N/A
- **DISABLED:** N/A

---

## 1.2 Header Bar (DS P2 §15.2 adaptado)

Barra horizontal no topo com botao VOLTAR, titulo, icones (PF, historico, som), saldo.

```
COPIAR EXATAMENTE:
```
```css
.jackpot-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  position: relative;
  z-index: 10;
}

.jackpot-header__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  text-transform: uppercase;
  letter-spacing: 3px;
  color: #D4A843;
  text-shadow: 0 0 12px rgba(255,215,0,0.5), 0 2px 6px rgba(0,0,0,1);
}

.jackpot-header__balance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 18px);
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  letter-spacing: 1.5px;
}

.jackpot-header__icon {
  width: clamp(20px, 2.5vw, 28px);
  height: clamp(20px, 2.5vw, 28px);
  opacity: 0.7;
  cursor: pointer;
  min-width: 44px;
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: clamp(8px, 1vw, 12px);
  transition: opacity 0.2s ease;
}
```

**Estados:**
- **NORMAL:** opacity 0.7
- **HOVER:** opacity 1, transform scale(1.1)
- **ACTIVE:** opacity 1, transform scale(0.95)
- **DISABLED:** opacity 0.3, pointer-events none

---

## 1.3 Botao VOLTAR (Guia P3 §9.6)

```
COPIAR EXATAMENTE:
```
```css
.jackpot-btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px;
  cursor: pointer;
  text-transform: uppercase;
  transition: all 0.2s ease;
}
```

**Framer Motion:**
```
whileHover={{ scale: 1.05, borderColor: "rgba(212,168,67,0.8)" }}
whileTap={{ scale: 0.95 }}
```

**Estados:**
- **NORMAL:** CSS acima
- **HOVER:** borderColor 0.8, scale 1.05
- **ACTIVE:** scale 0.95

**Tooltip:** title={lang === "br" ? "Voltar ao painel" : "Back to panel"}

---

## 1.4 Layout Principal — Grid Lobby (DS P1 §4.2 adaptado)

O lobby do Jackpot tem 3 areas: painel de deposito (esquerda), donut spinner (centro), lista de jogadores (direita).

```
COPIAR EXATAMENTE:
```
```css
.jackpot-lobby {
  display: grid;
  grid-template-columns: clamp(200px, 25vw, 300px) 1fr clamp(200px, 25vw, 300px);
  gap: clamp(8px, 1vw, 14px);
  flex: 1;
  padding: clamp(8px, 1vw, 14px);
  overflow: hidden;
}

/* Responsivo: em telas menores, empilha */
@media (max-width: 900px) {
  .jackpot-lobby {
    grid-template-columns: 1fr;
    grid-template-rows: auto 1fr auto;
  }
}
```

---

## 1.5 Painel de Deposito (DS P2 §9.1 + §8.8 adaptado)

Lado esquerdo do lobby. Contem: input de valor, atalhos MIN/x2/÷2/MAX, botao DEPOSITAR, info de rake, min/max.

```
COPIAR EXATAMENTE:
```
```css
.jackpot-deposit {
  background: rgba(5,5,5,0.95);
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: clamp(10px, 1.2vw, 14px);
  padding: clamp(12px, 1.5vw, 20px);
  display: flex;
  flex-direction: column;
  gap: clamp(8px, 1vw, 12px);
  box-shadow:
    0 4px 15px rgba(0,0,0,0.6),
    0 1px 3px rgba(0,0,0,0.8),
    inset 0 1px 0 rgba(255,215,0,0.06),
    inset 0 0 15px rgba(0,0,0,0.4);
}

.jackpot-deposit__label {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  color: #D4A843;
  text-shadow: 0 0 8px rgba(255,215,0,0.3);
}

/* Input de valor */
.jackpot-deposit__input {
  width: 100%;
  background: rgba(15,15,15,0.9);
  border: 1.5px solid rgba(212,168,67,0.25);
  border-radius: 8px;
  padding: clamp(8px, 1vw, 14px) clamp(10px, 1.2vw, 16px);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 22px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
  outline: none;
  text-align: center;
  min-height: 44px;
  transition: border-color 0.2s ease;
}

.jackpot-deposit__input:focus {
  border-color: rgba(212,168,67,0.6);
  box-shadow: 0 0 12px rgba(212,168,67,0.15);
}

.jackpot-deposit__input::placeholder {
  color: rgba(212,168,67,0.3);
  font-weight: 400;
}

/* Atalhos MIN/x2/÷2/MAX (DS P2 §8.8) */
.jackpot-deposit__shortcuts {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: clamp(4px, 0.5vw, 6px);
}

.jackpot-deposit__shortcut {
  background: rgba(20,20,20,0.8);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 6px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1vw, 12px);
  color: rgba(212,168,67,0.6);
  padding: clamp(4px, 0.5vw, 8px);
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  text-transform: uppercase;
  transition: all 0.15s ease;
}

.jackpot-deposit__shortcut:hover {
  border-color: rgba(212,168,67,0.5);
  color: #D4A843;
  background: rgba(30,30,30,0.9);
}

.jackpot-deposit__shortcut:active {
  transform: scale(0.95);
}

/* Info rake e limites */
.jackpot-deposit__info {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 12px);
  color: rgba(212,168,67,0.4);
  line-height: 1.5;
}
```

**Tooltips atalhos:**
- MIN: title={lang === "br" ? "Aposta minima" : "Minimum bet"}
- x2: title={lang === "br" ? "Dobrar valor" : "Double value"}
- /2: title={lang === "br" ? "Metade do valor" : "Half value"}
- MAX: title={lang === "br" ? "Aposta maxima (ou saldo)" : "Maximum bet (or balance)"}

---

## 1.6 Botao DEPOSITAR — CTA Verde (DS P2 §8.1)

Botao principal do jogo. Verde brilhante com gradiente, bevel, glow.

```
COPIAR EXATAMENTE:
```
```css
.jackpot-btn-deposit {
  background: linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%);
  border: 1px solid rgba(0,230,118,0.5);
  border-radius: clamp(8px, 1vw, 12px);
  font-family: 'Cinzel', serif;
  font-weight: 800;
  font-size: clamp(12px, 1.5vw, 18px);
  text-transform: uppercase;
  letter-spacing: 2px;
  color: #FFFFFF;
  padding: clamp(10px, 1.2vw, 16px) clamp(20px, 3vw, 40px);
  min-height: 44px;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 4px 12px rgba(0,200,83,0.3),
    inset 0 1px 0 rgba(255,255,255,0.15);
  transition: all 0.2s ease;
}

/* Bevel ::before (DS P2 §8.1) */
.jackpot-btn-deposit::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: linear-gradient(180deg, rgba(255,255,255,0.1) 0%, transparent 50%, rgba(0,0,0,0.2) 100%);
  pointer-events: none;
}
```

**Framer Motion:**
```
whileHover={{ scale: 1.05, boxShadow: "0 6px 20px rgba(0,200,83,0.5)" }}
whileTap={{ scale: 0.95 }}
```

**Estados:**
- **NORMAL:** CSS acima
- **HOVER:** scale 1.05, boxShadow intensificado
- **ACTIVE:** scale 0.95
- **DISABLED (pot encerrado ou sem saldo):**
  ```css
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
  filter: grayscale(0.5);
  ```

**Tooltip:** title={lang === "br" ? "Depositar GCoins no pot" : "Deposit GCoins to pot"}

---

## 1.7 Botao Dourado Secundario (DS P2 §8.2)

Usado para: HISTORICO, VERIFICAR PF, JOGAR NOVAMENTE.

```
COPIAR EXATAMENTE:
```
```css
.jackpot-btn-gold {
  background: linear-gradient(135deg, rgba(212,168,67,0.15), rgba(212,168,67,0.05));
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: clamp(6px, 0.8vw, 10px);
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: #D4A843;
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2vw, 28px);
  min-height: 44px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: clamp(4px, 0.5vw, 8px);
  transition: all 0.2s ease;
}

.jackpot-btn-gold:hover {
  background: linear-gradient(135deg, rgba(212,168,67,0.25), rgba(212,168,67,0.1));
  border-color: rgba(212,168,67,0.7);
  color: #FFD700;
  box-shadow: 0 0 12px rgba(212,168,67,0.2);
}

.jackpot-btn-gold:active {
  transform: scale(0.95);
}

.jackpot-btn-gold:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}
```

---

## 1.8 Donut Spinner — Componente Central (DS custom — nao existe no DS base)

O donut e o elemento visual principal do Jackpot. Usa CSS conic-gradient para criar segmentos proporcionais ao deposito de cada jogador. A rotacao e feita via CSS transform: rotate() com easing personalizado.

```
COPIAR EXATAMENTE:
```
```css
/* Container do donut com moldura dourada */
.jackpot-donut {
  position: relative;
  width: clamp(200px, 30vw, 380px);
  height: clamp(200px, 30vw, 380px);
  margin: 0 auto;
}

/* Anel externo dourado (moldura) */
.jackpot-donut__frame {
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  background: conic-gradient(from 0deg, #8B6914, #D4A843, #FFD700, #F6E27A, #FFD700, #D4A843, #8B6914);
  box-shadow:
    0 0 20px rgba(212,168,67,0.3),
    inset 0 0 20px rgba(0,0,0,0.5);
}

/* Donut propriamente — conic-gradient dinamico */
.jackpot-donut__wheel {
  position: absolute;
  inset: 4px;
  border-radius: 50%;
  /* background gerado dinamicamente via JS:
     conic-gradient(
       #D32F2F 0deg 120deg,    -> jogador 1 (33%)
       #1E88E5 120deg 210deg,  -> jogador 2 (25%)
       #7B1FA2 210deg 280deg,  -> jogador 3 (19%)
       #00C853 280deg 360deg   -> jogador 4 (23%)
     )
  */
  transition: transform 8s cubic-bezier(0.2, 0.8, 0.3, 1);
  box-shadow: inset 0 0 40px rgba(0,0,0,0.6);
}

/* Centro escuro (buraco do donut) */
.jackpot-donut__center {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 45%;
  height: 45%;
  border-radius: 50%;
  background: radial-gradient(circle, #0F0F0F 0%, #0A0A0A 100%);
  border: 2px solid rgba(212,168,67,0.3);
  box-shadow:
    0 0 15px rgba(0,0,0,0.8),
    inset 0 0 10px rgba(0,0,0,0.5);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 5;
}

/* Pot total no centro */
.jackpot-donut__pot {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 28px);
  color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.6), 0 0 30px rgba(0,230,118,0.3);
  letter-spacing: 1px;
}

/* Label "POT TOTAL" */
.jackpot-donut__pot-label {
  font-family: 'Cinzel', serif;
  font-weight: 600;
  font-size: clamp(8px, 0.9vw, 11px);
  text-transform: uppercase;
  letter-spacing: 2px;
  color: rgba(212,168,67,0.5);
  margin-top: 2px;
}

/* Pointer/seta fixa no topo */
.jackpot-donut__pointer {
  position: absolute;
  top: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-top: 16px solid #FFD700;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.8)) drop-shadow(0 0 8px rgba(255,215,0,0.5));
  z-index: 10;
}

/* Timer circular ao redor do donut (estado COUNTDOWN) */
.jackpot-donut__timer-ring {
  position: absolute;
  inset: -12px;
  border-radius: 50%;
  border: 3px solid transparent;
  border-top-color: #FFD700;
  animation: jackpot-timer-spin 1s linear infinite;
}

@keyframes jackpot-timer-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

**Estados do Donut:**
- **IDLE (pot aberto, aguardando depositos):** wheel sem rotacao, segmentos proporcionais atualizando em tempo real conforme depositos
- **COUNTDOWN (timer de 15s antes de girar):** timer-ring aparece girando, pot total pulsa com glow intensificado
- **SPINNING (rodando por 8s):** transform rotate(Xdeg) com transition 8s cubic-bezier(0.2, 0.8, 0.3, 1). X = angulo do vencedor + N voltas completas
- **RESULT (parado no vencedor):** segmento vencedor com glow intenso, demais com opacity 0.4

---

**[FIM DA PARTE 1 — Continua Parte 2: Componentes 1.9-1.16 (Player Card, Lista Jogadores, Countdown, Resultado, History, PF Modal, Tooltip, Textos)]**

`[ENTREGA: 4.ADENDO-DS-JACKPOT-PARTE-1.md — 24/03/2026]`
