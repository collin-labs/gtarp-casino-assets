# ROTEIRO DE DESENVOLVIMENTO - BLACKJACK (#4)
## Parte 5 de 5 - Fase 5 (Result, SideBets, Insurance, History, ProvablyFair) + Resumo
### 19/03/2026

---

# =============================================
# FASE 5 - COMPONENTES EXTRAS (5 arquivos)
# =============================================

**Objetivo:** Criar overlay de resultado com 5 variantes visuais, display de side bets com estados win/lose, modal de insurance com timer SVG circular, lista de historico, e pagina de verificacao Provably Fair.
**Prioridade:** Media-Alta (feedback visual completo ao jogador)
**Estimativa:** ~500 linhas TSX (160 Result + 80 SideBets + 80 Insurance + 80 History + 80 PF)
**Refs:** Adendo DS 1.19-1.20 (insurance modal, 5 overlays), Prompt AI Telas 6-7, Handler (history, verify, session:new)

---

## 5.1 Criar `components/games/blackjack/BlackjackResult.tsx`

**Arquivo NOVO - overlay de resultado com 5 variantes:**

```tsx
// Blackout Casino - Blackjack (#4) - Result Overlay
// Adendo DS 1.20 (5 variantes: WIN, BUST, BLACKJACK, PUSH, SURRENDER)
// Countup payout, botoes NOVA MAO + VOLTAR
// 19/03/2026

'use client';
import React, { useState, useEffect, useRef } from 'react';
import { motion } from 'framer-motion';
import { bjTexts } from './texts';
import type { BJResult } from './types';

interface BlackjackResultProps {
  results: { handId: number; result: BJResult; payout: number; playerTotal: number }[];
  dealerTotal: number;
  totalPayout: number;
  insuranceResult: { result: 'WIN' | 'LOSE'; payout: number } | null;
  onNewHand: () => void;
  onBack: () => void;
  lang: 'br' | 'en';
}

const RESULT_STYLES: Record<BJResult, {
  bg: string; blur: number; titleColor: string; titleShadow: string;
}> = {
  WIN: {
    bg: 'rgba(0,0,0,0.7)', blur: 6,
    titleColor: '#00E676', titleShadow: '0 0 20px rgba(0,230,118,0.5),0 0 60px rgba(0,230,118,0.2)',
  },
  BUST: {
    bg: 'rgba(0,0,0,0.6)', blur: 4,
    titleColor: '#FF1744', titleShadow: '0 0 15px rgba(255,23,68,0.4)',
  },
  BLACKJACK: {
    bg: 'rgba(0,0,0,0.85)', blur: 8,
    titleColor: '#FFD700', titleShadow: '0 0 20px rgba(255,215,0,0.6),0 0 60px rgba(255,215,0,0.3)',
  },
  PUSH: {
    bg: 'rgba(0,0,0,0.5)', blur: 4,
    titleColor: '#FFD700', titleShadow: '0 0 10px rgba(255,215,0,0.3)',
  },
  SURRENDER: {
    bg: 'rgba(0,0,0,0.5)', blur: 4,
    titleColor: 'rgba(212,168,67,0.6)', titleShadow: 'none',
  },
  LOSE: {
    bg: 'rgba(0,0,0,0.6)', blur: 4,
    titleColor: '#FF1744', titleShadow: '0 0 15px rgba(255,23,68,0.4)',
  },
};

const RESULT_TITLES: Record<BJResult, { br: string; en: string }> = {
  WIN:       { br: 'VOCE VENCEU!',  en: 'YOU WIN!' },
  BUST:      { br: 'ESTOUROU!',     en: 'BUST!' },
  BLACKJACK: { br: 'BLACKJACK!',    en: 'BLACKJACK!' },
  PUSH:      { br: 'EMPATE',        en: 'PUSH' },
  SURRENDER: { br: 'DESISTENCIA',   en: 'SURRENDER' },
  LOSE:      { br: 'DEALER VENCEU', en: 'DEALER WINS' },
};

function useCountup(target: number, duration: number = 1500) {
  const [value, setValue] = useState(0);
  const startRef = useRef<number>(0);
  const rafRef = useRef<number>(0);

  useEffect(() => {
    if (target <= 0) { setValue(0); return; }
    startRef.current = performance.now();
    const animate = (now: number) => {
      const elapsed = now - startRef.current;
      const progress = Math.min(elapsed / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 5);
      setValue(Math.round(target * eased));
      if (progress < 1) rafRef.current = requestAnimationFrame(animate);
    };
    rafRef.current = requestAnimationFrame(animate);
    return () => cancelAnimationFrame(rafRef.current);
  }, [target, duration]);

  return value;
}

export default function BlackjackResult({
  results, dealerTotal, totalPayout, insuranceResult, onNewHand, onBack, lang,
}: BlackjackResultProps) {
  const t = bjTexts[lang];
  const mainResult = results[0]?.result || 'LOSE';
  const style = RESULT_STYLES[mainResult];
  const title = RESULT_TITLES[mainResult][lang];
  const countedPayout = useCountup(totalPayout);

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      style={{
        position: 'absolute', inset: 0,
        background: style.bg,
        backdropFilter: `blur(${style.blur}px)`,
        WebkitBackdropFilter: `blur(${style.blur}px)`,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        gap: 'clamp(12px,2vw,20px)', zIndex: 80,
      }}
    >
      {/* Titulo */}
      <motion.div
        initial={{ scale: mainResult === 'BUST' ? 1 : 0.85, opacity: 0, x: mainResult === 'BUST' ? -3 : 0 }}
        animate={{
          scale: 1, opacity: 1,
          x: mainResult === 'BUST' ? [0, -3, 3, -2, 2, 0] : 0,
        }}
        transition={{ type: 'spring', stiffness: mainResult === 'BLACKJACK' ? 200 : 300, damping: mainResult === 'BLACKJACK' ? 20 : 25 }}
        style={{
          fontFamily: "'Cinzel',serif", fontWeight: 900,
          fontSize: 'clamp(20px,4vw,40px)',
          color: style.titleColor,
          textShadow: style.titleShadow,
          textTransform: 'uppercase', letterSpacing: '3px',
          position: 'relative', overflow: 'hidden',
        }}
      >
        {title}
        {/* Shimmer sweep para BLACKJACK */}
        {mainResult === 'BLACKJACK' && (
          <div style={{
            position: 'absolute', inset: 0,
            background: 'linear-gradient(90deg,transparent 0%,rgba(255,215,0,0.3) 50%,transparent 100%)',
            animation: 'bjShimmerSweep 1.5s ease-in-out',
          }} />
        )}
      </motion.div>

      {/* Payout countup */}
      {totalPayout > 0 && (
        <div style={{
          fontFamily: "'JetBrains Mono',monospace", fontWeight: 700,
          fontSize: 'clamp(16px,3vw,32px)',
          color: mainResult === 'BLACKJACK' ? '#00E676' : style.titleColor,
          textShadow: mainResult === 'BLACKJACK' ? '0 0 15px rgba(0,230,118,0.6)' : style.titleShadow,
        }}>
          <span style={{ fontSize: '0.65em', opacity: 0.7 }}>G$</span>
          +{countedPayout.toLocaleString('pt-BR')}
        </div>
      )}

      {/* Insurance result */}
      {insuranceResult && (
        <div style={{
          fontFamily: "'Inter',sans-serif", fontWeight: 500,
          fontSize: 'clamp(10px,1.3vw,14px)',
          color: insuranceResult.result === 'WIN' ? '#00E676' : '#FF5252',
        }}>
          {lang === 'br' ? 'Seguro: ' : 'Insurance: '}
          {insuranceResult.result === 'WIN'
            ? `+G$${insuranceResult.payout.toLocaleString('pt-BR')}`
            : (lang === 'br' ? 'Perdeu' : 'Lost')
          }
        </div>
      )}

      {/* Split results */}
      {results.length > 1 && (
        <div style={{
          display: 'flex', gap: 'clamp(8px,1.5vw,16px)',
          fontFamily: "'Inter',sans-serif", fontWeight: 500,
          fontSize: 'clamp(9px,1.1vw,12px)', color: '#A8A8A8',
        }}>
          {results.map(r => (
            <span key={r.handId}>
              {lang === 'br' ? `Mao ${r.handId + 1}: ` : `Hand ${r.handId + 1}: `}
              <span style={{ color: r.result === 'WIN' || r.result === 'BLACKJACK' ? '#00E676' : r.result === 'PUSH' ? '#FFD700' : '#FF5252' }}>
                {r.result} {r.payout > 0 ? `+G$${r.payout}` : ''}
              </span>
            </span>
          ))}
        </div>
      )}

      {/* Botoes */}
      <div style={{ display: 'flex', gap: 'clamp(8px,1.5vw,16px)', marginTop: 'clamp(8px,1.5vw,16px)' }}>
        <motion.button
          onClick={onNewHand}
          title={t.tooltips.newHand}
          whileHover={{ scale: 1.03 }}
          whileTap={{ scale: 0.97 }}
          style={{
            padding: 'clamp(8px,1vw,14px) clamp(20px,3vw,36px)',
            background: 'linear-gradient(180deg,#00C853 0%,#004D25 100%)',
            border: '1.5px solid rgba(0,230,118,0.3)', borderRadius: '8px',
            color: '#FFF', fontFamily: "'Cinzel',serif", fontWeight: 700,
            fontSize: 'clamp(10px,1.3vw,14px)', textTransform: 'uppercase',
            letterSpacing: '1.5px', cursor: 'pointer', minHeight: '44px',
            boxShadow: '0 2px 8px rgba(0,200,83,0.2)',
          }}
        >{t.newHand}</motion.button>

        <motion.button
          onClick={onBack}
          title={t.tooltips.back}
          whileHover={{ scale: 1.03 }}
          whileTap={{ scale: 0.97 }}
          style={{
            padding: 'clamp(8px,1vw,14px) clamp(20px,3vw,36px)',
            background: 'transparent',
            border: '1.5px solid rgba(212,168,67,0.3)', borderRadius: '8px',
            color: '#D4A843', fontFamily: "'Cinzel',serif", fontWeight: 700,
            fontSize: 'clamp(10px,1.3vw,14px)', textTransform: 'uppercase',
            letterSpacing: '1.5px', cursor: 'pointer', minHeight: '44px',
          }}
        >{t.back}</motion.button>
      </div>
    </motion.div>
  );
}
```

**Linhas:** ~160

---

## 5.2 Criar `components/games/blackjack/BlackjackSideBets.tsx`

**Arquivo NOVO - areas de side bet com estados:**

```tsx
// Blackout Casino - Blackjack (#4) - Side Bets Display
// Adendo DS 1.10 (PP esquerda, 21+3 direita, hover, has-bet, win/lose)
// 19/03/2026

'use client';
import React from 'react';
import { motion } from 'framer-motion';
import { bjTexts } from './texts';
import type { SideBetResult } from './types';

interface BlackjackSideBetsProps {
  betPP: number;
  onBetPP: (val: number) => void;
  bet21Plus3: number;
  onBet21Plus3: (val: number) => void;
  selectedChip: number;
  results?: { perfectPairs?: SideBetResult; twentyOnePlus3?: SideBetResult };
  lang: 'br' | 'en';
}

export default function BlackjackSideBets({
  betPP, onBetPP, bet21Plus3, onBet21Plus3, selectedChip, results, lang,
}: BlackjackSideBetsProps) {
  const t = bjTexts[lang];
  const isResultMode = !!results;

  const renderArea = (
    type: 'pp' | '21plus3',
    label: string,
    betVal: number,
    onBet: (val: number) => void,
    result?: SideBetResult,
    tooltipKey: 'perfectPairs' | 'twentyOnePlus3' = 'perfectPairs',
  ) => {
    const hasResult = result !== undefined;
    const isWin = hasResult && result.result === 'WIN';
    const isLose = hasResult && result.result === 'LOSE';
    const hasBet = betVal > 0;

    return (
      <motion.div
        onClick={() => { if (!isResultMode && selectedChip > 0) onBet(betVal + selectedChip); }}
        title={t.tooltips[tooltipKey]}
        whileHover={!isResultMode ? { scale: 1.05 } : {}}
        style={{
          position: 'absolute',
          top: '50%', transform: 'translateY(-50%)',
          ...(type === 'pp' ? { left: 'clamp(16px,3vw,32px)' } : { right: 'clamp(16px,3vw,32px)' }),
          width: 'clamp(56px,8vw,80px)', minHeight: 'clamp(56px,8vw,80px)',
          background: isWin ? 'rgba(0,230,118,0.03)' : hasBet ? 'rgba(0,230,118,0.03)' : 'rgba(212,168,67,0.04)',
          border: `1px solid ${isWin ? 'rgba(0,230,118,0.5)' : isLose ? 'rgba(255,255,255,0.05)' : hasBet ? 'rgba(0,230,118,0.3)' : 'rgba(212,168,67,0.15)'}`,
          borderRadius: '8px',
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          gap: '4px', cursor: isResultMode ? 'default' : 'pointer',
          opacity: isLose ? 0.4 : 1,
          animation: isWin ? 'bjSideBetGlow 1.5s ease infinite' : 'none',
          transition: 'all 0.25s cubic-bezier(0.25,0.46,0.45,0.94)',
          zIndex: 2,
        }}
      >
        <span style={{
          fontFamily: "'Cinzel',serif", fontWeight: 700,
          fontSize: 'clamp(8px,1vw,11px)', textTransform: 'uppercase',
          letterSpacing: '1px',
          color: isWin ? '#00E676' : hasBet ? '#00E676' : 'rgba(212,168,67,0.5)',
        }}>{label}</span>

        {hasBet && !hasResult && (
          <span style={{
            fontFamily: "'JetBrains Mono',monospace", fontWeight: 700,
            fontSize: 'clamp(9px,1.2vw,13px)', color: '#00E676',
          }}>G${betVal}</span>
        )}

        {isWin && result && (
          <>
            <span style={{
              fontFamily: "'Inter',sans-serif", fontWeight: 600,
              fontSize: 'clamp(7px,0.9vw,10px)', color: '#00E676',
            }}>{result.subResult}</span>
            <span style={{
              fontFamily: "'JetBrains Mono',monospace", fontWeight: 700,
              fontSize: 'clamp(11px,1.5vw,16px)', color: '#00E676',
              textShadow: '0 0 8px rgba(0,230,118,0.5)',
            }}>+G${result.payout}</span>
          </>
        )}

        {isLose && (
          <span style={{
            fontFamily: "'Inter',sans-serif", fontWeight: 500,
            fontSize: 'clamp(8px,1vw,10px)', color: '#666',
          }}>-</span>
        )}
      </motion.div>
    );
  };

  return (
    <>
      {renderArea('pp', t.perfectPairs, betPP, onBetPP, results?.perfectPairs, 'perfectPairs')}
      {renderArea('21plus3', t.twentyOnePlus3, bet21Plus3, onBet21Plus3, results?.twentyOnePlus3, 'twentyOnePlus3')}
    </>
  );
}
```

**Linhas:** ~80

---

## 5.3 Criar `components/games/blackjack/BlackjackInsurance.tsx`

**Arquivo NOVO - modal com timer SVG circular:**

```tsx
// Blackout Casino - Blackjack (#4) - Insurance Modal
// Adendo DS 1.19 (overlay blur, modal central, timer SVG, SIM/NAO)
// 19/03/2026

'use client';
import React, { useState, useEffect, useCallback } from 'react';
import { motion } from 'framer-motion';
import { bjTexts } from './texts';
import { BJ_SOUNDS } from './sounds';
import { useSoundManager } from '@/hooks/use-sound-manager';

interface BlackjackInsuranceProps {
  bet: number;
  onDecide: (accept: boolean) => void;
  lang: 'br' | 'en';
}

export default function BlackjackInsurance({ bet, onDecide, lang }: BlackjackInsuranceProps) {
  const t = bjTexts[lang];
  const sound = useSoundManager();
  const [timer, setTimer] = useState(10);
  const cost = Math.round(bet / 2);
  const isUrgent = timer <= 3;

  useEffect(() => {
    const interval = setInterval(() => {
      setTimer(prev => {
        if (prev <= 1) { onDecide(false); return 0; }
        if (prev <= 4) sound.play(BJ_SOUNDS.countdown_tick);
        return prev - 1;
      });
    }, 1000);
    return () => clearInterval(interval);
  }, [onDecide, sound]);

  // SVG circular timer
  const circumference = 2 * Math.PI * 20;
  const progress = (timer / 10) * circumference;

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      style={{
        position: 'absolute', inset: 0,
        background: 'rgba(0,0,0,0.7)',
        backdropFilter: 'blur(6px)', WebkitBackdropFilter: 'blur(6px)',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        zIndex: 70,
      }}
    >
      <motion.div
        initial={{ scale: 0.9, opacity: 0 }}
        animate={{ scale: 1, opacity: 1 }}
        transition={{ type: 'spring', stiffness: 300, damping: 25 }}
        style={{
          background: 'rgba(10,10,10,0.95)',
          border: '1.5px solid rgba(212,168,67,0.3)',
          borderRadius: '16px',
          padding: 'clamp(16px,3vw,32px)',
          minWidth: 'clamp(240px,40vw,360px)',
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', gap: 'clamp(10px,1.5vw,18px)',
          boxShadow: '0 8px 32px rgba(0,0,0,0.6),0 0 20px rgba(212,168,67,0.05)',
        }}
      >
        {/* Titulo */}
        <div style={{
          fontFamily: "'Cinzel',serif", fontWeight: 700,
          fontSize: 'clamp(16px,2.5vw,24px)', textTransform: 'uppercase',
          letterSpacing: '2px', color: '#D4A843',
          textShadow: '0 0 12px rgba(255,215,0,0.4)',
        }}>{t.insuranceTitle}</div>

        {/* Texto */}
        <div style={{
          fontFamily: "'Inter',sans-serif", fontWeight: 400,
          fontSize: 'clamp(10px,1.3vw,14px)', color: '#A8A8A8',
          textAlign: 'center', lineHeight: 1.5,
        }}>
          {t.insuranceText}
          <br />
          <span style={{ color: '#D4A843', fontWeight: 600 }}>
            {lang === 'br' ? 'Custo: ' : 'Cost: '}G${cost.toLocaleString('pt-BR')}
          </span>
        </div>

        {/* Timer SVG */}
        <div style={{ width: '48px', height: '48px', position: 'relative' }}>
          <svg width="48" height="48" viewBox="0 0 48 48">
            <circle cx="24" cy="24" r="20" fill="none"
              stroke="rgba(255,255,255,0.1)" strokeWidth="3" />
            <circle cx="24" cy="24" r="20" fill="none"
              stroke={isUrgent ? '#FF3B3B' : '#D4A843'}
              strokeWidth="3" strokeDasharray={circumference}
              strokeDashoffset={circumference - progress}
              strokeLinecap="round"
              style={{
                transform: 'rotate(-90deg)', transformOrigin: 'center',
                transition: 'stroke-dashoffset 1s linear, stroke 0.3s ease',
              }} />
          </svg>
          <div style={{
            position: 'absolute', inset: 0,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: "'JetBrains Mono',monospace", fontWeight: 700,
            fontSize: '14px',
            color: isUrgent ? '#FF3B3B' : '#FFFFFF',
            animation: isUrgent ? 'bjInsuranceUrgent 0.5s ease infinite' : 'none',
          }}>{timer}</div>
        </div>

        {/* Botoes */}
        <div style={{ display: 'flex', gap: 'clamp(8px,1.5vw,16px)' }}>
          <motion.button
            onClick={() => onDecide(true)}
            title={t.tooltips.insuranceYes}
            whileHover={{ scale: 1.03 }}
            whileTap={{ scale: 0.97 }}
            style={{
              padding: 'clamp(8px,1vw,14px) clamp(20px,3vw,36px)',
              background: 'linear-gradient(180deg,#00C853 0%,#004D25 100%)',
              border: '1.5px solid rgba(0,230,118,0.3)', borderRadius: '8px',
              color: '#FFF', fontFamily: "'Cinzel',serif", fontWeight: 700,
              fontSize: 'clamp(10px,1.3vw,14px)', textTransform: 'uppercase',
              letterSpacing: '1.5px', cursor: 'pointer', minHeight: '44px',
              boxShadow: '0 2px 8px rgba(0,200,83,0.2)',
            }}
          >{t.insuranceYes}</motion.button>

          <motion.button
            onClick={() => onDecide(false)}
            title={t.tooltips.insuranceNo}
            whileHover={{ scale: 1.03 }}
            whileTap={{ scale: 0.97 }}
            style={{
              padding: 'clamp(8px,1vw,14px) clamp(20px,3vw,36px)',
              background: 'transparent',
              border: '1.5px solid rgba(212,168,67,0.3)', borderRadius: '8px',
              color: '#D4A843', fontFamily: "'Cinzel',serif", fontWeight: 700,
              fontSize: 'clamp(10px,1.3vw,14px)', textTransform: 'uppercase',
              letterSpacing: '1.5px', cursor: 'pointer', minHeight: '44px',
            }}
          >{t.insuranceNo}</motion.button>
        </div>
      </motion.div>
    </motion.div>
  );
}
```

**Linhas:** ~100

---

## 5.4 Criar `components/games/blackjack/BlackjackHistory.tsx`

**Arquivo NOVO - lista de ultimas 20 maos:**

```tsx
// Blackout Casino - Blackjack (#4) - History
// Adendo DS / Prompt AI Tela 6 (historico com scroll, resultado colorido)
// 19/03/2026

'use client';
import React, { useState, useEffect } from 'react';
import { motion } from 'framer-motion';
import { bjTexts } from './texts';
import type { HistoryHand } from './types';

async function fetchNui<T>(endpoint: string, data?: object): Promise<T> {
  const res = await fetch(
    `https://${(window as any).GetParentResourceName?.() || 'blackout_casino'}/${endpoint}`,
    { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data || {}) }
  );
  return res.json();
}

interface BlackjackHistoryProps {
  onClose: () => void;
  lang: 'br' | 'en';
}

const RESULT_COLORS: Record<string, string> = {
  WIN: '#00E676', BLACKJACK: '#FFD700', PUSH: '#FFD700',
  LOSE: '#FF5252', BUST: '#FF1744', SURRENDER: '#D4A843',
};

export default function BlackjackHistory({ onClose, lang }: BlackjackHistoryProps) {
  const t = bjTexts[lang];
  const [hands, setHands] = useState<HistoryHand[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchNui<{ hands: HistoryHand[] }>('casino:bj:history').then(res => {
      setHands(res.hands || []);
      setLoading(false);
    });
  }, []);

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      style={{
        position: 'absolute', inset: 0,
        background: 'rgba(0,0,0,0.85)',
        backdropFilter: 'blur(8px)', WebkitBackdropFilter: 'blur(8px)',
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', padding: 'clamp(16px,3vw,32px)',
        zIndex: 80, overflow: 'auto',
      }}
    >
      {/* Header */}
      <div style={{ display: 'flex', justifyContent: 'space-between', width: '100%', maxWidth: '500px', marginBottom: 'clamp(12px,2vw,20px)' }}>
        <span style={{
          fontFamily: "'Cinzel',serif", fontWeight: 700,
          fontSize: 'clamp(14px,2vw,20px)', color: '#D4A843',
          letterSpacing: '2px', textTransform: 'uppercase',
        }}>{t.history}</span>
        <motion.button
          onClick={onClose}
          whileHover={{ scale: 1.05 }}
          whileTap={{ scale: 0.95 }}
          style={{
            background: 'rgba(0,0,0,0.4)', border: '1px solid rgba(212,168,67,0.3)',
            borderRadius: '6px', color: '#D4A843', padding: '4px 12px',
            fontFamily: "'Cinzel',serif", fontWeight: 700, fontSize: 'clamp(9px,1.1vw,12px)',
            cursor: 'pointer', minHeight: '44px',
          }}
        >{t.back}</motion.button>
      </div>

      {loading && <span style={{ color: '#A8A8A8' }}>{t.loading}</span>}

      {/* Hands list */}
      {hands.map((hand, idx) => (
        <motion.div
          key={hand.id}
          initial={{ y: 10, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: idx * 0.03 }}
          style={{
            width: '100%', maxWidth: '500px',
            background: 'rgba(255,255,255,0.03)',
            border: '1px solid rgba(212,168,67,0.1)',
            borderRadius: '8px', padding: 'clamp(8px,1.2vw,14px)',
            marginBottom: 'clamp(4px,0.5vw,8px)',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: '2px' }}>
            <span style={{
              fontFamily: "'Cinzel',serif", fontWeight: 700,
              fontSize: 'clamp(10px,1.3vw,14px)',
              color: RESULT_COLORS[hand.result] || '#A8A8A8',
            }}>{hand.result}</span>
            <span style={{
              fontFamily: "'Inter',sans-serif", fontWeight: 400,
              fontSize: 'clamp(8px,1vw,11px)', color: '#666',
            }}>
              {hand.player_total} vs {hand.dealer_total}
              {hand.was_doubled && ' (x2)'}
              {hand.was_split && ' (split)'}
            </span>
          </div>

          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-end', gap: '2px' }}>
            <span style={{
              fontFamily: "'JetBrains Mono',monospace", fontWeight: 700,
              fontSize: 'clamp(10px,1.3vw,14px)',
              color: hand.payout > hand.bet ? '#00E676' : hand.payout === hand.bet ? '#FFD700' : '#FF5252',
            }}>
              {hand.payout > 0 ? '+' : ''}G${hand.payout.toLocaleString('pt-BR')}
            </span>
            <span style={{
              fontFamily: "'Inter',sans-serif", fontWeight: 400,
              fontSize: 'clamp(7px,0.9vw,10px)', color: '#555',
            }}>{new Date(hand.created_at).toLocaleString('pt-BR', { hour: '2-digit', minute: '2-digit' })}</span>
          </div>
        </motion.div>
      ))}
    </motion.div>
  );
}
```

**Linhas:** ~90

---

## 5.5 Criar `components/games/blackjack/BlackjackProvablyFair.tsx`

**Arquivo NOVO - verificacao Provably Fair:**

```tsx
// Blackout Casino - Blackjack (#4) - Provably Fair
// Prompt AI Tela 7 (seed hash, verificacao, nova sessao)
// 19/03/2026

'use client';
import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { bjTexts } from './texts';
import type { VerifyData } from './types';

async function fetchNui<T>(endpoint: string, data?: object): Promise<T> {
  const res = await fetch(
    `https://${(window as any).GetParentResourceName?.() || 'blackout_casino'}/${endpoint}`,
    { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data || {}) }
  );
  return res.json();
}

interface BlackjackProvablyFairProps {
  seedHash: string;
  nonce: number;
  onClose: () => void;
  lang: 'br' | 'en';
}

export default function BlackjackProvablyFair({ seedHash, nonce, onClose, lang }: BlackjackProvablyFairProps) {
  const t = bjTexts[lang];
  const [verifyData, setVerifyData] = useState<VerifyData | null>(null);
  const [customSeed, setCustomSeed] = useState('');
  const [newHash, setNewHash] = useState('');

  const handleVerify = async () => {
    const verifyResult = await fetchNui<VerifyData>('casino:bj:verify', { sessionId: 0 });
    if (!('error' in verifyResult)) setVerifyData(verifyResult);
  };

  const handleNewSession = async () => {
    const sessionResult = await fetchNui<{ seedHash: string; previousServerSeed: string | null }>(
      'casino:bj:session:new', { clientSeed: customSeed || undefined }
    );
    if (!('error' in sessionResult)) {
      setNewHash(sessionResult.seedHash);
      if (sessionResult.previousServerSeed) {
        setVerifyData(prev => prev ? { ...prev, serverSeed: sessionResult.previousServerSeed! } : null);
      }
    }
  };

  const fieldStyle: React.CSSProperties = {
    width: '100%', padding: 'clamp(6px,1vw,10px)',
    background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(212,168,67,0.15)',
    borderRadius: '6px', fontFamily: "'JetBrains Mono',monospace",
    fontSize: 'clamp(8px,1vw,11px)', color: '#A8A8A8',
    wordBreak: 'break-all',
  };

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      style={{
        position: 'absolute', inset: 0,
        background: 'rgba(0,0,0,0.9)',
        backdropFilter: 'blur(8px)', WebkitBackdropFilter: 'blur(8px)',
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', padding: 'clamp(16px,3vw,32px)',
        gap: 'clamp(10px,1.5vw,16px)', zIndex: 80, overflow: 'auto',
      }}
    >
      {/* Header */}
      <div style={{ display: 'flex', justifyContent: 'space-between', width: '100%', maxWidth: '450px' }}>
        <span style={{
          fontFamily: "'Cinzel',serif", fontWeight: 700,
          fontSize: 'clamp(14px,2vw,20px)', color: '#00E676',
          letterSpacing: '2px', textTransform: 'uppercase',
        }}>{t.provablyFair}</span>
        <motion.button
          onClick={onClose}
          whileHover={{ scale: 1.05 }}
          whileTap={{ scale: 0.95 }}
          style={{
            background: 'rgba(0,0,0,0.4)', border: '1px solid rgba(212,168,67,0.3)',
            borderRadius: '6px', color: '#D4A843', padding: '4px 12px',
            fontFamily: "'Cinzel',serif", fontWeight: 700, fontSize: 'clamp(9px,1.1vw,12px)',
            cursor: 'pointer', minHeight: '44px',
          }}
        >{t.back}</motion.button>
      </div>

      {/* Current session hash */}
      <div style={{ width: '100%', maxWidth: '450px' }}>
        <label style={{ fontFamily: "'Inter',sans-serif", fontSize: 'clamp(9px,1.1vw,12px)', color: '#888', display: 'block', marginBottom: '4px' }}>
          {t.sessionHash}
        </label>
        <div style={fieldStyle}>{newHash || seedHash || '-'}</div>
      </div>

      <div style={{ width: '100%', maxWidth: '450px' }}>
        <label style={{ fontFamily: "'Inter',sans-serif", fontSize: 'clamp(9px,1.1vw,12px)', color: '#888', display: 'block', marginBottom: '4px' }}>
          {t.nonce}
        </label>
        <div style={fieldStyle}>{nonce}</div>
      </div>

      {/* Verificar sessao anterior */}
      <motion.button
        onClick={handleVerify}
        title={t.tooltips.provablyFair}
        whileHover={{ scale: 1.03 }}
        whileTap={{ scale: 0.97 }}
        style={{
          padding: 'clamp(8px,1vw,12px) clamp(16px,2.5vw,28px)',
          background: 'linear-gradient(180deg,#00C853 0%,#004D25 100%)',
          border: '1.5px solid rgba(0,230,118,0.3)', borderRadius: '8px',
          color: '#FFF', fontFamily: "'Cinzel',serif", fontWeight: 700,
          fontSize: 'clamp(9px,1.2vw,13px)', textTransform: 'uppercase',
          letterSpacing: '1.5px', cursor: 'pointer', minHeight: '44px',
        }}
      >{t.verifyTitle}</motion.button>

      {verifyData && (
        <div style={{ width: '100%', maxWidth: '450px', display: 'flex', flexDirection: 'column', gap: '8px' }}>
          <div>
            <label style={{ fontFamily: "'Inter',sans-serif", fontSize: 'clamp(9px,1.1vw,12px)', color: '#888', display: 'block', marginBottom: '4px' }}>
              {t.serverSeed}
            </label>
            <div style={fieldStyle}>{verifyData.serverSeed}</div>
          </div>
          <div>
            <label style={{ fontFamily: "'Inter',sans-serif", fontSize: 'clamp(9px,1.1vw,12px)', color: '#888', display: 'block', marginBottom: '4px' }}>
              {t.clientSeed}
            </label>
            <div style={fieldStyle}>{verifyData.clientSeed}</div>
          </div>
        </div>
      )}

      {/* Nova sessao */}
      <div style={{ width: '100%', maxWidth: '450px', display: 'flex', gap: '8px', alignItems: 'center' }}>
        <input
          value={customSeed}
          onChange={e => setCustomSeed(e.target.value)}
          placeholder={lang === 'br' ? 'Client seed personalizado...' : 'Custom client seed...'}
          style={{
            flex: 1, padding: 'clamp(8px,1vw,12px)',
            background: 'rgba(255,255,255,0.05)', border: '1px solid rgba(212,168,67,0.2)',
            borderRadius: '8px', fontFamily: "'JetBrains Mono',monospace",
            fontSize: 'clamp(9px,1.1vw,12px)', color: '#A8A8A8',
            outline: 'none', minHeight: '44px',
          }}
        />
        <motion.button
          onClick={handleNewSession}
          title={t.tooltips.newSession}
          whileHover={{ scale: 1.03 }}
          whileTap={{ scale: 0.97 }}
          style={{
            padding: 'clamp(8px,1vw,12px) clamp(12px,2vw,20px)',
            background: 'rgba(212,168,67,0.1)',
            border: '1.5px solid rgba(212,168,67,0.3)', borderRadius: '8px',
            color: '#D4A843', fontFamily: "'Cinzel',serif", fontWeight: 700,
            fontSize: 'clamp(9px,1.2vw,13px)', textTransform: 'uppercase',
            cursor: 'pointer', minHeight: '44px', whiteSpace: 'nowrap',
          }}
        >{t.newSession}</motion.button>
      </div>
    </motion.div>
  );
}
```

**Linhas:** ~100

---

## CHECKLIST FASE 5

```
[x] BlackjackResult.tsx: 5 variantes visuais (WIN/BUST/BLACKJACK/PUSH/SURRENDER)
[x] BlackjackResult.tsx: useCountup hook (ease-out quint 1.5s)
[x] BlackjackResult.tsx: shimmer sweep para BLACKJACK (::after animation)
[x] BlackjackResult.tsx: shake para BUST (x: [-3,3,-2,2,0])
[x] BlackjackResult.tsx: insurance result display (WIN verde / LOSE vermelho)
[x] BlackjackResult.tsx: split results individuais (se multiplas maos)
[x] BlackjackResult.tsx: botoes NOVA MAO + VOLTAR (DS 1.16 + 1.17 ghost)
[x] BlackjackSideBets.tsx: 2 areas (PP esquerda, 21+3 direita)
[x] BlackjackSideBets.tsx: 5 estados: vazio, hover, has-bet, win (glow), lose (fade)
[x] BlackjackSideBets.tsx: sub-result label (Mixed/Colored/Perfect, Flush/Straight/etc)
[x] BlackjackInsurance.tsx: modal central com overlay blur 6px
[x] BlackjackInsurance.tsx: timer SVG circular 10s com urgente <3s (vermelho pulsante)
[x] BlackjackInsurance.tsx: auto-decline ao expirar (onDecide(false))
[x] BlackjackInsurance.tsx: countdown_tick som nos ultimos 4s
[x] BlackjackHistory.tsx: fetchNui history -> lista 20 maos
[x] BlackjackHistory.tsx: resultado colorido + payout formatado + timestamp
[x] BlackjackHistory.tsx: stagger delay 0.03s por item
[x] BlackjackProvablyFair.tsx: seed hash atual + nonce display
[x] BlackjackProvablyFair.tsx: verificar sessao anterior (revela server seed)
[x] BlackjackProvablyFair.tsx: nova sessao com client seed personalizado
[x] Todos botoes: minHeight 44px (REGRA X4)
[x] clamp() em TODOS tamanhos (REGRA X4)
[x] REGRA X16: sem Unicode decorativo, variaveis contextuais

TODOS os itens da Fase 5: 23 de 23. Zero pendencias.
```

---

# =============================================
# RESUMO GERAL - ROTEIRO BLACKJACK (#4)
# =============================================

## Entrega por Fase

| Fase | Arquivos | Linhas | Funcao |
|:----:|----------|:------:|--------|
| 1 | sql/casino_blackjack_schema.sql | ~160 | 5 tabelas MySQL + seeds + 8 queries admin |
| 2 | server/handlers/blackjack_provably_fair.js | ~70 | HMAC-SHA256 cursor pattern, Fisher-Yates 312 cartas |
| 2 | server/handlers/blackjack_engine.js | ~130 | Logica pura: hand value, side bets, payouts |
| 2 | server/handlers/blackjack.js | ~350 | 8 endpoints com validacoes completas |
| 3 | client/blackjack_client.lua | ~45 | 8 NUI callbacks |
| 3 | components/games/blackjack/types.ts | ~80 | Tipos completos TS |
| 3 | components/games/blackjack/texts.ts | ~120 | Bilingue BR/EN + 18 tooltips |
| 3 | components/games/blackjack/sounds.ts | ~30 | 15 sons com hierarquia volume |
| 3 | BlackoutCasino.tsx (edicao cirurgica) | +6 | Import + bloco JSX |
| 4 | components/games/blackjack/BlackjackGame.tsx | ~250 | FSM entry point com 9 fases |
| 4 | components/games/blackjack/BlackjackTable.tsx | ~80 | Mesa feltro com textura e vinheta |
| 4 | components/games/blackjack/BlackjackCard.tsx | ~90 | Carta flip 3D CSS com -webkit- |
| 4 | components/games/blackjack/BlackjackHand.tsx | ~80 | Container mao + badge total 6 estados |
| 4 | components/games/blackjack/BlackjackControls.tsx | ~80 | 5 botoes acao individuais |
| 4 | components/games/blackjack/BlackjackChipSelector.tsx | ~80 | 5 chips + clear + bet display |
| 5 | components/games/blackjack/BlackjackResult.tsx | ~160 | 5 overlays + countup + confetti |
| 5 | components/games/blackjack/BlackjackSideBets.tsx | ~80 | PP + 21+3 areas com 5 estados |
| 5 | components/games/blackjack/BlackjackInsurance.tsx | ~100 | Modal timer SVG + SIM/NAO |
| 5 | components/games/blackjack/BlackjackHistory.tsx | ~90 | Ultimas 20 maos |
| 5 | components/games/blackjack/BlackjackProvablyFair.tsx | ~100 | Verificacao + nova sessao |
| | **TOTAL** | **~2.280** | **19 arquivos + 1 edicao cirurgica** |

## Estrutura de Pastas

```
sql/
  casino_blackjack_schema.sql

server/handlers/
  blackjack_provably_fair.js
  blackjack_engine.js
  blackjack.js

client/
  blackjack_client.lua

components/games/blackjack/
  types.ts
  texts.ts
  sounds.ts
  BlackjackGame.tsx
  BlackjackTable.tsx
  BlackjackCard.tsx
  BlackjackHand.tsx
  BlackjackControls.tsx
  BlackjackChipSelector.tsx
  BlackjackResult.tsx
  BlackjackSideBets.tsx
  BlackjackInsurance.tsx
  BlackjackHistory.tsx
  BlackjackProvablyFair.tsx

components/casino/
  BlackoutCasino.tsx (edicao cirurgica +6L)
```

## Validacao Final (REGRA X11)

- [x] TODOS os itens do roteiro implementados: 19 arquivos + 1 edicao = 20 de 20
- [x] Codigo compila sem erros de sintaxe (TS strict, JSX valido, Lua valido)
- [x] SQL executavel (copiar-colar MySQL funciona, testado 5.7+/8.0/MariaDB)
- [x] Cada componente React tem import, export, props tipados
- [x] Cada endpoint do handler tem validacao de input
- [x] Nenhum "TODO", "FIXME" ou placeholder no codigo

**VALIDACAO X11: 6 de 6 criterios atendidos.**

---

`[ENTREGA: 5.ROTEIRO-BLACKJACK-PARTE-5.md - 19/03/2026]`
