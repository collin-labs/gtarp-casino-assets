# ADENDO — ROTEIRO DE DESENVOLVIMENTO BLACKJACK (#4)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-BLACKJACK-PARTE-5.md — 23/03/2026

---

# 1. CONTEXTO

A Parte 5 cobre Fase 5 (Polish: HistoryModal, ProvablyFairModal, WinOverlays)
de forma mais descritiva. Este adendo fornece TSX real com tooltips inline.

---

# 2. FASE 5 — CODIGO REAL TSX

## 2.1 HistoryModal.tsx — Trechos com tooltips

```tsx
{history.map((hand, i) => (
  <motion.div
    key={hand.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: hand.won ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${hand.won ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    <span
      title={isBR
        ? (hand.result === "blackjack" ? "Blackjack natural 3:2" : hand.result === "push" ? "Empate" : hand.won ? "Vitoria" : hand.result === "bust" ? "Estourou" : "Derrota")
        : (hand.result === "blackjack" ? "Natural blackjack 3:2" : hand.result === "push" ? "Push" : hand.won ? "Win" : hand.result === "bust" ? "Bust" : "Lose")
      }
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(10px, 1vw, 14px)",
        fontWeight: 800,
        color: hand.result === "blackjack" ? "#FFD700" : hand.result === "push" ? "#A8A8A8" : hand.won ? "#00E676" : "#FF1744",
        minWidth: "36px",
        textAlign: "center",
      }}
    >
      {hand.result === "blackjack" ? "BJ" : hand.result === "push" ? "P" : hand.won ? "W" : hand.result === "bust" ? "B" : "L"}
    </span>

    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(9px, 0.9vw, 13px)", color: "rgba(212,168,67,0.5)" }}>
      {hand.playerScore} vs {hand.dealerScore}
    </span>

    <span style={{
      fontFamily: "var(--font-mono)",
      fontSize: "clamp(10px, 1vw, 14px)",
      fontWeight: 700,
      color: hand.won ? "#00E676" : hand.result === "push" ? "#A8A8A8" : "rgba(212,168,67,0.4)",
      marginLeft: "auto",
      textShadow: hand.won ? "0 0 8px rgba(0,230,118,0.4)" : "none",
    }}>
      {hand.won ? `+G$${hand.payout.toLocaleString()}` : hand.result === "push" ? "G$0" : `-G$${hand.bet.toLocaleString()}`}
    </span>

    <motion.button
      onClick={() => onVerify(hand.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={isBR ? "Verificar fairness desta mao" : "Verify this hand's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/shared/icons/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 2.2 ProvablyFairModal.tsx — Campos com copiar

```tsx
{(["serverSeedHash", "serverSeed", "clientSeed", "nonce", "deckShuffle"] as const).map((field) => (
  <div key={field} style={{ marginBottom: "clamp(8px, 0.8vw, 14px)" }}>
    <label style={{
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.8vw, 11px)",
      fontWeight: 600,
      color: "rgba(212,168,67,0.6)",
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      marginBottom: "4px",
      display: "block",
    }}>
      {pfLabels[field][isBR ? "br" : "en"]}
    </label>
    <div style={{ display: "flex", alignItems: "center", gap: "6px" }}>
      <input
        readOnly
        value={String(pfData[field])}
        style={{
          flex: 1,
          background: "rgba(0,0,0,0.4)",
          border: "1px solid rgba(212,168,67,0.2)",
          borderRadius: "6px",
          color: "#D4A843",
          fontFamily: "var(--font-mono)",
          fontSize: "clamp(9px, 0.9vw, 13px)",
          padding: "clamp(6px, 0.6vw, 10px)",
        }}
      />
      <motion.button
        onClick={() => handleCopy(field)}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        title={isBR ? "Copiar para area de transferencia" : "Copy to clipboard"}
        style={{
          background: copiedField === field ? "rgba(0,230,118,0.15)" : "rgba(212,168,67,0.08)",
          border: `1px solid ${copiedField === field ? "rgba(0,230,118,0.5)" : "rgba(212,168,67,0.3)"}`,
          borderRadius: "6px",
          padding: "clamp(6px, 0.6vw, 10px)",
          cursor: "pointer",
          minWidth: "44px",
          minHeight: "44px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        <img
          src={copiedField === field ? "/assets/shared/icons/icon-check.png" : "/assets/shared/icons/icon-copy.png"}
          alt=""
          style={{ width: "16px", height: "16px" }}
        />
      </motion.button>
    </div>
  </div>
))}
```

---

# 3. REGRAS DE ENTREGA COMPLEMENTO (item 5.11)

Complemento com 4 regras adicionais:

9. TODOS botoes de acao (HIT/STAND/DOUBLE/SPLIT/SURRENDER) com minHeight 44px
10. TODOS textos via texts[key][lang] — zero string hardcoded
11. Deck shuffle e deal SEMPRE server-authoritative (6-deck PF)
12. Zero console.log em componentes de producao

---

# 4. VALIDACAO

**5.11** Regras: +4 = 12 total. OK.
**5.14** TSX real: HistoryModal (hand list com badge resultado + verificar) + ProvablyFairModal (5 campos + copiar). OK.
**5.15** Tooltips: 7 inline (badge resultado contextual, verificar PF, copiar 5x). OK.

**VALIDACAO X17:** 3 itens exigidos, 3 entregues.

---

`[ENTREGA: ADENDO-ROTEIRO-BLACKJACK.md — 23/03/2026]`
