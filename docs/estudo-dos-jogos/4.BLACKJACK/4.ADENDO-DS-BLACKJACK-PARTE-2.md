# ADENDO DESIGN SYSTEM - BLACKJACK (#4)
## PARTE 2 DE 3 - Componentes CSS Real (1.9-1.15)
### 19/03/2026

---

# SECAO 1 - COMPONENTES COM CSS REAL (continuacao)

## 1.9 Bet Area Principal (DS Custom)

```css
/* COPIAR EXATAMENTE - area central de aposta na mesa */
.bj-bet-area {
  align-self: center;
  min-width: clamp(80px, 15vw, 140px);
  min-height: clamp(60px, 10vh, 100px);
  background: rgba(255,255,255,0.03);
  border: 1.5px dashed rgba(212,168,67,0.2);
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(4px, 0.5vw, 8px);
  padding: clamp(8px, 1vw, 12px);
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 2;
}

/* COPIAR - estado vazio (aguardando chips) */
.bj-bet-area--empty {
  border-color: rgba(212,168,67,0.15);
}
.bj-bet-area--empty::after {
  content: attr(data-placeholder);
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1.1vw, 12px);
  color: rgba(255,255,255,0.15);
  text-transform: uppercase;
  letter-spacing: 1px;
}

/* COPIAR - estado com chips posicionadas */
.bj-bet-area--active {
  border-color: rgba(0,230,118,0.3);
  border-style: solid;
  background: rgba(0,230,118,0.02);
}

/* COPIAR - valor da aposta exibido */
.bj-bet-area__value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
.bj-bet-area__value-prefix {
  font-size: 0.65em;
  opacity: 0.7;
  margin-right: 1px;
}

/* COPIAR - stack de chips visual dentro da bet area */
.bj-bet-area__stack {
  display: flex;
  flex-direction: column-reverse;
  align-items: center;
}
.bj-bet-area__stack .bj-chip--stacked + .bj-chip--stacked {
  margin-top: -4px;
}

/* COPIAR - animacao chip entrando na area */
/* Framer Motion: initial={{ y: 20, opacity: 0, scale: 0.8 }} */
/* animate={{ y: 0, opacity: 1, scale: 1 }} */
/* transition: { type: "spring", stiffness: 400, damping: 20 } */
/* Som: chip_place.mp3 */

/* COPIAR - doubled bet indicator (apos double down) */
.bj-bet-area--doubled {
  border-color: rgba(68,138,255,0.4);
  box-shadow: 0 0 12px rgba(68,138,255,0.15);
}
.bj-bet-area--doubled .bj-bet-area__value {
  color: #448AFF;
  text-shadow: 0 0 8px rgba(68,138,255,0.4);
}
```

---

## 1.10 Side Bet Area (DS P1 P4.4 + Custom)

```css
/* COPIAR EXATAMENTE - area de side bet (Perfect Pairs OU 21+3) */
.bj-sidebet {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: clamp(56px, 8vw, 80px);
  min-height: clamp(56px, 8vw, 80px);
  background: rgba(212,168,67,0.04);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;
  cursor: pointer;
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  z-index: 2;
}

/* COPIAR - posicao Perfect Pairs (esquerda) */
.bj-sidebet--pp {
  left: clamp(16px, 3vw, 32px);
}

/* COPIAR - posicao 21+3 (direita) */
.bj-sidebet--21plus3 {
  right: clamp(16px, 3vw, 32px);
}

/* COPIAR - label da side bet */
.bj-sidebet__label {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 11px);
  text-transform: uppercase;
  letter-spacing: 1px;
  color: rgba(212,168,67,0.5);
  transition: color 0.2s ease;
}

/* COPIAR - estados interativos */
.bj-sidebet:hover {
  border-color: rgba(212,168,67,0.4);
  background: rgba(212,168,67,0.08);
}
.bj-sidebet:hover .bj-sidebet__label {
  color: rgba(212,168,67,0.8);
}

/* COPIAR - com chip posicionada */
.bj-sidebet--has-bet {
  border-color: rgba(0,230,118,0.3);
  background: rgba(0,230,118,0.03);
}
.bj-sidebet--has-bet .bj-sidebet__label {
  color: #00E676;
}

/* COPIAR - resultado WIN */
.bj-sidebet--win {
  border-color: rgba(0,230,118,0.5);
  animation: bjSideBetGlow 1.5s ease infinite;
}
.bj-sidebet__payout {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.5vw, 16px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.5);
}
@keyframes bjSideBetGlow {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50% { box-shadow: 0 0 20px rgba(0,230,118,0.6); }
}

/* COPIAR - resultado LOSE */
.bj-sidebet--lose {
  border-color: rgba(255,255,255,0.05);
  opacity: 0.4;
}
.bj-sidebet__miss {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(8px, 1vw, 10px);
  color: #666666;
}

/* COPIAR - desabilitado (admin config off) */
.bj-sidebet--disabled {
  opacity: 0.2;
  pointer-events: none;
  cursor: default;
}
```

---

## 1.11 Botao HIT (DS P2 P8 + Custom)

```css
/* COPIAR EXATAMENTE - botao HIT (verde, acao principal) */
.bj-btn-action--hit {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px);
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-action--hit:hover {
  box-shadow: 0 4px 16px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.bj-btn-action--hit:active {
  transform: translateY(1px);
  box-shadow: 0 1px 4px rgba(0,200,83,0.15);
}
.bj-btn-action--hit[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}
```

---

## 1.12 Botao STAND (DS P2 P8 + Custom)

```css
/* COPIAR EXATAMENTE - botao STAND (dourado) */
.bj-btn-action--stand {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #D4A843 0%, #CB9B51 50%, #8B6914 100%);
  border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px);
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(212,168,67,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-action--stand:hover {
  box-shadow: 0 4px 16px rgba(212,168,67,0.3);
  transform: translateY(-1px);
}
.bj-btn-action--stand:active {
  transform: translateY(1px);
  box-shadow: 0 1px 4px rgba(212,168,67,0.15);
}
.bj-btn-action--stand[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}
```

---

## 1.13 Botao DOUBLE (DS P2 P8 + Custom)

```css
/* COPIAR EXATAMENTE - botao DOUBLE DOWN (azul) */
.bj-btn-action--double {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #448AFF 0%, #2962FF 50%, #1A237E 100%);
  border: 1.5px solid rgba(68,138,255,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px);
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(68,138,255,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-action--double:hover {
  box-shadow: 0 4px 16px rgba(68,138,255,0.3);
  transform: translateY(-1px);
}
.bj-btn-action--double:active {
  transform: translateY(1px);
  box-shadow: 0 1px 4px rgba(68,138,255,0.15);
}
.bj-btn-action--double[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}
```

---

## 1.14 Botao SPLIT (DS P2 P8 + Custom)

```css
/* COPIAR EXATAMENTE - botao SPLIT (roxo) */
.bj-btn-action--split {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #B388FF 0%, #7C4DFF 50%, #4A148C 100%);
  border: 1.5px solid rgba(179,136,255,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px);
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(179,136,255,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-action--split:hover {
  box-shadow: 0 4px 16px rgba(179,136,255,0.3);
  transform: translateY(-1px);
}
.bj-btn-action--split:active {
  transform: translateY(1px);
  box-shadow: 0 1px 4px rgba(179,136,255,0.15);
}
.bj-btn-action--split[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}
```

---

## 1.15 Botao SURRENDER (DS P2 P8 + Custom)

```css
/* COPIAR EXATAMENTE - botao SURRENDER (vermelho) */
.bj-btn-action--surrender {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #FF5252 0%, #D32F2F 50%, #B71C1C 100%);
  border: 1.5px solid rgba(255,82,82,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px);
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(255,82,82,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-action--surrender:hover {
  box-shadow: 0 4px 16px rgba(255,82,82,0.3);
  transform: translateY(-1px);
}
.bj-btn-action--surrender:active {
  transform: translateY(1px);
  box-shadow: 0 1px 4px rgba(255,82,82,0.15);
}
.bj-btn-action--surrender[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}
```

---

## 1.16 Botao DEAL - CTA Principal (DS P2 P8.1)

```css
/* COPIAR EXATAMENTE - botao DEAL (CTA verde premium) */
.bj-btn-deal {
  padding: clamp(10px, 1.5vw, 16px) clamp(24px, 4vw, 48px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 10px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  min-width: clamp(120px, 20vw, 200px);
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  overflow: hidden;
}

/* COPIAR - bevel highlight no topo */
.bj-btn-deal::before {
  content: '';
  position: absolute;
  top: 0; left: 10%; right: 10%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  pointer-events: none;
}

.bj-btn-deal:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3), inset 0 1px 0 rgba(255,255,255,0.15);
  transform: translateY(-1px);
}
.bj-btn-deal:active {
  transform: translateY(1px);
  box-shadow: 0 2px 8px rgba(0,200,83,0.15);
}
.bj-btn-deal[disabled] {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
  background: #1A1A1A;
  border-color: rgba(255,255,255,0.05);
  box-shadow: none;
}

/* COPIAR - botao NOVA MAO (pos-resultado, mesmo estilo) */
.bj-btn-new-hand {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-new-hand:hover {
  box-shadow: 0 4px 16px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.bj-btn-new-hand:active {
  transform: translateY(1px);
}
```

---

## 1.17 Botao VOLTAR (DS P3 P9.6)

```css
/* COPIAR EXATAMENTE - botao voltar padronizado Blackjack */
.bj-btn-back {
  position: absolute;
  top: clamp(8px, 1.5vw, 16px);
  left: clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 16px);
  letter-spacing: 2px;
  padding: clamp(6px, 0.8vw, 10px) clamp(10px, 1.5vw, 18px);
  min-height: 44px;
  cursor: pointer;
  z-index: 10;
  display: flex;
  align-items: center;
  gap: 5px;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-back:hover {
  border-color: rgba(212,168,67,0.8);
  transform: scale(1.05);
  box-shadow: 0 0 12px rgba(212,168,67,0.15);
}
.bj-btn-back:active {
  transform: scale(0.95);
}

/* COPIAR - VOLTAR ghost (no overlay de resultado) */
.bj-btn-back--ghost {
  position: static;
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  font-size: clamp(10px, 1.3vw, 14px);
}
.bj-btn-back--ghost:hover {
  border-color: rgba(212,168,67,0.6);
  background: rgba(212,168,67,0.05);
  transform: scale(1.05);
}
```

---

## 1.18 Saldo Display (DS P2 P15.2)

```css
/* COPIAR EXATAMENTE - saldo posicionado top-right */
.bj-balance {
  position: absolute;
  top: clamp(8px, 1.5vw, 16px);
  right: clamp(8px, 1.5vw, 16px);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(13px, 1.8vw, 20px);
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  z-index: 10;
  display: flex;
  align-items: baseline;
  gap: 2px;
  pointer-events: none;
}
.bj-balance__prefix {
  font-size: 0.65em;
  opacity: 0.7;
  margin-right: 1px;
}

/* COPIAR - flash ao ganhar */
.bj-balance--win-flash {
  animation: bjBalanceWin 0.8s ease;
}
@keyframes bjBalanceWin {
  0%, 100% { color: #00E676; text-shadow: 0 0 10px rgba(0,230,118,0.5); }
  25% { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.8); }
  50% { color: #00E676; text-shadow: 0 0 15px rgba(0,230,118,0.7); }
  75% { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.6); }
}

/* COPIAR - flash ao perder */
.bj-balance--lose-flash {
  animation: bjBalanceLose 0.5s ease;
}
@keyframes bjBalanceLose {
  0%, 100% { color: #00E676; }
  50% { color: rgba(255,59,59,0.8); }
}
```

---

## 1.19 Insurance Modal (DS P2 P12 + Custom)

```css
/* COPIAR EXATAMENTE - overlay escurecimento para insurance */
.bj-insurance-overlay {
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 70;
}

/* COPIAR - modal central */
.bj-insurance {
  background: rgba(10,10,10,0.95);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 16px;
  padding: clamp(16px, 3vw, 32px);
  min-width: clamp(240px, 40vw, 360px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(10px, 1.5vw, 18px);
  box-shadow: 0 8px 32px rgba(0,0,0,0.6), 0 0 20px rgba(212,168,67,0.05);
}

/* COPIAR - titulo SEGURO? */
.bj-insurance__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 24px);
  text-transform: uppercase;
  letter-spacing: 2px;
  color: #D4A843;
  text-shadow: 0 0 12px rgba(255,215,0,0.4);
}

/* COPIAR - texto explicativo */
.bj-insurance__text {
  font-family: 'Inter', sans-serif;
  font-weight: 400;
  font-size: clamp(10px, 1.3vw, 14px);
  color: #A8A8A8;
  text-align: center;
  line-height: 1.5;
}

/* COPIAR - timer circular SVG */
.bj-insurance__timer {
  width: 48px;
  height: 48px;
  position: relative;
}
.bj-insurance__timer-circle {
  fill: none;
  stroke: #D4A843;
  stroke-width: 3;
  stroke-dasharray: 126;
  stroke-linecap: round;
  transform: rotate(-90deg);
  transform-origin: center;
  transition: stroke-dashoffset 1s linear, stroke 0.3s ease;
}
.bj-insurance__timer-circle--urgent {
  stroke: #FF3B3B;
}
.bj-insurance__timer-value {
  position: absolute; inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 14px;
  color: #FFFFFF;
}
.bj-insurance__timer-value--urgent {
  color: #FF3B3B;
  animation: bjInsuranceUrgent 0.5s ease infinite;
}
@keyframes bjInsuranceUrgent {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* COPIAR - container botoes SIM/NAO */
.bj-insurance__buttons {
  display: flex;
  gap: clamp(8px, 1.5vw, 16px);
}

/* COPIAR - botao SIM (verde) */
.bj-insurance__btn-yes {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-insurance__btn-yes:hover {
  box-shadow: 0 4px 16px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.bj-insurance__btn-yes:active {
  transform: translateY(1px);
}

/* COPIAR - botao NAO (ghost dourado) */
.bj-insurance__btn-no {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-insurance__btn-no:hover {
  border-color: rgba(212,168,67,0.6);
  background: rgba(212,168,67,0.05);
}
.bj-insurance__btn-no:active {
  transform: translateY(1px);
}
```

---

**[FIM DA PARTE 2 - Continua Parte 3: Overlay Resultado (5 variantes), Atmosferas, Timelines, Checklist Final]**

`[ENTREGA: 4.ADENDO-DS-BLACKJACK-PARTE-2.md - 19/03/2026]`
