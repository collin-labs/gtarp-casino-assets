# ADENDO DESIGN SYSTEM — PAINEL FLUTUANTE BLACKOUT CASINO
## PARTE 2 DE 4 — Componentes Interativos CSS Real (Secoes 8-14, 7 componentes)
## Referencias diretas ao DS com CSS real — Complemento ao Prompt AI
### Blackout Casino GTARP — 23/03/2026

---

# SECAO 8 — DOCK / TABS 5 ABAS (DS P2 §8 Botoes adaptado + DS P1 §5.1 Bordas)

O Dock e a barra de navegacao fixa na borda inferior do painel. 5 abas (CASSINO, ARCADE, PVP, LOJA, EVENTOS) sobrepostas a uma imagem PNG ornamentada. Indicador ativo com Framer Motion layoutId que DESLIZA entre abas com spring physics.

```css
/* COPIAR EXATAMENTE — container do dock */
.panel-dock {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translate(-50%, 42%);  /* 42% fora do painel */
  width: 60%;
  z-index: 20;
}

/* COPIAR — imagem PNG ornamentada de fundo do dock */
.panel-dock__bg {
  width: 100%;
  height: auto;
  display: block;
  pointer-events: none;
  filter: drop-shadow(0 0 20px rgba(212, 168, 67, 0.35));
}

/* COPIAR — grid de 5 tabs sobrepostos na imagem */
.panel-dock__tabs {
  position: absolute;
  top: 50%;
  left: 8%;
  right: 8%;
  transform: translateY(-20%);
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  align-items: center;
}

/* COPIAR — tab individual base (todos os 5) */
.panel-dock__tab {
  background: transparent;
  border: none;
  cursor: pointer;
  text-align: center;
  padding: 0;
  padding-top: 6px;
  font-family: 'Cinzel', 'Playfair Display', serif;
  font-size: clamp(9px, 1.1vw, 16px);
  font-weight: 900;
  letter-spacing: 2px;
  position: relative;
  transition: color 0.3s ease, text-shadow 0.3s ease;
  min-height: 44px;  /* touch target X4 */
}

/* ---- 5 ESTADOS DO TAB ---- */

/* Estado 1: TAB ATIVA (selecionada) */
.panel-dock__tab--active {
  color: #00E676;
  text-shadow:
    0 0 12px rgba(0, 230, 118, 0.7),
    0 0 25px rgba(0, 230, 118, 0.35);
  cursor: default;
}

/* Estado 2: TAB INATIVA (nao selecionada, nao hover) */
.panel-dock__tab--inactive {
  color: #D4A843;
  text-shadow: 0 0 4px rgba(212, 168, 67, 0.3);
}

/* Estado 3: TAB INATIVA HOVER (mouse sobre tab nao selecionada) */
.panel-dock__tab--inactive:hover {
  color: #FFD700;
  text-shadow:
    0 0 10px rgba(255, 215, 0, 0.5),
    0 0 20px rgba(255, 215, 0, 0.25);
}

/* Estado 4: TAB ATIVA HOVER (mouse sobre tab ja selecionada — sem mudanca visual) */
.panel-dock__tab--active:hover {
  /* Mesmo visual que --active — nao muda */
  color: #00E676;
  text-shadow:
    0 0 12px rgba(0, 230, 118, 0.7),
    0 0 25px rgba(0, 230, 118, 0.35);
}

/* Estado 5: TAB DISABLED (quando jogo esta ativo ou modal aberto) */
.panel-dock__tab--disabled {
  color: #666666;
  text-shadow: none;
  opacity: 0.4;
  pointer-events: none;
  cursor: not-allowed;
}

/* COPIAR — indicador ativo: barra verde que desliza com layoutId */
.panel-dock__active-bar {
  position: absolute;
  top: -2px;
  left: 15%;
  right: 15%;
  height: 3px;
  background: linear-gradient(
    90deg,
    transparent,
    #00E676,
    transparent
  );
  box-shadow:
    0 0 10px rgba(0, 230, 118, 0.6),
    0 2px 8px rgba(0, 230, 118, 0.3);
  border-radius: 0 0 2px 2px;
}
```

**Framer Motion — layoutId spring (COPIAR EXATAMENTE):**
```tsx
{i === activeTab && (
  <motion.div
    layoutId="dock-active-bar"
    className="panel-dock__active-bar"
    transition={{ type: "spring", stiffness: 500, damping: 30 }}
  />
)}
```

**Framer Motion — tab hover/tap:**
```tsx
<motion.button
  className={`panel-dock__tab panel-dock__tab--${i === activeTab ? "active" : "inactive"}`}
  whileHover={{ scale: 1.08 }}
  whileTap={{ scale: 0.95 }}
  onClick={() => setActiveTab(i)}
  title={tooltips[i]}
>
  {/* layoutId bar */}
  {tab}
</motion.button>
```

**§ Referencias DS:**
- §8 Botoes — tab como botao com 4+ estados visuais, min-height 44px, transition 0.3s
- §5.1 Bordas — barra verde com linear-gradient transparente nas pontas
- §7.2 Glow Verde — box-shadow verde em 2 camadas na barra ativa
- §3 Tipografia — Cinzel uppercase, letter-spacing 2px

---

# SECAO 9 — GAMECARD 7 CAMADAS (DS P1 §4.4 + §5.1 + §7.1 + §7.2)

O GameCard e o componente visual mais elaborado do painel. Cada card renderiza 7 camadas de efeitos sobrepostos. O DS §4.4 define a base do card, e as camadas adicionais sao extensoes do DS para o nivel premium do Blackout Casino.

```css
/* COPIAR EXATAMENTE — container do card (camada 1: borda dupla) */
.panel-card {
  position: relative;
  width: 100%;
  height: 100%;
  border-radius: 14px;
  overflow: hidden;
  cursor: pointer;
  transition: border 0.3s ease, box-shadow 0.3s ease, outline 0.3s ease;
}

/* ---- 4 ESTADOS DO CARD ---- */

/* Estado 1: IDLE (nao hover, nao pressionado) */
.panel-card--idle {
  border: 2px solid rgba(212, 168, 67, 0.3);
  outline: 1.5px solid rgba(191, 149, 63, 0.1);
  outline-offset: 2px;
  box-shadow:
    0 4px 15px rgba(0, 0, 0, 0.4),
    0 1px 3px rgba(0, 0, 0, 0.3);
}

/* Estado 2: HOVER (mouse sobre o card) */
.panel-card--hover,
.panel-card--idle:hover {
  border: 2px solid rgba(212, 168, 67, 0.7);
  outline: 1.5px solid rgba(0, 230, 118, 0.4);
  outline-offset: 4px;
  box-shadow:
    0 8px 30px rgba(0, 0, 0, 0.5),
    0 0 20px rgba(0, 230, 118, 0.15);
}

/* Estado 3: PRESSED (clicando — whileTap) */
.panel-card--pressed {
  border: 2px solid rgba(255, 215, 0, 0.8);
  outline: 1.5px solid rgba(255, 215, 0, 0.5);
  outline-offset: 2px;
  box-shadow:
    0 2px 8px rgba(0, 0, 0, 0.4),
    0 0 15px rgba(255, 215, 0, 0.2);
  transform: scale(0.98);
}

/* Estado 4: DISABLED (quando modal ou jogo esta aberto) */
.panel-card--disabled {
  border: 2px solid rgba(212, 168, 67, 0.1);
  outline: none;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
  pointer-events: none;
  filter: brightness(0.7);
}

/* COPIAR — camada 2: imagem de fundo */
.panel-card__img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 12px;
}

/* COPIAR — camada 3: gradiente escurecimento topo (DS §4.4 overlay) */
.panel-card__grad-top {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 55%;
  background: linear-gradient(
    to bottom,
    rgba(0, 0, 0, 0.8) 0%,
    rgba(0, 0, 0, 0.3) 60%,
    transparent 100%
  );
  border-radius: 12px 12px 0 0;
  pointer-events: none;
  z-index: 2;
}

/* COPIAR — camada 4: gradiente escurecimento base */
.panel-card__grad-bot {
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 55%;
  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.8) 0%,
    rgba(0, 0, 0, 0.3) 50%,
    transparent 100%
  );
  border-radius: 0 0 12px 12px;
  pointer-events: none;
  z-index: 2;
}

/* Camadas 5, 6, 7 — ver SECAO 10 (Feixo + Linha + Shine) */

/* COPIAR — camada 7a: spotlight mouse-tracking */
.panel-card__spotlight {
  position: absolute;
  inset: 0;
  border-radius: 12px;
  pointer-events: none;
  z-index: 5;
  opacity: 0;
  transition: opacity 0.3s ease;
  /* background definido via JS:
     radial-gradient(circle 120px at ${mouseX}% ${mouseY}%,
       rgba(0, 230, 118, 0.2),
       rgba(255, 215, 0, 0.06) 40%,
       transparent 70%) */
}

.panel-card--idle:hover .panel-card__spotlight,
.panel-card--hover .panel-card__spotlight {
  opacity: 1;
}

/* COPIAR — logo bilingue sobre o card */
.panel-card__logo {
  position: absolute;
  top: clamp(6px, 1vw, 12px);
  left: 50%;
  transform: translateX(-50%);
  width: clamp(50px, 8vw, 100px);
  filter: drop-shadow(0 2px 6px rgba(0, 0, 0, 0.9));
  pointer-events: none;
  z-index: 6;
}

/* COPIAR — nome do jogo (sobre o gradiente base) */
.panel-card__name {
  position: absolute;
  bottom: clamp(8px, 1.2vw, 16px);
  left: 50%;
  transform: translateX(-50%);
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 16px);
  color: #FFFFFF;
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.8);
  text-transform: uppercase;
  letter-spacing: 1px;
  white-space: nowrap;
  z-index: 6;
  pointer-events: none;
}
```

**§ Referencias DS:**
- §4.4 Card de Jogo — "background linear-gradient, border rgba(212,168,67,0.2), border-radius 8px, box-shadow multi-layer, hover com border-color + transform"
- §5.1 Borda Dourada — "border 1px solid rgba(212,168,67,0.3), outline offset"
- §7.1 Glow Dourado — box-shadow hover com rgba dourado
- §7.2 Glow Verde — outline verde no hover, box-shadow verde

---

# SECAO 10 — FEIXO VERDE + LINHA DOURADA + SHINE SWEEP (DS P1 §5 + §7 — 3 efeitos isolados)

Estes 3 efeitos sao OBRIGATORIOS em todo GameCard (item 4.12 do checklist). Documentados como blocos CSS isolados para reutilizacao em qualquer componente.

```css
/* ============================================================
   EFEITO 1: FEIXO VERDE (base do card — permanente)
   DS §7.2 Glow Verde + §5.3 Separadores
   ============================================================ */

/* COPIAR EXATAMENTE — feixo verde permanente na base do card */
.panel-card__feixo {
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(
    90deg,
    transparent 5%,
    rgba(0, 230, 118, 0.3) 20%,
    #00E676 50%,
    rgba(0, 230, 118, 0.3) 80%,
    transparent 95%
  );
  box-shadow: 0 0 8px rgba(0, 230, 118, 0.3);
  border-radius: 0 0 12px 12px;
  pointer-events: none;
  z-index: 4;
  transition: box-shadow 0.3s ease;
}

/* COPIAR — feixo intensificado no hover */
.panel-card--idle:hover .panel-card__feixo,
.panel-card--hover .panel-card__feixo {
  box-shadow:
    0 0 15px rgba(0, 230, 118, 0.5),
    0 0 30px rgba(0, 230, 118, 0.2);
}

/* ============================================================
   EFEITO 2: LINHA DOURADA (topo do card — aparece sutil)
   DS §5.3 Separadores — gradiente transparente→dourado→transparente
   ============================================================ */

/* COPIAR EXATAMENTE — linha dourada no topo do card */
.panel-card__gold-line {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent 10%,
    rgba(212, 168, 67, 0.3) 30%,
    rgba(255, 215, 0, 0.5) 50%,
    rgba(212, 168, 67, 0.3) 70%,
    transparent 90%
  );
  pointer-events: none;
  z-index: 4;
  opacity: 0.5;
  transition: opacity 0.3s ease;
}

/* COPIAR — linha intensifica no hover */
.panel-card--idle:hover .panel-card__gold-line,
.panel-card--hover .panel-card__gold-line {
  opacity: 1.0;
}

/* ============================================================
   EFEITO 3: SHINE SWEEP (percorre card horizontalmente)
   DS P3A §20.4 Micro-interacoes — shimmer/shine animation
   ============================================================ */

/* COPIAR EXATAMENTE — shine sweep no card */
.panel-card__shine {
  position: absolute;
  inset: 0;
  border-radius: 12px;
  background: linear-gradient(
    105deg,
    transparent 40%,
    rgba(255, 215, 0, 0.06) 45%,
    rgba(255, 215, 0, 0.12) 50%,
    rgba(255, 215, 0, 0.06) 55%,
    transparent 60%
  );
  background-size: 200% 100%;
  animation: panelCardShine 2s ease infinite;
  pointer-events: none;
  z-index: 5;
}

@keyframes panelCardShine {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

**§ Referencias DS:**
- §7.2 Glow Verde — "box-shadow rgba(0,230,118,0.3) em 3 camadas". O feixo usa linear-gradient para efeito de "barra neon" ao inves de glow difuso
- §5.3 Separadores — "linear-gradient 90deg transparent→dourado→transparent". A linha dourada segue exatamente o padrao do DS
- §20.4 Micro-interacoes — shimmer/shine com background-size 200% e animation. O angulo de 105deg (ao inves de 90deg) cria efeito diagonal mais dinamico

---

# SECAO 11 — HEROCAROUSEL (DS P1 §4.4 adaptado + DS P2 §8.1 botoes)

Carrossel hero com imagem de jogo em formato pedestal (800×600, fundo transparente). Arrows para navegar. Direction-aware animation (Framer Motion). Botao JOGAR AGORA com imagem PNG.

```css
/* COPIAR EXATAMENTE — container do hero */
.panel-hero {
  border-radius: 16px;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 0 20px rgba(212, 168, 67, 0.15),
    inset 0 0 25px rgba(0, 0, 0, 0.4),
    0 8px 25px rgba(0, 0, 0, 0.5);
  width: 100%;
  height: 100%;
}

/* COPIAR — imagem pedestal centralizada */
.panel-hero__pedestal {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
}

/* COPIAR — logo bilingue sobreposto no topo */
.panel-hero__logo {
  position: absolute;
  top: clamp(8px, 1.2vw, 16px);
  left: 50%;
  transform: translateX(-50%);
  width: clamp(80px, 12vw, 160px);
  filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.8));
  z-index: 3;
  pointer-events: none;
}

/* COPIAR — arrows de navegacao: 3 estados */
.panel-hero__arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: clamp(28px, 3vw, 40px);
  height: clamp(28px, 3vw, 40px);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: clamp(12px, 1.5vw, 18px);
  cursor: pointer;
  min-height: 44px;  /* touch target X4 */
  min-width: 44px;
  z-index: 4;
  transition: all 0.25s ease;
}

.panel-hero__arrow--left  { left: clamp(8px, 1vw, 14px); }
.panel-hero__arrow--right { right: clamp(8px, 1vw, 14px); }

/* Arrow estado 1: IDLE */
.panel-hero__arrow--idle {
  background: rgba(0, 0, 0, 0.5);
  border: 1px solid rgba(212, 168, 67, 0.3);
  color: #D4A843;
}

/* Arrow estado 2: HOVER */
.panel-hero__arrow--idle:hover {
  background: rgba(0, 0, 0, 0.8);
  border-color: #FFD700;
  color: #FFD700;
  box-shadow: 0 0 10px rgba(255, 215, 0, 0.2);
}

/* Arrow estado 3: ACTIVE (pressionando) */
.panel-hero__arrow--idle:active {
  transform: translateY(-50%) scale(0.9);
  box-shadow: 0 0 5px rgba(255, 215, 0, 0.1);
}

/* COPIAR — botao JOGAR AGORA (imagem PNG) */
.panel-hero__play-btn {
  position: absolute;
  bottom: clamp(12px, 2vw, 24px);
  left: 50%;
  transform: translateX(-50%);
  width: clamp(100px, 14vw, 180px);
  cursor: pointer;
  z-index: 4;
  transition: all 0.25s ease;
}

/* Play btn estado 1: IDLE */
.panel-hero__play-btn--idle {
  filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.5));
}

/* Play btn estado 2: HOVER */
.panel-hero__play-btn--idle:hover {
  filter:
    brightness(1.15)
    drop-shadow(0 0 8px rgba(255, 215, 0, 0.4))
    drop-shadow(0 2px 8px rgba(0, 0, 0, 0.5));
}

/* Play btn estado 3: ACTIVE */
.panel-hero__play-btn--idle:active {
  transform: translateX(-50%) scale(0.95);
}
```

**HeroBeams — raios de luz dourada atras do hero (DS §6.1 adaptado):**
```css
/* COPIAR EXATAMENTE — raios de luz dourada no fundo do hero */
.panel-hero__beams {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  overflow: hidden;
}

/* 3 radial-gradients posicionados como raios */
.panel-hero__beams::before {
  content: '';
  position: absolute;
  top: -20%;
  left: 50%;
  transform: translateX(-50%);
  width: 120%;
  height: 80%;
  background:
    radial-gradient(ellipse 30% 60% at 30% 0%, rgba(212, 168, 67, 0.06) 0%, transparent 70%),
    radial-gradient(ellipse 25% 50% at 70% 10%, rgba(255, 215, 0, 0.04) 0%, transparent 70%),
    radial-gradient(ellipse 40% 40% at 50% 5%, rgba(212, 168, 67, 0.05) 0%, transparent 60%);
  pointer-events: none;
}
```

**HeroBrokenBorder — borda decorativa segmentada (DS §5.2 Cantos adaptado):**
```css
/* COPIAR EXATAMENTE — borda quebrada ornamental no hero */
.panel-hero__broken-border {
  position: absolute;
  inset: 0;
  border-radius: 16px;
  pointer-events: none;
  z-index: 2;
}

/* 4 segmentos de borda com gaps */
.panel-hero__broken-border::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 16px;
  border: 1.5px solid rgba(212, 168, 67, 0.2);
  mask-image: linear-gradient(
    90deg,
    transparent 0%, #000 5%, #000 20%, transparent 25%,
    transparent 35%, #000 40%, #000 60%, transparent 65%,
    transparent 75%, #000 80%, #000 95%, transparent 100%
  );
  -webkit-mask-image: linear-gradient(
    90deg,
    transparent 0%, #000 5%, #000 20%, transparent 25%,
    transparent 35%, #000 40%, #000 60%, transparent 65%,
    transparent 75%, #000 80%, #000 95%, transparent 100%
  );
}
```

**§ Referencias DS:**
- §4.4 Card — container com border-radius, box-shadow, overflow hidden
- §8.1 Botao Primario — play-btn com hover brightness, active scale, min-height 44px
- §6.1 Spotlight — radial-gradients para raios de luz
- §5.2 Cantos Ornamentais — borda segmentada adaptada com mask-image

---

# SECAO 12 — BENTOGRID 5 LAYOUTS (DS P1 §4.2 Container adaptado)

Cinco layouts CSS Grid distintos, um por aba. Cada layout e uma funcao interna do BentoGrid que posiciona cards + hero de forma unica.

```css
/* COPIAR EXATAMENTE — padding padrao do grid (GP = grid padding) */
:root {
  --panel-grid-padding: clamp(10px, 1.5vw, 20px);
  --panel-grid-gap: clamp(10px, 1.2vw, 20px);
}

/* Layout 1: CASINO (aba 0) — hero 50% + 4 cards 2x2 + 2 banners slim */
.panel-grid--casino {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  grid-template-rows: 1fr 1fr 0.65fr;
  gap: var(--panel-grid-gap);
  width: 100%; height: 100%;
  padding: var(--panel-grid-padding);
}
/* Hero: grid-column 1/3, grid-row 1/3 */
/* Cards: col 3-4, row 1-2 (2x2) */
/* Banners: col 1/3 e 3/5, row 3 */

/* Layout 2: ARCADE (aba 1) — hero 60% + 4 cards 2x2 */
.panel-grid--arcade {
  display: grid;
  grid-template-columns: 1.4fr 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: var(--panel-grid-gap);
  width: 100%; height: 100%;
  padding: var(--panel-grid-padding);
}
/* Hero: grid-column 1, grid-row 1/3 */
/* Cards: col 2-3, row 1-2 (2x2) */

/* Layout 3: PVP (aba 2) — 3 cards grandes lado a lado */
.panel-grid--pvp {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--panel-grid-gap);
  width: 100%; height: 100%;
  padding: var(--panel-grid-padding);
  align-items: center;
}

/* Layout 4: LOJA (aba 3) — 4 cards grid 2x2 */
.panel-grid--loja {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: var(--panel-grid-gap);
  width: 100%; height: 100%;
  padding: var(--panel-grid-padding);
}

/* Layout 5: EVENTOS (aba 4) — ver SpotlightLayout separado */
.panel-grid--eventos {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: clamp(16px, 2.5vw, 40px);
  padding: clamp(12px, 2vw, 28px);
  width: 100%; height: 100%;
  align-items: center;
  justify-items: center;
}

/* COPIAR — padding bottom para espaco do dock em TODOS os layouts */
.panel-grid {
  padding-bottom: clamp(55px, 8vh, 110px);
  box-sizing: border-box;
}
```

**§ Referencia DS:** §4.2 Container Primario — "display flex/grid, padding, gap". Os 5 layouts sao extensoes do padrao de container do DS, cada um adaptado para a quantidade e hierarquia de cards da aba.

---

# SECAO 13 — GAMEMODAL GLASSMORPHISM (DS P1 §4.3 + DS P2 §12)

Modal "JOGAR AGORA" que abre ao clicar em um card. Glassmorphism com backdrop-filter. Canvas de particulas internas. Imagem dourada do jogo (goldUrl). Botao JOGAR AGORA com imagem PNG.

```css
/* COPIAR EXATAMENTE — overlay escurecido do modal (DS §4.3 adaptado) */
.panel-modal__overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.65);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  z-index: 50;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: inherit;
}

/* COPIAR — container glassmorphism do modal (DS §4.3 + §12.1) */
.panel-modal__container {
  position: relative;
  width: clamp(280px, 45vw, 600px);
  max-height: 85%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(12px, 2vw, 24px);
  padding: clamp(20px, 3vw, 40px);
  /* SEM background — modal e "flutuante" sobre o overlay blur */
}

/* COPIAR — imagem dourada do jogo (goldUrl) */
.panel-modal__gold-img {
  width: clamp(160px, 25vw, 350px);
  height: auto;
  object-fit: contain;
  filter:
    drop-shadow(0 0 20px rgba(212, 168, 67, 0.4))
    drop-shadow(0 0 40px rgba(212, 168, 67, 0.15));
  position: relative;
  z-index: 2;
}

/* COPIAR — logo bilingue no topo do modal */
.panel-modal__logo {
  width: clamp(100px, 15vw, 200px);
  filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.8));
  position: relative;
  z-index: 2;
}

/* COPIAR — canvas de particulas internas (diferente do GoldParticles do painel) */
.panel-modal__particles {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  /* 100 particulas burst do centro (w/2, h*0.35)
     5 cores: rgba(255,215,0), rgba(212,168,67), rgba(0,230,118), rgba(255,240,200), rgba(180,140,50)
     Velocidade radial: cos(angle)*speed, sin(angle)*speed, speed 0.8-4.3
     Damping: vx *= 0.988, vy *= 0.988
     Glow central pulsante: radialGradient sin(time*0.025) */
}

/* COPIAR — botao JOGAR AGORA (imagem PNG, 3 estados) */
.panel-modal__play-btn {
  width: clamp(120px, 18vw, 220px);
  cursor: pointer;
  position: relative;
  z-index: 2;
  transition: all 0.25s ease;
}

.panel-modal__play-btn--idle {
  filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.5));
}

.panel-modal__play-btn--idle:hover {
  filter:
    brightness(1.15)
    drop-shadow(0 0 8px rgba(255, 215, 0, 0.4))
    drop-shadow(0 2px 8px rgba(0, 0, 0, 0.5));
}

.panel-modal__play-btn--idle:active {
  transform: scale(0.95);
}
```

**§ Referencias DS:**
- §4.3 Container Elevado — "backdrop-filter blur(16px), border rgba(212,168,67,0.15), box-shadow". O modal usa blur(4px) mais leve porque o fundo ja tem o bg-casino.png (blur pesado em imagem = custo GPU alto no CEF)
- §12.1 Modal Padrao — "display flex, flex-direction column, align-items center, gap, padding, border-radius 12px"
- §7.1 Glow Dourado — drop-shadow na imagem dourada

---

# SECAO 14 — DEPOSITMODAL GLASSMORPHISM (DS P1 §4.3 + DS P2 §9.1 + §8.1 + §12)

Modal de deposito/saque GCoin. Glassmorphism com container solido (diferente do GameModal que e flutuante). Tabs DEPOSITAR/SACAR. Input de valor com atalhos. Botao CONFIRMAR com 4 estados.

```css
/* COPIAR EXATAMENTE — overlay glassmorphism (DS §4.3) */
.panel-deposit__overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  z-index: 55;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: inherit;
}

/* COPIAR — container glassmorphism solido (DS §4.3 + §4.2) */
.panel-deposit__container {
  width: clamp(280px, 40vw, 480px);
  background: rgba(10, 10, 10, 0.95);
  border: 1.5px solid rgba(212, 168, 67, 0.3);
  border-radius: 16px;
  padding: clamp(16px, 2.5vw, 32px);
  display: flex;
  flex-direction: column;
  gap: clamp(12px, 1.8vw, 20px);
  box-shadow:
    0 0 30px rgba(0, 0, 0, 0.6),
    0 0 15px rgba(212, 168, 67, 0.1),
    inset 0 1px 0 rgba(212, 168, 67, 0.08);
}

/* Brilho sutil no topo (DS §4.2 — ::before) */
.panel-deposit__container::before {
  content: '';
  position: absolute;
  top: 0; left: 10%; right: 10%;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(212, 168, 67, 0.4) 30%,
    rgba(246, 226, 122, 0.6) 50%,
    rgba(212, 168, 67, 0.4) 70%,
    transparent 100%
  );
  pointer-events: none;
}

/* COPIAR — tabs DEPOSITAR / SACAR (3 estados cada) */
.panel-deposit__tabs {
  display: grid;
  grid-template-columns: 1fr 1fr;
  border-radius: 8px;
  overflow: hidden;
  border: 1px solid rgba(212, 168, 67, 0.2);
}

.panel-deposit__tab {
  padding: clamp(8px, 1vw, 12px);
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  border: none;
  text-align: center;
  min-height: 44px;
  transition: all 0.25s ease;
}

/* Tab ativa */
.panel-deposit__tab--active {
  background: rgba(212, 168, 67, 0.15);
  color: #FFD700;
  border-bottom: 2px solid #FFD700;
}

/* Tab inativa */
.panel-deposit__tab--inactive {
  background: transparent;
  color: #666666;
  border-bottom: 2px solid transparent;
}

/* Tab inativa hover */
.panel-deposit__tab--inactive:hover {
  color: #A8A8A8;
  background: rgba(212, 168, 67, 0.05);
}

/* COPIAR — input de valor (DS §9.1 Input de Aposta) */
.panel-deposit__input {
  width: 100%;
  padding: clamp(10px, 1.2vw, 14px);
  background: rgba(0, 0, 0, 0.5);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(14px, 1.8vw, 20px);
  font-weight: 700;
  text-align: center;
  outline: none;
  transition: border-color 0.25s ease, box-shadow 0.25s ease;
}

/* Input estado 1: VAZIO */
.panel-deposit__input--empty {
  border: 1.5px solid rgba(212, 168, 67, 0.2);
  color: #666666;
}

/* Input estado 2: VALIDO (com valor, saldo suficiente) */
.panel-deposit__input--valid {
  border: 1.5px solid #D4A843;
  box-shadow: 0 0 8px rgba(212, 168, 67, 0.2);
  color: #FFFFFF;
}

/* Input estado 3: ERRO (saldo insuficiente ou fora do range) */
.panel-deposit__input--error {
  border: 1.5px solid #FF1744;
  box-shadow: 0 0 8px rgba(255, 23, 68, 0.2);
  color: #FF3B3B;
}

/* Input estado 4: DISABLED (processando) */
.panel-deposit__input--disabled {
  opacity: 0.6;
  pointer-events: none;
  border: 1.5px solid rgba(102, 102, 102, 0.2);
}

/* COPIAR — atalhos MIN / x2 / div2 / MAX (DS §8.8 Atalhos de Aposta) */
.panel-deposit__shortcuts {
  display: flex;
  gap: clamp(6px, 0.8vw, 10px);
  justify-content: center;
}

.panel-deposit__shortcut {
  padding: clamp(6px, 0.8vw, 10px) clamp(10px, 1.2vw, 16px);
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid rgba(212, 168, 67, 0.25);
  border-radius: 6px;
  color: #D4A843;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(9px, 1.1vw, 13px);
  font-weight: 600;
  cursor: pointer;
  min-height: 36px;
  transition: all 0.2s ease;
}

.panel-deposit__shortcut:hover {
  border-color: #FFD700;
  color: #FFD700;
  background: rgba(212, 168, 67, 0.08);
}

.panel-deposit__shortcut:active {
  transform: scale(0.95);
}

.panel-deposit__shortcut--disabled {
  opacity: 0.4;
  pointer-events: none;
}

/* COPIAR — botao CONFIRMAR (DS §8.1 Botao Primario — 4 estados) */
.panel-deposit__confirm {
  width: 100%;
  padding: clamp(10px, 1.2vw, 14px);
  border-radius: 8px;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  min-height: 48px;
  transition: all 0.25s ease;
}

/* Confirmar estado 1: HABILITADO (saldo ok, valor valido) */
.panel-deposit__confirm--enabled {
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0, 230, 118, 0.3);
  color: #FFFFFF;
  box-shadow: 0 4px 16px rgba(0, 200, 83, 0.2);
}

.panel-deposit__confirm--enabled:hover {
  box-shadow: 0 6px 24px rgba(0, 200, 83, 0.3);
  transform: translateY(-1px);
  border-color: rgba(0, 230, 118, 0.5);
}

.panel-deposit__confirm--enabled:active {
  transform: translateY(1px);
  box-shadow: 0 2px 8px rgba(0, 200, 83, 0.15);
}

/* Confirmar estado 2: DESABILITADO (saldo insuficiente, sem valor) */
.panel-deposit__confirm--disabled {
  background: linear-gradient(180deg, #333333 0%, #1A1A1A 100%);
  border: 1.5px solid rgba(102, 102, 102, 0.3);
  color: #666666;
  cursor: not-allowed;
  opacity: 0.4;
  pointer-events: none;
}

/* Confirmar estado 3: LOADING (processando transacao) */
.panel-deposit__confirm--loading {
  background: linear-gradient(180deg, #333333 0%, #1A1A1A 100%);
  border: 1.5px solid rgba(212, 168, 67, 0.3);
  color: #D4A843;
  cursor: wait;
  pointer-events: none;
  /* Spinner dourado inline via pseudo-element ou SVG */
}

/* Confirmar estado 4: SUCESSO (flash verde apos confirmar) */
.panel-deposit__confirm--success {
  background: linear-gradient(180deg, #00E676 0%, #00C853 100%);
  border: 1.5px solid rgba(0, 230, 118, 0.5);
  color: #FFFFFF;
  box-shadow: 0 0 20px rgba(0, 230, 118, 0.3);
  pointer-events: none;
}

/* COPIAR — botao fechar X (DS §8.5 Botao Ghost) */
.panel-deposit__close {
  position: absolute;
  top: clamp(8px, 1vw, 14px);
  right: clamp(8px, 1vw, 14px);
  width: 32px; height: 32px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid rgba(212, 168, 67, 0.2);
  color: #A8A8A8;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  min-height: 44px; min-width: 44px;
  transition: all 0.2s ease;
}

.panel-deposit__close:hover {
  border-color: #FF1744;
  color: #FF1744;
  background: rgba(255, 23, 68, 0.08);
}

/* COPIAR — texto info de conversao */
.panel-deposit__info {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.2vw, 14px);
  color: #A8A8A8;
  text-align: center;
  line-height: 1.5;
}

.panel-deposit__info-amount {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #00E676;
}

/* COPIAR — mensagem de erro (saldo insuficiente) */
.panel-deposit__error {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.2vw, 13px);
  color: #FF1744;
  text-align: center;
}
```

**§ Referencias DS:**
- §4.3 Container Elevado — glassmorphism overlay com backdrop-filter
- §4.2 Container Primario — brilho ::before no topo, shadow 3 camadas
- §9.1 Input de Aposta — "padding, background rgba(0,0,0,0.5), border 1.5px, border-radius 8px, focus dourado, error vermelho"
- §8.8 Atalhos de Aposta — "MIN/x2/div2/MAX com border dourada, hover FFD700"
- §8.1 Botao Primario — "gradient verde, border rgba(0,230,118), hover shadow + translateY, disabled opacity 0.4"
- §8.5 Botao Ghost — "background transparente/sutil, border sutil, hover perigo vermelho"
- §12.1 Modal Padrao — estrutura flex column com gap e padding

---

**ITENS CHECKLIST COBERTOS NESTA PARTE:**

| Item Painel | Item Mestre | Secao | Status |
|:-----------:|:-----------:|:-----:|:------:|
| 4.1 Ref DS § | 4.1 Ref DS § | TODAS (7 secoes × §ref) | COBERTO |
| 4.2 CSS copiavel | 4.2 CSS copiavel | TODAS (COPIAR EXATAMENTE) | COBERTO |
| 4.3 Todos estados | 4.3 Todos estados | Dock 5, Card 4, Hero arrows 3, Play btn 3, Input 4, Confirm 4, Shortcut 3, Close 2, Tab 3 | COBERTO |
| 4.7 Dock/Tabs | — | Secao 8 (5 estados, layoutId, 11 blocos CSS) | COBERTO |
| 4.8 Game Cards | 4.7 Cards | Secao 9 (4 estados, 7 camadas, spotlight, logo, nome) | COBERTO |
| 4.9 Hero Carousel | — | Secao 11 (container, pedestal, arrows, play, beams, broken border) | COBERTO |
| 4.11 Glassmorphism | 4.10 Glassmorphism | Secoes 13-14 (GameModal + DepositModal) | COBERTO |
| 4.12 Feixo+Linha+Shine | — | Secao 10 (3 efeitos isolados com §ref) | COBERTO |
| — | 4.8 Botoes 3+ tipos | Secoes 11, 14 (play-btn, confirm, shortcut, close, tab) | COBERTO |
| — | 4.13 Input aposta | Secao 14 (input 4 estados + atalhos) | COBERTO |

**ACUMULADO P1+P2:** 4.1, 4.2, 4.3, 4.4, 4.5, 4.7, 4.8, 4.9, 4.10, 4.11, 4.12, 4.16 + Mestre 4.1-4.8, 4.10, 4.13, 4.14

**PENDENTES P3-P4:** 4.6 Header/Saldo (P3), 4.13 Tooltip CSS (P3), 4.14 Atmosferas (P4), 4.15 Timelines (P4), 4.17 Checklist §27 (P4) + Mestre 4.9 Textos, 4.11 Tooltip, 4.12 Glow, 4.15 Atmosferas, 4.16 Timelines, 4.17 Checklist

---

**[FIM DA PARTE 2 — Continua com Secoes 15-21: ComingSoon, Saldo/VIP, Botoes 4 tipos, Textos 5 tipos, Tooltip, Glow System, Bandeiras]**

**Seguindo roteiro (X3): Proximo: Doc 4 Adendo DS Parte 3. Anterior: Doc 4 Adendo DS Parte 2.**

`[ENTREGA: 4.ADENDO-DS-PAINEL-FLUTUANTE-PARTE-2.md — 23/03/2026]`
