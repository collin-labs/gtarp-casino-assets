# ADENDO DESIGN SYSTEM — DICE (#7)
## PARTE 1 DE 3 — Componentes CSS Real (1.1-1.10)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 21/03/2026

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1-4) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com §ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 25 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.dice-[block]__[element]--[modifier]`
- Blocks: `dice-game`, `dice-area`, `dice-cube`, `dice-face`, `dice-dot`, `dice-grid`, `dice-card`, `dice-bet`, `dice-roll`, `dice-lightning`, `dice-result`, `dice-overlay`, `dice-modal`, `dice-history`, `dice-chip`
- Modifiers: `--idle`, `--shaking`, `--rolling`, `--landing`, `--result`, `--bigwin`, `--selected`, `--lightning`, `--won`, `--lost`, `--disabled`, `--auto`, `--low`, `--high`, `--triple`

**Pesquisa REGRA X0:** BEM naming 2025-2026 confirmado como padrao dominante (getbem.com, sparkbox.com Fev/2025, valoremreply.com Abr/2025). `.block__element--modifier` com double underscore e double dash. backdrop-filter funciona no CEF Chromium do FiveM (docs.fivem.net). CSS 3D preserve-3d funciona em CEF Chromium 90+ (MDN compat table verificada). Framer Motion 11 spring physics suportado em React 19 (motion.dev docs 2025).

---

# SECAO 1 — COMPONENTES COM CSS REAL (25 Componentes)

## 1.1 Fundo do Jogo (DS P1 §4.1 + §6)

Quando o Dice abre dentro do painel (z-index 60), o fundo NUNCA e flat. Usa bg-casino.png com gradientes radiais para vinheta e atmosfera.

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Dice */
.dice-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute;
  inset: 0;
  z-index: 60;
  border-radius: inherit;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  padding: clamp(8px, 1vw, 16px);
}

/* COPIAR — vinheta overlay (DS P1 §6.2) */
.dice-game::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%);
  pointer-events: none;
  z-index: 0;
}

/* COPIAR — estado lightning phase (tint azulado sutil) */
.dice-game--lightning::after {
  content: '';
  position: absolute;
  inset: 0;
  background: rgba(68,136,255,0.02);
  pointer-events: none;
  z-index: 1;
  transition: opacity 0.5s ease;
}

/* COPIAR — estado big win (tint dourado) */
.dice-game--bigwin::after {
  content: '';
  position: absolute;
  inset: 0;
  background: rgba(212,168,67,0.04);
  pointer-events: none;
  z-index: 1;
}
```

---

## 1.2 Container de Area (DS P1 §4.2)

Container usado para a area dos dados e para o historico lateral. 5 camadas de box-shadow para profundidade real.

```css
/* COPIAR EXATAMENTE — container da area de dados */
.dice-area {
  background: rgba(0,0,0,0.3);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 14px;
  outline: 1px solid rgba(191,149,63,0.1);
  outline-offset: 3px;
  box-shadow:
    0 1px 2px rgba(0,0,0,0.5),       /* contact shadow */
    0 4px 8px rgba(0,0,0,0.3),        /* ambient close */
    0 12px 32px rgba(0,0,0,0.2),      /* ambient far */
    inset 0 1px 0 rgba(212,168,67,0.08), /* brilho interno topo */
    inset 0 -1px 0 rgba(0,0,0,0.3);  /* escurecimento interno base */
  padding: clamp(12px, 1.5vw, 24px);
  position: relative;
  overflow: hidden;
}

/* COPIAR — hover da area (quando mouse esta sobre, sutil) */
.dice-area:hover {
  border-color: rgba(212,168,67,0.45);
  box-shadow:
    0 1px 2px rgba(0,0,0,0.5),
    0 4px 8px rgba(0,0,0,0.3),
    0 12px 32px rgba(0,0,0,0.2),
    0 0 20px rgba(212,168,67,0.05),   /* glow dourado externo sutil */
    inset 0 1px 0 rgba(212,168,67,0.12),
    inset 0 -1px 0 rgba(0,0,0,0.3);
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
}
```

---

## 1.3 Card do Grid de Apostas (DS P1 §4.4 + §5.1)

Cada card no grid de apostas (totais 3-18) segue o padrao de cards do Blackout Casino com feixo verde, linha dourada e shine sweep no hover.

```css
/* COPIAR EXATAMENTE — card de total individual */
.dice-card {
  background: #141414;
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 10px;
  padding: clamp(6px, 0.8vw, 12px) clamp(4px, 0.5vw, 8px);
  text-align: center;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 2px 8px rgba(0,0,0,0.5),
    inset 0 1px 0 rgba(255,215,0,0.05);
  transition: border-color 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
}

/* COPIAR — hover (DS P1 §4.4 hover pattern) */
.dice-card:hover {
  border-color: rgba(212,168,67,0.7);
  background: #1A1A1A;
  box-shadow:
    0 4px 15px rgba(0,0,0,0.6),
    0 0 15px rgba(212,168,67,0.08),
    inset 0 1px 0 rgba(255,215,0,0.1);
  transform: scale(1.03);
}

/* COPIAR — selecionado (aposta feita neste total) */
.dice-card--selected {
  border-color: rgba(0,230,118,0.5);
  background: rgba(0,230,118,0.08);
  box-shadow:
    0 0 12px rgba(0,230,118,0.2),
    0 2px 8px rgba(0,0,0,0.5),
    inset 0 1px 0 rgba(0,230,118,0.1);
}

/* COPIAR — lightning ativo (multiplicador neste total) */
.dice-card--lightning {
  border-color: rgba(255,215,0,0.8);
  background: rgba(255,215,0,0.05);
  box-shadow:
    0 0 20px rgba(68,136,255,0.3),
    0 0 10px rgba(255,215,0,0.3),
    inset 0 0 15px rgba(68,136,255,0.05);
  animation: dice-lightning-pulse 1.5s ease-in-out infinite;
}

/* COPIAR — won (resultado acertou este total) */
.dice-card--won {
  border-color: #00E676;
  background: rgba(0,230,118,0.15);
  box-shadow:
    0 0 25px rgba(0,230,118,0.4),
    0 0 50px rgba(0,230,118,0.15);
}

/* COPIAR — lost (apostou mas errou) */
.dice-card--lost {
  opacity: 0.35;
  filter: grayscale(0.5);
  transition: opacity 1.5s ease, filter 1.5s ease;
}

/* COPIAR — disabled (durante roll, nao clicavel) */
.dice-card--disabled {
  pointer-events: none;
  opacity: 0.6;
  cursor: not-allowed;
}

@keyframes dice-lightning-pulse {
  0%, 100% { box-shadow: 0 0 15px rgba(68,136,255,0.2), 0 0 8px rgba(255,215,0,0.2), inset 0 0 10px rgba(68,136,255,0.03); }
  50% { box-shadow: 0 0 30px rgba(68,136,255,0.5), 0 0 15px rgba(255,215,0,0.4), inset 0 0 20px rgba(68,136,255,0.08); }
}
```

---

## 1.4 Feixo Verde Base (Guia P3 §2.3.3)

Linha neon verde na base de cards interativos. Permanente, intensifica no hover.

```css
/* COPIAR EXATAMENTE — feixo verde inferior */
.dice-card::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 5%;
  width: 90%;
  height: 3px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(0,230,118,0.2) 15%,
    rgba(0,230,118,0.4) 30%,
    #00E676 50%,
    rgba(0,230,118,0.4) 70%,
    rgba(0,230,118,0.2) 85%,
    transparent 100%
  );
  box-shadow: 0 0 8px rgba(0,230,118,0.3), 0 0 15px rgba(0,230,118,0.15);
  border-radius: 0 0 10px 10px;
  opacity: 0.6;
  transition: opacity 0.3s ease;
}

/* COPIAR — hover intensifica */
.dice-card:hover::after {
  opacity: 1;
  box-shadow: 0 0 12px rgba(0,230,118,0.5), 0 0 25px rgba(0,230,118,0.25);
}
```

---

## 1.5 Linha Dourada Topo (Guia P3 §2.3.3)

Linha neon dourada sutil no topo de cards e containers.

```css
/* COPIAR EXATAMENTE — linha dourada topo */
.dice-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 10%;
  width: 80%;
  height: 1px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(212,168,67,0.15) 20%,
    rgba(255,215,0,0.35) 50%,
    rgba(212,168,67,0.15) 80%,
    transparent 100%
  );
  opacity: 0.7;
  transition: opacity 0.3s ease;
}

/* COPIAR — hover intensifica */
.dice-card:hover::before {
  opacity: 1;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(212,168,67,0.25) 20%,
    rgba(255,215,0,0.5) 50%,
    rgba(212,168,67,0.25) 80%,
    transparent 100%
  );
}
```

---

## 1.6 Shine Sweep Hover (DS P1 §4.4)

Animacao de brilho que percorre o card no hover — efeito de reflexo em metal polido.

```css
/* COPIAR EXATAMENTE — shine sweep no hover */
/* Implementar como div overlay dentro do card, nao ::before (::before ja usado pela linha dourada) */
.dice-card__shine {
  position: absolute;
  inset: 0;
  background: linear-gradient(105deg,
    transparent 40%,
    rgba(255,255,255,0.03) 45%,
    rgba(255,255,255,0.06) 50%,
    rgba(255,255,255,0.03) 55%,
    transparent 60%
  );
  background-size: 200% 100%;
  pointer-events: none;
  border-radius: inherit;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.dice-card:hover .dice-card__shine {
  opacity: 1;
  animation: dice-shine-sweep 2s ease infinite;
}

@keyframes dice-shine-sweep {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

---

## 1.7 Botao Primario Verde — ROLAR DADOS (DS P2 §8.1)

Botao CTA principal do jogo. Verde com bevel 3D (::before), 4 estados.

```css
/* COPIAR EXATAMENTE — botao ROLAR DADOS */
.dice-roll__button {
  background: linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%);
  border: 2px solid rgba(0,230,118,0.6);
  border-radius: 10px;
  padding: clamp(10px, 1.2vw, 16px) clamp(16px, 2.5vw, 32px);
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 18px);
  letter-spacing: clamp(1px, 0.2vw, 2px);
  text-transform: uppercase;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  min-width: 48px;
  min-height: 48px;
  box-shadow: 0 4px 15px rgba(0,230,118,0.3), inset 0 1px 0 rgba(255,255,255,0.2);
  transition: box-shadow 0.2s ease, transform 0.1s ease;
}

/* COPIAR — bevel 3D topo (DS P2 §8.1 bevel pattern) */
.dice-roll__button::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 50%;
  background: linear-gradient(180deg, rgba(255,255,255,0.15) 0%, transparent 100%);
  border-radius: 10px 10px 0 0;
  pointer-events: none;
}

/* COPIAR — hover */
.dice-roll__button:hover {
  transform: scale(1.05);
  box-shadow: 0 6px 25px rgba(0,230,118,0.5), inset 0 1px 0 rgba(255,255,255,0.25);
  border-color: rgba(0,230,118,0.8);
}

/* COPIAR — active (pressed) */
.dice-roll__button:active {
  transform: scale(0.95);
  box-shadow: 0 2px 8px rgba(0,230,118,0.2), inset 0 2px 4px rgba(0,0,0,0.3);
}

/* COPIAR — disabled (durante roll) */
.dice-roll__button--disabled {
  background: #333333;
  border-color: #444444;
  color: #666666;
  cursor: not-allowed;
  box-shadow: none;
  opacity: 0.5;
  pointer-events: none;
}
.dice-roll__button--disabled::before {
  display: none;
}
```

---

## 1.8 Botao Dourado — Atalhos MIN/x2/÷2/MAX (DS P2 §8.2)

Botoes menores para atalhos de aposta. Estilo metalico dourado.

```css
/* COPIAR EXATAMENTE — botao atalho */
.dice-bet__shortcut {
  background: rgba(212,168,67,0.1);
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: 6px;
  padding: clamp(4px, 0.5vw, 8px) clamp(6px, 0.8vw, 12px);
  color: #D4A843;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 14px);
  cursor: pointer;
  min-width: 44px;
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.2s ease, border-color 0.2s ease;
}

/* COPIAR — hover */
.dice-bet__shortcut:hover {
  background: rgba(212,168,67,0.2);
  border-color: rgba(212,168,67,0.6);
  color: #FFD700;
  box-shadow: 0 0 8px rgba(212,168,67,0.15);
}

/* COPIAR — active */
.dice-bet__shortcut:active {
  transform: scale(0.92);
  background: rgba(212,168,67,0.3);
}

/* COPIAR — disabled */
.dice-bet__shortcut--disabled {
  opacity: 0.4;
  pointer-events: none;
  color: #666666;
  border-color: rgba(212,168,67,0.15);
}
```

---

## 1.9 Botao Voltar (DS P2 §8.3 + Guia P3 §9.6)

Padrao unificado do projeto — identico em todos os jogos.

```css
/* COPIAR EXATAMENTE — botao VOLTAR */
.dice-game__back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-size: clamp(10px, 1.2vw, 16px);
  letter-spacing: 2px;
  padding: clamp(4px, 0.5vw, 8px) clamp(8px, 1vw, 16px);
  cursor: pointer;
  text-transform: uppercase;
  transition: border-color 0.2s ease, background 0.2s ease;
  min-width: 44px;
  min-height: 44px;
}

/* COPIAR — hover */
.dice-game__back:hover {
  border-color: rgba(212,168,67,0.8);
  background: rgba(212,168,67,0.08);
  color: #FFD700;
}

/* COPIAR — active */
.dice-game__back:active {
  transform: scale(0.95);
}
```

---

## 1.10 Botao Ghost — LIMPAR / Icons Footer (DS P2 §8.4)

Botao fantasma com borda sutil, usado para acoes secundarias (limpar apostas, icones do footer).

```css
/* COPIAR EXATAMENTE — botao ghost */
.dice-game__ghost {
  background: transparent;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1.1vw, 13px);
  padding: clamp(4px, 0.5vw, 8px) clamp(8px, 1vw, 14px);
  cursor: pointer;
  transition: background 0.2s ease, border-color 0.2s ease, color 0.2s ease;
  min-width: 44px;
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — hover */
.dice-game__ghost:hover {
  background: rgba(212,168,67,0.08);
  border-color: rgba(212,168,67,0.5);
  color: #D4A843;
}

/* COPIAR — active */
.dice-game__ghost:active {
  transform: scale(0.92);
  background: rgba(212,168,67,0.12);
}

/* COPIAR — disabled */
.dice-game__ghost--disabled {
  opacity: 0.3;
  pointer-events: none;
}

/* COPIAR — variante icone (botoes do footer: som, historico, PF, auto) */
.dice-game__icon-button {
  width: clamp(28px, 3.5vw, 40px);
  height: clamp(28px, 3.5vw, 40px);
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  padding: clamp(4px, 0.5vw, 6px);
  min-width: 44px;
  min-height: 44px;
  transition: border-color 0.2s ease, background 0.2s ease;
}

.dice-game__icon-button:hover {
  border-color: rgba(212,168,67,0.7);
  background: rgba(212,168,67,0.1);
}

.dice-game__icon-button:active {
  transform: scale(0.9);
}

/* Imagem icone dentro do botao */
.dice-game__icon-button img {
  width: 60%;
  height: 60%;
  object-fit: contain;
  filter: brightness(0.8);
  transition: filter 0.2s ease;
}

.dice-game__icon-button:hover img {
  filter: brightness(1.2);
}
```

---

**[FIM DA PARTE 1 — Continua com Componentes 1.11-1.20: Glassmorphism, Tooltip, Textos, Glow, Input, Dado 3D, Lightning Badge]**

`[ENTREGA: 4.ADENDO-DS-DICE-PARTE-1.md — 21/03/2026]`
