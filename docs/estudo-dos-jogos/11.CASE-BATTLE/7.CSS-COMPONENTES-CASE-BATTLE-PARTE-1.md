# 🎨 CSS-COMPONENTES — CASE BATTLE
## Arquivo único com CSS real de TODOS os componentes · Copiável · Numerado
### Blackout Casino · Jogo #11 · 11/03/2026

---

## COMO USAR

Colar JUNTO com qualquer PROMPT-V0 do Case Battle. Cada seção é um componente independente, numerado. O V0/Bolt pode copiar o CSS diretamente. Referências ao Design System indicadas em cada bloco.

---

# ═══════════════════════════════════════════════════
# C1. VARIÁVEIS GLOBAIS DO CASE BATTLE
# ═══════════════════════════════════════════════════

```css
:root {
  /* Tipografia */
  --font-display: 'Cinzel', 'Playfair Display', 'Georgia', serif;
  --font-body: 'Inter', 'Segoe UI', -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;

  /* Dourado (DS P1 §2.2) */
  --gold-300: #FFD700;
  --gold-400: #D4A843;
  --gold-500: #CB9B51;
  --gold-700: #8B6914;
  --gold-900: #462523;

  /* Verde (DS P1 §2.3) */
  --emerald-300: #00E676;
  --emerald-600: #004D25;

  /* Vermelho (DS P1 §2.4) */
  --red-300: #FF1744;

  /* Backgrounds (DS P1 §2.1) */
  --bg-abyss: #050505;
  --bg-deep: #0A0A0A;
  --bg-base: #0F0F0F;
  --bg-surface1: #141414;
  --bg-surface2: #1A1A1A;
  --bg-surface3: #222222;

  /* Raridades */
  --rarity-common: #4B69FF;
  --rarity-uncommon: #8847FF;
  --rarity-rare: #D32CE6;
  --rarity-epic: #EB4B4B;
  --rarity-legendary: #FFD700;

  /* Battle específico */
  --battle-underdog: #FF6B00;

  /* Easing (DS P3A §18) */
  --ease-out: cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --ease-wheel: cubic-bezier(0.15, 0.85, 0.35, 1);
  --ease-bounce: cubic-bezier(0.68, -0.55, 0.27, 1.55);
  --ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
  --ease-enter: cubic-bezier(0.0, 0.0, 0.2, 1);
}
```

---

# ═══════════════════════════════════════════════════
# C2. FUNDO + ATMOSFERA (DS P1 §4.1, §6.1-6.4)
# ═══════════════════════════════════════════════════

```css
.battle-bg {
  position: absolute;
  inset: 0;
  z-index: 60;
  border-radius: inherit;
  overflow: hidden;
  background-color: var(--bg-deep);
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 50%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,230,118,0.02) 0%, transparent 50%);
}

.battle-bg-vignette {
  position: absolute;
  inset: 0;
  box-shadow: inset 0 0 80px rgba(0,0,0,0.5);
  pointer-events: none;
  z-index: 1;
}

.battle-bg-scanline {
  position: absolute;
  left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, rgba(0,230,118,0.1) 20%, rgba(0,230,118,0.2) 50%, rgba(0,230,118,0.1) 80%, transparent 100%);
  animation: scanline 8s linear infinite;
  pointer-events: none;
  z-index: 100;
  opacity: 0.3;
}

@keyframes scanline {
  0%   { top: -2px; }
  100% { top: 100%; }
}
```

---

# ═══════════════════════════════════════════════════
# C3. TÍTULOS + TEXTO DOURADO (DS P1 §3, §7.5)
# ═══════════════════════════════════════════════════

```css
.battle-title {
  font-family: var(--font-display);
  font-size: clamp(14px, 1.6vw, 22px);
  font-weight: 800;
  color: var(--gold-400);
  letter-spacing: 3px;
  text-transform: uppercase;
  text-shadow: 0 0 12px rgba(212,168,67,0.4), 0 2px 6px rgba(0,0,0,1);
}

.battle-subtitle {
  font-family: var(--font-display);
  font-size: clamp(10px, 1.1vw, 14px);
  font-weight: 700;
  color: var(--gold-400);
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.battle-label {
  font-family: var(--font-body);
  font-size: clamp(10px, 1vw, 13px);
  font-weight: 600;
  color: #A8A8A8;
}

.battle-value-money {
  font-family: var(--font-mono);
  font-weight: 700;
  color: var(--emerald-300);
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
}

.battle-value-gold {
  font-family: var(--font-mono);
  font-weight: 700;
  color: var(--gold-300);
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}
```

---

# ═══════════════════════════════════════════════════
# C4. BOTÕES (DS P2 §8.1-8.6)
# ═══════════════════════════════════════════════════

```css
/* Primário verde — ENTRAR, APOSTAR (DS P2 §8.1) */
.btn-battle-primary {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: clamp(6px, 0.8vw, 12px) clamp(12px, 1.5vw, 24px);
  min-height: 36px;
  border-radius: 8px;
  cursor: pointer;
  overflow: hidden;
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1px solid rgba(0,230,118,0.3);
  color: #FFF;
  font-family: var(--font-display);
  font-size: clamp(9px, 0.95vw, 12px);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.15), 0 2px 4px rgba(0,0,0,0.4), 0 0 12px rgba(0,230,118,0.15);
  transition: all 0.2s var(--ease-out);
}

.btn-battle-primary:hover {
  background: linear-gradient(180deg, #00E676 0%, #00693C 100%);
  transform: translateY(-1px);
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.2), 0 4px 8px rgba(0,0,0,0.4), 0 0 20px rgba(0,230,118,0.25);
}

.btn-battle-primary:active {
  transform: translateY(1px);
  background: linear-gradient(180deg, #004D25 0%, #003318 100%);
}

/* Dourado — CRIAR, JOGAR (DS P2 §8.2) */
.btn-battle-gold {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: clamp(6px, 0.8vw, 12px) clamp(14px, 1.5vw, 28px);
  min-height: 36px;
  border-radius: 8px;
  cursor: pointer;
  overflow: hidden;
  background: linear-gradient(180deg, #CB9B51 0%, #D4A843 30%, #8B6914 100%);
  border: 1px solid rgba(246,226,122,0.4);
  color: #0A0A0A;
  font-family: var(--font-display);
  font-size: clamp(9px, 0.95vw, 12px);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  text-shadow: 0 1px 0 rgba(255,255,255,0.2);
  box-shadow: inset 0 1px 0 rgba(255,236,179,0.4), inset 0 -1px 0 rgba(70,37,35,0.5), 0 2px 4px rgba(0,0,0,0.5), 0 0 16px rgba(212,168,67,0.15);
  transition: all 0.2s var(--ease-out);
}

.btn-battle-gold::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 60%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
  transition: left 0.5s ease;
  pointer-events: none;
}

.btn-battle-gold:hover::after { left: 120%; }

.btn-battle-gold:hover {
  background: linear-gradient(180deg, #D4A843 0%, #FFD700 30%, #B8860B 100%);
  transform: translateY(-1px);
}

/* Secundário — VOLTAR, CANCELAR (DS P2 §8.3) */
.btn-battle-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: clamp(6px, 0.7vw, 10px) clamp(10px, 1vw, 20px);
  min-height: 34px;
  border-radius: 8px;
  cursor: pointer;
  background: linear-gradient(180deg, #2A2A2A 0%, #1A1A1A 100%);
  border: 1px solid rgba(212,168,67,0.2);
  color: #A8A8A8;
  font-family: var(--font-body);
  font-size: clamp(9px, 0.9vw, 12px);
  font-weight: 600;
  letter-spacing: 0.5px;
  transition: all 0.2s ease;
}

.btn-battle-secondary:hover {
  border-color: rgba(212,168,67,0.4);
  color: var(--gold-400);
}

/* Perigo — CANCELAR BATALHA (DS P2 §8.4) */
.btn-battle-danger {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: clamp(6px, 0.7vw, 10px) clamp(10px, 1vw, 20px);
  min-height: 34px;
  border-radius: 8px;
  cursor: pointer;
  background: linear-gradient(180deg, #E53935 0%, #C62828 100%);
  border: 1px solid rgba(255,23,68,0.3);
  color: #FFF;
  font-family: var(--font-body);
  font-size: clamp(9px, 0.9vw, 12px);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.1), 0 0 8px rgba(255,23,68,0.15);
  transition: all 0.2s ease;
}

.btn-battle-danger:hover {
  background: linear-gradient(180deg, #FF1744 0%, #D32F2F 100%);
  transform: translateY(-1px);
}

/* Ghost — VERIFICAR FAIRNESS (DS P2 §8.6) */
.btn-battle-ghost {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: clamp(4px, 0.5vw, 8px) clamp(8px, 0.8vw, 14px);
  border-radius: 6px;
  cursor: pointer;
  background: transparent;
  border: 1px solid rgba(212,168,67,0.15);
  color: #666;
  font-family: var(--font-body);
  font-size: clamp(8px, 0.85vw, 11px);
  font-weight: 600;
  transition: all 0.15s ease;
}

.btn-battle-ghost:hover {
  border-color: rgba(212,168,67,0.3);
  color: var(--gold-400);
  background: rgba(212,168,67,0.05);
}
```

---

# ═══════════════════════════════════════════════════
# C5. BATTLE CARD (Lobby) (DS P1 §4.4, Guia P3 §2.3.3)
# ═══════════════════════════════════════════════════

```css
.battle-card {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(8px, 1vw, 14px);
  border-radius: 10px;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  background: linear-gradient(180deg, var(--bg-surface2), var(--bg-surface1));
  border: 1px solid rgba(212,168,67,0.15);
  box-shadow: 0 2px 8px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.03);
  transition: all 0.2s var(--ease-out);
}

.battle-card:hover {
  border-color: rgba(212,168,67,0.3);
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.4), 0 0 12px rgba(212,168,67,0.08);
}

/* Feixo verde base */
.battle-card-neon {
  position: absolute;
  bottom: 0; left: 10%; right: 10%;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, rgba(0,230,118,0.2) 20%, #00E676 50%, rgba(0,230,118,0.2) 80%, transparent 100%);
  box-shadow: 0 0 6px rgba(0,230,118,0.2);
  opacity: 0.6;
  transition: opacity 0.2s ease;
}

.battle-card:hover .battle-card-neon { opacity: 1; }

/* Shimmer sweep */
.battle-card-shimmer {
  position: absolute;
  inset: 0;
  background: linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 45%, rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.03) 55%, transparent 60%);
  transform: translateX(-100%);
  transition: transform 0.6s ease;
  pointer-events: none;
  z-index: 2;
}

.battle-card:hover .battle-card-shimmer { transform: translateX(50%); }
```

---

# ═══════════════════════════════════════════════════
# C6. BADGES (DS P2 §10)
# ═══════════════════════════════════════════════════

```css
.badge-battle {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  padding: 2px 8px;
  border-radius: 4px;
  font-family: var(--font-body);
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  white-space: nowrap;
}

.badge-classic { background: rgba(212,168,67,0.1); border: 1px solid rgba(212,168,67,0.2); color: var(--gold-400); }
.badge-underdog { background: rgba(255,107,0,0.12); border: 1px solid rgba(255,107,0,0.3); color: var(--battle-underdog); }
.badge-free { background: rgba(0,230,118,0.1); border: 1px solid rgba(0,230,118,0.3); color: var(--emerald-300); }
.badge-private { background: rgba(212,168,67,0.08); border: 1px solid rgba(212,168,67,0.15); color: rgba(212,168,67,0.6); }
```

---

# ═══════════════════════════════════════════════════
# C7. PLAYER CIRCLES + SLOTS (DS P1 §5)
# ═══════════════════════════════════════════════════

```css
.player-circle {
  width: clamp(24px, 2.8vw, 34px);
  height: clamp(24px, 2.8vw, 34px);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-family: var(--font-body);
  font-size: clamp(8px, 0.8vw, 11px);
  font-weight: 700;
}

.player-circle.filled {
  background: linear-gradient(135deg, var(--gold-400), var(--gold-700));
  border: 2px solid rgba(212,168,67,0.5);
  color: var(--bg-deep);
  box-shadow: 0 0 8px rgba(212,168,67,0.2);
}

.player-circle.empty {
  background: transparent;
  border: 2px dashed rgba(255,255,255,0.1);
  color: #444;
  animation: circlePulse 2s ease-in-out infinite;
}

@keyframes circlePulse {
  0%, 100% { border-color: rgba(255,255,255,0.1); }
  50%      { border-color: rgba(212,168,67,0.2); }
}

/* Slot grande na sala de espera */
.player-slot {
  width: clamp(80px, 12vw, 140px);
  height: clamp(90px, 13vw, 160px);
  border-radius: 12px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 8px; padding: 12px;
}

.player-slot.filled {
  background: linear-gradient(180deg, var(--bg-surface2), #111);
  border: 2px solid rgba(0,230,118,0.3);
  box-shadow: 0 0 12px rgba(0,230,118,0.15);
}

.player-slot.empty {
  background: rgba(0,0,0,0.2);
  border: 2px dashed rgba(212,168,67,0.15);
}
```

---

# ═══════════════════════════════════════════════════
# C8. VS SEPARATOR (DS P1 §5.3, §7.5)
# ═══════════════════════════════════════════════════

```css
.vs-text {
  font-family: var(--font-display);
  font-size: clamp(20px, 3vw, 36px);
  font-weight: 900;
  color: var(--gold-300);
  letter-spacing: 3px;
  text-shadow: 0 0 8px rgba(255,215,0,0.5), 0 0 16px rgba(255,215,0,0.25), 0 2px 4px rgba(0,0,0,0.5);
  animation: vsGlow 2s ease-in-out infinite;
}

@keyframes vsGlow {
  0%, 100% { text-shadow: 0 0 8px rgba(255,215,0,0.5), 0 0 16px rgba(255,215,0,0.25); }
  50%      { text-shadow: 0 0 12px rgba(255,215,0,0.7), 0 0 24px rgba(255,215,0,0.4); }
}

.vs-line-h {
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(212,168,67,0.3) 20%, rgba(255,215,0,0.5) 50%, rgba(212,168,67,0.3) 80%, transparent);
}

.vs-line-v {
  width: 1px;
  background: linear-gradient(180deg, transparent, rgba(255,215,0,0.2) 20%, rgba(255,215,0,0.4) 50%, rgba(255,215,0,0.2) 80%, transparent);
}
```

---

# ═══════════════════════════════════════════════════
# C9. REVEAL STRIP (DS P1 §4.2, P3A §18)
# ═══════════════════════════════════════════════════

```css
.strip-container {
  position: relative;
  width: 100%;
  height: clamp(60px, 8vw, 90px);
  overflow: hidden;
  border-radius: 8px;
  background: linear-gradient(180deg, var(--bg-base), var(--bg-surface1));
  border: 1px solid rgba(212,168,67,0.2);
  box-shadow: inset 0 0 20px rgba(0,0,0,0.5);
}

.strip-inner {
  display: flex;
  align-items: center;
  height: 100%;
  padding: 4px 0;
  will-change: transform;
}

.strip-marker {
  position: absolute;
  top: 0; bottom: 0;
  left: 50%; transform: translateX(-50%);
  width: 3px; z-index: 10;
  background: linear-gradient(180deg, var(--gold-300), var(--gold-400), var(--gold-300));
  box-shadow: 0 0 12px rgba(255,215,0,0.4);
}

.strip-marker-arrow {
  position: absolute;
  top: -1px; left: 50%; transform: translateX(-50%);
  width: 0; height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-top: 8px solid var(--gold-300);
  filter: drop-shadow(0 0 4px rgba(255,215,0,0.5));
}

.strip-fade-left {
  position: absolute; top: 0; bottom: 0; left: 0;
  width: 60px; z-index: 5; pointer-events: none;
  background: linear-gradient(90deg, var(--bg-base), transparent);
}

.strip-fade-right {
  position: absolute; top: 0; bottom: 0; right: 0;
  width: 60px; z-index: 5; pointer-events: none;
  background: linear-gradient(270deg, var(--bg-base), transparent);
}

.strip-item {
  flex-shrink: 0;
  width: clamp(55px, 7vw, 80px);
  height: clamp(50px, 6.5vw, 75px);
  border-radius: 6px;
  margin: 0 3px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 2px;
  background: linear-gradient(180deg, var(--bg-surface2), #111);
  border: 1px solid var(--item-color, rgba(255,255,255,0.1));
  overflow: hidden;
}

.strip-item.winner {
  border-width: 2px;
  box-shadow: 0 0 12px var(--item-color), 0 0 24px rgba(var(--item-color-rgb), 0.3);
  animation: itemPulse 2s ease-in-out infinite;
}

@keyframes itemPulse {
  0%, 100% { box-shadow: 0 0 8px var(--item-color); }
  50%      { box-shadow: 0 0 16px var(--item-color), 0 0 32px rgba(var(--item-color-rgb), 0.2); }
}

.strip-item-name {
  font-family: var(--font-body);
  font-size: 7px; font-weight: 600; color: #A8A8A8;
  text-align: center; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  max-width: 90%;
}

.strip-item-value {
  font-family: var(--font-mono);
  font-size: 8px; font-weight: 700;
  color: var(--item-color);
}
```

**GSAP:** `ease: "power3.out"`, duration: 5s

---

# ═══════════════════════════════════════════════════
# C10. ROUND BAR + SCORE BAR (DS P2 §16.3)
# ═══════════════════════════════════════════════════

```css
.round-bar {
  display: flex; align-items: center; gap: 10px;
  padding: 0 clamp(8px, 1vw, 14px);
  height: clamp(28px, 3.5vw, 38px);
}

.round-label {
  font-family: var(--font-display);
  font-size: clamp(10px, 1.1vw, 14px);
  font-weight: 700; color: var(--gold-400);
  letter-spacing: 1.5px; white-space: nowrap;
}

.round-progress {
  flex: 1; height: 6px; background: var(--bg-surface2);
  border-radius: 3px; overflow: hidden;
  border: 1px solid rgba(212,168,67,0.08);
}

.round-progress-fill {
  height: 100%; border-radius: 3px;
  background: linear-gradient(90deg, var(--emerald-600), var(--emerald-300));
  box-shadow: 0 0 8px rgba(0,230,118,0.3);
  transition: width 0.3s ease;
}

.score-bar {
  position: relative;
  height: clamp(24px, 3vw, 32px);
  border-radius: 6px; overflow: hidden;
  border: 1px solid rgba(212,168,67,0.15);
  background: var(--bg-base);
  display: flex;
}

.score-fill {
  height: 100%;
  transition: width 500ms ease;
  display: flex; align-items: center;
  padding: 0 6px;
}

.score-fill.gold {
  background: linear-gradient(90deg, var(--gold-400), rgba(212,168,67,0.6));
}

.score-fill.green {
  background: linear-gradient(270deg, var(--emerald-300), rgba(0,230,118,0.6));
  justify-content: flex-end;
}

.score-fill.leading {
  box-shadow: 0 0 8px currentColor;
}

.score-label {
  font-family: var(--font-mono);
  font-size: clamp(8px, 0.9vw, 11px);
  font-weight: 700; color: var(--bg-deep);
  white-space: nowrap;
}
```

---

# ═══════════════════════════════════════════════════
# C11. RESULTADO (DS P3A §20.2, §20.3)
# ═══════════════════════════════════════════════════

```css
.winner-lane {
  border: 2px solid rgba(255,215,0,0.5);
  animation: pulseGold 2s ease-in-out infinite;
  background: linear-gradient(180deg, rgba(255,215,0,0.05), #111);
}

@keyframes pulseGold {
  0%, 100% { box-shadow: 0 0 8px rgba(255,215,0,0.3); }
  50%      { box-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.15); }
}

.winner-text {
  font-family: var(--font-display);
  font-size: clamp(14px, 1.6vw, 20px);
  font-weight: 900; color: var(--gold-300);
  letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 0 24px rgba(255,215,0,0.3);
  animation: bigWin 0.6s var(--ease-spring);
}

@keyframes bigWin {
  0%   { transform: scale(0.3); opacity: 0; filter: blur(10px); }
  60%  { transform: scale(1.1); opacity: 1; filter: blur(0); }
  80%  { transform: scale(0.95); }
  100% { transform: scale(1); }
}

.loser-lane {
  opacity: 0.5;
  border: 1px solid rgba(255,23,68,0.2);
}

.prize-total {
  font-family: var(--font-mono);
  font-size: clamp(22px, 3vw, 36px);
  font-weight: 700; color: var(--gold-300);
  text-shadow: 0 0 12px rgba(255,215,0,0.5), 0 0 24px rgba(255,215,0,0.2);
}

@keyframes flashGreen {
  0% { background-color: transparent; }
  15% { background-color: rgba(0,230,118,0.15); }
  100% { background-color: transparent; }
}

@keyframes flashRed {
  0% { background-color: transparent; }
  15% { background-color: rgba(255,23,68,0.2); }
  100% { background-color: transparent; }
}
```

---

# ═══════════════════════════════════════════════════
# C12. COUNTDOWN (DS P3A §20.2, P3B §25.3)
# ═══════════════════════════════════════════════════

```css
.countdown-number {
  font-family: var(--font-display);
  font-size: clamp(48px, 8vw, 80px);
  font-weight: 900;
  color: var(--gold-300);
  text-shadow: 0 0 16px rgba(255,215,0,0.6), 0 0 32px rgba(255,215,0,0.3), 0 4px 8px rgba(0,0,0,0.5);
  animation: countPop 0.3s var(--ease-bounce);
}

@keyframes countPop {
  0%   { transform: scale(1.5); opacity: 0; }
  60%  { transform: scale(0.9); }
  100% { transform: scale(1); opacity: 1; }
}

.countdown-label {
  font-family: var(--font-body);
  font-size: clamp(10px, 1.2vw, 14px);
  color: #A8A8A8;
  letter-spacing: 2px;
  text-transform: uppercase;
}
```

---

# ═══════════════════════════════════════════════════
# C13. MODAL (DS P2 §12.1)
# ═══════════════════════════════════════════════════

```css
.battle-modal-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.6);
  display: flex; align-items: center; justify-content: center;
  z-index: 80;
  animation: fadeIn 0.2s ease;
}

.battle-modal {
  background: rgba(10,10,10,0.95);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 14px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.8), 0 0 20px rgba(212,168,67,0.1);
  max-width: clamp(400px, 60vw, 700px);
  max-height: 85vh;
  overflow-y: auto;
  padding: clamp(16px, 2vw, 28px);
  animation: modalIn 0.25s var(--ease-spring);
  scrollbar-width: thin;
  scrollbar-color: rgba(212,168,67,0.15) transparent;
}

@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

@keyframes modalIn {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}

.battle-modal-title {
  font-family: var(--font-display);
  font-size: clamp(14px, 1.6vw, 20px);
  font-weight: 800; color: var(--gold-400);
  letter-spacing: 2px; text-align: center;
  text-shadow: 0 0 12px rgba(212,168,67,0.3);
  margin-bottom: clamp(12px, 1.5vw, 20px);
}
```

---

# ═══════════════════════════════════════════════════
# C14. FILTROS / TABS (DS P2 §8.5 adaptado)
# ═══════════════════════════════════════════════════

```css
.filter-tabs {
  display: flex; gap: 4px;
  background: rgba(0,0,0,0.3);
  border-radius: 8px;
  padding: 3px;
}

.filter-tab {
  padding: clamp(4px, 0.5vw, 6px) clamp(10px, 1vw, 16px);
  border-radius: 6px;
  font-family: var(--font-body);
  font-size: clamp(9px, 0.9vw, 12px);
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  border: none;
  background: transparent;
  color: #666;
}

.filter-tab:hover { background: rgba(255,255,255,0.05); }

.filter-tab.active {
  background: rgba(212,168,67,0.15);
  color: var(--gold-400);
  border-bottom: 2px solid var(--gold-400);
}

.filter-tab.active-free {
  background: rgba(0,230,118,0.1);
  color: var(--emerald-300);
  border-bottom: 2px solid var(--emerald-300);
}
```

---

# ═══════════════════════════════════════════════════
# C15. SCROLLBAR (DS P2 §14)
# ═══════════════════════════════════════════════════

```css
.battle-scroll {
  scrollbar-width: thin;
  scrollbar-color: rgba(212,168,67,0.15) transparent;
}

.battle-scroll::-webkit-scrollbar { width: 4px; height: 4px; }
.battle-scroll::-webkit-scrollbar-track { background: transparent; }
.battle-scroll::-webkit-scrollbar-thumb { background: rgba(212,168,67,0.15); border-radius: 2px; }
.battle-scroll::-webkit-scrollbar-thumb:hover { background: rgba(212,168,67,0.3); }
```

---

**TOTAL: 15 componentes CSS · 12 keyframes · Todas as referências §DS incluídas**

---

`[ENTREGA: CSS-COMPONENTES — CASE BATTLE — CONCLUÍDA]`
