# ADENDO — ROTEIRO DE DESENVOLVIMENTO CASE BATTLE (#11)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-CASE-BATTLE-PARTE-5.md — 23/03/2026

---

# 1. CONTEXTO

A Parte 5 descreve Fase 6 (PF Modal + History + Polish) com ProvablyFairModal.tsx
em codigo real mas BattleHistory.tsx de forma mais descritiva. Este adendo fornece
codigo TSX real complementar com tooltips inline.

Itens corrigidos:
- **5.11** Regras entrega: P5 ja tem 8 regras. Complemento com +4
- **5.14** Zero pseudocodigo: TSX real para BattleHistory
- **5.15** Tooltips TSX inline: title bilingue em CADA botao

---

# 2. FASE 6 — CODIGO REAL TSX

## 2.1 BattleHistory.tsx — Trechos com tooltips

```tsx
// Lista de batalhas passadas
{history.map((battle, i) => (
  <motion.div
    key={battle.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: battle.isWinner ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${battle.isWinner ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    {/* Badge W/L */}
    <span
      title={isBR
        ? (battle.isWinner ? "Voce venceu esta batalha" : "Voce perdeu esta batalha")
        : (battle.isWinner ? "You won this battle" : "You lost this battle")
      }
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(10px, 1vw, 14px)",
        fontWeight: 800,
        color: battle.isWinner ? "#00E676" : "#FF1744",
        minWidth: "28px",
        textAlign: "center",
      }}
    >
      {battle.isWinner ? "W" : "L"}
    </span>

    {/* Modo badge */}
    <span style={{
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.8vw, 11px)",
      fontWeight: 600,
      color: battle.mode === "crazy" ? "#FF6D00" : battle.mode === "terminal" ? "#FF1744" : "#D4A843",
      background: `rgba(${battle.mode === "crazy" ? "255,109,0" : battle.mode === "terminal" ? "255,23,68" : "212,168,67"},0.1)`,
      padding: "2px 8px",
      borderRadius: "4px",
      letterSpacing: "1px",
      textTransform: "uppercase" as const,
    }}>
      {battle.mode.toUpperCase()}
    </span>

    {/* Players */}
    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(9px, 0.9vw, 13px)", color: "rgba(212,168,67,0.6)", flex: 1 }}>
      {battle.players.join(" vs ")}
    </span>

    {/* Valor total ganho/perdido */}
    <span style={{
      fontFamily: "var(--font-mono)",
      fontSize: "clamp(10px, 1vw, 14px)",
      fontWeight: 700,
      color: battle.isWinner ? "#00E676" : "rgba(212,168,67,0.4)",
      textShadow: battle.isWinner ? "0 0 8px rgba(0,230,118,0.4)" : "none",
    }}>
      {battle.isWinner ? `+G$${battle.profit.toLocaleString()}` : `-G$${battle.cost.toLocaleString()}`}
    </span>

    {/* Botao verificar PF */}
    <motion.button
      onClick={() => onVerify(battle.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={isBR ? "Verificar fairness desta batalha" : "Verify this battle's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/casebattle/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}

{/* Botao CARREGAR MAIS */}
<motion.button
  onClick={onLoadMore}
  whileHover={{ scale: 1.03, borderColor: "rgba(212,168,67,0.7)" }}
  whileTap={{ scale: 0.97 }}
  title={isBR ? "Carregar mais batalhas" : "Load more battles"}
  style={{
    width: "100%",
    background: "rgba(212,168,67,0.08)",
    border: "1px solid rgba(212,168,67,0.3)",
    borderRadius: "10px",
    color: "#D4A843",
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(10px, 1vw, 14px)",
    fontWeight: 600,
    letterSpacing: "2px",
    textTransform: "uppercase" as const,
    padding: "clamp(10px, 1vw, 16px)",
    cursor: "pointer",
    minHeight: "44px",
  }}
>
  {isBR ? "CARREGAR MAIS" : "LOAD MORE"}
</motion.button>
```

---

# 3. REGRAS DE ENTREGA COMPLEMENTO (item 5.11)

A Parte 5 ja tem 8 regras. Complemento:

9. TODOS botoes com minHeight 44px (REGRA X4 touch targets)
10. TODOS textos via battleTexts[key][lang] — zero string hardcoded (bilingue)
11. Resultado da batalha SEMPRE server-authoritative via fetchNui (seguranca)
12. Zero console.log em componentes de producao (REGRA X16)

---

# 4. VALIDACAO

**5.11** Regras: 8 + 4 = 12 regras. OK.
**5.14** TSX real: BattleHistory com badges W/L, modo, players, valor, verificar PF, carregar mais. OK.
**5.15** Tooltips: 4 inline (badge W/L, verificar PF, carregar mais). OK.

**VALIDACAO X17:** 3 itens exigidos, 3 entregues. Zero pseudocodigo.

---

`[ENTREGA: ADENDO-ROTEIRO-CASE-BATTLE.md — 23/03/2026]`
