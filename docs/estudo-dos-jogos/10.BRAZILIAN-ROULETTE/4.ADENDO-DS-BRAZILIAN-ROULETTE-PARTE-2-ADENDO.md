# ADENDO — DESIGN SYSTEM BRAZILIAN ROULETTE (#10)
## Parte 1 de 2 — Componentes CSS Novos + Estados Expandidos
## Referencia: 6.ADENDO-DS-BRAZILIAN-ROULETTE.md (389L, 10 componentes)
### Blackout Casino GTARP - 24/03/2026

---

## CONTEXTO

O Adendo DS existente (389L) tem 10 componentes CSS (§1.1-1.10), 3 atmosferas e checklist §27. A auditoria identificou 3 gaps:
- **4.3 PARCIAL:** Estados active/disabled parciais → este adendo expande TODOS os estados
- **4.16 FALTA:** 0 timelines formais → Parte 2 entrega 24+ timelines tabeladas
- **4.17 PARCIAL:** Checklist sem profundidade → Parte 2 expande

Este adendo adiciona 10 componentes NOVOS (§1.11-1.20) especificos da Brazilian Roulette que NAO estao no existente. O existente cobre componentes PADRAO do DS (container, modais, botoes, tooltip, glow, feixo, easing). Este adendo cobre componentes ESPECIFICOS do jogo.

---

# COMPONENTES NOVOS (§1.11 — §1.20)

## §1.11 Roulette Wheel Canvas Wrapper (DS P3A §21.5)

Container que envolve o canvas da roda. Borda dourada externa com glow sutil. Sombra interna para profundidade. Indicador (seta/diamante) no topo.

```css
/* COPIAR EXATAMENTE — wrapper do canvas da roda */
.br-roulette__wheel-wrapper {
  position: relative;
  width: clamp(280px, 35vw, 420px);
  height: clamp(280px, 35vw, 420px);
  border-radius: 50%;
  border: 3px solid rgba(212,168,67,0.4);
  box-shadow:
    0 0 20px rgba(212,168,67,0.15),
    0 0 40px rgba(212,168,67,0.05),
    inset 0 0 30px rgba(0,0,0,0.6);
  background: #0A0A0A;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.br-roulette__wheel-wrapper canvas {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}
/* Indicador (seta no topo) */
.br-roulette__wheel-indicator {
  position: absolute;
  top: -12px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-top: 16px solid #FFD700;
  filter: drop-shadow(0 2px 4px rgba(255,215,0,0.4));
  z-index: 5;
}
/* Estado: spinning */
.br-roulette__wheel-wrapper--spinning {
  border-color: rgba(212,168,67,0.6);
  box-shadow:
    0 0 30px rgba(212,168,67,0.25),
    0 0 60px rgba(212,168,67,0.1),
    inset 0 0 30px rgba(0,0,0,0.6);
}
/* Estado: result (numero vencedor) */
.br-roulette__wheel-wrapper--result {
  border-color: rgba(0,230,118,0.5);
  box-shadow:
    0 0 25px rgba(0,230,118,0.2),
    0 0 50px rgba(0,230,118,0.08),
    inset 0 0 30px rgba(0,0,0,0.6);
  transition: border-color 0.5s, box-shadow 0.5s;
}
```

## §1.12 Betting Grid (Mesa de Apostas)

Grid HTML table com celulas clicaveis. Cores vermelho/preto/verde conforme numeros. Hover com glow da cor. Celulas active (com aposta) tem chip stack visual.

```css
/* COPIAR EXATAMENTE — betting grid */
.br-roulette__grid {
  display: grid;
  gap: 1px;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 4px;
  padding: 2px;
  max-width: clamp(400px, 50vw, 600px);
}
.br-roulette__grid-cell {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: clamp(28px, 3.5vh, 36px);
  min-width: clamp(28px, 3.5vw, 36px);
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 700;
  color: #FFFFFF;
  cursor: pointer;
  border: 1px solid rgba(255,255,255,0.06);
  transition: all 0.15s;
}
.br-roulette__grid-cell--red {
  background: rgba(198,40,40,0.35);
}
.br-roulette__grid-cell--black {
  background: rgba(26,26,26,0.8);
}
.br-roulette__grid-cell--green {
  background: rgba(0,230,118,0.2);
}
.br-roulette__grid-cell:hover {
  transform: scale(1.05);
  z-index: 2;
}
.br-roulette__grid-cell--red:hover {
  box-shadow: 0 0 12px rgba(198,40,40,0.4), inset 0 0 8px rgba(198,40,40,0.2);
  border-color: rgba(198,40,40,0.6);
}
.br-roulette__grid-cell--black:hover {
  box-shadow: 0 0 12px rgba(255,255,255,0.1), inset 0 0 8px rgba(255,255,255,0.05);
  border-color: rgba(255,255,255,0.15);
}
.br-roulette__grid-cell--green:hover {
  box-shadow: 0 0 12px rgba(0,230,118,0.4), inset 0 0 8px rgba(0,230,118,0.2);
  border-color: rgba(0,230,118,0.6);
}
/* Celula com aposta ativa */
.br-roulette__grid-cell--active {
  border-color: rgba(255,215,0,0.4);
  box-shadow: 0 0 8px rgba(255,215,0,0.15);
}
/* Celula desabilitada (no_more_bets) */
.br-roulette__grid-cell--disabled {
  opacity: 0.5;
  pointer-events: none;
  cursor: not-allowed;
}
/* Celula vencedora (resultado) */
.br-roulette__grid-cell--winner {
  animation: cellWinPulse 1.5s ease infinite;
  border-color: rgba(0,230,118,0.8);
  z-index: 3;
}
@keyframes cellWinPulse {
  0%, 100% { box-shadow: 0 0 10px rgba(0,230,118,0.3); }
  50% { box-shadow: 0 0 25px rgba(0,230,118,0.6), 0 0 50px rgba(0,230,118,0.2); }
}
```

## §1.13 Chip Stack (Fichas de Aposta)

Fichas empilhadas sobre as celulas. 5 valores com cores distintas. Cada ficha tem borda, sombra e texto centralizado.

```css
/* COPIAR EXATAMENTE — chip */
.br-roulette__chip {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: clamp(22px, 2.8vw, 30px);
  height: clamp(22px, 2.8vw, 30px);
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(7px, 0.8vw, 9px);
  font-weight: 700;
  color: #FFFFFF;
  text-shadow: 0 1px 2px rgba(0,0,0,0.5);
  z-index: 4;
  pointer-events: none;
}
.br-roulette__chip--100   { background: radial-gradient(circle at 35% 35%, #5C6BC0, #303F9F); }
.br-roulette__chip--500   { background: radial-gradient(circle at 35% 35%, #66BB6A, #2E7D32); }
.br-roulette__chip--1000  { background: radial-gradient(circle at 35% 35%, #EF5350, #C62828); }
.br-roulette__chip--5000  { background: radial-gradient(circle at 35% 35%, #AB47BC, #6A1B9A); }
.br-roulette__chip--10000 { background: radial-gradient(circle at 35% 35%, #FFD700, #8B6914); }

/* Chip selector (barra inferior) */
.br-roulette__chip-selector {
  display: flex;
  gap: clamp(4px, 0.5vw, 8px);
  align-items: center;
  padding: clamp(4px, 0.5vh, 8px) 0;
}
.br-roulette__chip-selector-item {
  width: clamp(36px, 4.5vw, 48px);
  height: clamp(36px, 4.5vw, 48px);
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid transparent;
  transition: all 0.15s;
  min-width: 44px;
  min-height: 44px;
}
.br-roulette__chip-selector-item--selected {
  border-color: #FFD700;
  box-shadow: 0 0 12px rgba(255,215,0,0.3);
  transform: scale(1.1);
}
.br-roulette__chip-selector-item:hover {
  transform: scale(1.08);
  border-color: rgba(255,215,0,0.4);
}
```

## §1.14 Lightning Flash (Raio nos Numeros)

Efeito de raio dourado que aparece nos numeros selecionados com multiplicador. Fase Lightning entre betting e spinning.

```css
/* COPIAR EXATAMENTE — lightning flash overlay no numero */
.br-roulette__lightning-cell {
  position: relative;
  overflow: visible;
}
.br-roulette__lightning-cell::after {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 4px;
  background: transparent;
  border: 2px solid transparent;
  animation: lightningPulse 0.8s ease infinite;
  z-index: 5;
  pointer-events: none;
}
@keyframes lightningPulse {
  0%, 100% {
    border-color: rgba(255,215,0,0.3);
    box-shadow: 0 0 8px rgba(255,215,0,0.2), inset 0 0 4px rgba(255,215,0,0.1);
  }
  50% {
    border-color: rgba(255,215,0,0.8);
    box-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.2), inset 0 0 10px rgba(255,215,0,0.3);
  }
}
/* Flash de tela durante fase Lightning */
.br-roulette__lightning-screen-flash {
  position: absolute;
  inset: 0;
  background: rgba(255,215,0,0.06);
  opacity: 0;
  pointer-events: none;
  z-index: 10;
}
@keyframes screenFlash {
  0% { opacity: 0; }
  10% { opacity: 1; }
  100% { opacity: 0; }
}
```

## §1.15 Multiplier Badge (Badge Flutuante de Multiplicador)

Badge que aparece sobre o numero com multiplicador. Numero grande com glow. Animado com Framer Motion (scale bounce).

```css
/* COPIAR EXATAMENTE — badge multiplicador flutuante */
.br-roulette__multiplier-badge {
  position: absolute;
  top: -20px;
  left: 50%;
  transform: translateX(-50%);
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  background: linear-gradient(180deg, rgba(255,215,0,0.2) 0%, rgba(212,168,67,0.1) 100%);
  border: 1px solid rgba(255,215,0,0.5);
  border-radius: 4px;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(11px, 1.3vw, 15px);
  font-weight: 900;
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.5);
  white-space: nowrap;
  z-index: 8;
  pointer-events: none;
}
.br-roulette__multiplier-badge--big {
  font-size: clamp(14px, 1.6vw, 20px);
  border-color: rgba(255,215,0,0.8);
  box-shadow: 0 0 16px rgba(255,215,0,0.3);
  text-shadow: 0 0 16px rgba(255,215,0,0.7);
}
```

## §1.16 Racetrack (Apostas Vizinhos do Zero)

Representacao oval da roda com numeros em sequencia para apostas por vizinhanca. Overlay sobre a mesa principal.

```css
/* COPIAR EXATAMENTE — racetrack overlay */
.br-roulette__racetrack {
  position: absolute;
  top: clamp(40px, 5vh, 60px);
  left: 50%;
  transform: translateX(-50%);
  width: clamp(300px, 40vw, 500px);
  height: clamp(100px, 12vh, 140px);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 50px;
  background: rgba(10,10,10,0.95);
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;
  gap: 1px;
  padding: clamp(4px, 0.5vw, 8px);
  z-index: 20;
  backdrop-filter: blur(4px);
}
.br-roulette__racetrack-number {
  width: clamp(20px, 2.5vw, 28px);
  height: clamp(14px, 1.8vh, 20px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(8px, 0.9vw, 10px);
  font-weight: 600;
  cursor: pointer;
  border-radius: 2px;
  transition: all 0.1s;
}
.br-roulette__racetrack-number:hover {
  background: rgba(212,168,67,0.15);
  transform: scale(1.1);
}
.br-roulette__racetrack-section {
  border: 1px solid rgba(212,168,67,0.1);
  border-radius: 4px;
  padding: 2px 4px;
  font-family: 'Cinzel', serif;
  font-size: clamp(8px, 0.9vw, 10px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 1px;
}
```

## §1.17 Mini Roulette Board (Modo Mini / Double)

Board simplificado com 15 casas (7 vermelhas, 7 pretas, 1 branca). Layout circular ou linear.

```css
/* COPIAR EXATAMENTE — mini roulette board */
.br-roulette__mini-board {
  display: flex;
  gap: clamp(2px, 0.3vw, 4px);
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  max-width: clamp(300px, 40vw, 450px);
}
.br-roulette__mini-cell {
  width: clamp(36px, 4.5vw, 48px);
  height: clamp(36px, 4.5vw, 48px);
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(12px, 1.4vw, 16px);
  font-weight: 700;
  color: #FFFFFF;
  cursor: pointer;
  border: 1px solid rgba(255,255,255,0.08);
  transition: all 0.15s;
  min-width: 44px;
  min-height: 44px;
}
.br-roulette__mini-cell--red {
  background: linear-gradient(180deg, rgba(198,40,40,0.6) 0%, rgba(198,40,40,0.35) 100%);
}
.br-roulette__mini-cell--black {
  background: linear-gradient(180deg, rgba(40,40,40,0.8) 0%, rgba(20,20,20,0.6) 100%);
}
.br-roulette__mini-cell--white {
  background: linear-gradient(180deg, rgba(255,255,255,0.15) 0%, rgba(255,255,255,0.05) 100%);
  border-color: rgba(255,215,0,0.3);
}
.br-roulette__mini-cell:hover {
  transform: scale(1.08);
  border-color: rgba(255,215,0,0.4);
  box-shadow: 0 0 10px rgba(255,215,0,0.15);
}
.br-roulette__mini-cell--winner {
  animation: miniCellWin 1s ease infinite;
}
@keyframes miniCellWin {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.3); transform: scale(1); }
  50% { box-shadow: 0 0 20px rgba(0,230,118,0.6); transform: scale(1.05); }
}
```

## §1.18 History Bar (Fila de Resultados)

Barra horizontal com ultimos 20 resultados como circulos coloridos. Scroll horizontal se necessario.

```css
/* COPIAR EXATAMENTE — history bar */
.br-roulette__history-bar {
  display: flex;
  gap: clamp(3px, 0.4vw, 5px);
  overflow-x: auto;
  padding: clamp(4px, 0.5vh, 6px) 0;
  scrollbar-width: thin;
  scrollbar-color: rgba(212,168,67,0.3) transparent;
}
.br-roulette__history-item {
  flex-shrink: 0;
  width: clamp(22px, 2.8vw, 28px);
  height: clamp(22px, 2.8vw, 28px);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(8px, 0.9vw, 10px);
  font-weight: 700;
  color: #FFFFFF;
  border: 1px solid rgba(255,255,255,0.1);
}
.br-roulette__history-item--red { background: rgba(198,40,40,0.6); }
.br-roulette__history-item--black { background: rgba(40,40,40,0.8); }
.br-roulette__history-item--green { background: rgba(0,230,118,0.4); }
.br-roulette__history-item--multiplier {
  border-color: rgba(255,215,0,0.5);
  box-shadow: 0 0 6px rgba(255,215,0,0.2);
}
```

## §1.19 Bet Amount Bar (Barra de Aposta)

Barra inferior com chip selector + total apostado + botoes de acao.

```css
/* COPIAR EXATAMENTE — bet amount bar */
.br-roulette__bet-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: clamp(8px, 1vw, 14px);
  padding: clamp(6px, 0.8vh, 10px) clamp(10px, 1.2vw, 16px);
  background: rgba(17,17,17,0.95);
  border-top: 1px solid rgba(255,255,255,0.04);
  border-radius: 0 0 8px 8px;
}
.br-roulette__bet-total {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(14px, 1.6vw, 18px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.3);
}
.br-roulette__bet-actions {
  display: flex;
  gap: clamp(4px, 0.5vw, 8px);
}
.br-roulette__bet-action {
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 4px;
  color: #999999;
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  cursor: pointer;
  transition: all 0.15s;
  min-height: 44px;
  display: flex;
  align-items: center;
}
.br-roulette__bet-action:hover {
  background: rgba(212,168,67,0.08);
  color: #D4A843;
  border-color: rgba(212,168,67,0.3);
}
.br-roulette__bet-action:active {
  transform: scale(0.96);
}
.br-roulette__bet-action--disabled {
  opacity: 0.4;
  pointer-events: none;
  cursor: not-allowed;
}
```

## §1.20 Mode Select Cards (Selecao de Modo)

3 cards para escolher entre Relampago, Classica e Mini. Cada um com glow distinto.

```css
/* COPIAR EXATAMENTE — mode select cards */
.br-roulette__mode-card {
  position: relative;
  padding: clamp(12px, 1.5vw, 20px);
  background: rgba(17,17,17,0.9);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
  overflow: hidden;
  min-height: 44px;
}
.br-roulette__mode-card--lightning:hover {
  border-color: rgba(255,215,0,0.4);
  box-shadow: 0 0 20px rgba(255,215,0,0.15), 0 0 40px rgba(255,215,0,0.05);
}
.br-roulette__mode-card--classic:hover {
  border-color: rgba(0,230,118,0.4);
  box-shadow: 0 0 20px rgba(0,230,118,0.15), 0 0 40px rgba(0,230,118,0.05);
}
.br-roulette__mode-card--mini:hover {
  border-color: rgba(79,195,247,0.4);
  box-shadow: 0 0 20px rgba(79,195,247,0.15), 0 0 40px rgba(79,195,247,0.05);
}
.br-roulette__mode-card--selected {
  border-width: 2px;
}
.br-roulette__mode-card--lightning.br-roulette__mode-card--selected {
  border-color: rgba(255,215,0,0.6);
  box-shadow: 0 0 30px rgba(255,215,0,0.2);
}
.br-roulette__mode-card--classic.br-roulette__mode-card--selected {
  border-color: rgba(0,230,118,0.6);
  box-shadow: 0 0 30px rgba(0,230,118,0.2);
}
.br-roulette__mode-card--mini.br-roulette__mode-card--selected {
  border-color: rgba(79,195,247,0.6);
  box-shadow: 0 0 30px rgba(79,195,247,0.2);
}
.br-roulette__mode-card:active {
  transform: scale(0.98);
}
```

---

**[FIM DA PARTE 1 — Continua Parte 2: Timelines 24+ tabeladas + Atmosferas por estado + Checklist §27 expandido]**

`[ENTREGA: 4.ADENDO-DS-BRAZILIAN-ROULETTE-PARTE-2-ADENDO.md — 24/03/2026]`
