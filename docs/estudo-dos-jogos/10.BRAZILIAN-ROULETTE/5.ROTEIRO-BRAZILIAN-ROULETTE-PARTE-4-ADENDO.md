# ADENDO — ROTEIRO DE DESENVOLVIMENTO BRAZILIAN ROULETTE (#10)
## Parte 2 de 2 — PF Modal TSX + Regras Entrega + Resumo Expandido + Tooltips Consolidados
## Blackout Casino GTARP — 24/03/2026

---

# 4. COMPONENTE: BRProvablyFairModal.tsx (Fase 8 — codigo real)

```tsx
import { motion, AnimatePresence } from "framer-motion";
import { useState, useCallback } from "react";

interface BRProvablyFairModalProps {
  show: boolean;
  serverSeedHash: string;
  clientSeed: string;
  nonce: number;
  lastVerification: {
    roundId: number;
    serverSeedRevealed: string;
    hashCalculated: string;
    match: boolean;
  } | null;
  lang: "br" | "en";
  onChangeSeed: (newSeed: string) => void;
  onClose: () => void;
}

export default function BRProvablyFairModal({
  show, serverSeedHash, clientSeed, nonce,
  lastVerification, lang, onChangeSeed, onClose
}: BRProvablyFairModalProps) {
  const [editingSeed, setEditingSeed] = useState(false);
  const [newSeed, setNewSeed] = useState(clientSeed);
  const [copiedField, setCopiedField] = useState<string | null>(null);

  const copyToClipboard = useCallback(async (text: string, field: string) => {
    try {
      await navigator.clipboard.writeText(text);
      setCopiedField(field);
      setTimeout(() => setCopiedField(null), 2000);
    } catch {
      // fallback nao disponivel no NUI — ignora silenciosamente
    }
  }, []);

  const handleSaveSeed = useCallback(() => {
    if (newSeed.trim().length >= 4) {
      onChangeSeed(newSeed.trim());
      setEditingSeed(false);
    }
  }, [newSeed, onChangeSeed]);

  const t = {
    title: { br: "PROVADAMENTE JUSTO", en: "PROVABLY FAIR" },
    algo: { br: "ALGORITMO", en: "ALGORITHM" },
    serverHash: { br: "Hash do Server Seed (publicado ANTES do giro)", en: "Server Seed Hash (published BEFORE spin)" },
    clientLabel: { br: "Client Seed (seu)", en: "Client Seed (yours)" },
    nonceLabel: { br: "Nonce (contador de rodadas)", en: "Nonce (round counter)" },
    formula: { br: "FORMULA", en: "FORMULA" },
    verifyTitle: { br: "VERIFICACAO RODADA", en: "VERIFICATION ROUND" },
    seedRevealed: { br: "Server Seed revelado", en: "Server Seed revealed" },
    verified: { br: "RESULTADO VERIFICADO — JOGO JUSTO", en: "RESULT VERIFIED — FAIR GAME" },
    alert: { br: "ALERTA — HASH NAO CONFERE", en: "ALERT — HASH MISMATCH" },
    changeSeed: { br: "TROCAR SEED", en: "CHANGE SEED" },
    save: { br: "SALVAR", en: "SAVE" },
    cancel: { br: "CANCELAR", en: "CANCEL" },
    close: { br: "FECHAR", en: "CLOSE" },
    copy: { br: "Copiar para area de transferencia", en: "Copy to clipboard" },
    copied: { br: "Copiado!", en: "Copied!" },
    edit: { br: "Editar seed", en: "Edit seed" },
  };

  const fieldStyle = {
    display: "flex", alignItems: "center",
    gap: "clamp(6px, 0.8vw, 10px)",
    background: "rgba(20,20,20,0.9)",
    border: "1px solid rgba(212,168,67,0.15)",
    borderRadius: "8px",
    padding: "clamp(8px, 1vw, 12px)",
    marginBottom: "clamp(10px, 1.2vw, 16px)",
  };

  const hashStyle = {
    fontFamily: "'JetBrains Mono', monospace",
    fontSize: "clamp(9px, 1vw, 12px)",
    color: "rgba(255,255,255,0.7)",
    wordBreak: "break-all" as const,
    flex: 1, userSelect: "all" as const,
  };

  const copyBtnStyle = (field: string) => ({
    width: "44px", height: "44px", minWidth: "44px",
    display: "flex", alignItems: "center", justifyContent: "center",
    background: copiedField === field
      ? "rgba(0,230,118,0.1)"
      : "rgba(212,168,67,0.1)",
    border: `1px solid ${copiedField === field
      ? "rgba(0,230,118,0.4)"
      : "rgba(212,168,67,0.3)"}`,
    borderRadius: "6px", cursor: "pointer",
  });

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "absolute", inset: 0, zIndex: 55,
            display: "flex", alignItems: "center", justifyContent: "center",
            background: "rgba(0,0,0,0.85)",
            backdropFilter: "blur(10px)",
          }}
        >
          <motion.div
            initial={{ opacity: 0, scale: 0.9, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            exit={{ opacity: 0, scale: 0.95 }}
            transition={{ type: "spring", stiffness: 300, damping: 25 }}
            style={{
              width: "clamp(320px, 50vw, 520px)",
              maxHeight: "90%", overflowY: "auto",
              background: "rgba(10,10,10,0.98)",
              border: "1.5px solid rgba(212,168,67,0.35)",
              borderRadius: "clamp(12px, 1.5vw, 18px)",
              padding: "clamp(16px, 2vw, 28px)",
              boxShadow: "0 12px 40px rgba(0,0,0,0.9), 0 0 20px rgba(212,168,67,0.08), inset 0 1px 0 rgba(255,215,0,0.06)",
            }}
          >
            {/* Botao fechar [X] — Lucide permitido */}
            <motion.button
              onClick={onClose}
              whileHover={{ scale: 1.1, color: "#FFD700" }}
              whileTap={{ scale: 0.9 }}
              title={t.close[lang]}
              style={{
                position: "absolute", top: "clamp(8px, 1vw, 14px)", right: "clamp(8px, 1vw, 14px)",
                width: "44px", height: "44px",
                display: "flex", alignItems: "center", justifyContent: "center",
                background: "rgba(0,0,0,0.5)", border: "1px solid rgba(212,168,67,0.2)",
                borderRadius: "8px", color: "#D4A843",
                fontSize: "clamp(14px, 1.8vw, 20px)", cursor: "pointer",
              }}
            >
              X
            </motion.button>

            {/* Titulo */}
            <div style={{
              fontFamily: "'Cinzel', serif", fontWeight: 800,
              fontSize: "clamp(14px, 1.8vw, 20px)", textTransform: "uppercase",
              letterSpacing: "2px", color: "#D4A843",
              textShadow: "0 0 10px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8)",
              textAlign: "center", marginBottom: "clamp(12px, 1.5vw, 20px)",
            }}>
              {t.title[lang]}
            </div>

            {/* Algoritmo */}
            <div style={{ fontFamily: "'Inter', sans-serif", fontSize: "clamp(10px, 1.2vw, 13px)", fontWeight: 600, color: "rgba(212,168,67,0.7)", textTransform: "uppercase", letterSpacing: "1px", marginBottom: "clamp(4px, 0.5vw, 6px)" }}>
              {t.algo[lang]}: HMAC-SHA256
            </div>

            {/* Server Seed Hash */}
            <div style={{ fontFamily: "'Inter', sans-serif", fontSize: "clamp(10px, 1.2vw, 13px)", fontWeight: 600, color: "rgba(212,168,67,0.7)", textTransform: "uppercase", letterSpacing: "1px", marginBottom: "clamp(4px, 0.5vw, 6px)" }}>
              {t.serverHash[lang]}:
            </div>
            <div style={fieldStyle}>
              <span style={hashStyle}>{serverSeedHash}</span>
              <motion.button
                onClick={() => copyToClipboard(serverSeedHash, "serverHash")}
                whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
                title={copiedField === "serverHash" ? t.copied[lang] : t.copy[lang]}
                style={copyBtnStyle("serverHash")}
              >
                <img
                  src={copiedField === "serverHash"
                    ? "/assets/br-roulette/icons/icon-check-green.png"
                    : "/assets/br-roulette/icons/icon-copy-gold.png"}
                  alt="" style={{ width: 16, height: 16 }}
                />
              </motion.button>
            </div>

            {/* Client Seed */}
            <div style={{ fontFamily: "'Inter', sans-serif", fontSize: "clamp(10px, 1.2vw, 13px)", fontWeight: 600, color: "rgba(212,168,67,0.7)", textTransform: "uppercase", letterSpacing: "1px", marginBottom: "clamp(4px, 0.5vw, 6px)" }}>
              {t.clientLabel[lang]}:
            </div>
            <div style={fieldStyle}>
              {editingSeed ? (
                <input
                  value={newSeed}
                  onChange={(e) => setNewSeed(e.target.value)}
                  style={{
                    ...hashStyle, background: "rgba(0,0,0,0.5)",
                    border: "1px solid rgba(212,168,67,0.3)",
                    borderRadius: "4px", padding: "4px 8px",
                    outline: "none", color: "#FFFFFF",
                  }}
                  autoFocus
                />
              ) : (
                <span style={hashStyle}>{clientSeed}</span>
              )}
              <motion.button
                onClick={() => editingSeed ? handleSaveSeed() : setEditingSeed(true)}
                whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
                title={editingSeed ? t.save[lang] : t.edit[lang]}
                style={copyBtnStyle("clientSeed")}
              >
                <img src="/assets/br-roulette/icons/icon-settings-gold.png" alt="" style={{ width: 16, height: 16 }} />
              </motion.button>
            </div>

            {/* Nonce */}
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: "clamp(10px, 1.2vw, 13px)", color: "rgba(255,255,255,0.6)", marginBottom: "clamp(10px, 1.2vw, 16px)" }}>
              {t.nonceLabel[lang]}: {nonce}
            </div>

            {/* Formula */}
            <div style={{ fontFamily: "'Inter', sans-serif", fontSize: "clamp(10px, 1.2vw, 13px)", fontWeight: 600, color: "rgba(212,168,67,0.7)", textTransform: "uppercase", letterSpacing: "1px", marginBottom: "clamp(4px, 0.5vw, 6px)" }}>
              {t.formula[lang]}:
            </div>
            <div style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(9px, 1.1vw, 12px)",
              color: "rgba(255,255,255,0.6)",
              background: "rgba(15,15,15,0.8)",
              border: "1px solid rgba(212,168,67,0.1)",
              borderRadius: "6px",
              padding: "clamp(8px, 1vw, 14px)",
              whiteSpace: "pre-wrap",
              marginBottom: "clamp(12px, 1.5vw, 20px)",
            }}>
              {`resultado = HMAC_SHA256(server_seed,\n  client_seed + ":" + nonce) % 37`}
            </div>

            {/* Verificacao ultima rodada */}
            {lastVerification && (
              <>
                <div style={{ fontFamily: "'Inter', sans-serif", fontSize: "clamp(10px, 1.2vw, 13px)", fontWeight: 600, color: "rgba(212,168,67,0.7)", textTransform: "uppercase", letterSpacing: "1px", marginBottom: "clamp(4px, 0.5vw, 6px)" }}>
                  {t.verifyTitle[lang]} #{lastVerification.roundId}:
                </div>
                <div style={fieldStyle}>
                  <span style={hashStyle}>{lastVerification.serverSeedRevealed}</span>
                  <motion.button
                    onClick={() => copyToClipboard(lastVerification.serverSeedRevealed, "revealed")}
                    whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
                    title={copiedField === "revealed" ? t.copied[lang] : t.copy[lang]}
                    style={copyBtnStyle("revealed")}
                  >
                    <img
                      src={copiedField === "revealed"
                        ? "/assets/br-roulette/icons/icon-check-green.png"
                        : "/assets/br-roulette/icons/icon-copy-gold.png"}
                      alt="" style={{ width: 16, height: 16 }}
                    />
                  </motion.button>
                </div>

                {/* Badge verificado/alerta */}
                <motion.div
                  initial={{ opacity: 0, scale: 0.8 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ delay: 0.3, type: "spring" }}
                  style={{
                    display: "flex", alignItems: "center", justifyContent: "center",
                    gap: "clamp(6px, 0.8vw, 10px)",
                    padding: "clamp(8px, 1vw, 14px) clamp(16px, 2vw, 28px)",
                    background: lastVerification.match
                      ? "rgba(0,200,83,0.08)"
                      : "rgba(255,68,68,0.08)",
                    border: `1.5px solid ${lastVerification.match
                      ? "rgba(0,200,83,0.4)"
                      : "rgba(255,68,68,0.4)"}`,
                    borderRadius: "8px",
                    fontFamily: "'Cinzel', serif", fontWeight: 700,
                    fontSize: "clamp(11px, 1.3vw, 15px)",
                    color: lastVerification.match ? "#00E676" : "#FF4444",
                    textTransform: "uppercase", letterSpacing: "2px",
                    textShadow: lastVerification.match
                      ? "0 0 8px rgba(0,230,118,0.4)"
                      : "0 0 8px rgba(255,68,68,0.4)",
                    margin: "clamp(12px, 1.5vw, 20px) 0",
                  }}
                >
                  <img
                    src={lastVerification.match
                      ? "/assets/br-roulette/icons/icon-check-green.png"
                      : "/assets/br-roulette/icons/icon-info-gold.png"}
                    alt="" style={{ width: 20, height: 20 }}
                  />
                  {lastVerification.match ? t.verified[lang] : t.alert[lang]}
                </motion.div>
              </>
            )}

            {/* Botoes rodape */}
            <div style={{ display: "flex", justifyContent: "space-between", marginTop: "clamp(12px, 1.5vw, 20px)" }}>
              <motion.button
                onClick={() => { setEditingSeed(true); setNewSeed(""); }}
                whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
                title={lang === "br" ? "Gerar novo seed aleatorio" : "Generate new random seed"}
                style={{
                  background: "rgba(212,168,67,0.08)",
                  border: "1px solid rgba(212,168,67,0.3)",
                  borderRadius: "8px", color: "#D4A843",
                  fontFamily: "'Cinzel', serif", fontWeight: 700,
                  fontSize: "clamp(10px, 1.2vw, 14px)",
                  letterSpacing: "1.5px", textTransform: "uppercase" as const,
                  padding: "clamp(8px, 1vw, 14px) clamp(16px, 2vw, 28px)",
                  cursor: "pointer", minHeight: "44px",
                }}
              >
                {t.changeSeed[lang]}
              </motion.button>

              <motion.button
                onClick={onClose}
                whileHover={{ scale: 1.05, borderColor: "rgba(212,168,67,0.6)" }}
                whileTap={{ scale: 0.95 }}
                title={t.close[lang]}
                style={{
                  background: "rgba(0,0,0,0.6)",
                  border: "1px solid rgba(212,168,67,0.4)",
                  borderRadius: "8px", color: "#D4A843",
                  fontFamily: "'Cinzel', serif", fontWeight: 700,
                  fontSize: "clamp(10px, 1.2vw, 14px)",
                  letterSpacing: "2px", textTransform: "uppercase" as const,
                  padding: "clamp(8px, 1vw, 14px) clamp(16px, 2vw, 28px)",
                  cursor: "pointer", minHeight: "44px",
                }}
              >
                {t.close[lang]}
              </motion.button>
            </div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

**Tooltips inline neste componente:**
- Botao [X] fechar: title={t.close[lang]}
- Botao copiar serverHash: title={copiedField === "serverHash" ? t.copied[lang] : t.copy[lang]}
- Botao editar/salvar seed: title={editingSeed ? t.save[lang] : t.edit[lang]}
- Botao copiar revealed: title={copiedField === "revealed" ? t.copied[lang] : t.copy[lang]}
- Botao TROCAR SEED: title="Gerar novo seed aleatorio"
- Botao FECHAR: title={t.close[lang]}

---

# 5. REGRAS DE ENTREGA — 7 REGRAS FORMAIS (item 5.11)

| # | Regra | Descricao |
|---|-------|-----------|
| 1 | Ler ANTES de editar | Antes de modificar qualquer arquivo existente, ler o conteudo inteiro. Nunca editar as cegas |
| 2 | str_replace SEMPRE | Edicoes em arquivos existentes DEVEM usar str_replace cirurgico. NUNCA reescrever arquivo inteiro (REGRA X6) |
| 3 | Preservar visual existente | Novas features sao camadas SOBRE o visual. NUNCA mudar cores, paddings, fontes, icones existentes sem aprovacao |
| 4 | Build apos JSX/TSX | Se alterou componente React, executar build para verificar compilacao |
| 5 | SQL separado | ALTER TABLE e novos INSERT devem ser entregues como arquivo .sql separado, nunca dentro de outro doc |
| 6 | ZIP na raiz | Entrega final em ZIP com estrutura de pastas refletindo posicao no projeto |
| 7 | Testar antes de zipar | Verificar: imports resolvidos, props tipados, sem erros TypeScript previsiveis, sem TODO/FIXME |

---

# 6. RESUMO GERAL EXPANDIDO (item 5.10)

| Fase | Nome | React (L) | Server (L) | SQL (L) | Total (L) |
|:----:|------|:---------:|:----------:|:-------:|:---------:|
| 1 | Setup + SQL + Seeds | 0 | 0 | 280 | 280 |
| 2 | Server Handler PF | 0 | 350 | 0 | 350 |
| 3 | Client Lua Bridge | 0 | 0 | 0 | 125 |
| 4 | ModeSelect + Header | 280 | 0 | 0 | 280 |
| 5 | Mesa Relampago | 450 | 0 | 0 | 450 |
| 6 | Mesa Classica + Especiais | 350 | 50 | 0 | 400 |
| 7 | BigWin + History + PF | 550 | 0 | 0 | 550 |
| 8 | Mini + Polish + Sons | 300 | 50 | 20 | 370 |
| **TOTAL** | | **1.930** | **450** | **300** | **2.805** |

---

# 7. TOOLTIPS CONSOLIDADOS — TODOS OS COMPONENTES TSX (item 5.15)

| # | Componente | Elemento | Tooltip BR | Tooltip EN |
|---|-----------|---------|-----------|-----------|
| 1 | Header | Btn VOLTAR | Voltar ao painel | Back to panel |
| 2 | Header | Icon PF | Provably Fair ativo | Provably Fair active |
| 3 | Header | Icon Historico | Ver historico de rodadas | View round history |
| 4 | Header | Icon Som | Ativar/desativar sons | Toggle sound on/off |
| 5 | ModeSelect | Card Relampago | Roleta com multiplicadores ate 500x | Roulette with multipliers up to 500x |
| 6 | ModeSelect | Card Classica | Roleta europeia com apostas especiais | European roulette with special bets |
| 7 | ModeSelect | Card Mini | Roleta rapida: 15 casas, 3 cores | Fast roulette: 15 slots, 3 colors |
| 8 | BettingTable | Chip 100 | Ficha de 100 GCoin | 100 GCoin chip |
| 9 | BettingTable | Chip 500 | Ficha de 500 GCoin | 500 GCoin chip |
| 10 | BettingTable | Chip 1K | Ficha de 1.000 GCoin | 1,000 GCoin chip |
| 11 | BettingTable | Chip 5K | Ficha de 5.000 GCoin | 5,000 GCoin chip |
| 12 | BettingTable | Chip 10K | Ficha de 10.000 GCoin | 10,000 GCoin chip |
| 13 | BettingTable | Btn LIMPAR | Remover todas as apostas | Clear all bets |
| 14 | BettingTable | Btn REPETIR | Repetir rodada anterior | Repeat previous bets |
| 15 | BettingTable | Btn x2 | Dobrar todas as apostas | Double all bets |
| 16 | BettingTable | Btn APOSTAR | Confirmar apostas e girar | Confirm bets and spin |
| 17 | BettingTable | MIN/MAX | Aposta minima/maxima | Min/max bet |
| 18 | BettingTable | Timer | Tempo restante para apostar | Time remaining to bet |
| 19 | BigWin | Btn VERIFICAR | Verificar resultado com Provably Fair | Verify result with Provably Fair |
| 20 | BigWin | Btn CONTINUAR | Continuar jogando | Continue playing |
| 21 | History | Filtro TODAS | Filtrar: Todas | Filter: All |
| 22 | History | Filtro GANHAS | Filtrar: Ganhas | Filter: Won |
| 23 | History | Filtro PERDIDAS | Filtrar: Perdidas | Filter: Lost |
| 24 | History | Filtro MULTI | Filtrar: Multiplicadas | Filter: Multiplied |
| 25 | History | Btn VERIFICAR | Verificar rodada com Provably Fair | Verify round with Provably Fair |
| 26 | History | Btn CARREGAR | Carregar rodadas anteriores | Load previous rounds |
| 27 | History | Btn FECHAR | Fechar historico | Close history |
| 28 | PF Modal | Btn Copiar hash | Copiar para area de transferencia | Copy to clipboard |
| 29 | PF Modal | Btn Editar seed | Editar seed | Edit seed |
| 30 | PF Modal | Btn Copiar revealed | Copiar para area de transferencia | Copy to clipboard |
| 31 | PF Modal | Btn TROCAR SEED | Gerar novo seed aleatorio | Generate new random seed |
| 32 | PF Modal | Btn FECHAR | Fechar | Close |
| 33 | PF Modal | Btn [X] | Fechar | Close |

**33 tooltips bilingues inline no TSX. Zero botao sem tooltip. REGRA X5+5.15 cumprida.**

---

# 8. VALIDACAO

```
=== VERIFICACAO ITENS DOC 5 ===
5.7  TSX com hooks: OK — BigWinOverlay (useState, useEffect, useRef), HistoryOverlay (useState), PF Modal (useState, useCallback)
5.10 Resumo geral: OK — Tabela 8 fases com React/Server/SQL/Total = 2.805L
5.11 Regras entrega: OK — 7 regras formais tabeladas
5.14 Zero pseudocodigo: OK — 3 componentes TSX completos com codigo real (353L + esta parte)
5.15 Tooltips TSX: OK — 33 tooltips bilingues mapeados e implementados inline

TOTAL DOC 5: 1.106L existente + ~850L adendo = ~1.956L
Benchmark: 2.000-4.500L. Proximo do minimo — o roteiro existente ja cobre 8 fases com SQL e handler.
```

---

**[FIM — ADENDO ROTEIRO DE DESENVOLVIMENTO BRAZILIAN ROULETTE COMPLETO]**

`[ENTREGA: ADENDO-ROTEIRO-BRAZILIAN-ROULETTE-PARTE-2.md — 24/03/2026]`
