# PROMPT-V0 — SLOT-MACHINE (#2)
## Parte 1 de 2 - Contexto + Telas 1-5 (ModeSelect, VideoSlot, Spin/Tumble, FS Trigger, FS Playing)
## Copiar INTEIRO e colar no V0 / Bolt / ChatGPT
### Colar JUNTO com o arquivo CSS-COMPONENTES-SLOT-MACHINE.md
### NAO editar — prompt pronto para uso direto
### Blackout Casino GTARP - 19/03/2026

---

Crie um componente React TypeScript chamado "SlotsGame".

O componente e a tela do jogo "Slot Machine" para um cassino premium dentro do GTA RP (FiveM). Visual Las Vegas AAA, dark theme, ouro metalico, verde neon. Sistema hibrido Classic (3 reels) + Video (6x5 scatter pays com tumble cascade, multiplicadores e free spins). Provably Fair HMAC-SHA256.

RESTRICOES TECNICAS:
- React 19 + TypeScript + Framer Motion 11 (importar de "framer-motion")
- Dark mode APENAS — fundo #0A0A0A, NUNCA branco, NUNCA cinza claro
- CSS inline via style={{}} — NAO usar classes Tailwind para componentes do jogo
- Fontes: 'Cinzel' para titulos/botoes, 'Inter' para corpo, 'JetBrains Mono' para valores/numeros
- Responsividade: TUDO com clamp() — nunca px fixo para fontes/padding
- Framer Motion para: entrada, hover/tap, stagger, AnimatePresence mode="popLayout" no grid
- AnimatePresence mode="popLayout" OBRIGATORIO para tumble (remove exiting do layout flow)
- Bilingue: prop lang="br"|"en", todos os textos via objeto { br: "...", en: "..." }
- Touch targets: minimo 44px em TODOS os botoes
- Export: export default function SlotsGame({ onBack, lang, playerId, initialBalance })

ESTE PROMPT COBRE 10 TELAS/ESTADOS:
1. MODE SELECT (Classic vs Video)
2. VIDEO SLOT IDLE (grid 6x5 + bet controls)
3. SPINNING + TUMBLING (animacao grid)
4. FREE SPINS TRIGGER (overlay 3.2s)
5. FREE SPINS PLAYING (HUD dourado + multiplicadores)
6. WIN OVERLAY (4 tiers: Normal, Big, Mega, Jackpot)
7. CLASSIC SLOT (3 reels verticais)
8. PAYTABLE (modal com simbolos e payouts)
9. HISTORICO + PROVABLY FAIR (modal ultimos 20 spins)
10. BUY BONUS MODAL (confirmacao de compra)

---

PATHS DE IMAGENS (usar estes exatos):
```
Fundo:          /assets/shared/ui/bg-casino.png
Logo BR:        /assets/logos-br-para-cards/2.LOGO-BR-SLOT-MACHINE.png
Logo EN:        /assets/logos-in-para-cards/2.LOGO-IN-SLOT-MACHINE.png
Card:           /assets/image-para-cards/2.SLOT-MACHINE-IMAGE-768_1024.png
Imagem dourada: /assets/image-background-transparent/GTARP CASINO IMAGEM DOURADA/2.IMAGEM-DOURADA-SLOT-MACHINE.png
```

FUNDO DA TELA (usar em TODAS as telas):
```css
backgroundColor: "#0A0A0A",
backgroundImage: "url('/assets/shared/ui/bg-casino.png'), radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%), radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%)",
backgroundSize: "cover, 100% 100%, 100% 100%",
boxShadow: "inset 0 0 150px rgba(0,0,0,0.8)"
```

---

# TELA 1 — MODE SELECT (Selecao Classic vs Video)

```
+--------------------------------------------------------------+
| [< VOLTAR]                                    Saldo: GC 12.500|
|                                                                |
|                  SLOT MACHINE                                  |
|            "Escolha seu modo de jogo"                          |
|                                                                |
|   +------------------------+  +------------------------+      |
|   |        [EMOJI]         |  |        [EMOJI]         |      |
|   |                        |  |                        |      |
|   |       CLASSIC          |  |        VIDEO           |      |
|   |  3 rolos, rapido e     |  |  Grid 6x5, Scatter     |      |
|   |  nostalgico            |  |  Pays + Tumble          |      |
|   |                        |  |                        |      |
|   |     [ JOGAR ]          |  |     [ JOGAR ]          |      |
|   +------------------------+  +------------------------+      |
|                                                                |
+--------------------------------------------------------------+
```

BOTAO VOLTAR:
```css
position: "absolute", top: "clamp(8px,1.5vw,16px)", left: "clamp(8px,1.5vw,16px)",
background: "rgba(0,0,0,0.6)", border: "1px solid rgba(212,168,67,0.4)",
borderRadius: "8px", color: "#D4A843",
fontFamily: "'Cinzel', serif", fontWeight: 700, fontSize: "clamp(10px,1.2vw,14px)",
letterSpacing: "2px", padding: "clamp(5px,0.6vw,8px) clamp(8px,1.2vw,14px)",
minHeight: "44px", cursor: "pointer"
```
title (tooltip): "Retorna ao cassino" / "Return to casino"

SALDO:
```css
position: "absolute", top: "clamp(8px,1.5vw,16px)", right: "clamp(8px,1.5vw,16px)",
fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
fontSize: "clamp(12px,1.6vw,18px)", color: "#00E676",
textShadow: "0 0 8px rgba(0,230,118,0.4)"
```
title: "Seu saldo atual de GCoins" / "Your current GCoin balance"

TITULO "SLOT MACHINE":
```css
fontFamily: "'Cinzel', serif", fontWeight: 900,
fontSize: "clamp(24px,4.5vw,48px)", textTransform: "uppercase",
letterSpacing: "4px", color: "#D4A843",
textShadow: "0 0 25px rgba(212,168,67,0.5), 0 2px 10px rgba(0,0,0,1)"
```

SUBTITULO:
```css
fontFamily: "'Inter', sans-serif", fontWeight: 500,
fontSize: "clamp(10px,1.3vw,16px)", color: "#A8A8A8",
fontStyle: "italic"
```

MODE CARDS (2 cards lado a lado, gap clamp(16px,3vw,32px)):
```css
width: "clamp(180px,28vw,280px)", padding: "clamp(16px,2.5vw,32px)",
background: "rgba(5,5,5,0.9)", border: "1.5px solid rgba(212,168,67,0.2)",
borderRadius: "14px", cursor: "pointer",
display: "flex", flexDirection: "column", alignItems: "center",
gap: "clamp(8px,1.2vw,16px)",
boxShadow: "0 4px 16px rgba(0,0,0,0.4)"
```
Hover: border-color rgba(212,168,67,0.5)
Framer Motion: whileHover={{ scale: 1.03, y: -3 }}, whileTap={{ scale: 0.97 }}
Stagger: delay 0.15 * index, initial opacity 0 y 20 scale 0.9

ICONE DO CARD:
```css
width: "clamp(48px,7vw,72px)", height: "clamp(48px,7vw,72px)",
borderRadius: "12px", background: "rgba(212,168,67,0.08)",
border: "1px solid rgba(212,168,67,0.15)",
display: "flex", alignItems: "center", justifyContent: "center",
fontSize: "clamp(24px,3.5vw,36px)"
```

NOME DO MODO:
```css
fontFamily: "'Cinzel', serif", fontWeight: 700,
fontSize: "clamp(14px,1.8vw,20px)", color: "#D4A843",
textTransform: "uppercase", letterSpacing: "2px"
```

DESCRICAO DO MODO:
```css
fontFamily: "'Inter', sans-serif", fontWeight: 500,
fontSize: "clamp(10px,1.2vw,14px)", color: "#A8A8A8",
textAlign: "center", lineHeight: 1.5
```

BOTAO JOGAR (dentro do card):
```css
width: "100%", padding: "clamp(8px,1vw,12px) clamp(16px,2.5vw,28px)",
background: "linear-gradient(180deg, #00C853 0%, #004D25 100%)",
border: "1.5px solid rgba(0,230,118,0.3)", borderRadius: "8px",
color: "#FFFFFF", fontFamily: "'Cinzel', serif", fontWeight: 700,
fontSize: "clamp(10px,1.3vw,14px)", textTransform: "uppercase",
letterSpacing: "2px", cursor: "pointer", minHeight: "44px"
```
title: "Iniciar modo Classic/Video" / "Start Classic/Video mode"

ANIMACAO ENTRADA TELA:
```
initial={{ opacity: 0, scale: 0.92, y: 30 }}
animate={{ opacity: 1, scale: 1, y: 0 }}
transition={{ duration: 0.6, ease: [0, 0, 0.2, 1] }}
```

MOBILE (landscape 1280x720):
- Cards: flexWrap wrap, gap reduzido
- Titulo: fontSize reduzido via clamp
- Padding geral reduzido

Dados mock: balance 12.500

---

# TELA 2 — VIDEO SLOT IDLE (Grid 6x5 + Bet Controls)

```
+--------------------------------------------------------------+
| [< VOLTAR]     SLOT MACHINE      JACKPOT: GC 47.350  GC 12.500|
|---------------------------------------------------------------|
|                                                                |
|  +----------------------------------------------------------+ |
|  |  [frame dourado 2px solid]                                | |
|  |  +------+------+------+------+------+------+             | |
|  |  |crown |ring  |ruby  |topaz |sapp  |emer  |  row 0      | |
|  |  +------+------+------+------+------+------+             | |
|  |  |ame   |crown |scat  |ruby  |ring  |topaz |  row 1      | |
|  |  +------+------+------+------+------+------+             | |
|  |  |sapp  |emer  |crown |ame   |ruby  |scat  |  row 2      | |
|  |  +------+------+------+------+------+------+             | |
|  |  |topaz |ruby  |ring  |crown |emer  |ame   |  row 3      | |
|  |  +------+------+------+------+------+------+             | |
|  |  |ring  |topaz |ame   |sapp  |crown |ruby  |  row 4      | |
|  |  +------+------+------+------+------+------+             | |
|  +----------------------------------------------------------+ |
|                     WIN: -- GC                                 |
|---------------------------------------------------------------|
| [i] [H] | [MIN][x2][/2][MAX]  BET: 10 GC | [ANTE][SPIN][AUTO][TB] | [BONUS] |
+--------------------------------------------------------------+
```

HEADER (flex row, space-between):
```css
display: "flex", justifyContent: "space-between", alignItems: "center",
padding: "clamp(6px,0.8vw,10px) clamp(8px,1.5vw,16px)",
borderBottom: "1px solid rgba(212,168,67,0.1)",
flexShrink: 0, zIndex: 5
```

JACKPOT DISPLAY:
```css
fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
fontSize: "clamp(11px,1.4vw,16px)", color: "#FFD700",
textShadow: "0 0 8px rgba(255,215,0,0.4)"
```
title: "Jackpot progressivo — 1.5% de cada aposta alimenta o premio" / "Progressive jackpot — 1.5% of every bet feeds the prize"

FRAME DOURADO (container do grid):
```css
margin: "clamp(4px,0.8vw,8px) auto",
padding: "clamp(6px,1vw,12px)",
border: "2px solid #D4A843",
outline: "3px solid rgba(212,168,67,0.15)",
outlineOffset: "4px", borderRadius: "18px",
boxShadow: "0 0 30px rgba(212,168,67,0.08), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.4)",
background: "linear-gradient(180deg, #0F0F0F 0%, #0A0A0A 100%)",
width: "clamp(300px,70vw,600px)", flex: 1, maxHeight: "60vh",
display: "flex", alignItems: "center", justifyContent: "center",
position: "relative"
```

GRID 6x5 (CSS Grid):
```css
display: "grid",
gridTemplateColumns: "repeat(6, 1fr)",
gridTemplateRows: "repeat(5, 1fr)",
gap: "clamp(3px,0.4vw,6px)",
width: "100%", aspectRatio: "6 / 5"
```

CELULA SIMBOLO (normal):
```css
aspectRatio: "1", borderRadius: "8px",
background: "rgba(255,255,255,0.03)",
border: "1px solid rgba(255,255,255,0.04)",
display: "flex", alignItems: "center", justifyContent: "center",
fontSize: "clamp(18px,2.5vw,32px)",
transition: "all 0.3s ease"
```

CELULA SCATTER:
```css
background: "radial-gradient(circle, rgba(255,215,0,0.15) 0%, transparent 70%)",
border: "2px solid rgba(255,215,0,0.4)"
```

CELULA WINNING:
```css
opacity: 1, scale: 1.05,
boxShadow: "0 0 15px rgba(COLOR,0.5)",
border: "1.5px solid rgba(COLOR,0.6)"
```
Onde COLOR varia por simbolo: crown #FFD700, ring #D4A843, ruby #FF1744, sapphire #1E88E5, emerald #43A047, amethyst #8E24AA, topaz #FF8F00

CELULA DIMMED (durante win highlight):
```css
opacity: 0.3, transition: "opacity 0.3s ease"
```

Framer Motion grid items:
```
<AnimatePresence mode="popLayout">
  <motion.div
    key={sym.uniqueKey}
    layout
    initial={{ opacity: 0, y: -60, scale: 0.8 }}
    animate={{ opacity: 1, y: 0, scale: 1 }}
    exit={{ opacity: 0, scale: 0.3, transition: { duration: 0.2 } }}
    transition={{ type: "spring", stiffness: 200, damping: 20, mass: 0.8 }}
  />
</AnimatePresence>
```

WIN DISPLAY (abaixo do grid):
```css
textAlign: "center", padding: "clamp(4px,0.5vw,6px)",
fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
fontSize: "clamp(12px,1.6vw,18px)",
color: currentWin > 0 ? "#00E676" : "#666666",
textShadow: currentWin > 0 ? "0 0 10px rgba(0,230,118,0.5)" : "none",
minHeight: "clamp(20px,2.5vw,28px)"
```

BET CONTROLS BAR:
```css
display: "flex", alignItems: "center", justifyContent: "center",
gap: "clamp(4px,0.5vw,8px)",
padding: "clamp(6px,0.8vw,12px) clamp(8px,1.5vw,16px)",
background: "rgba(0,0,0,0.6)",
borderTop: "1px solid rgba(212,168,67,0.15)",
backdropFilter: "blur(4px)", flexShrink: 0, zIndex: 5
```

BOTAO BET (MIN/x2/÷2/MAX):
```css
minWidth: "44px", minHeight: "44px",
padding: "clamp(4px,0.5vw,6px) clamp(6px,0.8vw,10px)",
background: "rgba(255,255,255,0.05)",
border: "1px solid rgba(255,255,255,0.1)",
borderRadius: "6px", color: "#A8A8A8",
fontFamily: "'JetBrains Mono', monospace", fontWeight: 600,
fontSize: "clamp(9px,1.1vw,12px)", cursor: "pointer"
```
Hover: background rgba(255,255,255,0.1), borderColor rgba(255,255,255,0.2)
Disabled: opacity 0.3, cursor not-allowed

SPIN BUTTON:
```css
width: "56px", height: "56px", borderRadius: "50%",
background: "linear-gradient(180deg, #00C853 0%, #004D25 100%)",
border: "2px solid rgba(0,230,118,0.4)", color: "#FFFFFF",
display: "flex", alignItems: "center", justifyContent: "center",
cursor: "pointer", boxShadow: "0 0 15px rgba(0,230,118,0.3)",
fontSize: "20px", fontWeight: 900, flexShrink: 0
```
Disabled: opacity 0.4, cursor not-allowed
Framer Motion: whileHover={{ scale: 1.08 }}, whileTap={{ scale: 0.92 }}
title: "Girar os rolos! Custo: valor da aposta atual" / "Spin the reels! Cost: current bet value"

ANTE TOGGLE (ativo):
```css
background: "rgba(212,168,67,0.15)",
border: "1px solid rgba(212,168,67,0.3)",
color: "#D4A843", fontFamily: "'Cinzel', serif",
fontWeight: 700, textTransform: "uppercase", letterSpacing: "1px"
```
ANTE TOGGLE (inativo):
```css
background: "rgba(0,0,0,0.3)",
border: "1px solid rgba(255,255,255,0.05)", color: "#666666"
```
title: "Ativa: +25% no custo do spin, dobra chance de Free Spins" / "Active: +25% spin cost, doubles Free Spins chance"

DADOS MOCK:
```json
{ "balance": 12500, "bet": 10, "jackpotPool": 47350, "anteBet": false, "turboMode": false, "autoPlay": 0 }
```

MOBILE: Frame dourado width 95vw, BetControls overflow-x auto com scrollSnapType

---

# TELA 3 — SPINNING + TUMBLING (Animacao de Spin e Cascade)

Estado SPINNING: Grid revela simbolos com stagger por coluna (col0 em 1.0s, col1 em 1.2s, ..., col5 em 2.0s).
Simbolos entram por cima com initial y: -60, spring stiffness 200 damping 20.

TUMBLE BADGE (canto superior direito do frame):
```css
position: "absolute", top: "clamp(4px,0.5vw,8px)", right: "clamp(4px,0.5vw,8px)",
background: "rgba(0,230,118,0.1)", border: "1px solid rgba(0,230,118,0.3)",
borderRadius: "6px",
padding: "clamp(2px,0.3vw,4px) clamp(6px,0.8vw,10px)",
fontFamily: "'Cinzel', serif", fontWeight: 600,
fontSize: "clamp(9px,1.1vw,13px)", color: "#00E676",
textTransform: "uppercase", letterSpacing: "1px", zIndex: 10
```
Framer Motion: key={tumbleCount}, initial scale 1.3, animate scale 1, spring 400/15
title: "Numero de cascatas consecutivas neste spin" / "Number of consecutive cascades in this spin"
Texto: "TUMBLE x{N}"

SEQUENCIA TUMBLE (4 fases):
1. HIGHLIGHT (400ms): posicoes vencedoras scale 1.05, boxShadow glow, dimmed 0.3
2. EXPLOSAO (200ms): exit={{ opacity: 0, scale: 0.3 }}
3. CASCADE (spring): novos simbolos caem por cima com initial y -60
4. RE-AVALIACAO: volta a fase 1 se houver novos wins

Durante tumble: BetControls opacity 0.4, pointerEvents none

TURBO MODE: Todas duracoes * 0.5 (stagger, delays, transitions)

MOBILE: Mesmo layout, animacoes simplificadas (duration * 0.7)

---

# TELA 4 — FREE SPINS TRIGGER (Overlay 3.2s)

```
+--------------------------------------------------------------+
|                                                                |
|              (backdrop blur 8px + bg 80% preto)                |
|                                                                |
|                    FREE SPINS!                                 |
|                                                                |
|                   10 RODADAS                                   |
|                                                                |
+--------------------------------------------------------------+
```

OVERLAY:
```css
position: "absolute", inset: 0, zIndex: 100,
background: "rgba(0,0,0,0.8)", backdropFilter: "blur(8px)",
display: "flex", flexDirection: "column",
alignItems: "center", justifyContent: "center",
gap: "clamp(12px,2vw,24px)"
```

TITULO "FREE SPINS!":
```css
fontFamily: "'Cinzel', serif", fontWeight: 900,
fontSize: "clamp(24px,5vw,48px)", color: "#FFD700",
textTransform: "uppercase", letterSpacing: "4px",
textShadow: "0 0 25px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.3)"
```
Framer Motion: initial={{ scale: 0, rotate: -10 }}, animate={{ scale: 1, rotate: 0 }}, spring 200/12 delay 0.2

CONTAGEM RODADAS:
```css
fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
fontSize: "clamp(16px,3vw,28px)", color: "#FFFFFF"
```
Framer Motion: initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }}, delay 0.6 duration 0.4

AUTO-DISMISS: 3200ms (turbo: 1600ms) → transiciona para FS Playing

---

# TELA 5 — FREE SPINS PLAYING (HUD Dourado + Multiplicadores)

Mesmo layout do Video Slot Idle (Tela 2), com as seguintes diferencas:

FRAME DOURADO FS (borda dourada mais brilhante):
```css
border: "2px solid #FFD700",
outline: "3px solid rgba(255,215,0,0.25)",
boxShadow: "0 0 40px rgba(255,215,0,0.12), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.3)"
```

TINT DOURADO (overlay sutil sobre tudo):
```css
position: "absolute", inset: 0,
background: "rgba(212,168,67,0.05)",
pointerEvents: "none", zIndex: 1
```

FS HUD BAR (entre header e grid):
```css
display: "flex", justifyContent: "space-between", alignItems: "center",
width: "100%",
padding: "clamp(6px,0.8vw,10px) clamp(8px,1.5vw,16px)",
background: "rgba(0,0,0,0.7)",
borderBottom: "1px solid rgba(255,215,0,0.2)", zIndex: 10
```

FS COUNTER (esquerda do HUD):
```css
fontFamily: "'Cinzel', serif", fontWeight: 700,
fontSize: "clamp(10px,1.3vw,15px)", color: "#00E676",
textShadow: "0 0 8px rgba(0,230,118,0.4)",
textTransform: "uppercase", letterSpacing: "1px"
```
Texto: "FREE SPINS: {N}"
title: "Rodadas gratis restantes / total nesta sessao" / "Free spins remaining / total this session"

MULTIPLICADOR TOTAL (centro do HUD):
```css
fontFamily: "'JetBrains Mono', monospace", fontWeight: 900,
fontSize: "clamp(12px,1.6vw,18px)", color: "#FFD700",
textShadow: "0 0 10px rgba(255,215,0,0.5)"
```
Texto: "TOTAL x{N}"
Framer Motion: key={multiplier}, initial={{ scale: 1.4, color: "#FFFFFF" }}, animate={{ scale: 1, color: "#FFD700" }}, spring 300/15
title: "Multiplicador acumulado — soma de todos orbes" / "Accumulated multiplier — sum of all orbs"

FS WIN ACUMULADO (direita do HUD):
```css
fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
fontSize: "clamp(11px,1.4vw,16px)", color: "#00E676",
textShadow: "0 0 8px rgba(0,230,118,0.4)"
```
Texto: "WIN: {N} GC"

ORBE MULTIPLICADOR (aparece no grid durante FS):
```css
background: "radial-gradient(circle, #FFD700 0%, #D4A843 60%, #8B6914 100%)",
fontFamily: "'JetBrains Mono', monospace", fontWeight: 900,
fontSize: "clamp(12px,1.8vw,20px)", color: "#0A0A0A"
```
Texto: "x{valor}" (valores: 2, 5, 8, 10, 15, 25, 50, 100)

SPIN BUTTON FS (dourado em vez de verde):
```css
background: "linear-gradient(180deg, #FFD700 0%, #8B6914 100%)",
border: "2px solid rgba(255,215,0,0.6)", color: "#0A0A0A",
boxShadow: "0 0 20px rgba(255,215,0,0.4)"
```

FS END OVERLAY (quando FS acabam):
```css
position: "absolute", inset: 0, zIndex: 100,
background: "rgba(0,0,0,0.8)", backdropFilter: "blur(6px)",
display: "flex", flexDirection: "column",
alignItems: "center", justifyContent: "center",
gap: "clamp(8px,1.5vw,16px)"
```
Titulo: "FREE SPINS ENCERRADAS" — Cinzel 700 clamp(14px,2vw,22px) #D4A843
Total: JetBrains Mono 700 clamp(18px,3.5vw,36px) #00E676 textShadow "0 0 15px rgba(0,230,118,0.6)"
Botao CONTINUAR: background linear-gradient(180deg, #00C853, #004D25), minHeight 44px, Cinzel 700

DADOS MOCK FS:
```json
{ "freeSpinsRemaining": 7, "multiplierTotal": 15, "freeSpinTotalWin": 3450, "originalBet": 10 }
```

---

**[FIM PARTE 1 — Continua Parte 2: Telas 6-10 (Win Overlay, Classic, Paytable, History, Buy Bonus) + Checklist + Tooltips]**

`[ENTREGA: 6.PROMPT-V0-SLOT-MACHINE-PARTE-1.md — 19/03/2026]`
