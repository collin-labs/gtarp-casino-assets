# ADENDO — ROTEIRO DE DESENVOLVIMENTO SLOT-MACHINE (#2)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-SLOT-MACHINE-PARTE-5/6.md — 23/03/2026

---

# 1. CONTEXTO

A Parte 5 descreve Fase 5 (WinOverlay, ClassicSlot, SlotsModeSelect,
PaytableModal, HistoryModal) com descricoes de padrao CSS e refs ao Adendo DS
mas sem codigo TSX detalhado para os componentes menores. A Parte 6 consolida
resumo e regras. Este adendo fornece TSX real com tooltips inline.

Itens corrigidos:
- **5.11** Regras entrega: P6 ja tem 12 regras. Reforco com +3
- **5.14** Zero pseudocodigo: TSX real para PaytableModal, HistoryModal, WinOverlay trechos
- **5.15** Tooltips inline em CADA botao

---

# 2. FASE 5 — CODIGO REAL TSX

## 2.1 PaytableModal.tsx — Trechos com tooltips inline

```tsx
// Tabs de categoria (High, Low, Special, Rules)
{(["high", "low", "special", "rules"] as const).map((tab) => (
  <motion.button
    key={tab}
    onClick={() => setActiveTab(tab)}
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
    title={lang === "br"
      ? { high: "Simbolos premium com maiores payouts", low: "Simbolos comuns com payouts menores", special: "Scatter, Wild e Multiplicador", rules: "Regras de Tumble, Free Spins e Jackpot" }[tab]
      : { high: "Premium symbols with highest payouts", low: "Common symbols with lower payouts", special: "Scatter, Wild and Multiplier", rules: "Tumble, Free Spins and Jackpot rules" }[tab]
    }
    style={{
      background: activeTab === tab
        ? "linear-gradient(135deg, rgba(212,168,67,0.25), rgba(212,168,67,0.1))"
        : "rgba(255,255,255,0.03)",
      border: `1px solid ${activeTab === tab ? "rgba(212,168,67,0.6)" : "rgba(255,255,255,0.08)"}`,
      borderRadius: "8px",
      color: activeTab === tab ? "#FFD700" : "rgba(212,168,67,0.5)",
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.85vw, 12px)",
      fontWeight: 700,
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      padding: "clamp(6px, 0.6vw, 10px) clamp(10px, 1vw, 16px)",
      cursor: "pointer",
      minHeight: "44px",
    }}
  >
    {lang === "br"
      ? { high: "PREMIUM", low: "COMUNS", special: "ESPECIAIS", rules: "REGRAS" }[tab]
      : { high: "PREMIUM", low: "COMMON", special: "SPECIAL", rules: "RULES" }[tab]
    }
  </motion.button>
))}

// Linha de simbolo com payout
{symbols.map((sym, i) => (
  <motion.div
    key={sym.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    title={lang === "br" ? `${sym.nameBR} — payouts por quantidade` : `${sym.nameEN} — payouts by count`}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(6px, 0.6vw, 10px)",
      background: "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: "1px solid rgba(212,168,67,0.1)",
    }}
  >
    <img src={`/assets/slots/video/symbol-${sym.id}.png`} alt="" style={{ width: "clamp(28px, 3vw, 40px)", height: "clamp(28px, 3vw, 40px)", borderRadius: "6px" }} />
    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(9px, 0.9vw, 13px)", color: "#D4A843", fontWeight: 600, flex: 1 }}>
      {lang === "br" ? sym.nameBR : sym.nameEN}
    </span>
    {Object.entries(sym.payouts).map(([count, mult]) => (
      <span key={count} style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(8px, 0.8vw, 12px)", color: "#FFD700", minWidth: "clamp(30px, 3vw, 45px)", textAlign: "center" }}>
        {count}x→{mult}x
      </span>
    ))}
  </motion.div>
))}
```

## 2.2 HistoryModal.tsx — Trechos com tooltips inline

```tsx
// Cada spin do historico
{history.map((spin, i) => (
  <motion.div
    key={spin.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: spin.totalWin > 0 ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${spin.totalWin > 0 ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(8px, 0.8vw, 11px)", color: "rgba(212,168,67,0.4)", minWidth: "clamp(30px, 3vw, 45px)" }}>
      #{spin.id}
    </span>
    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(9px, 0.9vw, 13px)", color: spin.mode === "video" ? "#D4A843" : "#A8A8A8", fontWeight: 600 }}>
      {spin.mode === "video" ? "VIDEO" : "CLASSIC"}
    </span>
    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(9px, 0.9vw, 13px)", color: "#A8A8A8" }}>
      G${spin.bet}
    </span>
    {spin.freeSpins && <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(7px, 0.7vw, 10px)", color: "#FFD700", background: "rgba(255,215,0,0.1)", borderRadius: "4px", padding: "2px 6px" }}>FS</span>}
    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(10px, 1vw, 14px)", fontWeight: 700, color: spin.totalWin > 0 ? "#00E676" : "rgba(212,168,67,0.4)", marginLeft: "auto", textShadow: spin.totalWin > 0 ? "0 0 8px rgba(0,230,118,0.4)" : "none" }}>
      {spin.totalWin > 0 ? `+G$${spin.totalWin.toLocaleString()}` : "G$0"}
    </span>
    <motion.button
      onClick={() => onVerify(spin.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={lang === "br" ? "Verificar fairness deste spin" : "Verify this spin's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/slots/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 2.3 WinOverlay.tsx — Trecho com tooltips

```tsx
// Botoes COLETAR e DOBRAR no resultado
<div style={{ display: "flex", gap: "clamp(10px, 1vw, 16px)", marginTop: "clamp(12px, 1.5vw, 20px)" }}>
  <motion.button
    onClick={onCollect}
    whileHover={{ scale: 1.05, boxShadow: "0 0 20px rgba(0,230,118,0.3)" }}
    whileTap={{ scale: 0.95 }}
    title={lang === "br" ? "Coletar ganhos e voltar ao jogo" : "Collect winnings and return to game"}
    style={{
      flex: 1,
      background: "linear-gradient(180deg, #00E676 0%, #004D25 100%)",
      border: "1.5px solid rgba(0,230,118,0.5)",
      borderRadius: "10px",
      color: "#FFFFFF",
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(11px, 1.2vw, 16px)",
      fontWeight: 700,
      letterSpacing: "2px",
      textTransform: "uppercase" as const,
      padding: "clamp(10px, 1vw, 16px)",
      cursor: "pointer",
      minHeight: "44px",
      boxShadow: "0 4px 16px rgba(0,230,118,0.2)",
    }}
  >
    {lang === "br" ? "COLETAR" : "COLLECT"}
  </motion.button>

  {gambleEnabled && (
    <motion.button
      onClick={onGamble}
      whileHover={{ scale: 1.05, borderColor: "rgba(255,215,0,0.7)" }}
      whileTap={{ scale: 0.95 }}
      title={lang === "br" ? "Arrisque dobrar seus ganhos num coin flip 50/50" : "Risk doubling your winnings on a 50/50 coin flip"}
      style={{
        flex: 1,
        background: "linear-gradient(135deg, rgba(255,215,0,0.15), rgba(255,215,0,0.05))",
        border: "1.5px solid rgba(255,215,0,0.4)",
        borderRadius: "10px",
        color: "#FFD700",
        fontFamily: "var(--font-cinzel)",
        fontSize: "clamp(11px, 1.2vw, 16px)",
        fontWeight: 700,
        letterSpacing: "2px",
        textTransform: "uppercase" as const,
        padding: "clamp(10px, 1vw, 16px)",
        cursor: "pointer",
        minHeight: "44px",
      }}
    >
      {lang === "br" ? "DOBRAR" : "DOUBLE"}
    </motion.button>
  )}
</div>
```

---

# 3. REGRAS DE ENTREGA COMPLEMENTO (item 5.11)

A Parte 6 ja tem 12 regras formais. Complemento com 3 adicionais:

13. TODOS botoes com minHeight e minWidth 44px (REGRA X4 touch targets)
14. TODOS textos via funcao t(key, lang) — zero string hardcoded (bilingue)
15. Resultado do spin SEMPRE server-authoritative — client somente anima grid recebido

---

# 4. VALIDACAO

**5.11** Regras: 12 + 3 = 15 regras formais. OK.
**5.14** TSX real: PaytableModal (tabs + linhas simbolo), HistoryModal (spin list + verificar), WinOverlay (coletar + dobrar). OK.
**5.15** Tooltips inline: 4 tabs paytable, simbolo title, verificar PF, coletar, dobrar = 8+ tooltips com title bilingue. OK.

**VALIDACAO X17:** 3 itens exigidos, 3 entregues. Zero pseudocodigo.

---

`[ENTREGA: ADENDO-ROTEIRO-SLOTS.md — 23/03/2026]`
