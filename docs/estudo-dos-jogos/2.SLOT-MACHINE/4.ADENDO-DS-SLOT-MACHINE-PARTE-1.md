# ADENDO DESIGN SYSTEM — SLOT-MACHINE (#2)
## PARTE 1 DE 4 — Componentes CSS Real (1.1-1.8)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 18/03/2026

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1-4) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com §ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 25 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.slots-[block]__[element]--[modifier]`
- Block: `slots-game`, `slots-frame`, `slots-grid`, `slots-symbol`, `slots-bet`, `slots-spin`, `slots-overlay`, `slots-modal`, `slots-hud`, `slots-reel`, `slots-mode`
- Modifiers: `--idle`, `--spinning`, `--winning`, `--dimmed`, `--disabled`, `--fs-active`, `--ante`, `--turbo`, `--auto`, `--crown`, `--ruby`, `--sapphire`, `--emerald`, `--amethyst`, `--topaz`, `--ring`, `--hourglass`, `--chalice`, `--scatter`, `--multiplier`, `--classic`, `--video`

**Pesquisa REGRA X0:** BEM naming 2025-2026 confirmado como padrao dominante (getbem.com, sparkbox.com Fev/2025, valoremreply.com Abr/2025). `.block__element--modifier` com double underscore e double dash. backdrop-filter funciona no CEF Chromium do FiveM (docs.fivem.net). AnimatePresence mode="popLayout" para tumble confirmado (motion.dev docs 2025).

---

# SECAO 1 — COMPONENTES COM CSS REAL (25 Componentes)

## 1.1 Container Principal do Jogo (DS P1 §4.2)

Quando o Slot Machine abre dentro do painel (z-index 60), o fundo NUNCA e flat:

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Slot Machine */
.slots-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
}

/* COPIAR — fallback NUI (sem bg-casino.png carregado) */
.slots-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}

/* COPIAR — estado spinning (bet controls dimmed) */
.slots-game--spinning .slots-bet {
  opacity: 0.4;
  pointer-events: none;
  transition: opacity 0.3s ease;
}

/* COPIAR — estado free spins (tint dourado sobre tudo) */
.slots-game--fs-active::after {
  content: '';
  position: absolute; inset: 0;
  background: rgba(212,168,67,0.05);
  pointer-events: none;
  z-index: 1;
  transition: opacity 0.5s ease;
}
```

---

## 1.2 Header Bar (DS P2 §15.2 adaptado)

```css
/* COPIAR EXATAMENTE — barra superior com VOLTAR + titulo + jackpot + saldo */
.slots-game__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
  z-index: 5;
}

/* COPIAR — botao VOLTAR */
.slots-game__btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px;
  cursor: pointer;
  transition: all 0.25s ease;
}
.slots-game__btn-back:hover {
  border-color: rgba(212,168,67,0.8);
  background: rgba(0,0,0,0.8);
}
.slots-game__btn-back:active {
  transform: scale(0.95);
}

/* COPIAR — titulo "SLOT MACHINE" */
.slots-game__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
}

/* COPIAR — jackpot pool */
.slots-game__jackpot {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.4vw, 16px);
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}

/* COPIAR — saldo GCoin */
.slots-game__balance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
.slots-game__balance--win-flash {
  animation: balanceWinFlash 0.6s ease-out;
}
@keyframes balanceWinFlash {
  0% { color: #FFFFFF; text-shadow: 0 0 16px rgba(0,230,118,0.8); }
  100% { color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
}
```

---

## 1.3 Frame Dourado — Moldura do Grid (DS P1 §5.1)

```css
/* COPIAR EXATAMENTE — moldura ornamentada ao redor do grid 6x5 */
.slots-frame {
  margin: clamp(4px, 0.8vw, 8px) auto;
  padding: clamp(6px, 1vw, 12px);
  border: 2px solid #D4A843;
  outline: 3px solid rgba(212,168,67,0.15);
  outline-offset: 4px;
  border-radius: 18px;
  box-shadow:
    0 0 30px rgba(212,168,67,0.08),
    0 8px 32px rgba(0,0,0,0.5),
    inset 0 0 60px rgba(0,0,0,0.4),
    0 0 0 3px rgba(139,105,20,0.2);
  background: linear-gradient(180deg, #0F0F0F 0%, #0A0A0A 100%);
  width: clamp(300px, 70vw, 600px);
  flex: 1;
  max-height: 60vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

/* COPIAR — frame durante free spins (borda dourada intensificada) */
.slots-frame--fs-active {
  border-color: #FFD700;
  outline-color: rgba(255,215,0,0.25);
  box-shadow:
    0 0 40px rgba(255,215,0,0.12),
    0 8px 32px rgba(0,0,0,0.5),
    inset 0 0 60px rgba(0,0,0,0.3),
    0 0 0 3px rgba(255,215,0,0.2);
  transition: all 0.5s ease;
}
```

---

## 1.4 Grid 6x5 — Container de Simbolos (DS P1 §4.2 adaptado)

```css
/* COPIAR EXATAMENTE — grid de 30 celulas (6 colunas x 5 linhas) */
.slots-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  grid-template-rows: repeat(5, 1fr);
  gap: clamp(3px, 0.4vw, 6px);
  width: 100%;
  aspect-ratio: 6 / 5;
}
```

---

## 1.5 Celula de Simbolo — 4 Estados Base (DS P1 §4.4 adaptado)

```css
/* COPIAR EXATAMENTE — celula individual do grid */
.slots-symbol {
  aspect-ratio: 1;
  border-radius: 8px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.04);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: clamp(18px, 2.5vw, 32px);
  transition: all 0.2s ease;
  position: relative;
  overflow: hidden;
}

/* COPIAR — estado WINNING (simbolo faz parte de uma combinacao vencedora) */
.slots-symbol--winning {
  transform: scale(1.05);
  border-width: 1.5px;
  z-index: 2;
  /* box-shadow e border-color definidos por variante do simbolo (§1.6) */
  transition: all 0.3s ease;
}

/* COPIAR — estado DIMMED (simbolos que NAO fazem parte do win, durante avaliacao) */
.slots-symbol--dimmed {
  opacity: 0.3;
  filter: brightness(0.5);
  transition: opacity 0.3s ease, filter 0.3s ease;
}

/* COPIAR — estado SPINNING (durante scroll de colunas) */
.slots-symbol--spinning {
  filter: blur(2px);
  opacity: 0.6;
  transition: filter 0.15s ease, opacity 0.15s ease;
}

/* COPIAR — estado STOPPED (quando coluna para — remove blur) */
.slots-symbol--stopped {
  filter: blur(0);
  opacity: 1;
  transition: filter 0.2s ease, opacity 0.2s ease;
}

/* Framer Motion — simbolo entra no grid (cascade/tumble) */
/*
  initial={{ opacity: 0, y: -60, scale: 0.8 }}
  animate={{ opacity: 1, y: 0, scale: 1 }}
  transition={{ type: 'spring', stiffness: 200, damping: 20, mass: 0.8 }}
  layout
*/

/* Framer Motion — simbolo sai do grid (win explode) */
/*
  exit={{ opacity: 0, scale: 0.3 }}
  transition={{ duration: 0.2 }}
*/
```

---

## 1.6 Simbolo por Tipo — 11 Variantes (DS custom)

Cada tipo de simbolo tem sua cor de glow, border e badge para o estado `--winning`:

```css
/* COPIAR EXATAMENTE — HIGH SYMBOLS (4 tipos, glow intenso) */

/* Coroa (crown) — simbolo mais valioso */
.slots-symbol--crown {
  background: rgba(255,215,0,0.04);
}
.slots-symbol--crown.slots-symbol--winning {
  border-color: rgba(255,215,0,0.6);
  box-shadow: 0 0 15px rgba(255,215,0,0.6), 0 0 30px rgba(255,215,0,0.2);
}

/* Anel (ring) */
.slots-symbol--ring {
  background: rgba(212,168,67,0.04);
}
.slots-symbol--ring.slots-symbol--winning {
  border-color: rgba(212,168,67,0.5);
  box-shadow: 0 0 15px rgba(212,168,67,0.5), 0 0 30px rgba(212,168,67,0.15);
}

/* Ampulheta (hourglass) */
.slots-symbol--hourglass {
  background: rgba(203,155,81,0.04);
}
.slots-symbol--hourglass.slots-symbol--winning {
  border-color: rgba(203,155,81,0.5);
  box-shadow: 0 0 15px rgba(203,155,81,0.5), 0 0 30px rgba(203,155,81,0.15);
}

/* Calice (chalice) */
.slots-symbol--chalice {
  background: rgba(139,105,20,0.04);
}
.slots-symbol--chalice.slots-symbol--winning {
  border-color: rgba(139,105,20,0.5);
  box-shadow: 0 0 15px rgba(139,105,20,0.5), 0 0 30px rgba(139,105,20,0.15);
}

/* COPIAR EXATAMENTE — LOW SYMBOLS (5 tipos, glow sutil) */

/* Rubi (ruby) */
.slots-symbol--ruby {
  background: rgba(255,23,68,0.03);
}
.slots-symbol--ruby.slots-symbol--winning {
  border-color: rgba(255,23,68,0.4);
  box-shadow: 0 0 12px rgba(255,23,68,0.4), 0 0 24px rgba(255,23,68,0.1);
}

/* Safira (sapphire) */
.slots-symbol--sapphire {
  background: rgba(30,136,229,0.03);
}
.slots-symbol--sapphire.slots-symbol--winning {
  border-color: rgba(30,136,229,0.4);
  box-shadow: 0 0 12px rgba(30,136,229,0.4), 0 0 24px rgba(30,136,229,0.1);
}

/* Esmeralda (emerald) */
.slots-symbol--emerald {
  background: rgba(67,160,71,0.03);
}
.slots-symbol--emerald.slots-symbol--winning {
  border-color: rgba(67,160,71,0.4);
  box-shadow: 0 0 12px rgba(67,160,71,0.4), 0 0 24px rgba(67,160,71,0.1);
}

/* Ametista (amethyst) */
.slots-symbol--amethyst {
  background: rgba(142,36,170,0.03);
}
.slots-symbol--amethyst.slots-symbol--winning {
  border-color: rgba(142,36,170,0.4);
  box-shadow: 0 0 12px rgba(142,36,170,0.4), 0 0 24px rgba(142,36,170,0.1);
}

/* Topazio (topaz) */
.slots-symbol--topaz {
  background: rgba(255,143,0,0.03);
}
.slots-symbol--topaz.slots-symbol--winning {
  border-color: rgba(255,143,0,0.4);
  box-shadow: 0 0 12px rgba(255,143,0,0.4), 0 0 24px rgba(255,143,0,0.1);
}
```

---

## 1.7 Scatter — Raio Dourado (DS custom)

```css
/* COPIAR EXATAMENTE — scatter e o trigger de Free Spins */
.slots-symbol--scatter {
  background: radial-gradient(circle, rgba(255,215,0,0.15) 0%, transparent 70%);
  border: 2px solid rgba(255,215,0,0.4);
  border-radius: 8px;
}

/* COPIAR — scatter idle (glow sutil constante) */
.slots-symbol--scatter::after {
  content: '';
  position: absolute; inset: -2px;
  border-radius: 10px;
  box-shadow: 0 0 12px rgba(255,215,0,0.2);
  pointer-events: none;
}

/* COPIAR — scatter com anticipation ativa (2+ scatters no grid, reels girando) */
.slots-symbol--scatter.slots-symbol--anticipation {
  animation: scatterPulse 1.2s ease-in-out infinite;
}
@keyframes scatterPulse {
  0%, 100% {
    box-shadow: 0 0 15px rgba(255,215,0,0.3);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 30px rgba(255,215,0,0.6);
    transform: scale(1.05);
  }
}

/* COPIAR — scatter landing (quando scatter aterrissa numa coluna) */
.slots-symbol--scatter.slots-symbol--landing {
  animation: scatterLand 0.4s ease-out;
}
@keyframes scatterLand {
  0% { transform: scale(1.3); box-shadow: 0 0 40px rgba(255,215,0,0.8); }
  60% { transform: scale(0.95); }
  100% { transform: scale(1); box-shadow: 0 0 15px rgba(255,215,0,0.3); }
}
```

---

## 1.8 Multiplicador Orbe — Dourado (DS custom — aparece so em FS)

```css
/* COPIAR EXATAMENTE — orbe multiplicador que spawna no grid durante Free Spins */
.slots-symbol--multiplier {
  background: radial-gradient(circle, #FFD700 0%, #D4A843 60%, #8B6914 100%);
  border: 2px solid #F6E27A;
  border-radius: 50%;
  box-shadow: 0 0 25px rgba(255,215,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — numero do multiplicador dentro do orbe */
.slots-symbol__multiplier-value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 900;
  font-size: clamp(12px, 1.8vw, 20px);
  color: #0A0A0A;
  text-shadow: 0 1px 2px rgba(255,255,255,0.3);
}

/* COPIAR — orbe pulse continuo apos aparecer */
.slots-symbol--multiplier.slots-symbol--active {
  animation: orbPulse 1.5s ease-in-out infinite;
}
@keyframes orbPulse {
  0%, 100% { box-shadow: 0 0 20px rgba(255,215,0,0.4); }
  50% { box-shadow: 0 0 35px rgba(255,215,0,0.7); }
}

/* Framer Motion — orbe aparece com scale bounce */
/*
  initial={{ scale: 1.5, opacity: 0 }}
  animate={{ scale: 1, opacity: 1 }}
  transition={{ type: 'spring', stiffness: 150, damping: 10 }}
*/
```

---

## 1.9 BetControls — Barra Inferior (DS P2 §8 adaptado)

```css
/* COPIAR EXATAMENTE — barra de controles na base do jogo */
.slots-bet {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(4px, 0.5vw, 8px);
  padding: clamp(6px, 0.8vw, 12px) clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.6);
  border-top: 1px solid rgba(212,168,67,0.15);
  backdrop-filter: blur(4px);
  flex-shrink: 0;
  z-index: 5;
}

/* COPIAR — botao generico de bet (MIN, x2, /2, MAX) */
.slots-bet__btn {
  min-width: 44px;
  min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer;
  transition: all 0.2s ease;
}
.slots-bet__btn:hover {
  background: rgba(255,255,255,0.1);
  border-color: rgba(212,168,67,0.3);
  color: #D4A843;
}
.slots-bet__btn:active {
  transform: scale(0.95);
}
.slots-bet__btn--disabled {
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
}

/* COPIAR — display de BET (valor atual) */
.slots-bet__display {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 16px);
  color: #00E676;
  text-shadow: 0 0 6px rgba(0,230,118,0.3);
  min-width: clamp(60px, 10vw, 100px);
  text-align: center;
}

/* COPIAR — toggle ANTE BET inativo */
.slots-bet__ante {
  min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  border-radius: 6px;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 11px);
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.25s ease;
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(255,255,255,0.05);
  color: #666666;
}

/* COPIAR — toggle ANTE BET ativo */
.slots-bet__ante--active {
  background: rgba(212,168,67,0.15);
  border: 1px solid rgba(212,168,67,0.3);
  color: #D4A843;
  box-shadow: 0 0 8px rgba(212,168,67,0.15);
}
.slots-bet__ante--active:hover {
  background: rgba(212,168,67,0.2);
  border-color: rgba(212,168,67,0.5);
}
```

---

**[FIM PARTE 1 — Continua Parte 2: Componentes 1.10-1.18 (SPIN, AUTO, TURBO, WinDisplay, Badge TUMBLE, FS HUD, Win Overlays 4 tiers, Gamble)]**

`[ENTREGA: 4.ADENDO-DS-SLOT-MACHINE-PARTE-1.md — 18/03/2026]`
