# ADENDO — ROTEIRO DE DESENVOLVIMENTO LOTERIA (#18)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 6.ROTEIRO-LOTERIA-PARTE-3.md — 23/03/2026

---

# 1. FASE 6 POLISH — CODIGO REAL TSX

## 1.1 HistoryPanel.tsx — Trechos com tooltips

```tsx
{history.map((draw, i) => (
  <motion.div
    key={draw.id}
    initial={{ opacity: 0, x: 10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex", alignItems: "center", gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: draw.prize > 0 ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${draw.prize > 0 ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    <span
      title={lang === "br" ? `Sorteio #${draw.id} — ${draw.matches} acertos` : `Draw #${draw.id} — ${draw.matches} matches`}
      style={{
        fontFamily: "var(--font-mono)", fontSize: "clamp(10px, 1vw, 14px)", fontWeight: 800,
        color: draw.matches >= 3 ? "#00E676" : draw.matches >= 1 ? "#FFD700" : "rgba(212,168,67,0.4)",
        minWidth: "28px", textAlign: "center",
      }}
    >
      {draw.matches}/{6}
    </span>

    {draw.mega && (
      <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(7px, 0.7vw, 10px)", color: "#FFD700", background: "rgba(255,215,0,0.1)", borderRadius: "4px", padding: "2px 6px" }}>
        MEGA
      </span>
    )}

    <span style={{
      fontFamily: "var(--font-mono)", fontSize: "clamp(10px, 1vw, 14px)", fontWeight: 700,
      color: draw.prize > 0 ? "#00E676" : "rgba(212,168,67,0.4)", marginLeft: "auto",
      textShadow: draw.prize > 0 ? "0 0 8px rgba(0,230,118,0.4)" : "none",
    }}>
      {draw.prize > 0 ? `+G$${draw.prize.toLocaleString()}` : "-G$100"}
    </span>

    <motion.button
      onClick={() => onVerify(draw.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={lang === "br" ? "Verificar fairness deste sorteio" : "Verify this draw's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)", border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px", padding: "clamp(4px, 0.4vw, 8px)", cursor: "pointer",
        minWidth: "44px", minHeight: "44px", display: "flex", alignItems: "center", justifyContent: "center",
      }}
    >
      <img src="/assets/lottery/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 1.2 SoundManager integration

```tsx
const sounds = {
  ballReveal: () => SoundManager.play('lottery/ball-reveal'),
  ballMatch: () => SoundManager.play('lottery/ball-match'),
  megaBallReveal: () => SoundManager.play('lottery/mega-ball-reveal'),
  megaBallMatch: () => SoundManager.play('lottery/mega-ball-match'),
  ticketBuy: () => SoundManager.play('lottery/ticket-buy'),
  numberSelect: () => SoundManager.play('lottery/number-select'),
  win: () => SoundManager.play('lottery/win-fanfare'),
  noWin: () => SoundManager.play('lottery/no-win'),
};
```

---

# 2. REGRAS DE ENTREGA COMPLEMENTO (item 5.11)

9. TODOS botoes e celulas min 44px
10. TODOS textos via { br, en }
11. Sorteio SEMPRE server-authoritative (seed-based draw via HMAC-SHA256)
12. Canvas globo com requestAnimationFrame, NAO setInterval
13. Bolas reveladas com delay 1.5s cada (stagger visual)

---

**VALIDACAO X17:** 3 itens, 3 entregues.

`[ENTREGA: ADENDO-ROTEIRO-LOTERIA.md — 23/03/2026]`
