# ADENDO -- ROTEIRO DE DESENVOLVIMENTO UPGRADE (#15)
# PARTE 5 -- WinOverlay TSX real (item 5.14)
# Complementa: 5.ROTEIRO P1-P4 = 2.693L
# Blackout Casino GTARP - 26/03/2026

---

# 1. COMPONENTE: UpgradeWinOverlay.tsx (Fase 4 -- codigo real)

Pesquisa X0: CSGORoll "Quick Upgrade — multiplier for mini-inventory items,
server+client seed SHA256 PF". Wheel deceleration 3.5s com landing visual.
Item rarity tiers: Common (cinza), Uncommon (verde), Rare (azul),
Epic (roxo), Legendary (dourado).

```tsx
import { motion, AnimatePresence, useMotionValue, useTransform, animate } from "framer-motion";
import { useEffect, useRef } from "react";

type Rarity = "common" | "uncommon" | "rare" | "epic" | "legendary";

interface UpgradeWinOverlayProps {
  show: boolean;
  itemName: string;
  itemImage: string;
  rarity: Rarity;
  multiplier: number;
  valueGained: number;
  lang: "br" | "en";
  onClose: () => void;
}

const RARITY_COLORS: Record<Rarity, { color: string; glow: string }> = {
  common: { color: "#9E9E9E", glow: "rgba(158,158,158,0.3)" },
  uncommon: { color: "#4CAF50", glow: "rgba(76,175,80,0.3)" },
  rare: { color: "#2196F3", glow: "rgba(33,150,243,0.3)" },
  epic: { color: "#AA00FF", glow: "rgba(170,0,255,0.4)" },
  legendary: { color: "#FFD700", glow: "rgba(255,215,0,0.5)" },
};

export default function UpgradeWinOverlay({
  show, itemName, itemImage, rarity, multiplier, valueGained, lang, onClose,
}: UpgradeWinOverlayProps) {
  const { color, glow } = RARITY_COLORS[rarity];
  const countRef = useRef<HTMLSpanElement>(null);
  const countValue = useMotionValue(0);
  const rounded = useTransform(countValue, (v) => Math.floor(v).toLocaleString());

  useEffect(() => {
    if (!show || valueGained <= 0) return;
    const ctrl = animate(countValue, valueGained, { duration: 2, ease: "easeOut" });
    return () => ctrl.stop();
  }, [show, valueGained, countValue]);

  useEffect(() => {
    if (!countRef.current) return;
    const unsub = rounded.on("change", (v) => {
      if (countRef.current) countRef.current.textContent = `G$ ${v}`;
    });
    return unsub;
  }, [rounded]);

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "fixed", inset: 0,
            background: "rgba(0,0,0,0.9)",
            backdropFilter: "blur(12px)",
            display: "flex", flexDirection: "column",
            alignItems: "center", justifyContent: "center",
            zIndex: 1000,
          }}
        >
          {/* Particulas */}
          {Array.from({ length: rarity === "legendary" ? 35 : 20 }).map((_, i) => (
            <motion.div
              key={`p-${i}`}
              initial={{ opacity: 1, scale: 0, x: 0, y: 0 }}
              animate={{
                opacity: [1, 1, 0], scale: [0, 1, 0.3],
                x: (Math.random() - 0.5) * 350, y: (Math.random() - 0.5) * 350,
              }}
              transition={{ duration: 1.5, delay: Math.random() * 0.3 }}
              style={{
                position: "absolute",
                width: "clamp(3px, 0.5vw, 6px)", height: "clamp(3px, 0.5vw, 6px)",
                borderRadius: "50%", background: color, pointerEvents: "none",
              }}
            />
          ))}

          {/* UPGRADE SUCCESS */}
          <motion.div
            initial={{ scale: 0 }}
            animate={{ scale: 1 }}
            transition={{ type: "spring", stiffness: 200, damping: 12 }}
            title={lang === "br" ? "Upgrade bem-sucedido!" : "Upgrade successful!"}
            style={{
              fontFamily: "'Cinzel', serif",
              fontSize: "clamp(20px, 3vw, 36px)", fontWeight: 700,
              color: "#00E676", textTransform: "uppercase", letterSpacing: "3px",
              textShadow: "0 0 25px rgba(0,230,118,0.4)",
            }}
          >
            {lang === "br" ? "UPGRADE!" : "UPGRADED!"}
          </motion.div>

          {/* Item image com borda rarity */}
          <motion.div
            initial={{ scale: 0, rotate: -10 }}
            animate={{ scale: 1, rotate: 0 }}
            transition={{ type: "spring", stiffness: 200, delay: 0.3 }}
            title={`${itemName} (${rarity})`}
            style={{
              width: "clamp(80px, 10vw, 120px)", height: "clamp(80px, 10vw, 120px)",
              borderRadius: 14,
              border: `3px solid ${color}`,
              boxShadow: `0 0 25px ${glow}`,
              background: "rgba(17,17,17,0.9)",
              display: "flex", alignItems: "center", justifyContent: "center",
              marginTop: "clamp(8px, 1vw, 14px)",
              overflow: "hidden",
            }}
          >
            <img src={itemImage} alt={itemName}
              style={{ width: "80%", height: "80%", objectFit: "contain" }} />
          </motion.div>

          {/* Item name + multiplier */}
          <motion.div
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5 }}
            style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: "clamp(12px, 1.4vw, 16px)",
              color, marginTop: 8,
            }}
          >
            {itemName} ({multiplier}x)
          </motion.div>

          {/* Value countup */}
          <motion.div
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.8 }}
            title={lang === "br" ? `Valor ganho: G$ ${valueGained.toLocaleString()}` : `Value gained: G$ ${valueGained.toLocaleString()}`}
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(20px, 2.8vw, 30px)", fontWeight: 700,
              color: "#00E676", textShadow: "0 0 18px rgba(0,230,118,0.4)",
              marginTop: "clamp(6px, 0.8vw, 10px)",
            }}
          >
            <img src="/assets/upgrade/icons/icon-gcoin.png" alt=""
              style={{ width: 18, height: 18, marginRight: 6, verticalAlign: "middle" }} />
            <span ref={countRef}>G$ 0</span>
          </motion.div>

          {/* Close */}
          <motion.button
            initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.5 }}
            onClick={onClose} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
            title={lang === "br" ? "Proximo upgrade" : "Next upgrade"}
            style={{
              marginTop: "clamp(14px, 1.5vw, 20px)",
              padding: "clamp(8px, 1vw, 12px) clamp(20px, 2.5vw, 36px)",
              background: "transparent",
              border: "1.5px solid rgba(212,168,67,0.4)",
              borderRadius: 8, color: "#D4A843",
              fontFamily: "'Cinzel', serif", fontSize: "clamp(11px, 1.2vw, 14px)",
              textTransform: "uppercase", letterSpacing: "1px",
              cursor: "pointer", minHeight: 44,
            }}
          >
            {lang === "br" ? "PROXIMO UPGRADE" : "NEXT UPGRADE"}
          </motion.button>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

---

`[ENTREGA: 5.ROTEIRO-UPGRADE-PARTE-5-ADENDO.md -- 26/03/2026]`
