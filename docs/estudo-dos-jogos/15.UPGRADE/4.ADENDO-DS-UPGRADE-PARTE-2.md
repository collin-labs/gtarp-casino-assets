# ADENDO DESIGN SYSTEM — UPGRADE (#15)
## PARTE 2 DE 2 — Componentes 1.15-1.20, Atmosferas, Timelines, Checklist
### Blackout Casino GTARP - 16/03/2026

---

# SECAO 1 — COMPONENTES COM CSS REAL (continuacao)

## 1.15 Overlay Vitoria (DS P1 §7 — glassmorphism + rolling counter)

```css
/* COPIAR EXATAMENTE — overlay fullscreen vitoria */
.upgrade-overlay--win {
  position: absolute; inset: 0; z-index: 80;
  background: rgba(0,0,0,0.65);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 20px);
}

/* COPIAR — texto "UPGRADE!" */
.upgrade-overlay__title--win {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(24px, 4vw, 44px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 3px;
  text-shadow:
    0 0 20px rgba(255,215,0,0.6),
    0 0 60px rgba(255,215,0,0.2),
    0 2px 6px rgba(0,0,0,1);
  animation: victoryPulse 2s ease-in-out infinite;
}
@keyframes victoryPulse {
  0%, 100% {
    text-shadow: 0 0 20px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.2), 0 2px 6px rgba(0,0,0,1);
  }
  50% {
    text-shadow: 0 0 30px rgba(255,215,0,0.8), 0 0 80px rgba(255,215,0,0.3), 0 2px 6px rgba(0,0,0,1);
  }
}

/* COPIAR — imagem do item ganho (scale bounce) */
/*
  Framer Motion:
  initial={{ scale: 0, opacity: 0 }}
  animate={{ scale: [0, 1.15, 1.0], opacity: 1 }}
  transition={{ duration: 0.6, times: [0, 0.7, 1], ease: 'easeOut' }}
  style={{ filter: 'drop-shadow(0 0 20px rgba(0,230,118,0.4))' }}
*/
.upgrade-overlay__item-img {
  width: clamp(100px, 18vw, 160px);
  object-fit: contain;
}

/* COPIAR — rolling counter "+G$ 800" */
.upgrade-overlay__counter {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(20px, 3vw, 36px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.6);
}

/* COPIAR — flash final do counter */
@keyframes prizeCounterFlash {
  0%   { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.8); }
  100% { color: #00E676; text-shadow: 0 0 12px rgba(0,230,118,0.6); }
}

/* COPIAR — container botoes pos-vitoria */
.upgrade-overlay__actions {
  display: flex;
  gap: clamp(8px, 1.2vw, 14px);
  margin-top: clamp(8px, 1.2vw, 14px);
}

/* COPIAR — botao UPGRADE DE NOVO */
.upgrade-overlay__btn-again {
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #00C853, #004D25);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  cursor: pointer;
  min-height: 44px;
  transition: all 0.15s ease;
}
.upgrade-overlay__btn-again:hover { transform: scale(1.05); }

/* COPIAR — botao VOLTAR (pos-resultado) */
.upgrade-overlay__btn-back {
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2.5vw, 28px);
  background: transparent;
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(11px, 1.3vw, 14px);
  cursor: pointer;
  min-height: 44px;
}
.upgrade-overlay__btn-back:hover {
  border-color: rgba(255,255,255,0.3);
  color: #FFFFFF;
}
```

---

## 1.16 Overlay Derrota (DS P1 §7 — tom sobrio)

```css
/* COPIAR EXATAMENTE — overlay derrota */
.upgrade-overlay--lose {
  position: absolute; inset: 0; z-index: 80;
  background: rgba(0,0,0,0.7);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(10px, 1.5vw, 16px);
  /* SEM backdrop-filter blur — mais sobrio que vitoria */
}

/* COPIAR — texto "FALHOU" */
.upgrade-overlay__title--lose {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(20px, 3.5vw, 36px);
  color: #FF1744;
  text-transform: uppercase;
  letter-spacing: 2px;
  text-shadow: 0 0 10px rgba(255,23,68,0.3), 0 2px 4px rgba(0,0,0,0.8);
  /* SEM pulse — sem animacao no titulo de derrota. Respeito pela perda. */
}

/* COPIAR — texto "Seu item foi destruido" */
.upgrade-overlay__subtitle--lose {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(11px, 1.3vw, 15px);
  color: rgba(212,168,67,0.6);
  text-align: center;
}

/* COPIAR — imagem fade-out do item destruido */
/*
  Framer Motion:
  initial={{ opacity: 1, scale: 1 }}
  animate={{ opacity: 0, scale: 0.5 }}
  transition={{ duration: 0.8, ease: 'easeIn' }}
  style={{ filter: 'grayscale(0.5)' }}
*/

/* COPIAR — shake do wheel na derrota */
/*
  Framer Motion no container do wheel:
  animate={{ x: [-4, 4, -3, 3, -1, 1, 0] }}
  transition={{ duration: 0.5, ease: 'easeOut' }}
*/
```

---

## 1.17 Input GCoin + Multiplicadores (DS P2 §10 + Custom)

```css
/* COPIAR EXATAMENTE — input numerico de aposta */
.upgrade-gcoin__input {
  width: 100%;
  padding: clamp(8px, 1vw, 12px);
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 10px;
  color: #00E676;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  text-align: center;
  outline: none;
  transition: all 0.2s ease;
}
.upgrade-gcoin__input:focus {
  border-color: rgba(0,230,118,0.5);
  box-shadow: 0 0 8px rgba(0,230,118,0.15);
}
.upgrade-gcoin__input--invalid {
  border-color: rgba(255,23,68,0.4);
  color: #FF3B3B;
}

/* COPIAR — atalhos MIN/x2/div2/MAX */
.upgrade-gcoin__shortcuts {
  display: flex;
  gap: 4px;
}
.upgrade-gcoin__shortcut {
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  color: #A8A8A8;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer;
  transition: all 0.15s ease;
}
.upgrade-gcoin__shortcut:hover {
  border-color: rgba(212,168,67,0.5);
  color: #D4A843;
}

/* COPIAR EXATAMENTE — grid de multiplicadores (7 botoes) */
.upgrade-gcoin__multipliers {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 4px;
}
.upgrade-gcoin__mult-btn {
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 16px);
  text-align: center;
  cursor: pointer;
  transition: all 0.2s ease;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}
.upgrade-gcoin__mult-btn--active {
  background: rgba(212,168,67,0.1);
  border-color: rgba(212,168,67,0.6);
  color: #D4A843;
}
/* COPIAR — chance% abaixo do multiplicador */
.upgrade-gcoin__mult-chance {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: 9px;
  color: #00E676;
}

/* COPIAR — x50 ocupa 3 colunas centralizado */
.upgrade-gcoin__mult-btn--x50 {
  grid-column: span 3;
  max-width: 50%;
  justify-self: center;
}

/* COPIAR — preview ganho/perda */
.upgrade-gcoin__preview {
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.15);
  border-radius: 12px;
  padding: clamp(10px, 1.5vw, 16px);
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.upgrade-gcoin__preview-label {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: 11px;
  color: #A8A8A8;
}
.upgrade-gcoin__preview-win {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 2vw, 22px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
.upgrade-gcoin__preview-lose {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 18px);
  color: #FF3B3B;
}
```

---

## 1.18 Historico Panel — Slide-in (DS P1 §4.2 + Custom)

```css
/* COPIAR EXATAMENTE — painel lateral historico */
.upgrade-history {
  position: absolute;
  top: 0; right: 0;
  width: clamp(280px, 35vw, 380px);
  height: 100%;
  background: rgba(5,5,5,0.95);
  border-left: 1.5px solid rgba(212,168,67,0.15);
  z-index: 75;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

/* COPIAR — header historico */
.upgrade-history__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(10px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
}
.upgrade-history__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: 14px;
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 2px;
}
.upgrade-history__btn-close {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: rgba(255,255,255,0.05);
  border: none;
  color: #A8A8A8;
  font-size: 14px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.15s ease;
}
.upgrade-history__btn-close:hover {
  background: rgba(255,59,59,0.15);
  color: #FF3B3B;
}

/* COPIAR — entry card (cada tentativa) */
.upgrade-history__entry {
  padding: clamp(8px, 1vw, 12px);
  border-bottom: 1px solid rgba(255,255,255,0.03);
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.upgrade-history__entry-id {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: 10px;
  color: #666666;
}
.upgrade-history__entry-items {
  font-family: 'Cinzel', serif;
  font-weight: 600;
  font-size: 12px;
  color: #D4A843;
}
.upgrade-history__entry-chance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: 11px;
  color: #A8A8A8;
}
.upgrade-history__entry-date {
  font-family: 'Inter', sans-serif;
  font-weight: 400;
  font-size: 10px;
  color: #666666;
}

/* COPIAR — badge resultado */
.upgrade-history__badge--win {
  display: inline-block;
  background: rgba(0,230,118,0.12);
  border: 1px solid rgba(0,230,118,0.3);
  color: #00E676;
  font-size: 10px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 4px;
}
.upgrade-history__badge--lose {
  display: inline-block;
  background: rgba(255,23,68,0.12);
  border: 1px solid rgba(255,23,68,0.3);
  color: #FF3B3B;
  font-size: 10px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 4px;
}

/* COPIAR — Framer Motion slide-in */
/*
  initial={{ x: '100%' }}
  animate={{ x: 0 }}
  exit={{ x: '100%' }}
  transition={{ duration: 0.3, ease: 'easeOut' }}
*/

/* COPIAR — mobile fullwidth */
@media (max-width: 768px) {
  .upgrade-history {
    width: 100%;
  }
}
```

---

## 1.19 Modal Provably Fair (DS P2 §12 glassmorphism)

```css
/* COPIAR EXATAMENTE — modal fair (base = 1.13 modal mas mais largo) */
.upgrade-fair {
  width: clamp(300px, 55vw, 460px);
  max-height: 80vh;
  overflow-y: auto;
  /* Herda todos os estilos de .upgrade-modal */
}

/* COPIAR — campo readonly (hash, resultado, chance) */
.upgrade-fair__field-readonly {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 500;
  font-size: clamp(10px, 1.2vw, 13px);
  overflow: hidden;
  text-overflow: ellipsis;
  user-select: all;
}

/* COPIAR — campo editavel (client_seed) */
.upgrade-fair__field-editable {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 500;
  font-size: clamp(10px, 1.2vw, 13px);
  outline: none;
}
.upgrade-fair__field-editable:focus {
  border-color: rgba(0,230,118,0.5);
  box-shadow: 0 0 6px rgba(0,230,118,0.1);
}

/* COPIAR — label de campo */
.upgrade-fair__label {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(10px, 1.1vw, 12px);
  color: #666666;
  margin-bottom: 4px;
}

/* COPIAR — info tecnica (margem + formula) */
.upgrade-fair__info {
  background: rgba(212,168,67,0.05);
  border: 1px solid rgba(212,168,67,0.1);
  border-radius: 8px;
  padding: clamp(8px, 1vw, 12px);
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(10px, 1.1vw, 12px);
  color: #A8A8A8;
}
.upgrade-fair__info strong {
  color: #D4A843;
}
.upgrade-fair__info code {
  font-family: 'JetBrains Mono', monospace;
  font-size: 11px;
  color: #A8A8A8;
}

/* COPIAR — botao copiar hash */
.upgrade-fair__btn-copy {
  width: 28px; height: 28px;
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  color: #A8A8A8;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  font-size: 12px;
  transition: all 0.15s ease;
}
.upgrade-fair__btn-copy:hover {
  border-color: rgba(212,168,67,0.5);
  color: #D4A843;
}

/* COPIAR — badge verificacao */
.upgrade-fair__badge--verified {
  background: rgba(0,230,118,0.1);
  border: 1px solid rgba(0,230,118,0.3);
  color: #00E676;
  padding: 6px 16px;
  border-radius: 8px;
  text-align: center;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: 13px;
}
.upgrade-fair__badge--failed {
  background: rgba(255,23,68,0.1);
  border: 1px solid rgba(255,23,68,0.3);
  color: #FF3B3B;
  padding: 6px 16px;
  border-radius: 8px;
  text-align: center;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: 13px;
}
```

---

## 1.20 Botao "?" — Provably Fair Trigger (DS P2 §8 adaptado)

```css
/* COPIAR EXATAMENTE — botao "?" no header */
.upgrade-btn-fair {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(212,168,67,0.3);
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: 13px;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  transition: all 0.15s ease;
  min-width: 28px;
}
.upgrade-btn-fair:hover {
  border-color: rgba(212,168,67,0.7);
  background: rgba(212,168,67,0.1);
}
```

---

# SECAO 2 — ATMOSFERAS POR ESTADO

## 2.1 Estado SELECTING (Aguardando selecao de itens)

Atmosfera de exploracao. Tudo acessivel, wheel neutro:

- **Fundo:** bg-casino.png + vinheta inset 150px. Radial gradient dourado sutil no topo.
- **Wheel:** cinza neutro `rgba(255,255,255,0.08)`, centro "?", chance "0.0%". Ticker parado no topo (12h).
- **Slots:** vazios com dashed border dourada 0.2 opacity. Icone "+" sutil.
- **Grids:** ativos, item cards com hover lift + borda raridade. Scroll vertical ativo.
- **Filtros:** toggles de raridade e tabs de tipo funcionais. Default: todos ativos.
- **Botao UPGRADE:** `--disabled`. Cinza, opacity 0.5, pointer-events none.
- **Header:** saldo verde, "?" ativo, VOLTAR ativo.
- **Particulas:** GoldParticles density baixa (~8), speedY -0.05, cor #D4A843 opacity 0.1. Sutis.
- **Som:** `upgrade_open` — chime curto 200ms, vol 40% ao abrir o jogo.

## 2.2 Estado SPINNING (Wheel girando — 3.5 segundos)

Atmosfera de tensao maxima. Foco TOTAL no wheel:

- **Controles:** TODOS dimmed `opacity: 0.4, pointer-events: none` (grids, slots, filtros, VOLTAR).
- **Wheel:** ticker girando via Framer Motion rotate. Zonas verde/vermelha visiveis em full opacity.
- **Botao UPGRADE:** transiciona para `--spinning` ("AGUARDE..." + waitPulse @keyframes).
- **Sons (sequencia em tempo real):**
  - t=0ms: `spin_start` — woosh ascendente 300ms, vol 70%.
  - t=0-2500ms: `spin_loop` — tick-tick-tick acelerando e desacelerando, vol 50%, pitch synced com velocidade do ticker.
  - t=3200ms: `spin_stop` — clunk metalico curto 150ms, vol 80% quando ticker quase para.
- **Particulas:** density ZERO durante spin (foco total no wheel).
- **Barra de risco:** visivel, estatica, sem animacao (referencia visual).

## 2.3 Estado RESULTADO WIN (Apos ticker parar no verde)

Atmosfera de celebracao:

- **Delay:** 0.3s apos ticker parar antes do overlay aparecer.
- **Wheel:** glow verde intenso na zona de parada `drop-shadow(0 0 15px rgba(0,230,118,0.6))`.
- **Overlay:** `upgrade-overlay--win` com blur 4px fade-in 0.4s.
- **Item:** scale bounce [0, 1.15, 1.0] com glow verde. delay: 0.2s apos overlay.
- **Titulo:** "UPGRADE!" victoryPulse 2s infinite.
- **Counter:** 0 → valor em 2s ease-out. Flash final prizeCounterFlash.
- **Confetti:** GoldParticles pool 30, cores #FFD700/D4A843/00E676/FFFFFF, life 2.5s, ativo por 2.5s.
- **Saldo:** balanceWinFlash 0.8s — APOS rolling counter terminar (nao antes).
- **Botoes:** UPGRADE DE NOVO (delay 0.8s) + VOLTAR (delay 0.9s).
- **Sons:**
  - t=300ms (apos ticker): `win` — fanfarra 1.5s, vol 85%.
  - t=500ms: confetti inicia.
  - t=2800ms (apos counter): `coin_drop` — cascata moedas 0.5s, vol 60%.

## 2.4 Estado RESULTADO LOSE (Apos ticker parar no vermelho)

Atmosfera de perda respeitosa:

- **Delay:** 0.3s apos ticker parar.
- **Wheel:** glow vermelho sutil `drop-shadow(0 0 15px rgba(255,23,68,0.4))` + shake x:[-4,4,-3,3,-1,1,0] 0.5s.
- **Overlay:** `upgrade-overlay--lose` SEM blur (mais sobrio). rgba(0,0,0,0.7).
- **Item:** fade-out [1→0] + scale [1→0.5] + grayscale(0.5). Duracao 0.8s. O jogador VE o item desaparecer.
- **Titulo:** "FALHOU" sem pulse, sem glow excessivo.
- **Subtitulo:** "Seu item foi destruido." dourado sutil.
- **Confetti:** ZERO. Sem particulas. Respeito pela perda.
- **Saldo:** balanceLoseFlash 0.5s (1x vermelho sutil) — apos fade-out do item terminar.
- **Botoes:** UPGRADE DE NOVO (delay 0.5s) + VOLTAR (delay 0.6s). Mesmos componentes, sem celebracao.
- **Sons:**
  - t=300ms: `lose` — tom descendente 0.8s, vol 50% (respeitoso, nao agressivo).

---

# SECAO 3 — TIMELINES EXATAS

## 3.1 Timeline Upgrade Flow (Item mode — desde clique UPGRADE ate resultado)

```
t=0ms       Jogador clica UPGRADE!
            Modal de confirmacao aparece (Framer Motion spring 0.3s)

t=0-Xms     Jogador le aviso de destruicao. Decide.

t=Xms       Jogador clica CONFIRMAR.
            Modal fecha (exit 0.2s).
            fetchNui('casino:upgrade:play', { invId, targetId })

t=X+0ms     Botao transiciona para --spinning ("AGUARDE..." + waitPulse).
            Controles dimmed (opacity 0.4).

t=X+200ms   Server retorna resultado:
            { won, resultado, anguloFinal, hash, chance }

t=X+200ms   Ticker inicia animacao:
            animate={{ rotate: anguloFinal }}
            transition={{ duration: 3.5, ease: [0.12, 0.8, 0.3, 1] }}
            play('spin_start') — woosh 300ms.

t=X+200-2700ms  Ticker girando. Tick-tick-tick desacelerando.
                 4 voltas completas. 8 cruzamentos fronteira verde/vermelho.

t=X+3200ms  Ticker quase parando. play('spin_stop') — clunk.

t=X+3700ms  Ticker parou. onAnimationComplete dispara.
            Glow na zona de parada (win/lose).

t=X+4000ms  SE WIN:
            → upgrade-overlay--win monta (fade 0.4s)
            → Item scale bounce (0.6s)
            → "UPGRADE!" com victoryPulse
            → play('win') — fanfarra 1.5s

t=X+4500ms  → Rolling counter inicia (0 → valor em 2s)
            → Confetti GoldParticles ativo

t=X+6500ms  → Counter termina. prizeCounterFlash.
            → play('coin_drop') — cascata moedas
            → Saldo atualiza com balanceWinFlash

t=X+7300ms  → Botao UPGRADE DE NOVO aparece (delay 0.8s)
t=X+7400ms  → Botao VOLTAR aparece (delay 0.9s)

TOTAL WIN: ~7.4 segundos apos confirmar
-----------------------------------------

t=X+4000ms  SE LOSE:
            → Wheel shake 0.5s
            → upgrade-overlay--lose monta (SEM blur)
            → Item fade-out 0.8s + grayscale
            → "FALHOU" + "Seu item foi destruido"
            → play('lose') — tom descendente 0.8s

t=X+4800ms  → Item completamente invisivel (opacity 0)
            → Saldo: balanceLoseFlash 0.5s

t=X+5300ms  → Botao UPGRADE DE NOVO aparece
t=X+5400ms  → Botao VOLTAR aparece

TOTAL LOSE: ~5.4 segundos apos confirmar
```

## 3.2 Timeline Modo GCoin (simplificado — sem modal de confirmacao)

```
t=0ms       Jogador clica UPGRADE! no modo GCoin
            SEM modal de confirmacao (aposta e em GCoin, nao item fisico)
            fetchNui('casino:upgrade:gcoin', { bet, multiplier })

t=0ms       Botao --spinning. Controles dimmed.

t=200ms     Server retorna. Ticker inicia (mesma animacao 3.5s).

t=3700ms    Ticker parou. Resultado.

t=4000ms    Overlay WIN ou LOSE (mesmos tempos do 3.1, mas texto mostra
            "+G$2.000" ou "-G$1.000" em vez de nome de item).

TOTAL: mesmos tempos do 3.1 menos tempo do modal (~1-2s menos)
```

## 3.3 Timeline Historico Abertura

```
t=0ms       Jogador clica [H] historico.
            Panel slide-in: initial={{ x: '100%' }} animate={{ x: 0 }}
            transition={{ duration: 0.3, ease: 'easeOut' }}

t=0ms       fetchNui('casino:upgrade:history')

t=200ms     Server retorna 20 entries.

t=200-600ms Entries montam com stagger:
            Cada entry: initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: 0.03 * i, duration: 0.2 }}

t=800ms     Todas entries visiveis. Panel totalmente renderizado.

FECHAR:
t=0ms       Clica [X]. exit={{ x: '100%' }} transition 0.2s.
t=200ms     Panel removido do DOM via AnimatePresence.
```

---

# SECAO 4 — CHECKLIST DE VALIDACAO (42 itens)

## Superficies e Cores (7 itens)
- [ ] Fundo usa bg-casino.png + radial gradient + vinheta inset 150px (nao flat)
- [ ] Wheel: zona verde #00E676 + zona vermelha #FF1744 sobre SVG com pathLength="100"
- [ ] Item cards: borda por raridade (Common #4B69FF, Uncommon #8847FF, Rare #D32CE6, Epic #EB4B4B, Legendary #FFD700)
- [ ] Slots vazios: dashed border dourada 0.2 opacity. Preenchidos: solid 0.4 opacity
- [ ] Victory overlay: backdrop-filter blur 4px, rgba(0,0,0,0.65)
- [ ] Defeat overlay: SEM blur, rgba(0,0,0,0.7) — mais sobrio
- [ ] Nenhum fundo branco ou cinza claro em NENHUM lugar

## Tipografia (6 itens)
- [ ] Titulos ("UPGRADE", "UPGRADE!", "FALHOU"): Cinzel 700-900 uppercase letter-spacing 2-3px text-shadow glow
- [ ] Valores GCoin: JetBrains Mono 700 #00E676 text-shadow verde
- [ ] Chance%: JetBrains Mono 700 clamp(18px,3vw,36px) #00E676 (centro wheel)
- [ ] Multiplicador: JetBrains Mono 600 clamp(12px,1.8vw,22px) #D4A843 (abaixo chance)
- [ ] Tudo com clamp() — NENHUM px fixo (REGRA X4)
- [ ] Nenhuma fonte < 9px em qualquer resolucao

## Bordas e Shadows (5 itens)
- [ ] Container: box-shadow inset 0 0 150px + multi-layer
- [ ] Modal glassmorphism: blur 12px + border dourada 0.2 + shadow 32px
- [ ] Botao UPGRADE: 2 camadas box-shadow (glow + inset bevel)
- [ ] Item card selecionado: border #00E676 + itemSelectedPulse animation
- [ ] Todas bordas usam rgba com opacidade (NAO hex solido)

## Animacoes e @keyframes (8 itens)
- [ ] `itemSelectedPulse` 1.5s infinite: border-color verde alterna 0.4↔0.8
- [ ] `victoryPulse` 2s infinite: text-shadow dourado intensifica
- [ ] `prizeCounterFlash` final: branco → verde rolling counter
- [ ] `balanceWinFlash` 0.8s: verde→branco alternando
- [ ] `balanceLoseFlash` 0.5s: verde→vermelho 1x sutil
- [ ] `waitPulse` 1.5s infinite: opacity 0.5→0.8 (AGUARDE...)
- [ ] `toastIn` 0.2s: translateY + opacity (tooltip mobile)
- [ ] Ticker: Framer Motion rotate cubic-bezier [0.12, 0.8, 0.3, 1] duracao 3.5s (NAO @keyframes — GPU composited via Framer Motion)

## Interatividade / Tooltips (5 itens — REGRA X5)
- [ ] 12 tooltips BR/EN mapeados (ver Prompt AI Parte 3)
- [ ] Tooltip desktop: hover 150ms delay, translateY 4px → 0
- [ ] Tooltip mobile: long-press 500ms, toastIn, position fixed bottom
- [ ] 4 estados em cada botao: normal/hover/active/disabled
- [ ] Touch targets: minimo 44px height em TODOS os botoes

## Responsividade — Mobile Portrait (5 itens — REGRA X4)
- [ ] Layout stack vertical em < 768px (flex-direction column)
- [ ] Wheel: clamp(180px, 50vw, 280px) centralizado
- [ ] Item grids: 2 colunas com clamp(70px, 22vw, 100px)
- [ ] Botao UPGRADE: width 100% min-height 48px
- [ ] Slots: flex-direction row com imagem 48px (compacto)

## Assets (6 itens — paths exatos)
- [ ] Fundo: `/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png`
- [ ] Card: `/assets/image-para-cards/15.UPGRADE-IMAGE-1344_576.png`
- [ ] Logo BR: `/assets/logos-br-para-cards/15.LOGO-BR-UPGRADE.png`
- [ ] Logo EN: `/assets/logos-in-para-cards/15.LOGO-IN-UPGRADE.png`
- [ ] Dourada: `/assets/image-background-transparent/.../15.IMAGEM-DOURADA-UPGRADE.png`
- [ ] Itens: `/assets/cases/itens/arsenal/*.png` e `/assets/cases/itens/garagem/*.png`

## Sons (3 itens — 10 sons mapeados)
- [ ] `upgrade_open` (200ms, 40%), `spin_start` (300ms, 70%), `spin_loop` (tick sync), `spin_stop` (150ms, 80%), `win` (1.5s, 85%), `lose` (0.8s, 50%), `coin_drop` (0.5s, 60%)
- [ ] `select` (click metalico, 100ms, 50%), `target` (tone agudo, 150ms, 50%), `hover` (tick sutil, 50ms, 30%)
- [ ] Volume hierarchy: hover 30% → UI 40-50% → spin FX 50-80% → resultado 50-85%

## Bilingue (2 itens)
- [ ] Todos textos via objeto `{ br: '...', en: '...' }`
- [ ] 12 tooltips bilingues (ver Prompt AI Parte 3)

---

# COMPARACAO COM BENCHMARKS

| Metrica | Loteria #18 | Giveaways #20 | **Upgrade #15** | Pool Game #22 |
|---------|:---:|:---:|:---:|:---:|
| Partes | 1 | 2 | **2** | 2 |
| Linhas totais | 685 | 1.054 | **ver total** | 1.917 |
| Componentes CSS | 14 | 12 | **20** | 18 |
| @keyframes | ~5 | - | **7** | 13 |
| Atmosferas | 4 | - | **4** | 4 |
| Timelines com ms | - | - | **3** | 4 |
| Checklist itens | - | - | **42** | 48 |
| Sons mapeados | 16 | - | **10** | 18 |

---

# VALIDACAO (REGRA X11)

- [x] **Secao 1 (Partes 1+2):** 20 componentes com CSS real `/* COPIAR EXATAMENTE */`, referenciando DS P1/P2/P3/Guia P3
- [x] **Secao 2:** 4 atmosferas com specs de sons (volume + timing), particulas, estados de cada componente
- [x] **Secao 3:** 3 timelines com timestamps ms precisos (Upgrade Flow item, GCoin, Historico)
- [x] **Secao 4:** 42 itens em 8 categorias (Superficies, Tipografia, Bordas, Animacoes, Interatividade, Mobile, Assets, Sons, Bilingue)
- [x] BEM naming: `.upgrade-[block]__[element]--[modifier]` consistente em todos os 20 componentes
- [x] 7 @keyframes com duracao, easing e loop documentados
- [x] 10 sons com volume hierarchy (30% → 85%)
- [x] 5 variantes de raridade para item cards (Common→Legendary) com borda + badge + hover
- [x] Fallback NUI para bg-casino.png documentado (§1.1 `.--loading`)
- [x] Mobile portrait: layout stack + wheel clamp + grids 2col + botao fullwidth
- [x] Precedencia sobre Prompt AI declarada

**STATUS: ADENDO DS — UPGRADE #15 — COMPLETO (2 partes)**

---

`[ENTREGA: 4.ADENDO-DS-UPGRADE-PARTE-2.md — 16/03/2026]`
