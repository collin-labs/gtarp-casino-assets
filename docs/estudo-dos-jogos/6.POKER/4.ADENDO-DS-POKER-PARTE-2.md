# ADENDO DESIGN SYSTEM — POKER (#6)
## PARTE 2 DE 4 — Componentes CSS Real (1.10-1.18)
### Blackout Casino GTARP - 19/03/2026

---

## 1.10 Botao FOLD — 4 Estados (DS P2 §8.1 adaptado)

```css
/* COPIAR EXATAMENTE — botao FOLD base */
.poker-btn-fold {
  min-width: clamp(100px, 16vw, 160px);
  min-height: 48px;
  background: rgba(139,26,26,0.6);
  border: 1px solid rgba(255,23,68,0.4);
  border-radius: 10px;
  color: #FF3B3B;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(14px, 2vw, 22px);
  cursor: pointer;
  transition: all 0.25s ease;
}

/* COPIAR — hover */
.poker-btn-fold:hover {
  background: rgba(139,26,26,0.8);
  border-color: rgba(255,23,68,0.7);
  box-shadow: 0 0 10px rgba(255,23,68,0.15);
}

/* COPIAR — active (pressed) */
.poker-btn-fold:active {
  transform: scale(0.95);
}

/* COPIAR — disabled (saldo insuficiente ou fase invalida) */
.poker-btn-fold--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
  filter: grayscale(0.3);
}
```

Framer Motion:
```
whileHover={{ scale: 1.03 }}
whileTap={{ scale: 0.95 }}
```

---

## 1.11 Botao RAISE/BET — 4 Estados + Pulse (DS P2 §8.1 adaptado)

```css
/* COPIAR EXATAMENTE — botao RAISE/BET base (acao principal) */
.poker-btn-bet {
  min-width: clamp(120px, 18vw, 180px);
  min-height: 48px;
  background: rgba(70,37,35,0.6);
  border: 1.5px solid rgba(212,168,67,0.5);
  border-radius: 10px;
  color: #FFD700;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(14px, 2vw, 22px);
  cursor: pointer;
  transition: all 0.3s ease;
  animation: pokerBorderPulse 2s ease-in-out infinite;
}

@keyframes pokerBorderPulse {
  0%, 100% { border-color: rgba(212,168,67,0.5); }
  50% { border-color: #FFD700; }
}

/* COPIAR — hover */
.poker-btn-bet:hover {
  background: rgba(70,37,35,0.8);
  border-color: #FFD700;
  box-shadow: 0 0 15px rgba(255,215,0,0.25);
}

/* COPIAR — active */
.poker-btn-bet:active {
  transform: scale(0.95);
}

/* COPIAR — disabled */
.poker-btn-bet--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
  animation: none;
  filter: grayscale(0.5);
}

/* COPIAR — variante secundaria (BET 3x, menos enfase que 4x) */
.poker-btn-bet--secondary {
  min-width: clamp(100px, 15vw, 150px);
  background: rgba(70,37,35,0.4);
  border: 1px solid rgba(212,168,67,0.3);
  color: #D4A843;
  animation: none;
}

.poker-btn-bet--secondary:hover {
  background: rgba(70,37,35,0.6);
  border-color: rgba(212,168,67,0.6);
  box-shadow: 0 0 10px rgba(212,168,67,0.15);
}
```

Framer Motion:
```
whileHover={{ scale: 1.03 }}
whileTap={{ scale: 0.95 }}
```

---

## 1.12 Botao CHECK — 4 Estados (DS P2 §8 adaptado)

```css
/* COPIAR EXATAMENTE — botao CHECK base (acao neutra) */
.poker-btn-check {
  min-width: clamp(80px, 12vw, 120px);
  min-height: 48px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 10px;
  color: #A8A8A8;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(12px, 1.5vw, 18px);
  cursor: pointer;
  transition: all 0.25s ease;
}

/* COPIAR — hover */
.poker-btn-check:hover {
  background: rgba(255,255,255,0.1);
  color: #FFFFFF;
  border-color: rgba(255,255,255,0.25);
}

/* COPIAR — active */
.poker-btn-check:active {
  transform: scale(0.95);
}

/* COPIAR — disabled */
.poker-btn-check--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}
```

---

## 1.13 Botao DEAL/DISTRIBUIR — 4 Estados + Pulse (DS P2 §8.1)

```css
/* COPIAR EXATAMENTE — botao DEAL (CTA principal, centralizado) */
.poker-btn-deal {
  width: clamp(150px, 24vw, 220px);
  min-height: 48px;
  background: rgba(70,37,35,0.6);
  border: 1.5px solid rgba(212,168,67,0.5);
  border-radius: 10px;
  color: #FFD700;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 17px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(10px, 1.2vw, 14px) clamp(16px, 2vw, 24px);
  cursor: pointer;
  transition: all 0.3s ease;
  align-self: center;
  animation: pokerBorderPulse 2s ease-in-out infinite;
}

/* COPIAR — hover */
.poker-btn-deal:hover {
  background: rgba(70,37,35,0.8);
  border-color: #FFD700;
  box-shadow: 0 0 15px rgba(255,215,0,0.25);
  transform: scale(1.03);
}

/* COPIAR — active */
.poker-btn-deal:active {
  transform: scale(0.97);
}

/* COPIAR — disabled (saldo insuficiente) */
.poker-btn-deal--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
  animation: none;
}
```

---

## 1.14 BetControls — Caribbean (DS P2 §8 + §7.3 adaptado)

```css
/* COPIAR EXATAMENTE — container de controles de aposta Caribbean */
.poker-bet {
  width: 100%;
  max-width: clamp(400px, 60vw, 700px);
  background: rgba(5,5,5,0.8);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 12px;
  padding: clamp(10px, 1.5vw, 18px);
  display: flex;
  flex-direction: column;
  gap: clamp(8px, 1vw, 14px);
  transition: opacity 0.3s ease;
}

/* COPIAR — row do slider e valor */
.poker-bet__slider-row {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1.2vw, 14px);
}

/* COPIAR — label ANTE */
.poker-bet__ante-label {
  font-family: 'Cinzel', serif;
  font-weight: 600;
  font-size: clamp(9px, 1vw, 12px);
  color: rgba(212,168,67,0.5);
  text-transform: uppercase;
  letter-spacing: 2px;
  white-space: nowrap;
}

/* COPIAR — slider input range */
.poker-bet__slider {
  -webkit-appearance: none;
  appearance: none;
  flex: 1;
  height: 4px;
  background: linear-gradient(
    90deg,
    rgba(212,168,67,0.3) 0%,
    #D4A843 var(--pct),
    rgba(255,255,255,0.1) var(--pct)
  );
  border-radius: 2px;
  outline: none;
}

.poker-bet__slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: clamp(18px, 2.5vw, 24px);
  height: clamp(18px, 2.5vw, 24px);
  background: radial-gradient(circle at 40% 40%, #FFD700 0%, #D4A843 100%);
  border-radius: 50%;
  border: 2px solid #FFD700;
  box-shadow: 0 0 8px rgba(255,215,0,0.4);
  cursor: pointer;
}

/* COPIAR — valor do ante exibido */
.poker-bet__ante-value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 20px);
  color: #00E676;
  text-shadow: 0 0 6px rgba(0,230,118,0.3);
  min-width: clamp(50px, 7vw, 80px);
  text-align: right;
}

/* COPIAR — row de presets (MIN / x2 / /2 / MAX) */
.poker-bet__presets {
  display: flex;
  gap: clamp(4px, 0.6vw, 8px);
}

/* COPIAR — botao preset individual */
.poker-bet__preset {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1vw, 12px);
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  min-height: 44px;
  min-width: 44px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.poker-bet__preset:hover {
  background: rgba(255,255,255,0.1);
  color: #FFD700;
  border-color: rgba(212,168,67,0.3);
}

.poker-bet__preset:active {
  transform: scale(0.95);
}

/* COPIAR — toggle side bet (jackpot $1) */
.poker-bet__side-bet {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
}

.poker-bet__toggle {
  width: clamp(36px, 4vw, 44px);
  height: clamp(20px, 2.5vw, 24px);
  background: rgba(255,255,255,0.1);
  border-radius: 12px;
  cursor: pointer;
  transition: background 0.3s ease;
  position: relative;
}

.poker-bet__toggle--on {
  background: rgba(0,230,118,0.4);
}

.poker-bet__toggle-thumb {
  width: 16px; height: 16px;
  border-radius: 50%;
  background: #FFFFFF;
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  left: 2px;
  transition: left 0.3s ease;
}

.poker-bet__toggle--on .poker-bet__toggle-thumb {
  left: calc(100% - 18px);
}

/* COPIAR — label side bet */
.poker-bet__side-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(9px, 1vw, 12px);
  color: #A8A8A8;
}

/* COPIAR — jackpot pool valor */
.poker-bet__jackpot-value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}
```

---

## 1.15 BetControls — UTH Bet Display Row (DS custom)

```css
/* COPIAR EXATAMENTE — row de bets ativos no UTH */
.poker-bet-display {
  display: flex;
  gap: clamp(10px, 1.5vw, 18px);
  justify-content: center;
  align-items: center;
  flex-wrap: wrap;
  padding-bottom: clamp(6px, 0.8vw, 10px);
  border-bottom: 1px solid rgba(255,255,255,0.05);
  margin-bottom: clamp(6px, 0.8vw, 10px);
}

/* COPIAR — item de bet individual (ANTE | BLIND | TRIPS | PLAY) */
.poker-bet-display__item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}

.poker-bet-display__label {
  font-family: 'Cinzel', serif;
  font-size: clamp(8px, 0.8vw, 10px);
  color: rgba(212,168,67,0.5);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.poker-bet-display__value {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(11px, 1.3vw, 15px);
  font-weight: 700;
  color: #00E676;
}

/* COPIAR — valor vazio (PLAY antes de bet) */
.poker-bet-display__value--empty {
  color: #666666;
}

/* COPIAR — estado LOCKED (jogador ja apostou, aguardando showdown) */
.poker-bet__locked-msg {
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(10px, 1.2vw, 14px);
  color: #666666;
  padding: clamp(8px, 1vw, 12px);
}
```

---

## 1.16 Chip Stack Visual (DS custom)

```css
/* COPIAR EXATAMENTE — chip individual */
.poker-chip {
  width: clamp(24px, 3vw, 36px);
  height: clamp(24px, 3vw, 36px);
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, #222222 0%, #141414 100%);
  border: 2px solid rgba(212,168,67,0.6);
  box-shadow: 0 2px 6px rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(8px, 0.9vw, 11px);
  color: #00E676;
}

/* COPIAR — chip empilhado (stack visual) */
.poker-chip-stack {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.poker-chip-stack .poker-chip + .poker-chip {
  margin-top: -3px;
}

/* COPIAR — chip em animacao de toss (voa para area de bet) */
.poker-chip--tossing {
  position: absolute;
  z-index: 20;
  pointer-events: none;
}
```

Framer Motion chip toss:
```
initial={{ x: saldoPos.x, y: saldoPos.y, scale: 0.5, opacity: 0 }}
animate={{
  x: [saldoPos.x, midX, betAreaPos.x],
  y: [saldoPos.y, midY - 40, betAreaPos.y],
  scale: [0.5, 1.2, 1],
  opacity: [0, 1, 1]
}}
transition={{ duration: 0.6, ease: [0.4, 0, 0.2, 1], times: [0, 0.5, 1] }}
```

---

## 1.17 Result Panel — 3 Variantes (DS P1 §4.4 + P2 §12 adaptado)

```css
/* COPIAR EXATAMENTE — result panel base */
.poker-result {
  width: 100%;
  max-width: clamp(400px, 60vw, 700px);
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 14px;
  padding: clamp(14px, 2vw, 24px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 12px);
  backdrop-filter: blur(4px);
}

/* COPIAR — titulo resultado WIN */
.poker-result__title--win {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(16px, 2.5vw, 28px);
  color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.5), 0 2px 4px rgba(0,0,0,0.8);
  text-transform: uppercase;
  letter-spacing: 3px;
}

/* COPIAR — titulo resultado LOSE */
.poker-result__title--lose {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(16px, 2.5vw, 28px);
  color: #FF3B3B;
  text-shadow: 0 0 15px rgba(255,23,68,0.3), 0 2px 4px rgba(0,0,0,0.8);
  text-transform: uppercase;
  letter-spacing: 3px;
}

/* COPIAR — titulo resultado PUSH */
.poker-result__title--push {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(16px, 2.5vw, 28px);
  color: #D4A843;
  text-shadow: 0 0 12px rgba(212,168,67,0.4), 0 2px 4px rgba(0,0,0,0.8);
  text-transform: uppercase;
  letter-spacing: 3px;
}

/* COPIAR — descricao resultado (ex: "PAR DE REIS VENCE PAR DE VALETE") */
.poker-result__description {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(10px, 1.2vw, 14px);
  color: #A8A8A8;
  text-align: center;
}

/* COPIAR — breakdown payout Caribbean (2 colunas: Ante + Raise) */
.poker-result__breakdown {
  display: flex;
  justify-content: center;
  gap: clamp(8px, 1.5vw, 16px);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(10px, 1.2vw, 14px);
}

/* COPIAR — breakdown payout UTH (4 colunas: Ante + Blind + Play + Trips) */
.poker-result__breakdown--uth {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: clamp(6px, 1vw, 12px);
  width: 100%;
}

.poker-result__breakdown-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}

.poker-result__breakdown-label {
  font-family: 'Cinzel', serif;
  font-size: clamp(8px, 0.8vw, 10px);
  color: rgba(212,168,67,0.5);
  text-transform: uppercase;
}

.poker-result__breakdown-value--positive {
  color: #00E676;
}

.poker-result__breakdown-value--negative {
  color: #FF3B3B;
}

.poker-result__breakdown-value--zero {
  color: #666666;
}

/* COPIAR — total payout (grande, destaque) */
.poker-result__total {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 2.2vw, 24px);
}

.poker-result__total--positive {
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.4);
}

.poker-result__total--negative {
  color: #FF3B3B;
  text-shadow: 0 0 10px rgba(255,23,68,0.3);
}

.poker-result__total--zero {
  color: #D4A843;
}

/* COPIAR — botoes acao (NOVA MAO + VOLTAR LOBBY) */
.poker-result__actions {
  display: flex;
  gap: clamp(10px, 1.5vw, 18px);
  margin-top: clamp(4px, 0.5vw, 8px);
}

/* COPIAR — botao NOVA MAO */
.poker-result__btn-new {
  background: rgba(70,37,35,0.6);
  border: 1.5px solid rgba(212,168,67,0.5);
  border-radius: 10px;
  color: #FFD700;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2vw, 24px);
  min-height: 48px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.poker-result__btn-new:hover {
  background: rgba(70,37,35,0.8);
  border-color: #FFD700;
  box-shadow: 0 0 12px rgba(255,215,0,0.2);
}

/* COPIAR — botao VOLTAR LOBBY */
.poker-result__btn-lobby {
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 10px;
  color: #A8A8A8;
  font-family: 'Cinzel', serif;
  font-weight: 600;
  font-size: clamp(10px, 1.2vw, 14px);
  text-transform: uppercase;
  letter-spacing: 1px;
  padding: clamp(8px, 1vw, 12px) clamp(14px, 1.8vw, 20px);
  min-height: 48px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.poker-result__btn-lobby:hover {
  color: #D4A843;
  border-color: rgba(212,168,67,0.3);
}

/* COPIAR — bonus info line (Blind Bonus / Trips) */
.poker-result__bonus-info {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1vw, 12px);
  color: #666666;
}

.poker-result__bonus-info--paid {
  color: #00E676;
}
```

Framer Motion result panel entrada:
```
initial={{ opacity: 0, y: 20, scale: 0.95 }}
animate={{ opacity: 1, y: 0, scale: 1 }}
transition={{ delay: 1.3, type: "spring", stiffness: 300, damping: 25 }}
```

---

## 1.18 Win Overlay — 4 Tiers (DS P1 §7 glassmorphism + custom)

```css
/* COPIAR EXATAMENTE — overlay base (Tier 2+) */
.poker-overlay-win {
  position: absolute; inset: 0; z-index: 70;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

/* COPIAR — Tier 2 Big Win (straight, flush, full house) */
.poker-overlay-win--big {
  background: radial-gradient(ellipse at center, rgba(0,0,0,0.7) 0%, rgba(0,0,0,0.95) 100%);
}

/* COPIAR — Tier 3 Huge Win (four kind, straight flush) */
.poker-overlay-win--huge {
  background: radial-gradient(ellipse at center, rgba(0,0,0,0.7) 0%, rgba(0,0,0,0.95) 100%);
  border: 2px solid rgba(212,168,67,0.4);
  border-radius: 18px;
  animation: winBorderPulse 1s ease-in-out infinite;
}

@keyframes winBorderPulse {
  0%, 100% { border-color: rgba(212,168,67,0.4); box-shadow: 0 0 15px rgba(212,168,67,0.1); }
  50% { border-color: #FFD700; box-shadow: 0 0 30px rgba(255,215,0,0.3); }
}

/* COPIAR — Tier 4 JACKPOT (royal flush + progressive) */
.poker-overlay-win--jackpot {
  background: radial-gradient(ellipse at center, rgba(70,37,35,0.8) 0%, rgba(0,0,0,0.98) 100%);
  z-index: 80;
}

/* COPIAR — titulo hand name (Big/Huge tiers) */
.poker-overlay-win__hand-name {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 4px;
}

.poker-overlay-win__hand-name--big {
  font-size: clamp(24px, 4vw, 48px);
  color: #FFD700;
  text-shadow:
    0 0 20px rgba(255,215,0,0.6),
    0 0 40px rgba(255,215,0,0.3),
    0 4px 8px rgba(0,0,0,0.8);
}

.poker-overlay-win__hand-name--huge {
  font-size: clamp(28px, 5vw, 56px);
  color: #FFD700;
  text-shadow:
    0 0 30px rgba(255,215,0,0.8),
    0 0 60px rgba(255,215,0,0.4),
    0 6px 12px rgba(0,0,0,0.9);
}

/* COPIAR — titulo JACKPOT (gradient text) */
.poker-overlay-win__jackpot-title {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(32px, 6vw, 72px);
  background: linear-gradient(180deg, #F6F2C0 0%, #FFD700 30%, #D4A843 60%, #8B6914 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  filter: drop-shadow(0 4px 12px rgba(255,215,0,0.6));
  text-transform: uppercase;
  letter-spacing: 6px;
}

/* COPIAR — valor payout (countup animado) */
.poker-overlay-win__payout {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #00E676;
}

.poker-overlay-win__payout--big {
  font-size: clamp(20px, 3.5vw, 40px);
  text-shadow: 0 0 15px rgba(0,230,118,0.5), 0 4px 8px rgba(0,0,0,0.8);
}

.poker-overlay-win__payout--jackpot {
  font-size: clamp(24px, 4.5vw, 52px);
  text-shadow:
    0 0 20px rgba(0,230,118,0.6),
    0 0 40px rgba(0,230,118,0.3),
    0 4px 8px rgba(0,0,0,0.8);
}

/* COPIAR — botao COLETAR (somente Tier 4 Jackpot) */
.poker-overlay-win__collect {
  margin-top: clamp(8px, 1.5vw, 16px);
  background: linear-gradient(180deg, rgba(0,200,83,0.3) 0%, rgba(0,77,37,0.6) 100%);
  border: 2px solid #00E676;
  border-radius: 12px;
  color: #00E676;
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(14px, 2vw, 22px);
  text-transform: uppercase;
  letter-spacing: 3px;
  padding: clamp(12px, 1.5vw, 18px) clamp(24px, 3vw, 40px);
  min-height: 56px;
  cursor: pointer;
  animation: pokerCollectPulse 1.5s ease-in-out infinite;
}

@keyframes pokerCollectPulse {
  0%, 100% { box-shadow: 0 0 10px rgba(0,230,118,0.3); transform: scale(1); }
  50% { box-shadow: 0 0 25px rgba(0,230,118,0.6); transform: scale(1.03); }
}

.poker-overlay-win__collect:hover {
  background: linear-gradient(180deg, rgba(0,200,83,0.5) 0%, rgba(0,77,37,0.8) 100%);
  box-shadow: 0 0 30px rgba(0,230,118,0.5);
}
```

---

**[FIM DA PARTE 2 - Continua na Parte 3 com componentes 1.19-1.25 (ModeSelect, Modais, Tooltip, Paytable, NavBar) + Atmosferas por Estado]**

`[ENTREGA: 4.ADENDO-DS-POKER-PARTE-2.md - 19/03/2026]`
