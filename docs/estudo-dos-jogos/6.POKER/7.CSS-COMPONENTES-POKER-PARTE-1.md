# CSS COMPONENTES — POKER (#6)
## Parte 1 de 2 - Tokens :root + @keyframes (6) + Componentes 1-12
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-POKER
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 19/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Poker no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.poker-card__front { border: 1px solid rgba(212,168,67,0.3); }` daqui.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Poker)

```css
:root {
  /* --- Primitivos --- */
  --poker-gold-primary:    #D4A843;
  --poker-gold-light:      #F6E27A;
  --poker-gold-dark:       #8B6914;
  --poker-gold-deeper:     #462523;
  --poker-green-neon:      #00E676;
  --poker-green-dark:      #004D25;
  --poker-green-medium:    #00C853;
  --poker-black-bg:        #0A0A0A;
  --poker-black-surface:   #0F0F0F;
  --poker-red:             #FF1744;
  --poker-red-error:       #FF3B3B;
  --poker-red-fold:        rgba(139,26,26,0.6);
  --poker-yellow:          #FFD700;
  --poker-white:           #FFFFFF;
  --poker-text-secondary:  #A8A8A8;
  --poker-text-muted:      #666666;

  /* --- Naipes --- */
  --poker-suit-gold:       #D4A843;
  --poker-suit-green:      #00E676;

  /* --- Semanticos --- */
  --poker-border-card:        1px solid rgba(212,168,67,0.3);
  --poker-border-card-back:   1px solid rgba(212,168,67,0.5);
  --poker-border-area:        1px solid rgba(212,168,67,0.15);
  --poker-border-area-player: 1px solid rgba(212,168,67,0.2);
  --poker-border-btn-bet:     1.5px solid rgba(212,168,67,0.5);
  --poker-border-btn-fold:    1px solid rgba(255,23,68,0.4);
  --poker-shadow-card:        0 2px 8px rgba(0,0,0,0.5), 0 0 1px rgba(212,168,67,0.2);
  --poker-shadow-card-win:    0 2px 8px rgba(0,0,0,0.5), 0 0 15px rgba(0,230,118,0.2), 0 0 30px rgba(0,230,118,0.1);
  --poker-shadow-modal:       0 8px 40px rgba(0,0,0,0.6), 0 0 20px rgba(212,168,67,0.05);
  --poker-text-shadow-gold:   0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
  --poker-text-shadow-green:  0 0 8px rgba(0,230,118,0.4);

  /* --- Fontes --- */
  --poker-font-title: 'Cinzel', serif;
  --poker-font-body:  'Inter', sans-serif;
  --poker-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --poker-touch-min:     44px;
  --poker-radius-modal:  14px;
  --poker-radius-area:   12px;
  --poker-radius-btn:    10px;
  --poker-radius-card:   8px;
  --poker-radius-badge:  6px;
}
```

---

# 1. @KEYFRAMES (6 animacoes do Poker)

```css
/* 1. pokerBorderPulse — borda dourada pulsante em DEAL/RAISE/BET */
@keyframes pokerBorderPulse {
  0%, 100% { border-color: rgba(212,168,67,0.5); }
  50% { border-color: #FFD700; }
}

/* 2. pokerFlipGlow — glow durante card flip (50% pico) */
@keyframes pokerFlipGlow {
  0%   { box-shadow: 0 2px 8px rgba(0,0,0,0.5); }
  50%  { box-shadow: 0 0 20px rgba(255,215,0,0.4), 0 2px 8px rgba(0,0,0,0.5); }
  100% { box-shadow: 0 2px 8px rgba(0,0,0,0.5); }
}

/* 3. winBorderPulse — borda overlay Tier 3 Huge */
@keyframes winBorderPulse {
  0%, 100% { border-color: rgba(212,168,67,0.4); box-shadow: 0 0 15px rgba(212,168,67,0.1); }
  50% { border-color: #FFD700; box-shadow: 0 0 30px rgba(255,215,0,0.3); }
}

/* 4. pokerCollectPulse — botao COLETAR jackpot */
@keyframes pokerCollectPulse {
  0%, 100% { box-shadow: 0 0 10px rgba(0,230,118,0.3); transform: scale(1); }
  50% { box-shadow: 0 0 25px rgba(0,230,118,0.6); transform: scale(1.03); }
}

/* 5. handNamePulse — nome mao forte (flush+) pulsando */
@keyframes handNamePulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

/* 6. riverGlow — glow da ultima community card */
@keyframes riverGlow {
  0%   { box-shadow: 0 0 0px rgba(255,215,0,0); }
  50%  { box-shadow: 0 0 20px rgba(255,215,0,0.4); }
  100% { box-shadow: 0 0 0px rgba(255,215,0,0); }
}
```

---

# 2. COMPONENTE: Container Principal

```css
.poker-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column; align-items: center;
  padding: clamp(8px, 1.5vw, 16px); gap: clamp(8px, 1.2vw, 16px);
}
.poker-game--dealing .poker-bet { opacity: 0.4; pointer-events: none; }
.poker-game--result .poker-dealer, .poker-game--result .poker-player { filter: brightness(0.8); }
.poker-game__accent-line {
  position: absolute; bottom: 0; left: 0; right: 0; height: 3px;
  background: linear-gradient(90deg, transparent 5%, rgba(0,230,118,0.3) 20%, #00E676 50%, rgba(0,230,118,0.3) 80%, transparent 95%);
  box-shadow: 0 0 8px rgba(0,230,118,0.3); pointer-events: none; z-index: 5;
}
```

---

# 3. COMPONENTE: Header Bar

```css
.poker-header { width: 100%; display: flex; justify-content: space-between; align-items: center; padding: 0 clamp(4px,0.6vw,8px); }
.poker-header__back { background: rgba(0,0,0,0.6); border: 1px solid rgba(212,168,67,0.4); border-radius: 8px; color: #D4A843; font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(10px,1.2vw,14px); letter-spacing: 2px; text-transform: uppercase; padding: clamp(5px,0.6vw,8px) clamp(8px,1.2vw,14px); min-height: 44px; cursor: pointer; }
.poker-header__back:hover { border-color: rgba(212,168,67,0.8); background: rgba(0,0,0,0.8); }
.poker-header__title { font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(14px,2vw,22px); color: #D4A843; text-transform: uppercase; letter-spacing: 3px; text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8); }
.poker-header__balance { font-family: 'JetBrains Mono', monospace; font-weight: 700; font-size: clamp(12px,1.6vw,18px); color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
```

---

# 4. COMPONENTE: Card (6 estados)

```css
.poker-card { width: clamp(50px,7vw,80px); height: clamp(70px,10vw,112px); perspective: 1000px; flex-shrink: 0; }
.poker-card__inner { width: 100%; height: 100%; position: relative; transform-style: preserve-3d; transition: transform 0.4s cubic-bezier(0.4,0,0.2,1); }
.poker-card__inner--face-down { transform: rotateY(180deg); }
.poker-card__inner--face-up { transform: rotateY(0deg); }
.poker-card__front { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 8px; background: linear-gradient(135deg,#0A0A0A,#1A1A1A); border: 1px solid rgba(212,168,67,0.3); box-shadow: 0 2px 8px rgba(0,0,0,0.5), 0 0 1px rgba(212,168,67,0.2); display: flex; flex-direction: column; justify-content: space-between; padding: clamp(3px,0.4vw,5px); }
.poker-card__back { position: absolute; inset: 0; backface-visibility: hidden; transform: rotateY(180deg); border-radius: 8px; background: linear-gradient(135deg,#0F0F0F,#141414); border: 1px solid rgba(212,168,67,0.5); box-shadow: 0 2px 8px rgba(0,0,0,0.5); }
.poker-card__back::after { content: ''; position: absolute; inset: 4px; border-radius: 6px; background: repeating-linear-gradient(45deg,transparent 0px,transparent 4px,rgba(212,168,67,0.06) 4px,rgba(212,168,67,0.06) 5px); border: 1px solid rgba(212,168,67,0.15); }
.poker-card__value { font-family: 'Cinzel', serif; font-weight: 900; font-size: clamp(12px,1.6vw,20px); color: #FFFFFF; text-shadow: 0 1px 3px rgba(0,0,0,0.6); line-height: 1; }
.poker-card__suit--gold { color: #D4A843; }
.poker-card__suit--green { color: #00E676; }
.poker-card__suit-center { position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%); font-size: clamp(20px,3vw,36px); opacity: 0.3; }
.poker-card--winning .poker-card__front { box-shadow: 0 2px 8px rgba(0,0,0,0.5), 0 0 15px rgba(0,230,118,0.2), 0 0 30px rgba(0,230,118,0.1); border-color: rgba(0,230,118,0.4); }
.poker-card--flipping { animation: pokerFlipGlow 0.4s ease; }
.poker-card--disabled { opacity: 0.6; filter: grayscale(0.3); pointer-events: none; }
```

---

# 5. COMPONENTE: Dealer Area

```css
.poker-dealer { width: 100%; max-width: clamp(400px,60vw,700px); min-height: clamp(60px,10vh,100px); display: flex; flex-direction: column; align-items: center; gap: clamp(4px,0.5vw,6px); background: rgba(5,5,5,0.4); border: 1px solid rgba(212,168,67,0.15); border-radius: 12px; padding: clamp(8px,1.2vw,14px); position: relative; }
.poker-dealer__label { position: absolute; top: clamp(4px,0.5vw,6px); left: clamp(8px,1vw,12px); font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(8px,0.9vw,11px); color: rgba(212,168,67,0.5); text-transform: uppercase; letter-spacing: 2px; }
.poker-dealer__cards { display: flex; gap: clamp(6px,1vw,12px); justify-content: center; }
.poker-dealer__hand-name { font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(10px,1.2vw,14px); color: #A8A8A8; text-transform: uppercase; letter-spacing: 2px; }
.poker-dealer--empty .poker-dealer__label { position: static; font-size: clamp(10px,1.2vw,14px); color: rgba(212,168,67,0.3); }
.poker-dealer--loses { opacity: 0.7; }
```

---

# 6. COMPONENTE: Player Area

```css
.poker-player { width: 100%; max-width: clamp(400px,60vw,700px); min-height: clamp(80px,12vh,120px); display: flex; flex-direction: column; align-items: center; gap: clamp(4px,0.5vw,6px); background: rgba(5,5,5,0.5); border: 1px solid rgba(212,168,67,0.2); border-radius: 12px; padding: clamp(8px,1.2vw,14px); position: relative; }
.poker-player__hand-name { font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(11px,1.4vw,16px); color: #FFD700; text-shadow: 0 0 8px rgba(255,215,0,0.3); text-transform: uppercase; letter-spacing: 2px; }
.poker-player--winning { box-shadow: 0 0 20px rgba(0,230,118,0.15); border-color: rgba(0,230,118,0.25); }
.poker-player--losing { opacity: 0.7; }
.poker-player__hand-name--strong { animation: handNamePulse 1.5s ease-in-out infinite; }
```

---

# 7. COMPONENTE: Community Slots (UTH)

```css
.poker-community { width: 100%; max-width: clamp(380px,55vw,600px); display: flex; justify-content: center; gap: clamp(6px,1vw,12px); padding: clamp(6px,0.8vw,10px); min-height: clamp(70px,11vh,112px); }
.poker-community__slot--empty { width: clamp(50px,7vw,80px); height: clamp(70px,10vw,112px); border-radius: 8px; border: 1.5px dashed rgba(212,168,67,0.15); background: rgba(255,255,255,0.02); display: flex; align-items: center; justify-content: center; }
.poker-community__slot-placeholder { color: rgba(212,168,67,0.1); font-size: clamp(14px,2vw,22px); font-family: 'Cinzel', serif; }
.poker-community__slot--winning .poker-card__front { box-shadow: 0 2px 8px rgba(0,0,0,0.5), 0 0 12px rgba(0,230,118,0.25), 0 0 24px rgba(0,230,118,0.1); border-color: rgba(0,230,118,0.4); }
.poker-community__slot--river-reveal { animation: riverGlow 0.8s ease 0.5s; }
```

---

# 8. COMPONENTE: Divisor Dourado

```css
.poker-table__divider { width: 100%; max-width: clamp(400px,60vw,700px); height: 1px; background: linear-gradient(90deg, transparent 5%, rgba(212,168,67,0.3) 30%, rgba(212,168,67,0.5) 50%, rgba(212,168,67,0.3) 70%, transparent 95%); margin: clamp(2px,0.4vw,6px) 0; }
```

---

# 9. COMPONENTE: Qualification Badge (6 variantes)

```css
.poker-badge { display: inline-flex; align-items: center; gap: clamp(4px,0.5vw,6px); border-radius: 6px; padding: clamp(3px,0.4vw,5px) clamp(6px,0.8vw,10px); font-family: 'Inter', sans-serif; font-weight: 600; font-size: clamp(8px,0.9vw,11px); text-transform: uppercase; letter-spacing: 1px; }
.poker-badge__dot { width: 6px; height: 6px; border-radius: 50%; }
.poker-badge--qualifies { background: rgba(0,230,118,0.1); border: 1px solid rgba(0,230,118,0.3); color: #00E676; }
.poker-badge--qualifies .poker-badge__dot { background: #00E676; box-shadow: 0 0 4px rgba(0,230,118,0.5); }
.poker-badge--no-qualify { background: rgba(255,23,68,0.1); border: 1px solid rgba(255,23,68,0.3); color: #FF3B3B; }
.poker-badge--no-qualify .poker-badge__dot { background: #FF3B3B; }
.poker-badge--win { background: rgba(0,230,118,0.08); border: 1px solid rgba(0,230,118,0.2); color: #00E676; }
.poker-badge--lose { background: rgba(255,23,68,0.08); border: 1px solid rgba(255,23,68,0.2); color: #FF3B3B; }
.poker-badge--push { background: rgba(212,168,67,0.08); border: 1px solid rgba(212,168,67,0.2); color: #D4A843; }
.poker-badge--mode { background: rgba(212,168,67,0.08); border: 1px solid rgba(212,168,67,0.15); color: #D4A843; font-family: 'Cinzel', serif; }
```

---

# 10. COMPONENTE: Botao FOLD (4 estados)

```css
.poker-btn-fold { min-width: clamp(100px,16vw,160px); min-height: 48px; background: rgba(139,26,26,0.6); border: 1px solid rgba(255,23,68,0.4); border-radius: 10px; color: #FF3B3B; font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(11px,1.3vw,15px); text-transform: uppercase; letter-spacing: 2px; padding: clamp(8px,1vw,12px) clamp(14px,2vw,22px); cursor: pointer; transition: all 0.25s ease; }
.poker-btn-fold:hover { background: rgba(139,26,26,0.8); border-color: rgba(255,23,68,0.7); box-shadow: 0 0 10px rgba(255,23,68,0.15); }
.poker-btn-fold:active { transform: scale(0.95); }
.poker-btn-fold--disabled { opacity: 0.4; cursor: not-allowed; pointer-events: none; }
```

---

# 11. COMPONENTE: Botao BET/RAISE (4 estados + pulse + secundario)

```css
.poker-btn-bet { min-width: clamp(120px,18vw,180px); min-height: 48px; background: rgba(70,37,35,0.6); border: 1.5px solid rgba(212,168,67,0.5); border-radius: 10px; color: #FFD700; font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(11px,1.3vw,15px); text-transform: uppercase; letter-spacing: 2px; padding: clamp(8px,1vw,12px) clamp(14px,2vw,22px); cursor: pointer; transition: all 0.3s ease; animation: pokerBorderPulse 2s ease-in-out infinite; }
.poker-btn-bet:hover { background: rgba(70,37,35,0.8); border-color: #FFD700; box-shadow: 0 0 15px rgba(255,215,0,0.25); }
.poker-btn-bet:active { transform: scale(0.95); }
.poker-btn-bet--disabled { opacity: 0.4; cursor: not-allowed; pointer-events: none; animation: none; filter: grayscale(0.5); }
.poker-btn-bet--secondary { min-width: clamp(100px,15vw,150px); background: rgba(70,37,35,0.4); border: 1px solid rgba(212,168,67,0.3); color: #D4A843; animation: none; }
.poker-btn-bet--secondary:hover { background: rgba(70,37,35,0.6); border-color: rgba(212,168,67,0.6); box-shadow: 0 0 10px rgba(212,168,67,0.15); }
```

---

# 12. COMPONENTE: Botao CHECK (4 estados)

```css
.poker-btn-check { min-width: clamp(80px,12vw,120px); min-height: 48px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.15); border-radius: 10px; color: #A8A8A8; font-family: 'Cinzel', serif; font-weight: 700; font-size: clamp(11px,1.3vw,15px); text-transform: uppercase; letter-spacing: 2px; padding: clamp(8px,1vw,12px) clamp(12px,1.5vw,18px); cursor: pointer; transition: all 0.25s ease; }
.poker-btn-check:hover { background: rgba(255,255,255,0.1); color: #FFFFFF; border-color: rgba(255,255,255,0.25); }
.poker-btn-check:active { transform: scale(0.95); }
.poker-btn-check--disabled { opacity: 0.4; cursor: not-allowed; pointer-events: none; }
```

---

**[FIM DA PARTE 1 - Continua na Parte 2 com Componentes 13-20 (DEAL, BetControls, Slider, ModeSelect, Modal, Paytable, Result, WinOverlay, Tooltip, NavBar)]**

`[ENTREGA: 7.CSS-COMPONENTES-POKER-PARTE-1.md - 19/03/2026]`
