# ADENDO — ROTEIRO DE DESENVOLVIMENTO POKER (#6)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-POKER-PARTE-5.md — 23/03/2026

---

# 1. FASE 5 POLISH — CODIGO REAL TSX

## 1.1 HistoryPanel.tsx — Trechos com tooltips

```tsx
{history.map((hand, i) => (
  <motion.div
    key={hand.id}
    initial={{ opacity: 0, x: 10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: hand.won ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${hand.won ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    <span
      title={isBR ? (hand.won ? "Vitoria" : hand.result === "push" ? "Empate" : "Derrota") : (hand.won ? "Win" : hand.result === "push" ? "Push" : "Lose")}
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(10px, 1vw, 14px)",
        fontWeight: 800,
        color: hand.won ? "#00E676" : hand.result === "push" ? "#A8A8A8" : "#FF1744",
        minWidth: "28px",
        textAlign: "center",
      }}
    >
      {hand.won ? "W" : hand.result === "push" ? "P" : "L"}
    </span>

    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(8px, 0.8vw, 11px)", fontWeight: 600, color: hand.mode === "caribbean" ? "#D4A843" : "#00E676", background: `rgba(${hand.mode === "caribbean" ? "212,168,67" : "0,230,118"},0.1)`, padding: "2px 8px", borderRadius: "4px", letterSpacing: "1px", textTransform: "uppercase" as const }}>
      {hand.mode === "caribbean" ? "CSP" : "UTH"}
    </span>

    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(8px, 0.8vw, 12px)", color: "rgba(212,168,67,0.6)", flex: 1 }}>
      {hand.handRank}
    </span>

    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(10px, 1vw, 14px)", fontWeight: 700, color: hand.won ? "#00E676" : hand.result === "push" ? "#A8A8A8" : "rgba(212,168,67,0.4)", marginLeft: "auto", textShadow: hand.won ? "0 0 8px rgba(0,230,118,0.4)" : "none" }}>
      {hand.won ? `+G$${hand.payout.toLocaleString()}` : hand.result === "push" ? "G$0" : `-G$${hand.totalBet.toLocaleString()}`}
    </span>

    <motion.button
      onClick={() => onVerify(hand.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={isBR ? "Verificar fairness desta mao" : "Verify this hand's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/poker/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 1.2 PaytableModal.tsx — Trechos

```tsx
{/* Tabs Caribbean vs UTH */}
{(["caribbean", "uth"] as const).map((tab) => (
  <motion.button
    key={tab}
    onClick={() => setActiveMode(tab)}
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
    title={isBR
      ? { caribbean: "Payouts do Caribbean Stud", uth: "Payouts do Ultimate Texas Hold'em" }[tab]
      : { caribbean: "Caribbean Stud payouts", uth: "Ultimate Texas Hold'em payouts" }[tab]
    }
    style={{
      background: activeMode === tab ? "linear-gradient(135deg, rgba(212,168,67,0.25), rgba(212,168,67,0.1))" : "rgba(255,255,255,0.03)",
      border: `1px solid ${activeMode === tab ? "rgba(212,168,67,0.6)" : "rgba(255,255,255,0.08)"}`,
      borderRadius: "8px",
      color: activeMode === tab ? "#FFD700" : "rgba(212,168,67,0.5)",
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(9px, 0.95vw, 13px)",
      fontWeight: 700,
      letterSpacing: "2px",
      textTransform: "uppercase" as const,
      padding: "clamp(6px, 0.6vw, 10px) clamp(12px, 1.2vw, 20px)",
      cursor: "pointer",
      minHeight: "44px",
    }}
  >
    {tab === "caribbean" ? "CARIBBEAN STUD" : "UTH"}
  </motion.button>
))}
```

---

# 2. REGRAS COMPLEMENTO (item 5.11)

9. TODOS botoes min 44px (DEAL, RAISE, FOLD, CALL, side bets)
10. TODOS textos via pokerTexts[key][lang]
11. Deck shuffle e hand evaluation SEMPRE server-authoritative
12. Card flip SEMPRE com perspective(1000px) + Framer rotateY

---

**VALIDACAO X17:** 3 itens, 3 entregues.

`[ENTREGA: ADENDO-ROTEIRO-POKER.md — 23/03/2026]`
