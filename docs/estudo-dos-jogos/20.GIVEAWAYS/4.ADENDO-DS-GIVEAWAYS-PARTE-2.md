# ADENDO DESIGN SYSTEM — GIVEAWAYS (#20)
## PARTE 2 DE 4 — Componentes CSS Real (1.9-1.16)
### Blackout Casino GTARP - 25/03/2026

---

## 1.9 Modal Base Glassmorphism (DS P1 §4.3 + DS P2 §12.1)

Base reutilizada por Modal Compra e Modal PF. Qualquer modal do Giveaways herda daqui.

```css
/* COPIAR EXATAMENTE — backdrop overlay (escurece e blur o fundo) */
.giveaway-modal__backdrop {
  position: fixed; inset: 0; z-index: 70;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(5px);
  -webkit-backdrop-filter: blur(5px);
  display: flex; align-items: center; justify-content: center;
}

/* COPIAR — conteudo do modal (glassmorphism dark) */
.giveaway-modal__content {
  background: rgba(10,10,10,0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: clamp(12px, 2vw, 20px);
  padding: clamp(20px, 3vw, 32px);
  width: clamp(280px, 45vw, 420px);
  max-height: 85vh;
  overflow-y: auto;
  box-shadow:
    0 20px 60px rgba(0,0,0,0.5),
    0 0 1px rgba(212,168,67,0.3),
    inset 0 1px 0 rgba(255,255,255,0.03);
  display: flex; flex-direction: column;
  gap: clamp(12px, 2vw, 20px);
  position: relative;
}

/* COPIAR — titulo do modal */
.giveaway-modal__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  text-align: center;
}
```

---

## 1.10 Input Quantidade Tickets (DS P2 §9.1 + §8.8 adaptado)

Componente especifico: input numerico com botoes -/+ e display central. Usado no Modal Compra.

```css
/* COPIAR EXATAMENTE — container do input quantidade */
.giveaway-modal__qty-container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1.5vw, 16px);
}

/* COPIAR — botao -/+ (DS P2 §8.8 atalhos adaptado) */
.giveaway-modal__qty-btn {
  width: clamp(36px, 5vw, 48px);
  height: clamp(36px, 5vw, 48px);
  background: #1A1A1A;
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: 10px;
  color: #D4A843;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(16px, 2vw, 24px);
  font-weight: 700;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.15s ease;
  min-height: 44px; min-width: 44px;
}
.giveaway-modal__qty-btn:hover {
  background: #222222;
  border-color: #FFD700;
  color: #FFD700;
}
.giveaway-modal__qty-btn:active {
  transform: scale(0.9);
}
.giveaway-modal__qty-btn--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  transform: none;
}
.giveaway-modal__qty-btn--disabled:hover {
  background: #1A1A1A;
  border-color: rgba(212,168,67,0.3);
  color: #D4A843;
}

/* COPIAR — display quantidade central */
.giveaway-modal__qty-display {
  width: clamp(60px, 10vw, 100px);
  height: clamp(44px, 6vw, 56px);
  background: #141414;
  border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 12px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(20px, 3vw, 32px);
  color: #FFFFFF;
  display: flex; align-items: center; justify-content: center;
  text-align: center;
  box-shadow: inset 0 2px 6px rgba(0,0,0,0.3);
}
```

---

## 1.11 Linhas de Informacao do Modal (DS P2 §10 tabelas adaptado)

Linhas label + valor usadas no modal compra (preco unitario, subtotal, saldo).

```css
/* COPIAR EXATAMENTE — linha info */
.giveaway-modal__info-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(4px, 0.5vw, 8px) 0;
}

/* COPIAR — label (lado esquerdo) */
.giveaway-modal__info-label {
  font-family: 'Inter', sans-serif;
  font-size: clamp(12px, 1.4vw, 15px);
  color: #A8A8A8;
}

/* COPIAR — valor GCoin (lado direito) */
.giveaway-modal__info-value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(14px, 1.8vw, 18px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.2);
}

/* COPIAR — subtotal (destaque dourado) */
.giveaway-modal__info-value--subtotal {
  font-size: clamp(16px, 2.2vw, 22px);
  font-weight: 700;
  color: #FFD700;
  text-shadow: 0 0 10px rgba(255,215,0,0.3);
}

/* COPIAR — saldo insuficiente (vermelho) */
.giveaway-modal__info-value--insufficient {
  color: #FF3B3B;
  text-shadow: 0 0 8px rgba(255,59,59,0.2);
}
```

---

## 1.12 Drum Viewport (DS custom — componente especifico Giveaways)

Container central do draw com scroll vertical e fade gradients topo/base.

```css
/* COPIAR EXATAMENTE — viewport do drum */
.giveaway-drum__viewport {
  width: clamp(260px, 40vw, 400px);
  height: clamp(200px, 30vh, 320px);
  background: rgba(10,10,10,0.95);
  border: 2px solid rgba(212,168,67,0.3);
  border-radius: clamp(12px, 2vw, 20px);
  overflow: hidden;
  position: relative;
  box-shadow:
    0 0 40px rgba(0,0,0,0.6),
    inset 0 0 20px rgba(0,0,0,0.4),
    0 0 1px rgba(212,168,67,0.2);
}

/* COPIAR — fade gradient topo (mascara items cortados) */
.giveaway-drum__fade-top {
  position: absolute; top: 0; left: 0; right: 0;
  height: 40px; z-index: 2;
  background: linear-gradient(180deg, rgba(10,10,10,0.95) 0%, transparent 100%);
  pointer-events: none;
}

/* COPIAR — fade gradient base */
.giveaway-drum__fade-bottom {
  position: absolute; bottom: 0; left: 0; right: 0;
  height: 40px; z-index: 2;
  background: linear-gradient(0deg, rgba(10,10,10,0.95) 0%, transparent 100%);
  pointer-events: none;
}

/* COPIAR — track do drum (motion.div com translateY) */
.giveaway-drum__track {
  position: absolute;
  top: 0; left: 0; right: 0;
  will-change: transform;
}

/* COPIAR — blur no track durante fase rapida */
.giveaway-drum__track--blurred {
  filter: blur(2px);
  transition: filter 1s ease-out;
}
.giveaway-drum__track--clear {
  filter: blur(0);
}
```

---

## 1.13 Drum Frame Dourado (DS custom — highlight do vencedor)

Frame fixo centralizado no viewport que destaca o ticket vencedor quando o drum para.

```css
/* COPIAR EXATAMENTE — frame dourado central */
.giveaway-drum__frame {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: calc(100% - 16px);
  height: clamp(40px, 6vh, 56px);
  border: 3px solid #FFD700;
  border-radius: 10px;
  box-shadow:
    0 0 20px rgba(255,215,0,0.4),
    0 0 40px rgba(255,215,0,0.15),
    inset 0 0 10px rgba(255,215,0,0.1);
  pointer-events: none;
  z-index: 3;
}

/* COPIAR — frame bounce apos drum parar */
.giveaway-drum__frame--bounce {
  animation: frameBounce 0.3s ease-out;
}
@keyframes frameBounce {
  0% { transform: translate(-50%, -50%) scale(1); box-shadow: 0 0 20px rgba(255,215,0,0.4); }
  50% { transform: translate(-50%, -50%) scale(1.08); box-shadow: 0 0 40px rgba(255,215,0,0.8); }
  100% { transform: translate(-50%, -50%) scale(1); box-shadow: 0 0 20px rgba(255,215,0,0.4); }
}
```

---

## 1.14 Drum Item — Ticket Individual (DS P2 §10 tabelas adaptado)

Cada ticket visivel dentro do drum scroll.

```css
/* COPIAR EXATAMENTE — item normal do drum */
.giveaway-drum__item {
  height: clamp(40px, 6vh, 56px);
  display: flex; align-items: center;
  padding: 0 clamp(12px, 2vw, 20px);
  gap: clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.06);
}

/* COPIAR — numero do ticket */
.giveaway-drum__item-number {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  color: #D4A843;
  min-width: 50px;
}

/* COPIAR — nome do jogador */
.giveaway-drum__item-name {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(13px, 1.6vw, 17px);
  color: #A8A8A8;
}

/* COPIAR — item VENCEDOR (apos drum parar) */
.giveaway-drum__item--winner {
  background: rgba(212,168,67,0.15);
  border-left: 3px solid #FFD700;
}
.giveaway-drum__item--winner .giveaway-drum__item-number {
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}
.giveaway-drum__item--winner .giveaway-drum__item-name {
  color: #FFFFFF;
  font-weight: 700;
}
```

---

## 1.15 Winner Overlay — Versao GANHOU (DS P1 §7 + DS P3A §20.2)

Overlay fullscreen quando o jogador eh o vencedor. Celebracao maxima.

```css
/* COPIAR EXATAMENTE — overlay winner */
.giveaway-winner {
  position: fixed; inset: 0; z-index: 90;
  background: rgba(5,5,5,0.92);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

/* COPIAR — avatar com crown */
.giveaway-winner__avatar-container {
  position: relative;
}
.giveaway-winner__avatar {
  width: clamp(60px, 10vw, 80px);
  height: clamp(60px, 10vw, 80px);
  border-radius: 50%;
  border: 3px solid #FFD700;
  box-shadow: 0 0 20px rgba(255,215,0,0.4);
  object-fit: cover;
}
.giveaway-winner__crown {
  position: absolute;
  top: clamp(-12px, -2vw, -18px);
  right: clamp(-8px, -1.5vw, -12px);
  width: clamp(28px, 4vw, 40px);
  height: auto;
  transform: rotate(15deg);
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.5));
}

/* COPIAR — texto VOCE GANHOU */
.giveaway-winner__heading {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(18px, 3.5vw, 36px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  text-shadow:
    0 0 20px rgba(255,215,0,0.5),
    0 0 40px rgba(255,215,0,0.2);
  animation: glowPulseGold 1.5s ease-in-out infinite alternate;
}
@keyframes glowPulseGold {
  from { text-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.2); }
  to { text-shadow: 0 0 30px rgba(255,215,0,0.7), 0 0 60px rgba(255,215,0,0.3); }
}

/* COPIAR — nome do vencedor */
.giveaway-winner__name {
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: clamp(20px, 4vw, 40px);
  color: #FFFFFF;
  text-shadow: 0 0 20px rgba(0,230,118,0.5), 0 2px 8px rgba(0,0,0,0.5);
}

/* COPIAR — numero do ticket vencedor */
.giveaway-winner__ticket {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 500;
  font-size: clamp(12px, 1.5vw, 16px);
  color: #D4A843;
}

/* COPIAR — premio verde neon gigante */
.giveaway-winner__prize {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(24px, 5vw, 56px);
  color: #00E676;
  text-shadow:
    0 0 20px rgba(0,230,118,0.4),
    0 0 40px rgba(0,230,118,0.2);
  animation: glowPulseGreen 1.5s ease-in-out infinite alternate;
}
@keyframes glowPulseGreen {
  from { text-shadow: 0 0 20px rgba(0,230,118,0.4), 0 0 40px rgba(0,230,118,0.2); }
  to { text-shadow: 0 0 30px rgba(0,230,118,0.6), 0 0 60px rgba(0,230,118,0.3); }
}
```

---

## 1.16 Winner Overlay — Versao PERDEU (DS custom)

Overlay discreto quando outro jogador ganhou. Feedback rapido, nao punitivo.

```css
/* COPIAR EXATAMENTE — overlay perdedor (reutiliza .giveaway-winner como base) */
.giveaway-winner--loser {
  /* Herda layout de .giveaway-winner, sobrescreve visual */
}

/* COPIAR — titulo RESULTADO (nao VOCE GANHOU) */
.giveaway-winner--loser .giveaway-winner__heading {
  font-size: clamp(14px, 2vw, 22px);
  color: #A8A8A8;
  text-shadow: none;
  animation: none;
}

/* COPIAR — nome do vencedor (outro jogador) */
.giveaway-winner--loser .giveaway-winner__name {
  font-size: clamp(16px, 2.5vw, 24px);
  color: #D4A843;
  text-shadow: none;
}

/* COPIAR — premio (menor, sem glow intenso) */
.giveaway-winner--loser .giveaway-winner__prize {
  font-size: clamp(16px, 3vw, 28px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.2);
  animation: none;
}

/* COPIAR — mensagem consolacao */
.giveaway-winner__consolation {
  font-family: 'Inter', sans-serif;
  font-weight: 400;
  font-size: clamp(13px, 1.8vw, 18px);
  color: #666666;
  font-style: italic;
}

/* SEM confete, SEM crown, SEM coins cascade */
/* SEM glowPulse animations */
/* Tudo com transition tween, nao spring */
```

---

`[FIM DA PARTE 2 — Continua Parte 3: Historico + PF + Ticket + Tooltip + Glow System + Atmosferas + Timelines]`

`[ENTREGA: 4.ADENDO-DS-GIVEAWAYS-PARTE-2.md — 25/03/2026]`
