# PROMPT AI — GIVEAWAYS (#20)
## PARTE 1 DE 4 — Header Tecnico, Paleta, Tipografia, Tela Lista, Tela Modal Compra
### Prompt para gerar visual pixel-perfect em V0 / Bolt / Claude / ChatGPT
### Blackout Casino GTARP - 25/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Prompt AI do Padrao de Estudo Individual
- **Gold Standard consultado:** Prompt AI Coinflip #12 (5 partes, 2.825 linhas), Prompt AI Slot-Machine #2 (4 partes, 2.369 linhas)
- **Benchmark de linhas:** 1.500-2.300 linhas (4 partes de ~450L)
- **Pesquisa X0:** 5 buscas (flip countdown CSS rotateX perspective, Framer Motion v12 useAnimate inertia, glassmorphism dark theme backdrop-filter, confetti burst animation keyframes, progress bar React neon glow)
- **Descobertas que impactam este documento:**
  (1) Flip clock: rotateX(-180deg) + perspective 400px + backface-visibility hidden — padrão para countdown flip cards
  (2) Motion v12 inertia: type "inertia" com power + timeConstant para deceleration do drum scroll
  (3) Dark glassmorphism: rgba(255,255,255,0.05) + blur 12px + border rgba(255,255,255,0.1) — sweet spot dark mode
  (4) Confetti burst: @keyframes com CSS custom properties --x --y para direcao, 100 particulas burst
  (5) Progress bar inline: width percentage via style={{width: `${pct}%`}}, transition width 0.5s ease-in-out
- **Telas previstas:**
  1. Lista de Sorteios (grid cards, countdown flip, progress bar, historico mini)
  2. Modal Compra Tickets (input quantidade, subtotal, saldo, confirmar)
  3. Drum Reveal Overlay (scroll vertical, desaceleracao, highlight winner)
  4. Winner Overlay (confete, nome grande, premio verde, crown)
  5. Historico Completo (tabela paginada, badges, link PF)
  6. Provably Fair Modal (seeds, hash, instrucoes verificacao)

---

# HEADER TECNICO (Colar no inicio do prompt para qualquer IA)

```
RESTRICOES TECNICAS:
- React 19 + TypeScript + Framer Motion 11 (Motion v12) + Next.js 16
- Container: div com position absolute, inset 0, z-index 60, borderRadius inherit, overflow hidden
- SOMENTE dark mode — fundo NUNCA branco, NUNCA cinza claro
- CSS inline via style={{}} — NAO usar classes Tailwind para componentes de jogo
- Lista: card principal grande (sorteio ativo) + cards mini (proximos) em grid
- Countdown: flip cards com rotateX + perspective 400px + backface-visibility hidden
- Drum reveal: coluna vertical, motion.div translateY com deceleration easeOutCubic
- Confete: CSS @keyframes burst com custom properties --x --y, 40 particulas douradas
- Progress bar: div interna com width percentage + transition 0.5s + glow verde neon
- DOM para: cards, modais, overlays, tabelas, countdown — NAO Canvas (exceto confete opcional)
- Fontes: Cinzel (titulos), Inter (body), JetBrains Mono (valores GCoin, tickets, seeds PF)
- Responsividade: TUDO com clamp() — nunca px fixo para fontes/padding/sizes
- Mobile: LANDSCAPE natural NUI (1280x720). Desktop: mesmo layout responsivo
- Touch targets: minimo 44px em TODOS elementos interativos
- Bilingue: prop lang="br"|"en", todos textos via objeto { br: "...", en: "..." }
- Sons: useSoundManager() hook — 12 sons mapeados por evento
- FiveM NUI: Chromium CEF (suporta CSS Grid, AnimatePresence, backdrop-filter, clamp)
- Icones visuais: PNGs dourados (som, ticket, trophy, clock, verificar)
- Lucide icons: SOMENTE para botao fechar [X] de modais
- Provably Fair: HMAC-SHA256 server-side, hash pre-commit ANTES do draw, seeds revelados APOS
- Resultado server-side — client NUNCA calcula resultado, APENAS anima drum com resultado recebido
- Particulas: reutilizar GoldParticles.tsx existente (burst mode, max 40 particulas)
- ZERO dependencias novas (Motion ja no projeto)
- Sorteios: sistema de tickets (comprar/gratis) com countdown ate draw automatico
```

---

# PALETA BASE — BLACKOUT CASINO (OBRIGATORIA)

```
BACKGROUNDS:
  #050505   — fundo absoluto (atras de tudo)
  #0A0A0A   — fundo do jogo (background-color principal)
  #0F0F0F   — fundo do painel
  #141414   — card, container
  #1A1A1A   — sub-container, input inativo
  #222222   — hover state, item selecionado
  #2A2A2A   — tooltip, dropdown

OURO METALICO (bordas, titulos, moldura, ornamentos):
  #462523   — sombra profunda do metal
  #8B6914   — ouro escuro
  #CB9B51   — ouro medio
  #D4A843   — ouro principal (COR MAIS USADA — texto, borda, UI)
  #F6E27A   — ouro claro (highlight)
  #F6F2C0   — ponto de brilho maximo
  #FFD700   — ouro puro (hover, accent, CTA)

VERDE ESMERALDA (win, ativo, CTA, saldo, acertos):
  #00E676   — verde brilhante (accent principal)
  #00C853   — verde forte (botao primario CTA)
  #004D25   — verde escuro (base botao)

VERMELHO (perda, erro, urgencia):
  #FF1744   — vermelho vibrante
  #FF3B3B   — vermelho de erro

AZUL (ticket gratis, info):
  #2196F3   — azul badge GRATIS

TEXTO:
  #FFFFFF   — texto principal
  #A8A8A8   — texto secundario
  #666666   — texto inativo/placeholder
```

# PALETA ESPECIFICA — GIVEAWAYS

```
COUNTDOWN FLIP CARDS:
  fundo card:     #141414 (topo) / #1A1A1A (base, separacao horizontal)
  borda:          rgba(212,168,67,0.3)
  texto digito:   #D4A843 (normal) / #FF1744 (urgente, <60s)
  label (d/h/m/s): #666666

CARD SORTEIO ATIVO:
  background:     linear-gradient(135deg, #141414 0%, #1A1A1A 100%)
  borda:          1.5px solid rgba(212,168,67,0.25)
  feixo verde:    box-shadow 0 2px 0 0 rgba(0,230,118,0.4) (base do card)
  shine sweep:    linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 50%, transparent 60%)

PROGRESS BAR TICKETS:
  track:          #1A1A1A (fundo)
  fill:           linear-gradient(90deg, #004D25 0%, #00E676 100%)
  glow:           box-shadow 0 0 8px rgba(0,230,118,0.3)
  texto:          #A8A8A8 (ex: "234/500 tickets")

DRUM REVEAL:
  viewport bg:    rgba(10,10,10,0.95)
  item normal:    #A8A8A8 texto, #141414 bg
  item highlight: #FFD700 texto, rgba(212,168,67,0.15) bg, border 2px #D4A843
  frame dourado:  border 3px solid #FFD700, box-shadow 0 0 20px rgba(255,215,0,0.4)

WINNER OVERLAY:
  nome vencedor:  #FFFFFF com text-shadow 0 0 20px rgba(0,230,118,0.5)
  premio:         #00E676 font-mono bold
  confete:        mix #D4A843, #FFD700, #00E676
  derrota:        opacity 0.5, texto #666666

BADGE STATUS:
  UPCOMING:       bg #2A2A2A, texto #666666 (cinza inativo)
  ACTIVE:         bg rgba(0,230,118,0.15), texto #00E676, pulse @keyframes
  URGENTE:        bg rgba(255,23,68,0.15), texto #FF1744, pulse rapido
  DRAWING:        bg rgba(255,215,0,0.15), texto #FFD700, fade in/out
  COMPLETED:      bg rgba(212,168,67,0.1), texto #D4A843, estatico
  CANCELLED:      bg rgba(255,59,59,0.1), texto #FF3B3B, estatico

TICKET:
  pago:           bg #141414, borda rgba(212,168,67,0.2), numero #D4A843
  gratis:         bg rgba(33,150,243,0.1), borda rgba(33,150,243,0.3), badge #2196F3
  winner:         bg rgba(0,230,118,0.1), borda #00E676, icone trophy
```

---

# TIPOGRAFIA

```
TITULOS (nome do sorteio, "GIVEAWAYS", "RESULTADO"):
  font-family: 'Cinzel', 'Times New Roman', serif
  font-weight: 700
  text-transform: uppercase
  letter-spacing: 0.05em
  font-size: clamp(16px, 2.5vw, 28px)
  color: #D4A843

CORPO (descricoes, instrucoes, labels):
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
  font-weight: 400
  font-size: clamp(12px, 1.5vw, 16px)
  line-height: 1.5
  color: #FFFFFF (principal) ou #A8A8A8 (secundario)

VALORES (GCoin, precos, contadores, tickets, percentuais):
  font-family: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace
  font-weight: 600
  font-size: clamp(14px, 2vw, 24px)
  color: #00E676 (premios, saldo)
  color: #D4A843 (precos, tickets)
  text-shadow: 0 0 10px rgba(0,230,118,0.3) (valores verdes)

COUNTDOWN DIGITOS:
  font-family: 'JetBrains Mono', monospace
  font-weight: 700
  font-size: clamp(20px, 4vw, 48px)
  color: #D4A843 (normal) / #FF1744 (urgente)
  text-shadow: 0 2px 4px rgba(0,0,0,0.5)

SEEDS PF (server_seed, client_seed, hash):
  font-family: 'JetBrains Mono', monospace
  font-weight: 400
  font-size: clamp(10px, 1.2vw, 13px)
  color: #A8A8A8
  background: rgba(0,0,0,0.4)
  padding: 8px 12px
  borderRadius: 8px
  wordBreak: break-all
```

---

# TELA 1: LISTA DE SORTEIOS (Estado: navegando sorteios disponiveis)

## Layout ASCII

```
+================================================================+
|  [<] VOLTAR                          SORTEIOS        [Som][?]  |
+================================================================+
|                                                                 |
|  +----------------------------------------------------------+  |
|  |  BADGE: ATIVO                    [countdown flip cards]   |  |
|  |  "MEGA GIVEAWAY DE NATAL"        02d : 14h : 32m : 08s   |  |
|  |  Premio: G$ 50.000                                        |  |
|  |  Ticket: G$ 100                                           |  |
|  |  [============================-------] 234/500 (46.8%)    |  |
|  |  Seus tickets: 5/10  |  Chance: 2.1%                     |  |
|  |  [ COMPRAR TICKET ]   [ TICKET GRATIS (14h32m) ]          |  |
|  +----------------------------------------------------------+  |
|                                                                 |
|  PROXIMOS SORTEIOS                                              |
|  +------------------+  +------------------+  +----------------+ |
|  | FRIDAY FRENZY    |  | VIP EXCLUSIVE    |  | WEEKLY LUCKY   | |
|  | G$ 25.000        |  | G$ 100.000       |  | G$ 10.000      | |
|  | 3d:08h restantes |  | 5d:02h restantes |  | 1d:22h rest.   | |
|  +------------------+  +------------------+  +----------------+ |
|                                                                 |
|  ULTIMOS RESULTADOS                                             |
|  +----------------------------------------------------------+  |
|  | "HALLOWEEN SPOOKY"  |  PlayerXPTO  |  G$30.000  |  PF    |  |
|  | "DAILY DRAW #42"    |  JogadorABC  |  G$5.000   |  PF    |  |
|  | "BIG FRIDAY"        |  Nick123     |  G$75.000  |  PF    |  |
|  +----------------------------------------------------------+  |
+================================================================+
```

## Especificacoes Tela 1

**Container principal:**
```css
{
  position: 'absolute', inset: 0, zIndex: 60,
  background: '#0A0A0A',
  borderRadius: 'inherit',
  overflow: 'hidden',
  display: 'flex', flexDirection: 'column',
  padding: 'clamp(12px, 2vw, 24px)',
  gap: 'clamp(12px, 2vw, 20px)'
}
```

**Header (VOLTAR + titulo + icones):**
```css
{
  display: 'flex', alignItems: 'center', justifyContent: 'space-between',
  padding: '0 0 clamp(8px, 1vw, 16px) 0',
  borderBottom: '1px solid rgba(212,168,67,0.15)'
}
/* Botao VOLTAR */
{
  background: 'transparent', border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '8px', padding: 'clamp(6px, 1vw, 10px) clamp(10px, 1.5vw, 16px)',
  color: '#D4A843', fontFamily: "'Inter', sans-serif",
  fontSize: 'clamp(11px, 1.2vw, 14px)', cursor: 'pointer',
  transition: 'all 0.2s ease'
}
/* Titulo SORTEIOS */
{
  fontFamily: "'Cinzel', serif", fontWeight: 700,
  fontSize: 'clamp(16px, 2.5vw, 28px)', color: '#D4A843',
  textTransform: 'uppercase', letterSpacing: '0.05em'
}
```

**Card sorteio ativo (card principal grande):**
```css
{
  background: 'linear-gradient(135deg, #141414 0%, #1A1A1A 100%)',
  border: '1.5px solid rgba(212,168,67,0.25)',
  borderRadius: 'clamp(10px, 1.5vw, 16px)',
  padding: 'clamp(16px, 2.5vw, 28px)',
  boxShadow: '0 2px 0 0 rgba(0,230,118,0.4), 0 4px 20px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.03)',
  position: 'relative', overflow: 'hidden'
}
/* Shine sweep pseudo-element via background */
{
  backgroundImage: 'linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 50%, transparent 60%)',
  backgroundSize: '200% 100%',
  animation: 'shineSweep 4s infinite'
}
```

**Countdown flip cards (4 digitos: d:h:m:s):**
```css
/* Container flip card individual */
{
  display: 'inline-flex', flexDirection: 'column', alignItems: 'center',
  gap: '4px'
}
/* Digito flip card */
{
  width: 'clamp(36px, 5vw, 56px)', height: 'clamp(44px, 6vw, 68px)',
  background: '#141414',
  border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '8px',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
  fontSize: 'clamp(20px, 4vw, 48px)',
  color: '#D4A843',
  textShadow: '0 2px 4px rgba(0,0,0,0.5)',
  position: 'relative',
  perspective: '400px',
  overflow: 'hidden'
}
/* Separacao horizontal meio do card (simula flip split) */
{
  position: 'absolute', top: '50%', left: 0, right: 0,
  height: '1px',
  background: 'rgba(0,0,0,0.4)'
}
/* Label abaixo (D, H, M, S) */
{
  fontFamily: "'Inter', sans-serif", fontSize: '10px',
  color: '#666666', textTransform: 'uppercase',
  letterSpacing: '0.1em'
}
/* Estado URGENTE (<60s): cor muda */
{
  color: '#FF1744',
  textShadow: '0 0 10px rgba(255,23,68,0.4)',
  animation: 'pulseUrgent 0.5s ease-in-out infinite alternate'
}
```

**Progress bar tickets:**
```css
/* Track */
{
  width: '100%', height: 'clamp(6px, 1vw, 10px)',
  background: '#1A1A1A',
  borderRadius: '999px',
  overflow: 'hidden'
}
/* Fill */
{
  height: '100%',
  background: 'linear-gradient(90deg, #004D25 0%, #00E676 100%)',
  borderRadius: '999px',
  transition: 'width 0.5s ease-in-out',
  boxShadow: '0 0 8px rgba(0,230,118,0.3)'
}
/* Texto "234/500 (46.8%)" */
{
  fontFamily: "'JetBrains Mono', monospace", fontSize: 'clamp(10px, 1.2vw, 13px)',
  color: '#A8A8A8', marginTop: '4px'
}
```

**Botao COMPRAR TICKET (CTA verde):**
```css
{
  background: 'linear-gradient(180deg, #00C853 0%, #004D25 100%)',
  border: '1px solid rgba(0,230,118,0.3)',
  borderRadius: '10px',
  padding: 'clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px)',
  color: '#FFFFFF', fontFamily: "'Inter', sans-serif", fontWeight: 600,
  fontSize: 'clamp(12px, 1.5vw, 16px)',
  cursor: 'pointer', minHeight: '44px',
  boxShadow: '0 0 12px rgba(0,230,118,0.2), 0 4px 12px rgba(0,0,0,0.3)',
  transition: 'all 0.2s ease'
}
```

**Botao TICKET GRATIS:**
```css
{
  background: 'rgba(33,150,243,0.1)',
  border: '1px solid rgba(33,150,243,0.3)',
  borderRadius: '10px',
  padding: 'clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px)',
  color: '#2196F3', fontFamily: "'Inter', sans-serif", fontWeight: 500,
  fontSize: 'clamp(12px, 1.5vw, 16px)',
  cursor: 'pointer', minHeight: '44px',
  transition: 'all 0.2s ease'
}
```

**Cards mini proximos sorteios (grid):**
```css
/* Grid container */
{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fill, minmax(clamp(160px, 20vw, 240px), 1fr))',
  gap: 'clamp(8px, 1.5vw, 16px)'
}
/* Card mini individual */
{
  background: '#141414',
  border: '1px solid rgba(212,168,67,0.15)',
  borderRadius: 'clamp(8px, 1vw, 12px)',
  padding: 'clamp(10px, 1.5vw, 16px)',
  cursor: 'pointer',
  transition: 'all 0.2s ease'
}
/* Card mini hover */
{
  background: '#1A1A1A',
  borderColor: 'rgba(212,168,67,0.3)',
  transform: 'translateY(-2px)',
  boxShadow: '0 4px 16px rgba(0,0,0,0.3)'
}
```

**Secao historico mini (ultimos 3 draws):**
```css
/* Linha historico */
{
  display: 'flex', alignItems: 'center', justifyContent: 'space-between',
  padding: 'clamp(8px, 1vw, 12px) 0',
  borderBottom: '1px solid rgba(212,168,67,0.08)',
  fontSize: 'clamp(11px, 1.3vw, 14px)'
}
/* Botao PF (outline dourado) */
{
  background: 'transparent', border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '6px', padding: '4px 8px',
  color: '#D4A843', fontSize: '11px', cursor: 'pointer',
  transition: 'all 0.2s ease'
}
```

## Framer Motion — Tela 1 Lista

```
ENTRADA DA TELA:
  container: initial={{ opacity: 0 }}, animate={{ opacity: 1 }}, transition={{ duration: 0.3 }}
  card ativo: initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }}, transition={{ delay: 0.1, duration: 0.4 }}
  cards mini: stagger 0.05s entre cada, initial={{ opacity: 0, y: 15 }}, animate={{ opacity: 1, y: 0 }}
  linhas historico: stagger 0.03s, initial={{ opacity: 0, x: -10 }}, animate={{ opacity: 1, x: 0 }}

INTERACOES:
  botao VOLTAR: whileHover={{ scale: 1.05, borderColor: '#FFD700' }}, whileTap={{ scale: 0.95 }}
  botao COMPRAR: whileHover={{ scale: 1.03, boxShadow: '0 0 20px rgba(0,230,118,0.4)' }}, whileTap={{ scale: 0.97 }}
  botao GRATIS: whileHover={{ scale: 1.03, borderColor: '#2196F3' }}, whileTap={{ scale: 0.97 }}
  card mini: whileHover={{ y: -4, borderColor: 'rgba(212,168,67,0.4)' }}
  botao PF: whileHover={{ borderColor: '#FFD700', color: '#FFD700' }}

TRANSICOES INTERNAS:
  countdown flip: AnimateNumber ou @keyframes rotateX para cada digito ao mudar
  progress bar: transition width 0.5s ease-in-out (CSS, nao Motion)
  badge status: @keyframes pulse (ACTIVE: 2s infinite, URGENTE: 0.5s infinite)
  shine sweep: @keyframes shineSweep background-position -200% → 200%, 4s infinite

SAIDA:
  AnimatePresence: exit={{ opacity: 0, y: -10 }}, transition={{ duration: 0.2 }}
```

## Estados Visuais — Tela 1 Lista

```
ESTADO 1 — IDLE (sorteio ativo, countdown >60min):
  - Card ativo: borda dourada 0.25 opacity, countdown dourado calmo
  - Badge: ATIVO verde pulsante lento (2s)
  - Progress bar: fill estatico, sem glow extra
  - Shine sweep: animando suavemente

ESTADO 2 — URGENTE (countdown <60min):
  - Card ativo: borda opacity sobe para 0.5, box-shadow glow vermelho sutil
  - Badge: URGENTE vermelho, pulse rapido 0.5s
  - Countdown: digitos mudam para #FF1744, text-shadow vermelho
  - Sons: heartbeat loop 800ms comeca
  - Botao COMPRAR: glow verde mais intenso (chamar atencao pra comprar antes de acabar)

ESTADO 3 — DRAWING (countdown zerou, draw em andamento):
  - Badge: SORTEANDO amarelo/dourado, fade in/out 1s
  - Botao COMPRAR: desabilitado, opacity 0.5, cursor not-allowed
  - Progress bar: fill 100%, pulse glow
  - Transicao automatica para Tela 3 (Drum Reveal) apos 1s

ESTADO 4 — VAZIO (nenhum sorteio ativo):
  - Card principal: ilustracao de ticket tracejado + texto "Nenhum sorteio ativo no momento"
  - Grid proximos: vazio ou "Em breve..."
  - Cores desaturadas: todos textos #666666
```

---

# TELA 2: MODAL COMPRA TICKETS (Estado: comprando tickets)

## Layout ASCII

```
+====================================================+
|            COMPRAR TICKETS                    [X]   |
+====================================================+
|                                                      |
|  "MEGA GIVEAWAY DE NATAL"                            |
|  Premio: G$ 50.000                                   |
|                                                      |
|  Quantidade:                                         |
|  [ - ]  [   5   ]  [ + ]                            |
|                                                      |
|  Preco unitario:    G$ 100                           |
|  Subtotal:          G$ 500                           |
|  Seus tickets:      5/10 (max 10)                    |
|  Saldo disponivel:  G$ 12.450                        |
|                                                      |
|  +------------------------------------------------+  |
|  |  [ CONFIRMAR COMPRA — G$ 500 ]                 |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  |  [ CANCELAR ]                                  |  |
|  +------------------------------------------------+  |
+====================================================+
```

## Especificacoes Tela 2

**Modal glassmorphism (overlay + conteudo):**
```css
/* Backdrop overlay */
{
  position: 'fixed', inset: 0, zIndex: 70,
  background: 'rgba(0,0,0,0.6)',
  backdropFilter: 'blur(5px)',
  WebkitBackdropFilter: 'blur(5px)',
  display: 'flex', alignItems: 'center', justifyContent: 'center'
}
/* Modal conteudo */
{
  background: 'rgba(10,10,10,0.85)',
  backdropFilter: 'blur(12px)',
  WebkitBackdropFilter: 'blur(12px)',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: 'clamp(12px, 2vw, 20px)',
  padding: 'clamp(20px, 3vw, 32px)',
  width: 'clamp(280px, 45vw, 420px)',
  boxShadow: '0 20px 60px rgba(0,0,0,0.5), 0 0 1px rgba(212,168,67,0.3)',
  display: 'flex', flexDirection: 'column',
  gap: 'clamp(12px, 2vw, 20px)'
}
```

**Input quantidade (com botoes -/+):**
```css
/* Container input */
{
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  gap: 'clamp(8px, 1.5vw, 16px)'
}
/* Botao -/+ */
{
  width: 'clamp(36px, 5vw, 48px)', height: 'clamp(36px, 5vw, 48px)',
  background: '#1A1A1A', border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '10px', color: '#D4A843',
  fontFamily: "'JetBrains Mono', monospace", fontSize: 'clamp(16px, 2vw, 24px)',
  fontWeight: 700, cursor: 'pointer',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  transition: 'all 0.15s ease', minHeight: '44px', minWidth: '44px'
}
/* Botao -/+ hover */
{
  background: '#222222', borderColor: '#FFD700'
}
/* Display quantidade */
{
  width: 'clamp(60px, 10vw, 100px)', height: 'clamp(44px, 6vw, 56px)',
  background: '#141414', border: '1.5px solid rgba(212,168,67,0.4)',
  borderRadius: '12px',
  fontFamily: "'JetBrains Mono', monospace", fontWeight: 700,
  fontSize: 'clamp(20px, 3vw, 32px)', color: '#FFFFFF',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  textAlign: 'center'
}
```

**Linhas de informacao (preco, subtotal, saldo):**
```css
/* Linha info */
{
  display: 'flex', justifyContent: 'space-between', alignItems: 'center',
  padding: 'clamp(4px, 0.5vw, 8px) 0'
}
/* Label (preco unitario, subtotal...) */
{
  fontFamily: "'Inter', sans-serif", fontSize: 'clamp(12px, 1.4vw, 15px)',
  color: '#A8A8A8'
}
/* Valor GCoin */
{
  fontFamily: "'JetBrains Mono', monospace", fontWeight: 600,
  fontSize: 'clamp(14px, 1.8vw, 18px)',
  color: '#00E676',
  textShadow: '0 0 8px rgba(0,230,118,0.2)'
}
/* Subtotal (destaque maior) */
{
  fontSize: 'clamp(16px, 2.2vw, 22px)', fontWeight: 700,
  color: '#FFD700',
  textShadow: '0 0 10px rgba(255,215,0,0.3)'
}
```

**Botao CONFIRMAR COMPRA:**
```css
{
  background: 'linear-gradient(180deg, #00C853 0%, #004D25 100%)',
  border: '1px solid rgba(0,230,118,0.4)',
  borderRadius: '12px',
  padding: 'clamp(12px, 2vw, 16px)',
  color: '#FFFFFF', fontFamily: "'Inter', sans-serif", fontWeight: 700,
  fontSize: 'clamp(14px, 1.8vw, 18px)',
  cursor: 'pointer', textAlign: 'center', minHeight: '48px',
  boxShadow: '0 0 16px rgba(0,230,118,0.2), 0 6px 20px rgba(0,0,0,0.3)',
  transition: 'all 0.2s ease',
  textTransform: 'uppercase', letterSpacing: '0.03em'
}
/* Botao desabilitado (saldo insuficiente ou max tickets) */
{
  opacity: 0.4, cursor: 'not-allowed',
  background: '#1A1A1A', border: '1px solid #333',
  boxShadow: 'none'
}
```

**Botao CANCELAR:**
```css
{
  background: 'transparent',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: '10px',
  padding: 'clamp(10px, 1.5vw, 14px)',
  color: '#A8A8A8', fontFamily: "'Inter', sans-serif",
  fontSize: 'clamp(12px, 1.5vw, 15px)',
  cursor: 'pointer', textAlign: 'center',
  transition: 'all 0.2s ease'
}
```

**Botao fechar [X] (Lucide):**
```css
{
  position: 'absolute', top: 'clamp(12px, 1.5vw, 16px)', right: 'clamp(12px, 1.5vw, 16px)',
  background: 'transparent', border: 'none',
  color: '#666666', cursor: 'pointer',
  width: '24px', height: '24px',
  transition: 'color 0.2s ease'
}
/* Hover */
{ color: '#FF3B3B' }
```

## Framer Motion — Tela 2 Modal Compra

```
ENTRADA:
  backdrop: initial={{ opacity: 0 }}, animate={{ opacity: 1 }}, transition={{ duration: 0.2 }}
  modal: initial={{ opacity: 0, scale: 0.95, y: 20 }}, animate={{ opacity: 1, scale: 1, y: 0 }}, transition={{ type: 'spring', stiffness: 300, damping: 25 }}

INTERACOES:
  botao -/+: whileHover={{ scale: 1.1, borderColor: '#FFD700' }}, whileTap={{ scale: 0.9 }}
  display quantidade: ao mudar valor, animate={{ scale: [1, 1.1, 1] }}, transition={{ duration: 0.15 }}
  subtotal: ao mudar, animate={{ color: ['#FFD700', '#FFFFFF', '#FFD700'] }}, transition={{ duration: 0.3 }}
  botao CONFIRMAR: whileHover={{ scale: 1.02, boxShadow: '0 0 25px rgba(0,230,118,0.4)' }}, whileTap={{ scale: 0.98 }}
  botao CANCELAR: whileHover={{ borderColor: 'rgba(212,168,67,0.4)', color: '#D4A843' }}
  botao [X]: whileHover={{ color: '#FF3B3B', scale: 1.1 }}, whileTap={{ scale: 0.9 }}

SAIDA:
  AnimatePresence: modal exit={{ opacity: 0, scale: 0.95, y: 10 }}, transition={{ duration: 0.15 }}
  backdrop exit={{ opacity: 0 }}, transition={{ duration: 0.2 }}
```

## Estados Visuais — Tela 2 Modal Compra

```
ESTADO 1 — IDLE (modal aberto, quantidade default 1):
  - Input mostra "1", subtotal = preco unitario
  - Saldo em verde, botao CONFIRMAR habilitado
  - Botao - desabilitado (quantidade minima)

ESTADO 2 — AJUSTANDO (jogador mudando quantidade):
  - Display quantidade pulsa ao mudar (scale 1→1.1→1)
  - Subtotal anima ao atualizar (flash dourado)
  - Se quantidade > tickets disponiveis do jogador: auto-clamp

ESTADO 3 — SALDO INSUFICIENTE:
  - Saldo muda para vermelho #FF3B3B
  - Botao CONFIRMAR: desabilitado (opacity 0.4, cursor not-allowed)
  - Tooltip: "Saldo insuficiente para X tickets"

ESTADO 4 — LIMITE ATINGIDO:
  - Texto "Seus tickets: 10/10 (MAX)" em vermelho
  - Botao +: desabilitado
  - Botao CONFIRMAR: desabilitado
  - Tooltip: "Limite de tickets atingido"

ESTADO 5 — COMPRANDO (loading):
  - Botao CONFIRMAR: spinner girando, texto "Processando..."
  - Todos inputs desabilitados
  - Apos sucesso: toast verde "5 tickets comprados!" + som coin drop + modal fecha
  - Apos erro: toast vermelho + shake no modal (translateX [-5, 5, -5, 5, 0])
```

---

`[FIM DA PARTE 1 — Continua Parte 2: Tela Drum Reveal + Tela Winner Overlay]`

`[ENTREGA: 3.PROMPT-AI-GIVEAWAYS-PARTE-1.md — 25/03/2026]`
