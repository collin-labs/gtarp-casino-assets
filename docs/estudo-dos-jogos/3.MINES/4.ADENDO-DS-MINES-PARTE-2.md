# ADENDO DESIGN SYSTEM — MINES / CAMPO MINADO (#3)
## PARTE 2 DE 4 — Input Aposta, Atalhos, Seletor Minas, Multiplicador, Botoes
### 19/03/2026

---

## 1.10 Input de Aposta (DS P2 par.9 bet input)

```css
/* COPIAR EXATAMENTE — input de valor da aposta */
.mines-bet__input {
  width: 100%;
  background: rgba(10, 18, 8, 0.95);
  border: 1px solid rgba(0, 230, 118, 0.3);
  border-radius: 8px;
  color: #00E676;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 22px);
  padding: clamp(8px, 1vw, 12px);
  text-align: center;
  outline: none;
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
  -moz-appearance: textfield;
}

.mines-bet__input::-webkit-outer-spin-button,
.mines-bet__input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  margin: 0;
}

/* COPIAR — focus state */
.mines-bet__input:focus {
  border-color: rgba(0, 230, 118, 0.6);
  box-shadow: 0 0 8px rgba(0,230,118,0.2), 0 0 20px rgba(0,230,118,0.08);
}

/* COPIAR — disabled state (durante rodada ativa) */
.mines-bet__input--disabled {
  background: rgba(10, 18, 8, 0.6);
  border-color: rgba(0, 230, 118, 0.15);
  color: rgba(0, 230, 118, 0.4);
  cursor: not-allowed;
  pointer-events: none;
}

/* COPIAR — erro (saldo insuficiente) */
.mines-bet__input--error {
  border-color: rgba(235, 75, 75, 0.5);
  box-shadow: 0 0 8px rgba(235,75,75,0.2);
  color: #EB4B4B;
  animation: minesInputShake 0.3s ease;
}

@keyframes minesInputShake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4px); }
  75% { transform: translateX(4px); }
}
```

---

## 1.11 Atalhos de Aposta — MIN / x2 / /2 / MAX (DS P2 par.8.4 botao terciario)

```css
/* COPIAR EXATAMENTE — container dos 4 atalhos */
.mines-bet__shortcuts {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: clamp(3px, 0.4vw, 6px);
}

/* COPIAR — botao atalho individual */
.mines-bet__shortcut {
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid rgba(212, 168, 67, 0.2);
  border-radius: 6px;
  color: rgba(212, 168, 67, 0.7);
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(8px, 1vw, 11px);
  font-weight: 600;
  padding: clamp(4px, 0.5vw, 6px);
  text-align: center;
  cursor: pointer;
  transition: border-color 0.15s ease, color 0.15s ease, background 0.15s ease;
  min-height: clamp(28px, 3.5vw, 36px);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — hover */
.mines-bet__shortcut:hover {
  border-color: rgba(212, 168, 67, 0.5);
  color: #D4A843;
  background: rgba(212, 168, 67, 0.06);
}

/* COPIAR — active (pressionado) */
.mines-bet__shortcut:active {
  background: rgba(212, 168, 67, 0.12);
  transform: scale(0.95);
}

/* COPIAR — disabled (durante rodada) */
.mines-bet__shortcut--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  pointer-events: none;
}
```

---

## 1.12 Seletor de Minas — Botoes [-] [valor] [+] (DS P2 par.9 numerico)

```css
/* COPIAR EXATAMENTE — container seletor */
.mines-selector {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
}

/* COPIAR — botao [-] e [+] */
.mines-selector__btn {
  width: clamp(30px, 4vw, 38px);
  height: clamp(30px, 4vw, 38px);
  border-radius: 8px;
  background: rgba(0, 0, 0, 0.5);
  border: 1px solid rgba(212, 168, 67, 0.25);
  color: #D4A843;
  font-size: clamp(14px, 1.8vw, 20px);
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: border-color 0.15s ease, transform 0.1s ease, background 0.15s ease;
}

.mines-selector__btn:hover {
  border-color: rgba(212, 168, 67, 0.6);
  background: rgba(212, 168, 67, 0.08);
  transform: scale(1.05);
}

.mines-selector__btn:active {
  transform: scale(0.95);
  background: rgba(212, 168, 67, 0.15);
}

.mines-selector__btn--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  pointer-events: none;
}

/* COPIAR — display valor central */
.mines-selector__value {
  flex: 1;
  text-align: center;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 800;
  font-size: clamp(18px, 2.5vw, 28px);
  transition: color 0.3s ease, text-shadow 0.3s ease;
}

/* COPIAR — cor dinamica por faixa de risco */
/* Baixo risco: 1-5 minas */
.mines-selector__value--low {
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}

/* Medio risco: 6-15 minas */
.mines-selector__value--medium {
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}

/* Alto risco: 16-24 minas */
.mines-selector__value--high {
  color: #EB4B4B;
  text-shadow: 0 0 8px rgba(235,75,75,0.4);
}

/* COPIAR — info abaixo do seletor (gemas e probabilidade) */
.mines-selector__info {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1.1vw, 12px);
  color: rgba(255, 255, 255, 0.5);
  text-align: center;
  margin-top: clamp(2px, 0.3vw, 4px);
}
```

---

## 1.13 Multiplicador Display (DS P1 par.3 tipografia destaque)

```css
/* COPIAR EXATAMENTE — container multiplicador */
.mines-multiplier {
  text-align: center;
  padding: clamp(8px, 1vw, 14px) 0;
}

/* COPIAR — multiplicador idle (antes de jogar) */
.mines-multiplier__value--idle {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 900;
  font-size: clamp(18px, 2.5vw, 30px);
  color: rgba(212, 168, 67, 0.4);
  text-shadow: none;
  transition: all 0.3s ease;
}

/* COPIAR — multiplicador ativo (durante rodada) */
.mines-multiplier__value--active {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 900;
  font-size: clamp(22px, 3.5vw, 40px);
  color: #FFD700;
  text-shadow:
    0 0 15px rgba(255,215,0,0.6),
    0 0 30px rgba(255,215,0,0.2),
    0 2px 6px rgba(0,0,0,1);
  transition: all 0.3s ease;
}

/* COPIAR — preview "Proximo: X.XXx" */
.mines-multiplier__next {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(10px, 1.3vw, 14px);
  color: rgba(0, 230, 118, 0.6);
  margin-top: clamp(4px, 0.5vw, 8px);
}

/* COPIAR — profit acumulado "+X GC" */
.mines-multiplier__profit {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(12px, 1.5vw, 16px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  margin-top: clamp(2px, 0.3vw, 4px);
}
```

**Framer Motion pulse a cada gema:**
```
animate={{ scale: [1, 1.15, 1] }}
transition={{ duration: 0.3, ease: 'easeOut' }}
```

---

## 1.14 Botao BET / APOSTAR (DS P2 par.8.1 botao primario verde)

```css
/* COPIAR EXATAMENTE — botao principal de aposta */
.mines-btn--bet {
  width: 100%;
  padding: clamp(12px, 1.5vw, 18px);
  border-radius: 10px;
  background: linear-gradient(180deg, #00C853 0%, #00E676 30%, #004D25 100%);
  border: 1.5px solid rgba(0, 230, 118, 0.4);
  color: #050505;
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-weight: 800;
  font-size: clamp(12px, 1.5vw, 17px);
  text-transform: uppercase;
  letter-spacing: clamp(1.5px, 0.3vw, 3px);
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 4px 12px rgba(0,200,83,0.3),
    inset 0 1px 0 rgba(255,255,255,0.15);
  transition: filter 0.15s ease, transform 0.1s ease;
}

/* COPIAR — bevel highlight (DS P2 par.8.1 ::before) */
.mines-btn--bet::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 50%;
  background: linear-gradient(180deg, rgba(255,255,255,0.12) 0%, transparent 100%);
  pointer-events: none;
  border-radius: 10px 10px 0 0;
}

/* COPIAR — hover */
.mines-btn--bet:hover {
  filter: brightness(1.15);
  transform: scale(1.03);
  box-shadow:
    0 6px 16px rgba(0,200,83,0.4),
    inset 0 1px 0 rgba(255,255,255,0.2);
}

/* COPIAR — active (pressionado) */
.mines-btn--bet:active {
  transform: scale(0.97);
  filter: brightness(0.95);
  box-shadow:
    0 2px 6px rgba(0,200,83,0.2),
    inset 0 2px 4px rgba(0,0,0,0.3);
}

/* COPIAR — disabled (saldo insuficiente ou rodada ativa) */
.mines-btn--bet--disabled {
  background: rgba(40, 40, 40, 0.6);
  border: 1px solid rgba(0, 230, 118, 0.15);
  color: rgba(0, 230, 118, 0.3);
  cursor: not-allowed;
  opacity: 0.4;
  pointer-events: none;
  box-shadow: none;
  filter: none;
}

.mines-btn--bet--disabled::before {
  display: none;
}
```

---

## 1.15 Botao CASH OUT / RETIRAR (DS P2 par.8.2 botao destaque dourado)

```css
/* COPIAR EXATAMENTE — botao cash out (quando ativo e pulsante) */
.mines-btn--cashout {
  width: 100%;
  padding: clamp(12px, 1.5vw, 18px);
  border-radius: 10px;
  background: linear-gradient(180deg, #CB9B51 0%, #D4A843 40%, #8B6914 100%);
  border: 1.5px solid rgba(255, 215, 0, 0.5);
  color: #050505;
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-weight: 800;
  font-size: clamp(12px, 1.5vw, 17px);
  text-transform: uppercase;
  letter-spacing: clamp(1.5px, 0.3vw, 3px);
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 4px 12px rgba(212,168,67,0.3),
    inset 0 1px 0 rgba(255,255,255,0.2);
  animation: minesCashOutGlow 1.5s ease-in-out infinite;
}

/* COPIAR — bevel highlight */
.mines-btn--cashout::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 50%;
  background: linear-gradient(180deg, rgba(255,255,255,0.15) 0%, transparent 100%);
  pointer-events: none;
  border-radius: 10px 10px 0 0;
}

@keyframes minesCashOutGlow {
  0%, 100% {
    border-color: rgba(255, 215, 0, 0.4);
    box-shadow: 0 0 8px rgba(255,215,0,0.2), 0 4px 12px rgba(212,168,67,0.2);
  }
  50% {
    border-color: rgba(255, 215, 0, 0.8);
    box-shadow: 0 0 18px rgba(255,215,0,0.4), 0 4px 16px rgba(212,168,67,0.4);
  }
}

/* COPIAR — hover */
.mines-btn--cashout:hover {
  filter: brightness(1.1);
  transform: scale(1.03);
}

/* COPIAR — active */
.mines-btn--cashout:active {
  transform: scale(0.97);
  filter: brightness(0.95);
}

/* COPIAR — sub-texto com valor de payout */
.mines-btn--cashout__value {
  display: block;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(10px, 1.3vw, 14px);
  font-weight: 700;
  color: rgba(5, 5, 5, 0.7);
  margin-top: clamp(2px, 0.3vw, 4px);
}

/* COPIAR — disabled (antes de jogar ou apos bust) */
.mines-btn--cashout--disabled {
  background: rgba(40, 40, 40, 0.6);
  border: 1px solid rgba(212, 168, 67, 0.15);
  color: rgba(212, 168, 67, 0.3);
  cursor: not-allowed;
  opacity: 0.5;
  pointer-events: none;
  animation: none;
  box-shadow: none;
}

.mines-btn--cashout--disabled::before {
  display: none;
}
```

---

## 1.16 Botao ALEATORIO (DS P2 par.8.3 botao secundario)

```css
/* COPIAR EXATAMENTE — botao revelar tile aleatoria */
.mines-btn--random {
  width: 100%;
  padding: clamp(8px, 1vw, 12px);
  border-radius: 8px;
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid rgba(212, 168, 67, 0.25);
  color: #D4A843;
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-size: clamp(10px, 1.2vw, 13px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: border-color 0.15s ease, background 0.15s ease, transform 0.1s ease;
}

.mines-btn--random:hover {
  border-color: rgba(212, 168, 67, 0.5);
  background: rgba(212, 168, 67, 0.06);
  transform: scale(1.02);
}

.mines-btn--random:active {
  transform: scale(0.97);
  background: rgba(212, 168, 67, 0.1);
}

.mines-btn--random--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  pointer-events: none;
}
```

---

## 1.17 Toggle AUTO Mode (DS P2 par.8.3 botao toggle)

```css
/* COPIAR EXATAMENTE — toggle auto mode (inativo) */
.mines-btn--auto {
  width: 100%;
  padding: clamp(8px, 1vw, 12px);
  border-radius: 8px;
  background: rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(212, 168, 67, 0.15);
  color: rgba(212, 168, 67, 0.6);
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-size: clamp(10px, 1.2vw, 13px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.mines-btn--auto:hover {
  border-color: rgba(212, 168, 67, 0.35);
  color: rgba(212, 168, 67, 0.8);
}

/* COPIAR — toggle auto mode (ATIVO — verde) */
.mines-btn--auto--active {
  background: rgba(0, 230, 118, 0.1);
  border: 1px solid rgba(0, 230, 118, 0.3);
  color: #00E676;
}

.mines-btn--auto--active:hover {
  border-color: rgba(0, 230, 118, 0.5);
  background: rgba(0, 230, 118, 0.15);
}
```

---

## 1.18 Botao VOLTAR (DS Guia P3 par.9.6)

```css
/* COPIAR EXATAMENTE — botao voltar padrao do Blackout Casino */
.mines-btn--back {
  background: rgba(0, 0, 0, 0.6);
  border: 1px solid rgba(212, 168, 67, 0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-size: clamp(10px, 1.2vw, 16px);
  font-weight: 600;
  letter-spacing: 2px;
  padding: clamp(6px, 0.8vw, 10px) clamp(10px, 1.2vw, 16px);
  cursor: pointer;
  text-transform: uppercase;
  transition: border-color 0.15s ease, transform 0.1s ease;
}

.mines-btn--back:hover {
  border-color: rgba(212, 168, 67, 0.8);
  transform: scale(1.05);
}

.mines-btn--back:active {
  transform: scale(0.95);
}
```

**Framer Motion:**
```
whileHover={{ scale: 1.05, borderColor: 'rgba(212,168,67,0.8)' }}
whileTap={{ scale: 0.95 }}
```

---

## 1.19 Header (barra superior) (DS P1 par.3 + Guia P3 par.9.6)

```css
/* COPIAR EXATAMENTE — header do jogo */
.mines-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 12px) clamp(10px, 1.2vw, 16px);
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  z-index: 10;
}

/* COPIAR — titulo central */
.mines-header__title {
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-weight: 800;
  font-size: clamp(16px, 2.2vw, 26px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: clamp(2px, 0.3vw, 4px);
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
}

/* COPIAR — botoes circulares [?] e [H] */
.mines-header__icon-btn {
  width: clamp(30px, 4vw, 40px);
  height: clamp(30px, 4vw, 40px);
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.5);
  border: 1px solid rgba(212, 168, 67, 0.3);
  color: #D4A843;
  font-family: var(--font-cinzel), 'Cinzel Decorative', serif;
  font-size: clamp(12px, 1.5vw, 16px);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: border-color 0.15s ease, transform 0.1s ease;
}

.mines-header__icon-btn:hover {
  border-color: rgba(212, 168, 67, 0.7);
  transform: scale(1.1);
  background: rgba(0, 0, 0, 0.7);
}

.mines-header__icon-btn:active {
  transform: scale(0.95);
}
```

---

**[FIM DA PARTE 2 — Continua na Parte 3 com Overlays (Win, Bust), Modal Provably Fair, Historico, Badges]**

`[ENTREGA: ADENDO-DS-MINES-PARTE-2.md — 19/03/2026]`
