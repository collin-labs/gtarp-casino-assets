# CSS-COMPONENTES — MINES (#3)
## PARTE 1 DE 2 — Tokens, Keyframes, Componentes 0-11
## Arquivo unico com CSS real — Copiavel — Numerado — 23/03/2026

---

## COMO USAR
1. Colar JUNTO com o Prompt-V0 e Adendo DS no V0/Bolt
2. Cada componente tem: numero, nome, referencia DS, CSS copiavel
3. Para edicoes futuras: str_replace cirurgico (REGRA X6)

---

## 0. TOKENS CSS (:root)

```css
:root {
  /* Paleta base */
  --mines-bg: #0A0A0A;
  --mines-gold: #D4A843;
  --mines-gold-bright: #FFD700;
  --mines-green: #00E676;
  --mines-green-dark: #004D25;
  --mines-red: #FF1744;
  --mines-red-dark: #8B0000;
  --mines-white: rgba(255,255,255,0.87);
  --mines-muted: rgba(212,168,67,0.5);
  --mines-border: rgba(212,168,67,0.15);
  --mines-glass: rgba(10,10,10,0.85);

  /* Tipografia */
  --font-cinzel: 'Cinzel', serif;
  --font-inter: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* Tile estados */
  --tile-closed-bg: rgba(255,255,255,0.03);
  --tile-closed-border: rgba(212,168,67,0.15);
  --tile-hover-bg: rgba(212,168,67,0.08);
  --tile-hover-border: rgba(212,168,67,0.35);
  --tile-gem-bg: rgba(0,230,118,0.08);
  --tile-gem-border: rgba(0,230,118,0.5);
  --tile-bomb-bg: rgba(255,23,68,0.08);
  --tile-bomb-border: rgba(255,23,68,0.5);
  --tile-disabled-bg: rgba(255,255,255,0.015);
}
```

---

## 1. @KEYFRAMES

```css
@keyframes mines-pulse-gold {
  0%, 100% { box-shadow: 0 0 8px rgba(212,168,67,0.2); }
  50% { box-shadow: 0 0 20px rgba(212,168,67,0.4); }
}
@keyframes mines-pulse-green {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.2); }
  50% { box-shadow: 0 0 20px rgba(0,230,118,0.4); }
}
@keyframes mines-shake {
  0%, 100% { transform: translateX(0); }
  20% { transform: translateX(-4px); }
  40% { transform: translateX(4px); }
  60% { transform: translateX(-3px); }
  80% { transform: translateX(2px); }
}
@keyframes mines-flash-red {
  0% { background: rgba(255,23,68,0.2); }
  100% { background: transparent; }
}
@keyframes mines-shine-sweep {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
```

---

## 2. FUNDO DO JOGO (Adendo DS §1.1)

```css
.mines-bg {
  position: absolute; inset: 0; z-index: 0;
  background: url('/assets/shared/ui/bg-casino.png') center/cover no-repeat;
}
.mines-bg::after {
  content: ''; position: absolute; inset: 0;
  background: radial-gradient(ellipse at 50% 30%, rgba(10,10,10,0.75) 0%, rgba(10,10,10,0.92) 70%, #0A0A0A 100%);
  z-index: 1;
}
```

---

## 3. HEADER BAR (Adendo DS §1.19)

```css
.mines-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: clamp(8px, 0.8vw, 14px) clamp(12px, 1.2vw, 20px);
  border-bottom: 1px solid var(--mines-border);
  position: relative; z-index: 10;
}
.mines-title {
  font-family: var(--font-cinzel); font-weight: 800;
  font-size: clamp(14px, 2vw, 22px); color: var(--mines-gold);
  text-transform: uppercase; letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 6px rgba(0,0,0,1);
}
```

---

## 4. BOTAO VOLTAR (Adendo DS §1.18)

```css
.mines-btn-back {
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 8px; color: rgba(212,168,67,0.5);
  font-family: var(--font-cinzel); font-weight: 600;
  font-size: clamp(9px, 0.95vw, 12px); letter-spacing: 1.5px;
  text-transform: uppercase; padding: clamp(6px, 0.6vw, 10px) clamp(10px, 1vw, 16px);
  cursor: pointer; min-height: 44px;
  transition: all 0.3s ease;
}
.mines-btn-back:hover {
  background: rgba(255,255,255,0.06);
  border-color: rgba(212,168,67,0.3); color: var(--mines-gold);
}
```

---

## 5. SALDO GCOIN (Adendo DS §3.4)

```css
.mines-balance {
  font-family: var(--font-mono); font-weight: 700;
  font-size: clamp(12px, 1.4vw, 18px); color: var(--mines-green);
  text-shadow: 0 0 10px rgba(0,230,118,0.3);
}
```

---

## 6. FEIXO VERDE BASE

```css
.mines-feixo-verde {
  position: absolute; bottom: 0; left: 5%; right: 5%; height: 3px;
  background: linear-gradient(90deg, transparent 5%, rgba(0,230,118,0.3) 20%, #00E676 50%, rgba(0,230,118,0.3) 80%, transparent 95%);
  box-shadow: 0 0 8px rgba(0,230,118,0.3), 0 0 15px rgba(0,230,118,0.15);
  pointer-events: none; z-index: 2;
}
```

---

## 7. LINHA DOURADA TOPO

```css
.mines-line-gold-top {
  position: absolute; top: 0; left: 10%; right: 10%; height: 1px;
  background: linear-gradient(90deg, transparent 0%, rgba(212,168,67,0.15) 15%, rgba(255,215,0,0.35) 50%, rgba(212,168,67,0.15) 85%, transparent 100%);
  pointer-events: none; z-index: 2;
}
```

---

## 8. SHINE SWEEP

```css
.mines-shine-sweep { position: relative; overflow: hidden; }
.mines-shine-sweep::after {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 45%, rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.03) 55%, transparent 60%);
  background-size: 200% 100%; transform: translateX(-100%);
  pointer-events: none; z-index: 3; transition: transform 0.6s ease;
}
.mines-shine-sweep:hover::after { transform: translateX(50%); }
```

---

## 9. GRID 5x5 (Adendo DS §1.2)

```css
.mines-grid {
  display: grid; grid-template-columns: repeat(5, 1fr);
  gap: clamp(4px, 0.5vw, 8px);
  width: clamp(250px, 40vw, 500px); aspect-ratio: 1;
  padding: clamp(8px, 1vw, 16px);
  background: rgba(10,10,10,0.6);
  border: 1.5px solid var(--mines-border);
  border-radius: 14px;
  box-shadow: 0 4px 20px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,215,0,0.05);
}
```

---

## 10. TILE — 6 ESTADOS (Adendo DS §1.3-1.8)

```css
/* CLOSED */
.mines-tile {
  background: var(--tile-closed-bg);
  border: 1.5px solid var(--tile-closed-border);
  border-radius: 8px; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  aspect-ratio: 1; min-width: 44px; min-height: 44px;
  transition: all 0.2s ease; position: relative;
}
/* HOVER */
.mines-tile:hover:not(.mines-tile--revealed):not(.mines-tile--disabled) {
  background: var(--tile-hover-bg);
  border-color: var(--tile-hover-border);
  transform: scale(1.05);
  box-shadow: 0 0 12px rgba(212,168,67,0.15);
}
/* GEM */
.mines-tile--gem {
  background: var(--tile-gem-bg);
  border-color: var(--tile-gem-border);
  cursor: default;
  box-shadow: 0 0 12px rgba(0,230,118,0.2), inset 0 0 8px rgba(0,230,118,0.1);
}
/* BOMB */
.mines-tile--bomb {
  background: var(--tile-bomb-bg);
  border-color: var(--tile-bomb-border);
  cursor: default;
  box-shadow: 0 0 12px rgba(255,23,68,0.2), inset 0 0 8px rgba(255,23,68,0.1);
}
/* DISABLED (pos-rodada) */
.mines-tile--disabled {
  background: var(--tile-disabled-bg);
  border-color: rgba(255,255,255,0.05);
  cursor: default; opacity: 0.5;
}
/* LOADING */
.mines-tile--loading {
  animation: mines-pulse-gold 1.5s ease infinite;
  cursor: wait;
}
```

---

## 11. PAINEL LATERAL (Adendo DS §1.9)

```css
.mines-panel {
  display: flex; flex-direction: column;
  gap: clamp(10px, 1vw, 16px);
  width: clamp(200px, 25vw, 320px);
  padding: clamp(12px, 1.2vw, 20px);
  background: rgba(10,10,10,0.7);
  border: 1px solid var(--mines-border);
  border-radius: 14px;
  backdrop-filter: blur(8px);
  box-shadow: 0 4px 20px rgba(0,0,0,0.4);
}
```

---

`[FIM PARTE 1 — Continua com Componentes 12-19 + Lista Imagens + PROIBIDO]`
