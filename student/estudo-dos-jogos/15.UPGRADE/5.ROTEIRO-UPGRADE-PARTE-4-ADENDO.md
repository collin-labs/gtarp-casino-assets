# ADENDO — ROTEIRO DE DESENVOLVIMENTO UPGRADE (#15)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-UPGRADE.md — 23/03/2026

---

# 1. FASE 4 POLISH — CODIGO REAL TSX

## 1.1 HistoryPanel.tsx — Trechos com tooltips

```tsx
{history.map((upgrade, i) => (
  <motion.div
    key={upgrade.id}
    initial={{ opacity: 0, x: 10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: upgrade.won ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${upgrade.won ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    <span
      title={lang === "br" ? (upgrade.won ? "Upgrade bem-sucedido" : "Upgrade falhou") : (upgrade.won ? "Upgrade succeeded" : "Upgrade failed")}
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(10px, 1vw, 14px)",
        fontWeight: 800,
        color: upgrade.won ? "#00E676" : "#FF1744",
        minWidth: "28px",
        textAlign: "center",
      }}
    >
      {upgrade.won ? "W" : "L"}
    </span>
    <span style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(8px, 0.8vw, 12px)", color: "rgba(212,168,67,0.5)", flex: 1 }}>
      {upgrade.stake} → {upgrade.target}
    </span>
    <span style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(8px, 0.8vw, 11px)", color: "rgba(212,168,67,0.4)" }}>
      {upgrade.chance}%
    </span>
    <motion.button
      onClick={() => onVerify(upgrade.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={lang === "br" ? "Verificar fairness deste upgrade" : "Verify this upgrade's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/upgrade/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 1.2 ProvablyFairModal.tsx — Campos com copiar

```tsx
{(["serverSeedHash", "serverSeed", "clientSeed", "nonce"] as const).map((field) => (
  <div key={field} style={{ marginBottom: "clamp(8px, 0.8vw, 14px)" }}>
    <label style={{ fontFamily: "var(--font-cinzel)", fontSize: "clamp(8px, 0.8vw, 11px)", fontWeight: 600, color: "rgba(212,168,67,0.6)", letterSpacing: "1.5px", textTransform: "uppercase" as const, marginBottom: "4px", display: "block" }}>
      {pfLabels[field][lang]}
    </label>
    <div style={{ display: "flex", alignItems: "center", gap: "6px" }}>
      <input readOnly value={String(pfData[field])} style={{ flex: 1, background: "rgba(0,0,0,0.4)", border: "1px solid rgba(212,168,67,0.2)", borderRadius: "6px", color: "#D4A843", fontFamily: "var(--font-mono)", fontSize: "clamp(9px, 0.9vw, 13px)", padding: "clamp(6px, 0.6vw, 10px)" }} />
      <motion.button
        onClick={() => handleCopy(field)}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        title={lang === "br" ? "Copiar" : "Copy"}
        style={{ background: copiedField === field ? "rgba(0,230,118,0.15)" : "rgba(212,168,67,0.08)", border: `1px solid ${copiedField === field ? "rgba(0,230,118,0.5)" : "rgba(212,168,67,0.3)"}`, borderRadius: "6px", padding: "clamp(6px, 0.6vw, 10px)", cursor: "pointer", minWidth: "44px", minHeight: "44px", display: "flex", alignItems: "center", justifyContent: "center" }}
      >
        <img src={copiedField === field ? "/assets/upgrade/icon-check.png" : "/assets/upgrade/icon-copy.png"} alt="" style={{ width: "16px", height: "16px" }} />
      </motion.button>
    </div>
  </div>
))}

<pre style={{ fontFamily: "var(--font-mono)", fontSize: "clamp(8px, 0.8vw, 12px)", color: "rgba(212,168,67,0.7)", lineHeight: 1.8, whiteSpace: "pre-wrap", background: "rgba(0,0,0,0.3)", borderRadius: "8px", padding: "clamp(10px, 1vw, 16px)", border: "1px solid rgba(212,168,67,0.15)", margin: 0 }}>
{`hash = HMAC_SHA256(serverSeed, clientSeed + ":" + nonce)
roll = (parseInt(hash.slice(0, 8), 16) / 0xFFFFFFFF) * 100
won = roll < chancePercent`}
</pre>
```

---

# 2. REGRAS COMPLEMENTO (item 5.11)

9. TODOS botoes min 44px
10. TODOS textos via { br, en }
11. Resultado SEMPRE server-authoritative
12. Wheel SVG com requestAnimationFrame, NAO setInterval

---

**VALIDACAO X17:** 3 itens, 3 entregues.

`[ENTREGA: ADENDO-ROTEIRO-UPGRADE.md — 23/03/2026]`
