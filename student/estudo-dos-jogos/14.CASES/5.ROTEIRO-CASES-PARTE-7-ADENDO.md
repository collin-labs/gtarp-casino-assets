# ADENDO -- ROTEIRO DE DESENVOLVIMENTO CASES (#14)
# PARTE 7 -- CaseResultOverlay TSX real (item 5.14)
# Complementa: 5.ROTEIRO P1-P6 = 2.920L
# Blackout Casino GTARP - 26/03/2026

---

# COMPONENTE: CaseResultOverlay.tsx (Fase 5 -- codigo real)

Pesquisa X0: CS2 5 rarity tiers (Mil-Spec 79.92%, Restricted 15.98%,
Classified 3.20%, Covert 0.64%, Knife/Gold 0.26%). "Scrolling animation
is purely cosmetic — result generated server-side". Sell-back 70%.

```tsx
import { motion, AnimatePresence, useMotionValue, useTransform, animate } from "framer-motion";
import { useEffect, useRef } from "react";

type Rarity = "common" | "uncommon" | "rare" | "epic" | "legendary";

interface CaseResultOverlayProps {
  show: boolean;
  itemName: string;
  itemImage: string;
  rarity: Rarity;
  itemValue: number;
  sellBackValue: number;
  lang: "br" | "en";
  onKeep: () => void;
  onSellBack: () => void;
}

const RARITY: Record<Rarity, { br: string; en: string; color: string; glow: string }> = {
  common: { br: "COMUM", en: "COMMON", color: "#4B69FF", glow: "rgba(75,105,255,0.3)" },
  uncommon: { br: "RESTRITO", en: "RESTRICTED", color: "#8847FF", glow: "rgba(136,71,255,0.3)" },
  rare: { br: "CLASSIFICADO", en: "CLASSIFIED", color: "#D32CE6", glow: "rgba(211,44,230,0.4)" },
  epic: { br: "SECRETO", en: "COVERT", color: "#EB4B4B", glow: "rgba(235,75,75,0.4)" },
  legendary: { br: "RARO ESPECIAL", en: "RARE SPECIAL", color: "#FFD700", glow: "rgba(255,215,0,0.5)" },
};

export default function CaseResultOverlay({
  show, itemName, itemImage, rarity, itemValue, sellBackValue, lang, onKeep, onSellBack,
}: CaseResultOverlayProps) {
  const cfg = RARITY[rarity];
  const isRare = rarity === "epic" || rarity === "legendary";

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "fixed", inset: 0,
            background: "rgba(0,0,0,0.92)",
            backdropFilter: "blur(12px)",
            display: "flex", flexDirection: "column",
            alignItems: "center", justifyContent: "center",
            zIndex: 1000,
          }}
        >
          {/* Particulas (rare+) */}
          {isRare && Array.from({ length: rarity === "legendary" ? 40 : 25 }).map((_, i) => (
            <motion.div
              key={`p-${i}`}
              initial={{ opacity: 1, scale: 0, x: 0, y: 0 }}
              animate={{
                opacity: [1, 1, 0], scale: [0, 1, 0.3],
                x: (Math.random() - 0.5) * 400, y: (Math.random() - 0.5) * 400,
              }}
              transition={{ duration: 1.5, delay: Math.random() * 0.3 }}
              style={{
                position: "absolute",
                width: "clamp(3px,0.5vw,6px)", height: "clamp(3px,0.5vw,6px)",
                borderRadius: "50%", background: cfg.color, pointerEvents: "none",
              }}
            />
          ))}

          {/* Rarity label */}
          <motion.div
            initial={{ scale: 0 }}
            animate={{ scale: 1 }}
            transition={{ type: "spring", stiffness: 200, damping: 12 }}
            title={lang === "br" ? cfg.br : cfg.en}
            style={{
              fontFamily: "'Cinzel', serif",
              fontSize: isRare ? "clamp(22px,3vw,36px)" : "clamp(16px,2vw,24px)",
              fontWeight: 700, color: cfg.color, textTransform: "uppercase",
              letterSpacing: "3px", textShadow: `0 0 25px ${cfg.glow}`,
            }}
          >
            {lang === "br" ? cfg.br : cfg.en}
          </motion.div>

          {/* Item image */}
          <motion.div
            initial={{ scale: 0, rotate: -10 }}
            animate={{ scale: 1, rotate: 0 }}
            transition={{ type: "spring", stiffness: 200, delay: 0.3 }}
            title={itemName}
            style={{
              width: "clamp(100px,12vw,160px)", height: "clamp(100px,12vw,160px)",
              borderRadius: 14, border: `3px solid ${cfg.color}`,
              boxShadow: `0 0 30px ${cfg.glow}`,
              background: "rgba(17,17,17,0.9)",
              display: "flex", alignItems: "center", justifyContent: "center",
              marginTop: "clamp(8px,1vw,14px)", overflow: "hidden",
            }}
          >
            <img src={itemImage} alt={itemName}
              style={{ width: "85%", height: "85%", objectFit: "contain" }} />
          </motion.div>

          {/* Item name + value */}
          <motion.div
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5 }}
            style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: "clamp(13px,1.5vw,17px)",
              color: cfg.color, marginTop: 8, textAlign: "center",
            }}
          >
            {itemName}
          </motion.div>
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.6 }}
            title={`G$ ${itemValue.toLocaleString()}`}
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(16px,2vw,22px)", fontWeight: 700,
              color: "#00E676", marginTop: 4,
            }}
          >
            G$ {itemValue.toLocaleString()}
          </motion.div>

          {/* Buttons: Keep / Sell Back */}
          <div style={{ display: "flex", gap: 12, marginTop: "clamp(14px,1.5vw,20px)" }}>
            <motion.button
              initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1 }}
              onClick={onKeep} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
              title={lang === "br" ? "Guardar no inventario" : "Keep in inventory"}
              style={{
                padding: "clamp(8px,1vw,12px) clamp(16px,2vw,28px)",
                background: "linear-gradient(135deg, #00E676, #00C853)",
                border: "none", borderRadius: 8,
                color: "#0A0A0A", fontFamily: "'Cinzel',serif",
                fontSize: "clamp(10px,1.1vw,13px)", fontWeight: 700,
                textTransform: "uppercase", cursor: "pointer", minHeight: 44,
              }}
            >
              {lang === "br" ? "GUARDAR" : "KEEP"}
            </motion.button>
            <motion.button
              initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1 }}
              onClick={onSellBack} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
              title={lang === "br"
                ? `Vender por G$ ${sellBackValue.toLocaleString()} (70%)`
                : `Sell for G$ ${sellBackValue.toLocaleString()} (70%)`
              }
              style={{
                padding: "clamp(8px,1vw,12px) clamp(16px,2vw,28px)",
                background: "transparent",
                border: "1.5px solid rgba(212,168,67,0.4)",
                borderRadius: 8, color: "#D4A843",
                fontFamily: "'Cinzel',serif", fontSize: "clamp(10px,1.1vw,13px)",
                textTransform: "uppercase", cursor: "pointer", minHeight: 44,
              }}
            >
              {lang === "br" ? `VENDER G$ ${sellBackValue.toLocaleString()}` : `SELL G$ ${sellBackValue.toLocaleString()}`}
            </motion.button>
          </div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

---

`[ENTREGA: 5.ROTEIRO-CASES-PARTE-7-ADENDO.md -- 26/03/2026]`
