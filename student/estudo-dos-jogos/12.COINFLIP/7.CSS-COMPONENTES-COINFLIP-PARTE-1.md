# CSS COMPONENTES — COINFLIP (#12)
## Parte 1 de 2 - Tokens :root + @keyframes (8) + Componentes 1-10
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-COINFLIP
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 20/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Coinflip no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.coinflip-room { border: 1px solid #D4A843; }` daqui.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Coinflip)

```css
/* ====================================================
   COINFLIP — DESIGN TOKENS
   Separacao: primitivos (raw) -> semanticos (uso)
   Ref: Adendo DS sec1, Prompt AI sec Header Tecnico
   ==================================================== */

:root {
  /* --- Primitivos — cores base --- */
  --cf-gold-primary:    #D4A843;
  --cf-gold-light:      #F6E27A;
  --cf-gold-dark:       #8B6914;
  --cf-gold-deeper:     #462523;
  --cf-gold-pure:       #FFD700;
  --cf-green-neon:      #00E676;
  --cf-green-dark:      #004D25;
  --cf-green-medium:    #00C853;
  --cf-black-bg:        #0A0A0A;
  --cf-black-surface:   #0F0F0F;
  --cf-black-card:      #141414;
  --cf-black-hover:     #1A1A1A;
  --cf-black-tooltip:   #2A2A2A;
  --cf-red:             #FF1744;
  --cf-white:           #FFFFFF;
  --cf-text-primary:    #F5F5F5;
  --cf-text-secondary:  #A8A8A8;
  --cf-text-muted:      #666666;
  --cf-silver:          #C0C0C0;
  --cf-bronze:          #CD7F32;

  /* --- Semanticos — uso especifico Coinflip --- */
  --cf-cara:                  #00E676;
  --cf-coroa:                 #FFD700;
  --cf-border-gold:           1px solid rgba(212,168,67,0.15);
  --cf-border-gold-hover:     1px solid rgba(212,168,67,0.3);
  --cf-border-gold-active:    1px solid rgba(212,168,67,0.5);
  --cf-border-subtle:         1px solid rgba(212,168,67,0.1);
  --cf-shadow-card:           0 4px 16px rgba(0,0,0,0.3);
  --cf-shadow-card-hover:     0 8px 30px rgba(0,0,0,0.4);
  --cf-shadow-modal:          0 0 40px rgba(0,0,0,0.5), 0 0 80px rgba(212,168,67,0.05);
  --cf-shadow-coin-idle:      0 0 12px rgba(255,215,0,0.15), inset 0 -3px 6px rgba(0,0,0,0.2), inset 0 3px 6px rgba(255,215,0,0.1);
  --cf-shadow-coin-spin:      0 0 20px rgba(255,215,0,0.2), inset 0 -4px 8px rgba(0,0,0,0.3), inset 0 4px 8px rgba(255,215,0,0.15);
  --cf-shadow-coin-landed:    0 0 30px rgba(255,215,0,0.3), 0 0 60px rgba(255,215,0,0.1), inset 0 -4px 8px rgba(0,0,0,0.3), inset 0 4px 8px rgba(255,215,0,0.2);
  --cf-text-shadow-gold:      0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
  --cf-text-shadow-green:     0 0 8px rgba(0,230,118,0.4);

  /* --- Fontes --- */
  --cf-font-title: 'Cinzel', serif;
  --cf-font-body:  'Inter', sans-serif;
  --cf-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --cf-touch-min:      44px;
  --cf-radius-card:    clamp(8px, 1vw, 12px);
  --cf-radius-btn:     8px;
  --cf-radius-modal:   clamp(8px, 1vw, 16px);
  --cf-radius-small:   6px;
}
```

---

# 1. @KEYFRAMES (8 animacoes do Coinflip)

```css
/* 1 — Shimmer sweep nos cards do lobby (DS P1 sec4.4 shine sweep) */
@keyframes cfShimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

/* 2 — Badge AGUARDANDO pulso na borda verde */
@keyframes cfWaitingPulse {
  0%, 100% { border-color: rgba(0,230,118,0.2); }
  50% { border-color: rgba(0,230,118,0.5); }
}

/* 3 — Glow pulsante da moeda durante spin (DS custom) */
@keyframes cfCoinGlow {
  0%, 100% { box-shadow: 0 0 20px rgba(255,215,0,0.2); }
  50% { box-shadow: 0 0 40px rgba(255,215,0,0.5); }
}

/* 4 — Glow pulsante do avatar vencedor (DS P3A sec20.2) */
@keyframes cfWinnerGlow {
  0%, 100% { box-shadow: 0 0 20px rgba(255,215,0,0.3); }
  50% { box-shadow: 0 0 35px rgba(255,215,0,0.5); }
}

/* 5 — Barra de suspense preenche durante flip */
@keyframes cfSuspenseFill {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(0%); }
}

/* 6 — Flash branco no momento do resultado */
@keyframes cfFlash {
  0% { opacity: 0; }
  50% { opacity: 0.3; }
  100% { opacity: 0; }
}

/* 7 — Spinner de loading (botao confirmar processando) */
@keyframes cfSpin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* 8 — Particula dourada caindo com gravidade (burst vitoria) */
@keyframes cfParticleFall {
  0% { opacity: 1; transform: translateY(0) scale(1) rotate(0deg); }
  100% { opacity: 0; transform: translateY(80px) scale(0.3) rotate(180deg); }
}
```

---

# 2. FUNDO DO JOGO (DS P1 sec4.1)

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Coinflip */
.coinflip-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
}

/* COPIAR — fallback NUI (bg-casino nao carregado) */
.coinflip-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}

/* COPIAR — estado flip ativo (lobby desfoca atras) */
.coinflip-game--flipping .coinflip-lobby {
  opacity: 0.15;
  pointer-events: none;
  filter: blur(4px);
  transition: all 0.3s ease;
}
```

---

# 3. HEADER BAR (DS P2 sec15.2 adaptado)

```css
/* COPIAR EXATAMENTE — barra superior */
.coinflip-game__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
  z-index: 5;
}

/* COPIAR — titulo "CARA OU COROA" */
.coinflip-game__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
}

/* COPIAR — saldo GCoin */
.coinflip-game__balance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}

/* COPIAR — icones PNG do header (historico, leaderboard) */
.coinflip-game__header-icon {
  width: clamp(32px, 4vw, 44px);
  height: clamp(32px, 4vw, 44px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  background: transparent;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  transition: all 0.2s ease;
}
.coinflip-game__header-icon:hover {
  border-color: rgba(212,168,67,0.5);
  background: rgba(212,168,67,0.05);
}
.coinflip-game__header-icon:active {
  transform: scale(0.95);
}
.coinflip-game__header-icon img {
  width: clamp(16px, 2vw, 22px);
  height: clamp(16px, 2vw, 22px);
}
```

---

# 4. BOTAO VOLTAR (DS P2 sec8.3 adaptado)

```css
/* COPIAR EXATAMENTE */
.coinflip-btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px;
  cursor: pointer;
  transition: all 0.25s ease;
}
.coinflip-btn-back:hover {
  border-color: rgba(212,168,67,0.8);
  background: rgba(0,0,0,0.8);
}
.coinflip-btn-back:active {
  transform: scale(0.95);
}
```

---

# 5. FILTROS DO LOBBY (DS P2 sec8.8 adaptado)

```css
/* COPIAR EXATAMENTE — container filtros + botao criar */
.coinflip-lobby__filters {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
}

/* COPIAR — filtro inativo */
.coinflip-lobby__filter {
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 1.5vw, 20px);
  border-radius: 8px;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 500;
  cursor: pointer;
  border: 1px solid rgba(212,168,67,0.2);
  background: transparent;
  color: #D4A843;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-lobby__filter:hover {
  background: rgba(212,168,67,0.08);
  border-color: rgba(212,168,67,0.3);
}
.coinflip-lobby__filter:active {
  transform: scale(0.97);
}

/* COPIAR — filtro ativo */
.coinflip-lobby__filter--active {
  background: rgba(212,168,67,0.15);
  border-color: #FFD700;
  color: #FFD700;
}
.coinflip-lobby__filter--active:hover {
  background: rgba(212,168,67,0.20);
}
```

---

# 6. BOTAO PRIMARIO VERDE (DS P2 sec8.1 — CRIAR SALA, CONFIRMAR, VOLTAR AO LOBBY)

```css
/* COPIAR EXATAMENTE */
.coinflip-btn-primary {
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2vw, 28px);
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
  transition: all 0.25s ease;
}
.coinflip-btn-primary:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.coinflip-btn-primary:active {
  transform: translateY(1px);
  box-shadow: 0 2px 8px rgba(0,200,83,0.15);
}

/* COPIAR — estado desabilitado (ja tem sala / saldo insuficiente) */
.coinflip-btn-primary--disabled {
  background: #222222;
  border-color: rgba(255,255,255,0.06);
  color: #666666;
  box-shadow: none;
  cursor: not-allowed;
  opacity: 0.5;
}
.coinflip-btn-primary--disabled:hover {
  transform: none;
  box-shadow: none;
}

/* COPIAR — estado loading (processando) */
.coinflip-btn-primary--loading {
  pointer-events: none;
  opacity: 0.7;
  position: relative;
}
.coinflip-btn-primary--loading::after {
  content: '';
  width: 16px; height: 16px;
  border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #FFFFFF;
  border-radius: 50%;
  animation: cfSpin 0.6s linear infinite;
  position: absolute;
  right: clamp(12px, 1.5vw, 20px);
  top: 50%;
  transform: translateY(-50%);
}
```

---

# 7. BOTAO DOURADO SECUNDARIO (DS P2 sec8.3 — ENTRAR, CANCELAR, VERIFICAR)

```css
/* COPIAR EXATAMENTE */
.coinflip-btn-gold {
  background: transparent;
  border: 1px solid #D4A843;
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(10px, 1.1vw, 13px);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 1.5vw, 20px);
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-btn-gold:hover {
  background: rgba(212,168,67,0.08);
  border-color: #FFD700;
  color: #FFD700;
}
.coinflip-btn-gold:active {
  transform: scale(0.97);
}

/* COPIAR — botao cancelar (terciario, mais discreto) */
.coinflip-btn-cancel {
  background: transparent;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  color: #A8A8A8;
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(10px, 1.2vw, 14px);
  padding: clamp(8px, 1vw, 12px);
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-btn-cancel:hover {
  border-color: rgba(212,168,67,0.4);
  color: #D4A843;
}
.coinflip-btn-cancel:active {
  transform: scale(0.98);
}
```

---

# 8. GRID DE SALAS DO LOBBY (DS P1 sec4.2 adaptado)

```css
/* COPIAR EXATAMENTE — grid responsivo */
.coinflip-lobby__grid {
  flex: 1;
  overflow-y: auto;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(clamp(140px, 18vw, 200px), 1fr));
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(8px, 1vw, 16px) clamp(8px, 1.5vw, 16px);
  align-content: start;
}

/* COPIAR — scrollbar customizada */
.coinflip-lobby__grid::-webkit-scrollbar {
  width: 4px;
}
.coinflip-lobby__grid::-webkit-scrollbar-track {
  background: transparent;
}
.coinflip-lobby__grid::-webkit-scrollbar-thumb {
  background: rgba(212,168,67,0.2);
  border-radius: 2px;
}

/* COPIAR — lobby vazio (nenhuma sala) */
.coinflip-lobby__empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(32px, 5vw, 64px);
  grid-column: 1 / -1;
}
.coinflip-lobby__empty-icon {
  width: clamp(48px, 6vw, 80px);
  height: clamp(48px, 6vw, 80px);
  opacity: 0.3;
}
.coinflip-lobby__empty-text {
  font-family: 'Inter', sans-serif;
  font-size: clamp(11px, 1.2vw, 14px);
  color: #666666;
  text-align: center;
}
```

---

# 9. ROOM CARD — 4 ESTADOS (DS P1 sec4.4 adaptado)

```css
/* COPIAR EXATAMENTE — card base (estado WAITING) */
.coinflip-room {
  background: #141414;
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(12px, 1.5vw, 20px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  position: relative;
  overflow: hidden;
  cursor: pointer;
  transition: all 0.3s ease;
}
.coinflip-room:hover {
  border-color: rgba(212,168,67,0.3);
  transform: translateY(-4px);
  box-shadow: 0 8px 30px rgba(0,0,0,0.4);
}
.coinflip-room:active {
  transform: translateY(0px);
}

/* COPIAR — shimmer sweep (DS P1 sec4.4 shine sweep) */
.coinflip-room::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,215,0,0.03), transparent);
  pointer-events: none;
}
.coinflip-room:hover::after {
  animation: cfShimmer 0.6s ease forwards;
}

/* COPIAR — estado HIGH STAKE (valor > 10.000 GC) */
.coinflip-room--high-stake {
  border-color: rgba(212,168,67,0.5);
  box-shadow: 0 0 20px rgba(212,168,67,0.1);
}
.coinflip-room--high-stake:hover {
  border-color: rgba(212,168,67,0.7);
  box-shadow: 0 0 30px rgba(212,168,67,0.15), 0 8px 30px rgba(0,0,0,0.4);
}

/* COPIAR — estado OWN (sua sala, aguardando oponente) */
.coinflip-room--own {
  border-color: rgba(0,230,118,0.3);
  box-shadow: 0 0 12px rgba(0,230,118,0.05);
  cursor: default;
}
.coinflip-room--own:hover {
  transform: none;
  box-shadow: 0 0 12px rgba(0,230,118,0.05);
}

/* COPIAR — estado COMPLETED (historico recente, so leitura) */
.coinflip-room--completed {
  opacity: 0.6;
  cursor: default;
}
.coinflip-room--completed:hover {
  transform: none;
  border-color: rgba(212,168,67,0.15);
  box-shadow: none;
}

/* COPIAR — avatar do criador */
.coinflip-room__avatar {
  width: clamp(36px, 4.5vw, 56px);
  height: clamp(36px, 4.5vw, 56px);
  border-radius: 50%;
  border: 2px solid #D4A843;
  object-fit: cover;
}

/* COPIAR — nickname */
.coinflip-room__nick {
  font-family: 'Cinzel', serif;
  font-size: clamp(10px, 1.2vw, 14px);
  font-weight: 600;
  color: #F5F5F5;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
}

/* COPIAR — indicador lado CARA */
.coinflip-room__side--cara {
  display: flex;
  align-items: center;
  gap: clamp(4px, 0.5vw, 6px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 12px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #00E676;
}

/* COPIAR — indicador lado COROA */
.coinflip-room__side--coroa {
  display: flex;
  align-items: center;
  gap: clamp(4px, 0.5vw, 6px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 12px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #FFD700;
}

/* COPIAR — mini icone moeda ao lado do texto */
.coinflip-room__side-icon {
  width: clamp(14px, 1.8vw, 20px);
  height: clamp(14px, 1.8vw, 20px);
  border-radius: 50%;
}

/* COPIAR — valor GCoins */
.coinflip-room__value {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(14px, 1.8vw, 20px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
}

/* COPIAR — botao ENTRAR (dentro do card) */
.coinflip-room__btn-join {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: transparent;
  border: 1px solid #D4A843;
  border-radius: clamp(4px, 0.5vw, 6px);
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  transition: all 0.2s ease;
}
.coinflip-room__btn-join:hover {
  background: rgba(212,168,67,0.08);
  border-color: #FFD700;
  color: #FFD700;
}
.coinflip-room__btn-join:active {
  transform: scale(0.97);
}

/* COPIAR — badge AGUARDANDO (propria sala, substitui btn-join) */
.coinflip-room__badge-waiting {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(0,230,118,0.08);
  border: 1px solid rgba(0,230,118,0.2);
  border-radius: clamp(4px, 0.5vw, 6px);
  color: #00E676;
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  text-align: center;
  animation: cfWaitingPulse 2s ease-in-out infinite;
}

/* COPIAR — badge HIGH STAKE (canto do card) */
.coinflip-room__badge-high {
  position: absolute;
  top: clamp(6px, 0.8vw, 10px);
  left: clamp(6px, 0.8vw, 10px);
  background: rgba(255,23,68,0.15);
  border: 1px solid rgba(255,23,68,0.3);
  border-radius: clamp(3px, 0.4vw, 6px);
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(8px, 0.9vw, 10px);
  font-weight: 600;
  color: #FF1744;
  text-transform: uppercase;
  z-index: 2;
}

/* COPIAR — botao X cancelar (canto superior direito, so propria sala) */
.coinflip-room__btn-x {
  position: absolute;
  top: clamp(6px, 0.8vw, 10px);
  right: clamp(6px, 0.8vw, 10px);
  width: clamp(24px, 3vw, 32px);
  height: clamp(24px, 3vw, 32px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 4px;
  background: transparent;
  color: #A8A8A8;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  z-index: 2;
  transition: all 0.2s ease;
}
.coinflip-room__btn-x:hover {
  border-color: #FF1744;
  color: #FF1744;
  background: rgba(255,23,68,0.05);
}
.coinflip-room__btn-x:active {
  transform: scale(0.9);
}
```

---

# 10. HISTORICO RECENTE DO LOBBY (DS P2 sec10 tabelas)

```css
/* COPIAR EXATAMENTE — secao na base do lobby */
.coinflip-lobby__recent {
  border-top: 1px solid rgba(212,168,67,0.1);
  padding: clamp(8px, 1vw, 12px) clamp(8px, 1.5vw, 16px);
  flex-shrink: 0;
}

/* COPIAR — titulo "ULTIMAS PARTIDAS" */
.coinflip-lobby__recent-title {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 500;
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: clamp(6px, 0.8vw, 10px);
}

/* COPIAR — linha individual */
.coinflip-lobby__recent-row {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  border-radius: 4px;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 12px);
  color: #A8A8A8;
  transition: background 0.2s ease;
}

/* COPIAR — linha win */
.coinflip-lobby__recent-row--win {
  background: rgba(0,230,118,0.03);
}

/* COPIAR — linha loss */
.coinflip-lobby__recent-row--loss {
  background: rgba(255,23,68,0.03);
}

/* COPIAR — nome vencedor destaque */
.coinflip-lobby__recent-winner {
  color: #00E676;
  font-weight: 600;
}

/* COPIAR — tempo relativo */
.coinflip-lobby__recent-time {
  color: #666666;
  font-size: clamp(9px, 1vw, 11px);
  margin-left: auto;
}
```

---

**[FIM DA PARTE 1 — Continua com Componentes 11-21 + Responsive + Checklist]**

`[ENTREGA: 7.CSS-COMPONENTES-COINFLIP-PARTE-1.md — 20/03/2026]`
