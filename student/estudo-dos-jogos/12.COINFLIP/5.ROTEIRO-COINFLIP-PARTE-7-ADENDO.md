# ADENDO -- ROTEIRO DE DESENVOLVIMENTO COINFLIP (#12)
# PARTE 7 -- CoinflipResultOverlay TSX real (item 5.14)
# Complementa: 5.ROTEIRO P1-P6 = 2.508L
# Blackout Casino GTARP - 26/03/2026

---

# COMPONENTE: CoinflipResultOverlay.tsx (Fase 5 -- codigo real)

Pesquisa X0: CSGORoll "PvP 3-part seed (public+server+nonce)".
CSGOEmpire "CT vs T coin flip, winner takes all".
Payout 1.9x (2x - 5% rake). Lobby rooms instant-match.

```tsx
import { motion, AnimatePresence, useMotionValue, useTransform, animate } from "framer-motion";
import { useEffect, useRef } from "react";

interface CoinflipResultOverlayProps {
  show: boolean;
  isWinner: boolean;
  winnerName: string;
  loserName: string;
  potValue: number;
  coinResult: "heads" | "tails";
  playerSide: "heads" | "tails";
  lang: "br" | "en";
  onRevanche: () => void;
  onClose: () => void;
}

export default function CoinflipResultOverlay({
  show, isWinner, winnerName, loserName, potValue, coinResult, playerSide, lang, onRevanche, onClose,
}: CoinflipResultOverlayProps) {
  const countRef = useRef<HTMLSpanElement>(null);
  const countValue = useMotionValue(0);
  const rounded = useTransform(countValue, (v) => Math.floor(v).toLocaleString());

  useEffect(() => {
    if (!show || !isWinner || potValue <= 0) return;
    const ctrl = animate(countValue, potValue, { duration: 2, ease: "easeOut" });
    return () => ctrl.stop();
  }, [show, isWinner, potValue, countValue]);

  useEffect(() => {
    if (!countRef.current) return;
    const unsub = rounded.on("change", (v) => {
      if (countRef.current) countRef.current.textContent = `G$ ${v}`;
    });
    return unsub;
  }, [rounded]);

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "fixed", inset: 0,
            background: "rgba(0,0,0,0.9)",
            backdropFilter: "blur(12px)",
            display: "flex", flexDirection: "column",
            alignItems: "center", justifyContent: "center",
            zIndex: 1000,
          }}
        >
          {/* Confetti (winner) */}
          {isWinner && Array.from({ length: 30 }).map((_, i) => (
            <motion.div
              key={`c-${i}`}
              initial={{ opacity: 1, scale: 0, x: 0, y: 0 }}
              animate={{
                opacity: [1, 1, 0], scale: [0, 1, 0.3],
                x: (Math.random() - 0.5) * 400, y: (Math.random() - 0.5) * 400,
              }}
              transition={{ duration: 1.5, delay: Math.random() * 0.3 }}
              style={{
                position: "absolute",
                width: "clamp(4px,0.5vw,7px)", height: "clamp(4px,0.5vw,7px)",
                borderRadius: "50%",
                background: Math.random() > 0.5 ? "#FFD700" : "#00E676",
                pointerEvents: "none",
              }}
            />
          ))}

          {/* Title */}
          <motion.div
            initial={{ scale: 0 }}
            animate={{ scale: 1 }}
            transition={{ type: "spring", stiffness: 200, damping: 12 }}
            title={isWinner
              ? (lang === "br" ? "Voce ganhou o coinflip!" : "You won the coinflip!")
              : (lang === "br" ? "Voce perdeu o coinflip" : "You lost the coinflip")
            }
            style={{
              fontFamily: "'Cinzel', serif",
              fontSize: "clamp(24px,3.5vw,42px)", fontWeight: 700,
              color: isWinner ? "#FFD700" : "#FF4444",
              textTransform: "uppercase", letterSpacing: "4px",
              textShadow: isWinner
                ? "0 0 30px rgba(255,215,0,0.5)"
                : "0 0 20px rgba(255,68,68,0.3)",
            }}
          >
            {isWinner
              ? (lang === "br" ? "VITORIA!" : "VICTORY!")
              : (lang === "br" ? "DERROTA" : "DEFEAT")
            }
          </motion.div>

          {/* Coin result */}
          <motion.div
            initial={{ rotateY: 720, scale: 0 }}
            animate={{ rotateY: 0, scale: 1 }}
            transition={{ type: "spring", stiffness: 150, damping: 15, delay: 0.3 }}
            title={`${coinResult === "heads" ? (lang === "br" ? "Cara" : "Heads") : (lang === "br" ? "Coroa" : "Tails")}`}
            style={{
              width: "clamp(64px,8vw,96px)", height: "clamp(64px,8vw,96px)",
              borderRadius: "50%",
              background: coinResult === "heads"
                ? "linear-gradient(135deg, #FFD700, #D4A843)"
                : "linear-gradient(135deg, #C0C0C0, #808080)",
              border: "3px solid rgba(255,255,255,0.2)",
              display: "flex", alignItems: "center", justifyContent: "center",
              fontFamily: "'Cinzel', serif",
              fontSize: "clamp(10px,1.2vw,14px)", fontWeight: 700,
              color: coinResult === "heads" ? "#0A0A0A" : "#FFFFFF",
              marginTop: "clamp(8px,1vw,14px)",
              boxShadow: "0 0 20px rgba(212,168,67,0.3)",
            }}
          >
            {coinResult === "heads"
              ? (lang === "br" ? "CARA" : "HEADS")
              : (lang === "br" ? "COROA" : "TAILS")
            }
          </motion.div>

          {/* Winner/Loser names */}
          <motion.div
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.6 }}
            style={{
              display: "flex", gap: "clamp(16px,2vw,24px)",
              marginTop: "clamp(10px,1.5vw,16px)",
            }}
          >
            <div style={{
              textAlign: "center", opacity: isWinner ? 1 : 0.4,
              transform: isWinner ? "scale(1.05)" : "scale(0.95)",
            }}>
              <div style={{ fontFamily: "'Inter',sans-serif", fontSize: "clamp(10px,1.1vw,13px)", color: "#00E676" }}>
                {winnerName}
              </div>
              <div style={{ fontFamily: "'Cinzel',serif", fontSize: "clamp(8px,0.9vw,10px)", color: "rgba(0,230,118,0.7)" }}>
                WINNER
              </div>
            </div>
            <div style={{ color: "rgba(255,255,255,0.2)", fontFamily: "'Cinzel',serif", fontSize: "clamp(10px,1.2vw,14px)" }}>
              VS
            </div>
            <div style={{
              textAlign: "center", opacity: isWinner ? 0.4 : 1,
              transform: isWinner ? "scale(0.95)" : "scale(1.05)",
            }}>
              <div style={{ fontFamily: "'Inter',sans-serif", fontSize: "clamp(10px,1.1vw,13px)", color: "#FF4444" }}>
                {loserName}
              </div>
              <div style={{ fontFamily: "'Cinzel',serif", fontSize: "clamp(8px,0.9vw,10px)", color: "rgba(255,68,68,0.7)" }}>
                LOSER
              </div>
            </div>
          </motion.div>

          {/* Pot value (winner) */}
          {isWinner && potValue > 0 && (
            <motion.div
              initial={{ opacity: 0, y: 15 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.9 }}
              title={lang === "br" ? `Pot: G$ ${potValue.toLocaleString()}` : `Pot: G$ ${potValue.toLocaleString()}`}
              style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: "clamp(22px,3vw,36px)", fontWeight: 700,
                color: "#00E676", textShadow: "0 0 20px rgba(0,230,118,0.4)",
                marginTop: "clamp(8px,1vw,14px)",
              }}
            >
              <img src="/assets/coinflip/icons/icon-gcoin.png" alt=""
                style={{ width: 20, height: 20, marginRight: 6, verticalAlign: "middle" }} />
              <span ref={countRef}>G$ 0</span>
            </motion.div>
          )}

          {/* Buttons */}
          <div style={{ display: "flex", gap: 12, marginTop: "clamp(14px,1.5vw,20px)" }}>
            <motion.button
              initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.5 }}
              onClick={onRevanche} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
              title={lang === "br" ? "Revanche" : "Rematch"}
              style={{
                padding: "clamp(8px,1vw,12px) clamp(16px,2vw,28px)",
                background: "linear-gradient(135deg, #00E676, #00C853)",
                border: "none", borderRadius: 8, color: "#0A0A0A",
                fontFamily: "'Cinzel',serif", fontSize: "clamp(10px,1.1vw,13px)",
                fontWeight: 700, textTransform: "uppercase", cursor: "pointer", minHeight: 44,
              }}
            >
              {lang === "br" ? "REVANCHE" : "REMATCH"}
            </motion.button>
            <motion.button
              initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.5 }}
              onClick={onClose} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
              title={lang === "br" ? "Voltar ao lobby" : "Back to lobby"}
              style={{
                padding: "clamp(8px,1vw,12px) clamp(16px,2vw,28px)",
                background: "transparent",
                border: "1.5px solid rgba(212,168,67,0.4)",
                borderRadius: 8, color: "#D4A843",
                fontFamily: "'Cinzel',serif", fontSize: "clamp(10px,1.1vw,13px)",
                textTransform: "uppercase", cursor: "pointer", minHeight: 44,
              }}
            >
              LOBBY
            </motion.button>
          </div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

---

`[ENTREGA: 5.ROTEIRO-COINFLIP-PARTE-7-ADENDO.md -- 26/03/2026]`
