# CSS COMPONENTES — COINFLIP (#12)
## Parte 2 de 2 - Componentes 11-21 + Responsive + Checklist Final
## Colar JUNTO com Parte 1 e PROMPT-V0 ao gerar visual no V0/Bolt
### Blackout Casino GTARP - 20/03/2026

---

# 11. MODAL GLASSMORPHISM (DS P2 sec8.2 — Criar, Entrar, PF)

```css
/* COPIAR EXATAMENTE — overlay escuro */
.coinflip-modal-overlay {
  position: fixed;
  inset: 0;
  z-index: 100;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — container modal padrao */
.coinflip-modal {
  background: rgba(13,13,13,0.95);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: clamp(8px, 1vw, 16px);
  padding: clamp(20px, 2.5vw, 36px);
  width: clamp(280px, 35vw, 420px);
  max-height: 90vh;
  overflow-y: auto;
  position: relative;
  box-shadow: 0 0 40px rgba(0,0,0,0.5), 0 0 80px rgba(212,168,67,0.05);
}

/* COPIAR — modal wide (Provably Fair) */
.coinflip-modal--wide {
  width: clamp(300px, 40vw, 480px);
}

/* COPIAR — titulo do modal */
.coinflip-modal__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 20px);
  color: #D4A843;
  text-transform: uppercase;
  text-shadow: 0 0 12px rgba(212,168,67,0.3);
}

/* COPIAR — botao [X] fechar (Lucide permitido aqui) */
.coinflip-modal__close {
  position: absolute;
  top: clamp(12px, 1.5vw, 20px);
  right: clamp(12px, 1.5vw, 20px);
  width: 36px; height: 36px;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  background: transparent;
  color: #A8A8A8;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  transition: all 0.2s ease;
}
.coinflip-modal__close:hover {
  border-color: rgba(212,168,67,0.5);
  color: #D4A843;
}
.coinflip-modal__close:active {
  transform: scale(0.9);
}

/* COPIAR — label generico de modal (ex: "VALOR DA APOSTA") */
.coinflip-modal__label {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 500;
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: clamp(4px, 0.5vw, 8px);
}

/* COPIAR — scrollbar do modal */
.coinflip-modal::-webkit-scrollbar { width: 4px; }
.coinflip-modal::-webkit-scrollbar-track { background: transparent; }
.coinflip-modal::-webkit-scrollbar-thumb { background: rgba(212,168,67,0.2); border-radius: 2px; }
```

---

# 12. INPUT DE APOSTA (DS P2 sec9.1)

```css
/* COPIAR EXATAMENTE */
.coinflip-bet-input {
  width: 100%;
  background: rgba(10,10,10,0.8);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: clamp(6px, 0.8vw, 10px);
  padding: clamp(12px, 1.5vw, 20px);
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(20px, 2.5vw, 32px);
  font-weight: 700;
  color: #00E676;
  text-align: center;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
  outline: none;
  transition: all 0.2s ease;
}
.coinflip-bet-input:focus {
  border-color: rgba(0,230,118,0.4);
  box-shadow: 0 0 12px rgba(0,230,118,0.15);
}
.coinflip-bet-input::placeholder {
  color: #666666;
  text-shadow: none;
}

/* COPIAR — estado erro (valor invalido / saldo insuficiente) */
.coinflip-bet-input--error {
  border-color: #FF1744;
  box-shadow: 0 0 12px rgba(255,23,68,0.15);
  color: #FF1744;
}
```

---

# 13. ATALHOS VALOR (MIN, x2, div2, MAX — DS P2 sec9.2)

```css
/* COPIAR EXATAMENTE */
.coinflip-shortcut {
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 1.5vw, 18px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  background: transparent;
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 12px);
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-shortcut:hover {
  background: rgba(212,168,67,0.08);
  border-color: rgba(212,168,67,0.4);
}
.coinflip-shortcut:active {
  transform: scale(0.95);
}
```

---

# 14. SIDE CARDS ESCOLHA LADO (DS P2 sec9.5 — CARA e COROA)

```css
/* COPIAR EXATAMENTE — card nao selecionado */
.coinflip-side-card {
  background: #141414;
  border: 2px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 14px);
  padding: clamp(12px, 1.5vw, 24px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 12px);
  cursor: pointer;
  opacity: 0.5;
  transition: all 0.3s ease;
}
.coinflip-side-card:hover {
  opacity: 0.75;
  transform: translateY(-3px);
}
.coinflip-side-card:active {
  transform: scale(0.97);
}

/* COPIAR — CARA selecionado */
.coinflip-side-card--cara-active {
  opacity: 1;
  border-color: #00E676;
  box-shadow: 0 0 16px rgba(0,230,118,0.15);
}
.coinflip-side-card--cara-active:hover {
  opacity: 1;
  box-shadow: 0 0 24px rgba(0,230,118,0.2);
}

/* COPIAR — COROA selecionado */
.coinflip-side-card--coroa-active {
  opacity: 1;
  border-color: #FFD700;
  box-shadow: 0 0 16px rgba(255,215,0,0.15);
}
.coinflip-side-card--coroa-active:hover {
  opacity: 1;
  box-shadow: 0 0 24px rgba(255,215,0,0.2);
}

/* COPIAR — imagem da moeda dentro do side card */
.coinflip-side-card__coin {
  width: clamp(56px, 7vw, 88px);
  height: clamp(56px, 7vw, 88px);
  border-radius: 50%;
}

/* COPIAR — label do lado */
.coinflip-side-card__label--cara {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  color: #00E676;
  text-transform: uppercase;
  font-size: clamp(11px, 1.3vw, 15px);
}
.coinflip-side-card__label--coroa {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  color: #FFD700;
  text-transform: uppercase;
  font-size: clamp(11px, 1.3vw, 15px);
}
```

---

# 15. MOEDA 3D (DS P2 sec11 coin component)

```css
/* COPIAR EXATAMENTE — perspective container */
.coinflip-coin {
  width: clamp(100px, 14vw, 180px);
  height: clamp(100px, 14vw, 180px);
  perspective: 1000px;
  position: relative;
}

/* COPIAR — inner que rotaciona */
.coinflip-coin__inner {
  width: 100%; height: 100%;
  position: relative;
  transform-style: preserve-3d;
  transition: transform 2.5s cubic-bezier(0.15, 0.05, 0.25, 1.0);
}

/* COPIAR — face CARA (frente, visivel por default) */
.coinflip-coin__face--cara {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  border-radius: 50%;
  background-image: url('/assets/coinflip/coin-cara.png');
  background-size: cover;
  background-position: center;
  box-shadow: 0 0 20px rgba(255,215,0,0.2),
              inset 0 -4px 8px rgba(0,0,0,0.3),
              inset 0 4px 8px rgba(255,215,0,0.15);
}

/* COPIAR — face COROA (verso, rotada 180deg) */
.coinflip-coin__face--coroa {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  border-radius: 50%;
  background-image: url('/assets/coinflip/coin-coroa.png');
  background-size: cover;
  background-position: center;
  box-shadow: 0 0 20px rgba(255,215,0,0.2),
              inset 0 -4px 8px rgba(0,0,0,0.3),
              inset 0 4px 8px rgba(255,215,0,0.15);
  transform: rotateY(180deg);
}

/* COPIAR — glow durante spin */
.coinflip-coin__glow {
  position: absolute;
  inset: -10%;
  border-radius: 50%;
  pointer-events: none;
  animation: cfCoinGlow 0.5s ease-in-out infinite;
}

/* COPIAR — drop shadow dinamica */
.coinflip-coin__shadow {
  position: absolute;
  bottom: -12%;
  left: 15%;
  width: 70%;
  height: 10%;
  border-radius: 50%;
  pointer-events: none;
  background: radial-gradient(ellipse, rgba(0,0,0,0.4) 0%, transparent 70%);
}
```

---

# 16. FLIP CONTAINER (DS P3 sec12 — tela de animacao)

```css
/* COPIAR EXATAMENTE */
.coinflip-flip {
  position: absolute; inset: 0;
  z-index: 70;
  background-color: rgba(5,5,5,0.95);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
  padding: clamp(16px, 2vw, 32px);
}

/* COPIAR — pot total no topo */
.coinflip-flip__pot {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(18px, 2.5vw, 30px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.4);
}

/* COPIAR — area central (jogador1 + moeda + jogador2) */
.coinflip-flip__arena {
  display: flex;
  align-items: center;
  gap: clamp(24px, 5vw, 60px);
}

/* COPIAR — card jogador (esquerdo e direito) */
.coinflip-flip__player {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
}

/* COPIAR — avatar no flip (CARA) */
.coinflip-flip__avatar--cara {
  width: clamp(48px, 6vw, 72px);
  height: clamp(48px, 6vw, 72px);
  border-radius: 50%;
  border: 3px solid #00E676;
  object-fit: cover;
}

/* COPIAR — avatar no flip (COROA) */
.coinflip-flip__avatar--coroa {
  width: clamp(48px, 6vw, 72px);
  height: clamp(48px, 6vw, 72px);
  border-radius: 50%;
  border: 3px solid #FFD700;
  object-fit: cover;
}

/* COPIAR — barra de suspense */
.coinflip-flip__bar {
  width: 80%;
  height: clamp(3px, 0.4vw, 5px);
  border-radius: 3px;
  background: #1A1A1A;
  overflow: hidden;
}
.coinflip-flip__bar-fill {
  width: 100%; height: 100%;
  background: linear-gradient(90deg, #D4A843, #FFD700, #00E676);
  animation: cfSuspenseFill 2.5s ease-in-out forwards;
}

/* COPIAR — countdown (3, 2, 1) */
.coinflip-flip__countdown {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(48px, 8vw, 96px);
  color: #D4A843;
  text-shadow: 0 0 30px rgba(255,215,0,0.5), 0 0 60px rgba(255,215,0,0.2);
}
```

---

# 17. RESULTADO (DS P3 sec14-16 — vencedor e perdedor)

```css
/* COPIAR EXATAMENTE — flash branco instantaneo */
.coinflip-result__flash {
  position: absolute; inset: 0;
  z-index: 80;
  background: #FFFFFF;
  pointer-events: none;
  animation: cfFlash 0.2s ease-out forwards;
}

/* COPIAR — avatar vencedor com glow */
.coinflip-result__avatar--winner {
  width: clamp(56px, 7vw, 80px);
  height: clamp(56px, 7vw, 80px);
  border-radius: 50%;
  border: 3px solid #FFD700;
  object-fit: cover;
  animation: cfWinnerGlow 1.5s ease-in-out infinite;
}

/* COPIAR — badge VENCEDOR */
.coinflip-result__badge--winner {
  background: rgba(255,215,0,0.15);
  border: 1px solid rgba(255,215,0,0.3);
  border-radius: 6px;
  padding: clamp(4px, 0.5vw, 8px) clamp(10px, 1.2vw, 18px);
  color: #FFD700;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  text-transform: uppercase;
  font-size: clamp(10px, 1.2vw, 14px);
}

/* COPIAR — payout positivo */
.coinflip-result__payout--win {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(24px, 3.5vw, 40px);
  color: #00E676;
  text-shadow: 0 0 16px rgba(0,230,118,0.5), 0 0 40px rgba(0,230,118,0.2);
}

/* COPIAR — mini coin particles (6 voam do centro) */
.coinflip-result__particle {
  position: absolute;
  width: clamp(12px, 1.5vw, 18px);
  height: clamp(12px, 1.5vw, 18px);
  border-radius: 50%;
  background: radial-gradient(circle, #FFD700, #D4A843);
  box-shadow: 0 0 6px rgba(255,215,0,0.5);
  animation: cfParticleFall 0.6s ease-out forwards;
}

/* COPIAR — container perdedor (opacity reduzida) */
.coinflip-result__loser {
  opacity: 0.4;
}

/* COPIAR — avatar perdedor */
.coinflip-result__avatar--loser {
  width: clamp(48px, 6vw, 72px);
  height: clamp(48px, 6vw, 72px);
  border-radius: 50%;
  border: 2px solid rgba(212,168,67,0.15);
  object-fit: cover;
  filter: grayscale(30%);
}

/* COPIAR — payout negativo */
.coinflip-result__payout--lose {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 2vw, 24px);
  color: #FF1744;
  opacity: 0.6;
}
```

---

# 18. HISTORY ROW (DS P2 sec10.4 — overlay historico)

```css
/* COPIAR EXATAMENTE — linha de partida */
.coinflip-history__row {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1vw, 12px);
  padding: clamp(8px, 1vw, 14px) clamp(10px, 1.2vw, 16px);
  background: #111111;
  border: 1px solid rgba(212,168,67,0.08);
  border-radius: clamp(6px, 0.8vw, 10px);
  cursor: pointer;
  transition: background 0.2s ease;
}
.coinflip-history__row:hover {
  background: #1A1A1A;
}

/* COPIAR — badge W (vitoria) */
.coinflip-history__badge--w {
  background: rgba(0,230,118,0.15);
  color: #00E676;
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: 6px;
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 700;
  min-width: 28px;
  text-align: center;
}

/* COPIAR — badge L (derrota) */
.coinflip-history__badge--l {
  background: rgba(255,23,68,0.08);
  color: #FF1744;
  border: 1px solid rgba(255,23,68,0.15);
  border-radius: 6px;
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 700;
  min-width: 28px;
  text-align: center;
}

/* COPIAR — profit positivo */
.coinflip-history__profit--positive {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #00E676;
}

/* COPIAR — profit negativo */
.coinflip-history__profit--negative {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #FF1744;
}

/* COPIAR — resultado CARA */
.coinflip-history__result--cara {
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  text-transform: uppercase;
  color: #00E676;
}

/* COPIAR — resultado COROA */
.coinflip-history__result--coroa {
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  text-transform: uppercase;
  color: #FFD700;
}
```

---

# 19. LEADERBOARD ROW (DS P2 sec10.5 — overlay ranking)

```css
/* COPIAR EXATAMENTE — grid header */
.coinflip-lb__header {
  display: grid;
  grid-template-columns: clamp(30px, 4vw, 50px) 1fr clamp(50px, 7vw, 80px) clamp(40px, 5vw, 60px) clamp(80px, 10vw, 120px);
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(8px, 1vw, 12px) clamp(10px, 1.2vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.15);
}
.coinflip-lb__header-cell {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1vw, 11px);
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

/* COPIAR — linha de jogador */
.coinflip-lb__row {
  display: grid;
  grid-template-columns: clamp(30px, 4vw, 50px) 1fr clamp(50px, 7vw, 80px) clamp(40px, 5vw, 60px) clamp(80px, 10vw, 120px);
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(8px, 1vw, 12px) clamp(10px, 1.2vw, 16px);
  align-items: center;
  transition: background 0.2s ease;
}
.coinflip-lb__row:hover {
  background: rgba(212,168,67,0.03);
}

/* COPIAR — posicao #1 dourado */
.coinflip-lb__pos--1 {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #FFD700;
}

/* COPIAR — posicao #2 prata */
.coinflip-lb__pos--2 {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #C0C0C0;
}

/* COPIAR — posicao #3 bronze */
.coinflip-lb__pos--3 {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #CD7F32;
}

/* COPIAR — posicao #4+ normal */
.coinflip-lb__pos {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  color: #A8A8A8;
}

/* COPIAR — avatar mini do ranking */
.coinflip-lb__avatar {
  width: clamp(24px, 3vw, 32px);
  height: clamp(24px, 3vw, 32px);
  border-radius: 50%;
  object-fit: cover;
}
.coinflip-lb__avatar--gold { border: 2px solid #FFD700; }
.coinflip-lb__avatar--silver { border: 2px solid #C0C0C0; }
.coinflip-lb__avatar--bronze { border: 2px solid #CD7F32; }
```

---

# 20. PROVABLY FAIR FIELDS (DS P3 sec19 — campos hash)

```css
/* COPIAR EXATAMENTE — campo hash readonly */
.coinflip-pf__field {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  background: rgba(10,10,10,0.8);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(4px, 0.5vw, 8px);
  padding: clamp(8px, 1vw, 12px);
  transition: border-color 0.3s ease;
}

/* COPIAR — texto hash */
.coinflip-pf__hash {
  flex: 1;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(9px, 1vw, 11px);
  color: #A8A8A8;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  user-select: all;
}

/* COPIAR — botao copiar */
.coinflip-pf__btn-copy {
  width: clamp(28px, 3.5vw, 36px);
  height: clamp(28px, 3.5vw, 36px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 4px;
  background: transparent;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  min-width: 44px;
  transition: all 0.2s ease;
}
.coinflip-pf__btn-copy:hover {
  border-color: rgba(212,168,67,0.5);
}
.coinflip-pf__btn-copy img {
  width: 16px; height: 16px;
}

/* COPIAR — estado copiado (verde 1s) */
.coinflip-pf__btn-copy--copied {
  border-color: #00E676;
}
.coinflip-pf__field--copied {
  border-color: rgba(0,230,118,0.3);
}

/* COPIAR — bloco formula */
.coinflip-pf__formula {
  background: rgba(10,10,10,0.6);
  border: 1px solid rgba(212,168,67,0.1);
  border-radius: clamp(6px, 0.8vw, 10px);
  padding: clamp(10px, 1.2vw, 16px);
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(9px, 1vw, 11px);
  color: #A8A8A8;
  line-height: 1.8;
  white-space: pre-line;
}

/* COPIAR — badge JUSTO */
.coinflip-pf__badge--fair {
  width: 100%;
  padding: clamp(10px, 1.2vw, 16px);
  background: rgba(0,230,118,0.08);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: clamp(6px, 0.8vw, 10px);
  color: #00E676;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(12px, 1.4vw, 16px);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(6px, 0.8vw, 10px);
}
.coinflip-pf__badge--fair img { width: 20px; height: 20px; }

/* COPIAR — badge ALERTA (mismatch) */
.coinflip-pf__badge--alert {
  width: 100%;
  padding: clamp(10px, 1.2vw, 16px);
  background: rgba(255,23,68,0.08);
  border: 1px solid rgba(255,23,68,0.3);
  border-radius: clamp(6px, 0.8vw, 10px);
  color: #FF1744;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(12px, 1.4vw, 16px);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(6px, 0.8vw, 10px);
}
```

---

# 21. SEPARADORES VISUAIS

```css
/* COPIAR — feixo verde sutil */
.coinflip-divider--green {
  width: 100%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(0,230,118,0.15), transparent);
}

/* COPIAR — linha dourada horizontal */
.coinflip-divider--gold {
  width: 100%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(212,168,67,0.15), transparent);
}
```

---

# 22. RESPONSIVE (REGRA X4)

```css
/* Tudo ja usa clamp(), grid auto-fill adapta, modais com min/max width.
   Nao precisa de @media queries.

   Resumo dos clamps de cada camada:

   Fontes:
     Titulos Cinzel:  clamp(14px, 2vw, 22px)
     Labels Inter:    clamp(9px, 1vw, 11px)
     Valores Mono:    clamp(14px, 1.8vw, 22px)
     Payout grande:   clamp(24px, 3.5vw, 40px)
     Countdown:       clamp(48px, 8vw, 96px)

   Sizing:
     Moeda 3D:        clamp(100px, 14vw, 180px)
     Avatar lobby:    clamp(36px, 4.5vw, 56px)
     Avatar flip:     clamp(48px, 6vw, 72px)
     Avatar result:   clamp(56px, 7vw, 80px)
     Avatar lb:       clamp(24px, 3vw, 32px)
     Modal width:     clamp(280px, 35vw, 420px)
     Modal wide:      clamp(300px, 40vw, 480px)

   Grid:
     Lobby cards:     repeat(auto-fill, minmax(clamp(140px,18vw,200px), 1fr))
     Leaderboard:     grid-template-columns com 5 colunas clamp

   Touch:
     TODOS botoes:    min-height: 44px
*/
```

---

# CHECKLIST FINAL (42 itens)

## Superficies e Cores (8 itens)

| # | Item | Verificar |
|---|------|-----------|
| 1 | Fundo #0A0A0A com bg-casino.png + 2 radial-gradients | .coinflip-game |
| 2 | Card surface #141414 | .coinflip-room |
| 3 | Modal surface rgba(13,13,13,0.95) | .coinflip-modal |
| 4 | CARA sempre #00E676 (labels, bordas, avatares) | --cf-cara |
| 5 | COROA sempre #FFD700 (labels, bordas, avatares) | --cf-coroa |
| 6 | GCoins sempre #00E676 | .coinflip-room__value |
| 7 | Profit positivo #00E676, negativo #FF1744 | history + lb |
| 8 | Vinheta inset 150px | .coinflip-game box-shadow |

## Tipografia (7 itens)

| 9 | Cinzel em titulos e botoes primarios | --cf-font-title |
| 10 | Inter em corpo e labels | --cf-font-body |
| 11 | JetBrains Mono em valores e hashes | --cf-font-mono |
| 12 | TODOS tamanhos com clamp() | 0 valores px fixos |
| 13 | text-shadow gold em titulos | .coinflip-game__title |
| 14 | text-shadow green em valores | .coinflip-game__balance |
| 15 | letter-spacing em labels uppercase | .coinflip-modal__label |

## Bordas e Shadows (6 itens)

| 16 | 3 niveis de border-gold (0.15, 0.3, 0.5) | tokens semanticos |
| 17 | shadow-card e shadow-card-hover | .coinflip-room |
| 18 | shadow-modal com gold glow | .coinflip-modal |
| 19 | Moeda 3 shadow states (idle, spin, landed) | tokens |
| 20 | Backdrop-filter blur em modais | .coinflip-modal-overlay |
| 21 | -webkit-backdrop-filter (CEF compat) | todos blur |

## @keyframes (8 itens)

| 22 | cfShimmer — sweep nos cards | room :hover |
| 23 | cfWaitingPulse — border verde propria sala | badge-waiting |
| 24 | cfCoinGlow — glow moeda spin | coin__glow |
| 25 | cfWinnerGlow — glow avatar vencedor | avatar--winner |
| 26 | cfSuspenseFill — barra preenche | flip__bar-fill |
| 27 | cfFlash — branco no resultado | result__flash |
| 28 | cfSpin — spinner loading | btn-primary--loading |
| 29 | cfParticleFall — coin burst vitoria | result__particle |

## Interatividade / Tooltips (7 itens — REGRA X5)

| 30 | Botao voltar: 3 estados | normal, :hover, :active |
| 31 | Botao primario: 4 estados | normal, :hover, :active, --disabled, --loading |
| 32 | Botao gold: 3 estados | normal, :hover, :active |
| 33 | Room card: 4 variantes | waiting, high-stake, own, completed |
| 34 | Side card: 3 estados | inativo, :hover, --cara-active, --coroa-active |
| 35 | Input: 3 estados | normal, :focus, --error |
| 36 | PF copy: 2 estados | normal, --copied |

## Responsividade Mobile (5 itens — REGRA X4)

| 37 | Lobby grid auto-fill com clamp | .coinflip-lobby__grid |
| 38 | Modal width com clamp min 280px | .coinflip-modal |
| 39 | Moeda escala 100-180px | .coinflip-coin |
| 40 | Avatars escalam por tela | 4 tamanhos |
| 41 | Touch 44px em TODOS botoes | min-height 44px |

## Geral

| 42 | 21 componentes numerados + responsive | Completo |

---

**VALIDACAO: 21 componentes numerados (0-21) + responsive (22). 8 @keyframes. Tokens :root completos. 42 itens checklist.**

`[ENTREGA: 7.CSS-COMPONENTES-COINFLIP-PARTE-2.md — 20/03/2026]`
