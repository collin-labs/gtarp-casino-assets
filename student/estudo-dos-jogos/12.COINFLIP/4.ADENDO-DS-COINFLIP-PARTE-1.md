# ADENDO DESIGN SYSTEM — COINFLIP (#12)
## PARTE 1 DE 4 — Componentes CSS Real (1.1-1.8)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 20/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Adendo DS do Padrao de Estudo Individual
- **Gold Standard consultado:** Adendo DS Slot-Machine #2 (4 partes, 1.974 linhas, 25 componentes), Adendo DS Poker #6 (4 partes, 2.525 linhas, 25 componentes)
- **Benchmark de linhas:** 1.500-2.000 linhas (4 partes)
- **DS consultado:** DS-PARTE-1-FUNDAMENTOS (§2-§6), DS-PARTE-2-COMPONENTES (§8-§12), DS-PARTE-3A-ANIMACOES (§20-§23)
- **Componentes previstos:** 20 componentes com CSS real copiavel + 5 atmosferas + 4 timelines + checklist 45 itens

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1-4) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com §ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 20 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.coinflip-[block]__[element]--[modifier]`
- Blocks: `coinflip-game`, `coinflip-lobby`, `coinflip-room`, `coinflip-flip`, `coinflip-coin`, `coinflip-result`, `coinflip-modal`, `coinflip-history`, `coinflip-leaderboard`, `coinflip-pf`
- Modifiers: `--idle`, `--waiting`, `--high-stake`, `--own`, `--active`, `--spinning`, `--landed`, `--cara`, `--coroa`, `--winner`, `--loser`, `--disabled`, `--expanded`, `--selected`, `--loading`

**Pesquisa REGRA X0:** BEM naming 2025-2026 confirmado como padrao dominante (getbem.com, sparkbox.com Fev/2025). `.block__element--modifier` com double underscore e double dash. backdrop-filter funciona no CEF Chromium do FiveM (docs.fivem.net). perspective + rotateY + backfaceVisibility confirmado funcional no CEF (caniuse.com 98%+ browsers). Framer Motion 11 layout animation confirmada para reflow de grid ao filtrar (motion.dev docs 2025).

---

# SECAO 1 — COMPONENTES COM CSS REAL (20 Componentes)

## 1.1 Container Principal do Jogo (DS P1 §4.1 + §4.2)

Quando o Coinflip abre dentro do painel (z-index 60), o fundo NUNCA e flat:

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Coinflip */
.coinflip-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
}

/* COPIAR — fallback NUI (sem bg-casino.png) */
.coinflip-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}

/* COPIAR — estado flip ativo (lobby escurecido atras) */
.coinflip-game--flipping .coinflip-lobby {
  opacity: 0.15;
  pointer-events: none;
  filter: blur(4px);
  transition: all 0.3s ease;
}
```

---

## 1.2 Header Bar (DS P2 §15.2 adaptado)

```css
/* COPIAR EXATAMENTE — barra superior VOLTAR + titulo + icones + saldo */
.coinflip-game__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
  z-index: 5;
}

/* COPIAR — botao VOLTAR */
.coinflip-game__btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px;
  cursor: pointer;
  transition: all 0.25s ease;
}
.coinflip-game__btn-back:hover {
  border-color: rgba(212,168,67,0.8);
  background: rgba(0,0,0,0.8);
}
.coinflip-game__btn-back:active {
  transform: scale(0.95);
}

/* COPIAR — titulo "CARA OU COROA" */
.coinflip-game__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
}

/* COPIAR — icones do header (historico, leaderboard) */
.coinflip-game__header-icon {
  width: clamp(32px, 4vw, 44px);
  height: clamp(32px, 4vw, 44px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  background: transparent;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  transition: all 0.2s ease;
}
.coinflip-game__header-icon:hover {
  border-color: rgba(212,168,67,0.5);
  background: rgba(212,168,67,0.05);
}
.coinflip-game__header-icon:active {
  transform: scale(0.95);
}
.coinflip-game__header-icon img {
  width: clamp(16px, 2vw, 22px);
  height: clamp(16px, 2vw, 22px);
  /* Icones PNG dourados: icon-history.png, icon-leaderboard.png */
}

/* COPIAR — saldo GCoin */
.coinflip-game__balance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
```

---

## 1.3 Barra de Filtros (DS P2 §8.8 adaptado)

```css
/* COPIAR EXATAMENTE — container filtros + botao criar */
.coinflip-lobby__filters {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
}

/* COPIAR — filtro button inativo */
.coinflip-lobby__filter {
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 1.5vw, 20px);
  border-radius: 8px;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 500;
  cursor: pointer;
  border: 1px solid rgba(212,168,67,0.2);
  background: transparent;
  color: #D4A843;
  min-height: 44px;
  transition: all 0.2s ease;
}
.coinflip-lobby__filter:hover {
  background: rgba(212,168,67,0.08);
  border-color: rgba(212,168,67,0.3);
}
.coinflip-lobby__filter:active {
  transform: scale(0.97);
}

/* COPIAR — filtro button ativo */
.coinflip-lobby__filter--active {
  background: rgba(212,168,67,0.15);
  border-color: #FFD700;
  color: #FFD700;
}

/* COPIAR — botao + CRIAR SALA (DS P2 §8.1 botao primario) */
.coinflip-lobby__btn-create {
  margin-left: auto;
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 2px;
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2vw, 28px);
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
  transition: all 0.25s ease;
}
.coinflip-lobby__btn-create:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.coinflip-lobby__btn-create:active {
  transform: translateY(1px);
}
.coinflip-lobby__btn-create--disabled {
  background: #222222;
  border-color: rgba(255,255,255,0.06);
  color: #666666;
  box-shadow: none;
  cursor: not-allowed;
  opacity: 0.5;
}
```

---

## 1.4 Grid de Salas (DS P1 §4.2 adaptado)

```css
/* COPIAR EXATAMENTE — grid responsivo de room cards */
.coinflip-lobby__grid {
  flex: 1;
  overflow-y: auto;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(clamp(140px, 18vw, 200px), 1fr));
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(8px, 1vw, 16px) clamp(8px, 1.5vw, 16px);
  align-content: start;
}

/* COPIAR — scrollbar customizada */
.coinflip-lobby__grid::-webkit-scrollbar {
  width: 4px;
}
.coinflip-lobby__grid::-webkit-scrollbar-track {
  background: transparent;
}
.coinflip-lobby__grid::-webkit-scrollbar-thumb {
  background: rgba(212,168,67,0.2);
  border-radius: 2px;
}

/* COPIAR — lobby vazio */
.coinflip-lobby__empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(32px, 5vw, 64px);
  grid-column: 1 / -1;
}
.coinflip-lobby__empty-icon {
  width: clamp(48px, 6vw, 80px);
  height: clamp(48px, 6vw, 80px);
  opacity: 0.3;
}
.coinflip-lobby__empty-text {
  font-family: 'Inter', sans-serif;
  font-size: clamp(11px, 1.2vw, 14px);
  color: #666666;
  text-align: center;
}
```

---

## 1.5 Room Card — 4 Estados (DS P1 §4.4 adaptado)

O RoomCard e o componente visual mais importante do lobby. 4 estados visuais distintos:

```css
/* COPIAR EXATAMENTE — room card base (estado WAITING) */
.coinflip-room {
  background: #141414;
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(12px, 1.5vw, 20px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  position: relative;
  overflow: hidden;
  cursor: pointer;
  transition: all 0.3s ease;
}
.coinflip-room:hover {
  border-color: rgba(212,168,67,0.3);
  transform: translateY(-4px);
  box-shadow: 0 8px 30px rgba(0,0,0,0.4);
}
.coinflip-room:active {
  transform: translateY(0px);
}

/* COPIAR — shimmer sweep no hover (DS P1 §4.4 shine sweep) */
.coinflip-room::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,215,0,0.03), transparent);
  transition: left 0.6s ease;
  pointer-events: none;
}
.coinflip-room:hover::after {
  left: 100%;
}

/* COPIAR — estado HIGH STAKE (valor > 10.000 GC) */
.coinflip-room--high-stake {
  border-color: rgba(212,168,67,0.5);
  box-shadow: 0 0 20px rgba(212,168,67,0.1);
}
.coinflip-room--high-stake:hover {
  border-color: rgba(212,168,67,0.7);
  box-shadow: 0 0 30px rgba(212,168,67,0.15), 0 8px 30px rgba(0,0,0,0.4);
}

/* COPIAR — estado OWN ROOM (sua sala, aguardando) */
.coinflip-room--own {
  border-color: rgba(0,230,118,0.3);
  box-shadow: 0 0 12px rgba(0,230,118,0.05);
  cursor: default;
}
.coinflip-room--own:hover {
  transform: none;
  box-shadow: 0 0 12px rgba(0,230,118,0.05);
}

/* COPIAR — estado COMPLETED (historico recente, so leitura) */
.coinflip-room--completed {
  opacity: 0.6;
  cursor: default;
}
.coinflip-room--completed:hover {
  transform: none;
  border-color: rgba(212,168,67,0.15);
  box-shadow: none;
}

/* COPIAR — avatar do criador */
.coinflip-room__avatar {
  width: clamp(36px, 4.5vw, 56px);
  height: clamp(36px, 4.5vw, 56px);
  border-radius: 50%;
  border: 2px solid #D4A843;
  object-fit: cover;
}

/* COPIAR — nickname */
.coinflip-room__nick {
  font-family: 'Cinzel', serif;
  font-size: clamp(10px, 1.2vw, 14px);
  font-weight: 600;
  color: #F5F5F5;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
}

/* COPIAR — indicador de lado */
.coinflip-room__side {
  display: flex;
  align-items: center;
  gap: clamp(4px, 0.5vw, 6px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 12px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.coinflip-room__side--cara {
  color: #00E676;
}
.coinflip-room__side--coroa {
  color: #FFD700;
}
.coinflip-room__side-icon {
  width: clamp(14px, 1.8vw, 20px);
  height: clamp(14px, 1.8vw, 20px);
  border-radius: 50%;
}

/* COPIAR — valor GCoins */
.coinflip-room__value {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(14px, 1.8vw, 20px);
  font-weight: 700;
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.3);
}

/* COPIAR — botao ENTRAR (DS P2 §8.3 botao secundario adaptado) */
.coinflip-room__btn-join {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: transparent;
  border: 1px solid #D4A843;
  border-radius: clamp(4px, 0.5vw, 6px);
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 13px);
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  transition: all 0.2s ease;
}
.coinflip-room__btn-join:hover {
  background: rgba(212,168,67,0.08);
  border-color: #FFD700;
  color: #FFD700;
}
.coinflip-room__btn-join:active {
  transform: scale(0.97);
}
```

---

## 1.6 Badges do Room Card (DS P2 §10.2 adaptado)

```css
/* COPIAR EXATAMENTE — badge AGUARDANDO (pulso verde, propria sala) */
.coinflip-room__badge-waiting {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(0,230,118,0.08);
  border: 1px solid rgba(0,230,118,0.2);
  border-radius: clamp(4px, 0.5vw, 6px);
  color: #00E676;
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  text-align: center;
  /* Pulso via Framer Motion: borderColor oscila 0.2 -> 0.5 -> 0.2 em 2s loop */
}

/* COPIAR — badge HIGH STAKE (canto superior esquerdo) */
.coinflip-room__badge-high {
  position: absolute;
  top: clamp(6px, 0.8vw, 10px);
  left: clamp(6px, 0.8vw, 10px);
  background: rgba(255,23,68,0.15);
  border: 1px solid rgba(255,23,68,0.3);
  border-radius: clamp(3px, 0.4vw, 6px);
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Inter', sans-serif;
  font-size: clamp(8px, 0.9vw, 10px);
  font-weight: 600;
  color: #FF1744;
  text-transform: uppercase;
  z-index: 2;
}

/* COPIAR — botao X cancelar (canto superior direito, so propria sala) */
.coinflip-room__btn-cancel {
  position: absolute;
  top: clamp(6px, 0.8vw, 10px);
  right: clamp(6px, 0.8vw, 10px);
  width: clamp(24px, 3vw, 32px);
  height: clamp(24px, 3vw, 32px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 4px;
  background: transparent;
  color: #A8A8A8;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  z-index: 2;
  transition: all 0.2s ease;
}
.coinflip-room__btn-cancel:hover {
  border-color: #FF1744;
  color: #FF1744;
  background: rgba(255,23,68,0.05);
}
.coinflip-room__btn-cancel:active {
  transform: scale(0.9);
}
```

---

## 1.7 Historico Recente do Lobby (DS P2 §10 tabelas adaptado)

```css
/* COPIAR EXATAMENTE — secao na base do lobby */
.coinflip-lobby__recent {
  border-top: 1px solid rgba(212,168,67,0.1);
  padding: clamp(8px, 1vw, 12px) clamp(8px, 1.5vw, 16px);
  flex-shrink: 0;
}

/* COPIAR — titulo "ULTIMAS PARTIDAS" */
.coinflip-lobby__recent-title {
  font-family: 'Inter', sans-serif;
  font-size: clamp(9px, 1vw, 11px);
  font-weight: 500;
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: clamp(6px, 0.8vw, 10px);
}

/* COPIAR — linha individual */
.coinflip-lobby__recent-row {
  display: flex;
  align-items: center;
  gap: clamp(6px, 0.8vw, 10px);
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 12px);
  border-radius: 4px;
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 1.1vw, 12px);
  color: #A8A8A8;
  transition: background 0.2s ease;
}

/* COPIAR — linha win */
.coinflip-lobby__recent-row--win {
  background: rgba(0,230,118,0.03);
}

/* COPIAR — linha loss */
.coinflip-lobby__recent-row--loss {
  background: rgba(255,23,68,0.03);
}

/* COPIAR — nome do vencedor na linha (destaque) */
.coinflip-lobby__recent-winner {
  color: #00E676;
  font-weight: 600;
}

/* COPIAR — tempo relativo ("2min", "1h") */
.coinflip-lobby__recent-time {
  color: #666666;
  font-size: clamp(9px, 1vw, 11px);
  margin-left: auto;
}
```

---

## 1.8 Modal Base Glassmorphism (DS P1 §4.3 + DS P2 §12.1)

Todas modais do Coinflip (Criar, Entrar, PF) usam esta base:

```css
/* COPIAR EXATAMENTE — overlay escurecido */
.coinflip-modal__overlay {
  position: fixed;
  inset: 0;
  z-index: 100;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — container da modal */
.coinflip-modal {
  background: rgba(13,13,13,0.95);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: clamp(8px, 1vw, 16px);
  padding: clamp(20px, 2.5vw, 36px);
  width: clamp(280px, 35vw, 420px);
  max-height: 90vh;
  overflow-y: auto;
  position: relative;
  box-shadow:
    0 0 40px rgba(0,0,0,0.5),
    0 0 80px rgba(212,168,67,0.05);
}

/* COPIAR — modal mais larga (Provably Fair) */
.coinflip-modal--wide {
  width: clamp(300px, 40vw, 480px);
}

/* COPIAR — titulo da modal */
.coinflip-modal__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 20px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  text-shadow: 0 0 12px rgba(212,168,67,0.3);
  margin-bottom: clamp(16px, 2vw, 24px);
}

/* COPIAR — botao fechar [X] (Lucide icon — UNICO uso de Lucide) */
.coinflip-modal__close {
  position: absolute;
  top: clamp(16px, 2vw, 24px);
  right: clamp(16px, 2vw, 24px);
  width: 36px; height: 36px;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  background: transparent;
  color: #A8A8A8;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 44px; min-width: 44px;
  transition: all 0.2s ease;
}
.coinflip-modal__close:hover {
  border-color: rgba(212,168,67,0.4);
  color: #D4A843;
}
.coinflip-modal__close:active {
  transform: scale(0.9);
}

/* COPIAR — label pequena dentro de modais */
.coinflip-modal__label {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1vw, 11px);
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: clamp(6px, 0.8vw, 8px);
}

/* COPIAR — scrollbar da modal */
.coinflip-modal::-webkit-scrollbar {
  width: 3px;
}
.coinflip-modal::-webkit-scrollbar-track {
  background: transparent;
}
.coinflip-modal::-webkit-scrollbar-thumb {
  background: rgba(212,168,67,0.15);
  border-radius: 2px;
}
```

---

**[FIM DA PARTE 1 — Continua com Componentes 1.9-1.16 (Modal Criar, Modal Entrar, Coin 3D, Flip, Countdown, Resultado, Botoes)]**

`[ENTREGA: 4.ADENDO-DS-COINFLIP-PARTE-1.md — 20/03/2026]`
