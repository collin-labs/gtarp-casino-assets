# 🎨 ADENDO DS — CASE BATTLE
## CSS real dos componentes · Referência §exata ao Design System
### Blackout Casino · Jogo #11 · 11/03/2026

---

## COMO USAR ESTE DOCUMENTO

Este adendo contém CSS REAL copiável para cada componente do Case Battle. Cada classe referencia a seção exata do Design System de onde foi derivada. Ao implementar, COPIAR o CSS daqui — não inventar.

Usar JUNTO com o Prompt AI (Partes 1+2) que descreve o layout e comportamento.

---

# ═══════════════════════════════════════════════════
# 1. FUNDO DO CASE BATTLE (ref: DS P1 §4.1, §6.1-6.4)
# ═══════════════════════════════════════════════════

```css
.battle-background {
  position: absolute;
  inset: 0;
  z-index: 60;
  border-radius: inherit;
  overflow: hidden;
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212, 168, 67, 0.03) 0%, transparent 50%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0, 230, 118, 0.02) 0%, transparent 50%);
}

.battle-background::before {
  content: '';
  position: absolute;
  inset: 0;
  box-shadow: inset 0 0 80px rgba(0, 0, 0, 0.5);
  pointer-events: none;
  z-index: 1;
}

.battle-scanline {
  position: absolute;
  left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, rgba(0, 230, 118, 0.1) 20%, rgba(0, 230, 118, 0.2) 50%, rgba(0, 230, 118, 0.1) 80%, transparent 100%);
  animation: scanline 8s linear infinite;
  pointer-events: none;
  z-index: 100;
  opacity: 0.3;
}

@keyframes scanline {
  0%   { top: -2px; }
  100% { top: 100%; }
}
```

---

# ═══════════════════════════════════════════════════
# 2. CARD DE BATALHA NO LOBBY (ref: DS P1 §4.4, §5.1, Guia P3 §2.3.3)
# ═══════════════════════════════════════════════════

```css
.battle-card {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1vw, 16px);
  padding: clamp(8px, 1vw, 14px);
  border-radius: 10px;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  background: linear-gradient(180deg, #1A1A1A, #141414);
  border: 1px solid rgba(212, 168, 67, 0.15);
  box-shadow: 0 2px 8px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.03), inset 0 -1px 0 rgba(0,0,0,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.battle-card:hover {
  border-color: rgba(212, 168, 67, 0.3);
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.4), 0 0 12px rgba(212,168,67,0.08), inset 0 1px 0 rgba(255,255,255,0.05);
}

.battle-card::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 10%; right: 10%;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, rgba(0,230,118,0.2) 20%, #00E676 50%, rgba(0,230,118,0.2) 80%, transparent 100%);
  box-shadow: 0 0 6px rgba(0,230,118,0.2);
  opacity: 0.6;
  transition: opacity 0.2s ease;
}

.battle-card:hover::after { opacity: 1; }

.battle-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 45%, rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.03) 55%, transparent 60%);
  transform: translateX(-100%);
  transition: transform 0.6s ease;
  pointer-events: none;
  z-index: 2;
}

.battle-card:hover::before { transform: translateX(50%); }
```

---

# ═══════════════════════════════════════════════════
# 3. BADGES DE MODO (ref: DS P2 §10)
# ═══════════════════════════════════════════════════

```css
.battle-badge {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  padding: 2px 8px;
  border-radius: 4px;
  font-family: var(--font-body);
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  white-space: nowrap;
}

.battle-badge-classic { background: rgba(212,168,67,0.1); border: 1px solid rgba(212,168,67,0.2); color: #D4A843; }
.battle-badge-underdog { background: rgba(255,107,0,0.12); border: 1px solid rgba(255,107,0,0.3); color: #FF6B00; }
.battle-badge-free { background: rgba(0,230,118,0.1); border: 1px solid rgba(0,230,118,0.3); color: #00E676; }
.battle-badge-private { background: rgba(212,168,67,0.08); border: 1px solid rgba(212,168,67,0.15); color: rgba(212,168,67,0.6); }
```

---

# ═══════════════════════════════════════════════════
# 4. PLAYER CIRCLES / SLOTS (ref: DS P1 §5, P2 §10)
# ═══════════════════════════════════════════════════

```css
.player-circle {
  width: clamp(24px, 2.8vw, 34px);
  height: clamp(24px, 2.8vw, 34px);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-body);
  font-size: clamp(8px, 0.8vw, 11px);
  font-weight: 700;
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.player-circle-filled {
  background: linear-gradient(135deg, #D4A843, #8B6914);
  border: 2px solid rgba(212,168,67,0.5);
  color: #0A0A0A;
  box-shadow: 0 0 8px rgba(212,168,67,0.2);
}

.player-circle-empty {
  background: transparent;
  border: 2px dashed rgba(255,255,255,0.1);
  color: #444;
  animation: circlePulse 2s ease-in-out infinite;
}

@keyframes circlePulse {
  0%, 100% { border-color: rgba(255,255,255,0.1); }
  50%      { border-color: rgba(212,168,67,0.2); }
}

.player-slot-waiting {
  width: clamp(80px, 12vw, 140px);
  height: clamp(90px, 13vw, 160px);
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 12px;
}

.player-slot-waiting.filled {
  background: linear-gradient(180deg, #1A1A1A, #111);
  border: 2px solid rgba(0,230,118,0.3);
  box-shadow: 0 0 12px rgba(0,230,118,0.15);
}

.player-slot-waiting.empty {
  background: rgba(0,0,0,0.2);
  border: 2px dashed rgba(212,168,67,0.15);
}
```

---

# ═══════════════════════════════════════════════════
# 5. VS SEPARATOR (ref: DS P1 §5.3, §7.5)
# ═══════════════════════════════════════════════════

```css
.battle-vs-text {
  font-family: var(--font-display);
  font-size: clamp(20px, 3vw, 36px);
  font-weight: 900;
  color: #FFD700;
  letter-spacing: 3px;
  text-shadow: 0 0 8px rgba(255,215,0,0.5), 0 0 16px rgba(255,215,0,0.25), 0 0 32px rgba(255,215,0,0.1), 0 2px 4px rgba(0,0,0,0.5);
  animation: vsGlow 2s ease-in-out infinite;
}

@keyframes vsGlow {
  0%, 100% { text-shadow: 0 0 8px rgba(255,215,0,0.5), 0 0 16px rgba(255,215,0,0.25); }
  50%      { text-shadow: 0 0 12px rgba(255,215,0,0.7), 0 0 24px rgba(255,215,0,0.4); }
}

.battle-vs-line {
  height: 1px;
  background: linear-gradient(90deg, transparent 0%, rgba(212,168,67,0.3) 20%, rgba(255,215,0,0.5) 50%, rgba(212,168,67,0.3) 80%, transparent 100%);
}

.battle-lane-separator {
  width: 1px;
  background: linear-gradient(180deg, transparent 0%, rgba(255,215,0,0.2) 20%, rgba(255,215,0,0.4) 50%, rgba(255,215,0,0.2) 80%, transparent 100%);
}
```

---

# ═══════════════════════════════════════════════════
# 6. REVEAL STRIP (ref: DS P1 §4.2, P3A §18, §20)
# ═══════════════════════════════════════════════════

```css
.reveal-strip-container {
  position: relative;
  width: 100%;
  height: clamp(60px, 8vw, 90px);
  overflow: hidden;
  border-radius: 8px;
  background: linear-gradient(180deg, #0F0F0F, #141414);
  border: 1px solid rgba(212,168,67,0.2);
  box-shadow: inset 0 0 20px rgba(0,0,0,0.5);
}

.reveal-strip-inner {
  display: flex;
  align-items: center;
  height: 100%;
  padding: 4px 0;
  will-change: transform;
}

.reveal-strip-marker {
  position: absolute;
  top: 0; bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 3px;
  z-index: 10;
  background: linear-gradient(180deg, #FFD700, #D4A843, #FFD700);
  box-shadow: 0 0 12px rgba(255,215,0,0.4);
}

.reveal-strip-marker::before {
  content: '';
  position: absolute;
  top: -1px;
  left: 50%;
  transform: translateX(-50%);
  width: 0; height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-top: 8px solid #FFD700;
  filter: drop-shadow(0 0 4px rgba(255,215,0,0.5));
}

.reveal-strip-container::before, .reveal-strip-container::after {
  content: '';
  position: absolute;
  top: 0; bottom: 0;
  width: 60px;
  z-index: 5;
  pointer-events: none;
}
.reveal-strip-container::before { left: 0; background: linear-gradient(90deg, #0F0F0F, transparent); }
.reveal-strip-container::after { right: 0; background: linear-gradient(270deg, #0F0F0F, transparent); }

.reveal-strip-item {
  flex-shrink: 0;
  width: clamp(55px, 7vw, 80px);
  height: clamp(50px, 6.5vw, 75px);
  border-radius: 6px;
  margin: 0 3px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  background: linear-gradient(180deg, #1A1A1A, #111);
  border: 1px solid var(--rarity-color, rgba(255,255,255,0.1));
  overflow: hidden;
}

.reveal-strip-item-winner {
  border-width: 2px;
  box-shadow: 0 0 12px var(--rarity-color), 0 0 24px rgba(var(--rarity-rgb), 0.3);
  animation: itemRevealPulse 2s ease-in-out infinite;
}

@keyframes itemRevealPulse {
  0%, 100% { box-shadow: 0 0 8px var(--rarity-color); }
  50%      { box-shadow: 0 0 16px var(--rarity-color), 0 0 32px rgba(var(--rarity-rgb), 0.2); }
}
```

**GSAP:** `ease: "power3.out"` (DS §18 --ease-wheel), duration 5s, onUpdate → tick sound

---

# ═══════════════════════════════════════════════════
# 7. ROUND BAR (ref: DS P2 §16.3)
# ═══════════════════════════════════════════════════

```css
.battle-round-label {
  font-family: var(--font-display);
  font-size: clamp(10px, 1.1vw, 14px);
  font-weight: 700;
  color: #D4A843;
  letter-spacing: 1.5px;
}

.battle-round-progress {
  flex: 1;
  height: 6px;
  background: #1A1A1A;
  border-radius: 3px;
  overflow: hidden;
  border: 1px solid rgba(212,168,67,0.08);
}

.battle-round-progress-fill {
  height: 100%;
  border-radius: 3px;
  background: linear-gradient(90deg, #004D25, #00E676);
  box-shadow: 0 0 8px rgba(0,230,118,0.3);
  transition: width 0.3s ease;
}
```

---

# ═══════════════════════════════════════════════════
# 8. SCORE BAR (ref: DS P2 §16.3 adaptado)
# ═══════════════════════════════════════════════════

```css
.battle-score-bar {
  position: relative;
  height: clamp(24px, 3vw, 32px);
  border-radius: 6px;
  overflow: hidden;
  border: 1px solid rgba(212,168,67,0.15);
  background: #0F0F0F;
  display: flex;
}

.battle-score-fill-p1 {
  height: 100%;
  background: linear-gradient(90deg, #D4A843, rgba(212,168,67,0.6));
  transition: width 500ms ease;
}

.battle-score-fill-p2 {
  height: 100%;
  background: linear-gradient(270deg, #00E676, rgba(0,230,118,0.6));
  transition: width 500ms ease;
}

.battle-score-fill-p1.leading { box-shadow: 0 0 8px rgba(212,168,67,0.4); }
.battle-score-fill-p2.leading { box-shadow: 0 0 8px rgba(0,230,118,0.4); }
```

---

# ═══════════════════════════════════════════════════
# 9. RESULTADO (ref: DS P3A §20.2, §20.3)
# ═══════════════════════════════════════════════════

```css
.battle-winner-lane {
  border: 2px solid rgba(255,215,0,0.5);
  animation: pulseGold 2s ease-in-out infinite;
  background: linear-gradient(180deg, rgba(255,215,0,0.05), #111);
}

@keyframes pulseGold {
  0%, 100% { box-shadow: 0 0 8px rgba(255,215,0,0.3); }
  50%      { box-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.15); }
}

.battle-winner-text {
  font-family: var(--font-display);
  font-size: clamp(14px, 1.6vw, 20px);
  font-weight: 900;
  color: #FFD700;
  letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 0 24px rgba(255,215,0,0.3);
  animation: bigWinEntrance 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes bigWinEntrance {
  0%   { transform: scale(0.3); opacity: 0; filter: blur(10px); }
  60%  { transform: scale(1.1); opacity: 1; filter: blur(0); }
  80%  { transform: scale(0.95); }
  100% { transform: scale(1); }
}

.battle-loser-lane { opacity: 0.5; border: 1px solid rgba(255,23,68,0.2); }

@keyframes flashGreen {
  0% { background-color: transparent; }
  15% { background-color: rgba(0,230,118,0.15); }
  100% { background-color: transparent; }
}

@keyframes flashRed {
  0% { background-color: transparent; }
  15% { background-color: rgba(255,23,68,0.2); }
  100% { background-color: transparent; }
}

.battle-prize-total {
  font-family: var(--font-mono);
  font-size: clamp(22px, 3vw, 36px);
  font-weight: 700;
  color: #FFD700;
  text-shadow: 0 0 12px rgba(255,215,0,0.5), 0 0 24px rgba(255,215,0,0.2);
}
```

---

# ═══════════════════════════════════════════════════
# 10. COUNTDOWN (ref: DS P3A §20.2, P3B §25.3)
# ═══════════════════════════════════════════════════

```css
.battle-countdown-number {
  font-family: var(--font-display);
  font-size: clamp(48px, 8vw, 80px);
  font-weight: 900;
  color: #FFD700;
  text-shadow: 0 0 16px rgba(255,215,0,0.6), 0 0 32px rgba(255,215,0,0.3), 0 4px 8px rgba(0,0,0,0.5);
  animation: countdownPop 0.3s cubic-bezier(0.68, -0.55, 0.27, 1.55);
}

@keyframes countdownPop {
  0%   { transform: scale(1.5); opacity: 0; }
  60%  { transform: scale(0.9); }
  100% { transform: scale(1); opacity: 1; }
}

.battle-countdown-label {
  font-family: var(--font-body);
  font-size: clamp(10px, 1.2vw, 14px);
  color: #A8A8A8;
  letter-spacing: 2px;
  text-transform: uppercase;
}
```

---

# ═══════════════════════════════════════════════════
# CHECKLIST TOOLTIPS — REGRA X5 (27 itens)
# ═══════════════════════════════════════════════════

| # | Tela | Elemento | Tooltip BR | Tooltip EN |
|---|------|----------|-----------|-----------|
| 1 | Lobby | Btn VOLTAR | Voltar ao catálogo | Back to catalog |
| 2 | Lobby | Btn CRIAR | Criar nova batalha PvP | Create new PvP battle |
| 3 | Lobby | Btn ENTRAR | Entrar (G$X) | Join (G$X) |
| 4 | Lobby | Badge GRÁTIS | Sem custo! | No cost! |
| 5 | Lobby | Badge UNDERDOG | Menor valor vence! | Lowest wins! |
| 6 | Lobby | Badge PRIVADA | Por convite | By invite |
| 7 | Lobby | Filtros | Filtrar tipo | Filter type |
| 8 | Criar | Card caixa | Adicionar caixa | Add case |
| 9 | Criar | Toggle Classic | Maior vence | Highest wins |
| 10 | Criar | Toggle Underdog | Menor vence | Lowest wins |
| 11 | Criar | 1v1 | 2 jogadores | 2 players |
| 12 | Criar | 2v2 | Times de 2 | Teams of 2 |
| 13 | Criar | 4P | Todos contra todos | Free for all |
| 14 | Criar | Privada | Só convidados | Invited only |
| 15 | Criar | Cashback | % aos perdedores | % to losers |
| 16 | Criar | CONFIRMAR | Criar e pagar G$X | Create and pay G$X |
| 17 | Criar | CANCELAR | Voltar sem criar | Back without creating |
| 18 | Espera | Slot vazio | Aguardando... | Waiting... |
| 19 | Espera | Slot cheio | Pronto! | Ready! |
| 20 | Espera | CANCELAR | Reembolso | Refund |
| 21 | Espera | COPIAR LINK | Copiar link | Copy link |
| 22 | Espera | Seed hash | Verificar após batalha | Verify after battle |
| 23 | Espera | Client seed | Sua randomização | Your randomization |
| 24 | Result | NOVA BATALHA | Criar nova | Create new |
| 25 | Result | LOBBY | Voltar à lista | Back to list |
| 26 | Result | FAIRNESS | Verificar (PF) | Verify (PF) |
| 27 | Result | Cashback | X% devolvido | X% returned |

---

# ═══════════════════════════════════════════════════
# VALIDAÇÃO (REGRA X11)
# ═══════════════════════════════════════════════════

- [x] 10 componentes com CSS REAL copiável
- [x] Cada componente referencia §exata do DS
- [x] 10 keyframes incluídos (scanline, circlePulse, vsGlow, itemRevealPulse, pulseGold, bigWinEntrance, flashGreen, flashRed, countdownPop, counterPulse referenciado)
- [x] Responsividade com clamp() em TODOS os tamanhos
- [x] 27 tooltips BR/EN (REGRA X5)
- [x] Formato Markdown (REGRA X8)

**STATUS: ✅ ADENDO DS COMPLETO**

---

`[ENTREGA: ADENDO DS — CASE BATTLE — CONCLUÍDA]`
