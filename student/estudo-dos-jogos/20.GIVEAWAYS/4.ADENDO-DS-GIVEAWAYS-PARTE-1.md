# ADENDO DESIGN SYSTEM — GIVEAWAYS (#20)
## PARTE 1 DE 4 — Componentes CSS Real (1.1-1.8)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 25/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Adendo DS do Padrao de Estudo Individual
- **Gold Standard consultado:** Adendo DS Coinflip #12 (4 partes, 2.176 linhas, 20 componentes, 5 atmosferas, 4 timelines, 45 itens checklist)
- **Benchmark de linhas:** 1.500-2.000 linhas (4 partes)
- **DS consultado:** DS-PARTE-1-FUNDAMENTOS (§1-§6), DS-PARTE-2-COMPONENTES (§8-§12), DS-PARTE-3A-ANIMACOES (§20-§23)
- **Componentes previstos:** 16 componentes com CSS real copiavel + 4 atmosferas + 3 timelines + checklist 40+ itens
- **Pesquisa X0:** BEM naming 2025/2026 (getbem.com, sparkbox.com, valoremreply.com), CSS box-shadow multi-layer neon glow (joshwcomeau.com, MDN, testmuai.com), CSS flip clock countdown rotateX (cssscript.com, codepen shshaw, 1stwebdesigner.com)
- **Descobertas X0:**
  (1) BEM `.block__element--modifier` confirmado padrao dominante 2025 — reduce code review time 35%
  (2) Multi-layer neon glow: stack box-shadow 5px/10px/20px/40px mesma cor = glow premium
  (3) Flip clock: perspective 400px + rotateX(-180deg) + backface-visibility hidden + z-index flip
  (4) Dark glassmorphism: rgba(255,255,255,0.05) bg + blur 12px + border rgba(255,255,255,0.1)
  (5) inset box-shadow 0 0 150px rgba(0,0,0,0.8) = vinheta escura como no Gold Standard

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1-4) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com § ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 16 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.giveaway-[block]__[element]--[modifier]`
- Blocks: `giveaway-game`, `giveaway-list`, `giveaway-card`, `giveaway-countdown`, `giveaway-progress`, `giveaway-badge`, `giveaway-modal`, `giveaway-drum`, `giveaway-winner`, `giveaway-history`, `giveaway-pf`, `giveaway-ticket`
- Modifiers: `--idle`, `--active`, `--urgent`, `--drawing`, `--completed`, `--cancelled`, `--winner-self`, `--winner-other`, `--disabled`, `--free`, `--loading`, `--esgotado`, `--selected`

---

# SECAO 1 — COMPONENTES COM CSS REAL (16 Componentes)

## 1.1 Container Principal do Jogo (DS P1 §4.1 + §4.2)

Quando o Giveaways abre dentro do painel (z-index 60), o fundo NUNCA e flat:

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Giveaways */
.giveaway-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
}

/* COPIAR — fallback NUI (sem bg-casino.png) */
.giveaway-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}

/* COPIAR — estado draw ativo (lista escurecida atras) */
.giveaway-game--drawing .giveaway-list {
  opacity: 0.15;
  pointer-events: none;
  filter: blur(4px);
  transition: all 0.3s ease;
}
```

---

## 1.2 Header Bar (DS P2 §15.2 adaptado)

```css
/* COPIAR EXATAMENTE — barra superior VOLTAR + titulo + icones */
.giveaway-game__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
  z-index: 5;
}

/* COPIAR — botao VOLTAR */
.giveaway-game__btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px;
  cursor: pointer;
  transition: all 0.2s ease;
}
.giveaway-game__btn-back:hover {
  border-color: #FFD700;
  color: #FFD700;
  box-shadow: 0 0 12px rgba(255,215,0,0.15);
}
.giveaway-game__btn-back:active {
  transform: scale(0.95);
}

/* COPIAR — titulo SORTEIOS */
.giveaway-game__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 28px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  text-shadow: 0 2px 8px rgba(0,0,0,0.4);
}

/* COPIAR — icones header (som, info) */
.giveaway-game__icon-btn {
  background: transparent;
  border: none;
  cursor: pointer;
  min-width: 44px; min-height: 44px;
  display: flex; align-items: center; justify-content: center;
  transition: opacity 0.2s ease;
}
.giveaway-game__icon-btn:hover {
  opacity: 0.8;
}
.giveaway-game__icon-btn img {
  width: clamp(20px, 2.5vw, 28px);
  height: auto;
  filter: drop-shadow(0 1px 2px rgba(0,0,0,0.3));
}
```

---

## 1.3 Card Sorteio Ativo (DS P1 §4.4 + DS P2 §10.2 adaptado)

O card principal grande que mostra o sorteio ativo com countdown, progress bar e CTA.

```css
/* COPIAR EXATAMENTE — card sorteio principal */
.giveaway-card {
  background: linear-gradient(135deg, #141414 0%, #1A1A1A 100%);
  border: 1.5px solid rgba(212,168,67,0.25);
  border-radius: clamp(10px, 1.5vw, 16px);
  padding: clamp(16px, 2.5vw, 28px);
  position: relative;
  overflow: hidden;
  /* 5 camadas de shadow (DS P1 §4.2) */
  box-shadow:
    0 2px 0 0 rgba(0,230,118,0.4),         /* feixo verde base */
    0 4px 20px rgba(0,0,0,0.4),             /* sombra profunda */
    0 1px 3px rgba(0,0,0,0.3),              /* sombra proxima */
    inset 0 1px 0 rgba(255,255,255,0.03),   /* highlight topo */
    inset 0 -1px 0 rgba(0,0,0,0.2);         /* sombra interna base */
  transition: all 0.3s ease;
}

/* COPIAR — shine sweep (brilho que percorre o card) */
.giveaway-card::after {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.03) 50%, transparent 60%);
  background-size: 200% 100%;
  animation: shineSweep 4s infinite;
  pointer-events: none;
  border-radius: inherit;
}
@keyframes shineSweep {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* COPIAR — titulo do sorteio dentro do card */
.giveaway-card__title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  margin-bottom: clamp(4px, 0.5vw, 8px);
}

/* COPIAR — premio dentro do card */
.giveaway-card__prize {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(18px, 3vw, 32px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.3);
}

/* COPIAR — preco ticket */
.giveaway-card__ticket-price {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 500;
  font-size: clamp(12px, 1.5vw, 16px);
  color: #D4A843;
}

/* COPIAR — info tickets do jogador */
.giveaway-card__my-tickets {
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(11px, 1.3vw, 14px);
  color: #A8A8A8;
}
.giveaway-card__chance {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  color: #FFD700;
}
```

---

## 1.4 Countdown Flip Cards (DS P3A §20.1 + custom)

Componente especifico do Giveaways: 4 digitos (d:h:m:s) com flip animation ao mudar.

```css
/* COPIAR EXATAMENTE — container countdown (4 pares de digitos) */
.giveaway-countdown {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1.5vw, 16px);
}

/* COPIAR — separador : entre pares */
.giveaway-countdown__separator {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 3vw, 36px);
  color: rgba(212,168,67,0.4);
}

/* COPIAR — par de digitos (container flip individual) */
.giveaway-countdown__digit {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

/* COPIAR — flip card de um digito */
.giveaway-countdown__flip-card {
  width: clamp(36px, 5vw, 56px);
  height: clamp(44px, 6vw, 68px);
  background: #141414;
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(20px, 4vw, 48px);
  color: #D4A843;
  text-shadow: 0 2px 4px rgba(0,0,0,0.5);
  position: relative;
  perspective: 400px;
  overflow: hidden;
  box-shadow:
    0 2px 8px rgba(0,0,0,0.3),
    inset 0 1px 0 rgba(255,255,255,0.03);
}

/* COPIAR — linha horizontal central (simula split flip) */
.giveaway-countdown__flip-card::after {
  content: '';
  position: absolute; top: 50%; left: 0; right: 0;
  height: 1px;
  background: rgba(0,0,0,0.4);
  pointer-events: none;
}

/* COPIAR — label abaixo do digito (D, H, M, S) */
.giveaway-countdown__label {
  font-family: 'Inter', sans-serif;
  font-size: 10px;
  color: #666666;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

/* COPIAR — estado URGENTE (<60 segundos) */
.giveaway-countdown__flip-card--urgent {
  color: #FF1744;
  text-shadow: 0 0 10px rgba(255,23,68,0.4);
  border-color: rgba(255,23,68,0.3);
  animation: pulseUrgent 0.5s ease-in-out infinite alternate;
}
@keyframes pulseUrgent {
  from { box-shadow: 0 2px 8px rgba(0,0,0,0.3), 0 0 5px rgba(255,23,68,0.2); }
  to { box-shadow: 0 2px 8px rgba(0,0,0,0.3), 0 0 15px rgba(255,23,68,0.4); }
}

/* COPIAR — animacao flip ao mudar digito (DS P3A §20.1 adaptado) */
@keyframes flipTop {
  0% { transform: rotateX(0deg); }
  100% { transform: rotateX(-90deg); }
}
@keyframes flipBottom {
  0% { transform: rotateX(90deg); }
  100% { transform: rotateX(0deg); }
}
.giveaway-countdown__flip-top {
  animation: flipTop 0.3s ease-in forwards;
  transform-origin: bottom center;
  backface-visibility: hidden;
}
.giveaway-countdown__flip-bottom {
  animation: flipBottom 0.3s ease-out forwards;
  animation-delay: 0.15s;
  transform-origin: top center;
  backface-visibility: hidden;
}
```

---

## 1.5 Progress Bar Tickets (DS custom — componente especifico Giveaways)

```css
/* COPIAR EXATAMENTE — track da barra de progresso */
.giveaway-progress__track {
  width: 100%;
  height: clamp(6px, 1vw, 10px);
  background: #1A1A1A;
  border-radius: 999px;
  overflow: hidden;
  position: relative;
}

/* COPIAR — fill da barra (animado via width inline) */
.giveaway-progress__fill {
  height: 100%;
  background: linear-gradient(90deg, #004D25 0%, #00E676 100%);
  border-radius: 999px;
  transition: width 0.5s ease-in-out;
  box-shadow: 0 0 8px rgba(0,230,118,0.3);
}

/* COPIAR — fill quase cheio (>80%) */
.giveaway-progress__fill--nearly-full {
  box-shadow: 0 0 12px rgba(0,230,118,0.5);
}

/* COPIAR — fill esgotado (100%) */
.giveaway-progress__fill--full {
  animation: pulseGlow 1s ease-in-out infinite alternate;
}
@keyframes pulseGlow {
  from { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  to { box-shadow: 0 0 16px rgba(0,230,118,0.6); }
}

/* COPIAR — texto abaixo da barra (234/500 (46.8%)) */
.giveaway-progress__label {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(10px, 1.2vw, 13px);
  color: #A8A8A8;
  margin-top: 4px;
}

/* COPIAR — label ESGOTADO (quando 100%) */
.giveaway-progress__label--sold-out {
  color: #FF1744;
  font-weight: 600;
}
```

---

## 1.6 Badge Status (DS P2 §10.2 adaptado)

```css
/* COPIAR EXATAMENTE — badge base */
.giveaway-badge {
  display: inline-flex;
  align-items: center;
  padding: clamp(3px, 0.4vw, 5px) clamp(8px, 1vw, 12px);
  border-radius: 6px;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(9px, 1vw, 12px);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

/* COPIAR — UPCOMING (cinza, inativo) */
.giveaway-badge--upcoming {
  background: #2A2A2A;
  color: #666666;
}

/* COPIAR — ACTIVE (verde pulsante) */
.giveaway-badge--active {
  background: rgba(0,230,118,0.15);
  color: #00E676;
  animation: badgePulse 2s ease-in-out infinite alternate;
}
@keyframes badgePulse {
  from { box-shadow: 0 0 0 rgba(0,230,118,0); }
  to { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
}

/* COPIAR — URGENTE (vermelho rapido) */
.giveaway-badge--urgent {
  background: rgba(255,23,68,0.15);
  color: #FF1744;
  animation: badgePulse 0.5s ease-in-out infinite alternate;
}

/* COPIAR — DRAWING (amarelo/dourado fade) */
.giveaway-badge--drawing {
  background: rgba(255,215,0,0.15);
  color: #FFD700;
  animation: badgeFade 1s ease-in-out infinite alternate;
}
@keyframes badgeFade {
  from { opacity: 0.6; }
  to { opacity: 1; }
}

/* COPIAR — COMPLETED (dourado estatico) */
.giveaway-badge--completed {
  background: rgba(212,168,67,0.1);
  color: #D4A843;
}

/* COPIAR — CANCELLED (vermelho estatico) */
.giveaway-badge--cancelled {
  background: rgba(255,59,59,0.1);
  color: #FF3B3B;
}
```

---

## 1.7 Cards Mini — Proximos Sorteios (DS P1 §4.4 + grid)

```css
/* COPIAR EXATAMENTE — grid de cards mini */
.giveaway-list__grid-upcoming {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(clamp(160px, 20vw, 240px), 1fr));
  gap: clamp(8px, 1.5vw, 16px);
}

/* COPIAR — card mini individual */
.giveaway-card--mini {
  background: #141414;
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(10px, 1.5vw, 16px);
  cursor: pointer;
  transition: all 0.2s ease;
  display: flex; flex-direction: column;
  gap: clamp(4px, 0.5vw, 8px);
}
.giveaway-card--mini:hover {
  background: #1A1A1A;
  border-color: rgba(212,168,67,0.3);
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.3);
}
.giveaway-card--mini:active {
  transform: translateY(0);
}

/* COPIAR — titulo card mini */
.giveaway-card--mini .giveaway-card__title {
  font-size: clamp(11px, 1.3vw, 14px);
}

/* COPIAR — premio card mini */
.giveaway-card--mini .giveaway-card__prize {
  font-size: clamp(14px, 2vw, 20px);
}

/* COPIAR — countdown compacto card mini */
.giveaway-card--mini .giveaway-countdown {
  gap: clamp(4px, 0.8vw, 8px);
}
.giveaway-card--mini .giveaway-countdown__flip-card {
  width: clamp(24px, 3vw, 32px);
  height: clamp(28px, 3.5vw, 36px);
  font-size: clamp(12px, 2vw, 20px);
}
```

---

## 1.8 Botoes do Card Ativo (DS P2 §8.1 + §8.3 + custom)

```css
/* COPIAR EXATAMENTE — botao COMPRAR TICKET (DS P2 §8.1 CTA verde) */
.giveaway-card__btn-buy {
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: 10px;
  padding: clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px);
  color: #FFFFFF;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 0 12px rgba(0,230,118,0.2), 0 4px 12px rgba(0,0,0,0.3);
  transition: all 0.2s ease;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}
.giveaway-card__btn-buy:hover {
  box-shadow: 0 0 20px rgba(0,230,118,0.4), 0 6px 16px rgba(0,0,0,0.3);
  transform: scale(1.03);
}
.giveaway-card__btn-buy:active {
  transform: scale(0.97);
}
.giveaway-card__btn-buy--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  background: #1A1A1A;
  border: 1px solid #333;
  box-shadow: none;
  transform: none;
}

/* COPIAR — botao TICKET GRATIS (custom azul) */
.giveaway-card__btn-free {
  background: rgba(33,150,243,0.1);
  border: 1px solid rgba(33,150,243,0.3);
  border-radius: 10px;
  padding: clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px);
  color: #2196F3;
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(12px, 1.5vw, 16px);
  cursor: pointer;
  min-height: 44px;
  transition: all 0.2s ease;
}
.giveaway-card__btn-free:hover {
  border-color: #2196F3;
  box-shadow: 0 0 12px rgba(33,150,243,0.2);
  transform: scale(1.03);
}
.giveaway-card__btn-free:active {
  transform: scale(0.97);
}
.giveaway-card__btn-free--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  transform: none;
}

/* COPIAR — botao VOLTAR AO LOBBY (DS P2 §8.3 secundario dourado) */
.giveaway-game__btn-lobby {
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 12px;
  padding: clamp(10px, 1.5vw, 14px) clamp(20px, 4vw, 40px);
  color: #D4A843;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  cursor: pointer;
  min-height: 44px;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  transition: all 0.2s ease;
}
.giveaway-game__btn-lobby:hover {
  border-color: #FFD700;
  color: #FFD700;
  box-shadow: 0 0 12px rgba(255,215,0,0.15);
}
.giveaway-game__btn-lobby:active {
  transform: scale(0.95);
}

/* COPIAR — botao ghost (fechar [X]) (DS P2 §8.5) */
.giveaway-modal__btn-close {
  position: absolute;
  top: clamp(12px, 1.5vw, 16px);
  right: clamp(12px, 1.5vw, 16px);
  background: transparent;
  border: none;
  color: #666666;
  cursor: pointer;
  width: 24px; height: 24px;
  display: flex; align-items: center; justify-content: center;
  transition: color 0.2s ease;
}
.giveaway-modal__btn-close:hover {
  color: #FF3B3B;
}
.giveaway-modal__btn-close:active {
  transform: scale(0.9);
}
```

---

`[FIM DA PARTE 1 — Continua Parte 2: Componentes 1.9-1.16 (Modal Compra, Input Quantidade, Drum, Winner)]`

`[ENTREGA: 4.ADENDO-DS-GIVEAWAYS-PARTE-1.md — 25/03/2026]`
