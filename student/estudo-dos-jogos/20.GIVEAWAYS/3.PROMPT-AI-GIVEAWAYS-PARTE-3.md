# PROMPT AI — GIVEAWAYS (#20)
## PARTE 3 DE 4 — Tela Historico, Modal Provably Fair, Componentes Especiais
### Blackout Casino GTARP - 25/03/2026

---

## PLANO DE ACAO

- **Gold Standard consultado:** Prompt AI Coinflip P3 (579L, 3 telas), P4 (448L, componentes)
- **Benchmark desta parte:** ~400L
- **Pesquisa X0:** React paginated table dark theme, HMAC-SHA256 verification UI, Motion useAnimate sequence spring
- **O que esta parte entrega:**
  1. Tela 5: Historico Completo (tabela paginada, badges, filtro pessoal)
  2. Tela 6: Modal Provably Fair (seeds, hash, instrucoes verificacao)
  3. Componentes Especiais (Countdown Flip Card, Drum Scroll Virtual, Progress Bar Glow)

---

# TELA 5: HISTORICO COMPLETO (Estado: consultando draws passados)

## Layout ASCII

```
+================================================================+
|  [<] VOLTAR                        HISTORICO            [Som]  |
+================================================================+
|                                                                 |
|  [ Todos os Sorteios ]  [ Meus Sorteios ]      ← filtro toggle |
|                                                                 |
|  +----------------------------------------------------------+  |
|  | Titulo            | Vencedor      | Premio  | Data  | PF  |  |
|  |-------------------+---------------+---------+-------+-----|  |
|  | MEGA GIVEAWAY     | JogadorXPTO   | G$50k   | 24/03 | [V] |  |
|  | FRIDAY FRENZY     | NickABC       | G$25k   | 21/03 | [V] |  |
|  | VIP EXCLUSIVE     | Player999     | G$100k  | 18/03 | [V] |  |
|  | WEEKLY LUCKY      | UserAlpha     | G$10k   | 15/03 | [V] |  |
|  | DAILY DRAW #42    | JogadorDEF    | G$5k    | 14/03 | [V] |  |
|  +----------------------------------------------------------+  |
|                                                                 |
|  [ < Anterior ]                    Pg 1/3         [ Proximo > ] |
|                                                                 |
+================================================================+
```

## Especificacoes Tela 5

**Container historico:**
```css
{
  position: 'absolute', inset: 0, zIndex: 60,
  background: '#0A0A0A',
  borderRadius: 'inherit', overflow: 'hidden',
  display: 'flex', flexDirection: 'column',
  padding: 'clamp(12px, 2vw, 24px)',
  gap: 'clamp(12px, 2vw, 20px)'
}
```

**Toggle filtro (Todos / Meus Sorteios):**
```css
/* Container toggle */
{
  display: 'flex', gap: '8px'
}
/* Botao toggle ativo */
{
  background: 'rgba(212,168,67,0.15)',
  border: '1px solid rgba(212,168,67,0.4)',
  borderRadius: '8px',
  padding: 'clamp(6px, 1vw, 10px) clamp(12px, 2vw, 20px)',
  color: '#FFD700', fontFamily: "'Inter', sans-serif",
  fontSize: 'clamp(11px, 1.3vw, 14px)', fontWeight: 600,
  cursor: 'pointer', transition: 'all 0.2s ease'
}
/* Botao toggle inativo */
{
  background: 'transparent',
  border: '1px solid rgba(212,168,67,0.15)',
  color: '#666666'
}
```

**Tabela de historico:**
```css
/* Container tabela */
{
  display: 'flex', flexDirection: 'column',
  border: '1px solid rgba(212,168,67,0.1)',
  borderRadius: 'clamp(8px, 1.5vw, 14px)',
  overflow: 'hidden'
}
/* Header da tabela */
{
  display: 'grid',
  gridTemplateColumns: '2fr 1.5fr 1fr 0.8fr 0.5fr',
  padding: 'clamp(8px, 1.5vw, 14px) clamp(12px, 2vw, 20px)',
  background: '#141414',
  borderBottom: '1px solid rgba(212,168,67,0.15)',
  fontFamily: "'Inter', sans-serif", fontSize: 'clamp(10px, 1.2vw, 12px)',
  color: '#666666', textTransform: 'uppercase',
  letterSpacing: '0.05em', fontWeight: 600
}
/* Linha da tabela */
{
  display: 'grid',
  gridTemplateColumns: '2fr 1.5fr 1fr 0.8fr 0.5fr',
  padding: 'clamp(10px, 1.5vw, 14px) clamp(12px, 2vw, 20px)',
  borderBottom: '1px solid rgba(212,168,67,0.06)',
  alignItems: 'center',
  transition: 'background 0.15s ease'
}
/* Linha hover */
{
  background: 'rgba(212,168,67,0.03)'
}
/* Linha onde o jogador GANHOU (filtro Meus Sorteios) */
{
  background: 'rgba(0,230,118,0.03)',
  borderLeft: '3px solid #00E676'
}
/* Titulo do sorteio (coluna 1) */
{
  fontFamily: "'Inter', sans-serif", fontWeight: 600,
  fontSize: 'clamp(12px, 1.4vw, 15px)',
  color: '#FFFFFF'
}
/* Nome vencedor (coluna 2) */
{
  fontFamily: "'Inter', sans-serif", fontWeight: 500,
  fontSize: 'clamp(11px, 1.3vw, 14px)',
  color: '#D4A843'
}
/* Premio (coluna 3) */
{
  fontFamily: "'JetBrains Mono', monospace", fontWeight: 600,
  fontSize: 'clamp(12px, 1.4vw, 15px)',
  color: '#00E676',
  textShadow: '0 0 6px rgba(0,230,118,0.2)'
}
/* Data (coluna 4) */
{
  fontFamily: "'Inter', sans-serif", fontSize: 'clamp(10px, 1.2vw, 13px)',
  color: '#A8A8A8'
}
/* Botao Verificar PF (coluna 5) */
{
  background: 'transparent',
  border: '1px solid rgba(212,168,67,0.25)',
  borderRadius: '6px',
  padding: '4px 8px',
  color: '#D4A843', fontSize: 'clamp(9px, 1vw, 11px)',
  fontFamily: "'Inter', sans-serif",
  cursor: 'pointer', transition: 'all 0.2s ease',
  textAlign: 'center', minHeight: '28px',
  display: 'flex', alignItems: 'center', justifyContent: 'center'
}
/* Botao Verificar hover */
{
  borderColor: '#FFD700', color: '#FFD700',
  boxShadow: '0 0 8px rgba(255,215,0,0.15)'
}
```

**Paginacao:**
```css
/* Container paginacao */
{
  display: 'flex', justifyContent: 'space-between', alignItems: 'center',
  padding: 'clamp(8px, 1vw, 12px) 0'
}
/* Botao Anterior/Proximo */
{
  background: 'transparent',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: '8px',
  padding: 'clamp(6px, 1vw, 10px) clamp(12px, 2vw, 20px)',
  color: '#D4A843', fontFamily: "'Inter', sans-serif",
  fontSize: 'clamp(11px, 1.2vw, 14px)',
  cursor: 'pointer', minHeight: '36px',
  transition: 'all 0.2s ease'
}
/* Botao desabilitado (primeira/ultima pagina) */
{
  opacity: 0.3, cursor: 'not-allowed'
}
/* Texto pagina */
{
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: 'clamp(11px, 1.2vw, 13px)',
  color: '#A8A8A8'
}
```

## Framer Motion — Tela 5 Historico

```
ENTRADA:
  container: initial={{ opacity: 0 }}, animate={{ opacity: 1 }}, transition={{ duration: 0.3 }}
  linhas tabela: stagger 0.03s, initial={{ opacity: 0, x: -10 }}, animate={{ opacity: 1, x: 0 }}

INTERACOES:
  botao VOLTAR: whileHover={{ scale: 1.05 }}, whileTap={{ scale: 0.95 }}
  toggle filtro: whileHover={{ borderColor: '#FFD700' }}, whileTap={{ scale: 0.97 }}
  linha tabela: whileHover={{ backgroundColor: 'rgba(212,168,67,0.03)' }}
  botao PF: whileHover={{ borderColor: '#FFD700', boxShadow: '0 0 8px rgba(255,215,0,0.15)' }}
  paginacao: whileHover={{ borderColor: '#FFD700' }}, whileTap={{ scale: 0.95 }}

TROCA DE PAGINA:
  AnimatePresence mode="wait":
    exit linhas: {{ opacity: 0, x: 10 }}, transition={{ duration: 0.15 }}
    enter linhas: {{ opacity: 0, x: -10 }} → {{ opacity: 1, x: 0 }}, stagger 0.03s

TROCA DE FILTRO:
  AnimatePresence mode="wait":
    mesma animacao que troca de pagina
```

---

# TELA 6: MODAL PROVABLY FAIR (Estado: verificando resultado)

## Layout ASCII

```
+====================================================+
|            PROVABLY FAIR                     [X]    |
+====================================================+
|                                                      |
|  Sorteio: "MEGA GIVEAWAY DE NATAL"                  |
|  Vencedor: JogadorXPTO (Ticket #127)                |
|                                                      |
|  +------------------------------------------------+  |
|  | Server Seed (revelado apos draw):              |  |
|  | a4f8c2...9b3d1e          [COPIAR]              |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  | Server Seed Hash (publicado ANTES do draw):    |  |
|  | 7e1f9a...c4b8d2          [COPIAR]              |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  | Client Seed:                                   |  |
|  | x7k2m9...p4q1            [COPIAR]              |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  | Nonce:     1                                    |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  | HMAC-SHA256 Resultado:                         |  |
|  | b2d7e4...f8a3c1          [COPIAR]              |  |
|  +------------------------------------------------+  |
|                                                      |
|  +------------------------------------------------+  |
|  | Winning Index:    127 de 234 tickets            |  |
|  +------------------------------------------------+  |
|                                                      |
|  COMO VERIFICAR:                                     |
|  1. Copie Server Seed e "ClientSeed:Nonce"          |
|  2. Calcule HMAC-SHA256 em devglan.com/hmac         |
|  3. Primeiros 8 chars hex → decimal → mod 234       |
|  4. Resultado deve ser = 127                         |
|                                                      |
|  [ ABRIR VERIFICADOR ONLINE ↗ ]                     |
|                                                      |
+====================================================+
```

## Especificacoes Tela 6

**Modal PF (glassmorphism, mesmo padrao Tela 2):**
```css
/* Backdrop */
{
  position: 'fixed', inset: 0, zIndex: 75,
  background: 'rgba(0,0,0,0.6)',
  backdropFilter: 'blur(5px)',
  WebkitBackdropFilter: 'blur(5px)',
  display: 'flex', alignItems: 'center', justifyContent: 'center'
}
/* Modal conteudo */
{
  background: 'rgba(10,10,10,0.9)',
  backdropFilter: 'blur(12px)',
  WebkitBackdropFilter: 'blur(12px)',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: 'clamp(12px, 2vw, 20px)',
  padding: 'clamp(20px, 3vw, 32px)',
  width: 'clamp(320px, 55vw, 520px)',
  maxHeight: '85vh', overflowY: 'auto',
  boxShadow: '0 20px 60px rgba(0,0,0,0.5)',
  display: 'flex', flexDirection: 'column',
  gap: 'clamp(12px, 2vw, 18px)'
}
```

**Campo seed/hash (monoespacado com botao copiar):**
```css
/* Container campo */
{
  display: 'flex', flexDirection: 'column', gap: '4px'
}
/* Label do campo */
{
  fontFamily: "'Inter', sans-serif", fontSize: 'clamp(10px, 1.1vw, 12px)',
  color: '#666666', textTransform: 'uppercase',
  letterSpacing: '0.05em'
}
/* Valor do campo + botao copiar */
{
  display: 'flex', alignItems: 'center', gap: '8px',
  background: 'rgba(0,0,0,0.4)',
  border: '1px solid rgba(212,168,67,0.1)',
  borderRadius: '8px',
  padding: 'clamp(8px, 1.2vw, 12px)'
}
/* Texto seed/hash */
{
  fontFamily: "'JetBrains Mono', monospace", fontWeight: 400,
  fontSize: 'clamp(10px, 1.2vw, 13px)',
  color: '#A8A8A8',
  wordBreak: 'break-all',
  flex: 1,
  userSelect: 'all'
}
/* Botao COPIAR */
{
  background: 'transparent',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: '6px',
  padding: '4px 10px',
  color: '#D4A843', fontSize: '11px',
  fontFamily: "'Inter', sans-serif",
  cursor: 'pointer', whiteSpace: 'nowrap',
  transition: 'all 0.15s ease',
  minHeight: '28px'
}
/* Botao COPIAR hover */
{
  borderColor: '#FFD700', color: '#FFD700'
}
/* Botao COPIAR apos clicar (feedback) */
{
  borderColor: '#00E676', color: '#00E676'
  /* Texto muda para "COPIADO!" por 1.5s depois volta */
}
```

**Secao "COMO VERIFICAR" (instrucoes):**
```css
{
  borderTop: '1px solid rgba(212,168,67,0.1)',
  paddingTop: 'clamp(12px, 2vw, 18px)'
}
/* Titulo COMO VERIFICAR */
{
  fontFamily: "'Inter', sans-serif", fontWeight: 600,
  fontSize: 'clamp(11px, 1.3vw, 14px)',
  color: '#D4A843', textTransform: 'uppercase',
  letterSpacing: '0.03em', marginBottom: '8px'
}
/* Passos numerados */
{
  fontFamily: "'Inter', sans-serif", fontSize: 'clamp(11px, 1.2vw, 13px)',
  color: '#A8A8A8', lineHeight: 1.6,
  paddingLeft: '16px'
}
/* Numero do passo */
{
  color: '#D4A843', fontWeight: 600
}
```

**Botao ABRIR VERIFICADOR ONLINE:**
```css
{
  background: 'transparent',
  border: '1.5px solid rgba(212,168,67,0.3)',
  borderRadius: '10px',
  padding: 'clamp(10px, 1.5vw, 14px)',
  color: '#D4A843', fontFamily: "'Inter', sans-serif", fontWeight: 600,
  fontSize: 'clamp(12px, 1.5vw, 15px)',
  cursor: 'pointer', textAlign: 'center',
  transition: 'all 0.2s ease',
  display: 'flex', alignItems: 'center', justifyContent: 'center',
  gap: '8px', minHeight: '44px'
}
/* Icone external link */
{
  fontSize: '14px', opacity: 0.7
}
```

## Framer Motion — Tela 6 PF Modal

```
ENTRADA:
  backdrop: initial={{ opacity: 0 }}, animate={{ opacity: 1 }}
  modal: initial={{ opacity: 0, scale: 0.95, y: 20 }}, animate={{ opacity: 1, scale: 1, y: 0 }}, transition={{ type: 'spring', stiffness: 300, damping: 25 }}
  campos: stagger 0.05s, initial={{ opacity: 0, y: 10 }}, animate={{ opacity: 1, y: 0 }}

INTERACOES:
  botao COPIAR: whileHover={{ borderColor: '#FFD700' }}, whileTap={{ scale: 0.95 }}
  feedback COPIADO: animate={{ color: '#00E676', borderColor: '#00E676' }}, depois resetar apos 1.5s
  botao VERIFICADOR: whileHover={{ borderColor: '#FFD700', boxShadow: '0 0 12px rgba(255,215,0,0.15)' }}
  botao [X]: whileHover={{ color: '#FF3B3B' }}, whileTap={{ scale: 0.9 }}

SAIDA:
  AnimatePresence: exit={{ opacity: 0, scale: 0.95 }}, transition={{ duration: 0.15 }}
```

## Estados Visuais — Tela 6 PF Modal

```
ESTADO 1 — IDLE (modal aberto, dados visiveis):
  - Todos campos mostram seeds/hash truncados com "..." (hover revela completo)
  - Botoes COPIAR em dourado
  - Instrucoes em cinza

ESTADO 2 — COPIANDO (jogador clicou COPIAR):
  - Botao muda texto "COPIAR" → "COPIADO!" por 1.5s
  - Cor muda dourado → verde (#00E676)
  - Texto seed do campo copiado: flash verde sutil (0.3s)
  - Apos 1.5s: volta ao estado IDLE

ESTADO 3 — VERIFICANDO (jogador abriu link externo):
  - Botao VERIFICADOR muda para "ABERTO ↗" por 2s
  - Link abre em nova aba (target="_blank")
```

---

# COMPONENTES ESPECIAIS DO GIVEAWAYS

## COMPONENTE: COUNTDOWN FLIP CARD (CountdownFlipCard.tsx)

```
DESCRICAO: Digito individual do countdown (d:h:m:s) com animacao flip ao mudar valor.
Inspirado em flip clocks fisicos — cada digito eh um card que "vira" quando muda.

ESTRUTURA:
  <div className="flip-card"> (perspective 400px, overflow hidden)
    <div className="top-half"> (metade superior: digito atual, clipPath inset(0 0 50% 0))
    <div className="bottom-half"> (metade inferior: digito anterior, clipPath inset(50% 0 0 0))
    <div className="flip-top"> (anima rotateX 0 → -90deg: "sai" a metade superior)
    <div className="flip-bottom"> (anima rotateX 90deg → 0: "entra" a metade inferior)
    <div className="separator"> (linha horizontal central 1px rgba(0,0,0,0.4))

CSS:
  container: width clamp(36px, 5vw, 56px), height clamp(44px, 6vw, 68px)
  background: #141414
  border: 1px solid rgba(212,168,67,0.3)
  borderRadius: 8px
  font: JetBrains Mono 700 clamp(20px, 4vw, 48px) #D4A843
  textShadow: 0 2px 4px rgba(0,0,0,0.5)

ANIMACAO (ao mudar digito):
  flip-top: animate rotateX 0 → -90deg, duration 0.3s, ease easeIn
  flip-bottom: animate rotateX 90deg → 0, duration 0.3s ease easeOut, delay 0.15s
  Ambos com perspective 400px, backfaceVisibility hidden, transformOrigin bottom/top

ESTADO URGENTE (<60s):
  color: #FF1744
  textShadow: 0 0 10px rgba(255,23,68,0.4)
  border: 1px solid rgba(255,23,68,0.3)
  animation: pulseUrgent 0.5s ease-in-out infinite alternate
  /* @keyframes pulseUrgent { from { opacity: 0.8 } to { opacity: 1 } } */

PROPS:
  value: number (0-99)
  label: string ("D" | "H" | "M" | "S")
  isUrgent: boolean
```

## COMPONENTE: DRUM SCROLL VIRTUAL (DrumScrollVirtual.tsx)

```
DESCRICAO: Container de scroll vertical que anima translateY para simular drum de sorteio.
Renderiza apenas ~15 items visiveis (virtualizacao) em vez de todos os 500 tickets.

ESTRUTURA:
  <div className="drum-viewport"> (overflow hidden, position relative)
    <div className="drum-fade-top"> (gradient topo, pointer-events none, z-index 2)
    <div className="drum-fade-bottom"> (gradient base, pointer-events none, z-index 2)
    <div className="drum-frame"> (frame dourado central, z-index 3)
    <motion.div ref={scope} className="drum-track"> (position absolute, willChange transform)
      {visibleItems.map(item => <DrumItem key={item.id} />)}

CSS:
  viewport: width clamp(260px, 40vw, 400px), height clamp(200px, 30vh, 320px)
  background: rgba(10,10,10,0.95)
  border: 2px solid rgba(212,168,67,0.3)
  borderRadius: clamp(12px, 2vw, 20px)
  boxShadow: 0 0 40px rgba(0,0,0,0.6), inset 0 0 20px rgba(0,0,0,0.4)
  
  frame: border 3px solid #FFD700
  boxShadow: 0 0 20px rgba(255,215,0,0.4), 0 0 40px rgba(255,215,0,0.15)
  position absolute, top 50%, transform translateY(-50%)

ANIMACAO (3 fases via useAnimate):
  FASE 1 rapido: translateY linear 2s, filter blur(2px) no track
  FASE 2 decelera: translateY ease [0.2,0,0,1] 3s, blur remove gradual
  FASE 3 bounce: frame scale [1, 1.08, 1] spring 0.3s

VIRTUALIZACAO:
  ITEM_HEIGHT = clamp(40px, 6vh, 56px)
  visibleStart = Math.max(0, currentIndex - 7)
  visibleEnd = Math.min(totalItems, currentIndex + 8)
  visibleItems = tickets.slice(visibleStart, visibleEnd)
  track translateY offset = -(visibleStart * ITEM_HEIGHT)

PROPS:
  tickets: Array<{ id, number, playerName }>
  winnerIndex: number
  onComplete: () => void
```

## COMPONENTE: PROGRESS BAR GLOW (ProgressBarGlow.tsx)

```
DESCRICAO: Barra de progresso horizontal com fill verde neon e glow box-shadow.
Mostra tickets vendidos / max tickets.

ESTRUTURA:
  <div className="progress-track"> (fundo escuro, border-radius pill)
    <motion.div className="progress-fill" style={{ width: `${pct}%` }} /> (fill verde)
  <span className="progress-label"> ("234/500 (46.8%)")

CSS:
  track: width 100%, height clamp(6px, 1vw, 10px), bg #1A1A1A, borderRadius 999px, overflow hidden
  fill: height 100%, bg linear-gradient(90deg, #004D25 0%, #00E676 100%), borderRadius 999px
  fill transition: width 0.5s ease-in-out
  fill boxShadow: 0 0 8px rgba(0,230,118,0.3)
  label: JetBrains Mono clamp(10px, 1.2vw, 13px) #A8A8A8, marginTop 4px

ESTADOS:
  normal (< 80%): fill verde padrao
  quase cheio (80-99%): fill mais brilhante, glow intensificado (box-shadow 0 0 12px)
  lotado (100%): fill pulsa (animation: pulseGlow 1s infinite alternate), label "ESGOTADO" vermelho

PROPS:
  current: number
  max: number
  showLabel: boolean (default true)
```

---

`[FIM DA PARTE 3 — Continua Parte 4: Mock JSON, Tooltips, PROIBIDO, Resumo, Paths, Checklist]`

`[ENTREGA: 3.PROMPT-AI-GIVEAWAYS-PARTE-3.md — 25/03/2026]`
