# CSS COMPONENTES — GIVEAWAYS (#20)
## Parte 2 de 3 - Componentes 9-17 (especificos Giveaways)
## Colar JUNTO com Parte 1 e PROMPT-V0 ao gerar visual no V0/Bolt
### Blackout Casino GTARP - 26/03/2026

---

# 9. BOTAO TICKET GRATIS (DS P2 sec8.4 — free ticket azul)

```css
.giveaway-btn--free {
  background: rgba(33,150,243,0.1);
  border: 1px solid rgba(33,150,243,0.3);
  border-radius: var(--gw-radius-btn);
  color: var(--gw-blue-free);
  font-family: var(--gw-font-body);
  font-weight: 500;
  font-size: clamp(12px, 1.5vw, 16px);
  padding: clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
}

.giveaway-btn--free:hover {
  border-color: var(--gw-blue-free);
  box-shadow: 0 0 12px rgba(33,150,243,0.2);
}

.giveaway-btn--free:active {
  transform: scale(0.97);
}

/* Estado cooldown (nao clicavel) */
.giveaway-btn--free--cooldown {
  opacity: 0.4;
  cursor: not-allowed;
  border-color: rgba(33,150,243,0.15);
}

.giveaway-btn--free--cooldown:hover {
  box-shadow: none;
  border-color: rgba(33,150,243,0.15);
}
```

---

# 10. BOTAO SECUNDARIO DOURADO (DS P2 sec8.3 — CANCELAR, VOLTAR AO LOBBY, VERIFICAR)

```css
.giveaway-btn--secondary {
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: clamp(10px, 1.5vw, 12px);
  color: var(--gw-gold-primary);
  font-family: var(--gw-font-body);
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  padding: clamp(10px, 1.5vw, 14px) clamp(20px, 3vw, 32px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
  text-transform: uppercase;
}

.giveaway-btn--secondary:hover {
  border-color: var(--gw-gold-pure);
}

.giveaway-btn--secondary:active {
  transform: scale(0.95);
}

.giveaway-btn--secondary:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```

---

# 11. MODAL GLASSMORPHISM (DS P2 sec8.2)

```css
/* Overlay fundo escuro + blur */
.giveaway-modal-overlay {
  position: fixed;
  inset: 0;
  z-index: 70;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(5px);
  -webkit-backdrop-filter: blur(5px);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Container glassmorphism */
.giveaway-modal {
  background: rgba(10,10,10,0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: var(--gw-radius-modal);
  padding: clamp(20px, 3vw, 32px);
  width: clamp(280px, 45vw, 420px);
  box-shadow: var(--gw-shadow-modal);
  position: relative;
  display: flex;
  flex-direction: column;
  gap: clamp(12px, 2vw, 20px);
}

/* Variante wide (PF modal) */
.giveaway-modal--wide {
  width: clamp(320px, 55vw, 520px);
}

/* Botao fechar X */
.giveaway-modal__close {
  position: absolute;
  top: clamp(12px, 1.5vw, 16px);
  right: clamp(12px, 1.5vw, 16px);
  background: transparent;
  border: none;
  color: var(--gw-text-muted);
  cursor: pointer;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
}

.giveaway-modal__close:hover {
  color: var(--gw-red);
}

.giveaway-modal__close:active {
  transform: scale(0.9);
}

/* Titulo modal */
.giveaway-modal__title {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
  text-align: center;
}
```

---

# 12. TOOLTIP (DS P2 sec5.3)

```css
/* Todos elementos interativos devem ter atributo title nativo HTML.
   Para tooltip customizado visual: */
.giveaway-tooltip {
  position: relative;
}

.giveaway-tooltip::after {
  content: attr(data-tooltip);
  position: absolute;
  bottom: calc(100% + 8px);
  left: 50%;
  transform: translateX(-50%);
  background: var(--gw-black-tooltip);
  color: var(--gw-text-primary);
  font-family: var(--gw-font-body);
  font-size: clamp(10px, 1.1vw, 12px);
  padding: 6px 12px;
  border-radius: var(--gw-radius-small);
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.15s ease;
  z-index: 100;
}

.giveaway-tooltip:hover::after {
  opacity: 1;
}
```

---

# 13. TEXTOS (DS P2 sec6 tipografia)

```css
/* Titulo principal (SORTEIOS, HISTORICO, PROVABLY FAIR) */
.giveaway-text--title {
  font-family: var(--gw-font-title);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--gw-gold-primary);
}

/* Titulo secao (PROXIMOS SORTEIOS, COMO VERIFICAR) */
.giveaway-text--section {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 16px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
}

/* Valor GCoin (SEMPRE verde neon) */
.giveaway-text--gcoin {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  color: var(--gw-gcoin);
  text-shadow: var(--gw-text-shadow-green);
}

/* Valor destaque ouro (subtotal, ticket #) */
.giveaway-text--gold {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  color: var(--gw-gold-pure);
  text-shadow: 0 0 10px rgba(255,215,0,0.3);
}

/* Texto corpo */
.giveaway-text--body {
  font-family: var(--gw-font-body);
  font-weight: 400;
  color: var(--gw-text-secondary);
}

/* Texto mono (seeds, hashes, formulas) */
.giveaway-text--mono {
  font-family: var(--gw-font-mono);
  color: var(--gw-gold-primary);
  font-size: clamp(9px, 1vw, 12px);
}

/* Texto erro/perda */
.giveaway-text--error {
  font-family: var(--gw-font-mono);
  font-weight: 600;
  color: var(--gw-red);
}

/* Texto muted (consolacao, timestamps) */
.giveaway-text--muted {
  font-family: var(--gw-font-body);
  font-weight: 400;
  color: var(--gw-text-muted);
  font-style: italic;
}
```

---

# 14. GLOW SYSTEM (DS P2 sec7 — 4 cores)

```css
/* Glow verde (GCoin, botao primario, progress bar) */
.giveaway-glow--green {
  box-shadow: 0 0 12px rgba(0,230,118,0.2);
}
.giveaway-glow--green-strong {
  box-shadow: 0 0 20px rgba(0,230,118,0.4);
}

/* Glow dourado (titulos, frame drum, winner) */
.giveaway-glow--gold {
  box-shadow: 0 0 12px rgba(255,215,0,0.2);
}
.giveaway-glow--gold-strong {
  box-shadow: 0 0 20px rgba(255,215,0,0.4), 0 0 40px rgba(255,215,0,0.15);
}

/* Glow azul (ticket gratis) */
.giveaway-glow--blue {
  box-shadow: 0 0 12px rgba(33,150,243,0.2);
}

/* Glow vermelho (erro, saldo insuficiente, badge ALERTA) */
.giveaway-glow--red {
  box-shadow: 0 0 12px rgba(255,59,59,0.2);
}
```

---

# 15. INPUT QUANTIDADE (DS P2 sec9.1 — modal compra)

```css
/* Container -/display/+ */
.giveaway-input-qty {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1.5vw, 16px);
}

/* Botoes -/+ */
.giveaway-input-qty__btn {
  width: clamp(36px, 5vw, 48px);
  height: clamp(36px, 5vw, 48px);
  background: var(--gw-black-hover);
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: var(--gw-radius-btn);
  color: var(--gw-gold-primary);
  font-family: var(--gw-font-mono);
  font-size: clamp(16px, 2vw, 24px);
  font-weight: 700;
  min-height: var(--gw-touch-min);
  min-width: var(--gw-touch-min);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}

.giveaway-input-qty__btn:hover {
  border-color: var(--gw-gold-pure);
}

.giveaway-input-qty__btn:active {
  transform: scale(0.9);
}

.giveaway-input-qty__btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

/* Display central quantidade */
.giveaway-input-qty__display {
  width: clamp(60px, 10vw, 100px);
  height: clamp(44px, 6vw, 56px);
  background: var(--gw-black-card);
  border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 12px;
  font-family: var(--gw-font-mono);
  font-weight: 700;
  font-size: clamp(20px, 3vw, 32px);
  color: var(--gw-white);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: inset 0 2px 6px rgba(0,0,0,0.3);
}
```

---

# 16. LINHAS INFO COMPRA (DS P2 sec9.3 — preco/subtotal/saldo)

```css
.giveaway-info-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.giveaway-info-row__label {
  font-family: var(--gw-font-body);
  font-size: clamp(12px, 1.4vw, 15px);
  color: var(--gw-text-secondary);
}

.giveaway-info-row__value {
  font-family: var(--gw-font-mono);
  font-weight: 600;
  font-size: clamp(14px, 1.8vw, 18px);
  color: var(--gw-gcoin);
}

/* Variante subtotal (maior, dourado) */
.giveaway-info-row__value--highlight {
  font-weight: 700;
  font-size: clamp(16px, 2.2vw, 22px);
  color: var(--gw-gold-pure);
  text-shadow: 0 0 10px rgba(255,215,0,0.3);
}

/* Variante saldo insuficiente (vermelho) */
.giveaway-info-row__value--error {
  color: var(--gw-red);
}
```

---

# 17. DRUM REVEAL (DS P3 sec20.3 — componente especifico Giveaways)

```css
/* Container externo do drum */
.giveaway-drum {
  width: clamp(260px, 40vw, 400px);
  height: clamp(200px, 30vh, 320px);
  background: rgba(10,10,10,0.95);
  border: 2px solid rgba(212,168,67,0.3);
  border-radius: var(--gw-radius-modal);
  overflow: hidden;
  position: relative;
  box-shadow: var(--gw-shadow-drum);
}

/* Fade gradients topo e base */
.giveaway-drum__fade--top {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 40px;
  background: linear-gradient(180deg, rgba(10,10,10,0.95), transparent);
  pointer-events: none;
  z-index: 2;
}

.giveaway-drum__fade--bottom {
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 40px;
  background: linear-gradient(0deg, rgba(10,10,10,0.95), transparent);
  pointer-events: none;
  z-index: 2;
}

/* Frame dourado central (indica winner) */
.giveaway-drum__frame {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: calc(100% - 16px);
  height: 48px;
  border: 3px solid var(--gw-gold-pure);
  border-radius: var(--gw-radius-btn);
  box-shadow: var(--gw-shadow-frame-gold);
  pointer-events: none;
  z-index: 3;
  animation: gwDrumGlow 2s ease-in-out infinite;
}

/* Track do drum (motion.div com translateY) */
.giveaway-drum__track {
  position: absolute;
  top: 0; left: 0; right: 0;
  will-change: transform;
}

/* Cada item do drum */
.giveaway-drum__item {
  height: 48px;
  display: flex;
  align-items: center;
  padding: 0 clamp(12px, 2vw, 20px);
  gap: clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.06);
}

/* Item vencedor (destaque) */
.giveaway-drum__item--winner {
  background: rgba(212,168,67,0.15);
  border-left: 3px solid var(--gw-gold-pure);
}

.giveaway-drum__item-number {
  font-family: var(--gw-font-mono);
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  color: var(--gw-gold-primary);
  min-width: 50px;
}

.giveaway-drum__item-player {
  font-family: var(--gw-font-body);
  font-weight: 500;
  font-size: clamp(13px, 1.6vw, 17px);
  color: var(--gw-text-secondary);
}
```

---

`[FIM DA PARTE 2 — Continua Parte 3: Componentes 18-21 + Responsive + Imagens + PROIBIDO + Checklist]`

`[ENTREGA: 7.CSS-COMPONENTES-GIVEAWAYS-PARTE-2.md — 26/03/2026]`
