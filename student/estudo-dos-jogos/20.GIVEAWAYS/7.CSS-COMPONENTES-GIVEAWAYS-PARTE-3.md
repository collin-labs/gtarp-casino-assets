# CSS COMPONENTES — GIVEAWAYS (#20)
## Parte 3 de 3 - Componentes 18-21 + Responsive + Imagens + PROIBIDO + Checklist
## Colar JUNTO com Partes 1-2 e PROMPT-V0 ao gerar visual no V0/Bolt
### Blackout Casino GTARP - 26/03/2026

---

# 18. WINNER OVERLAY (DS P3A sec20.2 — resultado vencedor/perdedor)

```css
/* Overlay resultado fullscreen */
.giveaway-result-overlay {
  position: fixed;
  inset: 0;
  z-index: 90;
  background: rgba(5,5,5,0.92);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

/* Vencedor — titulo glow */
.giveaway-result__title--win {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(18px, 3.5vw, 36px);
  color: var(--gw-gold-pure);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  text-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.2);
  animation: gwWinnerGlow 2s ease-in-out infinite;
}

/* Perdedor — titulo discreto */
.giveaway-result__title--lose {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: var(--gw-text-secondary);
  text-transform: uppercase;
}

/* Payout vencedor */
.giveaway-result__payout--win {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  font-size: clamp(24px, 5vw, 56px);
  color: var(--gw-gcoin);
  text-shadow: 0 0 20px rgba(0,230,118,0.4);
}

/* Payout perdedor */
.giveaway-result__payout--lose {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  font-size: clamp(16px, 3vw, 28px);
  color: var(--gw-gold-primary);
}

/* Crown icon (so vencedor) */
.giveaway-result__crown {
  width: clamp(28px, 4vw, 40px);
  transform: rotate(15deg);
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.5));
}

/* Consolacao (so perdedor) */
.giveaway-result__consolation {
  font-family: var(--gw-font-body);
  font-weight: 400;
  font-size: clamp(13px, 1.8vw, 18px);
  color: var(--gw-text-muted);
  font-style: italic;
}
```

---

# 19. HISTORICO ROWS (DS P2 sec10.4)

```css
/* Container historico */
.giveaway-history {
  display: flex;
  flex-direction: column;
  gap: 0;
  overflow-y: auto;
}

/* Toggle tabs Todos/Meus */
.giveaway-history__tabs {
  display: flex;
  gap: clamp(6px, 1vw, 10px);
  margin-bottom: clamp(8px, 1.5vw, 16px);
}

.giveaway-history__tab {
  border-radius: var(--gw-radius-btn);
  font-family: var(--gw-font-body);
  font-size: clamp(11px, 1.3vw, 14px);
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 2vw, 20px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
}

.giveaway-history__tab--active {
  background: rgba(212,168,67,0.15);
  border: 1px solid rgba(212,168,67,0.4);
  color: var(--gw-gold-primary);
  font-weight: 700;
}

.giveaway-history__tab--inactive {
  background: transparent;
  border: 1px solid rgba(212,168,67,0.1);
  color: var(--gw-text-muted);
  font-weight: 400;
}

.giveaway-history__tab--inactive:hover {
  border-color: rgba(212,168,67,0.25);
  color: var(--gw-text-secondary);
}

.giveaway-history__tab:active {
  transform: scale(0.95);
}

/* Linha da tabela */
.giveaway-history__row {
  display: grid;
  grid-template-columns: 2fr 1.5fr 1fr 0.8fr 0.5fr;
  padding: clamp(8px, 1.2vw, 14px) clamp(10px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.06);
  transition: background 0.1s ease;
}

.giveaway-history__row:hover {
  background: rgba(212,168,67,0.04);
}

/* Colunas */
.giveaway-history__cell--title {
  font-family: var(--gw-font-title);
  font-weight: 600;
  font-size: clamp(11px, 1.3vw, 14px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
}

.giveaway-history__cell--player {
  font-family: var(--gw-font-body);
  font-size: clamp(10px, 1.2vw, 13px);
  color: var(--gw-text-secondary);
}

.giveaway-history__cell--prize {
  font-family: var(--gw-font-mono);
  font-weight: 600;
  font-size: clamp(12px, 1.4vw, 15px);
  color: var(--gw-gcoin);
}

.giveaway-history__cell--date {
  font-family: var(--gw-font-body);
  font-size: clamp(10px, 1.1vw, 12px);
  color: var(--gw-text-muted);
}

/* Botao verificar PF por linha */
.giveaway-history__verify-btn {
  background: transparent;
  border: none;
  cursor: pointer;
  min-width: var(--gw-touch-min);
  min-height: var(--gw-touch-min);
  display: flex;
  align-items: center;
  justify-content: center;
}

.giveaway-history__verify-btn:hover {
  opacity: 0.8;
}

.giveaway-history__verify-btn:active {
  transform: scale(0.9);
}

.giveaway-history__verify-btn img {
  width: 16px;
  height: 16px;
}

/* Paginacao */
.giveaway-history__pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: clamp(8px, 1.5vw, 16px);
  padding: clamp(10px, 1.5vw, 16px) 0;
}

.giveaway-history__page-btn {
  font-family: var(--gw-font-title);
  font-weight: 600;
  font-size: clamp(11px, 1.3vw, 14px);
  color: var(--gw-gold-primary);
  background: transparent;
  border: 1px solid rgba(212,168,67,0.3);
  border-radius: var(--gw-radius-btn);
  padding: clamp(6px, 0.8vw, 10px) clamp(12px, 2vw, 20px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
}

.giveaway-history__page-btn:hover {
  border-color: var(--gw-gold-pure);
}

.giveaway-history__page-btn:active {
  transform: scale(0.95);
}

.giveaway-history__page-btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```

---

# 20. PROVABLY FAIR FIELDS (DS P3 sec19)

```css
/* Campo seed/hash readonly */
.giveaway-pf-field {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(8px, 1vw, 12px) clamp(10px, 1.5vw, 16px);
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(212,168,67,0.1);
  border-radius: clamp(6px, 0.8vw, 10px);
}

.giveaway-pf-field__label {
  font-family: var(--gw-font-body);
  font-size: clamp(10px, 1.1vw, 13px);
  color: var(--gw-text-secondary);
}

.giveaway-pf-field__value {
  font-family: var(--gw-font-mono);
  font-size: clamp(9px, 1vw, 12px);
  color: var(--gw-gold-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: clamp(160px, 25vw, 280px);
}

/* Botao copiar */
.giveaway-pf-field__copy {
  background: transparent;
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: var(--gw-radius-small);
  padding: 4px 8px;
  min-width: var(--gw-touch-min);
  min-height: var(--gw-touch-min);
  font-family: var(--gw-font-body);
  font-size: clamp(9px, 1vw, 11px);
  color: var(--gw-gold-primary);
  cursor: pointer;
}

.giveaway-pf-field__copy:hover {
  border-color: rgba(212,168,67,0.4);
}

.giveaway-pf-field__copy:active {
  transform: scale(0.95);
}

/* Estado copiado (1.5s feedback) */
.giveaway-pf-field__copy--copied {
  border-color: var(--gw-gcoin);
  color: var(--gw-gcoin);
}

/* Bloco formula */
.giveaway-pf-formula {
  background: rgba(10,10,10,0.6);
  border: 1px solid rgba(212,168,67,0.1);
  border-radius: clamp(6px, 0.8vw, 10px);
  padding: clamp(10px, 1.2vw, 16px);
  font-family: var(--gw-font-mono);
  font-size: clamp(9px, 1vw, 11px);
  color: var(--gw-text-secondary);
  line-height: 1.8;
}

/* Badge JUSTO */
.giveaway-pf-badge--valid {
  width: 100%;
  padding: clamp(10px, 1.2vw, 16px);
  background: rgba(0,230,118,0.08);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: clamp(6px, 0.8vw, 10px);
  color: var(--gw-gcoin);
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(12px, 1.4vw, 16px);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  display: flex;
  align-items: center;
  gap: 8px;
}

/* Badge ALERTA (hash nao confere) */
.giveaway-pf-badge--invalid {
  width: 100%;
  padding: clamp(10px, 1.2vw, 16px);
  background: rgba(255,59,59,0.08);
  border: 1px solid rgba(255,59,59,0.3);
  border-radius: clamp(6px, 0.8vw, 10px);
  color: var(--gw-red);
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(12px, 1.4vw, 16px);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  display: flex;
  align-items: center;
  gap: 8px;
}
```

---

# 21. RESPONSIVE (REGRA X4)

```css
/* Grid proximos sorteios — auto-fill responsivo */
.giveaway-grid--upcoming {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(clamp(160px, 20vw, 240px), 1fr));
  gap: clamp(8px, 1.5vw, 16px);
}

/* Stack vertical em telas estreitas */
@media (max-width: 480px) {
  .giveaway-history__row {
    grid-template-columns: 1fr 1fr;
    gap: 4px;
  }
  .giveaway-history__cell--date,
  .giveaway-history__cell--player {
    font-size: clamp(9px, 2vw, 11px);
  }
  .giveaway-drum {
    width: 90vw;
  }
  .giveaway-modal {
    width: 92vw;
    padding: clamp(16px, 4vw, 24px);
  }
}

/* Landscape FiveM (1280x720) */
@media (min-width: 1024px) and (max-height: 800px) {
  .giveaway-result-overlay {
    gap: clamp(8px, 1.5vw, 16px);
  }
  .giveaway-result__payout--win {
    font-size: clamp(20px, 4vw, 40px);
  }
}
```

---

# LISTA DE IMAGENS COM PATHS (REGRA 7.19)

```
/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png    Fundo geral do painel
/assets/giveaways/ticket-gold.png                         Ticket dourado 64x64
/assets/giveaways/trophy-winner.png                       Trofeu vencedor 80x80
/assets/giveaways/crown-winner.png                        Coroa vencedor 40x40
/assets/giveaways/icon-sound-on.png                       Som ativo 32x32
/assets/giveaways/icon-sound-off.png                      Som mudo 32x32
/assets/giveaways/icon-history.png                        Historico 32x32
/assets/giveaways/icon-provably-fair.png                  Provably Fair 32x32
/assets/giveaways/icon-copy.png                           Copiar 16x16
/assets/giveaways/icon-check.png                          Check verde 20x20
/assets/giveaways/icon-ticket.png                         Ticket mini 24x24
/assets/giveaways/icon-gift.png                           Gift/premio 24x24
/assets/giveaways/icon-clock.png                          Relogio 24x24
/assets/giveaways/icon-external-link.png                  Link externo 16x16
```

Total: **14 imagens** (1 fundo + 3 visuais + 10 icons). Todas PNG custom (REGRA X21).

---

# O QUE NAO DEVE TER (PROIBIDO — REGRA 7.20)

```
1.  NUNCA fundo branco ou cinza claro — SOMENTE #0A0A0A.
2.  NUNCA Tailwind classes — CSS BEM puro com variaveis :root.
3.  NUNCA sans-serif em titulos — SEMPRE Cinzel.
4.  NUNCA px fixo para fontes/padding — SEMPRE clamp().
5.  NUNCA border-radius >20px em containers.
6.  NUNCA icones SVG inline ou Lucide — SOMENTE PNG custom.
7.  NUNCA titulo sem text-shadow glow dourado.
8.  NUNCA botao sem 4 estados (default, :hover, :active, :disabled).
9.  NUNCA glassmorphism sem -webkit-backdrop-filter (CEF/Safari).
10. NUNCA progress bar sem gradiente verde (90deg #004D25 → #00E676).
11. NUNCA drum sem frame dourado central com glow pulsante.
12. NUNCA campo copiavel sem estado --copied (verde 1.5s).
13. NUNCA tab toggle sem estado --active vs --inactive.
```

---

# CHECKLIST FINAL (35 itens)

## Superficies e Cores (7 itens)

| # | Item | Origem |
|---|------|--------|
| 1 | Fundo #0A0A0A + bg-casino.png + 2 gradients + vinheta inset | comp #2 |
| 2 | Card ativo #141414 → #1A1A1A gradient 135deg | comp #5 |
| 3 | Card mini #141414 com borda 0.15 | comp #6 |
| 4 | Glassmorphism rgba(10,10,10,0.85) + blur(12px) + -webkit- | comp #11 |
| 5 | Drum rgba(10,10,10,0.95) + fade gradients | comp #17 |
| 6 | Result overlay rgba(5,5,5,0.92) | comp #18 |
| 7 | PF fields rgba(0,0,0,0.3) | comp #20 |

## Tipografia (6 itens)

| # | Item | Origem |
|---|------|--------|
| 8 | Cinzel 700 uppercase para titulos | comp #13 |
| 9 | Inter para corpo/labels | comp #13 |
| 10 | JetBrains Mono para valores/seeds/formulas | comp #13 |
| 11 | GCoin SEMPRE #00E676 | comp #13 |
| 12 | Gold highlight #FFD700 com text-shadow | comp #13 |
| 13 | Muted #666666 italic para consolacao | comp #13 |

## Bordas e Shadows (5 itens)

| # | Item | Origem |
|---|------|--------|
| 14 | Border gold 3 niveis (0.15, 0.3, 0.5) | tokens |
| 15 | Shadow card 4px 16px → hover 8px 30px | tokens |
| 16 | Shadow modal 20px 60px | tokens |
| 17 | Shadow drum 40px + inset 20px | tokens |
| 18 | Frame gold 3px solid + glow 20px+40px | comp #17 |

## @keyframes (6 itens)

| # | Item | Origem |
|---|------|--------|
| 19 | gwShimmer (sweep cards) | @kf #1 |
| 20 | gwActivePulse (borda verde card ativo) | @kf #2 |
| 21 | gwDrumGlow (frame dourado drum) | @kf #3 |
| 22 | gwWinnerGlow (titulo vencedor) | @kf #4 |
| 23 | gwProgressUrgent (barra >90%) | @kf #5 |
| 24 | gwCountdownUrgent (ultimos 60s) | @kf #6 |

## Interatividade / Tooltips (5 itens)

| # | Item | Origem |
|---|------|--------|
| 25 | Todos botoes com :hover, :active, :disabled | comps #4,8,9,10 |
| 26 | Tooltip customizado via data-tooltip + ::after | comp #12 |
| 27 | Copy feedback --copied verde 1.5s | comp #20 |
| 28 | Tab toggle --active vs --inactive | comp #19 |
| 29 | Page btn disabled opacity 0.3 | comp #19 |

## Responsividade Mobile (4 itens)

| # | Item | Origem |
|---|------|--------|
| 30 | clamp() em TODOS tamanhos | todos comps |
| 31 | minHeight 44px touch targets | todos botoes |
| 32 | @media 480px stack vertical | comp #21 |
| 33 | @media landscape 1280x720 | comp #21 |

## Geral (2 itens)

| # | Item | Origem |
|---|------|--------|
| 34 | BEM naming .giveaway-[block]__[element]--[modifier] | todos |
| 35 | 14 imagens listadas com paths e dimensoes | lista imagens |

---

**VALIDACAO X17: 21 componentes numerados, 6 @keyframes, 44 tokens :root, 35 itens checklist, 13 PROIBIDO, 14 imagens mapeadas.**

`[ENTREGA: 7.CSS-COMPONENTES-GIVEAWAYS-PARTE-3.md — 26/03/2026]`
