# CSS COMPONENTES — GIVEAWAYS (#20)
## Parte 1 de 3 - Tokens :root + @keyframes (6) + Componentes 1-8
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-GIVEAWAYS
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 26/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Giveaways no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.giveaway-card { border: 1px solid #D4A843; }` daqui.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Giveaways)

```css
/* ====================================================
   GIVEAWAYS — DESIGN TOKENS
   Separacao: primitivos (raw) -> semanticos (uso)
   Ref: Adendo DS sec1, Prompt AI sec Header Tecnico
   ==================================================== */

:root {
  /* --- Primitivos — cores base --- */
  --gw-gold-primary:    #D4A843;
  --gw-gold-light:      #F6E27A;
  --gw-gold-dark:       #8B6914;
  --gw-gold-deeper:     #462523;
  --gw-gold-pure:       #FFD700;
  --gw-green-neon:      #00E676;
  --gw-green-dark:      #004D25;
  --gw-green-medium:    #00C853;
  --gw-black-bg:        #0A0A0A;
  --gw-black-surface:   #0F0F0F;
  --gw-black-card:      #141414;
  --gw-black-hover:     #1A1A1A;
  --gw-black-tooltip:   #2A2A2A;
  --gw-red:             #FF3B3B;
  --gw-blue-free:       #2196F3;
  --gw-white:           #FFFFFF;
  --gw-text-primary:    #F5F5F5;
  --gw-text-secondary:  #A8A8A8;
  --gw-text-muted:      #666666;

  /* --- Semanticos — uso especifico Giveaways --- */
  --gw-gcoin:                  #00E676;
  --gw-prize:                  #00E676;
  --gw-ticket-free:            #2196F3;
  --gw-border-gold:            1px solid rgba(212,168,67,0.15);
  --gw-border-gold-hover:      1px solid rgba(212,168,67,0.3);
  --gw-border-gold-active:     1px solid rgba(212,168,67,0.5);
  --gw-border-subtle:          1px solid rgba(212,168,67,0.1);
  --gw-shadow-card:            0 4px 16px rgba(0,0,0,0.3);
  --gw-shadow-card-hover:      0 8px 30px rgba(0,0,0,0.4);
  --gw-shadow-modal:           0 20px 60px rgba(0,0,0,0.5);
  --gw-shadow-active-card:     0 2px 0 0 rgba(0,230,118,0.4), 0 4px 20px rgba(0,0,0,0.4), 0 1px 3px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.03);
  --gw-shadow-drum:            0 0 40px rgba(0,0,0,0.6), inset 0 0 20px rgba(0,0,0,0.4);
  --gw-shadow-frame-gold:      0 0 20px rgba(255,215,0,0.4), 0 0 40px rgba(255,215,0,0.15);
  --gw-text-shadow-gold:       0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
  --gw-text-shadow-green:      0 0 8px rgba(0,230,118,0.4);
  --gw-text-shadow-prize:      0 0 12px rgba(0,230,118,0.3);

  /* --- Fontes --- */
  --gw-font-title: 'Cinzel', serif;
  --gw-font-body:  'Inter', sans-serif;
  --gw-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --gw-touch-min:      44px;
  --gw-radius-card:    clamp(10px, 1.5vw, 16px);
  --gw-radius-btn:     10px;
  --gw-radius-modal:   clamp(12px, 2vw, 20px);
  --gw-radius-small:   6px;
  --gw-radius-pill:    999px;
}
```

---

# 1. @KEYFRAMES (6 animacoes do Giveaways)

```css
/* 1 — Shimmer sweep nos cards (DS P1 sec4.4 shine sweep) */
@keyframes gwShimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

/* 2 — Pulse na borda do card ativo (borda verde pulsa) */
@keyframes gwActivePulse {
  0%, 100% { box-shadow: 0 2px 0 0 rgba(0,230,118,0.3); }
  50% { box-shadow: 0 2px 0 0 rgba(0,230,118,0.6); }
}

/* 3 — Glow pulsante no frame dourado do drum */
@keyframes gwDrumGlow {
  0%, 100% { box-shadow: 0 0 20px rgba(255,215,0,0.3); }
  50% { box-shadow: 0 0 35px rgba(255,215,0,0.5); }
}

/* 4 — Glow do badge vencedor apos draw */
@keyframes gwWinnerGlow {
  0%, 100% { text-shadow: 0 0 20px rgba(255,215,0,0.4); }
  50% { text-shadow: 0 0 35px rgba(255,215,0,0.6); }
}

/* 5 — Progress bar pulse (quando quase esgotando >90%) */
@keyframes gwProgressUrgent {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50% { box-shadow: 0 0 16px rgba(0,230,118,0.6); }
}

/* 6 — Countdown urgencia (ultimos 60s) */
@keyframes gwCountdownUrgent {
  0%, 100% { color: #D4A843; }
  50% { color: #FF3B3B; }
}
```

---

# 2. FUNDO DO JOGO (DS P1 sec4.1)

```css
.giveaway-game {
  position: absolute;
  inset: 0;
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  border-radius: inherit;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
```

---

# 3. HEADER BAR (DS P2 sec15.2)

```css
.giveaway-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0;
  z-index: 5;
}

.giveaway-header__title {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 28px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.giveaway-header__icons {
  display: flex;
  gap: 8px;
}

.giveaway-header__icon-btn {
  background: transparent;
  border: none;
  cursor: pointer;
  min-width: var(--gw-touch-min);
  min-height: var(--gw-touch-min);
  display: flex;
  align-items: center;
  justify-content: center;
}

.giveaway-header__icon-btn:hover {
  opacity: 0.8;
}

.giveaway-header__icon-btn:active {
  transform: scale(0.9);
}

.giveaway-header__icon-btn img {
  width: clamp(20px, 2.5vw, 28px);
  height: clamp(20px, 2.5vw, 28px);
}
```

---

# 4. BOTAO VOLTAR (DS P2 sec8.3)

```css
.giveaway-btn-back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: var(--gw-radius-btn);
  color: var(--gw-gold-primary);
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
  text-transform: uppercase;
}

.giveaway-btn-back:hover {
  border-color: var(--gw-gold-pure);
}

.giveaway-btn-back:active {
  transform: scale(0.95);
}

.giveaway-btn-back:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```

---

# 5. CARD SORTEIO ATIVO (DS P1 sec4.2 + P3A sec20.1)

```css
.giveaway-card--active {
  background: linear-gradient(135deg, #141414, #1A1A1A);
  border: 1.5px solid rgba(212,168,67,0.25);
  border-radius: var(--gw-radius-card);
  padding: clamp(16px, 2.5vw, 28px);
  position: relative;
  overflow: hidden;
  box-shadow: var(--gw-shadow-active-card);
  animation: gwActivePulse 3s ease-in-out infinite;
}

/* Shine sweep ::after */
.giveaway-card--active::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 60%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.02), transparent);
  animation: gwShimmer 6s ease-in-out infinite;
  pointer-events: none;
}

.giveaway-card--active:hover {
  border-color: rgba(212,168,67,0.4);
  box-shadow: var(--gw-shadow-card-hover);
}

.giveaway-card--active__title {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
  margin-bottom: clamp(4px, 0.5vw, 8px);
}

.giveaway-card--active__prize {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  font-size: clamp(18px, 3vw, 32px);
  color: var(--gw-gcoin);
  text-shadow: var(--gw-text-shadow-prize);
}
```

---

# 6. CARD MINI PROXIMO SORTEIO (DS P1 sec4.3)

```css
.giveaway-card--mini {
  background: var(--gw-black-card);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(10px, 1.5vw, 16px);
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: clamp(4px, 0.5vw, 8px);
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.giveaway-card--mini:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.3);
  border-color: rgba(212,168,67,0.25);
}

.giveaway-card--mini:active {
  transform: translateY(0);
}

.giveaway-card--mini__title {
  font-family: var(--gw-font-title);
  font-weight: 700;
  font-size: clamp(11px, 1.3vw, 14px);
  color: var(--gw-gold-primary);
  text-transform: uppercase;
}

.giveaway-card--mini__prize {
  font-family: var(--gw-font-mono);
  font-weight: 700;
  font-size: clamp(14px, 2vw, 20px);
  color: var(--gw-gcoin);
}
```

---

# 7. PROGRESS BAR (DS P2 sec15.5)

```css
.giveaway-progress {
  width: 100%;
}

.giveaway-progress__track {
  width: 100%;
  height: clamp(6px, 1vw, 10px);
  background: var(--gw-black-hover);
  border-radius: var(--gw-radius-pill);
  overflow: hidden;
}

.giveaway-progress__fill {
  height: 100%;
  border-radius: var(--gw-radius-pill);
  background: linear-gradient(90deg, #004D25, #00E676);
  box-shadow: 0 0 8px rgba(0,230,118,0.3);
  transition: width 0.5s ease;
}

/* Estado urgente >90% */
.giveaway-progress__fill--urgent {
  animation: gwProgressUrgent 1.5s ease-in-out infinite;
}

.giveaway-progress__label {
  font-family: var(--gw-font-mono);
  font-size: clamp(10px, 1.2vw, 13px);
  color: var(--gw-text-secondary);
  margin-top: 4px;
  display: block;
}
```

---

# 8. BOTAO PRIMARIO VERDE (DS P2 sec8.1 — COMPRAR TICKET, CONFIRMAR)

```css
.giveaway-btn--primary {
  background: linear-gradient(180deg, #00C853, #004D25);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: var(--gw-radius-btn);
  color: var(--gw-white);
  font-family: var(--gw-font-body);
  font-weight: 600;
  font-size: clamp(12px, 1.5vw, 16px);
  padding: clamp(10px, 1.5vw, 14px) clamp(16px, 3vw, 32px);
  min-height: var(--gw-touch-min);
  cursor: pointer;
  text-transform: uppercase;
  box-shadow: 0 0 12px rgba(0,230,118,0.2), 0 4px 12px rgba(0,0,0,0.3);
  position: relative;
  overflow: hidden;
}

/* Bevel ::before (DS P2 sec8.1.b) */
.giveaway-btn--primary::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
}

.giveaway-btn--primary:hover {
  box-shadow: 0 0 20px rgba(0,230,118,0.4), 0 4px 16px rgba(0,0,0,0.3);
  transform: scale(1.03);
}

.giveaway-btn--primary:active {
  transform: scale(0.97);
  box-shadow: 0 0 8px rgba(0,230,118,0.2);
}

.giveaway-btn--primary:disabled {
  background: var(--gw-black-hover);
  border-color: #333;
  opacity: 0.4;
  cursor: not-allowed;
  box-shadow: none;
  transform: none;
}
```

---

`[FIM DA PARTE 1 — Continua Parte 2: Componentes 9-17 (especificos Giveaways)]`

`[ENTREGA: 7.CSS-COMPONENTES-GIVEAWAYS-PARTE-1.md — 26/03/2026]`
