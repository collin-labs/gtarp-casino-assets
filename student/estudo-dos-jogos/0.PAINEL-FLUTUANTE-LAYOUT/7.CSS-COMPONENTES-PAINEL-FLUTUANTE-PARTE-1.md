# CSS COMPONENTES — PAINEL FLUTUANTE BLACKOUT CASINO
## Parte 1 de 2 — Tokens :root + @keyframes (11) + Componentes 1-12
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-PAINEL-FLUTUANTE
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP — 23/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Painel Flutuante no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.panel-container { border: 1.5px solid #D4A843; }` daqui.

**Quantidade:** 22 componentes CSS numerados + 11 @keyframes + tokens :root.
**Naming:** BEM `.panel-[block]__[element]--[modifier]`.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Painel)

```css
/* ====================================================
   PAINEL FLUTUANTE — DESIGN TOKENS
   Separacao: primitivos (raw) → semanticos (uso)
   Ref: DS P1 §2 Paleta, DS P1 §3 Tipografia
   ==================================================== */

:root {
  /* --- Primitivos — cores base --- */
  --panel-gold-primary:    #D4A843;
  --panel-gold-light:      #F6E27A;
  --panel-gold-pure:       #FFD700;
  --panel-gold-dark:       #8B6914;
  --panel-gold-deeper:     #462523;
  --panel-gold-mid:        #CB9B51;
  --panel-green-neon:      #00E676;
  --panel-green-dark:      #004D25;
  --panel-green-medium:    #00C853;
  --panel-black-bg:        #0A0A0A;
  --panel-black-surface:   #111111;
  --panel-red:             #FF1744;
  --panel-white:           #FFFFFF;
  --panel-text-secondary:  #A8A8A8;
  --panel-text-muted:      #666666;
  --panel-text-placeholder: #444444;

  /* --- Semanticos — uso especifico --- */
  --panel-border-gold:       1.5px solid #D4A843;
  --panel-border-gold-hover: 2px solid rgba(212,168,67,0.7);
  --panel-border-subtle:     1px solid rgba(212,168,67,0.2);
  --panel-border-green:      1.5px solid rgba(0,230,118,0.3);

  --panel-shadow-container:
    0 0 25px rgba(212,168,67,0.2),
    0 0 50px rgba(212,168,67,0.08),
    inset 0 0 60px rgba(0,0,0,0.4),
    0 8px 32px rgba(0,0,0,0.3),
    0 2px 8px rgba(0,0,0,0.5);
  --panel-shadow-card-idle:
    0 4px 15px rgba(0,0,0,0.4),
    0 1px 3px rgba(0,0,0,0.3);
  --panel-shadow-card-hover:
    0 8px 30px rgba(0,0,0,0.5),
    0 0 20px rgba(0,230,118,0.15);
  --panel-shadow-modal:
    0 0 30px rgba(0,0,0,0.6),
    0 0 15px rgba(212,168,67,0.1),
    inset 0 1px 0 rgba(212,168,67,0.08);

  --panel-text-shadow-gold:  0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
  --panel-text-shadow-green: 0 0 12px rgba(0,230,118,0.7), 0 0 25px rgba(0,230,118,0.35);

  /* --- Fontes --- */
  --panel-font-title: 'Cinzel', 'Playfair Display', serif;
  --panel-font-body:  'Inter', sans-serif;
  --panel-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --panel-touch-min:     44px;
  --panel-radius-panel:  18px;
  --panel-radius-card:   14px;
  --panel-radius-btn:    8px;
  --panel-radius-badge:  4px;
  --panel-radius-tooltip: 6px;
}
```

---

# 1. @KEYFRAMES (11 animacoes do Painel)

```css
/* ====================================================
   PAINEL FLUTUANTE — @KEYFRAMES (11)
   Ref: Adendo DS P1 (haloBreath, scanline, shine),
        Adendo DS P3 (glow pulse, VIP pulse),
        Adendo DS P4 (atmosferas)
   ==================================================== */

/* 1. Borda do painel pulsando glow dourado — Adendo DS P1 §1 */
@keyframes panelBorderGlow {
  0%, 100% {
    box-shadow:
      0 0 25px rgba(212,168,67,0.2),
      0 0 50px rgba(212,168,67,0.08),
      inset 0 0 60px rgba(0,0,0,0.4),
      0 8px 32px rgba(0,0,0,0.3),
      0 2px 8px rgba(0,0,0,0.5);
  }
  50% {
    box-shadow:
      0 0 35px rgba(212,168,67,0.3),
      0 0 70px rgba(212,168,67,0.12),
      inset 0 0 60px rgba(0,0,0,0.4),
      0 8px 32px rgba(0,0,0,0.3),
      0 2px 8px rgba(0,0,0,0.5);
  }
}

/* 2. Scan line HUD horizontal — Adendo DS P1 §5 */
@keyframes panelScanline {
  0%   { top: 0%; }
  100% { top: 100%; }
}

/* 3. Halo do letreiro respirando — Adendo DS P1 §6 */
@keyframes panelHaloBreath {
  0%, 100% { opacity: 0.7; }
  50%      { opacity: 1.0; }
}

/* 4. Marquee scroll infinito — Adendo DS P1 §6 */
@keyframes panelMarqueeScroll {
  0%   { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

/* 5. Shine sweep nos cards — Adendo DS P2 §10 */
@keyframes panelCardShine {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* 6. Shimmer metalico (ComingSoon titulo) — Adendo DS P3 §15 */
@keyframes panelComingShimmer {
  0%   { background-position: 200% center; }
  100% { background-position: -200% center; }
}

/* 7. Glow verde pulsante (feixo cards aba Arcade) — Adendo DS P4 §22.2 */
@keyframes panelFeixoPulseArcade {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50%      { box-shadow: 0 0 15px rgba(0,230,118,0.5); }
}

/* 8. Glow verde FORTE pulsante (feixo cards aba Eventos) — Adendo DS P4 §22.5 */
@keyframes panelFeixoPulseEventos {
  0%, 100% { box-shadow: 0 0 5px rgba(0,230,118,0.2); }
  50%      { box-shadow: 0 0 20px rgba(0,230,118,0.6); }
}

/* 9. VIP Blackout badge pulsante — Adendo DS P3 §16 */
@keyframes panelVipBlackoutPulse {
  0%, 100% { box-shadow: 0 0 5px rgba(255,23,68,0.2); }
  50%      { box-shadow: 0 0 15px rgba(255,23,68,0.4); }
}

/* 10. Saldo flash apos deposito — Adendo DS P3 §16 */
@keyframes panelSaldoFlash {
  0%   { color: #FFFFFF; text-shadow: 0 0 15px rgba(255,255,255,0.6); }
  100% { color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
}

/* 11. Pontos de luz na divisoria — Adendo DS P1 §7 (divisoria) */
@keyframes panelDotPulse {
  0%   { opacity: 0.3; transform: translate(-50%,-50%) scale(0.8); }
  100% { opacity: 1.0; transform: translate(-50%,-50%) scale(1.4); }
}
```

---

# 2. COMPONENTE: OVERLAY FULLSCREEN

```css
/* COPIAR — overlay sobre o GTA (Adendo DS P1 §0) */
.panel-overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0,0,0,0.78);
  backdrop-filter: blur(3px);
  -webkit-backdrop-filter: blur(3px);
  font-family: var(--panel-font-title);
  z-index: 9999;
  overflow: hidden;
}
```

---

# 3. COMPONENTE: CONTAINER PRINCIPAL

```css
/* COPIAR — painel flutuante centralizado (Adendo DS P1 §1) */
.panel-container {
  position: relative;
  width: 80vw;
  max-width: 1600px;
  height: calc(80vw * 8 / 16);
  max-height: 78vh;
  border-radius: var(--panel-radius-panel);
  overflow: hidden;
  background-color: var(--panel-black-bg);
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  background-position: center;
  border: var(--panel-border-gold);
  outline: 1.5px solid rgba(191,149,63,0.45);
  outline-offset: 4px;
  box-shadow: var(--panel-shadow-container);
  animation: panelBorderGlow 4s ease-in-out infinite;
  display: grid;
  grid-template-rows: 1fr;
}

/* COPIAR — escurecimento leve sobre bg (Adendo DS P1 §2) */
.panel-container__darken {
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.15);
  border-radius: 15px;
  z-index: 0; pointer-events: none;
}
```

---

# 4. COMPONENTE: VINHETA RADIAL

```css
/* COPIAR — vinheta radial (Adendo DS P1 §3) */
.panel-vignette {
  position: absolute; inset: 0;
  background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%);
  border-radius: var(--panel-radius-panel);
  z-index: 1; pointer-events: none;
}
```

---

# 5. COMPONENTE: SCAN LINE HUD

```css
/* COPIAR — scan line horizontal (Adendo DS P1 §5) */
.panel-scanline {
  position: absolute; left: 0; right: 0; height: 2px;
  background: linear-gradient(90deg,
    transparent 5%, rgba(212,168,67,0.15) 20%, rgba(212,168,67,0.3) 50%,
    rgba(212,168,67,0.15) 80%, transparent 95%);
  box-shadow: 0 0 8px rgba(212,168,67,0.15), 0 0 16px rgba(212,168,67,0.06);
  z-index: 3; pointer-events: none;
  animation: panelScanline 8s linear infinite;
}

.panel-scanline--paused { animation-play-state: paused; }
```

---

# 6. COMPONENTE: LETREIRO MARQUEE

```css
/* COPIAR — container letreiro (Adendo DS P1 §6) */
.panel-letreiro {
  position: absolute; top: 0; left: 50%;
  transform: translate(-50%, -60%);
  width: clamp(200px, 28vw, 420px);
  z-index: 20; pointer-events: none;
}

.panel-letreiro__halo {
  position: absolute; top: 50%; left: 50%;
  width: 160%; height: 250%;
  transform: translate(-50%, -50%);
  background: radial-gradient(ellipse at center,
    rgba(212,168,67,0.3) 0%, rgba(255,215,0,0.1) 30%,
    rgba(212,168,67,0.04) 50%, transparent 70%);
  pointer-events: none; z-index: 0;
  animation: panelHaloBreath 3s ease-in-out infinite;
}

.panel-letreiro__img {
  width: 100%; height: auto; display: block;
  filter: drop-shadow(0 0 12px rgba(212,168,67,0.4)) drop-shadow(0 0 25px rgba(212,168,67,0.15));
  position: relative; z-index: 1;
}

.panel-letreiro__text-container {
  position: absolute; inset: 0;
  display: flex; align-items: center;
  padding-bottom: 20px; overflow: hidden; z-index: 2;
  mask-image: linear-gradient(90deg, transparent 0%, #000 15%, #000 85%, transparent 100%);
  -webkit-mask-image: linear-gradient(90deg, transparent 0%, #000 15%, #000 85%, transparent 100%);
}

.panel-letreiro__marquee {
  white-space: nowrap;
  animation: panelMarqueeScroll 50s linear infinite;
}

.panel-letreiro__text {
  font-family: var(--panel-font-title);
  font-weight: 700; font-size: clamp(9px, 1.3vw, 18px);
  color: var(--panel-gold-pure);
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 0 30px rgba(255,215,0,0.2);
  letter-spacing: 3px; padding-right: 80px;
}
```

---

# 7. COMPONENTE: DOCK / TABS 5 ABAS

```css
/* COPIAR — dock ornamentado (Adendo DS P2 §8) */
.panel-dock {
  position: absolute; bottom: 0; left: 50%;
  transform: translate(-50%, 42%);
  width: 60%; z-index: 20;
}

.panel-dock__bg {
  width: 100%; height: auto; display: block;
  pointer-events: none;
  filter: drop-shadow(0 0 20px rgba(212,168,67,0.35));
}

.panel-dock__tabs {
  position: absolute; top: 50%; left: 8%; right: 8%;
  transform: translateY(-20%);
  display: grid; grid-template-columns: repeat(5, 1fr);
  align-items: center;
}

.panel-dock__tab {
  background: transparent; border: none; cursor: pointer;
  text-align: center; padding: 0; padding-top: 6px;
  font-family: var(--panel-font-title);
  font-size: clamp(9px, 1.1vw, 16px);
  font-weight: 900; letter-spacing: 2px;
  position: relative; min-height: var(--panel-touch-min);
  transition: color 0.3s ease, text-shadow 0.3s ease;
}

/* 5 estados */
.panel-dock__tab--active {
  color: var(--panel-green-neon);
  text-shadow: var(--panel-text-shadow-green);
  cursor: default;
}
.panel-dock__tab--inactive {
  color: var(--panel-gold-primary);
  text-shadow: 0 0 4px rgba(212,168,67,0.3);
}
.panel-dock__tab--inactive:hover {
  color: var(--panel-gold-pure);
  text-shadow: 0 0 10px rgba(255,215,0,0.5), 0 0 20px rgba(255,215,0,0.25);
}
.panel-dock__tab--active:hover {
  color: var(--panel-green-neon);
  text-shadow: var(--panel-text-shadow-green);
}
.panel-dock__tab--disabled {
  color: var(--panel-text-muted); text-shadow: none;
  opacity: 0.4; pointer-events: none; cursor: not-allowed;
}

/* Barra ativa — layoutId spring */
.panel-dock__active-bar {
  position: absolute; top: -2px; left: 15%; right: 15%; height: 3px;
  background: linear-gradient(90deg, transparent, #00E676, transparent);
  box-shadow: 0 0 10px rgba(0,230,118,0.6), 0 2px 8px rgba(0,230,118,0.3);
  border-radius: 0 0 2px 2px;
}
```

---

# 8. COMPONENTE: GAME CARD (7 camadas + 4 estados)

```css
/* COPIAR — card container (Adendo DS P2 §9) */
.panel-card {
  position: relative; width: 100%; height: 100%;
  border-radius: var(--panel-radius-card);
  overflow: hidden; cursor: pointer;
  transition: border 0.3s ease, box-shadow 0.3s ease, outline 0.3s ease;
}

/* Estado 1: idle */
.panel-card--idle {
  border: 2px solid rgba(212,168,67,0.3);
  outline: 1.5px solid rgba(191,149,63,0.1);
  outline-offset: 2px;
  box-shadow: var(--panel-shadow-card-idle);
}

/* Estado 2: hover */
.panel-card--idle:hover {
  border: 2px solid rgba(212,168,67,0.7);
  outline: 1.5px solid rgba(0,230,118,0.4);
  outline-offset: 4px;
  box-shadow: var(--panel-shadow-card-hover);
}

/* Estado 3: pressed */
.panel-card--pressed {
  border: 2px solid rgba(255,215,0,0.8);
  outline: 1.5px solid rgba(255,215,0,0.5);
  outline-offset: 2px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.4), 0 0 15px rgba(255,215,0,0.2);
  transform: scale(0.98);
}

/* Estado 4: disabled */
.panel-card--disabled {
  border: 2px solid rgba(212,168,67,0.1);
  outline: none; box-shadow: 0 2px 8px rgba(0,0,0,0.3);
  pointer-events: none; filter: brightness(0.7);
}

/* Camada 2: imagem */
.panel-card__img {
  position: absolute; inset: 0;
  width: 100%; height: 100%;
  object-fit: cover; border-radius: 12px;
}

/* Camada 3: gradiente topo */
.panel-card__grad-top {
  position: absolute; top: 0; left: 0; right: 0; height: 55%;
  background: linear-gradient(to bottom, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0.3) 60%, transparent 100%);
  border-radius: 12px 12px 0 0; pointer-events: none; z-index: 2;
}

/* Camada 4: gradiente base */
.panel-card__grad-bot {
  position: absolute; bottom: 0; left: 0; right: 0; height: 55%;
  background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0.3) 50%, transparent 100%);
  border-radius: 0 0 12px 12px; pointer-events: none; z-index: 2;
}

/* Camada 7a: spotlight mouse-tracking */
.panel-card__spotlight {
  position: absolute; inset: 0;
  border-radius: 12px; pointer-events: none; z-index: 5;
  opacity: 0; transition: opacity 0.3s ease;
}
.panel-card--idle:hover .panel-card__spotlight { opacity: 1; }

/* Logo bilingue */
.panel-card__logo {
  position: absolute; top: clamp(6px,1vw,12px); left: 50%;
  transform: translateX(-50%);
  width: clamp(50px,8vw,100px);
  filter: drop-shadow(0 2px 6px rgba(0,0,0,0.9));
  pointer-events: none; z-index: 6;
}

/* Nome do jogo */
.panel-card__name {
  position: absolute; bottom: clamp(8px,1.2vw,16px); left: 50%;
  transform: translateX(-50%);
  font-family: var(--panel-font-title);
  font-weight: 700; font-size: clamp(10px,1.3vw,16px);
  color: var(--panel-white);
  text-shadow: 0 2px 6px rgba(0,0,0,0.8);
  text-transform: uppercase; letter-spacing: 1px;
  white-space: nowrap; z-index: 6; pointer-events: none;
}
```

---

# 9. COMPONENTE: FEIXO VERDE (base do card)

```css
/* COPIAR — feixo verde permanente (Adendo DS P2 §10, DS §7.2) */
.panel-card__feixo {
  position: absolute; bottom: 0; left: 0; right: 0; height: 3px;
  background: linear-gradient(90deg,
    transparent 5%, rgba(0,230,118,0.3) 20%, #00E676 50%,
    rgba(0,230,118,0.3) 80%, transparent 95%);
  box-shadow: 0 0 8px rgba(0,230,118,0.3);
  border-radius: 0 0 12px 12px;
  pointer-events: none; z-index: 4;
  transition: box-shadow 0.3s ease;
}

.panel-card--idle:hover .panel-card__feixo {
  box-shadow: 0 0 15px rgba(0,230,118,0.5), 0 0 30px rgba(0,230,118,0.2);
}

/* Variante LOJA: feixo dourado (Adendo DS P4 §22.4) */
.panel-card__feixo--gold {
  background: linear-gradient(90deg,
    transparent 5%, rgba(212,168,67,0.3) 20%, #D4A843 50%,
    rgba(212,168,67,0.3) 80%, transparent 95%);
  box-shadow: 0 0 8px rgba(212,168,67,0.3);
}
```

---

# 10. COMPONENTE: LINHA DOURADA (topo do card)

```css
/* COPIAR — linha dourada sutil (Adendo DS P2 §10, DS §5.3) */
.panel-card__gold-line {
  position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg,
    transparent 10%, rgba(212,168,67,0.3) 30%, rgba(255,215,0,0.5) 50%,
    rgba(212,168,67,0.3) 70%, transparent 90%);
  pointer-events: none; z-index: 4;
  opacity: 0.5; transition: opacity 0.3s ease;
}

.panel-card--idle:hover .panel-card__gold-line { opacity: 1.0; }
```

---

# 11. COMPONENTE: SHINE SWEEP (animacao no card)

```css
/* COPIAR — shine sweep horizontal (Adendo DS P2 §10, DS §20.4) */
.panel-card__shine {
  position: absolute; inset: 0;
  border-radius: 12px;
  background: linear-gradient(105deg,
    transparent 40%, rgba(255,215,0,0.06) 45%, rgba(255,215,0,0.12) 50%,
    rgba(255,215,0,0.06) 55%, transparent 60%);
  background-size: 200% 100%;
  animation: panelCardShine 2s ease infinite;
  pointer-events: none; z-index: 5;
}
```

---

# 12. COMPONENTE: HERO CAROUSEL

```css
/* COPIAR — container hero (Adendo DS P2 §11) */
.panel-hero {
  border-radius: 16px; position: relative; overflow: hidden;
  box-shadow:
    0 0 20px rgba(212,168,67,0.15),
    inset 0 0 25px rgba(0,0,0,0.4),
    0 8px 25px rgba(0,0,0,0.5);
  width: 100%; height: 100%;
}

.panel-hero__pedestal {
  width: 100%; height: 100%;
  object-fit: contain; object-position: center;
}

.panel-hero__logo {
  position: absolute; top: clamp(8px,1.2vw,16px); left: 50%;
  transform: translateX(-50%);
  width: clamp(80px,12vw,160px);
  filter: drop-shadow(0 2px 8px rgba(0,0,0,0.8));
  z-index: 3; pointer-events: none;
}

/* Arrows 3 estados */
.panel-hero__arrow {
  position: absolute; top: 50%; transform: translateY(-50%);
  width: clamp(28px,3vw,40px); height: clamp(28px,3vw,40px);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: clamp(12px,1.5vw,18px);
  cursor: pointer;
  min-height: var(--panel-touch-min);
  min-width: var(--panel-touch-min);
  z-index: 4; transition: all 0.25s ease;
}
.panel-hero__arrow--left  { left: clamp(8px,1vw,14px); }
.panel-hero__arrow--right { right: clamp(8px,1vw,14px); }

.panel-hero__arrow--idle {
  background: rgba(0,0,0,0.5);
  border: 1px solid rgba(212,168,67,0.3);
  color: var(--panel-gold-primary);
}
.panel-hero__arrow--idle:hover {
  background: rgba(0,0,0,0.8);
  border-color: var(--panel-gold-pure);
  color: var(--panel-gold-pure);
  box-shadow: 0 0 10px rgba(255,215,0,0.2);
}
.panel-hero__arrow--idle:active {
  transform: translateY(-50%) scale(0.9);
  box-shadow: 0 0 5px rgba(255,215,0,0.1);
}

/* Play button PNG */
.panel-hero__play-btn {
  position: absolute; bottom: clamp(12px,2vw,24px); left: 50%;
  transform: translateX(-50%);
  width: clamp(100px,14vw,180px);
  cursor: pointer; z-index: 4; transition: all 0.25s ease;
}
.panel-hero__play-btn--idle {
  filter: drop-shadow(0 2px 8px rgba(0,0,0,0.5));
}
.panel-hero__play-btn--idle:hover {
  filter: brightness(1.15) drop-shadow(0 0 8px rgba(255,215,0,0.4)) drop-shadow(0 2px 8px rgba(0,0,0,0.5));
}
.panel-hero__play-btn--idle:active {
  transform: translateX(-50%) scale(0.95);
}

/* Raios de luz dourada (Adendo DS P2 §11 — beams) */
.panel-hero__beams::before {
  content: ''; position: absolute; top: -20%; left: 50%;
  transform: translateX(-50%); width: 120%; height: 80%;
  background:
    radial-gradient(ellipse 30% 60% at 30% 0%, rgba(212,168,67,0.06) 0%, transparent 70%),
    radial-gradient(ellipse 25% 50% at 70% 10%, rgba(255,215,0,0.04) 0%, transparent 70%),
    radial-gradient(ellipse 40% 40% at 50% 5%, rgba(212,168,67,0.05) 0%, transparent 60%);
  pointer-events: none;
}
```

---

**ITENS CHECKLIST COBERTOS NESTA PARTE:**

| Painel | Mestre | Componente |
|:------:|:------:|-----------|
| 7.1 Arquivo unico | 7.1,7.2 Numeracao | SIM — 1 arquivo, 12 componentes numerados |
| 7.2 Fundo+vinheta | 7.3 Fundo | §3 Container + §4 Vinheta |
| 7.3 Container | 7.4 Container | §3 Panel 5 shadows |
| 7.5 Dock | — | §7 (5 estados + bar) |
| 7.6 Game Card | 7.5 Card hover | §8 (7 camadas + 4 estados) |
| 7.7 Hero | — | §12 (pedestal, arrows 3 est, play 3 est, beams) |
| 7.8 Feixo | 7.6 Feixo verde | §9 (isolado + variante LOJA dourada) |
| 7.9 Linha | 7.7 Linha dourada | §10 (isolado + hover intensifica) |
| 7.10 Shine | 7.8 Shine sweep | §11 (panelCardShine 2s) |

**PENDENTES P2:** 7.4 Header/Saldo, 7.11 Glassmorphism, 7.12 Tooltip, 7.13 Imagens, 7.14 PROIBIDO + Mestre 7.9-7.11 Botões, 7.12 Glass, 7.13 Tooltip, 7.14 Textos, 7.15 Glow, 7.16-7.17 Input/Atalhos, 7.18 Específicos, 7.19 Imagens, 7.20 PROIBIDO, 7.21 Estados

---

**[FIM DA PARTE 1 — Continua com Componentes 13-22: Glassmorphism, Tooltip, Botoes 3, Textos 4, Glow 3, Input, Especificos, Imagens, PROIBIDO, Estados]**

`[ENTREGA: 7.CSS-COMPONENTES-PAINEL-FLUTUANTE-PARTE-1.md — 23/03/2026]`
