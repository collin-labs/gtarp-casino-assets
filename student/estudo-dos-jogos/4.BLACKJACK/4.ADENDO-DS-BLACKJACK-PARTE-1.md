# ADENDO DESIGN SYSTEM - BLACKJACK (#4)
## PARTE 1 DE 3 - Componentes CSS Real (1.1-1.8)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 19/03/2026

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1, 2 e 3) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 15 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.bj-[block]__[element]--[modifier]`
- Block: `bj-game`, `bj-mesa`, `bj-card`, `bj-hand`, `bj-chip`, `bj-bet`, `bj-sidebet`, `bj-btn`, `bj-overlay`, `bj-insurance`, `bj-history`, `bj-pf`
- Modifiers: `--face-up`, `--face-down`, `--flipping`, `--active`, `--completed`, `--bust`, `--win`, `--bj-natural`, `--push`, `--surrender`, `--disabled`, `--chip-1`, `--chip-5`, `--chip-25`, `--chip-100`, `--chip-500`

**Pesquisa REGRA X0:** BEM naming 2026 confirmado (getbem.com, sparkbox.com, sitepoint.com Nov/2024). `.block__element--modifier` padrao. CSS `perspective: 1000px` + `transform-style: preserve-3d` + `backface-visibility: hidden` para card flip 3D confirmado (DevToolbox Feb/2026, W3Schools). `-webkit-backface-visibility` necessario para Chromium 93 FiveM NUI.

---

# SECAO 1 - COMPONENTES COM CSS REAL (15 Componentes)

## 1.1 Container Principal do Jogo (DS P1 P4.2)

Quando o Blackjack abre dentro do painel (z-index 60), o fundo NUNCA e flat:

```css
/* COPIAR EXATAMENTE - fundo de qualquer tela do Blackjack */
.bj-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
  padding: clamp(6px, 1vw, 12px);
  gap: clamp(4px, 0.6vw, 8px);
}

/* COPIAR - fallback NUI (bg-casino.png nao carregado) */
.bj-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}
```

---

## 1.2 Mesa de Blackjack (DS P1 P4.2 + P7.1 adaptado)

A mesa DOM com feltro verde e moldura dourada ornamentada:

```css
/* COPIAR EXATAMENTE - mesa de blackjack com feltro e moldura */
.bj-mesa {
  flex: 1;
  position: relative;
  background: #1a472a;
  border: 3px solid rgba(212,168,67,0.4);
  border-radius: clamp(12px, 2vw, 20px);
  box-shadow:
    0 0 20px rgba(212,168,67,0.1),
    0 8px 32px rgba(0,0,0,0.6),
    inset 0 0 100px rgba(0,0,0,0.3),
    0 0 0 3px rgba(139,105,20,0.25);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  padding: clamp(8px, 1.5vw, 16px);
  overflow: hidden;
  min-height: clamp(280px, 50vh, 500px);
}

/* COPIAR - vinheta interna sutil para profundidade */
.bj-mesa::before {
  content: '';
  position: absolute; inset: 0;
  border-radius: inherit;
  box-shadow: inset 0 0 60px rgba(0,0,0,0.4);
  pointer-events: none;
  z-index: 1;
}

/* COPIAR - textura sutil do feltro */
.bj-mesa::after {
  content: '';
  position: absolute; inset: 0;
  border-radius: inherit;
  background: repeating-linear-gradient(
    0deg,
    transparent,
    transparent 2px,
    rgba(0,0,0,0.015) 2px,
    rgba(0,0,0,0.015) 4px
  );
  pointer-events: none;
  z-index: 1;
}
```

---

## 1.3 Carta - Face Up (DS Custom + P3A easing)

Cada carta tem container com `perspective`, inner com `preserve-3d`, e dois filhos (front/back):

```css
/* COPIAR EXATAMENTE - container da carta com perspective */
.bj-card {
  width: clamp(48px, 8vw, 72px);
  height: clamp(68px, 11.5vw, 104px);
  perspective: 1000px;
  position: relative;
  flex-shrink: 0;
}

/* COPIAR - inner que rotaciona (Framer Motion controla rotateY) */
.bj-card__inner {
  position: relative;
  width: 100%;
  height: 100%;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  /* Framer Motion animate={{ rotateY: isFlipped ? 0 : 180 }} */
  /* transition: { type: "spring", stiffness: 300, damping: 25 } */
}

/* COPIAR - face frontal (valor + naipe) */
.bj-card__front {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  background: #FFFFFF;
  border-radius: clamp(4px, 0.6vw, 6px);
  border: 1px solid rgba(0,0,0,0.1);
  box-shadow: 0 2px 8px rgba(0,0,0,0.3);
  display: flex;
  flex-direction: column;
  padding: clamp(2px, 0.3vw, 4px);
  overflow: hidden;
}

/* COPIAR - valor top-left */
.bj-card__value {
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: clamp(10px, 1.5vw, 16px);
  line-height: 1;
}

/* COPIAR - naipe top-left (abaixo do valor) */
.bj-card__suit-small {
  font-size: clamp(8px, 1.2vw, 12px);
  line-height: 1;
  margin-top: -1px;
}

/* COPIAR - naipe grande centro */
.bj-card__suit-center {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: clamp(16px, 2.5vw, 28px);
}

/* COPIAR - valor bottom-right invertido */
.bj-card__value-bottom {
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: clamp(10px, 1.5vw, 16px);
  line-height: 1;
  align-self: flex-end;
  transform: rotate(180deg);
}

/* COPIAR - cores por naipe */
.bj-card--hearts .bj-card__value,
.bj-card--hearts .bj-card__suit-small,
.bj-card--hearts .bj-card__suit-center,
.bj-card--hearts .bj-card__value-bottom,
.bj-card--diamonds .bj-card__value,
.bj-card--diamonds .bj-card__suit-small,
.bj-card--diamonds .bj-card__suit-center,
.bj-card--diamonds .bj-card__value-bottom {
  color: #C62828;
}

.bj-card--spades .bj-card__value,
.bj-card--spades .bj-card__suit-small,
.bj-card--spades .bj-card__suit-center,
.bj-card--spades .bj-card__value-bottom,
.bj-card--clubs .bj-card__value,
.bj-card--clubs .bj-card__suit-small,
.bj-card--clubs .bj-card__suit-center,
.bj-card--clubs .bj-card__value-bottom {
  color: #1A1A1A;
}
```

---

## 1.4 Carta - Face Down / Back (DS Custom)

```css
/* COPIAR EXATAMENTE - verso da carta design Blackout Casino */
.bj-card__back {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  transform: rotateY(180deg);
  background: linear-gradient(135deg, #1A1A1A 0%, #0A0A0A 50%, #1A1A1A 100%);
  border: 2px solid rgba(212,168,67,0.3);
  border-radius: clamp(4px, 0.6vw, 6px);
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

/* COPIAR - padrao diamante decorativo no verso */
.bj-card__back::before {
  content: '';
  position: absolute; inset: 4px;
  border: 1px solid rgba(212,168,67,0.12);
  border-radius: clamp(2px, 0.3vw, 4px);
  background: repeating-linear-gradient(
    45deg,
    transparent,
    transparent 5px,
    rgba(212,168,67,0.04) 5px,
    rgba(212,168,67,0.04) 10px
  );
}

/* COPIAR - logo "BC" central no verso */
.bj-card__back-logo {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 12px);
  color: rgba(212,168,67,0.2);
  text-transform: uppercase;
  letter-spacing: 2px;
  z-index: 1;
}

/* COPIAR - glow durante flip (hole card reveal) */
.bj-card--revealing .bj-card__back {
  box-shadow: 0 0 20px rgba(212,168,67,0.4), 0 2px 8px rgba(0,0,0,0.4);
  transition: box-shadow 0.4s ease;
}
```

---

## 1.5 Carta - Animacao Deal (slide do shoe) (DS P3A easing)

```css
/* COPIAR EXATAMENTE - carta entrando na mesa vinda do shoe */
/* Usa Framer Motion - CSS abaixo e para fallback/referencia */
.bj-card--dealing {
  /* Framer Motion props:
     initial: { x: shoeX, y: shoeY, opacity: 0, rotateY: 180 }
     animate: { x: targetX, y: targetY, opacity: 1, rotateY: isFaceUp ? 0 : 180 }
     transition: {
       x: { duration: 0.4, ease: [0.25, 0.1, 0.25, 1] },
       y: { duration: 0.4, ease: [0.25, 0.1, 0.25, 1] },
       opacity: { duration: 0.2 },
       rotateY: { type: "spring", stiffness: 300, damping: 25, delay: 0.2 }
     }
  */
}

/* COPIAR - stagger entre 4 cartas do deal */
/* Carta 1 (player): delay 0s */
/* Carta 2 (dealer up): delay 0.25s */
/* Carta 3 (player): delay 0.5s */
/* Carta 4 (dealer down): delay 0.75s */
/* Som: card_slide.mp3 a cada carta, card_flip.mp3 em cada face-up */

/* COPIAR - carta hit do jogador (durante PLAYER_TURN) */
.bj-card--hitting {
  /* Framer Motion:
     initial: { x: shoeX, y: shoeY, opacity: 0, rotateY: 180 }
     animate: { x: targetX, y: targetY, opacity: 1, rotateY: 0 }
     transition: { duration: 0.4, rotateY spring 300/25 delay 0.15 }
  */
}

/* COPIAR - carta do dealer hit (durante DEALER_TURN) */
.bj-card--dealer-hitting {
  /* Framer Motion identico ao hitting mas com stagger 500ms entre cartas */
  /* Som: card_slide.mp3 + card_flip.mp3 por carta */
}
```

---

## 1.6 Mao (Hand Container) (DS Custom)

```css
/* COPIAR EXATAMENTE - container de cartas (player ou dealer) */
.bj-hand {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
  min-height: clamp(72px, 12vw, 108px);
  z-index: 2;
}

/* COPIAR - offset entre cartas (overlap parcial) */
.bj-hand .bj-card + .bj-card {
  margin-left: clamp(-12px, -1.5vw, -8px);
}

/* COPIAR - mao ativa em split (borda verde) */
.bj-hand--active {
  padding: clamp(4px, 0.6vw, 8px);
  border: 1.5px solid rgba(0,230,118,0.5);
  border-radius: 10px;
  box-shadow: 0 0 12px rgba(0,230,118,0.15);
  background: rgba(0,230,118,0.03);
}

/* COPIAR - mao inativa em split (aguardando) */
.bj-hand--inactive {
  padding: clamp(4px, 0.6vw, 8px);
  border: 1.5px solid rgba(255,255,255,0.05);
  border-radius: 10px;
  background: rgba(255,255,255,0.01);
  opacity: 0.7;
}

/* COPIAR - mao completa em split (ja jogou) */
.bj-hand--completed {
  padding: clamp(4px, 0.6vw, 8px);
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 10px;
  opacity: 0.5;
}

/* COPIAR - label da mao em split */
.bj-hand__label {
  position: absolute;
  top: clamp(-14px, -2vw, -10px);
  left: 50%;
  transform: translateX(-50%);
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(8px, 1vw, 11px);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}
.bj-hand--active .bj-hand__label { color: #00E676; }
.bj-hand--inactive .bj-hand__label { color: #666666; }
.bj-hand--completed .bj-hand__label { color: rgba(212,168,67,0.5); }

/* COPIAR - indicador dot verde (mao ativa) */
.bj-hand__active-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: #00E676;
  box-shadow: 0 0 6px rgba(0,230,118,0.5);
  position: absolute;
  bottom: clamp(-10px, -1.5vw, -6px);
  left: 50%;
  transform: translateX(-50%);
  animation: bjDotPulse 2s ease infinite;
}
@keyframes bjDotPulse {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  50% { transform: translateX(-50%) translateY(-2px); }
}
```

---

## 1.7 Hand Total Badge (DS P1 P3)

```css
/* COPIAR EXATAMENTE - badge de total da mao */
.bj-hand__total {
  display: inline-flex;
  align-items: center;
  gap: clamp(2px, 0.3vw, 4px);
  margin-top: clamp(2px, 0.3vw, 4px);
  background: rgba(0,0,0,0.75);
  padding: clamp(2px, 0.4vw, 4px) clamp(6px, 1vw, 10px);
  border-radius: 6px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  z-index: 3;
  transition: all 0.2s ease;
}

/* COPIAR - estados por valor */
.bj-hand__total--normal { color: #00E676; }
.bj-hand__total--soft {
  color: #00E676;
}
.bj-hand__total--soft::before {
  content: 'Soft ';
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1.1vw, 12px);
  color: #A8A8A8;
}
.bj-hand__total--twentyone {
  color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.5);
}
.bj-hand__total--blackjack {
  color: #FFD700;
  text-shadow: 0 0 12px rgba(255,215,0,0.6);
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(14px, 2vw, 22px);
  letter-spacing: 2px;
  animation: bjTotalGlow 1.5s ease infinite;
}
.bj-hand__total--bust {
  color: #FF1744;
  text-shadow: 0 0 8px rgba(255,23,68,0.4);
}
.bj-hand__total--dealer-hidden {
  color: #A8A8A8;
}

/* COPIAR - bounce ao receber nova carta */
.bj-hand__total--bounce {
  animation: bjTotalBounce 0.3s ease;
}
@keyframes bjTotalBounce {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.15); }
}

/* COPIAR - glow para blackjack natural */
@keyframes bjTotalGlow {
  0%, 100% { text-shadow: 0 0 12px rgba(255,215,0,0.6); }
  50% { text-shadow: 0 0 20px rgba(255,215,0,0.9), 0 0 40px rgba(255,215,0,0.3); }
}
```

---

## 1.8 Chip Individual - 5 Variantes (DS P1 P4.4 + Custom)

```css
/* COPIAR EXATAMENTE - base de chip compartilhada */
.bj-chip {
  width: clamp(36px, 5.5vw, 52px);
  height: clamp(36px, 5.5vw, 52px);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 13px);
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}
.bj-chip:hover {
  transform: scale(1.1);
  box-shadow: 0 0 12px rgba(212,168,67,0.4);
}
.bj-chip:active {
  transform: scale(0.95);
}
.bj-chip--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  pointer-events: none;
}
/* COPIAR - ring de selecao (chip ativa/selecionada) */
.bj-chip--selected {
  box-shadow: 0 0 16px rgba(0,230,118,0.4);
  transform: scale(1.15);
}
.bj-chip--selected::after {
  content: '';
  position: absolute; inset: -3px;
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,0.3);
}

/* COPIAR EXATAMENTE - chip 1 GCoin (branco) */
.bj-chip--1 {
  background: radial-gradient(circle at 35% 35%, #FFFFFF, #E0E0E0, #BDBDBD);
  border: 2px solid rgba(189,189,189,0.6);
  color: #333333;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3), inset 0 1px 2px rgba(255,255,255,0.5);
}

/* COPIAR EXATAMENTE - chip 5 GCoins (vermelho) */
.bj-chip--5 {
  background: radial-gradient(circle at 35% 35%, #FF5252, #E53935, #C62828);
  border: 2px solid rgba(229,57,53,0.6);
  color: #FFFFFF;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3), inset 0 1px 2px rgba(255,82,82,0.4);
}

/* COPIAR EXATAMENTE - chip 25 GCoins (verde) */
.bj-chip--25 {
  background: radial-gradient(circle at 35% 35%, #66BB6A, #43A047, #2E7D32);
  border: 2px solid rgba(67,160,71,0.6);
  color: #FFFFFF;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3), inset 0 1px 2px rgba(102,187,106,0.4);
}

/* COPIAR EXATAMENTE - chip 100 GCoins (preto + dourado) */
.bj-chip--100 {
  background: radial-gradient(circle at 35% 35%, #424242, #212121, #0A0A0A);
  border: 2px solid rgba(212,168,67,0.5);
  color: #D4A843;
  box-shadow: 0 2px 6px rgba(0,0,0,0.4), inset 0 1px 2px rgba(66,66,66,0.4);
}

/* COPIAR EXATAMENTE - chip 500 GCoins (roxo) */
.bj-chip--500 {
  background: radial-gradient(circle at 35% 35%, #AB47BC, #7B1FA2, #4A148C);
  border: 2px solid rgba(123,31,162,0.6);
  color: #FFFFFF;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3), inset 0 1px 2px rgba(171,71,188,0.4);
}

/* COPIAR - marcas decorativas nas bordas do chip */
.bj-chip::before {
  content: '';
  position: absolute; inset: 3px;
  border-radius: 50%;
  border: 1.5px dashed rgba(255,255,255,0.15);
  pointer-events: none;
}

/* COPIAR - mini stack visual (chip na area de bet) */
.bj-chip--stacked {
  cursor: default;
  pointer-events: none;
}
.bj-chip--stacked + .bj-chip--stacked {
  margin-top: -4px;
}
```

---

**[FIM DA PARTE 1 - Continua Parte 2: Componentes 1.9-1.15 (Bet Area, Side Bet, Botoes Acao 5 individuais, DEAL, VOLTAR, Saldo, Insurance Modal)]**

`[ENTREGA: 4.ADENDO-DS-BLACKJACK-PARTE-1.md - 19/03/2026]`
