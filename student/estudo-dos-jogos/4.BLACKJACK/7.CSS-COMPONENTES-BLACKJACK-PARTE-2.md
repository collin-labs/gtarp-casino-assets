# CSS-COMPONENTES - BLACKJACK (#4)
## Parte 2 de 2 - Componentes 11-20 (Botoes, Saldo, Insurance, Overlays)
## Colar JUNTO com Parte 1 e PROMPT-V0
### Blackout Casino GTARP - 19/03/2026

---

# 11. BOTAO HIT (Adendo DS P2 1.11)

```css
.bj-btn--hit {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px); min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn--hit:hover {
  box-shadow: 0 4px 16px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.bj-btn--hit:active { transform: translateY(1px); }
.bj-btn--hit[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 12. BOTAO STAND (Adendo DS P2 1.12)

```css
.bj-btn--stand {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #D4A843 0%, #CB9B51 50%, #8B6914 100%);
  border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px); min-height: 44px;
  box-shadow: 0 2px 8px rgba(212,168,67,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn--stand:hover {
  box-shadow: 0 4px 16px rgba(212,168,67,0.3);
  transform: translateY(-1px);
}
.bj-btn--stand:active { transform: translateY(1px); }
.bj-btn--stand[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 13. BOTAO DOUBLE (Adendo DS P2 1.13)

```css
.bj-btn--double {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #448AFF 0%, #2962FF 50%, #1A237E 100%);
  border: 1.5px solid rgba(68,138,255,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px); min-height: 44px;
  box-shadow: 0 2px 8px rgba(68,138,255,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn--double:hover {
  box-shadow: 0 4px 16px rgba(68,138,255,0.3);
  transform: translateY(-1px);
}
.bj-btn--double:active { transform: translateY(1px); }
.bj-btn--double[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 14. BOTAO SPLIT (Adendo DS P2 1.14)

```css
.bj-btn--split {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #B388FF 0%, #7C4DFF 50%, #4A148C 100%);
  border: 1.5px solid rgba(179,136,255,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px); min-height: 44px;
  box-shadow: 0 2px 8px rgba(179,136,255,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn--split:hover {
  box-shadow: 0 4px 16px rgba(179,136,255,0.3);
  transform: translateY(-1px);
}
.bj-btn--split:active { transform: translateY(1px); }
.bj-btn--split[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 15. BOTAO SURRENDER (Adendo DS P2 1.15)

```css
.bj-btn--surrender {
  padding: clamp(8px, 1vw, 14px) clamp(16px, 2.5vw, 28px);
  background: linear-gradient(180deg, #FF5252 0%, #D32F2F 50%, #B71C1C 100%);
  border: 1.5px solid rgba(255,82,82,0.4);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer;
  min-width: clamp(60px, 10vw, 80px); min-height: 44px;
  box-shadow: 0 2px 8px rgba(255,82,82,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn--surrender:hover {
  box-shadow: 0 4px 16px rgba(255,82,82,0.3);
  transform: translateY(-1px);
}
.bj-btn--surrender:active { transform: translateY(1px); }
.bj-btn--surrender[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 16. BOTAO DEAL CTA (Adendo DS P2 1.16)

```css
.bj-btn-deal {
  padding: clamp(10px, 1.5vw, 16px) clamp(24px, 4vw, 48px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 10px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  text-transform: uppercase; letter-spacing: 2px;
  cursor: pointer;
  min-width: clamp(120px, 20vw, 200px); min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative; overflow: hidden;
}

/* Bevel highlight topo */
.bj-btn-deal::before {
  content: ''; position: absolute;
  top: 0; left: 10%; right: 10%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  pointer-events: none;
}

.bj-btn-deal:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3), inset 0 1px 0 rgba(255,255,255,0.15);
  transform: translateY(-1px);
}
.bj-btn-deal:active { transform: translateY(1px); }
.bj-btn-deal[disabled] {
  opacity: 0.35; cursor: not-allowed; pointer-events: none;
  background: #1A1A1A; border-color: rgba(255,255,255,0.05); box-shadow: none;
}
```

---

# 17. BOTAO VOLTAR + SALDO (Adendo DS P2 1.17-1.18)

```css
/* Botao VOLTAR (top-left) */
.bj-btn-back {
  position: absolute;
  top: clamp(8px, 1.5vw, 16px); left: clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  color: #D4A843;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.2vw, 16px); letter-spacing: 2px;
  padding: clamp(6px, 0.8vw, 10px) clamp(10px, 1.5vw, 18px);
  min-height: 44px; cursor: pointer; z-index: 10;
  display: flex; align-items: center; gap: 5px;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-btn-back:hover {
  border-color: rgba(212,168,67,0.8);
  transform: scale(1.05);
  box-shadow: 0 0 12px rgba(212,168,67,0.15);
}
.bj-btn-back:active { transform: scale(0.95); }

/* Ghost variant (overlay resultado) */
.bj-btn-back--ghost {
  position: static;
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  font-size: clamp(10px, 1.3vw, 14px);
}
.bj-btn-back--ghost:hover {
  border-color: rgba(212,168,67,0.6);
  background: rgba(212,168,67,0.05);
}

/* Saldo display (top-right) */
.bj-balance {
  position: absolute;
  top: clamp(8px, 1.5vw, 16px); right: clamp(8px, 1.5vw, 16px);
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(13px, 1.8vw, 20px);
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  z-index: 10; pointer-events: none;
  display: flex; align-items: baseline; gap: 2px;
}
.bj-balance__prefix { font-size: 0.65em; opacity: 0.7; }

/* Saldo flash ganhou */
.bj-balance--win { animation: bjBalanceWin 0.8s ease; }

/* Saldo flash perdeu */
.bj-balance--lose { animation: bjBalanceLose 0.5s ease; }
```

---

# 18. INSURANCE MODAL (Adendo DS P2 1.19)

```css
/* Overlay escurecimento */
.bj-insurance-overlay {
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  display: flex; align-items: center; justify-content: center;
  z-index: 70;
}

/* Modal central */
.bj-insurance {
  background: rgba(10,10,10,0.95);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 16px;
  padding: clamp(16px, 3vw, 32px);
  min-width: clamp(240px, 40vw, 360px);
  display: flex; flex-direction: column;
  align-items: center; gap: clamp(10px, 1.5vw, 18px);
  box-shadow: 0 8px 32px rgba(0,0,0,0.6), 0 0 20px rgba(212,168,67,0.05);
}

/* Titulo */
.bj-insurance__title {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(16px, 2.5vw, 24px);
  text-transform: uppercase; letter-spacing: 2px;
  color: #D4A843;
  text-shadow: 0 0 12px rgba(255,215,0,0.4);
}

/* Timer urgente */
.bj-insurance__timer--urgent {
  color: #FF3B3B;
  animation: bjInsuranceUrgent 0.5s ease infinite;
}

/* Botao SIM */
.bj-insurance__btn-yes {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px; color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer; min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s ease;
}
.bj-insurance__btn-yes:hover { box-shadow: 0 4px 16px rgba(0,200,83,0.3); transform: translateY(-1px); }
.bj-insurance__btn-yes[disabled] { opacity: 0.35; cursor: not-allowed; background: #1A1A1A; }

/* Botao NAO */
.bj-insurance__btn-no {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: transparent;
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 8px; color: #D4A843;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer; min-height: 44px;
  transition: all 0.2s ease;
}
.bj-insurance__btn-no:hover {
  border-color: rgba(212,168,67,0.6);
  background: rgba(212,168,67,0.05);
}
```

---

# 19. OVERLAY RESULTADO - BASE + 5 VARIANTES (Adendo DS P3 1.20)

```css
/* Base compartilhada */
.bj-overlay {
  position: absolute; inset: 0; z-index: 80;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(12px, 2vw, 20px);
}
.bj-overlay__title {
  font-family: 'Cinzel', serif; font-weight: 900;
  text-transform: uppercase; letter-spacing: 3px;
  position: relative;
}
.bj-overlay__payout {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
}
.bj-overlay__buttons {
  display: flex; gap: clamp(8px, 1.5vw, 16px);
  margin-top: clamp(8px, 1.5vw, 16px);
}

/* WIN */
.bj-overlay--win {
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px);
}
.bj-overlay--win .bj-overlay__title {
  font-size: clamp(20px, 4vw, 40px);
  color: #00E676;
  text-shadow: 0 0 20px rgba(0,230,118,0.5);
}
.bj-overlay--win .bj-overlay__payout {
  font-size: clamp(16px, 3vw, 32px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.6);
}

/* BUST */
.bj-overlay--bust {
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
}
.bj-overlay--bust .bj-overlay__title {
  font-size: clamp(20px, 4vw, 36px);
  color: #FF1744;
  text-shadow: 0 0 15px rgba(255,23,68,0.4);
}
.bj-overlay--bust .bj-overlay__payout {
  font-size: clamp(14px, 2.5vw, 24px);
  color: #FF1744; opacity: 0.7;
}

/* BLACKJACK NATURAL */
.bj-overlay--natural {
  background: rgba(0,0,0,0.85);
  backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
}
.bj-overlay--natural .bj-overlay__title {
  font-size: clamp(24px, 5vw, 48px);
  color: #FFD700;
  text-shadow: 0 0 20px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.3);
  overflow: hidden;
}
.bj-overlay--natural .bj-overlay__title::after {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(90deg, transparent 0%, rgba(255,215,0,0.3) 50%, transparent 100%);
  animation: bjShimmerSweep 1.5s ease-in-out;
}
.bj-overlay--natural .bj-overlay__payout {
  font-size: clamp(18px, 3.5vw, 36px);
  color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.6);
}

/* PUSH */
.bj-overlay--push {
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
}
.bj-overlay--push .bj-overlay__title {
  font-size: clamp(18px, 3.5vw, 32px);
  color: #FFD700;
  text-shadow: 0 0 10px rgba(255,215,0,0.3);
}

/* SURRENDER */
.bj-overlay--surrender {
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
}
.bj-overlay--surrender .bj-overlay__title {
  font-size: clamp(16px, 2.5vw, 28px);
  color: rgba(212,168,67,0.6);
  text-shadow: none;
}
.bj-overlay--surrender .bj-overlay__payout {
  font-size: clamp(12px, 1.8vw, 18px);
  color: #D4A843; text-shadow: none;
}
```

---

# 20. BOTAO NOVA MAO + HISTORY ROW (Adendo DS P2 1.16 + Custom)

```css
/* Nova Mao (pos-resultado) */
.bj-btn-new-hand {
  padding: clamp(8px, 1vw, 14px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px; color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer; min-height: 44px;
  box-shadow: 0 2px 8px rgba(0,200,83,0.2);
  transition: all 0.2s ease;
}
.bj-btn-new-hand:hover {
  box-shadow: 0 4px 16px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}

/* History row */
.bj-history-row {
  display: flex; justify-content: space-between; align-items: center;
  padding: clamp(8px, 1vw, 14px) clamp(12px, 1.5vw, 20px);
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(255,255,255,0.05);
  border-radius: 8px;
  transition: background 0.2s ease;
}
.bj-history-row:hover {
  background: rgba(255,255,255,0.04);
}

/* PF container info */
.bj-pf-box {
  width: 100%; max-width: 500px;
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 12px;
  padding: clamp(12px, 2vw, 20px);
  display: flex; flex-direction: column;
  gap: clamp(8px, 1vw, 14px);
}

/* PF input client seed */
.bj-pf-input {
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px);
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px; color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(9px, 1.1vw, 12px);
  outline: none;
  transition: border-color 0.2s ease;
}
.bj-pf-input:focus {
  border-color: rgba(212,168,67,0.5);
}

/* PF seed revelada */
.bj-pf-revealed {
  background: rgba(0,230,118,0.03);
  border: 1px solid rgba(0,230,118,0.2);
  border-radius: 12px;
  padding: clamp(12px, 2vw, 20px);
}

/* Botao Trocar Seed */
.bj-btn-rotate {
  padding: clamp(8px, 1vw, 12px) clamp(16px, 2vw, 28px);
  background: linear-gradient(180deg, #D4A843 0%, #8B6914 100%);
  border: 1.5px solid rgba(212,168,67,0.4);
  border-radius: 8px; color: #FFFFFF;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.2vw, 13px);
  text-transform: uppercase; letter-spacing: 1.5px;
  cursor: pointer; min-height: 44px;
  transition: all 0.2s ease;
}
.bj-btn-rotate:hover {
  box-shadow: 0 4px 16px rgba(212,168,67,0.3);
  transform: translateY(-1px);
}
.bj-btn-rotate[disabled] { opacity: 0.5; cursor: not-allowed; }
```

---

# VALIDACAO FINAL

| Metrica | Giveaways | Loteria | Pool | **Blackjack #4** |
|---------|:---------:|:-------:|:----:|:----------------:|
| Partes | 1 | 1 | 2 | **2** |
| Componentes numerados | 12 | 14 | 18 | **20** |
| @keyframes | 4 | 5 | 8 | **8** |
| Variantes chip/cor | 0 | 0 | 5 | **5 chips + 5 overlays** |
| Tokens :root | sim | sim | sim (expandido) | **sim (expandido)** |
| BEM naming | .giveaway-* | .lottery-* | .pool-* | **.bj-*** |
| Todos estados | normal+hover+disabled | normal+hover+disabled | 4 estados | **4 estados + variantes** |

**VALIDACAO X17:** 20 componentes exigidos pelo Adendo DS, 20 entregues. 5 botoes acao individuais (nao "mesmo padrao"). 5 variantes overlay individuais. 5 chips individuais. 6 estados hand total. 8 @keyframes. Cada componente com normal + hover + active + disabled.

---

**[FIM DO CSS-COMPONENTES BLACKJACK - COMPLETO (2 partes)]**

`[ENTREGA: 7.CSS-COMPONENTES-BLACKJACK-PARTE-2.md - 19/03/2026]`
