# CSS-COMPONENTES - BLACKJACK (#4)
## Parte 1 de 2 - Tokens :root - @keyframes - Componentes 1-10
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-BLACKJACK
### NAO editar - CSS pronto para copiar
### Blackout Casino GTARP - 19/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Blackjack no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.bj-mesa { border: 3px solid rgba(212,168,67,0.4); }` daqui.
Cada componente e numerado e referencia o Adendo DS correspondente.

---

# 0. TOKENS CSS (:root - primitivos e semanticos do Blackjack)

```css
:root {
  /* --- Primitivos - cores base --- */
  --bj-gold-primary:    #D4A843;
  --bj-gold-light:      #F6E27A;
  --bj-gold-dark:       #8B6914;
  --bj-gold-deeper:     #5D4600;
  --bj-green-neon:      #00E676;
  --bj-green-dark:      #004D25;
  --bj-green-medium:    #00C853;
  --bj-felt:            #1a472a;
  --bj-felt-dark:       #0f2d1a;
  --bj-felt-edge:       #091a0f;
  --bj-black-bg:        #0A0A0A;
  --bj-red-bust:        #FF1744;
  --bj-red-soft:        #FF3B3B;
  --bj-blue-double:     #448AFF;
  --bj-blue-dark:       #1A237E;
  --bj-purple-split:    #B388FF;
  --bj-purple-dark:     #4A148C;
  --bj-white:           #FFFFFF;
  --bj-text-secondary:  #A8A8A8;
  --bj-text-muted:      #666666;

  /* --- Semanticos - uso especifico --- */
  --bj-border-gold:         3px solid rgba(212,168,67,0.4);
  --bj-border-gold-subtle:  1.5px solid rgba(212,168,67,0.3);
  --bj-border-green:        1.5px solid rgba(0,230,118,0.3);
  --bj-border-green-active: 1px solid rgba(0,230,118,0.3);
  --bj-shadow-mesa:         0 0 30px rgba(212,168,67,0.08), 0 8px 32px rgba(0,0,0,0.4), inset 0 0 150px rgba(0,0,0,0.3), 0 0 60px rgba(0,0,0,0.2);
  --bj-shadow-card:         0 2px 8px rgba(0,0,0,0.3);
  --bj-shadow-card-back:    0 2px 8px rgba(0,0,0,0.4);
  --bj-shadow-chip:         inset 0 -2px 4px rgba(0,0,0,0.3), 0 2px 4px rgba(0,0,0,0.2);
  --bj-text-shadow-gold:    0 0 12px rgba(255,215,0,0.4);
  --bj-text-shadow-green:   0 0 10px rgba(0,230,118,0.5);
  --bj-text-shadow-victory: 0 0 20px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.3);

  /* --- Fontes --- */
  --bj-font-title:    'Cinzel', serif;
  --bj-font-body:     'Inter', sans-serif;
  --bj-font-mono:     'JetBrains Mono', monospace;

  /* --- Timing --- */
  --bj-ease-default:  cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --bj-spring-card:   300ms cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

---

# 0B. @KEYFRAMES (8 animacoes do Blackjack)

```css
/* KF-1: Dot pulse indicador mao ativa */
@keyframes bjDotPulse {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-2px); }
}

/* KF-2: Badge total bounce ao receber carta */
@keyframes bjTotalBounce {
  0% { transform: scale(1); }
  40% { transform: scale(1.15); }
  100% { transform: scale(1); }
}

/* KF-3: Glow infinito no total 21 (blackjack) */
@keyframes bjTotalGlow {
  0%, 100% { text-shadow: 0 0 8px rgba(255,215,0,0.4); }
  50% { text-shadow: 0 0 20px rgba(255,215,0,0.8), 0 0 40px rgba(255,215,0,0.4); }
}

/* KF-4: Side bet win glow pulsante */
@keyframes bjSideBetGlow {
  0%, 100% { box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50% { box-shadow: 0 0 20px rgba(0,230,118,0.6); }
}

/* KF-5: Shimmer sweep no titulo BLACKJACK */
@keyframes bjShimmerSweep {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* KF-6: Saldo flash ao ganhar (verde->branco alternando) */
@keyframes bjBalanceWin {
  0%, 100% { color: #00E676; text-shadow: 0 0 10px rgba(0,230,118,0.5); }
  25% { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.8); }
  50% { color: #00E676; text-shadow: 0 0 15px rgba(0,230,118,0.7); }
  75% { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.6); }
}

/* KF-7: Saldo flash ao perder (verde->vermelho 1x) */
@keyframes bjBalanceLose {
  0%, 100% { color: #00E676; }
  50% { color: rgba(255,59,59,0.8); }
}

/* KF-8: Timer insurance urgente (<3s) pulsante */
@keyframes bjInsuranceUrgent {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

---

# 1. FUNDO DO JOGO (Adendo DS P1 1.1)

```css
.bj-bg {
  background-color: #0A0A0A;
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
}
```

---

# 2. MESA DE JOGO (Adendo DS P1 1.2)

```css
.bj-mesa {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: clamp(280px, 55vw, 600px);
  min-height: clamp(280px, 50vh, 500px);
  background: radial-gradient(ellipse at 50% 50%, #1a472a 0%, #0f2d1a 70%, #091a0f 100%);
  border-radius: clamp(12px, 2vw, 20px);
  border: 3px solid rgba(212,168,67,0.4);
  box-shadow:
    0 0 30px rgba(212,168,67,0.08),
    0 8px 32px rgba(0,0,0,0.4),
    inset 0 0 150px rgba(0,0,0,0.3),
    0 0 60px rgba(0,0,0,0.2);
  display: flex; flex-direction: column;
  align-items: center; justify-content: space-between;
  padding: clamp(16px, 3vw, 32px) clamp(12px, 2vw, 24px);
  overflow: hidden; position: relative;
}

/* Textura felt */
.bj-mesa::before {
  content: ''; position: absolute; inset: 0;
  background-image: radial-gradient(circle, rgba(255,255,255,0.02) 1px, transparent 1px);
  background-size: 8px 8px;
  pointer-events: none; border-radius: inherit;
}

/* Divider central */
.bj-mesa__divider {
  width: 60%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(212,168,67,0.15), transparent);
}
```

---

# 3. CARTA FACE UP (Adendo DS P1 1.3)

```css
.bj-card {
  width: clamp(48px, 8vw, 72px);
  height: clamp(67px, 11.2vw, 101px);
  perspective: 1000px;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  position: relative; flex-shrink: 0;
}

.bj-card__front {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  background: #FFFFFF;
  border-radius: clamp(4px, 0.6vw, 8px);
  border: 1px solid rgba(0,0,0,0.15);
  box-shadow: 0 2px 8px rgba(0,0,0,0.3);
  display: flex; flex-direction: column;
  justify-content: space-between;
  padding: clamp(2px, 0.3vw, 4px);
  overflow: hidden;
}

/* Rank top-left */
.bj-card__rank {
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: clamp(9px, 1.3vw, 14px);
  line-height: 1;
}

/* Naipe central */
.bj-card__suit-center {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  font-size: clamp(16px, 2.5vw, 28px);
  opacity: 0.8;
}

/* Cores por naipe */
.bj-card--hearts .bj-card__rank,
.bj-card--hearts .bj-card__suit-center,
.bj-card--diamonds .bj-card__rank,
.bj-card--diamonds .bj-card__suit-center {
  color: #C62828;
}
.bj-card--spades .bj-card__rank,
.bj-card--spades .bj-card__suit-center,
.bj-card--clubs .bj-card__rank,
.bj-card--clubs .bj-card__suit-center {
  color: #1A1A1A;
}
```

---

# 4. CARTA FACE DOWN (Adendo DS P1 1.4)

```css
.bj-card__back {
  position: absolute; inset: 0;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  transform: rotateY(180deg);
  background: linear-gradient(135deg, #1A1A1A 0%, #2A2A2A 50%, #1A1A1A 100%);
  border-radius: clamp(4px, 0.6vw, 8px);
  border: 1.5px solid rgba(212,168,67,0.3);
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}

/* Padrao diamante interno */
.bj-card__back-pattern {
  width: 70%; height: 70%;
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 4px;
  display: flex; align-items: center; justify-content: center;
}

/* Logo BC */
.bj-card__back-logo {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(10px, 1.5vw, 16px);
  color: rgba(212,168,67,0.4);
  letter-spacing: 1px;
}
```

---

# 5. HAND CONTAINER (Adendo DS P1 1.6)

```css
.bj-hand {
  display: flex; flex-direction: column;
  align-items: center; gap: clamp(4px, 0.6vw, 8px);
  position: relative;
}

/* Cartas com overlap negativo */
.bj-hand__cards {
  display: flex;
  gap: clamp(-8px, -1vw, -12px);
  position: relative;
}

/* Label DEALER / PLAYER / MAO X */
.bj-hand__label {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 11px);
  color: rgba(255,255,255,0.15);
  text-transform: uppercase;
  letter-spacing: 2px;
}

/* Wrapper split - mao ativa */
.bj-hand--split-active {
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: 12px;
  padding: clamp(4px, 0.5vw, 8px);
}

/* Wrapper split - mao inativa */
.bj-hand--split-inactive {
  border: 1px solid rgba(255,255,255,0.05);
  border-radius: 12px;
  padding: clamp(4px, 0.5vw, 8px);
}

/* Wrapper split - mao completada */
.bj-hand--split-completed {
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 12px;
  padding: clamp(4px, 0.5vw, 8px);
  opacity: 0.6;
}
```

---

# 6. HAND TOTAL BADGE (Adendo DS P1 1.7)

```css
.bj-total {
  padding: clamp(2px, 0.3vw, 4px) clamp(8px, 1vw, 14px);
  background: rgba(0,0,0,0.7);
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.1);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.5vw, 16px);
  transition: all 0.3s ease;
}

/* Normal (< 17) */
.bj-total--normal {
  color: #A8A8A8;
  text-shadow: none;
}

/* Soft hand */
.bj-total--soft {
  color: #448AFF;
  text-shadow: 0 0 6px rgba(68,138,255,0.3);
}

/* Bom (17-20) */
.bj-total--good {
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}

/* Perfeito (21) */
.bj-total--perfect {
  color: #FFD700;
  text-shadow: 0 0 12px rgba(255,215,0,0.6);
  animation: bjTotalGlow 1.5s infinite;
}

/* Bust (> 21) */
.bj-total--bust {
  color: #FF1744;
  text-shadow: 0 0 8px rgba(255,23,68,0.5);
}

/* Dealer neutro */
.bj-total--dealer {
  color: #A8A8A8;
  text-shadow: none;
}
```

---

# 7. DOT PULSE - INDICADOR MAO ATIVA (Adendo DS P1 1.6)

```css
.bj-dot-pulse {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: #00E676;
  box-shadow: 0 0 6px rgba(0,230,118,0.6);
  animation: bjDotPulse 2s ease infinite;
}
```

---

# 8. CHIP - 5 VARIANTES (Adendo DS P1 1.8)

```css
.bj-chip {
  width: clamp(36px, 5.5vw, 52px);
  height: clamp(36px, 5.5vw, 52px);
  border-radius: 50%;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(9px, 1.2vw, 13px);
  color: #FFFFFF;
  cursor: pointer;
  min-height: 44px;
  display: flex; align-items: center; justify-content: center;
  box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 2px 4px rgba(0,0,0,0.2);
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.bj-chip:hover { transform: scale(1.1); }
.bj-chip:active { transform: scale(0.95); }

/* G$10 - Prata */
.bj-chip--10 {
  background: radial-gradient(circle at 35% 35%, #F5F5F5 0%, #BDBDBD 60%, #9E9E9E 100%);
  border: 2px solid #9E9E9E;
}
.bj-chip--10:hover { box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 0 12px rgba(158,158,158,0.5); }

/* G$25 - Verde */
.bj-chip--25 {
  background: radial-gradient(circle at 35% 35%, #66BB6A 0%, #388E3C 60%, #1B5E20 100%);
  border: 2px solid #2E7D32;
}
.bj-chip--25:hover { box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 0 12px rgba(46,125,50,0.5); }

/* G$50 - Azul */
.bj-chip--50 {
  background: radial-gradient(circle at 35% 35%, #448AFF 0%, #1565C0 60%, #0D47A1 100%);
  border: 2px solid #1565C0;
}
.bj-chip--50:hover { box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 0 12px rgba(21,101,192,0.5); }

/* G$100 - Preto */
.bj-chip--100 {
  background: radial-gradient(circle at 35% 35%, #1A1A1A 0%, #424242 60%, #212121 100%);
  border: 2px solid #424242;
}
.bj-chip--100:hover { box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 0 12px rgba(66,66,66,0.5); }

/* G$500 - Dourado */
.bj-chip--500 {
  background: radial-gradient(circle at 35% 35%, #D4A843 0%, #8B6914 60%, #5D4600 100%);
  border: 2px solid #8B6914;
}
.bj-chip--500:hover { box-shadow: inset 0 -2px 4px rgba(0,0,0,0.3), 0 0 12px rgba(139,105,20,0.5); }

/* Chip disabled */
.bj-chip--disabled {
  background: #1A1A1A;
  border: 2px solid rgba(255,255,255,0.1);
  opacity: 0.35;
  cursor: not-allowed;
  pointer-events: none;
}

/* Chip selecionada (ring branco) */
.bj-chip--selected {
  outline: 2px solid rgba(255,255,255,0.5);
  outline-offset: 2px;
  transform: scale(1.15);
}
```

---

# 9. BET AREA (Adendo DS P2 1.9)

```css
.bj-bet-area {
  align-self: center;
  min-width: clamp(80px, 15vw, 140px);
  min-height: clamp(60px, 10vh, 100px);
  background: rgba(255,255,255,0.03);
  border: 1.5px dashed rgba(212,168,67,0.2);
  border-radius: 8px;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(4px, 0.5vw, 8px);
  padding: clamp(8px, 1vw, 12px);
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative; z-index: 2;
}

/* Vazio */
.bj-bet-area--empty {
  border-color: rgba(212,168,67,0.15);
}
.bj-bet-area--empty::after {
  content: attr(data-placeholder);
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  font-size: clamp(9px, 1.1vw, 12px);
  color: rgba(255,255,255,0.15);
  text-transform: uppercase;
  letter-spacing: 1px;
}

/* Com chips */
.bj-bet-area--active {
  border-color: rgba(0,230,118,0.3);
  border-style: solid;
  background: rgba(0,230,118,0.02);
}

/* Valor da aposta */
.bj-bet-area__value {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}

/* Doubled */
.bj-bet-area--doubled {
  border-color: rgba(68,138,255,0.4);
  box-shadow: 0 0 12px rgba(68,138,255,0.15);
}
.bj-bet-area--doubled .bj-bet-area__value {
  color: #448AFF;
  text-shadow: 0 0 8px rgba(68,138,255,0.4);
}
```

---

# 10. SIDE BET AREA (Adendo DS P2 1.10)

```css
.bj-sidebet {
  position: absolute; top: 50%; transform: translateY(-50%);
  width: clamp(48px, 7vw, 64px);
  min-height: clamp(48px, 7vw, 64px);
  background: rgba(212,168,67,0.04);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: 8px;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 2px;
  cursor: pointer;
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  z-index: 5;
}

/* Posicao PP (esquerda) */
.bj-sidebet--pp { left: clamp(16px, 3vw, 32px); }

/* Posicao 21+3 (direita) */
.bj-sidebet--21plus3 { right: clamp(16px, 3vw, 32px); }

/* Label */
.bj-sidebet__label {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 11px);
  text-transform: uppercase;
  letter-spacing: 1px;
  color: rgba(212,168,67,0.5);
  transition: color 0.2s ease;
}

/* Hover */
.bj-sidebet:hover {
  border-color: rgba(212,168,67,0.4);
  background: rgba(212,168,67,0.08);
}
.bj-sidebet:hover .bj-sidebet__label {
  color: rgba(212,168,67,0.8);
}

/* Com aposta */
.bj-sidebet--has-bet {
  border-color: rgba(0,230,118,0.3);
  background: rgba(0,230,118,0.03);
}
.bj-sidebet--has-bet .bj-sidebet__label {
  color: #00E676;
}

/* Win */
.bj-sidebet--win {
  border-color: rgba(0,230,118,0.5);
  animation: bjSideBetGlow 1.5s ease infinite;
}
.bj-sidebet__payout {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.5vw, 16px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.5);
}

/* Lose */
.bj-sidebet--lose {
  border-color: rgba(255,255,255,0.05);
  opacity: 0.4;
}

/* Disabled */
.bj-sidebet--disabled {
  opacity: 0.2;
  pointer-events: none;
  cursor: default;
}
```

---

**[FIM DA PARTE 1 - Continua Parte 2: Componentes 11-20 (5 Botoes Acao, DEAL, VOLTAR, Saldo, Insurance Modal, 5 Overlays Resultado)]**

`[ENTREGA: 7.CSS-COMPONENTES-BLACKJACK-PARTE-1.md - 19/03/2026]`
