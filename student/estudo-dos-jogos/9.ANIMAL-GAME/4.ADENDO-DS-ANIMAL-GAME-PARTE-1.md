# ADENDO DESIGN SYSTEM — ANIMAL GAME / JOGO DO BICHO (#9)
## PARTE 1 DE 3 — Componentes CSS Real (1.1–1.7)
## Referencias diretas ao DS com CSS real - Complemento ao Prompt AI
### Blackout Casino GTARP - 18/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Adendo DS padrao (4 secoes: Componentes CSS, Atmosferas, Timelines, Checklist)
- **Gold Standard consultado:** Pool Game #22 (1.917L, 2 partes, 18 componentes), Giveaways #20 (1.054L, 2 partes, 12 componentes), Loteria #18 (685L, 1 parte, 14 componentes)
- **Benchmark de linhas:** 1.500-2.000 linhas (3 partes de ~500L)
- **Estrutura:**
  - Parte 1: Como Usar, Pesquisa, Componentes 1.1-1.7 (container, card animal, grid, capsula, chip, tab, botao jogar)
  - Parte 2: Componentes 1.8-1.14 (botao random, botao voltar, saldo, payout, historico, PF modal, overlays, tooltip)
  - Parte 3: Atmosferas (4 estados), Timelines (3 sequencias), Checklist (40+ itens)

---

# COMO USAR ESTE DOCUMENTO

Este adendo e colado JUNTO com o Prompt AI (Partes 1, 2 e 3) ao gerar o visual em qualquer IA. Enquanto o Prompt AI descreve O QUE cada tela tem e como se parece, este adendo garante que CADA componente referencie o Design System com CSS REAL copiavel.

**Regra:** Se o Prompt AI diz "borda dourada" e este adendo diz o CSS exato com section-ref, a IA DEVE usar o CSS daqui. **O adendo tem precedencia sobre descricoes vagas do Prompt AI.**

**Quantidade de componentes:** 14 componentes com CSS real copiavel, cada um referenciando secao exata do DS.

**Naming BEM adotado:** `.bicho-[block]__[element]--[modifier]`
- Block: `bicho-game`, `bicho-grid`, `bicho-card`, `bicho-capsule`, `bicho-bet`, `bicho-tab`, `bicho-chip`, `bicho-btn`, `bicho-overlay`, `bicho-history`, `bicho-pf`, `bicho-saldo`
- Modifiers: `--normal`, `--hover`, `--selected`, `--disabled`, `--match`, `--closed`, `--shaking`, `--open`, `--revealed`, `--win`, `--lose`, `--active`

**Pesquisa REGRA X0:**
- BEM 2026: getbem.com, sparkbox.com/foundry/bem_by_example — `.block__element--modifier` confirmado
- Shimmer GPU: dev.to/emrahg (Aug 2025) — usar `transform: translateX()` nao `background-position`
- Box-shadow FiveM: docs.fivem.net — CEF Chromium suporta multi-layer box-shadow e backdrop-filter
- Layered shadows: joshwcomeau.com (Aug 2025) — multiple layers criam profundidade realista

---

# SECAO 1 — COMPONENTES COM CSS REAL (14 Componentes)

## 1.1 Container Principal do Jogo (DS P1 S4.2)

Quando o Animal Game abre dentro do painel (z-index 60), o fundo NUNCA e flat:

```css
/* COPIAR EXATAMENTE — fundo de qualquer tela do Animal Game */
.bicho-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute;
  inset: 0;
  z-index: 60;
  border-radius: inherit;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  padding: clamp(6px, 1vw, 12px);
}

/* COPIAR — fallback NUI (sem bg-casino.png carregado ainda) */
.bicho-game--loading {
  background-color: #0A0A0A;
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.6) 0%, transparent 70%);
}

/* COPIAR — header bar (titulo + saldo) */
.bicho-game__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(4px, 0.6vw, 8px) clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(4px);
  border-bottom: 1px solid rgba(212,168,67,0.15);
  border-radius: 8px 8px 0 0;
  flex-shrink: 0;
}

/* COPIAR — area de conteudo (grid + painel lado a lado) */
.bicho-game__content {
  display: flex;
  gap: clamp(8px, 1.5vw, 16px);
  flex: 1;
  overflow: hidden;
  align-items: flex-start;
  padding: clamp(4px, 0.8vw, 12px);
}

/* COPIAR — mobile: stack vertical */
@media (max-height: 500px) {
  .bicho-game__content {
    flex-direction: row;
    /* Em landscape NUI 1280x720: sempre row */
  }
}
```

---

## 1.2 Card de Animal — 4 Estados (DS P1 S4.4 + S5.1 adaptado)

Cada um dos 25 cards no grid. O card e o componente mais interativo — TODOS os 4 estados sao obrigatorios:

```css
/* COPIAR EXATAMENTE — card base (estado normal) */
.bicho-card {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: clamp(2px, 0.3vw, 6px);
  min-height: clamp(44px, 8vw, 80px);
  background: rgba(5,5,5,0.95);
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 8px;
  cursor: pointer;
  overflow: hidden;
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
}

/* COPIAR — imagem do animal dentro do card */
.bicho-card__img {
  width: clamp(24px, 4vw, 48px);
  height: auto;
  object-fit: contain;
  transition: filter 0.25s ease;
  pointer-events: none;
  user-select: none;
}

/* COPIAR — nome do animal */
.bicho-card__name {
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: clamp(7px, 0.9vw, 11px);
  color: #D4A843;
  text-align: center;
  margin-top: 2px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
}

/* COPIAR — numero do grupo (canto superior direito) */
.bicho-card__grupo {
  position: absolute;
  top: 2px;
  right: 4px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(7px, 0.8vw, 10px);
  color: rgba(212,168,67,0.4);
  line-height: 1;
}

/* COPIAR — estado HOVER */
.bicho-card:hover {
  border-color: rgba(212,168,67,0.5);
  transform: scale(1.05);
  box-shadow:
    0 4px 16px rgba(0,0,0,0.5),
    0 0 12px rgba(212,168,67,0.15);
}
.bicho-card:hover .bicho-card__img {
  filter: brightness(1.1);
}

/* COPIAR — shimmer no hover (GPU-friendly: usa transform nao background-position) */
.bicho-card::after {
  content: '';
  position: absolute;
  inset: 0;
  width: 200%;
  height: 200%;
  pointer-events: none;
  opacity: 0;
  background: linear-gradient(
    110deg,
    transparent 25%,
    rgba(255,255,255,0.06) 50%,
    transparent 75%
  );
  transform: translate(-100%, -100%) rotate(45deg);
  transition: opacity 0.3s ease;
  z-index: 1;
}
.bicho-card:hover::after {
  opacity: 1;
  animation: bicho-shimmer 0.8s ease-in-out;
}
@keyframes bicho-shimmer {
  from { transform: translate(-100%, -100%) rotate(45deg); }
  to   { transform: translate(200%, 200%) rotate(45deg); }
}

/* COPIAR — estado SELECIONADO */
.bicho-card--selected {
  border: 2px solid #00E676;
  box-shadow:
    0 0 15px rgba(0,230,118,0.4),
    0 0 30px rgba(0,230,118,0.15);
  animation: bicho-pulse-glow 1.5s infinite ease-in-out;
}
@keyframes bicho-pulse-glow {
  0%, 100% { box-shadow: 0 0 15px rgba(0,230,118,0.3), 0 0 30px rgba(0,230,118,0.1); }
  50%      { box-shadow: 0 0 20px rgba(0,230,118,0.5), 0 0 40px rgba(0,230,118,0.2); }
}

/* COPIAR — badge de ordem de selecao (aparece quando selecionado) */
.bicho-card__badge {
  position: absolute;
  top: -6px;
  right: -6px;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: #00E676;
  color: #000000;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 800;
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2px 6px rgba(0,230,118,0.4);
  z-index: 2;
  /* Framer Motion: initial={{ scale: 0 }} animate={{ scale: 1 }}
     transition={{ type: "spring", stiffness: 400, damping: 15 }} */
}

/* COPIAR — estado DISABLED (max selecao atingido) */
.bicho-card--disabled {
  opacity: 0.4;
  filter: grayscale(0.5);
  cursor: not-allowed;
  pointer-events: none;
}
.bicho-card--disabled:hover {
  transform: none;
  border-color: rgba(212,168,67,0.2);
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
}
.bicho-card--disabled::after {
  display: none;
}
```

---

## 1.3 Grid de Animais — Container 5x5 (DS P1 S4.2 adaptado)

```css
/* COPIAR EXATAMENTE — grid 5x5 de cards de animais */
.bicho-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: clamp(3px, 0.4vw, 6px);
  flex: 0 0 clamp(250px, 55%, 500px);
  align-content: start;
}

/* COPIAR — Framer Motion stagger nos filhos */
/*
  {animals.map((animal, i) => (
    <motion.div
      key={animal.grupo}
      initial={{ opacity: 0, y: 15, scale: 0.9 }}
      animate={{ opacity: 1, y: 0, scale: 1 }}
      transition={{
        delay: i * 0.03,
        duration: 0.3,
        ease: [0.0, 0.0, 0.2, 1]
      }}
    >
      <AnimalCard {...} />
    </motion.div>
  ))}
*/

/* COPIAR — mobile landscape: grid compacto */
@media (max-width: 900px) and (orientation: landscape) {
  .bicho-grid {
    gap: clamp(2px, 0.3vw, 4px);
    flex: 0 0 clamp(200px, 50%, 400px);
  }
}
```

---

## 1.4 Capsula de Reveal — 5 Estados (DS P1 S7 + Custom metalico)

A capsula e o componente mais animado do jogo. 5 estados visuais distintos:

```css
/* COPIAR EXATAMENTE — capsula fechada (estado idle) */
.bicho-capsule {
  position: relative;
  width: clamp(40px, 6vw, 70px);
  height: clamp(40px, 6vw, 70px);
  border-radius: 50%;
  background: radial-gradient(
    circle at 35% 30%,
    #FFD700 0%,
    #D4A843 40%,
    #8B6914 80%,
    #462523 100%
  );
  border: 2px solid rgba(255,215,0,0.6);
  box-shadow:
    inset 0 -3px 8px rgba(0,0,0,0.4),
    inset 0 3px 6px rgba(255,215,0,0.2),
    0 0 20px rgba(212,168,67,0.3),
    0 4px 12px rgba(0,0,0,0.6);
  overflow: hidden;
  flex-shrink: 0;
}

/* COPIAR — reflexo metalico (::before) */
.bicho-capsule::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: radial-gradient(
    ellipse 60% 40% at 35% 25%,
    rgba(255,255,255,0.25),
    transparent
  );
  pointer-events: none;
  z-index: 1;
}

/* COPIAR — tampa da capsula (lid) */
.bicho-capsule__lid {
  position: absolute;
  top: 0;
  left: 10%;
  right: 10%;
  height: 45%;
  border-radius: 50% 50% 0 0;
  background: linear-gradient(
    180deg,
    #FFD700 0%,
    #D4A843 60%,
    #8B6914 100%
  );
  border-bottom: 1px solid rgba(139,105,20,0.5);
  transform-origin: bottom center;
  z-index: 2;
}

/* COPIAR — estado TREMENDO (shake antes de abrir) */
.bicho-capsule--shaking {
  /* Framer Motion: animate={{ x: [-3, 3, -3, 0] }}
     transition={{ duration: 0.4 }} */
}

/* COPIAR — estado ABERTO (lid voou, interior visivel) */
.bicho-capsule__lid--open {
  /* Framer Motion: animate={{ rotateX: -120, y: -30, opacity: 0 }}
     transition={{ duration: 0.5, ease: "easeIn" }} */
}

/* COPIAR — interior da capsula (animal + nome) */
.bicho-capsule__interior {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  opacity: 0;
}

/* COPIAR — imagem do animal revelado */
.bicho-capsule__animal-img {
  width: clamp(24px, 3.5vw, 44px);
  height: auto;
  object-fit: contain;
  /* Framer Motion: animate={{ scale: [0, 1.2, 1], opacity: [0, 1] }}
     transition={{ duration: 0.4, ease: "easeOut" }} */
}

/* COPIAR — nome do animal revelado */
.bicho-capsule__animal-name {
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: clamp(7px, 0.8vw, 10px);
  color: #D4A843;
  text-align: center;
  margin-top: 2px;
  /* Framer Motion: animate={{ opacity: [0, 1], y: [10, 0] }}
     transition={{ duration: 0.3 }} */
}

/* COPIAR — milhar abaixo do nome */
.bicho-capsule__milhar {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(6px, 0.7vw, 9px);
  color: rgba(212,168,67,0.4);
  margin-top: 1px;
}

/* COPIAR — estado MATCH (animal acertado pelo jogador) */
.bicho-capsule--match {
  border: 2px solid #00E676;
  box-shadow:
    0 0 25px rgba(0,230,118,0.6),
    0 0 50px rgba(0,230,118,0.2);
  animation: bicho-match-flash 0.6s ease 2;
}
@keyframes bicho-match-flash {
  0%   { box-shadow: 0 0 25px rgba(0,230,118,0.6), 0 0 50px rgba(0,230,118,0.2); }
  50%  { box-shadow: 0 0 40px rgba(0,230,118,0.8), 0 0 70px rgba(0,230,118,0.3); }
  100% { box-shadow: 0 0 25px rgba(0,230,118,0.6), 0 0 50px rgba(0,230,118,0.2); }
}

/* COPIAR — label de posicao acima da capsula ("1o", "2o", etc) */
.bicho-capsule__label {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(8px, 0.9vw, 11px);
  color: rgba(212,168,67,0.3);
  text-align: center;
  margin-bottom: 4px;
}

/* COPIAR — container das 5 capsulas */
.bicho-capsule-row {
  display: flex;
  gap: clamp(10px, 2vw, 30px);
  align-items: center;
  justify-content: center;
}
```

---

## 1.5 Chip de Aposta — 3 Estados (DS P2 S8.3 adaptado)

```css
/* COPIAR EXATAMENTE — chip base (estado normal) */
.bicho-chip {
  display: flex;
  align-items: center;
  justify-content: center;
  width: clamp(36px, 5vw, 52px);
  height: clamp(36px, 5vw, 52px);
  border-radius: 50%;
  background: rgba(10,10,10,0.9);
  border: 1.5px solid rgba(212,168,67,0.2);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(9px, 1.1vw, 13px);
  color: #00E676;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  user-select: none;
  /* Framer Motion: whileHover={{ scale: 1.08 }} whileTap={{ scale: 0.95 }} */
}

/* COPIAR — estado HOVER */
.bicho-chip:hover {
  border-color: rgba(212,168,67,0.4);
  background: rgba(15,15,15,0.95);
  box-shadow: 0 0 8px rgba(212,168,67,0.1);
}

/* COPIAR — estado ATIVO (chip selecionado) */
.bicho-chip--active {
  border-color: #FFD700;
  color: #FFD700;
  transform: scale(1.1);
  box-shadow: 0 0 10px rgba(255,215,0,0.3);
  background: rgba(20,18,5,0.95);
}

/* COPIAR — estado DISABLED */
.bicho-chip--disabled {
  opacity: 0.3;
  cursor: not-allowed;
  pointer-events: none;
}

/* COPIAR — container dos chips */
.bicho-chip-row {
  display: flex;
  gap: clamp(4px, 0.5vw, 8px);
  justify-content: center;
  align-items: center;
  flex-wrap: wrap;
}

/* COPIAR — input custom (campo numerico) */
.bicho-chip__input {
  width: clamp(50px, 8vw, 80px);
  height: clamp(28px, 3.5vw, 36px);
  background: rgba(0,0,0,0.4);
  border: 1px solid rgba(212,168,67,0.2);
  border-radius: 6px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1vw, 13px);
  color: #00E676;
  text-align: center;
  outline: none;
  transition: all 0.2s ease;
}
.bicho-chip__input:focus {
  border-color: rgba(212,168,67,0.5);
  box-shadow: 0 0 8px rgba(212,168,67,0.15);
}
.bicho-chip__input::placeholder {
  color: #666666;
}
```

---

## 1.6 Tab de Modo — 3 Estados (DS P2 S8 adaptado)

```css
/* COPIAR EXATAMENTE — tab base (estado inativo) */
.bicho-tab {
  padding: clamp(4px, 0.6vw, 8px) clamp(8px, 1.5vw, 16px);
  border-radius: 6px;
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(255,255,255,0.05);
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(8px, 1vw, 12px);
  color: rgba(212,168,67,0.4);
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.25s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  user-select: none;
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* COPIAR — estado HOVER (inativo) */
.bicho-tab:hover {
  background: rgba(212,168,67,0.05);
  color: rgba(212,168,67,0.6);
  border-color: rgba(212,168,67,0.1);
}

/* COPIAR — estado ATIVO */
.bicho-tab--active {
  background: rgba(212,168,67,0.1);
  color: #D4A843;
  border: 1px solid rgba(212,168,67,0.3);
  border-bottom: 2px solid #D4A843;
  /* Framer Motion layoutId="bicho-mode-underline" no indicador */
}

/* COPIAR — container das tabs */
.bicho-tab-row {
  display: flex;
  flex-wrap: wrap;
  gap: clamp(3px, 0.4vw, 6px);
  justify-content: center;
}
```

---

## 1.7 Botao JOGAR — 2 Estados (DS P2 S8.1 + Asset PNG)

```css
/* COPIAR EXATAMENTE — botao JOGAR base */
.bicho-btn-jogar {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: clamp(120px, 18vw, 200px);
  min-height: clamp(36px, 5vw, 48px);
  background-image: url('/assets/ui/BUTTON-JOGAR-AGORA-ATIVO-DESATIVO/BUTTON-JOGAR-AGORA-ATIVO-SEM-TEXTO.png');
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
  border: none;
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(10px, 1.3vw, 16px);
  color: #FFFFFF;
  text-transform: uppercase;
  letter-spacing: 2px;
  text-shadow: 0 1px 3px rgba(0,0,0,0.8);
  cursor: pointer;
  transition: all 0.2s ease;
  /* Framer Motion: whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.97 }} */
}

/* COPIAR — estado HOVER */
.bicho-btn-jogar:hover {
  filter: brightness(1.1);
  transform: scale(1.05);
  box-shadow: 0 0 20px rgba(212,168,67,0.15);
}

/* COPIAR — estado DISABLED (nada selecionado ou saldo insuficiente) */
.bicho-btn-jogar--disabled {
  opacity: 0.4;
  filter: grayscale(0.5);
  cursor: not-allowed;
  pointer-events: none;
}

/* COPIAR — texto dentro do botao */
.bicho-btn-jogar__text {
  position: relative;
  z-index: 1;
  /* Formato: "{JOGAR|PLAY} G${betAmount}" */
}
```

---

**[FIM DA PARTE 1 — Continua na Parte 2 com Componentes 1.8-1.14]**

`[ENTREGA: 4.ADENDO-DS-ANIMAL-GAME-PARTE-1.md — 18/03/2026]`
