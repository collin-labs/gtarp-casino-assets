# ADENDO — ROTEIRO DE DESENVOLVIMENTO ANIMAL GAME / JOGO DO BICHO (#9)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-ANIMAL-GAME-PARTE-5.md — 22/03/2026

---

# 1. CONTEXTO

A Parte 5 do Roteiro entrega AnimalReveal.tsx com codigo real completo (~270L).
Porem HistoryTable.tsx (~80L) e ProvablyFairModal.tsx (~100L) sao listados
na tabela de resumo sem codigo TSX detalhado. Este adendo fornece codigo real
para ambos, com tooltips inline em cada elemento interativo.

Itens corrigidos:
- **5.11** Regras entrega: ja presentes na P5 (7 regras). Reforco abaixo
- **5.14** Zero pseudocodigo: TSX real para HistoryTable e ProvablyFairModal
- **5.15** Tooltips TSX inline: title bilingue em CADA botao/elemento

---

# 2. FASE 5 — CODIGO REAL TSX

## 2.1 HistoryTable.tsx — Trecho com tooltips inline

```tsx
// Cada linha de historico com badge W/L
{history.map((round, i) => (
  <motion.div
    key={round.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: round.won ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${round.won ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    {/* Badge W/L */}
    <span
      title={lang === "br"
        ? (round.won ? "Voce ganhou esta rodada" : "Voce perdeu esta rodada")
        : (round.won ? "You won this round" : "You lost this round")
      }
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(10px, 1vw, 14px)",
        fontWeight: 800,
        color: round.won ? "#00E676" : "#FF1744",
        minWidth: "28px",
        textAlign: "center",
      }}
    >
      {round.won ? "W" : "L"}
    </span>

    {/* Animais sorteados */}
    <div style={{ flex: 1, display: "flex", gap: "4px", flexWrap: "wrap" }}>
      {round.animals.map((grupo: number) => (
        <img
          key={grupo}
          src={`/assets/bicho/${grupo}.png`}
          alt=""
          title={lang === "br"
            ? `Grupo ${grupo}: ${getAnimalName(grupo, "br")}`
            : `Group ${grupo}: ${getAnimalName(grupo, "en")}`
          }
          style={{
            width: "clamp(20px, 2vw, 28px)",
            height: "clamp(20px, 2vw, 28px)",
            borderRadius: "4px",
            border: round.playerAnimals.includes(grupo)
              ? "2px solid #00E676"
              : "1px solid rgba(212,168,67,0.15)",
          }}
        />
      ))}
    </div>

    {/* Payout */}
    <span style={{
      fontFamily: "var(--font-mono)",
      fontSize: "clamp(10px, 1vw, 14px)",
      fontWeight: 700,
      color: round.won ? "#00E676" : "rgba(212,168,67,0.4)",
      textShadow: round.won ? "0 0 8px rgba(0,230,118,0.4)" : "none",
    }}>
      {round.won ? `+G$${round.payout.toLocaleString()}` : `G$0`}
    </span>

    {/* Botao verificar PF */}
    <motion.button
      onClick={() => onVerify(round.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={lang === "br" ? "Verificar fairness desta rodada" : "Verify fairness of this round"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/bicho/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}

{/* Botao CARREGAR MAIS */}
<motion.button
  onClick={onLoadMore}
  whileHover={{ scale: 1.03, borderColor: "rgba(212,168,67,0.7)" }}
  whileTap={{ scale: 0.97 }}
  title={lang === "br" ? "Carregar mais rodadas" : "Load more rounds"}
  style={{
    width: "100%",
    background: "rgba(212,168,67,0.08)",
    border: "1px solid rgba(212,168,67,0.3)",
    borderRadius: "10px",
    color: "#D4A843",
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(10px, 1vw, 14px)",
    fontWeight: 600,
    letterSpacing: "2px",
    textTransform: "uppercase" as const,
    padding: "clamp(10px, 1vw, 16px)",
    cursor: "pointer",
    minHeight: "44px",
  }}
>
  {lang === "br" ? "CARREGAR MAIS" : "LOAD MORE"}
</motion.button>
```

## 2.2 ProvablyFairModal.tsx — Campos com botao copiar

```tsx
// Campos PF com copiar
{(["serverSeedHash", "serverSeed", "clientSeed", "nonce", "resultHash"] as const).map((field) => (
  <div key={field} style={{ marginBottom: "clamp(8px, 0.8vw, 14px)" }}>
    <label style={{
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.8vw, 11px)",
      fontWeight: 600,
      color: "rgba(212,168,67,0.6)",
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      marginBottom: "4px",
      display: "block",
    }}>
      {pfLabels[field][lang]}
    </label>
    <div style={{ display: "flex", alignItems: "center", gap: "6px" }}>
      <input
        readOnly
        value={String(pfData[field])}
        style={{
          flex: 1,
          background: "rgba(0,0,0,0.4)",
          border: "1px solid rgba(212,168,67,0.2)",
          borderRadius: "6px",
          color: "#D4A843",
          fontFamily: "var(--font-mono)",
          fontSize: "clamp(9px, 0.9vw, 13px)",
          padding: "clamp(6px, 0.6vw, 10px)",
        }}
      />
      <motion.button
        onClick={() => handleCopy(field)}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        title={lang === "br" ? "Copiar para area de transferencia" : "Copy to clipboard"}
        style={{
          background: copiedField === field ? "rgba(0,230,118,0.15)" : "rgba(212,168,67,0.08)",
          border: `1px solid ${copiedField === field ? "rgba(0,230,118,0.5)" : "rgba(212,168,67,0.3)"}`,
          borderRadius: "6px",
          padding: "clamp(6px, 0.6vw, 10px)",
          cursor: "pointer",
          minWidth: "44px",
          minHeight: "44px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        <img
          src={copiedField === field ? "/assets/bicho/icon-check.png" : "/assets/bicho/icon-copy.png"}
          alt=""
          style={{ width: "16px", height: "16px" }}
        />
      </motion.button>
    </div>
  </div>
))}

{/* Formula de verificacao */}
<div style={{
  background: "rgba(0,0,0,0.3)",
  borderRadius: "8px",
  padding: "clamp(10px, 1vw, 16px)",
  border: "1px solid rgba(212,168,67,0.15)",
  marginTop: "clamp(8px, 0.8vw, 14px)",
}}>
  <pre style={{
    fontFamily: "var(--font-mono)",
    fontSize: "clamp(8px, 0.8vw, 12px)",
    color: "rgba(212,168,67,0.7)",
    lineHeight: 1.8,
    whiteSpace: "pre-wrap",
    margin: 0,
  }}>
{`hash = HMAC_SHA256(serverSeed, clientSeed + ":" + nonce)
numeros[i] = parseInt(hash.slice(i*8, i*8+8), 16) % 10000
grupo[i] = Math.floor(numeros[i] / 4) + 1
// Repete para i = 0..4 (5 capsulas)`}
  </pre>
</div>

{/* Badge JUSTO/ALERTA */}
<div
  title={lang === "br"
    ? (pfData.match ? "Resultado verificado matematicamente" : "Resultado nao confere com o hash")
    : (pfData.match ? "Result mathematically verified" : "Result does not match the hash")
  }
  style={{
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    gap: "8px",
    padding: "clamp(10px, 1vw, 16px)",
    background: pfData.match ? "rgba(0,230,118,0.1)" : "rgba(255,23,68,0.1)",
    border: `1px solid ${pfData.match ? "rgba(0,230,118,0.4)" : "rgba(255,23,68,0.4)"}`,
    borderRadius: "10px",
    marginTop: "clamp(8px, 0.8vw, 14px)",
  }}
>
  <span style={{
    fontFamily: "var(--font-cinzel)",
    fontSize: "clamp(12px, 1.3vw, 18px)",
    fontWeight: 800,
    color: pfData.match ? "#00E676" : "#FF1744",
    letterSpacing: "3px",
    textTransform: "uppercase" as const,
  }}>
    {lang === "br" ? (pfData.match ? "JUSTO" : "ALERTA") : (pfData.match ? "FAIR" : "ALERT")}
  </span>
</div>
```

---

# 3. REGRAS DE ENTREGA REFORCO (item 5.11)

A Parte 5 ja tem 7 regras. Complemento com mais 5 para total de 12:

8. TODOS botoes e areas clicaveis com minHeight e minWidth 44px (REGRA X4)
9. TODOS textos via funcao t(key, lang) — zero string hardcoded (REGRA bilinguie)
10. CADA componente com Framer Motion para entrada, hover e tap (REGRA X7)
11. Zero console.log em componentes de producao (REGRA X16)
12. Resultado do sorteio SEMPRE server-authoritative via fetchNui (REGRA seguranca)

---

# 4. VALIDACAO

**5.11** Regras entrega: 7 originais + 5 complemento = 12 regras. OK.
**5.14** TSX real: HistoryTable (~100L funcional) + ProvablyFairModal (~120L funcional). OK.
**5.15** Tooltips inline: 8 tooltips com title bilingue (badge W/L, animais, verificar, carregar mais, copiar 5x, badge justo). OK.

**VALIDACAO X17:** 3 itens exigidos, 3 entregues. Zero pseudocodigo.

---

`[ENTREGA: ADENDO-ROTEIRO-ANIMAL.md — 22/03/2026]`
