# CSS COMPONENTES — CRASH (#1)
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com os PROMPT-V0 do Crash
### Blackout Casino GTARP - 18/03/2026

---

# COMO USAR

Este arquivo tem **precedencia total** sobre descricoes vagas do Prompt-V0.
Se o Prompt-V0 diz "borda dourada", este arquivo diz o CSS exato.
Colar inteiro no inicio da sessao V0 ANTES do prompt de cada tela.
Componentes numerados, BEM naming `.crash-[block]__[element]--[modifier]`.

---

# 0. TOKENS CSS (:root)

```css
:root {
  /* --- Primitivos --- */
  --crash-gold:          #D4A843;
  --crash-gold-light:    #F6E27A;
  --crash-gold-dark:     #8B6914;
  --crash-green:         #00E676;
  --crash-green-dark:    #004D25;
  --crash-green-med:     #00C853;
  --crash-red:           #FF4444;
  --crash-red-light:     #FF6B6B;
  --crash-yellow:        #FFD700;
  --crash-bg:            #0A0A0A;
  --crash-white:         #FFFFFF;
  --crash-text-sec:      #A8A8A8;
  --crash-text-muted:    #666666;

  /* --- Semanticos --- */
  --crash-border-gold:       1.5px solid rgba(212,168,67,0.2);
  --crash-border-green:      1.5px solid rgba(0,230,118,0.3);
  --crash-border-red:        1.5px solid rgba(255,68,68,0.25);
  --crash-shadow-gold:       0 0 12px rgba(212,168,67,0.05);
  --crash-shadow-green:      0 0 20px rgba(0,230,118,0.1);
  --crash-shadow-red:        0 0 20px rgba(255,68,68,0.15);
  --crash-shadow-deep:       0 4px 16px rgba(0,0,0,0.4);
  --crash-shadow-inset:      inset 0 0 40px rgba(0,0,0,0.3);
  --crash-text-shadow-gold:  0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
  --crash-text-shadow-green: 0 0 20px rgba(0,230,118,0.6), 0 4px 8px rgba(0,0,0,1);
  --crash-text-shadow-red:   0 0 20px rgba(255,68,68,0.5), 0 4px 8px rgba(0,0,0,1);

  /* --- Fontes --- */
  --crash-font-title: 'Cinzel', serif;
  --crash-font-body:  'Inter', sans-serif;
  --crash-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --crash-touch-min: 44px;
  --crash-radius-card: 12px;
  --crash-radius-btn:  10px;
  --crash-radius-sm:   6px;
}

@property --crash-countdown-progress {
  syntax: '<percentage>';
  inherits: false;
  initial-value: 0%;
}
```

---

# 1. @KEYFRAMES (8 animacoes)

```css
/* 1. Pulse do "APOSTE AGORA" durante BETTING */
@keyframes crashBettingPulse {
  0%, 100% { opacity: 0.8; transform: translate(-50%,-50%) scale(1); }
  50%      { opacity: 1;   transform: translate(-50%,-50%) scale(1.06); }
}

/* 2. Pulse do multiplicador durante RISING */
@keyframes crashRisingPulse {
  0%, 100% { transform: translate(-50%,-50%) scale(1); }
  50%      { transform: translate(-50%,-50%) scale(1.05); }
}

/* 3. Flash scale do multiplicador no CRASH */
@keyframes crashMultFlash {
  0%   { transform: translate(-50%,-50%) scale(1); }
  40%  { transform: translate(-50%,-50%) scale(1.35); }
  100% { transform: translate(-50%,-50%) scale(1.1); }
}

/* 4. Pulse do botao CASHOUT */
@keyframes crashCashoutPulse {
  0%, 100% {
    box-shadow: 0 0 10px rgba(255,68,68,0.3), 0 4px 16px rgba(255,68,68,0.2);
  }
  50% {
    box-shadow: 0 0 25px rgba(255,68,68,0.6), 0 0 40px rgba(255,68,68,0.15), 0 6px 20px rgba(255,68,68,0.3);
  }
}

/* 5. Badge novo slide-in */
@keyframes crashBadgeIn {
  0%   { opacity: 0; transform: scale(0.5) translateX(-20px); }
  100% { opacity: 1; transform: scale(1) translateX(0); }
}

/* 6. Win flash no saldo */
@keyframes crashBalanceWin {
  0%   { color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
  25%  { color: #FFFFFF; text-shadow: 0 0 15px rgba(255,255,255,0.6); }
  50%  { color: #00E676; text-shadow: 0 0 12px rgba(0,230,118,0.6); }
  75%  { color: #FFFFFF; text-shadow: 0 0 15px rgba(255,255,255,0.6); }
  100% { color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
}

/* 7. Lose flash no saldo */
@keyframes crashBalanceLose {
  0%   { color: #00E676; }
  50%  { color: #FF4444; text-shadow: 0 0 8px rgba(255,68,68,0.3); }
  100% { color: #00E676; text-shadow: 0 0 8px rgba(0,230,118,0.4); }
}

/* 8. Rolling counter flash final (branco -> verde) */
@keyframes crashCounterFlash {
  0%   { color: #FFFFFF; text-shadow: 0 0 20px rgba(255,255,255,0.8); }
  100% { color: #00E676; text-shadow: 0 0 15px rgba(0,230,118,0.6); }
}
```

---

# 2. FUNDO DO JOGO (DS P1 §4.2)

```css
.crash-game {
  background-color: #0A0A0A;
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
  padding: clamp(6px, 1vw, 12px);
  gap: clamp(4px, 0.6vw, 8px);
}
.crash-game--loading {
  background-image:
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.05) 0%, transparent 70%);
}
```

---

# 3. CANVAS WRAPPER — 4 estados (DS P1 §4.2 + §5.1)

```css
.crash-canvas {
  flex: 0 0 58%;
  position: relative;
  border-radius: clamp(8px, 1vw, 12px);
  overflow: hidden;
  border: 1.5px solid rgba(212,168,67,0.15);
  background: rgba(0,0,0,0.3);
  box-shadow: 0 0 12px rgba(212,168,67,0.05), 0 4px 16px rgba(0,0,0,0.4), inset 0 0 40px rgba(0,0,0,0.3);
  transition: box-shadow 0.5s ease, border-color 0.5s ease;
}
.crash-canvas--waiting {
  border-color: rgba(212,168,67,0.15);
}
.crash-canvas--rising {
  border-color: rgba(0,230,118,0.25);
  box-shadow: 0 0 20px rgba(0,230,118,0.1), 0 4px 16px rgba(0,0,0,0.4), inset 0 0 40px rgba(0,0,0,0.3);
}
.crash-canvas--crashed {
  border-color: rgba(255,68,68,0.25);
  box-shadow: 0 0 20px rgba(255,68,68,0.15), 0 4px 16px rgba(0,0,0,0.4), inset 0 0 40px rgba(0,0,0,0.3);
}
.crash-canvas canvas {
  position: absolute; inset: 0;
  width: 100%; height: 100%; display: block;
  border-radius: clamp(6px, 0.8vw, 10px);
}
@media (max-width: 768px) and (orientation: portrait) {
  .crash-canvas { flex: none; width: 100%; aspect-ratio: 2/1; }
}
```

---

# 4. MULTIPLICADOR CENTRAL — 4 estados (DS P1 §3)

```css
.crash-mult {
  position: absolute; top: 30%; left: 50%;
  transform: translate(-50%,-50%);
  pointer-events: none; z-index: 2; text-align: center;
}
.crash-mult--waiting {
  font-family: var(--crash-font-title); font-weight: 700;
  font-size: clamp(14px, 2vw, 24px); text-transform: uppercase;
  letter-spacing: 2px; color: #D4A843;
  text-shadow: var(--crash-text-shadow-gold);
}
.crash-mult--betting {
  font-family: var(--crash-font-title); font-weight: 800;
  font-size: clamp(16px, 2.2vw, 28px); text-transform: uppercase;
  letter-spacing: 2px; color: #FFD700;
  text-shadow: 0 0 20px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
  animation: crashBettingPulse 2s ease-in-out infinite;
}
.crash-mult--rising {
  font-family: var(--crash-font-mono); font-weight: 800;
  font-size: clamp(28px, 5vw, 72px); letter-spacing: 2px;
  color: #00E676;
  text-shadow: var(--crash-text-shadow-green);
  animation: crashRisingPulse 0.5s ease-in-out infinite;
}
.crash-mult--crashed {
  font-family: var(--crash-font-mono); font-weight: 800;
  font-size: clamp(28px, 5vw, 72px); letter-spacing: 2px;
  color: #FF4444;
  text-shadow: var(--crash-text-shadow-red);
  animation: crashMultFlash 0.4s ease-out forwards;
}
.crash-mult--instant { color: #FFD700; }
```

---

# 5. BADGES HISTORICO — 5 faixas + novo (DS P2 §10.1)

```css
.crash-badge-bar {
  display: flex; gap: clamp(3px, 0.4vw, 6px);
  overflow-x: auto; overflow-y: hidden;
  padding: clamp(3px, 0.4vw, 6px) 0;
  scrollbar-width: none;
}
.crash-badge-bar::-webkit-scrollbar { display: none; }

.crash-badge {
  padding: clamp(2px, 0.3vw, 4px) clamp(5px, 0.7vw, 8px);
  border-radius: clamp(3px, 0.4vw, 5px);
  font-family: var(--crash-font-mono); font-weight: 700;
  font-size: clamp(8px, 0.9vw, 12px);
  white-space: nowrap; flex-shrink: 0;
  transition: all 0.2s ease; cursor: default;
}
.crash-badge:hover { transform: scale(1.08); filter: brightness(1.1); }

.crash-badge--red {
  background: rgba(255,68,68,0.12); color: #FF6B6B;
  border: 1px solid rgba(255,68,68,0.25);
}
.crash-badge--gold-low {
  background: rgba(212,168,67,0.08); color: rgba(212,168,67,0.6);
  border: 1px solid rgba(212,168,67,0.15);
}
.crash-badge--gold {
  background: rgba(212,168,67,0.15); color: #D4A843;
  border: 1px solid rgba(212,168,67,0.3);
}
.crash-badge--green {
  background: rgba(0,230,118,0.12); color: #00E676;
  border: 1px solid rgba(0,230,118,0.25);
}
.crash-badge--instant {
  background: rgba(255,215,0,0.15); color: #FFD700;
  border: 1px solid rgba(255,215,0,0.3);
}
.crash-badge--new {
  animation: crashBadgeIn 0.35s cubic-bezier(0.34,1.56,0.64,1) forwards;
}
```

---

# 6. PAINEL DE CONTROLES (DS P1 §4.2)

```css
.crash-ctrl {
  flex: 0 0 38%;
  display: flex; flex-direction: column;
  gap: clamp(6px, 0.8vw, 10px);
  background: rgba(5,5,5,0.85);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: clamp(8px, 1vw, 12px);
  padding: clamp(8px, 1.2vw, 14px);
  overflow-y: auto;
  box-shadow: 0 4px 16px rgba(0,0,0,0.4), inset 0 1px 0 rgba(212,168,67,0.05);
}
@media (max-width: 768px) and (orientation: portrait) {
  .crash-ctrl { flex: none; width: 100%; }
}
```

---

# 7. INPUT DE APOSTA — 4 estados (DS P2 §9.1)

```css
.crash-ctrl__input {
  width: 100%;
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.25);
  border-radius: 8px;
  color: #00E676;
  font-family: var(--crash-font-mono); font-weight: 700;
  font-size: clamp(13px, 1.5vw, 20px);
  padding: clamp(8px, 1vw, 12px);
  outline: none; min-height: var(--crash-touch-min);
  transition: all 0.25s ease;
}
.crash-ctrl__input:focus {
  border-color: rgba(0,230,118,0.5);
  box-shadow: 0 0 8px rgba(0,230,118,0.15);
}
.crash-ctrl__input--error {
  border-color: rgba(255,68,68,0.5); color: #FF4444;
  box-shadow: 0 0 8px rgba(255,68,68,0.1);
}
.crash-ctrl__input--disabled {
  opacity: 0.5; pointer-events: none; cursor: not-allowed;
}
.crash-ctrl__input::placeholder { color: #666666; font-weight: 500; }

.crash-ctrl__label {
  font-family: var(--crash-font-body); font-weight: 600;
  font-size: clamp(9px, 1vw, 13px); text-transform: uppercase;
  letter-spacing: 1px; color: #A8A8A8; margin-bottom: 4px;
}
```

---

# 8. ATALHOS MIN/x2/div2/MAX (DS P2 §8.8)

```css
.crash-ctrl__shortcuts {
  display: flex; gap: clamp(3px, 0.4vw, 5px);
}
.crash-ctrl__shortcut {
  flex: 1; min-width: 44px; min-height: 44px;
  background: rgba(212,168,67,0.08);
  border: 1px solid rgba(212,168,67,0.15);
  border-radius: var(--crash-radius-sm);
  color: #D4A843;
  font-family: var(--crash-font-mono); font-weight: 600;
  font-size: clamp(9px, 1vw, 12px);
  cursor: pointer; transition: all 0.2s ease;
  display: flex; align-items: center; justify-content: center;
}
.crash-ctrl__shortcut:hover {
  background: rgba(212,168,67,0.15);
  border-color: rgba(212,168,67,0.3);
}
.crash-ctrl__shortcut:active { transform: scale(0.95); }
.crash-ctrl__shortcut--disabled {
  opacity: 0.3; cursor: not-allowed; pointer-events: none;
}
```

---

**[FIM DA PARTE 1 — Continua na Parte 2 com Componentes 9-14 + Checklist]**

`[ENTREGA: 7.CSS-COMPONENTES-CRASH-PARTE-1.md — 18/03/2026]`
