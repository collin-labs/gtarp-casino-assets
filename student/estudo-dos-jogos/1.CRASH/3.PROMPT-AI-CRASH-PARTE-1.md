# PROMPT AI — CRASH (#1)
## PARTE 1 DE 4 — Header Tecnico, Paleta, Tipografia, Tela Waiting/Betting
### Prompt para gerar visual pixel-perfect em V0 / Bolt / Claude / ChatGPT
### Blackout Casino GTARP - 17/03/2026

---

# HEADER TECNICO (Colar no inicio do prompt para qualquer IA)

```
RESTRICOES TECNICAS:
- React 19 + TypeScript + Framer Motion 11 + Next.js 16
- Container: div com position absolute, inset 0, z-index 60, borderRadius inherit, overflow hidden
- SOMENTE dark mode — fundo NUNCA branco, NUNCA cinza claro
- CSS inline via style={{}} — NAO usar classes Tailwind para componentes de jogo
- Canvas 2D para: curva ascendente, grid de fundo, particulas de crash — NAO usar WebGL/Three.js
- DOM para: multiplicador central, botoes, controles, overlays, feed, historico — NAO Canvas para UI
- Fontes: Cinzel Decorative (titulos, multiplicador, botoes CTA), Inter (body, labels), JetBrains Mono (valores GCoin, multiplicador numerico, timer)
- Responsividade: TUDO com clamp() — nunca px fixo para fontes/padding/sizes
- Touch targets: minimo 44x44px em TODOS elementos interativos
- Bilingue: prop lang="br"|"en", todos os textos via objeto { br: "...", en: "..." }
- Sons: useSoundManager() hook — sons procedurais Web Audio API (11 sons mapeados)
- FiveM NUI: Chromium 93+ (suporta backdrop-filter, Canvas 2D, Web Audio API, CSS Grid, clamp)
- Curva: Canvas 2D com requestAnimationFrame, lineWidth 3, shadowBlur 0 (glow via gradiente fake)
- Multiplicador: DOM overlay sobre Canvas, NAO renderizado no Canvas
- Gradiente da curva: createLinearGradient com stops #00E676 (0%), #FFD700 (60%), #FF4444 (90%)
- Grid de fundo: linhas rgba(212,168,67,0.05) a cada 20% do canvas
- Crash point: calculado server-side via HMAC_SHA256 Provably Fair — client NUNCA conhece antes do crash
- Multiplicador formula: multiplier = Math.pow(Math.E, GROWTH_RATE * elapsed)
- Tom ascendente: OscillatorNode sine, frequency 200Hz->2000Hz proporcional ao log(multiplier)
- Confetti: reutilizar GoldParticles.tsx existente, pool max 20 particulas
- ZERO dependencias novas — Canvas 2D nativo, Web Audio API nativa, Framer Motion ja instalado
- ShadowBlur DESLIGADO no Canvas — glow via CSS overlay ou radialGradient fake no Canvas
- Anti-cheat: server e autoridade unica. Client envia bet/cashout, server valida e retorna resultado
- RTP: 99% (house edge 1%). ~1% dos rounds crasham em 1.00x instantaneamente
```

---

# PALETA BASE — BLACKOUT CASINO (OBRIGATORIA)

```
BACKGROUNDS:
  #050505   — fundo absoluto (atras de tudo)
  #0A0A0A   — fundo do jogo (background-color principal)
  #0F0F0F   — fundo do painel
  #141414   — card, container
  #1A1A1A   — sub-container, input inativo
  #222222   — hover state, item selecionado
  #2A2A2A   — tooltip, dropdown

OURO METALICO (bordas, titulos, moldura, ornamentos):
  #462523   — sombra profunda do metal
  #8B6914   — ouro escuro
  #CB9B51   — ouro medio
  #D4A843   — ouro principal (COR MAIS USADA — texto, borda, UI)
  #F6E27A   — ouro claro (highlight)
  #F6F2C0   — ponto de brilho maximo
  #FFD700   — ouro puro (hover, accent, CTA)

VERDE ESMERALDA (win, ativo, CTA, saldo, acertos):
  #00E676   — verde brilhante (accent principal)
  #00C853   — verde forte (botao primario CTA)
  #004D25   — verde escuro (base botao)

VERMELHO (perda, erro, crash):
  #FF1744   — vermelho vibrante
  #FF4444   — vermelho de crash/perda
  #FF6B6B   — vermelho claro (botao cashout)

TEXTO:
  #FFFFFF   — texto principal
  #A8A8A8   — texto secundario
  #666666   — texto inativo/placeholder
```

# PALETA ESPECIFICA — CRASH

```
CURVA ASCENDENTE (Canvas 2D):
  Gradiente da linha (createLinearGradient diagonal):
    stop 0.0: #00E676  (verde neon — inicio, multiplicador baixo)
    stop 0.4: #00E676  (verde mantem ate ~2x)
    stop 0.6: #FFD700  (dourado — zona intermediaria ~2x-5x)
    stop 0.8: #FF6B6B  (vermelho claro — zona de perigo ~5x-10x)
    stop 1.0: #FF4444  (vermelho intenso — zona extrema 10x+)
  
  Glow da curva (CSS overlay, NAO shadowBlur):
    box-shadow no container do canvas:
      0 0 20px rgba(0,230,118,0.15) durante RISING
      0 0 20px rgba(255,68,68,0.2) durante CRASHED

  Espessura: lineWidth 3 (constante)
  Estilo: ctx.lineCap = 'round'; ctx.lineJoin = 'round'

GRID DE FUNDO (Canvas 2D):
  Linhas horizontais: strokeStyle rgba(212,168,67,0.05), lineWidth 0.5
  Linhas verticais: strokeStyle rgba(212,168,67,0.05), lineWidth 0.5
  Labels eixo Y: fillStyle rgba(212,168,67,0.25), font clamp(8px,0.7vw,11px) JetBrains Mono
  Labels: "1x" a 20% da altura, "2x" a 40%, "5x" a 60%, "10x" a 80%, "50x" a 95%

MULTIPLICADOR CENTRAL (DOM overlay):
  WAITING:  color #D4A843, textShadow 0 0 12px rgba(255,215,0,0.4)
  BETTING:  color #FFD700, textShadow 0 0 20px rgba(255,215,0,0.6) — pulsa
  RISING:   color #00E676, textShadow 0 0 20px rgba(0,230,118,0.6) — pulsa scale
  CRASHED:  color #FF4444, textShadow 0 0 20px rgba(255,68,68,0.5) — flash + scale up

HISTORICO BADGES (por faixa de multiplicador):
  < 1.50x (vermelho):
    background: rgba(255,68,68,0.12)
    color: #FF6B6B
    border: 1px solid rgba(255,68,68,0.25)
  
  1.50x - 1.99x (dourado fraco):
    background: rgba(212,168,67,0.08)
    color: rgba(212,168,67,0.6)
    border: 1px solid rgba(212,168,67,0.15)
  
  2.00x - 9.99x (dourado forte):
    background: rgba(212,168,67,0.15)
    color: #D4A843
    border: 1px solid rgba(212,168,67,0.3)
  
  >= 10.00x (verde):
    background: rgba(0,230,118,0.12)
    color: #00E676
    border: 1px solid rgba(0,230,118,0.25)
  
  1.00x (crash instantaneo — amarelo especial):
    background: rgba(255,215,0,0.15)
    color: #FFD700
    border: 1px solid rgba(255,215,0,0.3)

CRASH ANIMATION (Canvas + DOM):
  Flash vermelho: ctx.fillStyle rgba(255,68,68,0.12), fillRect full canvas, 200ms fade
  Particulas de explosao (20 circulos):
    cores: #FF4444 (50%), #FFD700 (30%), #D4A843 (20%)
    radius: random 2-5px
    velocidade: random 1-4px/frame
    gravidade: 0.05px/frame
    opacity: 1.0 -> 0.0 em 60 frames
    posicao inicial: ponto final da curva

FEED DE APOSTAS:
  Nome jogador: color rgba(212,168,67,0.6), font Inter 500
  Valor aposta: color #00E676, font JetBrains Mono 600
  Status "Sacou Xx": color #00E676
  Status "Perdeu": color #FF4444
  Status "Apostou": color rgba(212,168,67,0.4)
  Separador: borderBottom 1px solid rgba(212,168,67,0.06)

BOTAO CASHOUT (estado especifico do Crash):
  Normal: background linear-gradient(180deg, #FF6B6B 0%, #FF4444 100%)
  Pulsando: @keyframes cashoutPulse {
    0%, 100% { box-shadow: 0 0 10px rgba(255,68,68,0.3) }
    50% { box-shadow: 0 0 25px rgba(255,68,68,0.6), 0 0 40px rgba(255,68,68,0.2) }
  }
  Hover: transform scale(1.05), box-shadow 0 6px 24px rgba(255,68,68,0.4)
  Active: transform scale(0.97)
  Texto dinamico: "SACAR {valor} GC" em JetBrains Mono 800

COUNTDOWN ENTRE ROUNDS:
  Barra de progresso: linear-gradient(90deg, #D4A843 0%, #00E676 100%)
  Altura: 3px, borderRadius 2px
  Texto: Cinzel 700, color #D4A843, fontSize clamp(14px,2vw,24px)
  Numero: JetBrains Mono 800, color #FFD700, fontSize clamp(20px,3vw,36px)
```

---

# TIPOGRAFIA

```
TITULO DO JOGO "CRASH":
  fontFamily: 'Cinzel', serif
  fontWeight: 900
  fontSize: clamp(14px, 2vw, 24px)
  textTransform: uppercase
  letterSpacing: 3px
  color: #D4A843
  textShadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1)

MULTIPLICADOR CENTRAL:
  fontFamily: 'JetBrains Mono', monospace
  fontWeight: 800
  fontSize: clamp(28px, 5vw, 72px)
  letterSpacing: 2px
  /* Cor e shadow variam por estado — ver paleta especifica */

LABELS DE CONTROLE ("Aposta:", "Sacar em:", "Manual", "Auto"):
  fontFamily: 'Inter', sans-serif
  fontWeight: 600
  fontSize: clamp(9px, 1vw, 13px)
  textTransform: uppercase
  letterSpacing: 1px
  color: #A8A8A8

VALORES GCOIN (saldo, aposta, payout):
  fontFamily: 'JetBrains Mono', monospace
  fontWeight: 700
  fontSize: clamp(12px, 1.4vw, 18px)
  color: #00E676
  textShadow: 0 0 8px rgba(0,230,118,0.4)

BADGES DE HISTORICO:
  fontFamily: 'JetBrains Mono', monospace
  fontWeight: 700
  fontSize: clamp(8px, 0.9vw, 12px)
  /* Cor varia por faixa — ver paleta historico badges */

BOTOES CTA ("APOSTAR", "CASHOUT"):
  fontFamily: 'Cinzel', serif
  fontWeight: 800
  fontSize: clamp(12px, 1.5vw, 20px)
  textTransform: uppercase
  letterSpacing: 2px

FEED DE APOSTAS (nome, valor, status):
  fontFamily: 'Inter', sans-serif (nomes) / 'JetBrains Mono' (valores)
  fontWeight: 500 (nomes) / 600 (valores)
  fontSize: clamp(9px, 1vw, 13px)

COUNTDOWN TIMER:
  fontFamily: 'JetBrains Mono', monospace
  fontWeight: 800
  fontSize: clamp(20px, 3vw, 36px)
  color: #FFD700
  textShadow: 0 0 15px rgba(255,215,0,0.5)

TOOLTIP:
  fontFamily: 'Inter', sans-serif
  fontWeight: 500
  fontSize: clamp(9px, 0.9vw, 12px)
  color: #FFFFFF
  background: #2A2A2A
  border: 1px solid rgba(212,168,67,0.2)
  borderRadius: 6px
  padding: 6px 10px
  boxShadow: 0 4px 12px rgba(0,0,0,0.5)
```

---

# TELA 1: WAITING / BETTING — Configuracao de Aposta + Countdown

## Layout ASCII (Landscape — NUI FiveM)

```
+------------------------------------------------------------------+
| [< VOLTAR]         CRASH          G$8.250    [PF] [SOM]          |
|------------------------------------------------------------------|
| [1.23x][45.67x][1.01x][8.92x][2.34x][156x][1.05x][3.41x][2.8x] |
|------------------------------------------------------------------|
|                                           |                       |
|   +--- CANVAS (58% largura) ----------+   | +-- CONTROLES (38%) -+|
|   |                                    |   | |                    ||
|   | Grid dourado sutil                 |   | | Aposta:            ||
|   |                                    |   | | [______] GC        ||
|   |                                    |   | | [MIN][x2][/2][MAX] ||
|   |        PROXIMO ROUND               |   | |                    ||
|   |           EM 4s                    |   | | Sacar em:          ||
|   |     [====--------] (barra)        |   | | [______] x         ||
|   |                                    |   | |                    ||
|   |                                    |   | | [MANUAL] [AUTO]    ||
|   +------------------------------------+   | |                    ||
|                                           | | [   APOSTAR    ]   ||
|   +--- FEED (sob canvas) -------------+   | |                    ||
|   | Aguardando apostas...              |   | +--------------------+|
|   +------------------------------------+   |                       |
+------------------------------------------------------------------+
```

## Especificacoes Tela 1

**Container principal (CrashGame.tsx):**
```css
position: absolute; inset: 0; z-index: 60;
border-radius: inherit; overflow: hidden;
background-color: #0A0A0A;
background-image:
  url('/assets/shared/ui/bg-casino.png'),
  radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
  radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
background-size: cover, 100% 100%, 100% 100%;
box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
display: flex; flex-direction: column;
padding: clamp(6px, 1vw, 12px);
gap: clamp(4px, 0.6vw, 8px);
```

**Header (titulo + saldo + icones):**
```css
display: flex; align-items: center; justify-content: space-between;
padding: 0 clamp(4px, 0.8vw, 10px);
height: clamp(28px, 4vw, 40px);
/* Titulo "CRASH": Cinzel 900, ver tipografia */
/* Saldo: JetBrains Mono 700, #00E676, textShadow verde */
/* Icones PF e SOM: 20x20px, opacity 0.6, hover opacity 1, cursor pointer */
```

**Botao VOLTAR:**
```css
position: absolute;
top: clamp(6px, 1vw, 12px); left: clamp(6px, 1vw, 12px);
background: rgba(0,0,0,0.6);
border: 1px solid rgba(212,168,67,0.4);
border-radius: 8px;
color: #D4A843;
font-family: 'Cinzel', serif; font-weight: 700;
font-size: clamp(9px, 1.1vw, 14px);
letter-spacing: 2px;
padding: clamp(5px, 0.7vw, 8px) clamp(8px, 1.2vw, 14px);
cursor: pointer;
transition: all 0.25s ease;
/* Hover: borderColor rgba(212,168,67,0.8), transform scale(1.05) */
/* Active: transform scale(0.95) */
```

**Barra de historico:**
```css
display: flex; gap: clamp(3px, 0.4vw, 6px);
overflow-x: auto; overflow-y: hidden;
padding: clamp(3px, 0.4vw, 6px) 0;
scrollbar-width: none; /* Firefox */
-webkit-overflow-scrolling: touch;
/* Cada badge: ver paleta HISTORICO BADGES */
/* padding: clamp(2px, 0.3vw, 4px) clamp(5px, 0.7vw, 8px) */
/* border-radius: clamp(3px, 0.4vw, 5px) */
/* font: JetBrains Mono 700, clamp(8px, 0.9vw, 12px) */
/* white-space: nowrap */
/* transition: all 0.2s ease */
/* Hover: transform scale(1.05), brightness(1.1) */
```

**Area central (Canvas + Controles lado a lado):**
```css
display: flex; flex: 1;
gap: clamp(8px, 1.2vw, 16px);
min-height: 0; /* permite flex shrink */
/* Canvas wrapper: flex 0 0 58% */
/* Controles wrapper: flex 0 0 38% */
/* Mobile portrait: flex-direction column, ambos 100% width */
```

**Canvas wrapper:**
```css
flex: 0 0 58%;
position: relative;
background: rgba(0,0,0,0.3);
border: 1px solid rgba(212,168,67,0.15);
border-radius: clamp(8px, 1vw, 12px);
overflow: hidden;
/* Canvas interno: width 100%, aspect-ratio 2/1 */
```

**Multiplicador central (DOM overlay sobre canvas):**
```css
position: absolute;
top: 50%; left: 50%;
transform: translate(-50%, -50%);
font-family: 'JetBrains Mono', monospace;
font-weight: 800;
font-size: clamp(28px, 5vw, 72px);
letter-spacing: 2px;
pointer-events: none;
z-index: 2;
/* WAITING: "PROXIMO ROUND" em Cinzel 700, clamp(14px,2vw,24px), #D4A843 */
/* BETTING: "APOSTE AGORA" em Cinzel 800, clamp(16px,2.2vw,28px), #FFD700, animation pulseGlow 2s infinite */
/* RISING: "{multiplier}x" em JetBrains Mono 800, ver tamanho acima, #00E676 */
/* CRASHED: "CRASHED @ {X}x" em JetBrains Mono 800, #FF4444, transform scale(1.3) momentaneo */
```

**Countdown (dentro do canvas wrapper, durante WAITING):**
```css
position: absolute;
bottom: clamp(12px, 2vw, 24px); left: 50%;
transform: translateX(-50%);
display: flex; flex-direction: column; align-items: center; gap: 6px;
z-index: 2;
/* Texto "PROXIMO ROUND EM": Inter 600, clamp(9px,1vw,13px), #A8A8A8 */
/* Numero "4s": JetBrains Mono 800, clamp(20px,3vw,36px), #FFD700, textShadow 0 0 15px rgba(255,215,0,0.5) */
/* Barra: width clamp(120px,20vw,200px), height 3px, borderRadius 2px, background #1A1A1A */
/* Barra fill: height 100%, borderRadius 2px, background linear-gradient(90deg, #D4A843, #00E676), transition width 1s linear */
```

**Painel de controles:**
```css
flex: 0 0 38%;
display: flex; flex-direction: column;
gap: clamp(6px, 0.8vw, 10px);
background: rgba(5,5,5,0.85);
border: 1px solid rgba(212,168,67,0.15);
border-radius: clamp(8px, 1vw, 12px);
padding: clamp(8px, 1.2vw, 14px);
overflow-y: auto;
```

**Input de aposta:**
```css
width: 100%;
background: rgba(0,0,0,0.6);
border: 1px solid rgba(212,168,67,0.25);
border-radius: 8px;
color: #00E676;
font-family: 'JetBrains Mono', monospace;
font-weight: 700;
font-size: clamp(13px, 1.5vw, 20px);
padding: clamp(8px, 1vw, 12px);
outline: none;
transition: all 0.25s ease;
/* Focus: border-color rgba(0,230,118,0.5), box-shadow 0 0 8px rgba(0,230,118,0.15) */
/* Error (saldo insuficiente): border-color rgba(255,68,68,0.5), color #FF4444 */
/* Placeholder: color #666666 */
```

**Atalhos MIN/x2/div2/MAX:**
```css
display: flex; gap: clamp(3px, 0.4vw, 5px);
/* Cada botao: */
flex: 1;
min-width: 44px; min-height: 44px;
background: rgba(212,168,67,0.08);
border: 1px solid rgba(212,168,67,0.15);
border-radius: 6px;
color: #D4A843;
font-family: 'JetBrains Mono', monospace;
font-weight: 600;
font-size: clamp(9px, 1vw, 12px);
cursor: pointer;
transition: all 0.2s ease;
display: flex; align-items: center; justify-content: center;
/* Hover: background rgba(212,168,67,0.15), border-color rgba(212,168,67,0.3) */
/* Active: transform scale(0.95) */
/* Disabled: opacity 0.3, cursor not-allowed */
```

**Input Auto Cashout:**
```css
/* Mesmo estilo do input de aposta */
/* Label: "Sacar em:" Inter 600, clamp(9px,1vw,13px), #A8A8A8, uppercase, letterSpacing 1px */
/* Suffix "x": position absolute, right 12px, top 50%, transform translateY(-50%), color #A8A8A8 */
/* Vazio = cashout manual (placeholder: "Manual") */
```

**Toggle Manual/Auto:**
```css
display: flex;
border-bottom: 1px solid rgba(212,168,67,0.1);
margin-bottom: clamp(4px, 0.6vw, 8px);
/* Cada tab: */
flex: 1;
padding: clamp(6px, 0.8vw, 10px);
font-family: 'Cinzel', serif; font-weight: 700;
font-size: clamp(9px, 1.1vw, 13px);
text-transform: uppercase; letter-spacing: 1.5px;
text-align: center; cursor: pointer;
transition: all 0.25s ease;
/* Inactive: color #666666, border-bottom 2px solid transparent */
/* Active: color #D4A843, border-bottom 2px solid #D4A843, background rgba(212,168,67,0.05) */
/* Hover inactive: color rgba(212,168,67,0.5) */
```

**Botao APOSTAR (durante BETTING):**
```css
width: 100%;
padding: clamp(10px, 1.4vw, 16px);
background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
border: 1.5px solid rgba(0,230,118,0.3);
border-radius: 10px;
color: #FFFFFF;
font-family: 'Cinzel', serif; font-weight: 800;
font-size: clamp(12px, 1.5vw, 20px);
text-transform: uppercase; letter-spacing: 2px;
cursor: pointer;
min-height: 48px;
box-shadow: 0 4px 16px rgba(0,200,83,0.2), inset 0 1px 0 rgba(255,255,255,0.1);
transition: all 0.25s ease;
/* Hover: box-shadow 0 6px 24px rgba(0,200,83,0.3), transform translateY(-1px) */
/* Active: transform translateY(1px), box-shadow 0 2px 8px rgba(0,200,83,0.15) */
/* Disabled (round nao em betting ou saldo insuficiente): opacity 0.35, cursor not-allowed, background #1A1A1A, border-color rgba(255,255,255,0.05) */
```

**Feed de apostas (sob o canvas):**
```css
max-height: clamp(60px, 10vh, 100px);
overflow-y: auto;
background: rgba(0,0,0,0.2);
border: 1px solid rgba(212,168,67,0.08);
border-radius: clamp(6px, 0.8vw, 8px);
padding: clamp(4px, 0.6vw, 8px);
/* Cada entrada: display flex, justify-content space-between, padding 3px 6px */
/* Separador: border-bottom 1px solid rgba(212,168,67,0.06) */
/* Mensagem vazia: "Aguardando apostas..." Inter 500 11px #666666 italic */
/* Ver paleta FEED DE APOSTAS para cores */
```

**Framer Motion — entrada Tela 1:**
```tsx
<motion.div
  initial={{ opacity: 0, scale: 0.92, y: 30 }}
  animate={{ opacity: 1, scale: 1, y: 0 }}
  transition={{ duration: 0.6, ease: [0.0, 0.0, 0.2, 1] }}
>
```

**Framer Motion — badges historico stagger:**
```tsx
{history.map((h, i) => (
  <motion.div
    key={h.roundId}
    initial={{ opacity: 0, x: -15, scale: 0.85 }}
    animate={{ opacity: 1, x: 0, scale: 1 }}
    transition={{ delay: 0.04 * i, duration: 0.25 }}
    whileHover={{ scale: 1.08, brightness: 1.1 }}
  />
))}
```

**Framer Motion — botao APOSTAR:**
```tsx
<motion.button
  whileHover={{ scale: 1.03, y: -1 }}
  whileTap={{ scale: 0.97 }}
  transition={{ type: 'spring', stiffness: 400, damping: 25 }}
/>
```

**Framer Motion — countdown numero:**
```tsx
<motion.span
  key={secondsLeft}
  initial={{ scale: 1.4, opacity: 0 }}
  animate={{ scale: 1, opacity: 1 }}
  transition={{ duration: 0.3, ease: 'easeOut' }}
/>
```

**Framer Motion — "APOSTE AGORA" pulso (durante BETTING):**
```tsx
<motion.div
  animate={{ scale: [1, 1.06, 1], opacity: [0.8, 1, 0.8] }}
  transition={{ duration: 2, repeat: Infinity, ease: 'easeInOut' }}
/>
```

---

**[FIM DA PARTE 1 — Continua na Parte 2 com TELA 2 Rising (curva + cashout + feed ativo) e TELA 3 Crashed/Result]**

VALIDACAO X17: Tela 1 com 16 componentes CSS especificados (container, header, VOLTAR, historico, area central, canvas wrapper, multiplicador, countdown, painel controles, input aposta, atalhos, input cashout, toggle, botao APOSTAR, feed, 6 Framer Motion). 16 de 16 completos com estados (hover/active/disabled/focus documentados inline).

`[ENTREGA: 3.PROMPT-AI-CRASH-PARTE-1.md — 17/03/2026]`
