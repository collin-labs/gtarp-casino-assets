# CSS COMPONENTES — SLOT-MACHINE (#2)
## Parte 2 de 2 - Componentes 14-25 + Responsive + Checklist Final
## Colar JUNTO com Parte 1 e PROMPT-V0 ao gerar visual no V0/Bolt
### Blackout Casino GTARP - 19/03/2026

---

# 14. TUMBLE BADGE

```css
/* DS sec1.14 — badge no canto superior direito do frame */
.slots-tumble-badge {
  position: absolute;
  top: clamp(4px, 0.5vw, 8px); right: clamp(4px, 0.5vw, 8px);
  background: rgba(0,230,118,0.1);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: 6px;
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Cinzel', serif; font-weight: 600;
  font-size: clamp(9px, 1.1vw, 13px);
  color: #00E676; text-transform: uppercase; letter-spacing: 1px;
  z-index: 10;
  animation: slotsTumbleBump 0.3s ease;
}
```

---

# 15. FREE SPINS HUD

```css
/* DS sec1.15 — barra HUD entre header e grid durante FS */
.slots-fs-hud {
  display: flex; justify-content: space-between; align-items: center;
  width: 100%;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.7);
  border-bottom: 1px solid rgba(255,215,0,0.2);
  z-index: 10;
}

.slots-fs-hud__counter {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.3vw, 15px); color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
  text-transform: uppercase; letter-spacing: 1px;
}

.slots-fs-hud__multiplier {
  font-family: 'JetBrains Mono', monospace; font-weight: 900;
  font-size: clamp(12px, 1.6vw, 18px); color: #FFD700;
  text-shadow: 0 0 10px rgba(255,215,0,0.5);
}

.slots-fs-hud__win {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(11px, 1.4vw, 16px); color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
```

---

# 16. FS TINT DOURADO

```css
/* DS sec2.3 — overlay sutil durante free spins */
.slots-fs-tint {
  position: absolute; inset: 0;
  background: rgba(212,168,67,0.05);
  pointer-events: none; z-index: 1;
}
```

---

# 17. WIN OVERLAY (4 tiers)

```css
/* DS sec1.16 — overlay base (shared) */
.slots-win-overlay {
  position: absolute; inset: 0;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

/* BIG WIN (ratio 5x-49x) */
.slots-win-overlay--big {
  z-index: 80; background: rgba(0,0,0,0.5);
  backdrop-filter: blur(4px);
}

/* MEGA WIN (ratio 50x-499x) */
.slots-win-overlay--mega {
  z-index: 90; background: rgba(0,0,0,0.75);
  backdrop-filter: blur(8px);
}

/* JACKPOT (ratio >= 500x) */
.slots-win-overlay--jackpot {
  z-index: 100; background: rgba(0,0,0,0.85);
  backdrop-filter: blur(12px);
  animation: slotsJackpotShake 0.5s ease 0.3s;
}

/* titulo do win */
.slots-win-overlay__title {
  font-family: 'Cinzel', serif; text-transform: uppercase;
  color: #FFD700;
}

.slots-win-overlay__title--big {
  font-weight: 800; font-size: clamp(18px, 3vw, 32px);
  letter-spacing: 3px;
  text-shadow: 0 0 15px rgba(255,215,0,0.5), 0 2px 6px rgba(0,0,0,1);
}

.slots-win-overlay__title--mega {
  font-weight: 900; font-size: clamp(22px, 4vw, 40px);
  letter-spacing: 4px;
  text-shadow: 0 0 25px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.3), 0 2px 8px rgba(0,0,0,1);
}

.slots-win-overlay__title--jackpot {
  font-weight: 900; font-size: clamp(26px, 5vw, 48px);
  letter-spacing: 5px;
  text-shadow: 0 0 30px rgba(255,215,0,0.8), 0 0 80px rgba(255,215,0,0.4), 0 0 120px rgba(255,215,0,0.2);
}

/* countup do valor */
.slots-win-overlay__value {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(18px, 3.5vw, 36px); color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.6);
}
```

---

# 18. FS TRIGGER + FS END OVERLAYS

```css
/* DS sec1.18 — overlay FS trigger (3.2s) */
.slots-fs-trigger {
  position: absolute; inset: 0; z-index: 100;
  background: rgba(0,0,0,0.8); backdrop-filter: blur(8px);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

.slots-fs-trigger__title {
  font-family: 'Cinzel', serif; font-weight: 900;
  font-size: clamp(24px, 5vw, 48px); color: #FFD700;
  text-transform: uppercase; letter-spacing: 4px;
  text-shadow: 0 0 25px rgba(255,215,0,0.6), 0 0 60px rgba(255,215,0,0.3);
  animation: slotsFSTriggerTitle 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275) both;
}

.slots-fs-trigger__count {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(16px, 3vw, 28px); color: #FFFFFF;
}

/* DS sec1.18 — overlay FS end */
.slots-fs-end {
  position: absolute; inset: 0; z-index: 100;
  background: rgba(0,0,0,0.8); backdrop-filter: blur(6px);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: clamp(8px, 1.5vw, 16px);
}

.slots-fs-end__title {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(14px, 2vw, 22px); color: #D4A843;
  text-transform: uppercase; letter-spacing: 2px;
  text-shadow: 0 0 10px rgba(212,168,67,0.4);
}

.slots-fs-end__total {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(18px, 3.5vw, 36px); color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.6);
}
```

---

# 19. CLASSIC REEL

```css
/* DS sec1.19 — reel individual */
.slots-reel {
  width: clamp(60px, 10vw, 90px);
  height: clamp(180px, 25vw, 270px);
  overflow: hidden;
  border: 1.5px solid rgba(212,168,67,0.25);
  border-radius: 10px;
  background: linear-gradient(180deg, rgba(0,0,0,0.6) 0%, transparent 15%, transparent 85%, rgba(0,0,0,0.6) 100%);
  position: relative;
}

/* simbolo dentro do reel */
.slots-reel__symbol {
  width: 100%; height: clamp(60px, 8.3vw, 90px);
  display: flex; align-items: center; justify-content: center;
  font-weight: 900;
}

/* spinning state */
.slots-reel--spinning .slots-reel__inner {
  animation: slotsReelSpin 0.3s infinite linear;
}

/* stopped state */
.slots-reel--stopped .slots-reel__inner {
  transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
```

---

# 20. MODE SELECT CARD

```css
/* DS sec1.20 — card de selecao de modo */
.slots-mode-card {
  width: clamp(180px, 28vw, 280px);
  padding: clamp(16px, 2.5vw, 32px);
  background: rgba(5,5,5,0.9);
  border: 1.5px solid rgba(212,168,67,0.2);
  border-radius: 14px; cursor: pointer;
  display: flex; flex-direction: column; align-items: center;
  gap: clamp(8px, 1.2vw, 16px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.4);
  transition: all 0.3s ease;
}

.slots-mode-card:hover {
  border-color: rgba(212,168,67,0.5);
  transform: translateY(-3px);
}

.slots-mode-card:active {
  transform: scale(0.97);
}

.slots-mode-card__icon {
  width: clamp(48px, 7vw, 72px); height: clamp(48px, 7vw, 72px);
  border-radius: 12px; background: rgba(212,168,67,0.08);
  border: 1px solid rgba(212,168,67,0.15);
  display: flex; align-items: center; justify-content: center;
  font-size: clamp(24px, 3.5vw, 36px);
}

.slots-mode-card__name {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(14px, 1.8vw, 20px); color: #D4A843;
  text-transform: uppercase; letter-spacing: 2px;
}

.slots-mode-card__desc {
  font-family: 'Inter', sans-serif; font-weight: 500;
  font-size: clamp(10px, 1.2vw, 14px); color: #A8A8A8;
  text-align: center; line-height: 1.5;
}
```

---

# 21. MODAL GLASSMORPHISM (Paytable, History, Buy Bonus)

```css
/* DS sec1.21 — backdrop */
.slots-modal-backdrop {
  position: absolute; inset: 0; z-index: 70;
  display: flex; align-items: center; justify-content: center;
  background: rgba(0,0,0,0.6);
  animation: slotsModalFadeIn 0.2s ease;
}

/* card glassmorphism */
.slots-modal {
  width: clamp(300px, 65vw, 520px); max-height: 80vh; overflow-y: auto;
  background: rgba(10,10,10,0.95);
  border: 1.5px solid rgba(212,168,67,0.3); border-radius: 14px;
  padding: clamp(16px, 2.5vw, 28px);
  backdrop-filter: blur(12px);
  box-shadow: 0 8px 40px rgba(0,0,0,0.6);
  position: relative;
}

.slots-modal__title {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(13px, 1.8vw, 18px); color: #D4A843;
  text-transform: uppercase; letter-spacing: 2px;
  text-shadow: 0 0 8px rgba(255,215,0,0.3);
  margin-bottom: clamp(12px, 2vw, 20px);
}

.slots-modal__close {
  position: absolute; top: clamp(8px, 1vw, 12px); right: clamp(8px, 1vw, 12px);
  width: 36px; height: 36px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1); border-radius: 8px;
  color: #A8A8A8; font-size: 16px; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s ease;
}

.slots-modal__close:hover {
  background: rgba(255,255,255,0.10);
  color: #FFFFFF;
}
```

---

# 22. BOTAO CTA (JOGAR / CONTINUAR / CONFIRMAR)

```css
/* DS sec varios — botao CTA verde */
.slots-cta {
  padding: clamp(8px, 1vw, 12px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3); border-radius: 8px;
  color: #FFFFFF; font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase; letter-spacing: 2px;
  cursor: pointer; min-height: 44px;
  transition: all 0.2s ease;
}

.slots-cta:hover {
  transform: scale(1.05); box-shadow: 0 0 15px rgba(0,230,118,0.3);
}

.slots-cta:active {
  transform: scale(0.95);
}

.slots-cta:disabled {
  opacity: 0.4; cursor: not-allowed; transform: none;
}

/* CTA dourado (Buy Bonus confirmar) */
.slots-cta--gold {
  background: linear-gradient(180deg, #FFD700 0%, #8B6914 100%);
  border-color: rgba(255,215,0,0.4); color: #0A0A0A;
}

.slots-cta--gold:hover {
  box-shadow: 0 0 15px rgba(255,215,0,0.4);
}
```

---

# 23. HISTORY SPIN ROW

```css
/* DS sec1.21 — row de historico */
.slots-history-row {
  display: flex; align-items: center; justify-content: space-between;
  padding: clamp(8px, 1vw, 12px) 0;
  border-bottom: 1px solid rgba(255,255,255,0.04);
  gap: clamp(6px, 0.8vw, 10px);
}

.slots-history-row__id {
  font-family: 'JetBrains Mono', monospace; font-weight: 600;
  font-size: clamp(10px, 1.2vw, 13px); color: #A8A8A8;
}

/* mode badge */
.slots-history-row__badge {
  padding: 1px 6px; border-radius: 4px;
  font-family: 'Inter', sans-serif; font-weight: 600;
  font-size: clamp(8px, 0.9vw, 10px); text-transform: uppercase;
}

.slots-history-row__badge--video {
  background: rgba(212,168,67,0.1); border: 1px solid rgba(212,168,67,0.2);
  color: #D4A843;
}

.slots-history-row__badge--classic {
  background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08);
  color: #A8A8A8;
}

/* win amount */
.slots-history-row__win { font-family: 'JetBrains Mono', monospace; font-weight: 700; font-size: clamp(10px, 1.2vw, 13px); }
.slots-history-row__win--profit { color: #00E676; }
.slots-history-row__win--big { color: #FFD700; }
.slots-history-row__win--zero { color: #666666; }
```

---

# 24. BUY BONUS BUTTON

```css
/* DS sec1.25 — botao Buy Bonus na bet bar */
.slots-buy-bonus {
  min-width: 44px; min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,215,0,0.1);
  border: 1px solid rgba(255,215,0,0.3);
  border-radius: 6px; color: #FFD700;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(7px, 0.9vw, 10px);
  text-transform: uppercase; letter-spacing: 0.5px;
  cursor: pointer; transition: all 0.2s ease;
}

.slots-buy-bonus:hover {
  background: rgba(255,215,0,0.15);
  box-shadow: 0 0 10px rgba(255,215,0,0.2);
}

.slots-buy-bonus:disabled {
  opacity: 0.35; cursor: not-allowed;
}
```

---

# 25. SEPARADOR VISUAL

```css
/* separadores na bet bar */
.slots-separator {
  width: 1px; height: 28px;
  background: rgba(255,255,255,0.08);
  flex-shrink: 0;
}
```

---

# 26. RESPONSIVE (REGRA X4)

```css
/* Mobile landscape (1280x720) e menor */
@media (max-width: 768px) and (orientation: landscape) {
  .slots-frame {
    width: 95vw; max-height: 50vh;
  }
  .slots-grid {
    gap: 2px;
  }
  .slots-cell {
    font-size: clamp(14px, 2vw, 22px);
    border-radius: 4px;
  }
  .slots-bet-bar {
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
  }
  .slots-reel {
    width: clamp(50px, 14vw, 70px);
    height: clamp(140px, 20vw, 210px);
  }
  .slots-mode-card {
    width: clamp(150px, 40vw, 200px);
    padding: clamp(12px, 2vw, 20px);
  }
  .slots-modal {
    width: 90vw; max-height: 70vh;
  }
}
```

---

# CHECKLIST FINAL DE VALIDACAO (52 itens — REGRA X10 + X5)

## Superficies e Cores (8 itens)
- [x] Fundo #0A0A0A com bg-casino.png e vinheta
- [x] Frame border #D4A843 (idle) / #FFD700 (FS)
- [x] Grid fundo #0F0F0F → #0A0A0A gradiente
- [x] Celula fundo rgba(255,255,255,0.03) (normal)
- [x] Scatter fundo radial-gradient dourado 15%
- [x] Bet bar fundo rgba(0,0,0,0.6) + blur 4px
- [x] Modal fundo rgba(10,10,10,0.95) + blur 12px
- [x] Win overlay 4 tiers com backdrop blur progressivo

## Tipografia (7 itens)
- [x] Cinzel para titulos, botoes, badges
- [x] JetBrains Mono para valores, payouts, timers
- [x] Inter para descricoes, corpo
- [x] Todos com clamp() (min, vw, max)
- [x] text-shadow em todos titulos/valores
- [x] letterSpacing em titulos e badges
- [x] textTransform uppercase em titulos e badges

## Bordas e Shadows (6 itens)
- [x] Frame: border + outline + outlineOffset + 3 camadas box-shadow
- [x] Celulas: border + transition all 0.3s
- [x] Scatter: border 2px + animation pulse
- [x] Modal: border + backdrop-filter + box-shadow
- [x] Spin button: border + box-shadow glow + animation pulse
- [x] Cards: border + box-shadow + transition hover

## @keyframes (9 itens)
- [x] slotsFrameFSGlow (frame FS pulse)
- [x] slotsWinGlow (celula winning)
- [x] slotsScatterPulse (scatter)
- [x] slotsSpinPulse + slotsSpinFSPulse (SPIN idle + FS)
- [x] slotsTumbleBump (badge scale)
- [x] slotsJackpotShake (screen shake)
- [x] slotsReelSpin (classic reel)
- [x] slotsShineSweep + slotsAnticipation (extras)
- [x] slotsModalFadeIn + slotsAutoPulse (UI)

## Interatividade / Tooltips (7 itens — REGRA X5)
- [x] 22 tooltips via title={} (mapeados no Prompt-V0)
- [x] Botoes: normal + :hover + :active + :disabled
- [x] ANTE: 2 estados (ativo/inativo)
- [x] AUTO: 2 estados + badge contagem
- [x] TURBO: 2 estados
- [x] SPIN: 4 estados (idle, hover, active, disabled) + variante FS
- [x] Celula: 5 estados (normal, hover, winning, dimmed, scatter) + 9 variantes winning

## Responsividade Mobile (5 itens — REGRA X4)
- [x] Frame: width 95vw em mobile
- [x] Grid: gap 2px em mobile
- [x] BetControls: overflow-x auto com scroll-snap
- [x] Reel: width/height reduzidos via clamp
- [x] Modal: 90vw em mobile

## Sons (22 — mapeados em sounds.ts)
- [x] 22 sons confirmados no Roteiro Fase 4

# VALIDACAO FINAL (REGRA X11)

| Criterio | Status |
|----------|:------:|
| TODOS os componentes do Adendo DS cobertos? | SIM — 25 componentes + responsive |
| Tokens :root separados primitivos/semanticos? | SIM — 65 variaveis |
| @keyframes numerados e referenciados ao DS? | SIM — 15 keyframes |
| Cada componente tem TODOS os estados? | SIM — normal, hover, active, disabled, variantes |
| BEM naming .slots-* consistente? | SIM — block__element--modifier |
| Valores hex/rgba explicitos (nunca "dourado")? | SIM — 0 descricoes vagas |

**VALIDACAO X11: 6 de 6 criterios atendidos.**
**VALIDACAO X17: 25 componentes exigidos, 25 componentes entregues. 15 @keyframes. 52 itens checklist. 0 frases proibidas.**

---

`[ENTREGA: 7.CSS-COMPONENTES-SLOT-MACHINE-PARTE-2.md — 19/03/2026]`
