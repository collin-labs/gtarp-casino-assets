# CSS COMPONENTES — SLOT-MACHINE (#2)
## Parte 1 de 2 - Tokens :root + @keyframes (15) + Componentes 1-13
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-SLOT-MACHINE
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 19/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Slot Machine no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.slots-frame { border: 2px solid #D4A843; }` daqui.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Slot Machine)

```css
/* ====================================================
   SLOT MACHINE — DESIGN TOKENS
   Separacao: primitivos (raw) -> semanticos (uso)
   Ref: Adendo DS sec1, Prompt AI sec Header Tecnico
   ==================================================== */

:root {
  /* --- Primitivos — cores base --- */
  --slots-gold-primary:    #D4A843;
  --slots-gold-light:      #F6E27A;
  --slots-gold-dark:       #8B6914;
  --slots-gold-deeper:     #462523;
  --slots-green-neon:      #00E676;
  --slots-green-dark:      #004D25;
  --slots-green-medium:    #00C853;
  --slots-black-bg:        #0A0A0A;
  --slots-black-surface:   #0F0F0F;
  --slots-red:             #FF1744;
  --slots-orange:          #FF8F00;
  --slots-yellow:          #FFD700;
  --slots-blue:            #1E88E5;
  --slots-purple:          #8E24AA;
  --slots-teal:            #43A047;
  --slots-white:           #FFFFFF;
  --slots-text-secondary:  #A8A8A8;
  --slots-text-muted:      #666666;

  /* --- Semanticos — uso especifico --- */
  --slots-border-gold:         2px solid #D4A843;
  --slots-border-gold-fs:      2px solid #FFD700;
  --slots-border-subtle:       1px solid rgba(212,168,67,0.1);
  --slots-border-cell:         1px solid rgba(255,255,255,0.04);
  --slots-shadow-frame:        0 0 30px rgba(212,168,67,0.08), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.4);
  --slots-shadow-frame-fs:     0 0 40px rgba(255,215,0,0.12), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.3);
  --slots-shadow-card:         0 4px 16px rgba(0,0,0,0.4);
  --slots-shadow-modal:        0 8px 40px rgba(0,0,0,0.6);
  --slots-text-shadow-gold:    0 0 25px rgba(212,168,67,0.5), 0 2px 10px rgba(0,0,0,1);
  --slots-text-shadow-green:   0 0 10px rgba(0,230,118,0.5);
  --slots-text-shadow-jackpot: 0 0 30px rgba(255,215,0,0.8), 0 0 80px rgba(255,215,0,0.4), 0 0 120px rgba(255,215,0,0.2);

  /* --- Fontes --- */
  --slots-font-title: 'Cinzel', serif;
  --slots-font-body:  'Inter', sans-serif;
  --slots-font-mono:  'JetBrains Mono', monospace;

  /* --- Sizing --- */
  --slots-touch-min:     44px;
  --slots-radius-frame:  18px;
  --slots-radius-card:   14px;
  --slots-radius-btn:    8px;
  --slots-radius-cell:   8px;
  --slots-radius-small:  6px;
}
```

---

# 1. @KEYFRAMES (15 animacoes do Slot Machine)

```css
/* ====================================================
   SLOT MACHINE — @KEYFRAMES (15)
   Ref: Adendo DS sec4 (4 timelines), Prompt AI sec7
   ==================================================== */

/* 1. Pulse glow dourado no frame durante FS — DS sec1.3 */
@keyframes slotsFrameFSGlow {
  0%, 100% { box-shadow: 0 0 40px rgba(255,215,0,0.12), 0 8px 32px rgba(0,0,0,0.5); }
  50% { box-shadow: 0 0 55px rgba(255,215,0,0.20), 0 8px 32px rgba(0,0,0,0.5); }
}

/* 2. Simbolo winning glow pulse — DS sec1.6 */
@keyframes slotsWinGlow {
  0%, 100% { box-shadow: 0 0 12px var(--glow-color); }
  50% { box-shadow: 0 0 22px var(--glow-color); }
}

/* 3. Scatter pulse — DS sec1.7 */
@keyframes slotsScatterPulse {
  0%, 100% { box-shadow: 0 0 15px rgba(255,215,0,0.3); border-color: rgba(255,215,0,0.4); }
  50% { box-shadow: 0 0 25px rgba(255,215,0,0.5); border-color: rgba(255,215,0,0.6); }
}

/* 4. Orbe multiplicador brilho rotativo — DS sec1.8 */
@keyframes slotsOrbeSpin {
  0% { filter: brightness(1); }
  50% { filter: brightness(1.3); }
  100% { filter: brightness(1); }
}

/* 5. Spin button pulse idle — DS sec1.10 */
@keyframes slotsSpinPulse {
  0%, 100% { box-shadow: 0 0 15px rgba(0,230,118,0.3); }
  50% { box-shadow: 0 0 25px rgba(0,230,118,0.5); }
}

/* 6. Spin button pulse FS (dourado) — DS sec1.10 */
@keyframes slotsSpinFSPulse {
  0%, 100% { box-shadow: 0 0 20px rgba(255,215,0,0.4); }
  50% { box-shadow: 0 0 35px rgba(255,215,0,0.6); }
}

/* 7. Tumble badge scale bump — DS sec1.14 */
@keyframes slotsTumbleBump {
  0% { transform: scale(1.3); }
  100% { transform: scale(1); }
}

/* 8. Win countup pulse — DS sec1.13 */
@keyframes slotsWinPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

/* 9. FS trigger titulo entrada — DS sec1.18 */
@keyframes slotsFSTriggerTitle {
  0% { transform: scale(0) rotate(-10deg); opacity: 0; }
  100% { transform: scale(1) rotate(0deg); opacity: 1; }
}

/* 10. Jackpot screen shake — DS sec1.16 tier4 */
@keyframes slotsJackpotShake {
  0%, 100% { transform: translateX(0); }
  10% { transform: translateX(-4px); }
  20% { transform: translateX(4px); }
  30% { transform: translateX(-3px); }
  40% { transform: translateX(3px); }
  60% { transform: translateX(-1px); }
  80% { transform: translateX(1px); }
}

/* 11. Near-miss anticipation pulse — DS sec1.24 */
@keyframes slotsAnticipation {
  0%, 100% { border-color: rgba(255,215,0,0.3); }
  50% { border-color: rgba(255,215,0,0.7); }
}

/* 12. Shine sweep no card — DS sec1.20 */
@keyframes slotsShineSweep {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(200%); }
}

/* 13. Classic reel spin (translateY loop) — DS sec1.19 */
@keyframes slotsReelSpin {
  0% { transform: translateY(0); }
  100% { transform: translateY(-300%); }
}

/* 14. Auto-play badge pulse — DS sec1.11 */
@keyframes slotsAutoPulse {
  0%, 100% { opacity: 0.8; }
  50% { opacity: 1; }
}

/* 15. Modal backdrop fade-in — DS sec1.21 */
@keyframes slotsModalFadeIn {
  0% { opacity: 0; }
  100% { opacity: 1; }
}
```

---

# 2. CONTAINER PRINCIPAL DO JOGO

```css
/* DS sec1.1 — container 3 estados: mode_select, idle, free_spins */
.slots-bg {
  background-color: #0A0A0A;
  background-image:
    url('/assets/shared/ui/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: inset 0 0 150px rgba(0,0,0,0.8);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
}
```

---

# 3. HEADER

```css
/* DS sec1.2 — barra superior */
.slots-header {
  display: flex; justify-content: space-between; align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  border-bottom: 1px solid rgba(212,168,67,0.1);
  flex-shrink: 0; z-index: 5;
}

.slots-header__title {
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(14px, 2vw, 22px); color: #D4A843;
  text-transform: uppercase; letter-spacing: 3px;
  text-shadow: 0 0 12px rgba(255,215,0,0.4), 0 2px 4px rgba(0,0,0,0.8);
}

.slots-header__jackpot {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(11px, 1.4vw, 16px); color: #FFD700;
  text-shadow: 0 0 8px rgba(255,215,0,0.4);
}

.slots-header__balance {
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px); color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
```

---

# 4. FRAME DOURADO (Container do Grid)

```css
/* DS sec1.3 — frame idle */
.slots-frame {
  margin: clamp(4px, 0.8vw, 8px) auto;
  padding: clamp(6px, 1vw, 12px);
  border: 2px solid #D4A843;
  outline: 3px solid rgba(212,168,67,0.15);
  outline-offset: 4px; border-radius: 18px;
  box-shadow: 0 0 30px rgba(212,168,67,0.08), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.4);
  background: linear-gradient(180deg, #0F0F0F 0%, #0A0A0A 100%);
  width: clamp(300px, 70vw, 600px); flex: 1; max-height: 60vh;
  display: flex; align-items: center; justify-content: center;
  position: relative;
  transition: all 0.5s ease;
}

/* DS sec1.3 — frame free spins */
.slots-frame--fs {
  border-color: #FFD700;
  outline-color: rgba(255,215,0,0.25);
  box-shadow: 0 0 40px rgba(255,215,0,0.12), 0 8px 32px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.3);
  animation: slotsFrameFSGlow 3s ease infinite;
}
```

---

# 5. GRID 6x5

```css
/* DS sec1.4 — grid CSS Grid */
.slots-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  grid-template-rows: repeat(5, 1fr);
  gap: clamp(3px, 0.4vw, 6px);
  width: 100%; aspect-ratio: 6 / 5;
}
```

---

# 6. CELULA SIMBOLO (5 estados)

```css
/* DS sec1.5 — celula normal */
.slots-cell {
  aspect-ratio: 1; border-radius: 8px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.04);
  display: flex; align-items: center; justify-content: center;
  font-size: clamp(18px, 2.5vw, 32px);
  transition: all 0.3s ease;
  position: relative;
}

/* hover */
.slots-cell:hover {
  background: rgba(255,255,255,0.06);
  border-color: rgba(255,255,255,0.08);
}

/* winning — glow por tipo */
.slots-cell--winning {
  opacity: 1; transform: scale(1.05);
  animation: slotsWinGlow 1s ease infinite;
}

.slots-cell--winning-crown     { --glow-color: rgba(255,215,0,0.6); border-color: rgba(255,215,0,0.6); }
.slots-cell--winning-ring      { --glow-color: rgba(212,168,67,0.5); border-color: rgba(212,168,67,0.5); }
.slots-cell--winning-hourglass { --glow-color: rgba(203,155,81,0.5); border-color: rgba(203,155,81,0.5); }
.slots-cell--winning-chalice   { --glow-color: rgba(139,105,20,0.5); border-color: rgba(139,105,20,0.5); }
.slots-cell--winning-ruby      { --glow-color: rgba(255,23,68,0.4); border-color: rgba(255,23,68,0.4); }
.slots-cell--winning-sapphire  { --glow-color: rgba(30,136,229,0.4); border-color: rgba(30,136,229,0.4); }
.slots-cell--winning-emerald   { --glow-color: rgba(67,160,71,0.4); border-color: rgba(67,160,71,0.4); }
.slots-cell--winning-amethyst  { --glow-color: rgba(142,36,170,0.4); border-color: rgba(142,36,170,0.4); }
.slots-cell--winning-topaz     { --glow-color: rgba(255,143,0,0.4); border-color: rgba(255,143,0,0.4); }

/* dimmed (durante highlight dos vencedores) */
.slots-cell--dimmed {
  opacity: 0.3; transition: opacity 0.3s ease;
}

/* scatter */
.slots-cell--scatter {
  background: radial-gradient(circle, rgba(255,215,0,0.15) 0%, transparent 70%);
  border: 2px solid rgba(255,215,0,0.4);
  animation: slotsScatterPulse 2s ease infinite;
}
```

---

# 7. ORBE MULTIPLICADOR (FS only)

```css
/* DS sec1.8 — aparece no grid durante free spins */
.slots-orbe {
  background: radial-gradient(circle, #FFD700 0%, #D4A843 60%, #8B6914 100%);
  border-radius: 8px;
  font-family: 'JetBrains Mono', monospace; font-weight: 900;
  font-size: clamp(12px, 1.8vw, 20px); color: #0A0A0A;
  animation: slotsOrbeSpin 2s ease infinite;
}
```

---

# 8. BET CONTROLS BAR

```css
/* DS sec1.9 — barra inferior */
.slots-bet-bar {
  display: flex; align-items: center; justify-content: center;
  gap: clamp(4px, 0.5vw, 8px);
  padding: clamp(6px, 0.8vw, 12px) clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.6);
  border-top: 1px solid rgba(212,168,67,0.15);
  backdrop-filter: blur(4px);
  flex-shrink: 0; z-index: 5;
  transition: opacity 0.3s ease;
}

/* locked durante spin */
.slots-bet-bar--locked {
  opacity: 0.4; pointer-events: none;
}
```

---

# 9. BOTAO BET (MIN/x2/div2/MAX)

```css
/* DS sec1.9 — botoes de ajuste de aposta */
.slots-bet-btn {
  min-width: 44px; min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px; color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace; font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer; transition: all 0.2s ease;
}

.slots-bet-btn:hover {
  background: rgba(255,255,255,0.10);
  border-color: rgba(255,255,255,0.2);
  color: #FFFFFF;
}

.slots-bet-btn:active {
  background: rgba(255,255,255,0.15);
  transform: scale(0.95);
}

.slots-bet-btn:disabled {
  opacity: 0.3; cursor: not-allowed;
  pointer-events: none;
}
```

---

# 10. SPIN BUTTON (4 estados)

```css
/* DS sec1.10 — botao SPIN normal (verde) */
.slots-spin {
  width: 56px; height: 56px; border-radius: 50%;
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 2px solid rgba(0,230,118,0.4);
  color: #FFFFFF; font-size: 20px; font-weight: 900;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer; flex-shrink: 0;
  box-shadow: 0 0 15px rgba(0,230,118,0.3);
  animation: slotsSpinPulse 2s ease infinite;
  transition: all 0.2s ease;
}

.slots-spin:hover {
  transform: scale(1.08);
  box-shadow: 0 0 25px rgba(0,230,118,0.5);
}

.slots-spin:active {
  transform: scale(0.92);
}

.slots-spin:disabled {
  opacity: 0.4; cursor: not-allowed;
  animation: none;
}

/* DS sec1.10 — SPIN durante Free Spins (dourado) */
.slots-spin--fs {
  background: linear-gradient(180deg, #FFD700 0%, #8B6914 100%);
  border-color: rgba(255,215,0,0.6);
  color: #0A0A0A;
  box-shadow: 0 0 20px rgba(255,215,0,0.4);
  animation: slotsSpinFSPulse 2s ease infinite;
}

.slots-spin--fs:hover {
  box-shadow: 0 0 35px rgba(255,215,0,0.6);
}
```

---

# 11. ANTE TOGGLE (2 estados)

```css
/* DS sec1.9 — ANTE desativado */
.slots-ante {
  min-width: 44px; min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(255,255,255,0.05);
  border-radius: 6px; color: #666666;
  font-family: 'Cinzel', serif; font-weight: 700;
  font-size: clamp(9px, 1.1vw, 12px);
  text-transform: uppercase; letter-spacing: 1px;
  cursor: pointer; transition: all 0.2s ease;
}

.slots-ante:hover {
  border-color: rgba(212,168,67,0.3);
  color: #A8A8A8;
}

/* DS sec1.9 — ANTE ativado */
.slots-ante--active {
  background: rgba(212,168,67,0.15);
  border-color: rgba(212,168,67,0.3);
  color: #D4A843;
}

.slots-ante--active:hover {
  background: rgba(212,168,67,0.20);
}
```

---

# 12. AUTO + TURBO TOGGLES

```css
/* DS sec1.11 — AUTO desativado */
.slots-auto {
  min-width: 44px; min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px; color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace; font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer; transition: all 0.2s ease;
  position: relative;
}

/* AUTO ativado */
.slots-auto--active {
  background: rgba(0,230,118,0.1);
  border-color: rgba(0,230,118,0.3);
  color: #00E676;
}

/* AUTO badge contagem */
.slots-auto__badge {
  position: absolute; top: -6px; right: -6px;
  padding: 1px 5px; border-radius: 4px;
  background: rgba(0,230,118,0.15);
  border: 1px solid rgba(0,230,118,0.3);
  font-size: 8px; color: #00E676;
  font-family: 'JetBrains Mono', monospace; font-weight: 600;
  animation: slotsAutoPulse 1.5s ease infinite;
}

/* DS sec1.12 — TURBO desativado */
.slots-turbo {
  min-width: 44px; min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px; color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace; font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer; transition: all 0.2s ease;
}

/* TURBO ativado */
.slots-turbo--active {
  background: rgba(255,215,0,0.1);
  border-color: rgba(255,215,0,0.3);
  color: #FFD700;
}
```

---

# 13. WIN DISPLAY

```css
/* DS sec1.13 — display de win inline */
.slots-win-display {
  text-align: center;
  padding: clamp(4px, 0.5vw, 6px);
  font-family: 'JetBrains Mono', monospace; font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  min-height: clamp(20px, 2.5vw, 28px);
  transition: all 0.3s ease;
}

/* sem win */
.slots-win-display--idle {
  color: #666666; text-shadow: none;
}

/* com win */
.slots-win-display--active {
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  animation: slotsWinPulse 1.5s ease 2;
}
```

---

**[FIM PARTE 1 — Continua Parte 2: Componentes 14-25 + Responsive + Checklist Final]**

`[ENTREGA: 7.CSS-COMPONENTES-SLOT-MACHINE-PARTE-1.md — 19/03/2026]`
