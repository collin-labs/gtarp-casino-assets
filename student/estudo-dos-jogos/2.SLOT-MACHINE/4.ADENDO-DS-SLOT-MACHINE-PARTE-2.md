# ADENDO DESIGN SYSTEM — SLOT-MACHINE (#2)
## PARTE 2 DE 4 — Componentes CSS Real (1.10-1.18)
### Blackout Casino GTARP - 18/03/2026

---

## 1.10 Botao SPIN — 4 Estados (DS P2 §8.1)

```css
/* COPIAR EXATAMENTE — botao circular principal CTA */
.slots-spin {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 2px solid rgba(0,230,118,0.4);
  color: #FFFFFF;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-shadow: 0 0 15px rgba(0,230,118,0.3);
  transition: all 0.25s ease;
  position: relative;
  flex-shrink: 0;
}

/* COPIAR — estado IDLE (pulse sutil continuo) */
.slots-spin--idle {
  animation: spinPulse 2s ease-in-out infinite;
}
@keyframes spinPulse {
  0%, 100% { box-shadow: 0 0 15px rgba(0,230,118,0.2); }
  50% { box-shadow: 0 0 25px rgba(0,230,118,0.4); }
}

/* COPIAR — hover */
.slots-spin:hover {
  transform: scale(1.08);
  box-shadow: 0 0 30px rgba(0,230,118,0.5);
}

/* COPIAR — active (tap/click) */
.slots-spin:active {
  transform: scale(0.92);
  box-shadow: 0 0 10px rgba(0,230,118,0.3);
}

/* COPIAR — estado SPINNING (icone rotaciona) */
.slots-spin--spinning {
  animation: none;
}
.slots-spin--spinning .slots-spin__icon {
  animation: spinRotate 0.8s linear infinite;
}
@keyframes spinRotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* COPIAR — estado DISABLED (durante tumble/avaliacao/saldo insuficiente) */
.slots-spin--disabled {
  opacity: 0.4;
  pointer-events: none;
  animation: none;
  cursor: not-allowed;
}

/* COPIAR — estado FREE SPINS (dourado em vez de verde) */
.slots-spin--free {
  background: linear-gradient(180deg, #FFD700 0%, #8B6914 100%);
  border: 2px solid rgba(255,215,0,0.6);
  color: #0A0A0A;
  box-shadow: 0 0 20px rgba(255,215,0,0.4);
}
.slots-spin--free.slots-spin--idle {
  animation: fsSpinPulse 2s ease-in-out infinite;
}
@keyframes fsSpinPulse {
  0%, 100% { box-shadow: 0 0 15px rgba(255,215,0,0.3); }
  50% { box-shadow: 0 0 25px rgba(255,215,0,0.5); }
}
```

---

## 1.11 Botao AUTO (DS P2 §8 adaptado)

```css
/* COPIAR EXATAMENTE — toggle de autoplay */
.slots-bet__auto {
  min-width: 44px;
  min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer;
  transition: all 0.2s ease;
}
.slots-bet__auto:hover {
  background: rgba(255,255,255,0.1);
  border-color: rgba(0,230,118,0.2);
  color: #00E676;
}

/* COPIAR — AUTO ativo */
.slots-bet__auto--active {
  background: rgba(0,230,118,0.1);
  border-color: rgba(0,230,118,0.3);
  color: #00E676;
}

/* COPIAR — badge de contagem "AUTO: 7/25" */
.slots-bet__auto-badge {
  position: absolute;
  top: -6px; right: -6px;
  padding: 1px 5px;
  border-radius: 4px;
  background: rgba(0,230,118,0.15);
  border: 1px solid rgba(0,230,118,0.3);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: 8px;
  color: #00E676;
}
```

---

## 1.12 Botao TURBO (DS P2 §8 adaptado)

```css
/* COPIAR EXATAMENTE — toggle de velocidade rapida */
.slots-bet__turbo {
  min-width: 44px;
  min-height: 44px;
  padding: clamp(4px, 0.5vw, 6px) clamp(6px, 0.8vw, 10px);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px;
  color: #A8A8A8;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 12px);
  cursor: pointer;
  transition: all 0.2s ease;
}
.slots-bet__turbo:hover {
  background: rgba(255,255,255,0.1);
  border-color: rgba(255,215,0,0.2);
  color: #FFD700;
}

/* COPIAR — TURBO ativo */
.slots-bet__turbo--active {
  background: rgba(255,215,0,0.1);
  border-color: rgba(255,215,0,0.3);
  color: #FFD700;
}
```

---

## 1.13 WinDisplay — Resultado Inline (DS P2 §15.2 adaptado)

```css
/* COPIAR EXATAMENTE — display de win entre grid e bet controls */
.slots-game__win-display {
  text-align: center;
  padding: clamp(4px, 0.5vw, 6px);
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(12px, 1.6vw, 18px);
  min-height: clamp(20px, 2.5vw, 28px);
  transition: all 0.3s ease;
}

/* COPIAR — estado idle (sem win) */
.slots-game__win-display--idle {
  color: #666666;
}

/* COPIAR — estado com win (countup ativo) */
.slots-game__win-display--winning {
  color: #00E676;
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
}

/* COPIAR — estado big win (>5x, dourado) */
.slots-game__win-display--big {
  color: #FFD700;
  text-shadow: 0 0 12px rgba(255,215,0,0.5);
  font-size: clamp(14px, 2vw, 22px);
}

/* Framer Motion — valor aparece com scale bump */
/*
  key={winAmount}
  initial={{ scale: 1.3, color: '#FFFFFF' }}
  animate={{ scale: 1, color: '#00E676' }}
  transition={{ type: 'spring', stiffness: 300, damping: 20 }}
*/
```

---

## 1.14 Badge TUMBLE (DS custom)

```css
/* COPIAR EXATAMENTE — contador de cascatas no canto do frame */
.slots-hud__tumble-badge {
  position: absolute;
  top: clamp(4px, 0.5vw, 8px);
  right: clamp(4px, 0.5vw, 8px);
  background: rgba(0,230,118,0.1);
  border: 1px solid rgba(0,230,118,0.3);
  border-radius: 6px;
  padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 10px);
  font-family: 'Cinzel', serif;
  font-weight: 600;
  font-size: clamp(9px, 1.1vw, 13px);
  color: #00E676;
  text-transform: uppercase;
  letter-spacing: 1px;
  z-index: 10;
}

/* Framer Motion — bump a cada incremento */
/*
  key={tumbleCount}
  initial={{ scale: 1.3 }}
  animate={{ scale: 1 }}
  transition={{ type: 'spring', stiffness: 400, damping: 15 }}
*/
```

---

## 1.15 Free Spins HUD — Counter + Multiplier (DS custom)

```css
/* COPIAR EXATAMENTE — barra HUD durante Free Spins (substitui parte do header) */
.slots-hud__fs-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: clamp(6px, 0.8vw, 10px) clamp(8px, 1.5vw, 16px);
  background: rgba(0,0,0,0.7);
  border-bottom: 1px solid rgba(255,215,0,0.2);
  z-index: 10;
}

/* COPIAR — counter "FREE SPINS: 7/10" */
.slots-hud__fs-counter {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 15px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
  text-transform: uppercase;
  letter-spacing: 1px;
  animation: fsPulse 2s ease-in-out infinite;
}
@keyframes fsPulse {
  0%, 100% { text-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50% { text-shadow: 0 0 16px rgba(0,230,118,0.6); }
}

/* COPIAR — total multiplier "TOTAL x12" */
.slots-hud__fs-multiplier {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 900;
  font-size: clamp(12px, 1.6vw, 18px);
  color: #FFD700;
  text-shadow: 0 0 10px rgba(255,215,0,0.5);
}

/* Framer Motion — multiplier update com scale bump */
/*
  key={totalMultiplier}
  initial={{ scale: 1.4, color: '#FFFFFF' }}
  animate={{ scale: 1, color: '#FFD700' }}
  transition={{ type: 'spring', stiffness: 300, damping: 15 }}
*/

/* COPIAR — win acumulado durante FS */
.slots-hud__fs-win {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(11px, 1.4vw, 16px);
  color: #00E676;
  text-shadow: 0 0 8px rgba(0,230,118,0.4);
}
```

---

## 1.16 Win Overlay — 4 Tiers Progressivos (DS P1 §7 glassmorphism)

```css
/* COPIAR EXATAMENTE — overlay base compartilhado pelos 4 tiers */
.slots-overlay {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
  z-index: 80;
}

/* COPIAR — TIER 1: Normal (<5x) — SEM overlay, apenas WinDisplay inline */
/* Nao usa .slots-overlay — resultado aparece no .slots-game__win-display--winning */

/* COPIAR — TIER 2: Big Win (5-50x) */
.slots-overlay--big {
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(4px);
}
.slots-overlay__title--big {
  font-family: 'Cinzel', serif;
  font-weight: 800;
  font-size: clamp(18px, 3vw, 32px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 3px;
  text-shadow: 0 0 15px rgba(255,215,0,0.5), 0 2px 6px rgba(0,0,0,1);
}

/* COPIAR — TIER 3: Mega Win (50-500x) */
.slots-overlay--mega {
  background: rgba(0,0,0,0.75);
  backdrop-filter: blur(8px);
  z-index: 90;
}
.slots-overlay__title--mega {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(22px, 4vw, 40px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 4px;
  text-shadow:
    0 0 25px rgba(255,215,0,0.6),
    0 0 60px rgba(255,215,0,0.3),
    0 2px 8px rgba(0,0,0,1);
}

/* COPIAR — TIER 4: Jackpot (500x+) */
.slots-overlay--jackpot {
  background: rgba(0,0,0,0.85);
  backdrop-filter: blur(12px);
  z-index: 100;
}
.slots-overlay__title--jackpot {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(26px, 5vw, 48px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 5px;
  text-shadow:
    0 0 30px rgba(255,215,0,0.8),
    0 0 80px rgba(255,215,0,0.4),
    0 0 120px rgba(255,215,0,0.2);
}

/* COPIAR — valor do win (countup) */
.slots-overlay__amount {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(18px, 3.5vw, 36px);
  color: #00E676;
  text-shadow: 0 0 15px rgba(0,230,118,0.6);
}

/* COPIAR — botao CONTINUAR */
.slots-overlay__btn-continue {
  margin-top: clamp(8px, 1.5vw, 16px);
  padding: clamp(8px, 1vw, 12px) clamp(20px, 3vw, 36px);
  background: linear-gradient(180deg, #00C853 0%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.3);
  border-radius: 8px;
  color: #FFFFFF;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(10px, 1.3vw, 14px);
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  min-height: 44px;
  box-shadow: 0 4px 16px rgba(0,200,83,0.2);
  transition: all 0.25s ease;
}
.slots-overlay__btn-continue:hover {
  box-shadow: 0 6px 24px rgba(0,200,83,0.3);
  transform: translateY(-1px);
}
.slots-overlay__btn-continue:active {
  transform: translateY(1px);
}

/* Framer Motion — entrada overlay Big Win */
/*
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  exit={{ opacity: 0 }}
  titulo: initial={{ scale: 0.5, opacity: 0 }}
         animate={{ scale: [0.5, 1.2, 1.0], opacity: 1 }}
         transition={{ duration: 0.6, times: [0, 0.6, 1] }}
*/

/* Framer Motion — entrada overlay Mega Win */
/*
  titulo: initial={{ scale: 0, rotate: -15 }}
         animate={{ scale: 1, rotate: 0 }}
         transition={{ type: 'spring', stiffness: 150, damping: 10, delay: 0.2 }}
*/

/* Framer Motion — entrada overlay Jackpot */
/*
  container: animate={{ x: [0, -3, 3, -2, 2, -1, 1, 0] }}
             transition={{ duration: 0.5, delay: 0.3 }}
  titulo: initial={{ scale: 0, rotate: -20 }}
         animate={{ scale: [0, 1.3, 1.0], rotate: [0, 5, 0] }}
         transition={{ duration: 0.8, times: [0, 0.5, 1] }}
*/
```

---

## 1.17 Gamble / Double Up (DS custom)

```css
/* COPIAR EXATAMENTE — botao DOBRAR que aparece apos qualquer win por 5s */
.slots-game__gamble-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: clamp(4px, 0.5vw, 6px) clamp(8px, 1vw, 14px);
  background: rgba(255,215,0,0.1);
  border: 1px solid rgba(255,215,0,0.3);
  border-radius: 6px;
  color: #FFD700;
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(9px, 1.1vw, 12px);
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  min-height: 44px;
  animation: gamblePulse 1.5s ease-in-out infinite;
  transition: all 0.2s ease;
}
@keyframes gamblePulse {
  0%, 100% { border-color: rgba(255,215,0,0.3); }
  50% { border-color: rgba(255,215,0,0.6); }
}
.slots-game__gamble-btn:hover {
  background: rgba(255,215,0,0.15);
  border-color: rgba(255,215,0,0.5);
}

/* COPIAR — overlay do coin flip 50/50 */
.slots-gamble-overlay {
  position: absolute;
  inset: 0;
  z-index: 85;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 20px);
}

/* COPIAR — moeda do coin flip */
.slots-gamble__coin {
  width: clamp(80px, 12vw, 120px);
  height: clamp(80px, 12vw, 120px);
  border-radius: 50%;
  background: linear-gradient(135deg, #FFD700 0%, #D4A843 50%, #8B6914 100%);
  border: 3px solid #F6E27A;
  box-shadow: 0 4px 20px rgba(255,215,0,0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(24px, 4vw, 40px);
  color: #0A0A0A;
}

/* COPIAR — moeda girando */
.slots-gamble__coin--flipping {
  animation: coinFlip 1.5s ease-in-out;
}
@keyframes coinFlip {
  0% { transform: rotateY(0deg); }
  100% { transform: rotateY(1800deg); }
}

/* COPIAR — resultado WIN (verde) */
.slots-gamble__result--win {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 24px);
  color: #00E676;
  text-shadow: 0 0 12px rgba(0,230,118,0.5);
  text-transform: uppercase;
}

/* COPIAR — resultado LOSE (vermelho) */
.slots-gamble__result--lose {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(16px, 2.5vw, 24px);
  color: #FF3B3B;
  text-shadow: 0 0 12px rgba(255,59,59,0.5);
  text-transform: uppercase;
}
```

---

## 1.18 Free Spins Trigger Overlay (DS P1 §7 + P2 §12)

```css
/* COPIAR EXATAMENTE — overlay dramatico quando 4+ scatters trigam FS */
.slots-overlay--fs-trigger {
  position: absolute;
  inset: 0;
  z-index: 100;
  background: rgba(0,0,0,0.8);
  backdrop-filter: blur(8px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(12px, 2vw, 24px);
}

/* COPIAR — titulo "FREE SPINS!" */
.slots-overlay__fs-title {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  font-size: clamp(24px, 5vw, 48px);
  color: #FFD700;
  text-transform: uppercase;
  letter-spacing: 4px;
  text-shadow:
    0 0 25px rgba(255,215,0,0.6),
    0 0 60px rgba(255,215,0,0.3);
}

/* COPIAR — subtitulo "10 RODADAS" */
.slots-overlay__fs-count {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: clamp(16px, 3vw, 28px);
  color: #FFFFFF;
}

/* COPIAR — overlay de encerramento das FS */
.slots-overlay--fs-end {
  position: absolute;
  inset: 0;
  z-index: 100;
  background: rgba(0,0,0,0.8);
  backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: clamp(8px, 1.5vw, 16px);
}
.slots-overlay__fs-end-title {
  font-family: 'Cinzel', serif;
  font-weight: 700;
  font-size: clamp(14px, 2vw, 22px);
  color: #D4A843;
  text-transform: uppercase;
  letter-spacing: 2px;
  text-shadow: 0 0 10px rgba(212,168,67,0.4);
}

/* Framer Motion — trigger overlay */
/*
  container: initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
  titulo: initial={{ scale: 0, rotate: -10 }}
         animate={{ scale: 1, rotate: 0 }}
         transition={{ type: 'spring', stiffness: 200, damping: 12, delay: 0.2 }}
  count: initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.6, duration: 0.4 }}
*/
```

---

**[FIM PARTE 2 — Continua Parte 3: Componentes 1.19-1.25 (Modais, Classic Reel, ModeSelect, Tooltip, Sparkle, Near-Miss, Buy Bonus)]**

`[ENTREGA: 4.ADENDO-DS-SLOT-MACHINE-PARTE-2.md — 18/03/2026]`
