# CSS COMPONENTES — DAILY-FREE (#19)
## Parte 1 de 2 - Tokens :root + @keyframes (8) + Componentes 1-10
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com o PROMPT-V0-DAILY-FREE
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 24/03/2026

---

# COMO USAR

Ao gerar qualquer tela do Daily-Free no V0, colar ESTE arquivo junto com o PROMPT-V0.
Este CSS tem **precedencia total** sobre descricoes vagas do Prompt.
Se o Prompt diz "borda dourada", usar `.daily-free-game { border: 1px solid #D4A843; }` daqui.

---

# 0. TOKENS CSS (:root — primitivos e semanticos do Daily-Free)

```css
/* ====================================================
   DAILY-FREE — DESIGN TOKENS
   Separacao: primitivos (raw) -> semanticos (uso)
   Ref: Adendo DS sec1, Prompt AI sec Header Tecnico
   ==================================================== */

:root {
  /* --- Primitivos — cores base --- */
  --df-gold-primary:    #D4A843;
  --df-gold-light:      #F6E27A;
  --df-gold-dark:       #8B6914;
  --df-gold-deeper:     #462523;
  --df-gold-pure:       #FFD700;
  --df-green-neon:      #00E676;
  --df-green-dark:      #004D25;
  --df-green-medium:    #00C853;
  --df-black-bg:        #0A0A0A;
  --df-black-surface:   #0F0F0F;
  --df-black-card:      #141414;
  --df-black-hover:     #1A1A1A;
  --df-black-sub:       #222222;
  --df-black-tooltip:   #2A2A2A;
  --df-red-soft:        #FF6B6B;
  --df-red-miss:        rgba(235,75,75,0.15);
  --df-white:           #FFFFFF;
  --df-text-primary:    rgba(255,255,255,0.85);
  --df-text-secondary:  rgba(255,255,255,0.5);
  --df-text-muted:      rgba(255,255,255,0.3);

  /* --- Semanticos — milestone tiers --- */
  --df-tier-7:          #4B69FF;
  --df-tier-14:         #8847FF;
  --df-tier-30:         #FFD700;
  --df-tier-7-bg:       rgba(75,105,255,0.15);
  --df-tier-14-bg:      rgba(136,71,255,0.15);
  --df-tier-30-bg:      rgba(255,215,0,0.15);
  --df-tier-7-glow:     rgba(75,105,255,0.3);
  --df-tier-14-glow:    rgba(136,71,255,0.3);
  --df-tier-30-glow:    rgba(255,215,0,0.3);

  /* --- Semanticos — bordas --- */
  --df-border-gold:         1px solid rgba(212,168,67,0.15);
  --df-border-gold-hover:   1px solid rgba(212,168,67,0.3);
  --df-border-gold-active:  1px solid rgba(212,168,67,0.5);
  --df-border-subtle:       1px solid rgba(212,168,67,0.08);
  --df-border-green:        1.5px solid #00E676;
  --df-border-claimed:      1px solid #D4A843;

  /* --- Semanticos — shadows --- */
  --df-shadow-vignette:     inset 0 0 150px rgba(0,0,0,0.8);
  --df-shadow-card:         0 4px 16px rgba(0,0,0,0.3);
  --df-shadow-card-hover:   0 8px 30px rgba(0,0,0,0.4);
  --df-shadow-overlay:      0 0 30px rgba(0,0,0,0.5), 0 0 60px rgba(0,0,0,0.3);
  --df-shadow-modal:        0 0 40px rgba(0,0,0,0.6), 0 0 80px rgba(0,0,0,0.3);
  --df-shadow-glow-green:   0 0 8px rgba(0,230,118,0.3);
  --df-shadow-glow-gold:    0 0 12px rgba(212,168,67,0.3);

  /* --- Semanticos — tipografia --- */
  --df-font-title:    'Cinzel Decorative', 'Cinzel', serif;
  --df-font-body:     'Inter', sans-serif;
  --df-font-mono:     'JetBrains Mono', monospace;
}
```

---

# 0B. @KEYFRAMES (8 animacoes nomeadas)

```css
/* 1 — Idle sway da roda (oscilacao -2/+2 graus) */
@keyframes dfIdleSway {
  0%, 100% { transform: rotate(-2deg); }
  50% { transform: rotate(2deg); }
}

/* 2 — Breathe do dia disponivel (scale pulsante) */
@keyframes dfDayBreathe {
  0%, 100% { transform: scale(1); box-shadow: 0 0 8px rgba(0,230,118,0.3); }
  50% { transform: scale(1.04); box-shadow: 0 0 16px rgba(0,230,118,0.5); }
}

/* 3 — Pulse do botao GIRAR (glow verde) */
@keyframes dfSpinPulse {
  0%, 100% { box-shadow: 0 0 15px rgba(0,230,118,0.3), 0 4px 12px rgba(0,0,0,0.5); }
  50% { box-shadow: 0 0 30px rgba(0,230,118,0.5), 0 0 60px rgba(0,230,118,0.15), 0 4px 12px rgba(0,0,0,0.5); }
}

/* 4 — Bounce do ponteiro ao parar */
@keyframes dfPointerBounce {
  0% { transform: translateX(-50%) translateY(0); }
  30% { transform: translateX(-50%) translateY(-4px); }
  60% { transform: translateX(-50%) translateY(2px); }
  100% { transform: translateX(-50%) translateY(0); }
}

/* 5 — Pulse do timer urgente (<1h) */
@keyframes dfTimerPulse {
  0%, 100% { opacity: 0.8; }
  50% { opacity: 1; text-shadow: 0 0 8px rgba(255,107,107,0.5); }
}

/* 6 — Streak pulse (>7 dias) */
@keyframes dfStreakPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

/* 7 — Confetti fall (particula caindo) */
@keyframes dfConfettiFall {
  0% { transform: translate(0, -20px) rotate(0deg); opacity: 1; }
  100% { transform: translate(var(--df-x-drift, 0px), 120vh) rotate(var(--df-rotation, 720deg)); opacity: 0; }
}

/* 8 — Burst radial (particula milestone) */
@keyframes dfBurstRadial {
  0% { transform: translate(0, 0) scale(1); opacity: 1; }
  100% { transform: translate(var(--df-burst-x, 0px), var(--df-burst-y, 0px)) scale(0); opacity: 0; }
}
```

---

# COMPONENTES CSS (15 numerados)

## 1. Fundo do Jogo (DS P1 §4.1 — NUNCA flat)

```css
/* COPIAR EXATAMENTE */
.daily-free-game {
  background-color: var(--df-black-bg);
  background-image:
    url('/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png'),
    radial-gradient(ellipse 80% 50% at 50% 0%, rgba(212,168,67,0.03) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,0,0,0.5) 0%, transparent 70%);
  background-size: cover, 100% 100%, 100% 100%;
  box-shadow: var(--df-shadow-vignette);
  position: absolute; inset: 0; z-index: 60;
  border-radius: inherit; overflow: hidden;
  display: flex; flex-direction: column;
  padding: clamp(8px, 1.5vw, 16px);
}
```

## 2. Container com 5 Shadows (DS P1 §5 — profundidade)

```css
/* COPIAR — overlay resultado card */
.daily-free-overlay__card {
  background: rgba(20,20,20,0.8);
  border: 1.5px solid rgba(212,168,67,0.3);
  border-radius: 18px;
  padding: clamp(24px, 4vw, 48px) clamp(32px, 5vw, 64px);
  box-shadow:
    0 0 30px rgba(0,0,0,0.5),       /* ambient shadow */
    0 0 60px rgba(0,0,0,0.3),       /* distant shadow */
    inset 0 1px 0 rgba(255,215,0,0.08), /* inner light top */
    0 4px 16px rgba(0,0,0,0.4),     /* contact shadow */
    0 0 100px rgba(212,168,67,0.02); /* gold haze */
  display: flex; flex-direction: column;
  align-items: center; gap: clamp(12px, 2vw, 24px);
  max-width: clamp(280px, 40vw, 420px);
}
```

## 3. Card Calendario Dia com Hover (DS P1 §4.4)

```css
/* COPIAR — dia futuro (base) */
.daily-free-calendar__day--future {
  background: var(--df-black-hover);
  border: var(--df-border-subtle);
  color: var(--df-text-muted);
  font-weight: 500; cursor: default;
  border-radius: 6px;
  aspect-ratio: 1;
  display: flex; align-items: center; justify-content: center;
  font-family: var(--df-font-body);
  font-size: clamp(9px, 1.1vw, 13px);
  transition: border-color 0.2s;
}
.daily-free-calendar__day--future:hover {
  border-color: rgba(212,168,67,0.15);
}
```

## 4. Feixo Verde (DS P3A §20 — dia disponivel)

```css
/* COPIAR — dia disponivel com glow verde */
.daily-free-calendar__day--available {
  background: var(--df-black-hover);
  border: var(--df-border-green);
  box-shadow: var(--df-shadow-glow-green), 0 0 16px rgba(0,230,118,0.1);
  color: var(--df-white); font-weight: 700;
  animation: dfDayBreathe 3s ease-in-out infinite;
}
```

## 5. Linha Dourada (DS P1 §3 — borda metalica)

```css
/* COPIAR — moldura da roda (Canvas radialGradient) */
/* Anel externo no offscreen Canvas:
   ctx.strokeStyle = radialGradient(#8B6914 → #D4A843 → #F6E27A → #D4A843 → #8B6914)
   ctx.lineWidth = 12
   ctx.arc(cx, cy, radius+2, 0, 2*Math.PI)
   ctx.stroke() */

/* Linha dourada em separadores DOM: */
.daily-free-separator {
  width: 100%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(212,168,67,0.3), transparent);
}
```

## 6. Shine Sweep (DS P1 §4.4 — reflexo passando)

```css
/* COPIAR — shimmer sweep no botao GIRAR (::after) */
.daily-free-button--spin::after {
  content: '';
  position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.06), transparent);
  animation: dfShimmer 4s ease-in-out infinite;
  pointer-events: none;
}
@keyframes dfShimmer {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}
```

## 7. Botao Primario com Bevel (DS P2 §8.1)

```css
/* COPIAR EXATAMENTE — botao GIRAR */
.daily-free-button--spin {
  width: clamp(160px, 20vw, 240px); height: clamp(44px, 5.5vw, 56px);
  margin: clamp(8px, 1vw, 14px) auto 0;
  background: linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%);
  border: 1.5px solid rgba(0,230,118,0.4); border-radius: 12px;
  font-family: var(--df-font-title); font-weight: 800;
  font-size: clamp(14px, 1.8vw, 20px); color: var(--df-white);
  text-transform: uppercase; letter-spacing: 3px;
  text-shadow: 0 1px 3px rgba(0,0,0,0.5);
  cursor: pointer; position: relative; overflow: hidden;
  animation: dfSpinPulse 2s ease-in-out infinite;
}
/* Bevel — reflexo de luz superior */
.daily-free-button--spin::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 50%;
  background: linear-gradient(to bottom, rgba(255,255,255,0.08), transparent);
  border-radius: 12px 12px 0 0; pointer-events: none;
}
.daily-free-button--spin:hover { transform: scale(1.05); border-color: #00E676; }
.daily-free-button--spin:active { transform: scale(0.95); background: linear-gradient(180deg, #00C853, #004D25, #002200); }
```

## 8. Botao Dourado (DS P2 §8.2 — VERIFICAR)

```css
/* COPIAR — botao dourado do modal PF */
.daily-free-button--verify {
  width: 100%; height: clamp(40px, 5vw, 48px);
  margin-top: clamp(12px, 1.5vw, 20px);
  background: linear-gradient(180deg, #D4A843 0%, #8B6914 100%);
  border: 1px solid rgba(255,215,0,0.3); border-radius: 10px;
  font-family: var(--df-font-title); font-weight: 700;
  font-size: clamp(11px, 1.3vw, 15px); color: var(--df-black-bg);
  text-transform: uppercase; letter-spacing: 2px; cursor: pointer;
}
.daily-free-button--verify:hover { background: linear-gradient(180deg, #FFD700, #D4A843); }
.daily-free-button--verify:active { transform: scale(0.95); }
```

## 9. Botao Voltar (DS P2 §8.3 — ghost)

```css
/* COPIAR — botao VOLTAR no header */
.daily-free-button--back {
  background: rgba(0,0,0,0.6);
  border: 1px solid rgba(212,168,67,0.4); border-radius: 8px;
  color: var(--df-gold-primary);
  font-family: var(--df-font-title); font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px); letter-spacing: 2px;
  padding: clamp(5px, 0.6vw, 8px) clamp(8px, 1.2vw, 14px);
  min-height: 44px; cursor: pointer;
  transition: border-color 0.2s, background 0.2s;
}
.daily-free-button--back:hover { border-color: rgba(212,168,67,0.8); background: rgba(0,0,0,0.8); }
.daily-free-button--back:active { transform: scale(0.95); }
```

## 10. Glassmorphism (DS P1 §4.3 — overlays e modais)

```css
/* COPIAR — backdrop overlay resultado */
.daily-free-overlay--result {
  position: absolute; inset: 0; z-index: 70;
  background: rgba(10,10,10,0.7);
  backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
  display: flex; align-items: center; justify-content: center;
}

/* COPIAR — backdrop overlay milestone (mais escuro) */
.daily-free-overlay--milestone {
  position: absolute; inset: 0; z-index: 80;
  background: rgba(5,5,5,0.85);
  backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: clamp(16px, 2.5vw, 32px);
}

/* COPIAR — backdrop modal (PF, Historico) */
.daily-free-modal__backdrop {
  position: fixed; inset: 0; z-index: 85;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
  display: flex; align-items: center; justify-content: center;
}

/* COPIAR — card do modal */
.daily-free-modal__card {
  background: rgba(15,15,15,0.95);
  border: 1.5px solid rgba(212,168,67,0.25); border-radius: 16px;
  padding: clamp(20px, 3vw, 36px);
  box-shadow: var(--df-shadow-modal), inset 0 1px 0 rgba(255,215,0,0.06);
  max-height: 80vh; overflow-y: auto;
}
.daily-free-modal__card--pf { width: clamp(300px, 45vw, 480px); }
.daily-free-modal__card--history { width: clamp(340px, 55vw, 560px); }
```

---

`[FIM DA PARTE 1 — Continua Parte 2: Componentes 11-15 + Glow + Textos + Input + Imagens + PROIBIDO]`
