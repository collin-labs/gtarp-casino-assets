# ADENDO -- ROTEIRO DE DESENVOLVIMENTO PLINKO (#8)
# PARTE 7 -- BigWinOverlay TSX real (item 5.14)
# Complementa: 5.ROTEIRO P1-P6 = 4.156L
# Blackout Casino GTARP - 26/03/2026

---

# COMPONENTE: PlinkoBigWinOverlay.tsx (Fase 4 -- codigo real)

Pesquisa X0: Stake Plinko multipliers — Low 0.5x-5.6x, Medium 0.3x-110x,
High 0.2x-1000x. Tiers: Nice (<5x), Big (5-50x), Mega (50-500x), Plinko (500x+).

```tsx
import { motion, AnimatePresence, useMotionValue, useTransform, animate } from "framer-motion";
import { useEffect, useRef } from "react";

type WinTier = "nice" | "big" | "mega" | "plinko";

interface PlinkoBigWinOverlayProps {
  show: boolean;
  multiplier: number;
  winAmount: number;
  slotIndex: number;
  totalSlots: number;
  lang: "br" | "en";
  onClose: () => void;
}

function getTier(mult: number): { tier: WinTier; color: string; glow: string } {
  if (mult >= 500) return { tier: "plinko", color: "#AA00FF", glow: "rgba(170,0,255,0.5)" };
  if (mult >= 50) return { tier: "mega", color: "#FF9800", glow: "rgba(255,152,0,0.5)" };
  if (mult >= 5) return { tier: "big", color: "#FFD700", glow: "rgba(255,215,0,0.5)" };
  return { tier: "nice", color: "#00E676", glow: "rgba(0,230,118,0.5)" };
}

const LABELS: Record<WinTier, { br: string; en: string }> = {
  nice: { br: "GANHO", en: "WIN" },
  big: { br: "GRANDE GANHO", en: "BIG WIN" },
  mega: { br: "MEGA GANHO", en: "MEGA WIN" },
  plinko: { br: "PLINKO MAXIMO!", en: "MAX PLINKO!" },
};

export default function PlinkoBigWinOverlay({
  show, multiplier, winAmount, slotIndex, totalSlots, lang, onClose,
}: PlinkoBigWinOverlayProps) {
  const { tier, color, glow } = getTier(multiplier);
  const countRef = useRef<HTMLSpanElement>(null);
  const countValue = useMotionValue(0);
  const rounded = useTransform(countValue, (v) => Math.floor(v).toLocaleString());

  useEffect(() => {
    if (!show) return;
    const ctrl = animate(countValue, winAmount, { duration: 2, ease: "easeOut" });
    return () => ctrl.stop();
  }, [show, winAmount, countValue]);

  useEffect(() => {
    if (!countRef.current) return;
    const unsub = rounded.on("change", (v) => {
      if (countRef.current) countRef.current.textContent = `G$ ${v}`;
    });
    return unsub;
  }, [rounded]);

  const isEdge = slotIndex === 0 || slotIndex === totalSlots - 1;

  return (
    <AnimatePresence>
      {show && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          style={{
            position: "fixed", inset: 0,
            background: "rgba(0,0,0,0.9)",
            backdropFilter: "blur(12px)",
            display: "flex", flexDirection: "column",
            alignItems: "center", justifyContent: "center",
            zIndex: 1000,
          }}
        >
          {/* Tier label */}
          <motion.div
            initial={{ scale: 0, rotate: -5 }}
            animate={{ scale: 1, rotate: 0 }}
            transition={{ type: "spring", stiffness: 200, damping: 12 }}
            title={lang === "br" ? LABELS[tier].br : LABELS[tier].en}
            style={{
              fontFamily: "'Cinzel', serif",
              fontSize: tier === "plinko" ? "clamp(28px, 4vw, 48px)" : "clamp(20px, 2.8vw, 32px)",
              fontWeight: 700, color, textTransform: "uppercase",
              letterSpacing: "4px", textShadow: `0 0 30px ${glow}`,
            }}
          >
            {lang === "br" ? LABELS[tier].br : LABELS[tier].en}
          </motion.div>

          {/* Multiplier */}
          <motion.div
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3 }}
            title={`${multiplier}x ${isEdge ? (lang === "br" ? "(slot da borda!)" : "(edge slot!)") : ""}`}
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(16px, 2vw, 24px)", fontWeight: 700,
              color: "rgba(212,168,67,0.8)", marginTop: 4,
            }}
          >
            {multiplier}x {isEdge ? "EDGE!" : ""}
          </motion.div>

          {/* Prize countup */}
          <motion.div
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.6 }}
            title={lang === "br" ? `Premio: G$ ${winAmount.toLocaleString()}` : `Prize: G$ ${winAmount.toLocaleString()}`}
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: "clamp(22px, 3vw, 36px)", fontWeight: 700,
              color: "#00E676", textShadow: "0 0 20px rgba(0,230,118,0.4)",
              marginTop: "clamp(8px, 1vw, 14px)",
            }}
          >
            <img src="/assets/plinko/icons/icon-gcoin.png" alt=""
              style={{ width: 20, height: 20, marginRight: 6, verticalAlign: "middle" }} />
            <span ref={countRef}>G$ 0</span>
          </motion.div>

          {/* Close */}
          <motion.button
            initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.5 }}
            onClick={onClose} whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}
            title={lang === "br" ? "Jogar novamente" : "Play again"}
            style={{
              marginTop: "clamp(14px, 1.5vw, 20px)",
              padding: "clamp(8px, 1vw, 12px) clamp(20px, 2.5vw, 36px)",
              background: "transparent",
              border: "1.5px solid rgba(212,168,67,0.4)",
              borderRadius: 8, color: "#D4A843",
              fontFamily: "'Cinzel', serif", fontSize: "clamp(11px, 1.2vw, 14px)",
              textTransform: "uppercase", letterSpacing: "1px",
              cursor: "pointer", minHeight: 44,
            }}
          >
            {lang === "br" ? "JOGAR NOVAMENTE" : "PLAY AGAIN"}
          </motion.button>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

---

`[ENTREGA: 5.ROTEIRO-PLINKO-PARTE-7-ADENDO.md -- 26/03/2026]`
