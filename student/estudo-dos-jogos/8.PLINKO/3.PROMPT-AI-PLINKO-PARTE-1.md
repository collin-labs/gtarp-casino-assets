# PROMPT AI — PLINKO (#8)
## PARTE 1 DE 4 — Header Tecnico, Paleta, Tipografia, Tela 1 PlinkoGame Idle
### Prompt para gerar visual pixel-perfect em V0 / Bolt / Claude / ChatGPT
### Blackout Casino GTARP - 22/03/2026

---

## PLANO DE ACAO

- **Template seguido:** Secao 5 do Padrao de Estudo Individual (Template Prompt AI) + Prompt Unificado Doc 3
- **Gold Standards consultados:** Prompt AI Dice #7 (4 partes, 2.440 linhas), Prompt AI Slot-Machine #2 (4 partes, 2.369 linhas), Prompt AI Poker #6 (4 partes, 2.529 linhas)
- **Benchmark de linhas:** 1.500-2.300 linhas (4 partes de ~450-500L)
- **Telas previstas:**
  1. PlinkoGame Idle (tela principal completa: board de pegs, controles, historico, saldo)
  2. PlinkoGame Ball Dropping (bola descendo com bounce em pegs, trail neon)
  3. PlinkoResult (overlay de resultado: multiplicador, payout, contagem animada)
  4. PlinkoBigWin (overlay fullscreen para multiplicadores >= 50x: particulas, fanfarra)
  5. PlinkoProvablyFair (modal: seeds, hash, nonce, verificacao local)
  6. PlinkoHistoryModal (modal: lista de drops filtrada, paginacao)
  7. PlinkoAutobet (estado dentro dos controles: loop automatico configuravel)

---

# HEADER TECNICO (Colar no inicio do prompt para qualquer IA)

```
RESTRICOES TECNICAS:
- React 19 + TypeScript + Framer Motion 11 + Next.js 16
- Container: div com position absolute, inset 0, z-index 60, borderRadius inherit, overflow hidden
- SOMENTE dark mode — fundo NUNCA branco, NUNCA cinza claro
- CSS inline via style={{}} — NAO usar classes Tailwind para componentes de jogo
- Board de pegs: Canvas 2D (elemento <canvas>) — pegs como circulos, bola como circulo com trail
- Dual Canvas: canvas estatico (pegs + slots) + canvas animacao (bola + trail) sobrepostos
- Pegs: circulos dourados com radialGradient, offset alternado par/impar por row
- Bola: circulo verde neon (#00E676) com trail de posicoes anteriores em alpha decrescente
- Resultado server-side — client NUNCA calcula resultado, APENAS anima path recebido do server
- Path pre-calculado: array de 'L'/'R' (1 por row) determinado pelo HMAC-SHA256 Provably Fair
- Animacao da bola: bezier curves entre pegs com jitter visual + gravity easing (ease-in quadratico)
- Multiplicadores na base: retangulos coloridos por tier com texto centralizado
- DOM para: controles, overlays, modais, historico, badges — Canvas APENAS para board + bola
- Fontes: Cinzel Decorative (titulos), Inter (body), JetBrains Mono (valores, bet, multiplicadores)
- Responsividade: TUDO com clamp() — nunca px fixo para fontes/padding/sizes
- Mobile: LANDSCAPE natural NUI (1280x720). Desktop: mesmo layout responsivo
- Touch targets: minimo 48x48px em TODOS elementos interativos
- Bilingue: prop lang="br"|"en", todos os textos via objeto { br: "...", en: "..." }
- Sons: usePlinkoSound() hook — 14 sons mapeados por evento, progressive pitch nos peg hits
- FiveM NUI: Chromium CEF (suporta Canvas 2D, Framer Motion, backdrop-filter, clamp)
- Provably Fair: HMAC-SHA256 multi-round server-side, hash publicado ANTES do drop
- Risk levels: 3 niveis (Low/Medium/High) — cada um muda TODA a tabela de multiplicadores
- Rows: 8 a 16 configuraveis — redesenha board em tempo real ao mudar
- ZERO dependencias novas (Canvas 2D nativo + Framer Motion ja no projeto)
```

---

# PALETA BASE — BLACKOUT CASINO (OBRIGATORIA)

```
BACKGROUNDS:
  #050505   — fundo absoluto (atras de tudo)
  #0A0A0A   — fundo do jogo (background-color principal)
  #0F0F0F   — fundo do painel
  #141414   — card, container
  #1A1A1A   — sub-container, input inativo
  #222222   — hover state, item selecionado
  #2A2A2A   — tooltip, dropdown

OURO METALICO (bordas, titulos, moldura, ornamentos):
  #462523   — sombra profunda do metal
  #8B6914   — ouro escuro
  #CB9B51   — ouro medio
  #D4A843   — ouro principal (COR MAIS USADA — texto, borda, UI)
  #F6E27A   — ouro claro (highlight)
  #F6F2C0   — ponto de brilho maximo
  #FFD700   — ouro puro (hover, accent, CTA)

VERDE ESMERALDA (win, ativo, CTA, saldo, acertos):
  #00E676   — verde brilhante (accent principal, bola do plinko)
  #00C853   — verde forte (botao primario CTA)
  #004D25   — verde escuro (base botao)

VERMELHO (perda, erro, high risk):
  #FF1744   — vermelho vibrante
  #FF3B3B   — vermelho de erro

TEXTO:
  #FFFFFF   — texto principal
  #A8A8A8   — texto secundario
  #666666   — texto inativo/placeholder
```

---

# PALETA ESPECIFICA — PLINKO

```
PEGS (PINOS DO BOARD):
  #D4A843              — peg normal (idle, dourado principal)
  #FFD700              — peg flash (quando bola toca, 150ms)
  rgba(212,168,67,0.6) — glow do peg (radialGradient ao redor)
  rgba(212,168,67,0.15)— shimmer idle (onda horizontal que percorre pegs em loop)

BOLA:
  #00E676              — cor primaria da bola (verde neon)
  rgba(0,230,118,0.8)  — centro do radialGradient (glow intenso)
  rgba(0,230,118,0.0)  — borda do radialGradient (fade transparente)
  rgba(0,230,118,0.4)  — trail posicao recente (alpha alto)
  rgba(0,230,118,0.05) — trail posicao antiga (alpha quase zero)

SLOTS (MULTIPLICADORES NA BASE) — POR TIER:
  Low tier (mult <= 1x):
    background: rgba(100,100,100,0.3)  — cinza escuro (perda/break-even)
    text: #A8A8A8                       — cinza claro
    border: rgba(100,100,100,0.2)

  Medium tier (mult 1.1x - 5x):
    background: rgba(0,230,118,0.15)    — verde sutil
    text: #00E676                       — verde neon
    border: rgba(0,230,118,0.3)

  High tier (mult 5.1x - 50x):
    background: rgba(255,215,0,0.15)    — dourado sutil
    text: #FFD700                       — dourado puro
    border: rgba(255,215,0,0.4)

  Extreme tier (mult 51x - 1000x):
    background: rgba(255,23,68,0.15)    — vermelho sutil
    text: #FF1744                       — vermelho vibrante
    border: rgba(255,23,68,0.4)
    boxShadow: 0 0 12px rgba(255,23,68,0.3) — glow vermelho pulsante

RISK SELECTOR:
  Low button active:    bg #00C853, glow rgba(0,230,118,0.4), text #FFFFFF
  Medium button active: bg #FFD700, glow rgba(255,215,0,0.4), text #0A0A0A
  High button active:   bg #FF1744, glow rgba(255,23,68,0.4), text #FFFFFF
  Inactive button:      bg rgba(255,255,255,0.05), text #666666, border rgba(212,168,67,0.2)

BOARD TINT POR RISCO (overlay sutil sobre o Canvas):
  Low:    nenhum overlay (board normal)
  Medium: rgba(255,215,0,0.03) — tint dourado quase imperceptivel
  High:   rgba(255,23,68,0.03) — tint vermelho quase imperceptivel

ESTADOS DO JOGO:
  idle      — pegs dourados opacos, shimmer sutil, slots com multiplicadores visiveis
  dropping  — bola descendo, pegs flasham ao contato, trail verde, slots aguardando
  landed    — slot de destino pulsa com cor do tier, multiplicador revela com scale
  bigwin    — backdrop blur, flash dourado, particulas, valor pulsante
```

---

# TIPOGRAFIA

```
PRIMARIA — TITULOS E LABELS:
  font-family: 'Cinzel Decorative', 'Cinzel', 'Playfair Display', serif;
  font-weight: 700 a 900;
  text-transform: uppercase;
  letter-spacing: clamp(1px, 0.2vw, 3px);
  color: #D4A843 (normal) ou #FFD700 (hover/accent);
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
  USAR EM: titulo "PLINKO", labels de secao ("APOSTA", "RISCO", "LINHAS")

  Titulo principal "PLINKO":
    font-weight: 900;
    fontSize: clamp(18px, 2.8vw, 36px);
    color: #FFD700;
    textShadow: '0 0 25px rgba(255,215,0,0.9), 0 4px 10px rgba(0,0,0,1)';
    letterSpacing: clamp(3px, 0.5vw, 6px);

SECUNDARIA — CORPO E DESCRICOES:
  font-family: 'Inter', 'Segoe UI', system-ui, sans-serif;
  font-weight: 400 a 600;
  letter-spacing: 0;
  color: #A8A8A8 (secundario) ou #FFFFFF (primario);
  USAR EM: descricoes, labels menores, texto explicativo

NUMERICA — VALORES E MULTIPLICADORES:
  font-family: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace;
  font-weight: 700;
  letter-spacing: clamp(0.5px, 0.1vw, 1.5px);
  USAR EM: saldo GCoin, bet amount, multiplicadores, payouts, rows counter

  Saldo GCoin:
    fontSize: clamp(12px, 1.5vw, 22px);
    color: #00E676;
    textShadow: '0 0 10px rgba(0,230,118,0.5)';

  Multiplicador (slot base):
    fontSize: clamp(8px, 1vw, 14px);
    color: VARIAVEL POR TIER (ver PALETA ESPECIFICA);

  Multiplicador (resultado overlay):
    fontSize: clamp(28px, 4vw, 56px);
    color: VARIAVEL POR TIER;
    textShadow: '0 0 30px rgba(COR_TIER,0.8), 0 4px 12px rgba(0,0,0,1)';

  Payout "+500 GCoin":
    fontSize: clamp(14px, 2vw, 24px);
    color: #00E676;
    textShadow: '0 0 15px rgba(0,230,118,0.6)';

TAMANHOS RESPONSIVOS (padrao clamp):
  Titulo jogo:     clamp(18px, 2.8vw, 36px)
  Label secao:     clamp(10px, 1.2vw, 16px)
  Texto corpo:     clamp(9px, 1.1vw, 14px)
  Valores grandes: clamp(28px, 4vw, 56px)
  Valores medios:  clamp(12px, 1.5vw, 22px)
  Valores pequenos:clamp(8px, 1vw, 14px)
  Botao texto:     clamp(10px, 1.3vw, 16px)
```

---

# TELA 1: PLINKO GAME — IDLE (Estado: aguardando drops)

## Layout ASCII

```
+========================================================================+
| [< VOLTAR]           PLINKO           [Som] [PF]       GCoin: G$5.000 |
|------------------------------------------------------------------------|
|                                                                        |
| +--CONTROLES--------+  +--BOARD-----------------------------------+    |
| | APOSTA             |  |                     o                    |    |
| | [____G$500____]    |  |                    . .                   |    |
| | [MIN][x2][/2][MAX] |  |                   . . .                  |    |
| |                    |  |                  . . . .                  |    |
| | RISCO              |  |                 . . . . .                |    |
| | [LOW] [MED] [HIGH] |  |                . . . . . .               |    |
| |                    |  |               . . . . . . .              |    |
| | LINHAS             |  |              . . . . . . . .             |    |
| | [====O=======] 12  |  |             . . . . . . . . .            |    |
| |                    |  |            . . . . . . . . . .           |    |
| | +================+ |  |           . . . . . . . . . . .          |    |
| | |   APOSTAR      | |  |          . . . . . . . . . . . .         |    |
| | +================+ |  |         . . . . . . . . . . . . .        |    |
| |                    |  |                                          |    |
| | [x] AUTO  [_50_]  |  | [0.2][0.2][0.2][2][4][9][26][130][1000] |    |
| +--------------------+  | [1000][130][26][9][4][2][0.2][0.2][0.2] |    |
|                          +------------------------------------------+    |
|                                                                        |
| HISTORICO: [x1.1][x0.2][x4][x0.2][x26][x1][x0.2][x130][x0.2][x9]    |
|                                                                        |
| [Historico]  [Provably Fair]                                           |
+========================================================================+
```

**Descricao do layout:**
- **Header:** Botao VOLTAR esquerda, titulo "PLINKO" centro, icones Som/PF direita, saldo GCoin extrema direita
- **Area principal (flex row):** Controles (20-25% width) esquerda + Board Canvas (65-70% width) direita
- **Board:** Piramide triangular de pegs dourados com offset alternado. Abaixo dos pegs: 2 fileiras de slots com multiplicadores (simetricos, 0.2x nas bordas extremas ate 1000x nas bordas, depende do risk)
- **Controles:** Input aposta (dourado, borda, atalhos MIN/x2/div2/MAX), risk selector (3 botoes coloridos), rows slider (range 8-16), botao APOSTAR (verde primario com bevel), toggle autobet
- **Historico:** Barra horizontal scrollavel abaixo do board. Badges coloridos dos ultimos drops
- **Footer:** Botoes utilitarios Historico e Provably Fair

## Especificacoes Tela 1

### Container Principal (PlinkoGame.tsx)
```css
/* Container que ocupa todo o painel */
style={{
  position: 'absolute',
  inset: 0,
  zIndex: 60,
  borderRadius: 'inherit',
  overflow: 'hidden',
  backgroundImage: 'url(/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  display: 'flex',
  flexDirection: 'column',
  padding: 'clamp(6px, 0.8vw, 14px)',
  fontFamily: "'Inter', 'Segoe UI', system-ui, sans-serif",
}}

/* Overlay de escurecimento sobre bg-casino.png */
style={{
  position: 'absolute',
  inset: 0,
  background: 'rgba(0,0,0,0.75)',
  zIndex: 0,
}}

/* Vinheta radial */
style={{
  position: 'absolute',
  inset: 0,
  background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
  zIndex: 1,
  pointerEvents: 'none',
}}
```

### Header (Voltar + Titulo + Icons + Saldo)
```css
/* Header bar — topo do jogo */
style={{
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  padding: 'clamp(4px, 0.6vw, 10px) clamp(6px, 0.8vw, 14px)',
  borderBottom: '1px solid rgba(212,168,67,0.2)',
  marginBottom: 'clamp(4px, 0.5vw, 8px)',
  position: 'relative',
  zIndex: 10,
}}

/* Botao VOLTAR (padrao projeto) */
style={{
  background: 'rgba(0,0,0,0.6)',
  border: '1px solid rgba(212,168,67,0.4)',
  borderRadius: '8px',
  color: '#D4A843',
  fontFamily: "'Cinzel', serif",
  fontSize: 'clamp(9px, 1.1vw, 14px)',
  letterSpacing: '2px',
  padding: 'clamp(4px, 0.5vw, 8px) clamp(8px, 1vw, 16px)',
  cursor: 'pointer',
  textTransform: 'uppercase',
}}
/* Framer Motion: */
whileHover={{ scale: 1.05, borderColor: 'rgba(212,168,67,0.8)' }}
whileTap={{ scale: 0.95 }}

/* Titulo "PLINKO" */
style={{
  fontFamily: "'Cinzel Decorative', serif",
  fontWeight: 900,
  fontSize: 'clamp(18px, 2.8vw, 36px)',
  color: '#FFD700',
  textShadow: '0 0 25px rgba(255,215,0,0.9), 0 4px 10px rgba(0,0,0,1)',
  letterSpacing: 'clamp(3px, 0.5vw, 6px)',
  textTransform: 'uppercase',
}}

/* Icons (Som, Provably Fair) — PNGs dourados, nao Lucide (REGRA X21) */
style={{
  display: 'flex',
  gap: 'clamp(6px, 0.8vw, 12px)',
  alignItems: 'center',
}}
/* Cada icon: */
style={{
  width: 'clamp(20px, 2.5vw, 32px)',
  height: 'clamp(20px, 2.5vw, 32px)',
  cursor: 'pointer',
  opacity: 0.7,
  filter: 'brightness(1)',
  transition: 'opacity 0.2s, filter 0.2s',
}}
/* Hover icon: opacity 1, filter brightness(1.3) */

/* Saldo GCoin */
style={{
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(12px, 1.5vw, 22px)',
  color: '#00E676',
  textShadow: '0 0 10px rgba(0,230,118,0.5)',
  letterSpacing: '1px',
}}
```

### Area Principal (flex row: Controles + Board)
```css
/* Container flex horizontal */
style={{
  display: 'flex',
  flex: 1,
  gap: 'clamp(6px, 0.8vw, 14px)',
  minHeight: 0,
  position: 'relative',
  zIndex: 10,
}}
```

### Painel de Controles (esquerda, ~22% width)
```css
/* Container dos controles */
style={{
  flex: '0 0 clamp(160px, 22vw, 280px)',
  display: 'flex',
  flexDirection: 'column',
  gap: 'clamp(6px, 0.8vw, 12px)',
  padding: 'clamp(8px, 1vw, 16px)',
  background: 'rgba(0,0,0,0.5)',
  backdropFilter: 'blur(8px)',
  borderRadius: '12px',
  border: '1px solid rgba(212,168,67,0.2)',
  boxShadow: '0 4px 15px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,215,0,0.05)',
  overflow: 'auto',
}}

/* Label de secao (APOSTA, RISCO, LINHAS) */
style={{
  fontFamily: "'Cinzel', serif",
  fontWeight: 700,
  fontSize: 'clamp(8px, 1vw, 12px)',
  color: '#D4A843',
  textTransform: 'uppercase',
  letterSpacing: '2px',
  marginBottom: 'clamp(2px, 0.3vw, 4px)',
}}

/* Input de aposta */
style={{
  width: '100%',
  background: 'rgba(0,0,0,0.6)',
  border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '8px',
  padding: 'clamp(6px, 0.8vw, 12px)',
  color: '#00E676',
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(12px, 1.5vw, 18px)',
  textAlign: 'center',
  outline: 'none',
}}
/* Focus: borderColor rgba(0,230,118,0.5), boxShadow 0 0 8px rgba(0,230,118,0.2) */
/* Erro: borderColor #FF3B3B, boxShadow 0 0 8px rgba(255,59,59,0.3), animation shake 200ms */

/* Atalhos MIN/x2/div2/MAX */
style={{
  display: 'flex',
  gap: 'clamp(2px, 0.3vw, 4px)',
}}
/* Cada botao atalho: */
style={{
  flex: 1,
  padding: 'clamp(3px, 0.4vw, 6px)',
  background: 'rgba(212,168,67,0.1)',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: '4px',
  color: '#D4A843',
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: 'clamp(7px, 0.9vw, 11px)',
  fontWeight: 600,
  cursor: 'pointer',
  textAlign: 'center',
  transition: 'all 0.15s',
}}
/* Hover: bg rgba(212,168,67,0.2), borderColor rgba(212,168,67,0.4) */
/* Active: scale 0.95, bg rgba(212,168,67,0.3) */

/* Risk selector (3 botoes) */
style={{
  display: 'flex',
  gap: 'clamp(3px, 0.4vw, 6px)',
}}
/* Botao Low (ativo): */
style={{
  flex: 1,
  padding: 'clamp(6px, 0.8vw, 10px)',
  background: '#00C853',
  border: '1px solid rgba(0,200,83,0.6)',
  borderRadius: '6px',
  color: '#FFFFFF',
  fontFamily: "'Cinzel', serif",
  fontSize: 'clamp(8px, 1vw, 12px)',
  fontWeight: 700,
  cursor: 'pointer',
  textTransform: 'uppercase',
  letterSpacing: '1px',
  boxShadow: '0 0 12px rgba(0,230,118,0.4), inset 0 1px 0 rgba(255,255,255,0.1)',
  transition: 'all 0.2s',
}}
/* Botao Medium (ativo): bg #FFD700, border rgba(255,215,0,0.6), color #0A0A0A, boxShadow com dourado */
/* Botao High (ativo): bg #FF1744, border rgba(255,23,68,0.6), color #FFFFFF, boxShadow com vermelho */
/* Botao INativo: bg rgba(255,255,255,0.05), border rgba(212,168,67,0.2), color #666666, sem shadow */
/* Framer Motion ativo: */
whileHover={{ scale: 1.05 }}
whileTap={{ scale: 0.95 }}

/* Slider de rows */
/* Label: "12 LINHAS" ao lado do slider */
style={{
  display: 'flex',
  alignItems: 'center',
  gap: 'clamp(6px, 0.8vw, 10px)',
}}
/* Input range customizado: */
style={{
  flex: 1,
  WebkitAppearance: 'none',
  appearance: 'none',
  height: '4px',
  borderRadius: '2px',
  background: 'linear-gradient(90deg, #D4A843 0%, #D4A843 var(--pct), rgba(212,168,67,0.2) var(--pct), rgba(212,168,67,0.2) 100%)',
  outline: 'none',
  cursor: 'pointer',
}}
/* Thumb (CSS ::webkit-slider-thumb se necessario, ou div overlay): */
/* width 16px, height 16px, borderRadius 50%, bg #00E676, boxShadow glow verde */
/* Label do valor: */
style={{
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(10px, 1.2vw, 16px)',
  color: '#FFD700',
  minWidth: '32px',
  textAlign: 'center',
}}

/* Botao APOSTAR (CTA primario verde) */
style={{
  width: '100%',
  padding: 'clamp(8px, 1vw, 14px)',
  background: 'linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%)',
  border: '1px solid rgba(0,230,118,0.4)',
  borderRadius: '8px',
  color: '#FFFFFF',
  fontFamily: "'Cinzel', serif",
  fontWeight: 900,
  fontSize: 'clamp(11px, 1.4vw, 18px)',
  letterSpacing: '3px',
  textTransform: 'uppercase',
  cursor: 'pointer',
  boxShadow: '0 4px 12px rgba(0,200,83,0.3), inset 0 1px 0 rgba(255,255,255,0.15)',
  position: 'relative',
  overflow: 'hidden',
}}
/* Pseudo ::before (bevel highlight no topo): */
/* content '', position absolute, top 0, left 10%, right 10%, height 1px, bg rgba(255,255,255,0.3) */
/* Framer Motion: */
whileHover={{ scale: 1.03, boxShadow: '0 6px 20px rgba(0,200,83,0.5)' }}
whileTap={{ scale: 0.97 }}
/* Disabled (saldo insuficiente ou animando): opacity 0.4, cursor not-allowed, sem hover */

/* Toggle autobet */
style={{
  display: 'flex',
  alignItems: 'center',
  gap: 'clamp(4px, 0.5vw, 8px)',
}}
/* Checkbox customizado: */
style={{
  width: 'clamp(14px, 1.8vw, 20px)',
  height: 'clamp(14px, 1.8vw, 20px)',
  borderRadius: '4px',
  border: '1px solid rgba(212,168,67,0.3)',
  background: 'rgba(0,0,0,0.4)',
  cursor: 'pointer',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
}}
/* Checked: bg #00C853, border rgba(0,200,83,0.6), icone checkmark branco */
/* Input quantidade autobet: mesmo estilo do input aposta mas menor */
style={{
  width: 'clamp(40px, 5vw, 60px)',
  padding: 'clamp(3px, 0.4vw, 6px)',
  background: 'rgba(0,0,0,0.6)',
  border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '4px',
  color: '#D4A843',
  fontFamily: "'JetBrains Mono', monospace",
  fontSize: 'clamp(9px, 1.1vw, 13px)',
  textAlign: 'center',
}}
```

### Board Canvas (direita, ~70% width)
```css
/* Container do Canvas (position relative para sobreposicao dos 2 canvas) */
style={{
  flex: '1 1 70%',
  position: 'relative',
  borderRadius: '14px',
  border: '1.5px solid rgba(212,168,67,0.3)',
  outline: '1px solid rgba(191,149,63,0.1)',
  outlineOffset: '3px',
  boxShadow: '0 4px 15px rgba(0,0,0,0.6), 0 1px 3px rgba(0,0,0,0.8), inset 0 1px 0 rgba(255,215,0,0.08), inset 0 0 15px rgba(0,0,0,0.4)',
  overflow: 'hidden',
  background: 'rgba(0,0,0,0.4)',
}}

/* Canvas estatico (pegs + slots) — z-index 1 */
style={{
  position: 'absolute',
  inset: 0,
  zIndex: 1,
}}

/* Canvas animacao (bola + trail) — z-index 2, sobre o estatico */
style={{
  position: 'absolute',
  inset: 0,
  zIndex: 2,
}}

/* Overlay de tint por risco (z-index 3, pointer-events none) */
style={{
  position: 'absolute',
  inset: 0,
  zIndex: 3,
  pointerEvents: 'none',
  background: riskLevel === 'high' ? 'rgba(255,23,68,0.03)' :
              riskLevel === 'medium' ? 'rgba(255,215,0,0.03)' : 'transparent',
  transition: 'background 0.3s ease',
}}

/* PEGS renderizados no Canvas estatico: */
/* Para cada peg em cada row:
   ctx.beginPath();
   ctx.arc(peg.x, peg.y, pegRadius, 0, Math.PI * 2);
   
   // Gradiente radial para glow
   const grad = ctx.createRadialGradient(peg.x, peg.y, 0, peg.x, peg.y, pegRadius * 2);
   grad.addColorStop(0, '#D4A843');
   grad.addColorStop(0.5, 'rgba(212,168,67,0.6)');
   grad.addColorStop(1, 'rgba(212,168,67,0)');
   ctx.fillStyle = grad;
   ctx.fill();
   
   // Peg solido por cima
   ctx.beginPath();
   ctx.arc(peg.x, peg.y, pegRadius * 0.6, 0, Math.PI * 2);
   ctx.fillStyle = '#D4A843';
   ctx.fill();
*/

/* SLOTS renderizados na base do Canvas estatico: */
/* Para cada slot (N+1 slots para N rows):
   const slotWidth = boardWidth / (rows + 1);
   const slotHeight = slotWidth * 0.6;
   const y = boardHeight - slotHeight - margin;
   
   // Background do slot
   ctx.fillStyle = getSlotBackground(multiplier); // por tier
   ctx.roundRect(x, y, slotWidth - 2, slotHeight, 4);
   ctx.fill();
   
   // Border do slot
   ctx.strokeStyle = getSlotBorder(multiplier); // por tier
   ctx.lineWidth = 1;
   ctx.stroke();
   
   // Texto do multiplicador
   ctx.font = `bold ${fontSize}px 'JetBrains Mono', monospace`;
   ctx.fillStyle = getSlotTextColor(multiplier); // por tier
   ctx.textAlign = 'center';
   ctx.fillText(formatMultiplier(mult), x + slotWidth/2, y + slotHeight/2 + fontSize/3);
*/
```

### Barra de Historico (abaixo do board)
```css
/* Container horizontal scrollavel */
style={{
  display: 'flex',
  gap: 'clamp(3px, 0.4vw, 6px)',
  padding: 'clamp(4px, 0.5vw, 8px) 0',
  overflowX: 'auto',
  overflowY: 'hidden',
  scrollBehavior: 'smooth',
  position: 'relative',
  zIndex: 10,
}}

/* Badge individual de resultado */
style={{
  flexShrink: 0,
  padding: 'clamp(3px, 0.4vw, 6px) clamp(6px, 0.8vw, 12px)',
  borderRadius: '6px',
  background: 'VARIAVEL POR TIER (ver PALETA ESPECIFICA)',
  border: '1px solid VARIAVEL POR TIER',
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(8px, 1vw, 13px)',
  color: 'VARIAVEL POR TIER',
  whiteSpace: 'nowrap',
}}
/* Framer Motion entrada (stagger): */
initial={{ opacity: 0, scale: 0.8, x: -10 }}
animate={{ opacity: 1, scale: 1, x: 0 }}
transition={{ delay: index * 0.03, type: 'spring', stiffness: 300 }}
```

### Footer (Botoes utilitarios)
```css
/* Container footer */
style={{
  display: 'flex',
  gap: 'clamp(8px, 1vw, 16px)',
  padding: 'clamp(4px, 0.5vw, 8px) 0',
  position: 'relative',
  zIndex: 10,
}}

/* Botao utilitario (Historico, Provably Fair) */
style={{
  padding: 'clamp(4px, 0.5vw, 8px) clamp(10px, 1.2vw, 18px)',
  background: 'rgba(0,0,0,0.4)',
  border: '1px solid rgba(212,168,67,0.2)',
  borderRadius: '6px',
  color: '#D4A843',
  fontFamily: "'Cinzel', serif",
  fontSize: 'clamp(8px, 1vw, 12px)',
  letterSpacing: '1px',
  textTransform: 'uppercase',
  cursor: 'pointer',
  transition: 'all 0.2s',
}}
/* Hover: borderColor rgba(212,168,67,0.5), color #FFD700, bg rgba(0,0,0,0.6) */
/* Framer Motion: */
whileHover={{ scale: 1.03 }}
whileTap={{ scale: 0.97 }}
```

---

**[FIM DA PARTE 1 — Continua com Tela 2 Ball Dropping, Tela 3 Result, Tela 4 BigWin]**

`[ENTREGA: 3.PROMPT-AI-PLINKO-PARTE-1.md — 22/03/2026]`
