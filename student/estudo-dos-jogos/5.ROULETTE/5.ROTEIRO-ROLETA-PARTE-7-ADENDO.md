# ADENDO — ROTEIRO DE DESENVOLVIMENTO ROLETA (#5)
## Complemento para itens 5.11, 5.14, 5.15
## Referencia: 5.ROTEIRO-ROLETA-PARTE-5/6.md — 23/03/2026

---

# 1. CONTEXTO

As Partes 5-6 cobrem Fase 4 (Polish: HistoryOverlay, PFModal, BigWinOverlay)
de forma mais descritiva. Este adendo fornece TSX real com tooltips inline.

---

# 2. FASE 4 POLISH — CODIGO REAL TSX

## 2.1 HistoryOverlay.tsx — Trechos com tooltips

```tsx
{history.map((round, i) => (
  <motion.div
    key={round.id}
    initial={{ opacity: 0, x: -10 }}
    animate={{ opacity: 1, x: 0 }}
    transition={{ delay: i * 0.03 }}
    style={{
      display: "flex",
      alignItems: "center",
      gap: "clamp(8px, 0.8vw, 14px)",
      padding: "clamp(8px, 0.8vw, 14px)",
      background: round.win > 0 ? "rgba(0,230,118,0.05)" : "rgba(255,255,255,0.02)",
      borderRadius: "8px",
      border: `1px solid ${round.win > 0 ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.05)"}`,
    }}
  >
    {/* Numero resultado com cor */}
    <span
      title={lang === "br"
        ? `Numero ${round.result} — ${round.color === "red" ? "vermelho" : round.color === "black" ? "preto" : "verde"}`
        : `Number ${round.result} — ${round.color}`
      }
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(12px, 1.3vw, 18px)",
        fontWeight: 800,
        color: round.color === "red" ? "#FF1744" : round.color === "green" ? "#00E676" : "#FFFFFF",
        background: round.color === "red" ? "rgba(255,23,68,0.15)" : round.color === "green" ? "rgba(0,230,118,0.15)" : "rgba(255,255,255,0.1)",
        borderRadius: "6px",
        minWidth: "clamp(28px, 3vw, 40px)",
        textAlign: "center",
        padding: "4px 8px",
      }}
    >
      {round.result}
    </span>

    {/* Lightning badge */}
    {round.isLightning && (
      <span style={{
        fontFamily: "var(--font-mono)",
        fontSize: "clamp(8px, 0.8vw, 11px)",
        color: "#FFD700",
        background: "rgba(255,215,0,0.1)",
        borderRadius: "4px",
        padding: "2px 6px",
      }}>
        {round.multiplier}x
      </span>
    )}

    {/* Payout */}
    <span style={{
      fontFamily: "var(--font-mono)",
      fontSize: "clamp(10px, 1vw, 14px)",
      fontWeight: 700,
      color: round.win > 0 ? "#00E676" : "rgba(212,168,67,0.4)",
      marginLeft: "auto",
      textShadow: round.win > 0 ? "0 0 8px rgba(0,230,118,0.4)" : "none",
    }}>
      {round.win > 0 ? `+G$${round.win.toLocaleString()}` : "G$0"}
    </span>

    {/* Verificar PF */}
    <motion.button
      onClick={() => onVerify(round.id)}
      whileHover={{ scale: 1.1, borderColor: "rgba(0,230,118,0.6)" }}
      whileTap={{ scale: 0.9 }}
      title={lang === "br" ? "Verificar fairness desta rodada" : "Verify this round's fairness"}
      style={{
        background: "rgba(212,168,67,0.08)",
        border: "1px solid rgba(212,168,67,0.25)",
        borderRadius: "6px",
        padding: "clamp(4px, 0.4vw, 8px)",
        cursor: "pointer",
        minWidth: "44px",
        minHeight: "44px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <img src="/assets/shared/icons/icon-provably-fair.png" alt="" style={{ width: "16px", height: "16px" }} />
    </motion.button>
  </motion.div>
))}
```

## 2.2 ProvablyFairModal.tsx — Campos com copiar

```tsx
{(["serverSeedHash", "serverSeed", "clientSeed", "nonce"] as const).map((field) => (
  <div key={field} style={{ marginBottom: "clamp(8px, 0.8vw, 14px)" }}>
    <label style={{
      fontFamily: "var(--font-cinzel)",
      fontSize: "clamp(8px, 0.8vw, 11px)",
      fontWeight: 600,
      color: "rgba(212,168,67,0.6)",
      letterSpacing: "1.5px",
      textTransform: "uppercase" as const,
      marginBottom: "4px",
      display: "block",
    }}>
      {pfLabels[field][lang]}
    </label>
    <div style={{ display: "flex", alignItems: "center", gap: "6px" }}>
      <input readOnly value={String(pfData[field])} style={{
        flex: 1, background: "rgba(0,0,0,0.4)", border: "1px solid rgba(212,168,67,0.2)",
        borderRadius: "6px", color: "#D4A843", fontFamily: "var(--font-mono)",
        fontSize: "clamp(9px, 0.9vw, 13px)", padding: "clamp(6px, 0.6vw, 10px)",
      }} />
      <motion.button
        onClick={() => handleCopy(field)}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        title={lang === "br" ? "Copiar para area de transferencia" : "Copy to clipboard"}
        style={{
          background: copiedField === field ? "rgba(0,230,118,0.15)" : "rgba(212,168,67,0.08)",
          border: `1px solid ${copiedField === field ? "rgba(0,230,118,0.5)" : "rgba(212,168,67,0.3)"}`,
          borderRadius: "6px", padding: "clamp(6px, 0.6vw, 10px)", cursor: "pointer",
          minWidth: "44px", minHeight: "44px", display: "flex", alignItems: "center", justifyContent: "center",
        }}
      >
        <img src={copiedField === field ? "/assets/shared/icons/icon-check.png" : "/assets/shared/icons/icon-copy.png"} alt="" style={{ width: "16px", height: "16px" }} />
      </motion.button>
    </div>
  </div>
))}

{/* Formula: hash % 37 */}
<pre style={{
  fontFamily: "var(--font-mono)", fontSize: "clamp(8px, 0.8vw, 12px)",
  color: "rgba(212,168,67,0.7)", lineHeight: 1.8, whiteSpace: "pre-wrap",
  background: "rgba(0,0,0,0.3)", borderRadius: "8px",
  padding: "clamp(10px, 1vw, 16px)", border: "1px solid rgba(212,168,67,0.15)",
  margin: 0,
}}>
{`hash = HMAC_SHA256(serverSeed, clientSeed + ":" + nonce)
number = parseInt(hash.slice(0, 8), 16) % 37
// 0 = verde, 1-36 = vermelho/preto conforme layout europeu`}
</pre>
```

---

# 3. REGRAS DE ENTREGA COMPLEMENTO (item 5.11)

9. TODOS botoes com minHeight 44px (chips, GIRAR, LIMPAR, REPETIR, DESFAZER)
10. TODOS textos via t(key, lang) — zero string hardcoded
11. Resultado da roleta SEMPRE server-authoritative (hash % 37)
12. Canvas wheel: requestAnimationFrame com deceleration curve, NAO setInterval

---

# 4. VALIDACAO

**5.11** +4 regras = 12 total. OK.
**5.14** TSX real: HistoryOverlay (rounds com cor + lightning badge + verificar) + PFModal (4 campos + formula). OK.
**5.15** Tooltips: 6 inline (numero title contextual, verificar PF, copiar 4x). OK.

---

`[ENTREGA: ADENDO-ROTEIRO-ROLETA.md — 23/03/2026]`
