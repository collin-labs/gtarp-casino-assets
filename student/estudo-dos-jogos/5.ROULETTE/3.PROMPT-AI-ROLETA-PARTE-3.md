# PROMPT AI - ROLETA (#5)
## PARTE 3 DE 5 - TELA 3 (Lightning Phase) + TELA 4 (Spinning) + TELA 5 (Resultado)
### Blackout Casino GTARP - 17/03/2026

---

# TELA 3: LIGHTNING PHASE - REVELACAO LUCKY NUMBERS (Apenas modo Relampago)

## Layout ASCII

```
+--------------------------------------------------------------+
|                                                              |
|              (tela escurecida - overlay semi-opaco)           |
|                                                              |
|                                                              |
|         [RAIO]          [RAIO]          [RAIO]               |
|           |               |               |                  |
|           v               v               v                  |
|                                                              |
|         +---+           +---+           +---+                |
|         | 7 |           | 23|           | 32|                |
|         |100x|          |200x|          |500x|               |
|         +---+           +---+           +---+                |
|                                                              |
|                                                              |
|     +------------------------------------------------+       |
|     |  MESA COM NUMEROS LUCKY DESTACADOS             |       |
|     |  7=glow, 23=glow, 32=glow, demais=opacidade   |       |
|     +------------------------------------------------+       |
|                                                              |
+--------------------------------------------------------------+
```

## Especificacoes Tela 3

**Overlay escurecimento:**
```css
position: absolute; inset: 0; z-index: 70;
background: rgba(0,0,0,0.6);
backdrop-filter: blur(2px);
pointer-events: none;
/* Framer Motion: */
/* initial={{ opacity: 0 }} animate={{ opacity: 1 }} */
/* transition={{ duration: 0.5 }} */
```

**Flash de tela (por Lucky Number revelado):**
```css
/* @keyframes lightningFlash */
@keyframes lightningFlash {
  0% { opacity: 0; }
  10% { opacity: 0.3; }
  20% { opacity: 0; }
  30% { opacity: 0.15; }
  40% { opacity: 0; }
  100% { opacity: 0; }
}
/* Div cobrindo tela inteira: */
position: absolute; inset: 0; z-index: 75;
background: #FFFFFF;
animation: lightningFlash 0.4s ease-out forwards;
pointer-events: none;
/* Dispara 1 vez por Lucky Number revelado */
```

**Shake da tela (acompanha flash):**
```css
/* @keyframes lightningShake */
@keyframes lightningShake {
  0% { transform: translate(0, 0); }
  15% { transform: translate(-3px, 1px); }
  30% { transform: translate(2px, -2px); }
  45% { transform: translate(-1px, 2px); }
  60% { transform: translate(1px, -1px); }
  75% { transform: translate(-1px, 0); }
  100% { transform: translate(0, 0); }
}
/* Aplicado no container principal do jogo: */
animation: lightningShake 0.3s ease-out;
/* Dispara junto com cada flash */
```

**Raio visual (imagem PNG):**
```css
position: absolute;
width: clamp(30px, 5vw, 60px);
height: clamp(80px, 12vw, 160px);
/* Posicao: acima do numero Lucky, centralizado */
top: 20%;
z-index: 76;
filter: drop-shadow(0 0 12px rgba(255,215,0,0.6));
opacity: 0;
/* Framer Motion: */
/* initial={{ opacity: 0, scale: 2, y: -40 }} */
/* animate={{ opacity: 1, scale: 1, y: 0 }} */
/* transition={{ duration: 0.3, ease: 'easeOut' }} */
/* Cada raio tem delay stagger: index * 0.8s (0.8s entre revelacoes) */
```

**Lucky Number badge (numero + multiplicador):**
```css
position: absolute; z-index: 77;
display: flex; flex-direction: column; align-items: center; gap: 4px;

/* Numero: */
width: clamp(40px, 5vw, 64px); height: clamp(40px, 5vw, 64px);
border-radius: 50%;
display: flex; align-items: center; justify-content: center;
font: Inter 700 clamp(14px, 2vw, 24px); color: #FFFFFF;
border: 3px solid #FFD700;
box-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.2);
/* Background por cor: vermelho #DC2626, preto #1A1A1A, verde #15803D */

/* Framer Motion numero: */
/* initial={{ scale: 3, opacity: 0 }} */
/* animate={{ scale: 1, opacity: 1 }} */
/* transition={{ duration: 0.4, ease: [0.34, 1.56, 0.64, 1] }} bounce effect */

/* Multiplicador badge (abaixo do numero): */
background: linear-gradient(135deg, #FFD700, #D4A843);
color: #1A1A1A;
font: 'Cinzel Decorative', serif; font-weight: 900;
font-size: clamp(12px, 1.8vw, 22px);
padding: clamp(2px, 0.3vw, 4px) clamp(6px, 0.8vw, 12px);
border-radius: 6px;
text-shadow: none;
box-shadow: 0 2px 8px rgba(0,0,0,0.4);

/* Framer Motion badge: */
/* initial={{ scale: 0, opacity: 0 }} */
/* animate={{ scale: 1, opacity: 1 }} */
/* transition={{ delay: 0.2, duration: 0.3, ease: [0.34, 1.56, 0.64, 1] }} */

/* @keyframes multPulse aplicado apos revelacao: */
@keyframes multPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.08); }
}
/* animation: multPulse 1.5s ease-in-out infinite; */
```

**Mesa com numeros Lucky destacados:**
```css
/* Numeros Lucky na mesa: */
border: 2px solid #FFD700;
background: rgba(255,215,0,0.15);
box-shadow: 0 0 12px rgba(255,215,0,0.3);
/* Badge mini multiplicador (absolute top -8px right -8px): */
font: Cinzel 700 clamp(6px, 0.7vw, 9px);
background: #FFD700; color: #1A1A1A;
padding: 1px 3px; border-radius: 3px;

/* Numeros NAO-Lucky durante revelacao: */
opacity: 0.4;
filter: grayscale(50%);
transition: all 0.5s ease;
```

**Sequencia temporal:**
```
t=0.0s: Overlay escurece (0.5s fade in)
t=0.5s: lightning_intro.mp3
t=1.0s: Flash + shake + raio #1 + Lucky Number #1 + lightning_zap.mp3
t=1.8s: Flash + shake + raio #2 + Lucky Number #2 + lightning_zap.mp3
t=2.6s: Flash + shake + raio #3 + Lucky Number #3 + lightning_zap.mp3
t=3.4s: (se 4+ Lucky) raio #4
t=4.2s: (se 5 Lucky) raio #5
t=ultimo+1s: Overlay fade out, roda comeca a girar
```

**Mobile:**
```css
/* Lucky Numbers: width/height clamp(32px, 4vw, 48px) */
/* Multiplicador font: clamp(10px, 1.5vw, 16px) */
/* Raio: height clamp(50px, 8vw, 100px) */
```

---

# TELA 4: SPINNING - RODA GIRANDO + BOLA ORBITANDO

## Layout ASCII

```
+--------------------------------------------------------------+
| [< VOLTAR]    ROLETA CLASSICA    G$ 14.630   [PF] [HIST]     |
|--------------------------------------------------------------|
|                                                              |
| +----------------------------+   +--------------------+      |
| |  MESA COM APOSTAS          |   |                    |      |
| |  (chips visiveis)          |   |   [RODA GIRANDO]   |      |
| |  (areas desabilitadas)     |   |    o (bola orbit)  |      |
| |  (cursor not-allowed)      |   |                    |      |
| +----------------------------+   +--------------------+      |
|                                                              |
| [chips desabilitados]               [GIRAR: "GIRANDO..."]    |
+--------------------------------------------------------------+
```

## Especificacoes Tela 4

**Roda girando:**
```css
/* Framer Motion animate: */
/* animate={{ rotate: finalAngle }} */
/* finalAngle = voltasCompletas + (360 - anguloParada) */
/* voltasCompletas = (5 + Math.floor(Math.random() * 4)) * 360 */

/* transition: */
/* { duration: 4 + Math.random() * 1.5, ease: [0.15, 0.85, 0.35, 1.0] } */

/* Direcao alterna a cada spin */
/* Sombra dinamica: */
filter: drop-shadow(0 0 25px rgba(212,168,67,0.25));
```

**Bola orbitando (sentido oposto):**
```css
/* Fase 1 (0-70%): orbita rapida raio 82%, sentido oposto */
/* Fase 2 (70-90%): queda radial 82%->62%, ease-in */
/* Fase 3 (90-100%): bounce sutil +2px/-1px/0 */

/* Visual: */
width: clamp(8px, 1vw, 14px); height: clamp(8px, 1vw, 14px);
border-radius: 50%;
background: radial-gradient(circle at 30% 30%, #FFFFFF, #E0E0E0, #A0A0A0);
box-shadow: 0 1px 4px rgba(0,0,0,0.6);
z-index: 65;

/* Framer Motion: */
/* animate={{ rotate: [0, -1080, -1800, -2100, -2160] }} */
/* transition={{ duration: spinDuration, times: [0, 0.3, 0.7, 0.9, 1] }} */
```

**Mesa desabilitada:**
```css
pointer-events: none; cursor: not-allowed; opacity: 0.7;
/* Chips visiveis, hover desligado */
```

**Chips desabilitados:**
```css
pointer-events: none; opacity: 0.3; filter: grayscale(80%);
```

**Botao GIRAR (estado spinning):**
```css
background: #333333; color: #666666;
box-shadow: none; cursor: not-allowed; pointer-events: none;
/* Texto: "GIRANDO..." */
/* Framer Motion pulse: animate={{ opacity: [1, 0.5, 1] }} repeat Infinity 1.5s */
```

**Botoes LIMPAR/REPETIR desabilitados:**
```css
pointer-events: none; opacity: 0.3; cursor: not-allowed;
```

**Sons:**
```
t=0.0s: spin_start.mp3 (300ms)
t=0.1s: wheel_spin.mp3 (loop, volume decresce)
t=0.1s: ball_spin.mp3 (loop, frequencia decresce)
t=70%:  ball_drop.mp3 (quique descendente)
t=90%:  ball_stop.mp3 (click seco)
t=100%: silencio 500ms dramatico
```

---

# TELA 5: RESULTADO - OVERLAY VITORIA/DERROTA

## Layout ASCII — Vitoria

```
+--------------------------------------------------------------+
|           (fundo blur + overlay semi-transparente)            |
|                                                              |
|                      +---------+                             |
|                      |   17    |                             |
|                      |  PRETO  |                             |
|                      +---------+                             |
|                                                              |
|                  VOCE GANHOU!                                 |
|                   G$ 3.700                                    |
|                                                              |
|       Straight Up 17: 35:1 = G$ 3.500                        |
|       Vermelho: 1:1 = G$ 200                                 |
|                                                              |
|            [GIRAR NOVAMENTE]    [VOLTAR]                      |
+--------------------------------------------------------------+
```

## Especificacoes Tela 5

**Overlay fundo:**
```css
position: absolute; inset: 0; z-index: 80;
background: rgba(0,0,0,0.7);
backdrop-filter: blur(8px);
display: flex; flex-direction: column;
align-items: center; justify-content: center;
gap: clamp(12px, 2vw, 24px);
padding: clamp(16px, 3vw, 32px);
/* AnimatePresence: initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} */
```

**Circulo numero resultado:**
```css
width: clamp(80px, 12vw, 140px); height: clamp(80px, 12vw, 140px);
border-radius: 50%;
display: flex; flex-direction: column;
align-items: center; justify-content: center;
/* Vermelho: background radial-gradient(circle, #DC2626, #8B1A1A), border 4px solid #FF6B6B */
/* Preto: background radial-gradient(circle, #2A2A2A, #0A0A0A), border 4px solid #666666 */
/* Verde: background radial-gradient(circle, #15803D, #0D5C2A), border 4px solid #22C55E */
box-shadow: 0 0 30px rgba(cor, 0.4), 0 0 60px rgba(cor, 0.15);

/* Numero: Cinzel Decorative 900 clamp(32px, 5vw, 56px) #FFFFFF */
/* Label cor: Inter 600 clamp(8px, 1vw, 12px) rgba(255,255,255,0.7) uppercase */
/* Framer Motion: initial={{ scale: 0.4, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} */
/* transition={{ duration: 0.5, ease: [0.34, 1.56, 0.64, 1] }} */
```

**Texto vitoria:**
```css
font: Cinzel 900 clamp(18px, 3vw, 36px); color: #00E676;
text-transform: uppercase; letter-spacing: 3px;
text-shadow: 0 0 20px rgba(0,230,118,0.5), 0 2px 8px rgba(0,0,0,1);
/* initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} delay 0.3 */
```

**Texto derrota:**
```css
font: Cinzel 700 clamp(16px, 2.5vw, 28px); color: rgba(255,255,255,0.5);
text-transform: uppercase; letter-spacing: 2px;
/* Shake: animate={{ x: [-3, 3, -2, 2, 0] }} duration 0.4 delay 0.3 */
```

**Rolling counter (ganho):**
```css
font: JetBrains Mono 700 clamp(24px, 4vw, 48px); color: #00E676;
text-shadow: 0 0 15px rgba(0,230,118,0.5);
/* Counter 0 -> valor final em 2s, desacelera ultimos 20% */
```

**Valor perda:**
```css
font: JetBrains Mono 700 clamp(18px, 3vw, 32px); color: rgba(255,59,59,0.6);
/* Sem counter, aparece direto com prefixo "- G$ " */
```

**Detalhamento apostas ganhas:**
```css
display: flex; flex-direction: column; gap: 4px;
font: Inter 400 clamp(10px, 1.2vw, 14px); color: #A8A8A8;
/* Formato: "Straight Up 17: 35:1 = G$ 3.500" */
/* Valor em verde #00E676 */
```

**Confetti (vitoria):**
```css
/* GoldParticles.tsx: mode="burst", count=30 */
/* colors=['#FFD700','#D4A843','#00E676','#DC2626'] */
/* Canvas overlay position absolute inset 0 pointer-events none z-index 85 */
```

**Botoes:**
```css
/* GIRAR NOVAMENTE: verde gradient #00E676->#00C853, Cinzel 700, 44px min */
/* Hover: brightness 1.15, translateY(-2px) */
/* VOLTAR: transparent, border rgba(212,168,67,0.3), color #D4A843 */
/* Hover: background rgba(212,168,67,0.08) */
/* Ambos: whileHover={{ scale: 1.03 }} whileTap={{ scale: 0.97 }} */
```

**Sons:**
```
Vitoria < 10x: win_small.mp3
Vitoria 10x-199x: win_big.mp3
Vitoria 200x+: win_mega.mp3 -> trigger BigWin (Tela 6)
Derrota: lose.mp3
```

---

**[FIM PARTE 3 - Continua Parte 4: TELA 6 (BigWin) + TELA 7 (Historico) + TELA 8 (Provably Fair)]**

`[ENTREGA: 3.PROMPT-AI-ROLETA-PARTE-3.md - 17/03/2026]`
