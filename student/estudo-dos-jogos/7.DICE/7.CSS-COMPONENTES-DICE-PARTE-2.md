# CSS COMPONENTES — DICE (#7)
## Parte 2 de 2 - Componentes 2.13-2.20 + Resumo
## Arquivo de referencia CSS para V0 / Bolt / ChatGPT
## Colar JUNTO com Parte 1 e PROMPT-V0-DICE
### NAO editar — CSS pronto para copiar
### Blackout Casino GTARP - 21/03/2026

---

## 2.13 Texto Titulo Cinzel Dourado (Ref: Adendo DS 1.13, DS P1 §3.1)

```css
/* Titulo padrao — subtitulos, labels de secao */
.dice-text__title {
  font-family: var(--dice-font-title);
  font-weight: 700;
  font-size: clamp(12px, 1.8vw, 22px);
  color: var(--dice-gold-primary);
  text-transform: uppercase;
  letter-spacing: clamp(1px, 0.2vw, 3px);
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);
}

/* Titulo grande — nome do jogo "DICE" no header */
.dice-text__title--large {
  font-weight: 900;
  font-size: clamp(16px, 2.5vw, 32px);
  color: var(--dice-gold-pure);
  text-shadow:
    0 0 20px rgba(255,215,0,0.8),
    0 4px 8px rgba(0,0,0,1);
  letter-spacing: clamp(2px, 0.4vw, 5px);
}

/* Titulo de modal — HISTORICO, PROVABLY FAIR, AUTO-BET */
.dice-text__title--modal {
  font-weight: 800;
  font-size: clamp(14px, 2vw, 22px);
  color: var(--dice-gold-pure);
  text-shadow:
    0 0 15px rgba(255,215,0,0.6),
    0 2px 6px rgba(0,0,0,1);
  text-align: center;
  letter-spacing: clamp(1px, 0.2vw, 3px);
  margin-bottom: clamp(12px, 1.5vw, 20px);
  padding-bottom: clamp(8px, 1vw, 14px);
  border-bottom: 1px solid rgba(212,168,67,0.2);
}

/* Titulo historico lateral — menor */
.dice-text__title--sidebar {
  font-family: var(--dice-font-label);
  font-weight: 700;
  font-size: clamp(10px, 1.2vw, 14px);
  color: var(--dice-gold-primary);
  text-transform: uppercase;
  letter-spacing: 2px;
  text-align: center;
  padding-bottom: clamp(4px, 0.4vw, 6px);
  border-bottom: 1px solid rgba(212,168,67,0.15);
}
```

---

## 2.14 Texto Metalico Gradiente (Ref: Adendo DS 1.14, DS P1 §3.2)

```css
/* Texto com gradiente metalico ouro — labels importantes como "TOTAL:", "LIGHTNING" */
.dice-text__metallic {
  font-family: var(--dice-font-label);
  font-weight: 800;
  font-size: clamp(10px, 1.4vw, 18px);
  background: linear-gradient(180deg,
    #F6F2C0 0%,
    #F6E27A 20%,
    #D4A843 40%,
    #CB9B51 60%,
    #8B6914 80%,
    #462523 100%
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-transform: uppercase;
  letter-spacing: clamp(1px, 0.2vw, 2px);
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.8));
}
```

---

## 2.15 Texto Valores Monetarios (Ref: Adendo DS 1.15, DS P1 §3.3)

```css
/* Valor GCoin padrao — saldo, aposta, ganho */
.dice-text__value {
  font-family: var(--dice-font-mono);
  font-weight: 700;
  color: var(--dice-green-neon);
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  letter-spacing: clamp(0.5px, 0.1vw, 1.5px);
}

/* Valor pequeno — payout "6:1" abaixo do numero */
.dice-text__value--small {
  font-size: clamp(8px, 1vw, 13px);
  font-weight: 600;
  color: var(--dice-text-secondary);
  text-shadow: none;
}

/* Valor medio — saldo no header */
.dice-text__value--medium {
  font-size: clamp(12px, 1.5vw, 20px);
}

/* Valor grande — win display */
.dice-text__value--large {
  font-size: clamp(18px, 3vw, 36px);
  text-shadow:
    0 0 15px rgba(0,230,118,0.7),
    0 0 30px rgba(0,230,118,0.3);
}

/* Valor extra-grande — countup big win overlay */
.dice-text__value--xl {
  font-weight: 900;
  font-size: clamp(24px, 5vw, 52px);
  text-shadow:
    0 0 20px rgba(0,230,118,0.7),
    0 0 40px rgba(0,230,118,0.3);
  animation: dice-countup-flash 0.5s ease infinite;
}

/* Valor negativo — perda */
.dice-text__value--loss {
  color: var(--dice-red-error);
  text-shadow: 0 0 8px rgba(255,59,59,0.4);
}

/* Valor zero — sem ganho */
.dice-text__value--zero {
  color: var(--dice-text-muted);
  text-shadow: none;
}
```

---

## 2.16 Texto Multiplicador (Ref: Adendo DS 1.16, DS P1 §3.4+§7)

```css
/* Multiplicador padrao — badge inline no grid */
.dice-text__multiplier {
  font-family: var(--dice-font-mono);
  font-weight: 900;
  font-size: clamp(14px, 2vw, 28px);
  color: var(--dice-gold-pure);
  text-shadow:
    0 0 15px rgba(255,215,0,0.8),
    0 0 30px rgba(255,215,0,0.4),
    0 0 60px rgba(255,215,0,0.15);
}

/* Multiplicador inline menor — dentro do card de total */
.dice-text__multiplier--inline {
  font-size: clamp(9px, 1.1vw, 14px);
  font-weight: 800;
}

/* Multiplicador grande — na barra Lightning Phase */
.dice-text__multiplier--phase {
  font-size: clamp(16px, 2.2vw, 28px);
  text-shadow:
    0 0 15px rgba(255,215,0,0.8),
    0 0 30px rgba(255,215,0,0.4);
}

/* Multiplicador big win — overlay */
.dice-text__multiplier--bigwin {
  font-size: clamp(28px, 5vw, 56px);
  text-shadow:
    0 0 25px rgba(255,215,0,0.9),
    0 0 50px rgba(255,215,0,0.5),
    0 0 100px rgba(255,215,0,0.2);
}

/* Multiplicador no historico — pequeno */
.dice-text__multiplier--history {
  font-size: clamp(8px, 0.9vw, 11px);
  font-weight: 800;
  color: var(--dice-gold-pure);
  text-shadow: 0 0 6px rgba(255,215,0,0.4);
}

/* Sem multiplicador — placeholder "--" */
.dice-text__multiplier--none {
  color: var(--dice-text-disabled);
  text-shadow: none;
  font-weight: 600;
}
```

---

## 2.17 Input de Aposta (Ref: Adendo DS 1.18, DS P2 §9)

```css
.dice-bet__input {
  width: clamp(80px, 12vw, 140px);
  background: var(--dice-black-sub);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  padding: clamp(6px, 0.8vw, 10px);
  color: var(--dice-green-neon);
  font-family: var(--dice-font-mono);
  font-weight: 700;
  font-size: clamp(12px, 1.5vw, 18px);
  text-align: center;
  outline: none;
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.dice-bet__input:focus {
  border-color: var(--dice-gold-primary);
  box-shadow: 0 0 10px rgba(212,168,67,0.3), inset 0 0 5px rgba(0,0,0,0.3);
}

.dice-bet__input:hover {
  border-color: rgba(212,168,67,0.6);
}

.dice-bet__input:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  color: var(--dice-text-muted);
}

.dice-bet__input::placeholder {
  color: var(--dice-text-muted);
  font-weight: 400;
}

/* Input de modal (client seed, rodadas, stop conditions) */
.dice-modal__input {
  background: var(--dice-black-sub);
  border: 1px solid rgba(212,168,67,0.4);
  border-radius: 8px;
  padding: clamp(8px, 1vw, 12px);
  color: var(--dice-green-neon);
  font-family: var(--dice-font-mono);
  font-size: clamp(10px, 1.2vw, 14px);
  width: 100%;
  outline: none;
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.dice-modal__input:focus {
  border-color: var(--dice-gold-primary);
  box-shadow: 0 0 10px rgba(212,168,67,0.3);
}

.dice-modal__input::placeholder {
  color: var(--dice-text-muted);
  font-weight: 400;
}

/* Input readonly — hash display */
.dice-modal__input--readonly {
  color: var(--dice-gold-primary);
  background: var(--dice-black-surface);
  border-color: rgba(212,168,67,0.2);
  word-break: break-all;
  cursor: default;
  font-size: clamp(9px, 1vw, 12px);
}
```

---

## 2.18 Dado 3D Cubo Completo (Ref: Adendo DS 1.19, Customizado)

```css
/* Container perspective para 1 dado */
.dice-cube__perspective {
  perspective: 800px;
  width: clamp(60px, 8vw, 120px);
  height: clamp(60px, 8vw, 120px);
  --dice-half: calc(clamp(60px, 8vw, 120px) / 2);
}

/* Cubo 3D (container das 6 faces) */
.dice-cube {
  transform-style: preserve-3d;
  width: 100%;
  height: 100%;
  position: relative;
}

/* Face individual do dado */
.dice-face {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 12px;
  background: linear-gradient(145deg, #1A1A1A, #0A0A0A);
  border: 1px solid rgba(212,168,67,0.3);
  box-shadow:
    inset 0 0 15px rgba(0,0,0,0.5),
    inset 0 1px 0 rgba(212,168,67,0.15),
    inset 0 -1px 0 rgba(0,0,0,0.4);
  backface-visibility: hidden;
}

/* Posicoes das 6 faces */
.dice-face--1 { transform: rotateY(0deg) translateZ(var(--dice-half)); }
.dice-face--2 { transform: rotateY(-90deg) translateZ(var(--dice-half)); }
.dice-face--3 { transform: rotateX(90deg) translateZ(var(--dice-half)); }
.dice-face--4 { transform: rotateX(-90deg) translateZ(var(--dice-half)); }
.dice-face--5 { transform: rotateY(90deg) translateZ(var(--dice-half)); }
.dice-face--6 { transform: rotateY(180deg) translateZ(var(--dice-half)); }

/* Ponto do dado (branco com glow) */
.dice-dot {
  position: absolute;
  width: 22%;
  aspect-ratio: 1;
  border-radius: 50%;
  background: var(--dice-white);
  box-shadow:
    0 0 6px rgba(255,255,255,0.8),
    0 0 12px rgba(255,255,255,0.3),
    inset 0 -1px 2px rgba(0,0,0,0.2);
  transform: translate(-50%, -50%);
}

/* FACE 1: 1 ponto centralizado */
.dice-face--1 .dice-dot { left: 50%; top: 50%; width: 28%; }

/* FACE 2: 2 pontos diagonal */
.dice-face--2 .dice-dot:nth-child(1) { left: 72%; top: 28%; }
.dice-face--2 .dice-dot:nth-child(2) { left: 28%; top: 72%; }

/* FACE 3: 3 pontos diagonal */
.dice-face--3 .dice-dot:nth-child(1) { left: 72%; top: 22%; }
.dice-face--3 .dice-dot:nth-child(2) { left: 50%; top: 50%; }
.dice-face--3 .dice-dot:nth-child(3) { left: 28%; top: 78%; }

/* FACE 4: 4 pontos nos cantos */
.dice-face--4 .dice-dot:nth-child(1) { left: 30%; top: 30%; }
.dice-face--4 .dice-dot:nth-child(2) { left: 70%; top: 30%; }
.dice-face--4 .dice-dot:nth-child(3) { left: 30%; top: 70%; }
.dice-face--4 .dice-dot:nth-child(4) { left: 70%; top: 70%; }

/* FACE 5: 4 cantos + 1 centro */
.dice-face--5 .dice-dot:nth-child(1) { left: 28%; top: 28%; }
.dice-face--5 .dice-dot:nth-child(2) { left: 72%; top: 28%; }
.dice-face--5 .dice-dot:nth-child(3) { left: 50%; top: 50%; }
.dice-face--5 .dice-dot:nth-child(4) { left: 28%; top: 72%; }
.dice-face--5 .dice-dot:nth-child(5) { left: 72%; top: 72%; }

/* FACE 6: 2 colunas de 3 pontos */
.dice-face--6 .dice-dot:nth-child(1) { left: 32%; top: 22%; }
.dice-face--6 .dice-dot:nth-child(2) { left: 68%; top: 22%; }
.dice-face--6 .dice-dot:nth-child(3) { left: 32%; top: 50%; }
.dice-face--6 .dice-dot:nth-child(4) { left: 68%; top: 50%; }
.dice-face--6 .dice-dot:nth-child(5) { left: 32%; top: 78%; }
.dice-face--6 .dice-dot:nth-child(6) { left: 68%; top: 78%; }

/* Face hover (idle, mouse sobre dado) */
.dice-face:hover {
  border-color: rgba(212,168,67,0.5);
  box-shadow:
    inset 0 0 20px rgba(0,0,0,0.4),
    inset 0 1px 0 rgba(212,168,67,0.2),
    0 0 10px rgba(212,168,67,0.1);
}

/* Dado estado won (resultado acertado) */
.dice-cube--won .dice-face {
  border-color: rgba(0,230,118,0.5);
  box-shadow:
    inset 0 0 15px rgba(0,0,0,0.5),
    inset 0 1px 0 rgba(0,230,118,0.15),
    0 0 15px rgba(0,230,118,0.2);
}

/* Dado estado lost (resultado errado) */
.dice-cube--lost .dice-face {
  opacity: 0.6;
  filter: grayscale(0.3);
}

/* Sombra projetada abaixo do dado apos landing */
.dice-cube__shadow {
  position: absolute;
  bottom: -8px;
  left: 10%;
  width: 80%;
  height: 6px;
  background: radial-gradient(ellipse, rgba(0,0,0,0.5) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(3px);
}
```

---

## 2.19 Lightning Badge Multiplicador (Ref: Adendo DS 1.20, DS P2 §10)

```css
/* Badge no canto do card no grid */
.dice-lightning__badge {
  position: absolute;
  top: -4px;
  right: -4px;
  z-index: 5;
  background: linear-gradient(135deg, #FFD700 0%, #D4A843 50%, #CB9B51 100%);
  border: 1px solid rgba(255,215,0,0.8);
  border-radius: var(--dice-radius-sm);
  padding: clamp(2px, 0.3vw, 4px) clamp(4px, 0.6vw, 8px);
  box-shadow:
    0 0 10px rgba(255,215,0,0.5),
    0 0 20px rgba(255,215,0,0.2),
    0 2px 6px rgba(0,0,0,0.6);
  animation: dice-badge-pulse 2s ease-in-out infinite;
}

.dice-lightning__badge-text {
  font-family: var(--dice-font-mono);
  font-weight: 900;
  font-size: clamp(9px, 1.1vw, 14px);
  color: var(--dice-black-bg);
  text-shadow: none;
  line-height: 1;
}

/* Badge na barra Lightning Phase (maior) */
.dice-lightning__phase-badge {
  display: flex;
  align-items: center;
  gap: clamp(4px, 0.5vw, 8px);
  padding: clamp(4px, 0.5vw, 8px) clamp(8px, 1vw, 14px);
  background: linear-gradient(135deg, rgba(255,215,0,0.15), rgba(68,136,255,0.1));
  border: var(--dice-border-lightning);
  border-radius: 8px;
  box-shadow:
    0 0 15px rgba(255,215,0,0.3),
    0 0 30px rgba(68,136,255,0.15);
}

.dice-lightning__phase-number {
  font-family: var(--dice-font-label);
  font-weight: 700;
  font-size: clamp(14px, 1.8vw, 22px);
  color: var(--dice-white);
  text-shadow: 0 0 8px rgba(255,255,255,0.5);
}

.dice-lightning__phase-multiplier {
  font-family: var(--dice-font-mono);
  font-weight: 900;
  font-size: clamp(16px, 2.2vw, 28px);
  color: var(--dice-gold-pure);
  text-shadow:
    0 0 15px rgba(255,215,0,0.8),
    0 0 30px rgba(255,215,0,0.4);
}

/* Barra container da Lightning Phase */
.dice-lightning__phase {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: clamp(12px, 2vw, 24px);
  padding: clamp(8px, 1vw, 14px) clamp(12px, 1.5vw, 20px);
  background: rgba(0,0,0,0.6);
  border-radius: 12px;
  border: 1px solid rgba(68,136,255,0.3);
  box-shadow:
    0 0 20px rgba(68,136,255,0.15),
    inset 0 0 30px rgba(0,0,0,0.5);
  min-height: clamp(40px, 5vw, 60px);
}
```

---

## 2.20 Item do Historico + Scrollbar (Ref: Adendo DS 1.21, DS P2 §13+§14)

```css
/* Container do historico lateral */
.dice-history {
  flex: 1 1 35%;
  max-width: clamp(180px, 25vw, 300px);
  background: rgba(0,0,0,0.4);
  border-radius: var(--dice-radius-lg);
  border: var(--dice-border-subtle);
  padding: clamp(6px, 0.8vw, 12px);
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: clamp(4px, 0.4vw, 6px);
}

/* Scrollbar customizada */
.dice-history {
  scrollbar-width: thin;
  scrollbar-color: rgba(212,168,67,0.3) transparent;
}

.dice-history::-webkit-scrollbar {
  width: 4px;
}

.dice-history::-webkit-scrollbar-track {
  background: transparent;
}

.dice-history::-webkit-scrollbar-thumb {
  background: rgba(212,168,67,0.3);
  border-radius: 4px;
}

.dice-history::-webkit-scrollbar-thumb:hover {
  background: rgba(212,168,67,0.5);
}

/* Item individual (1 rodada) */
.dice-history__item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: clamp(4px, 0.5vw, 8px) clamp(6px, 0.8vw, 10px);
  border-radius: var(--dice-radius-sm);
  background: rgba(0,0,0,0.2);
  transition: background 0.2s ease;
  gap: clamp(4px, 0.4vw, 6px);
}

.dice-history__item:hover {
  background: rgba(212,168,67,0.06);
}

/* Item com multiplicador */
.dice-history__item--multiplier {
  border-left: 2px solid rgba(255,215,0,0.5);
  background: rgba(255,215,0,0.03);
}

/* Item com win */
.dice-history__item--won {
  border-left: 2px solid rgba(0,230,118,0.4);
}

/* Item com loss */
.dice-history__item--lost {
  opacity: 0.7;
}

/* Numero da rodada */
.dice-history__round {
  font-family: var(--dice-font-mono);
  font-weight: 600;
  font-size: clamp(8px, 0.9vw, 11px);
  color: var(--dice-text-muted);
  min-width: clamp(20px, 3vw, 32px);
}

/* Total da rodada */
.dice-history__total {
  font-family: var(--dice-font-mono);
  font-weight: 700;
  font-size: clamp(9px, 1vw, 12px);
  color: var(--dice-gold-primary);
  min-width: clamp(24px, 3vw, 36px);
}

/* Multiplicador inline no historico */
.dice-history__mult {
  font-family: var(--dice-font-mono);
  font-weight: 800;
  font-size: clamp(8px, 0.9vw, 11px);
  color: var(--dice-gold-pure);
  text-shadow: 0 0 6px rgba(255,215,0,0.4);
  min-width: clamp(24px, 3vw, 36px);
}

.dice-history__mult--none {
  color: var(--dice-text-disabled);
  text-shadow: none;
}

/* Ganho/perda */
.dice-history__profit {
  font-family: var(--dice-font-mono);
  font-weight: 700;
  font-size: clamp(9px, 1vw, 12px);
  text-align: right;
}

.dice-history__profit--positive {
  color: var(--dice-green-neon);
  text-shadow: 0 0 6px rgba(0,230,118,0.3);
}

.dice-history__profit--negative {
  color: var(--dice-red-error);
  text-shadow: 0 0 6px rgba(255,59,59,0.3);
}

.dice-history__profit--zero {
  color: var(--dice-text-muted);
  text-shadow: none;
}
```

---

# 3. RESUMO DOS 20 COMPONENTES

| # | Componente | Ref DS | Parte | Estados |
|---|-----------|:------:|:-----:|---------|
| 2.1 | Fundo do jogo | P1 §4.1+§6 | P1 | normal, lightning, bigwin |
| 2.2 | Container area | P1 §4.2 | P1 | normal, hover |
| 2.3 | Card grid apostas | P1 §4.4 | P1 | normal, hover, selected, lightning, won, lost, disabled |
| 2.4 | Feixo verde base | Guia §2.3.3 | P1 | normal, hover |
| 2.5 | Linha dourada topo | Guia §2.3.3 | P1 | normal, hover |
| 2.6 | Shine sweep hover | P1 §4.4 | P1 | normal, hover animado |
| 2.7 | Botao ROLAR DADOS | P2 §8.1 | P1 | normal, hover, active, idle pulse, disabled |
| 2.8 | Botao atalhos | P2 §8.2 | P1 | normal, hover, active, disabled |
| 2.9 | Botao voltar | P2 §8.3 | P1 | normal, hover, active |
| 2.10 | Botao ghost + icon | P2 §8.4 | P1 | normal, hover, active + imagem filter |
| 2.11 | Glassmorphism modal | P2 §12 | P1 | backdrop, content, titulo, close com hover |
| 2.12 | Tooltip | P2 §11 | P1 | hidden, visible |
| 2.13 | Texto titulo | P1 §3.1 | P2 | 4 variantes: padrao, large, modal, sidebar |
| 2.14 | Texto metalico | P1 §3.2 | P2 | gradiente 6 stops |
| 2.15 | Texto valores | P1 §3.3 | P2 | 6 variantes: padrao, small, medium, large, xl, loss, zero |
| 2.16 | Texto multiplicador | P1 §3.4 | P2 | 5 variantes: padrao, inline, phase, bigwin, history, none |
| 2.17 | Input aposta | P2 §9 | P2 | normal, focus, hover, disabled, placeholder + modal input + readonly |
| 2.18 | Dado 3D cubo | Customizado | P2 | 6 faces posicionadas, 6 layouts pontos, hover, won, lost, shadow |
| 2.19 | Lightning badge | P2 §10 | P2 | badge grid com pulse, badge phase, barra container |
| 2.20 | Item historico | P2 §13+§14 | P2 | normal, hover, multiplier, won, lost + scrollbar customizada |

**20 componentes, todos com CSS real copiavel, §ref do DS, BEM naming `.dice-*`, e todos os estados documentados.**

---

**[FIM — CSS-COMPONENTES DICE #7 COMPLETO (2 PARTES)]**

`[ENTREGA: 7.CSS-COMPONENTES-DICE-PARTE-2.md — 21/03/2026]`
