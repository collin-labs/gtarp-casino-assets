# PROMPT AI — DICE (#7)
## PARTE 1 DE 4 — Header Tecnico, Paleta, Tipografia, Tela 1 DiceGame Idle
### Prompt para gerar visual pixel-perfect em V0 / Bolt / Claude / ChatGPT
### Blackout Casino GTARP - 21/03/2026

---

## PLANO DE ACAO

- **Template seguido:** sec5 do Padrao de Estudo Individual (Template Prompt AI)
- **Gold Standard consultado:** Prompt AI Slot-Machine #2 (4 partes, 2.369 linhas), Prompt AI Poker #6 (4 partes, 2.529 linhas)
- **Benchmark de linhas:** 1.500-2.300 linhas (4 partes de ~400L)
- **Telas previstas:**
  1. DiceGame Idle (tela principal completa: 3 dados, grid apostas, controles, historico)
  2. Lightning Phase (multiplicadores sendo atribuidos com efeito de raio)
  3. Rolling + Landing (dados girando em 3D e pousando)
  4. Result (total revelado, apostas avaliadas, payout calculado)
  5. Big Win Overlay (multiplicador acertado, confetti, fanfarra)
  6. History Overlay (lista de rodadas com dados, totais, multiplicadores)
  7. Provably Fair Modal (seeds, hash, verificacao)
  8. Auto-bet Modal (configuracao de rodadas automaticas)

---

# HEADER TECNICO (Colar no inicio do prompt para qualquer IA)

```
RESTRICOES TECNICAS:
- React 19 + TypeScript + Framer Motion 11 + Next.js 16
- Container: div com position absolute, inset 0, z-index 60, borderRadius inherit, overflow hidden
- SOMENTE dark mode — fundo NUNCA branco, NUNCA cinza claro
- CSS inline via style={{}} — NAO usar classes Tailwind para componentes de jogo
- Dados 3D: CSS preserve-3d com 6 divs filhas (rotateX/Y + translateZ), perspective 800px no container
- Dado face: div position absolute, width 100%, height 100%, background gradiente escuro, border-radius 12px
- Dado ponto: span position absolute, border-radius 50%, background #FFFFFF, box-shadow glow
- Animacao roll: Framer Motion animate rotateX/Y/Z com spring (stiffness 80, damping 15)
- Resultado server-side — client NUNCA calcula resultado, APENAS anima dados recebidos
- Angulos finais: mapeamento face->rotateX/Y calculado + N voltas aleatorias (720-1440deg)
- Stagger dados: 200ms entre cada dado parar
- DOM para: dados 3D, grid apostas, chips, overlays, modais, historico — NAO Canvas para dados
- Fontes: Cinzel Decorative (titulos), Inter (body), JetBrains Mono (valores, bet, multiplicadores)
- Responsividade: TUDO com clamp() — nunca px fixo para fontes/padding/sizes
- Mobile: LANDSCAPE natural NUI (1280x720). Desktop: mesmo layout responsivo
- Touch targets: minimo 48x48px em TODOS elementos interativos
- Bilingue: prop lang="br"|"en", todos os textos via objeto { br: "...", en: "..." }
- Sons: useSoundManager() hook — 18 sons mapeados por evento
- FiveM NUI: Chromium CEF (suporta CSS 3D preserve-3d, AnimatePresence, backdrop-filter, clamp)
- Chips de aposta: PNG existentes em /assets/roulette/chips/ (chip-1 a chip-1000)
- Lightning: CSS @keyframes flash + box-shadow pulsante + badge multiplicador
- Provably Fair: HMAC-SHA256 server-side, hash publicado ANTES do roll, seed revelado APOS
- Confetti: reutilizar GoldParticles.tsx existente, pool max 30 particulas
- ZERO dependencias novas (Framer Motion ja no projeto)
```

---

# PALETA BASE — BLACKOUT CASINO (OBRIGATORIA)

```
BACKGROUNDS:
  #050505   — fundo absoluto (atras de tudo)
  #0A0A0A   — fundo do jogo (background-color principal)
  #0F0F0F   — fundo do painel
  #141414   — card, container
  #1A1A1A   — sub-container, input inativo
  #222222   — hover state, item selecionado
  #2A2A2A   — tooltip, dropdown

OURO METALICO (bordas, titulos, moldura, ornamentos):
  #462523   — sombra profunda do metal
  #8B6914   — ouro escuro
  #CB9B51   — ouro medio
  #D4A843   — ouro principal (COR MAIS USADA — texto, borda, UI)
  #F6E27A   — ouro claro (highlight)
  #F6F2C0   — ponto de brilho maximo
  #FFD700   — ouro puro (hover, accent, CTA)

VERDE ESMERALDA (win, ativo, CTA, saldo, acertos):
  #00E676   — verde brilhante (accent principal)
  #00C853   — verde forte (botao primario CTA)
  #004D25   — verde escuro (base botao)

VERMELHO (perda, erro):
  #FF1744   — vermelho vibrante
  #FF3B3B   — vermelho de erro

TEXTO:
  #FFFFFF   — texto principal
  #A8A8A8   — texto secundario
  #666666   — texto inativo/placeholder
```

# PALETA ESPECIFICA — DICE

```
DADOS 3D:
  #0A0A0A → #1A1A1A   — gradiente das faces do dado (escuro)
  rgba(212,168,67,0.3) — borda das faces (dourada sutil)
  #FFFFFF              — pontos do dado (branco puro)
  rgba(255,255,255,0.8)— glow dos pontos (box-shadow)
  rgba(212,168,67,0.15)— inner glow das faces (inset box-shadow)

LIGHTNING:
  #4488FF   — azul eletrico (raio primario)
  #66AAFF   — azul claro (raio secundario)
  #FFFFFF   — flash de raio (branco puro, opacity 0→1→0)
  #FFD700   — badge multiplicador (ouro puro)
  rgba(68,136,255,0.3) — glow do raio (box-shadow)
  rgba(255,215,0,0.4)  — glow do badge multiplicador

GRID DE APOSTAS:
  #141414   — fundo do card de total (normal)
  #1A1A1A   — fundo do card hover
  rgba(212,168,67,0.3) — borda normal
  rgba(212,168,67,0.7) — borda hover
  rgba(0,230,118,0.5)  — borda selecionado (aposta feita)
  rgba(0,230,118,0.15) — fundo selecionado
  #00E676   — texto do total acertado (resultado)
  rgba(255,255,255,0.4)— texto do total errado (fade)

ESTADOS:
  idle     — dados parados, glow dourado sutil, grid normal
  lightning— grid escurece, numeros com multiplicador brilham com raio azul/dourado
  shaking  — dados tremem, bordas piscam dourado↔verde
  rolling  — dados giram rapido, blur de movimento
  landing  — dados desaceleram, bounce no impacto, flash de luz
  result   — total aparece, grid reage (verde acerto, fade erro)
  bigwin   — backdrop blur, confetti, valor pulsante dourado
```

---

# TIPOGRAFIA

```
PRIMARIA — TITULOS E LABELS:
  font-family: 'Cinzel Decorative', 'Cinzel', 'Playfair Display', serif;
  font-weight: 700 a 900;
  text-transform: uppercase;
  letter-spacing: clamp(1px, 0.2vw, 3px);
  color: #D4A843;
  text-shadow: 0 0 12px rgba(255,215,0,0.6), 0 2px 6px rgba(0,0,0,1);

SECUNDARIA — CORPO E DESCRICOES:
  font-family: 'Inter', 'Segoe UI', system-ui, sans-serif;
  font-weight: 400 a 600;
  color: #A8A8A8 (secundario) ou #FFFFFF (principal);
  font-size: clamp(10px, 1.2vw, 16px);
  line-height: 1.4;

MONO — VALORES, BET, SALDO, MULTIPLICADORES:
  font-family: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
  font-weight: 700;
  color: #00E676 (valores GCoin);
  text-shadow: 0 0 10px rgba(0,230,118,0.5);
  letter-spacing: clamp(0.5px, 0.1vw, 1.5px);

MULTIPLICADOR — BADGE LIGHTNING:
  font-family: 'JetBrains Mono', monospace;
  font-weight: 900;
  color: #FFD700;
  text-shadow: 0 0 15px rgba(255,215,0,0.8), 0 0 30px rgba(255,215,0,0.4);
  font-size: clamp(14px, 2vw, 28px);

TITULO DO JOGO:
  font-family: 'Cinzel Decorative', serif;
  font-weight: 900;
  font-size: clamp(16px, 2.5vw, 32px);
  color: #FFD700;
  text-shadow: 0 0 20px rgba(255,215,0,0.8), 0 4px 8px rgba(0,0,0,1);
  letter-spacing: clamp(2px, 0.4vw, 5px);
```

---

# TELA 1: DICE GAME — IDLE (Estado: aguardando apostas)

## Layout ASCII

```
+====================================================================+
|  [< VOLTAR]          DICE          [Som] [PF]      GCoin: G$5.000  |
|--------------------------------------------------------------------|
|                                                                    |
|  +-------------------------------+   +-------------------------+   |
|  |                               |   |      HISTORICO          |   |
|  |    +-----+  +-----+  +-----+ |   |  #42  T:12  --   G$0   |   |
|  |    | [3] |  | [5] |  | [2] | |   |  #41  T:7   x100 G$1.2K|   |
|  |    +-----+  +-----+  +-----+ |   |  #40  T:15  x250 G$0   |   |
|  |                               |   |  #39  T:9   --   G$0   |   |
|  |       === TOTAL: -- ===       |   |  #38  T:11  x5   G$150 |   |
|  |                               |   |  ...                   |   |
|  +-------------------------------+   +-------------------------+   |
|                                                                    |
|  +-LIGHTNING PHASE----------------------------------------------+  |
|  |  (vazio — aparece apos ROLAR com multiplicadores)            |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +-GRID DE APOSTAS----------------------------------------------+  |
|  |  [ 3 ]  [ 4 ]  [ 5 ]  [ 6 ]  [ 7 ]  [ 8 ]  [ 9 ]  [10 ]  |  |
|  | 150:1   60:1   30:1   17:1   12:1    8:1    7:1    6:1     |  |
|  |                                                              |  |
|  |  [11 ]  [12 ]  [13 ]  [14 ]  [15 ]  [16 ]  [17 ]  [18 ]   |  |
|  |  6:1    7:1    8:1    12:1   17:1   30:1   60:1   150:1    |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--------------------------------------------------------------+  |
|  |  [ LOW (3-10) 1:1 ]  |  [ HIGH (11-18) 1:1 ]  | [TRIPLE 30:1]|  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  Aposta: [___G$100___] [MIN][x2][/2][MAX]   [ ROLAR DADOS ]      |
|                                                                    |
|  [Historico]  [Provably Fair]  [Auto Bet]     TOTAL: G$0          |
+====================================================================+
```

## Especificacoes Tela 1

### Container Principal
```css
/* DiceGame.tsx — container que ocupa todo o painel */
style={{
  position: 'absolute',
  inset: 0,
  zIndex: 60,
  borderRadius: 'inherit',
  overflow: 'hidden',
  backgroundImage: 'url(/assets/ui/FUNDO-PARA-PAINEL-FLUTUANTE/bg-casino.png)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  display: 'flex',
  flexDirection: 'column',
  padding: 'clamp(8px, 1vw, 16px)',
  fontFamily: "'Inter', 'Segoe UI', system-ui, sans-serif",
}}
```

### Header (Voltar + Titulo + Saldo)
```css
/* Header bar — topo do jogo */
style={{
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  padding: 'clamp(6px, 0.8vw, 12px) clamp(8px, 1vw, 16px)',
  borderBottom: '1px solid rgba(212,168,67,0.2)',
  marginBottom: 'clamp(4px, 0.5vw, 8px)',
}}

/* Botao VOLTAR */
style={{
  background: 'rgba(0,0,0,0.6)',
  border: '1px solid rgba(212,168,67,0.4)',
  borderRadius: '8px',
  color: '#D4A843',
  fontFamily: "'Cinzel', serif",
  fontSize: 'clamp(10px, 1.2vw, 16px)',
  letterSpacing: '2px',
  padding: 'clamp(4px, 0.5vw, 8px) clamp(8px, 1vw, 16px)',
  cursor: 'pointer',
  textTransform: 'uppercase',
}}
/* Framer Motion: */
whileHover={{ scale: 1.05, borderColor: 'rgba(212,168,67,0.8)' }}
whileTap={{ scale: 0.95 }}

/* Titulo "DICE" */
style={{
  fontFamily: "'Cinzel Decorative', serif",
  fontWeight: 900,
  fontSize: 'clamp(16px, 2.5vw, 32px)',
  color: '#FFD700',
  textShadow: '0 0 20px rgba(255,215,0,0.8), 0 4px 8px rgba(0,0,0,1)',
  letterSpacing: 'clamp(2px, 0.4vw, 5px)',
  textTransform: 'uppercase',
}}

/* Saldo GCoin */
style={{
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(12px, 1.5vw, 20px)',
  color: '#00E676',
  textShadow: '0 0 10px rgba(0,230,118,0.5)',
}}
```

### Area Central (Dados + Historico)
```css
/* Container flex: area de dados esquerda + historico direita */
style={{
  display: 'flex',
  flex: 1,
  gap: 'clamp(8px, 1vw, 16px)',
  minHeight: 0,
}}

/* Area dos dados (lado esquerdo, ~65% width) */
style={{
  flex: '1 1 65%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  background: 'rgba(0,0,0,0.3)',
  borderRadius: '14px',
  border: '1.5px solid rgba(212,168,67,0.3)',
  outline: '1px solid rgba(191,149,63,0.1)',
  outlineOffset: '3px',
  boxShadow: '0 4px 15px rgba(0,0,0,0.6), 0 1px 3px rgba(0,0,0,0.8), inset 0 1px 0 rgba(255,215,0,0.08), inset 0 0 15px rgba(0,0,0,0.4)',
  padding: 'clamp(12px, 1.5vw, 24px)',
  position: 'relative',
  overflow: 'hidden',
}}
```

### Dado 3D Individual
```css
/* Container perspective para 1 dado */
style={{
  perspective: '800px',
  width: 'clamp(60px, 8vw, 120px)',
  height: 'clamp(60px, 8vw, 120px)',
}}

/* Cubo 3D (container das 6 faces) */
style={{
  transformStyle: 'preserve-3d',
  width: '100%',
  height: '100%',
  position: 'relative',
}}
/* Framer Motion idle (leve oscilacao): */
animate={{ rotateX: [-2, 2, -2], rotateY: [-3, 3, -3] }}
transition={{ repeat: Infinity, duration: 4, ease: 'easeInOut' }}

/* Face do dado (6 divs, ex: face frontal = valor 1) */
style={{
  position: 'absolute',
  width: '100%',
  height: '100%',
  borderRadius: '12px',
  background: 'linear-gradient(145deg, #1A1A1A, #0A0A0A)',
  border: '1px solid rgba(212,168,67,0.3)',
  boxShadow: 'inset 0 0 15px rgba(0,0,0,0.5), inset 0 1px 0 rgba(212,168,67,0.15)',
  display: 'flex',
  flexWrap: 'wrap',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '15%',
  gap: '10%',
  backfaceVisibility: 'hidden',
}}

/* POSICOES DAS 6 FACES (translateZ = metade do tamanho do dado): */
/* Face 1 (frente):  transform: rotateY(0deg) translateZ(Npx)      */
/* Face 2 (esquerda): transform: rotateY(-90deg) translateZ(Npx)   */
/* Face 3 (topo):    transform: rotateX(90deg) translateZ(Npx)     */
/* Face 4 (baixo):   transform: rotateX(-90deg) translateZ(Npx)    */
/* Face 5 (direita): transform: rotateY(90deg) translateZ(Npx)     */
/* Face 6 (tras):    transform: rotateY(180deg) translateZ(Npx)    */
/* Onde N = metade de clamp(60px, 8vw, 120px) */

/* Ponto do dado (branco com glow) */
style={{
  width: '22%',
  aspectRatio: '1',
  borderRadius: '50%',
  background: '#FFFFFF',
  boxShadow: '0 0 6px rgba(255,255,255,0.8), 0 0 12px rgba(255,255,255,0.3)',
}}

/* Layout dos pontos por face:
   Face 1: 1 ponto centralizado (justify-content: center, align-items: center, sem gap)
   Face 2: 2 pontos diagonal (top-right + bottom-left)
   Face 3: 3 pontos diagonal (top-right + center + bottom-left)
   Face 4: 4 pontos cantos (grid 2x2)
   Face 5: 4 cantos + 1 centro
   Face 6: 2 colunas de 3 pontos
*/
```

### Display Total (abaixo dos dados)
```css
/* "=== TOTAL: 12 ===" — aparece apos resultado */
style={{
  fontFamily: "'Cinzel Decorative', serif",
  fontWeight: 900,
  fontSize: 'clamp(20px, 3vw, 40px)',
  color: '#FFD700',
  textShadow: '0 0 20px rgba(255,215,0,0.8), 0 4px 8px rgba(0,0,0,1)',
  textAlign: 'center',
  marginTop: 'clamp(8px, 1vw, 16px)',
  letterSpacing: 'clamp(2px, 0.3vw, 4px)',
  textTransform: 'uppercase',
  opacity: 0, /* visivel apenas apos resultado */
}}
/* Framer Motion reveal: */
initial={{ opacity: 0, scale: 0.5 }}
animate={{ opacity: 1, scale: 1 }}
transition={{ type: 'spring', stiffness: 300, damping: 20 }}
```

### Grid de Apostas (Totais 3-18)
```css
/* Container do grid */
style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(8, 1fr)',
  gap: 'clamp(4px, 0.5vw, 8px)',
  padding: 'clamp(6px, 0.8vw, 12px)',
}}

/* Card individual de total (ex: total 7, payout 12:1) */
style={{
  background: '#141414',
  border: '1.5px solid rgba(212,168,67,0.3)',
  borderRadius: '10px',
  padding: 'clamp(6px, 0.8vw, 12px) clamp(4px, 0.5vw, 8px)',
  textAlign: 'center',
  cursor: 'pointer',
  position: 'relative',
  overflow: 'hidden',
  boxShadow: '0 2px 8px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,215,0,0.05)',
  transition: 'border-color 0.2s, background 0.2s',
}}
/* Framer Motion: */
whileHover={{ scale: 1.05, borderColor: 'rgba(212,168,67,0.7)' }}
whileTap={{ scale: 0.95 }}

/* Numero do total */
style={{
  fontFamily: "'Cinzel', serif",
  fontWeight: 700,
  fontSize: 'clamp(14px, 1.8vw, 24px)',
  color: '#D4A843',
  textShadow: '0 0 8px rgba(255,215,0,0.4)',
}}

/* Payout abaixo do numero */
style={{
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 600,
  fontSize: 'clamp(8px, 1vw, 13px)',
  color: '#A8A8A8',
  marginTop: 'clamp(2px, 0.2vw, 4px)',
}}

/* ESTADO: Selecionado (aposta feita) — adicionar: */
style={{
  ...base,
  borderColor: 'rgba(0,230,118,0.5)',
  background: 'rgba(0,230,118,0.08)',
  boxShadow: '0 0 12px rgba(0,230,118,0.2), 0 2px 8px rgba(0,0,0,0.5)',
}}

/* ESTADO: Lightning ativo (multiplicador neste total) — adicionar: */
style={{
  ...base,
  borderColor: 'rgba(255,215,0,0.8)',
  background: 'rgba(255,215,0,0.05)',
  boxShadow: '0 0 20px rgba(68,136,255,0.3), 0 0 10px rgba(255,215,0,0.3)',
  animation: 'lightning-pulse 1.5s ease-in-out infinite',
}}

/* ESTADO: Resultado acertou — adicionar: */
style={{
  ...base,
  borderColor: '#00E676',
  background: 'rgba(0,230,118,0.15)',
  boxShadow: '0 0 25px rgba(0,230,118,0.4)',
}}

/* ESTADO: Resultado errou — adicionar: */
style={{
  ...base,
  opacity: 0.4,
  transition: 'opacity 2s ease',
}}

/* @keyframes para lightning pulse */
/* @keyframes lightning-pulse {
  0%, 100% { box-shadow: 0 0 15px rgba(68,136,255,0.2), 0 0 8px rgba(255,215,0,0.2); }
  50% { box-shadow: 0 0 30px rgba(68,136,255,0.5), 0 0 15px rgba(255,215,0,0.4); }
} */
```

### Apostas Extras (LOW / HIGH / TRIPLE)
```css
/* Container flex horizontal abaixo do grid */
style={{
  display: 'flex',
  gap: 'clamp(6px, 0.8vw, 12px)',
  justifyContent: 'center',
  padding: 'clamp(4px, 0.5vw, 8px) 0',
}}

/* Botao LOW/HIGH */
style={{
  flex: '1 1 0',
  maxWidth: 'clamp(120px, 20vw, 200px)',
  background: '#141414',
  border: '1.5px solid rgba(212,168,67,0.3)',
  borderRadius: '10px',
  padding: 'clamp(8px, 1vw, 14px)',
  textAlign: 'center',
  cursor: 'pointer',
  boxShadow: '0 2px 8px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,215,0,0.05)',
}}
/* Estados identicos ao card de total (selecionado, hover, etc) */

/* Botao TRIPLE */
style={{
  ...mesmoQueAcima,
  maxWidth: 'clamp(100px, 15vw, 160px)',
}}
```

### Controles de Aposta (Input + Atalhos + ROLAR DADOS)
```css
/* Barra de controles na base */
style={{
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  gap: 'clamp(6px, 0.8vw, 12px)',
  padding: 'clamp(8px, 1vw, 14px)',
  borderTop: '1px solid rgba(212,168,67,0.15)',
  marginTop: 'auto',
}}

/* Input de aposta */
style={{
  width: 'clamp(80px, 12vw, 140px)',
  background: '#1A1A1A',
  border: '1px solid rgba(212,168,67,0.4)',
  borderRadius: '8px',
  padding: 'clamp(6px, 0.8vw, 10px)',
  color: '#00E676',
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 700,
  fontSize: 'clamp(12px, 1.5vw, 18px)',
  textAlign: 'center',
  outline: 'none',
}}
/* focus: borderColor #D4A843, boxShadow 0 0 10px rgba(212,168,67,0.3) */

/* Botoes atalho MIN/x2/÷2/MAX */
style={{
  background: 'rgba(212,168,67,0.1)',
  border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '6px',
  padding: 'clamp(4px, 0.5vw, 8px) clamp(6px, 0.8vw, 12px)',
  color: '#D4A843',
  fontFamily: "'JetBrains Mono', monospace",
  fontWeight: 600,
  fontSize: 'clamp(9px, 1.1vw, 14px)',
  cursor: 'pointer',
  minWidth: '44px',
  minHeight: '44px',
}}
whileHover={{ background: 'rgba(212,168,67,0.2)', borderColor: 'rgba(212,168,67,0.6)' }}
whileTap={{ scale: 0.92 }}

/* Botao ROLAR DADOS — CTA primario verde */
style={{
  background: 'linear-gradient(180deg, #00E676 0%, #00C853 50%, #004D25 100%)',
  border: '2px solid rgba(0,230,118,0.6)',
  borderRadius: '10px',
  padding: 'clamp(10px, 1.2vw, 16px) clamp(16px, 2.5vw, 32px)',
  color: '#FFFFFF',
  fontFamily: "'Cinzel', serif",
  fontWeight: 700,
  fontSize: 'clamp(12px, 1.5vw, 18px)',
  letterSpacing: 'clamp(1px, 0.2vw, 2px)',
  textTransform: 'uppercase',
  cursor: 'pointer',
  boxShadow: '0 4px 15px rgba(0,230,118,0.3), inset 0 1px 0 rgba(255,255,255,0.2)',
  position: 'relative',
  overflow: 'hidden',
  minWidth: '48px',
  minHeight: '48px',
}}
/* bevel ::before para efeito 3D no topo (DS P2 sec8.1) */
whileHover={{ scale: 1.05, boxShadow: '0 6px 25px rgba(0,230,118,0.5)' }}
whileTap={{ scale: 0.95 }}

/* ESTADO: Disabled (durante roll) */
style={{
  ...base,
  background: '#333333',
  borderColor: '#444444',
  color: '#666666',
  cursor: 'not-allowed',
  boxShadow: 'none',
  opacity: 0.5,
}}
```

### Historico Lateral (sidebar direita)
```css
/* Container do historico (lado direito, ~35% width) */
style={{
  flex: '1 1 35%',
  maxWidth: 'clamp(180px, 25vw, 300px)',
  background: 'rgba(0,0,0,0.4)',
  borderRadius: '14px',
  border: '1px solid rgba(212,168,67,0.2)',
  padding: 'clamp(6px, 0.8vw, 12px)',
  overflowY: 'auto',
  display: 'flex',
  flexDirection: 'column',
  gap: 'clamp(4px, 0.4vw, 6px)',
}}

/* Titulo "HISTORICO" */
style={{
  fontFamily: "'Cinzel', serif",
  fontWeight: 700,
  fontSize: 'clamp(10px, 1.2vw, 14px)',
  color: '#D4A843',
  textTransform: 'uppercase',
  letterSpacing: '2px',
  textAlign: 'center',
  paddingBottom: 'clamp(4px, 0.4vw, 6px)',
  borderBottom: '1px solid rgba(212,168,67,0.15)',
}}

/* Item do historico (1 rodada) */
style={{
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  padding: 'clamp(4px, 0.4vw, 6px)',
  borderRadius: '6px',
  background: 'rgba(0,0,0,0.2)',
  fontSize: 'clamp(9px, 1vw, 12px)',
}}
/* Resultado: se win, texto verde #00E676. Se loss, texto #666666 */
/* Se tinha multiplicador: badge pequeno dourado ao lado */
```

### Footer (botoes utilitarios)
```css
/* Barra de icones abaixo dos controles */
style={{
  display: 'flex',
  justifyContent: 'center',
  gap: 'clamp(12px, 2vw, 24px)',
  padding: 'clamp(4px, 0.5vw, 8px) 0',
}}

/* Botao icone (Historico, PF, Auto Bet, Som) — PNG dourado NAO Lucide */
style={{
  width: 'clamp(28px, 3.5vw, 40px)',
  height: 'clamp(28px, 3.5vw, 40px)',
  background: 'rgba(0,0,0,0.4)',
  border: '1px solid rgba(212,168,67,0.3)',
  borderRadius: '8px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  cursor: 'pointer',
  padding: 'clamp(4px, 0.5vw, 6px)',
  minWidth: '44px',
  minHeight: '44px',
}}
whileHover={{ borderColor: 'rgba(212,168,67,0.7)', background: 'rgba(212,168,67,0.1)' }}
whileTap={{ scale: 0.9 }}
/* Imagem icone dentro: filter brightness(0.8), hover: brightness(1.2) */
```

---

**[FIM DA PARTE 1 — Continua com Telas 2-4: Lightning Phase, Rolling/Landing, Result]**

`[ENTREGA: 3.PROMPT-AI-DICE-PARTE-1.md — 21/03/2026]`
